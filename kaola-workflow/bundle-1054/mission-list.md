# Mission — Issue #1054: the finalize Mission List readers count the four-field table form as well as the line form

| item | status | dispatched | result |
|---|---|---|---|
| Author independent acceptance: `probeMissionListCoherence` (claim.js, all four forge trees) and `countComplete`/`compareLedgers` (ledger-compare, all four copies) read the four-field markdown table AND the existing `item:`/`status:`/`result:` line form; regressions cover the real 7-row `kaola-workflow/archive/bundle-1053/mission-list.md` (items 7, flagged 0), unchanged line-form results, a non-done row with an empty `result` (not flagged) vs a non-done row carrying a `result` (flagged), header and dash-separator rows not counted, real Markdown cell boundaries (an escaped pipe inside a cell, a pipe inside backticks, multi-sentence cells), and ledger-compare UNSAFE when a table-form destination records fewer done rows; no new schema, format enforcement, or gate; RED on baseline `662bcd33` | done | tdd-guide → new suite (name theirs) under `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1054/scripts/`, registered on `test:kaola-workflow:claude` and `:claude:full`, plus any extension of `scripts/test-ledger-compare.js`; RED on baseline `662bcd33` in a temporary worktree; evidence to `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/kaola-workflow/bundle-1054/.cache/acceptance-red.md` | WITHDRAWN (owner scope change, issue #1054 correction comment 2026-09-09 09:01Z). Draft suite reached RED 16/7 on `662bcd33` and 46 green on the withdrawn implementation; kept as evidence only in `kaola-workflow/bundle-1054/.cache/withdrawn/` + `.cache/acceptance-red.md`, `.cache/acceptance-withdrawn.md`; package.json registration reverted; not merged. |
| Minimal repair of both readers in the canonical scripts and propagation to the Codex copy and the GitLab/Gitea hand-ports through the existing sync mechanisms, without weakening the acceptance | done | implementer → `scripts/kaola-workflow-claim.js` (probe), `scripts/kaola-workflow-ledger-compare.js` (countComplete), then `edition-sync.js --write` for the Codex copy and hand-ports of the same function into `plugins/kaola-workflow-{gitlab,gitea}/scripts/kaola-*-workflow-claim.js`, ledger copies via the byte-identical group, in `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1054`; must not edit tests; evidence to `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/kaola-workflow/bundle-1054/.cache/implementation.md` | WITHDRAWN (same scope change). Dual-carrier readers were implemented and measured (bundle-1053 table → 7/7; focused suites green) but not merged; full diff kept at `kaola-workflow/bundle-1054/.cache/withdrawn/dual-carrier-implementation.patch`, notes in `.cache/implementation-withdrawn.md`; worktree restored to `662bcd33` by the orchestrator (git status clean). |
| Trace the sync guard: every caller of `compareLedgers`/ledger-compare (finalize mirror, sink, forge ports), the copy direction and authority on each path, the historical regression it was written for (v5.14.0 / #837 era), and the existing tests; measure the real behavior on this run's own files; deliver a minimal content-based protection proposal (no parser, no state system, no deletion of protection) | done | investigator → read-only trace in `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1054` (may read the main root and archives); measurements and proposal to `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/kaola-workflow/bundle-1054/.cache/sync-guard-trace.md` | Trace at `kaola-workflow/bundle-1054/.cache/sync-guard-trace.md` (baseline `662bcd33` read via git show). Callers: only claim.js `mirrorFinalizationArtifacts` (write) and `probeFinalizeMirror`/`finalize --check` (read-only), all 4 editions; no sink-merge caller. SOURCE = main-root record, DEST = worktree copy; normal direction main→worktree; on UNSAFE the #837 repair copies worktree→main unconditionally ('worktree wins') — can overwrite a newer main record. Origin: 58ffd812 (#399, v5.14.0 incident: main→worktree cp clobbered a finished copy), re-pointed at `status: done` lines in c0b48043. Tests line-form only. Measured: countComplete(bundle-1053 table)=0; compareLedgers(3-rows-stripped source, full dest) = safe/ok 0/0 (false-safe on real data). Adopted proposal: byte-identical → safe; dest absent → safe; otherwise refuse fail-closed with a diff summary for the orchestrator; retire the divergent-case auto repair; no containment arm (fails on in-place status advance); no counting. |
| Author independent acceptance for the corrected scope: different Mission List layouts do not affect finalization; no false-zero statistics are written; the raw record and completed results are never overwritten by a staler copy across first-sync, repeat-execution, and conflict paths on every forge port; safety is never judged by task counts; RED on the baseline | done | tdd-guide (tdd-1054, re-briefed) → new suite under `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1054/scripts/` registered on both claude chains; part 1 (no parsing/counting/auto statistics; layouts irrelevant; no false zero) now, part 2 (record never overwritten by a staler copy: first-sync/repeat/conflict, every forge port, never by counts) after reading `.cache/sync-guard-trace.md`; RED on `662bcd33`; evidence `.cache/acceptance-red-v2.md` | Corrected-scope acceptance: `test-issue-1054-mission-list-carriers.js` (116; layouts irrelevant, no auto stats, goal read intact, 4 trees), `test-issue-1054-ledger-guard.js` (28; first-sync/identical/diverged-refusal with both sides byte-unchanged, count-proxy trap, both paths + diff, 4 ports, --check parity), `test-ledger-compare.js` rewritten (22), finalize-door T17 and claim-hardening T2a rewritten to preservation-by-refusal. RED on `662bcd33`: carriers 44/72, finalize-door 3, ledger-compare first-assert, ledger-guard ENOENT. Evidence: `.cache/acceptance-red-v2.md`. Candidate commit on branch (see row 5). |
| Remove finalize's Mission List parsing, counting, and auto `## Mission List` statistics in the canonical claim.js and every forge port; land the minimal content-based sync protection from the trace; propagate through the existing sync mechanisms without weakening the acceptance | done | implementer (impl-1054, re-briefed) → part 1 now: remove `probeMissionListCoherence`/`persistMissionListToSummary` and the auto `## Mission List` statistics from canonical claim.js, Codex copy (edition-sync), GitLab/Gitea ports; ledger-compare untouched until the trace lands, then the minimal content-based protection per my follow-up brief; evidence `.cache/implementation-v2.md` | Part 1 + Part 2 landed as candidate commit on `workflow/bundle-1054` (pushed, not merged): finalize no longer parses/counts the Mission List (4 trees); compareLedgers by content with bounded diff, countComplete removed, divergent auto-repair retired, refusal under `mirror_sync_failed` with both paths; edition-sync propagation + hand-ports; validate-script-sync/suite-registration/focused suites green. Direct measurements on real bundle-1053 record match. Evidence: `.cache/implementation-v2.md`. NOT done here (same issue, after joint review): finalize.skeleton `## Mission List` wording, docs/api.md `countComplete` text, run-gaps/backlog-delta retirement. |
| Documentation and design note: docs/api.md finalize report, docs/architecture.md and workflow-state-contract record description, CHANGELOG `[Unreleased]`, ADR 0017 note if the design record needs the correction; consistent for all forges/runtimes; no rewrite of historical done rows | todo | | |
| Independent review of the exact frozen candidate against the corrected issue scope and ADR 0017 | todo | | |
| Verification on the frozen candidate: focused suites, producer-selected `npm test`, full walkthrough, edition suites; re-verify after any repair; run record with evidence map, the withdrawn drafts, and the unexecuted list | todo | | |

Constraints: issue #1054 as rewritten on 2026-09-09 (owner-approved scope change; correction comment wins over the original text) is the authority. Rows 1–2 were dispatched under the original scope and are closed as withdrawn; their drafts are evidence only. Do not rewrite historical archived mission lists to mask the parser. No new schema, carrier enforcement, approval gate, or Mission List field. Worktree `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1054` on `workflow/bundle-1054`; evidence under `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/kaola-workflow/bundle-1054/.cache/`. No release/tag.

Scope update 2026-09-09 (owner-approved, forge is authority): issue #1054 body was rewritten to the joint one-shot plan (14 role entries redefined by unique deliverable, all 31 audit items in-issue, natural review reports with `final-validation.md` written only from executed validation, table-form run as live acceptance, source-generated carriers, per-carrier install checks with unavailable hosts = unknown). Local copies: `/Users/ylminiserver/Documents/Codex/2026-09-09/workflow-rule-audit/issue-1054-proposal.md` and `joint-design.md`; my review at `fable-joint-review.md` (§7 = final position). Candidate `6ae5374b` on `workflow/bundle-1054` (pushed, not merged) holds part 1+2; rows 6–8 remain todo and must be re-read against the rewritten issue before any dispatch. Run paused in a resumable state awaiting the user's next instruction; no finalize/sink/close.

In-repo copy of the joint design record (audit, research, corrections, proposal, forge update evidence): `kaola-workflow/bundle-1054/.cache/joint-design/INDEX.md`. The external working copy at `/Users/ylminiserver/Documents/Codex/2026-09-09/workflow-rule-audit/` remains the editing entry; issue #1054 on the forge is the scope authority.
