diff --git a/plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js b/plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js
index 454e4e03..597e4b0e 100644
--- a/plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js
+++ b/plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js
@@ -4024,7 +4024,7 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 //     correction UNDER the stale line, never over it; reading the first reports ten of them whose
 //     author wrote `done` directly beneath.
 //   - A field is a NAME AT THE FRONT of a line — at the bullet, at two spaces, or at column zero,
-//     the three forms the archive uses. Never a substring: `result` prose quotes the record's own
+//     the forms the archive uses. Never a substring: `result` prose quotes the record's own
 //     vocabulary ("reads status: done") and `dispatched` prose says "the result:", and a scan
 //     anywhere in the line flips both verdicts.
 // An outcome is a field whose name STARTS with `result` — the plain key and the decorations the
@@ -4034,9 +4034,42 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 // outcome, so counting the key alone would tell a successor that something landed where nothing did
 // — the same wrongness this report exists to catch, arriving through the report. Nothing after the
 // colon and whitespace after the colon are one case; only `\S` tells either from a real value.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE (`| item | status |
+// dispatched | result |`), not the line form above — the archive's older records are line-form, and
+// current ones are table rows, and this probe reads both carriers with the same LAST-status-wins,
+// non-empty-outcome semantics. A table row is split into cells on unescaped `|` (a `\|` or a `|`
+// inside a backtick span is content, not a delimiter — a `result` cell legitimately quotes
+// `` `|---|` `` or `` `todo` \| `in-flight` ``); the header row (first cell `item`, case-insensitive)
+// and the `|---|---|` separator row are skipped, never counted as items.
 const MISSION_ITEM_LINE = /^(?:- )?item:/;
 const MISSION_STATUS_LINE = /^(?:- |  )?status:[ \t]*([A-Za-z][A-Za-z-]*)/;
 const MISSION_RESULT_LINE = /^(?:- |  )?result\b[^:]*:[ \t]*\S/;
+// Split a `|`-delimited table row into trimmed cells, honoring `\|` and backtick-span pipes as
+// content rather than delimiters. Returns null when the line is not a table row at all.
+function splitMissionTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  // A well-formed row starts and ends with `|`, so the first and last split cells are the
+  // (empty) text outside the leading/trailing delimiter; drop them.
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isMissionTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function probeMissionListCoherence(authorityDir) {
   let text;
   // A run with no record measures nothing and says nothing: the mission list is a convention, not
@@ -4046,6 +4079,15 @@ function probeMissionListCoherence(authorityDir) {
   const items = [];
   let cur = null;
   text.split('\n').forEach((line, i) => {
+    const cells = splitMissionTableRow(line);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') return; // header row
+      if (isMissionTableSeparatorRow(cells)) return; // `|---|---|---|---|`
+      const status = cells[1] ? cells[1].toLowerCase() : null;
+      const outcome = /\S/.test(cells[3] || '');
+      items.push({ line: i + 1, status: status || null, outcome });
+      return;
+    }
     if (MISSION_ITEM_LINE.test(line)) { cur = { line: i + 1, status: null, outcome: false }; items.push(cur); return; }
     if (!cur) return;
     const status = line.match(MISSION_STATUS_LINE);
@@ -6659,6 +6701,9 @@ module.exports = {
   mirrorFinalizationArtifacts,
   probeImplementationCommit,
   checkFinalizeStagingGuard,
+  // #1054: exported for direct unit coverage of the dual-carrier (line-form + table-row) mission
+  // list reading.
+  probeMissionListCoherence,
   appendClosureBlock,
   // #715 F1: exported for direct unit coverage (restore-gate dest exemption + base-branch guard).
   treeDirty,
diff --git a/plugins/kaola-workflow-gitea/scripts/kaola-workflow-ledger-compare.js b/plugins/kaola-workflow-gitea/scripts/kaola-workflow-ledger-compare.js
index 34d7b979..e6061c23 100644
--- a/plugins/kaola-workflow-gitea/scripts/kaola-workflow-ledger-compare.js
+++ b/plugins/kaola-workflow-gitea/scripts/kaola-workflow-ledger-compare.js
@@ -29,11 +29,44 @@ const fs = require('fs');
 // relies on for the legitimate first sync. It deliberately does NOT try to identify WHICH items
 // are done: the guard's question is comparative ("does the destination know about more finished
 // work than the source?"), and a count answers it without inventing a schema for a free-text field.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE
+// (`| item | status | dispatched | result |`), not the `status: done` line form above, so this also
+// counts table rows whose second cell is `done` (case-insensitive). A row is split on unescaped `|`
+// (a `\|` or a `|` inside a backtick span is content, not a delimiter); the header row and the
+// `|---|---|---|---|` separator row are skipped.
+function splitLedgerTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isLedgerTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function countComplete(missionListText) {
   if (typeof missionListText !== 'string' || missionListText.length === 0) return 0;
   let count = 0;
   for (const raw of missionListText.split('\n')) {
-    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) count++;
+    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) { count++; continue; }
+    const cells = splitLedgerTableRow(raw);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') continue; // header row
+      if (isLedgerTableSeparatorRow(cells)) continue; // `|---|---|---|---|`
+      if ((cells[1] || '').toLowerCase() === 'done') count++;
+    }
   }
   return count;
 }
diff --git a/plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js b/plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js
index c903db65..eeaff184 100644
--- a/plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js
+++ b/plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js
@@ -4027,7 +4027,7 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 //     correction UNDER the stale line, never over it; reading the first reports ten of them whose
 //     author wrote `done` directly beneath.
 //   - A field is a NAME AT THE FRONT of a line — at the bullet, at two spaces, or at column zero,
-//     the three forms the archive uses. Never a substring: `result` prose quotes the record's own
+//     the forms the archive uses. Never a substring: `result` prose quotes the record's own
 //     vocabulary ("reads status: done") and `dispatched` prose says "the result:", and a scan
 //     anywhere in the line flips both verdicts.
 // An outcome is a field whose name STARTS with `result` — the plain key and the decorations the
@@ -4037,9 +4037,42 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 // outcome, so counting the key alone would tell a successor that something landed where nothing did
 // — the same wrongness this report exists to catch, arriving through the report. Nothing after the
 // colon and whitespace after the colon are one case; only `\S` tells either from a real value.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE (`| item | status |
+// dispatched | result |`), not the line form above — the archive's older records are line-form, and
+// current ones are table rows, and this probe reads both carriers with the same LAST-status-wins,
+// non-empty-outcome semantics. A table row is split into cells on unescaped `|` (a `\|` or a `|`
+// inside a backtick span is content, not a delimiter — a `result` cell legitimately quotes
+// `` `|---|` `` or `` `todo` \| `in-flight` ``); the header row (first cell `item`, case-insensitive)
+// and the `|---|---|` separator row are skipped, never counted as items.
 const MISSION_ITEM_LINE = /^(?:- )?item:/;
 const MISSION_STATUS_LINE = /^(?:- |  )?status:[ \t]*([A-Za-z][A-Za-z-]*)/;
 const MISSION_RESULT_LINE = /^(?:- |  )?result\b[^:]*:[ \t]*\S/;
+// Split a `|`-delimited table row into trimmed cells, honoring `\|` and backtick-span pipes as
+// content rather than delimiters. Returns null when the line is not a table row at all.
+function splitMissionTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  // A well-formed row starts and ends with `|`, so the first and last split cells are the
+  // (empty) text outside the leading/trailing delimiter; drop them.
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isMissionTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function probeMissionListCoherence(authorityDir) {
   let text;
   // A run with no record measures nothing and says nothing: the mission list is a convention, not
@@ -4049,6 +4082,15 @@ function probeMissionListCoherence(authorityDir) {
   const items = [];
   let cur = null;
   text.split('\n').forEach((line, i) => {
+    const cells = splitMissionTableRow(line);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') return; // header row
+      if (isMissionTableSeparatorRow(cells)) return; // `|---|---|---|---|`
+      const status = cells[1] ? cells[1].toLowerCase() : null;
+      const outcome = /\S/.test(cells[3] || '');
+      items.push({ line: i + 1, status: status || null, outcome });
+      return;
+    }
     if (MISSION_ITEM_LINE.test(line)) { cur = { line: i + 1, status: null, outcome: false }; items.push(cur); return; }
     if (!cur) return;
     const status = line.match(MISSION_STATUS_LINE);
@@ -6666,6 +6708,9 @@ module.exports = {
   mirrorFinalizationArtifacts,
   probeImplementationCommit,
   checkFinalizeStagingGuard,
+  // #1054: exported for direct unit coverage of the dual-carrier (line-form + table-row) mission
+  // list reading.
+  probeMissionListCoherence,
   appendClosureBlock,
   // #715 F1: exported for direct unit coverage (restore-gate dest exemption + base-branch guard).
   treeDirty,
diff --git a/plugins/kaola-workflow-gitlab/scripts/kaola-workflow-ledger-compare.js b/plugins/kaola-workflow-gitlab/scripts/kaola-workflow-ledger-compare.js
index 34d7b979..e6061c23 100644
--- a/plugins/kaola-workflow-gitlab/scripts/kaola-workflow-ledger-compare.js
+++ b/plugins/kaola-workflow-gitlab/scripts/kaola-workflow-ledger-compare.js
@@ -29,11 +29,44 @@ const fs = require('fs');
 // relies on for the legitimate first sync. It deliberately does NOT try to identify WHICH items
 // are done: the guard's question is comparative ("does the destination know about more finished
 // work than the source?"), and a count answers it without inventing a schema for a free-text field.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE
+// (`| item | status | dispatched | result |`), not the `status: done` line form above, so this also
+// counts table rows whose second cell is `done` (case-insensitive). A row is split on unescaped `|`
+// (a `\|` or a `|` inside a backtick span is content, not a delimiter); the header row and the
+// `|---|---|---|---|` separator row are skipped.
+function splitLedgerTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isLedgerTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function countComplete(missionListText) {
   if (typeof missionListText !== 'string' || missionListText.length === 0) return 0;
   let count = 0;
   for (const raw of missionListText.split('\n')) {
-    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) count++;
+    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) { count++; continue; }
+    const cells = splitLedgerTableRow(raw);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') continue; // header row
+      if (isLedgerTableSeparatorRow(cells)) continue; // `|---|---|---|---|`
+      if ((cells[1] || '').toLowerCase() === 'done') count++;
+    }
   }
   return count;
 }
diff --git a/plugins/kaola-workflow/scripts/kaola-workflow-claim.js b/plugins/kaola-workflow/scripts/kaola-workflow-claim.js
index 69b104a1..a9da9528 100644
--- a/plugins/kaola-workflow/scripts/kaola-workflow-claim.js
+++ b/plugins/kaola-workflow/scripts/kaola-workflow-claim.js
@@ -4229,7 +4229,7 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 //     correction UNDER the stale line, never over it; reading the first reports ten of them whose
 //     author wrote `done` directly beneath.
 //   - A field is a NAME AT THE FRONT of a line — at the bullet, at two spaces, or at column zero,
-//     the three forms the archive uses. Never a substring: `result` prose quotes the record's own
+//     the forms the archive uses. Never a substring: `result` prose quotes the record's own
 //     vocabulary ("reads status: done") and `dispatched` prose says "the result:", and a scan
 //     anywhere in the line flips both verdicts.
 // An outcome is a field whose name STARTS with `result` — the plain key and the decorations the
@@ -4239,9 +4239,42 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 // outcome, so counting the key alone would tell a successor that something landed where nothing did
 // — the same wrongness this report exists to catch, arriving through the report. Nothing after the
 // colon and whitespace after the colon are one case; only `\S` tells either from a real value.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE (`| item | status |
+// dispatched | result |`), not the line form above — the archive's older records are line-form, and
+// current ones are table rows, and this probe reads both carriers with the same LAST-status-wins,
+// non-empty-outcome semantics. A table row is split into cells on unescaped `|` (a `\|` or a `|`
+// inside a backtick span is content, not a delimiter — a `result` cell legitimately quotes
+// `` `|---|` `` or `` `todo` \| `in-flight` ``); the header row (first cell `item`, case-insensitive)
+// and the `|---|---|` separator row are skipped, never counted as items.
 const MISSION_ITEM_LINE = /^(?:- )?item:/;
 const MISSION_STATUS_LINE = /^(?:- |  )?status:[ \t]*([A-Za-z][A-Za-z-]*)/;
 const MISSION_RESULT_LINE = /^(?:- |  )?result\b[^:]*:[ \t]*\S/;
+// Split a `|`-delimited table row into trimmed cells, honoring `\|` and backtick-span pipes as
+// content rather than delimiters. Returns null when the line is not a table row at all.
+function splitMissionTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  // A well-formed row starts and ends with `|`, so the first and last split cells are the
+  // (empty) text outside the leading/trailing delimiter; drop them.
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isMissionTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function probeMissionListCoherence(authorityDir) {
   let text;
   // A run with no record measures nothing and says nothing: the mission list is a convention, not
@@ -4251,6 +4284,15 @@ function probeMissionListCoherence(authorityDir) {
   const items = [];
   let cur = null;
   text.split('\n').forEach((line, i) => {
+    const cells = splitMissionTableRow(line);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') return; // header row
+      if (isMissionTableSeparatorRow(cells)) return; // `|---|---|---|---|`
+      const status = cells[1] ? cells[1].toLowerCase() : null;
+      const outcome = /\S/.test(cells[3] || '');
+      items.push({ line: i + 1, status: status || null, outcome });
+      return;
+    }
     if (MISSION_ITEM_LINE.test(line)) { cur = { line: i + 1, status: null, outcome: false }; items.push(cur); return; }
     if (!cur) return;
     const status = line.match(MISSION_STATUS_LINE);
@@ -6961,6 +7003,9 @@ module.exports = {
   mirrorFinalizationArtifacts,
   probeImplementationCommit,
   checkFinalizeStagingGuard,
+  // #1054: exported for direct unit coverage of the dual-carrier (line-form + table-row) mission
+  // list reading.
+  probeMissionListCoherence,
   // #700: the terminal archive-metadata writer reused by sink-merge's SOLE-archiver finalize path.
   appendClosureBlock,
   // #715 F1: exported for direct unit coverage (restore-gate dest exemption + base-branch guard).
diff --git a/plugins/kaola-workflow/scripts/kaola-workflow-ledger-compare.js b/plugins/kaola-workflow/scripts/kaola-workflow-ledger-compare.js
index 34d7b979..e6061c23 100644
--- a/plugins/kaola-workflow/scripts/kaola-workflow-ledger-compare.js
+++ b/plugins/kaola-workflow/scripts/kaola-workflow-ledger-compare.js
@@ -29,11 +29,44 @@ const fs = require('fs');
 // relies on for the legitimate first sync. It deliberately does NOT try to identify WHICH items
 // are done: the guard's question is comparative ("does the destination know about more finished
 // work than the source?"), and a count answers it without inventing a schema for a free-text field.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE
+// (`| item | status | dispatched | result |`), not the `status: done` line form above, so this also
+// counts table rows whose second cell is `done` (case-insensitive). A row is split on unescaped `|`
+// (a `\|` or a `|` inside a backtick span is content, not a delimiter); the header row and the
+// `|---|---|---|---|` separator row are skipped.
+function splitLedgerTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isLedgerTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function countComplete(missionListText) {
   if (typeof missionListText !== 'string' || missionListText.length === 0) return 0;
   let count = 0;
   for (const raw of missionListText.split('\n')) {
-    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) count++;
+    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) { count++; continue; }
+    const cells = splitLedgerTableRow(raw);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') continue; // header row
+      if (isLedgerTableSeparatorRow(cells)) continue; // `|---|---|---|---|`
+      if ((cells[1] || '').toLowerCase() === 'done') count++;
+    }
   }
   return count;
 }
diff --git a/scripts/kaola-workflow-claim.js b/scripts/kaola-workflow-claim.js
index 69b104a1..a9da9528 100644
--- a/scripts/kaola-workflow-claim.js
+++ b/scripts/kaola-workflow-claim.js
@@ -4229,7 +4229,7 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 //     correction UNDER the stale line, never over it; reading the first reports ten of them whose
 //     author wrote `done` directly beneath.
 //   - A field is a NAME AT THE FRONT of a line — at the bullet, at two spaces, or at column zero,
-//     the three forms the archive uses. Never a substring: `result` prose quotes the record's own
+//     the forms the archive uses. Never a substring: `result` prose quotes the record's own
 //     vocabulary ("reads status: done") and `dispatched` prose says "the result:", and a scan
 //     anywhere in the line flips both verdicts.
 // An outcome is a field whose name STARTS with `result` — the plain key and the decorations the
@@ -4239,9 +4239,42 @@ function persistChangedPathsToSummary(projectDir, changed, probe) {
 // outcome, so counting the key alone would tell a successor that something landed where nothing did
 // — the same wrongness this report exists to catch, arriving through the report. Nothing after the
 // colon and whitespace after the colon are one case; only `\S` tells either from a real value.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE (`| item | status |
+// dispatched | result |`), not the line form above — the archive's older records are line-form, and
+// current ones are table rows, and this probe reads both carriers with the same LAST-status-wins,
+// non-empty-outcome semantics. A table row is split into cells on unescaped `|` (a `\|` or a `|`
+// inside a backtick span is content, not a delimiter — a `result` cell legitimately quotes
+// `` `|---|` `` or `` `todo` \| `in-flight` ``); the header row (first cell `item`, case-insensitive)
+// and the `|---|---|` separator row are skipped, never counted as items.
 const MISSION_ITEM_LINE = /^(?:- )?item:/;
 const MISSION_STATUS_LINE = /^(?:- |  )?status:[ \t]*([A-Za-z][A-Za-z-]*)/;
 const MISSION_RESULT_LINE = /^(?:- |  )?result\b[^:]*:[ \t]*\S/;
+// Split a `|`-delimited table row into trimmed cells, honoring `\|` and backtick-span pipes as
+// content rather than delimiters. Returns null when the line is not a table row at all.
+function splitMissionTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  // A well-formed row starts and ends with `|`, so the first and last split cells are the
+  // (empty) text outside the leading/trailing delimiter; drop them.
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isMissionTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function probeMissionListCoherence(authorityDir) {
   let text;
   // A run with no record measures nothing and says nothing: the mission list is a convention, not
@@ -4251,6 +4284,15 @@ function probeMissionListCoherence(authorityDir) {
   const items = [];
   let cur = null;
   text.split('\n').forEach((line, i) => {
+    const cells = splitMissionTableRow(line);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') return; // header row
+      if (isMissionTableSeparatorRow(cells)) return; // `|---|---|---|---|`
+      const status = cells[1] ? cells[1].toLowerCase() : null;
+      const outcome = /\S/.test(cells[3] || '');
+      items.push({ line: i + 1, status: status || null, outcome });
+      return;
+    }
     if (MISSION_ITEM_LINE.test(line)) { cur = { line: i + 1, status: null, outcome: false }; items.push(cur); return; }
     if (!cur) return;
     const status = line.match(MISSION_STATUS_LINE);
@@ -6961,6 +7003,9 @@ module.exports = {
   mirrorFinalizationArtifacts,
   probeImplementationCommit,
   checkFinalizeStagingGuard,
+  // #1054: exported for direct unit coverage of the dual-carrier (line-form + table-row) mission
+  // list reading.
+  probeMissionListCoherence,
   // #700: the terminal archive-metadata writer reused by sink-merge's SOLE-archiver finalize path.
   appendClosureBlock,
   // #715 F1: exported for direct unit coverage (restore-gate dest exemption + base-branch guard).
diff --git a/scripts/kaola-workflow-ledger-compare.js b/scripts/kaola-workflow-ledger-compare.js
index 34d7b979..e6061c23 100644
--- a/scripts/kaola-workflow-ledger-compare.js
+++ b/scripts/kaola-workflow-ledger-compare.js
@@ -29,11 +29,44 @@ const fs = require('fs');
 // relies on for the legitimate first sync. It deliberately does NOT try to identify WHICH items
 // are done: the guard's question is comparative ("does the destination know about more finished
 // work than the source?"), and a count answers it without inventing a schema for a free-text field.
+//
+// #1054: every current run writes the ADR-0017 four fields as a markdown TABLE
+// (`| item | status | dispatched | result |`), not the `status: done` line form above, so this also
+// counts table rows whose second cell is `done` (case-insensitive). A row is split on unescaped `|`
+// (a `\|` or a `|` inside a backtick span is content, not a delimiter); the header row and the
+// `|---|---|---|---|` separator row are skipped.
+function splitLedgerTableRow(line) {
+  const trimmed = line.trim();
+  if (!trimmed.startsWith('|')) return null;
+  const cells = [];
+  let cur = '';
+  let inTick = false;
+  for (let i = 0; i < trimmed.length; i++) {
+    const ch = trimmed[i];
+    if (ch === '`') { inTick = !inTick; cur += ch; continue; }
+    if (ch === '\\' && trimmed[i + 1] === '|') { cur += '|'; i++; continue; }
+    if (ch === '|' && !inTick) { cells.push(cur); cur = ''; continue; }
+    cur += ch;
+  }
+  cells.push(cur);
+  if (cells.length && cells[0].trim() === '') cells.shift();
+  if (cells.length && cells[cells.length - 1].trim() === '') cells.pop();
+  return cells.map(c => c.trim());
+}
+function isLedgerTableSeparatorRow(cells) {
+  return cells.every(c => /^:?-+:?$/.test(c) || c === '');
+}
 function countComplete(missionListText) {
   if (typeof missionListText !== 'string' || missionListText.length === 0) return 0;
   let count = 0;
   for (const raw of missionListText.split('\n')) {
-    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) count++;
+    if (/^[ \t]*(?:-[ \t]+)?status:[ \t]*done[ \t]*$/i.test(raw)) { count++; continue; }
+    const cells = splitLedgerTableRow(raw);
+    if (cells !== null && cells.length >= 4) {
+      if (cells[0].toLowerCase() === 'item') continue; // header row
+      if (isLedgerTableSeparatorRow(cells)) continue; // `|---|---|---|---|`
+      if ((cells[1] || '').toLowerCase() === 'done') count++;
+    }
   }
   return count;
 }
