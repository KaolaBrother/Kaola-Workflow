issue: #935
title: Unify the subagent tier classification across runtimes, then pin Codex role profiles (standard = Luna/max, reasoning = Sol/xhigh)
status: open
workflow_project: issue-935
next_step: Land Stage C FIRST and watch it fail — add a contract assertion that CODEX_PINNED_REASONING_ROLES equals exactly the roles whose DEFAULT_AGENT_MODELS value is reasoning-class (complement for the standard list), run it against the current tree, and record the failure naming build-error-resolver and adversarial-verifier; a guard added after the fix cannot show it was ever armed. Then Stage A promotes those two to the reasoning tier — adversarial-verifier via the reviewer GENERATOR's adapter (its Claude file is generated and hash-bound; never hand-edit), build-error-resolver by hand, DEFAULT_AGENT_MODELS across the canonical resolver + 3 edition copies, rewrite the resolver comment that argues adversarial-verifier is standard, resolve install.sh's stale default_agent_model() fallback (measured unreachable — verify the vendored-agent model: assert covers all 14, then delete), and REGENERATE opencode rather than editing it (its sync derives the tier from the canonical model value). Then Stage B inverts the Codex profile rule: standard pair becomes gpt-5.6-luna/max, profiles are REQUIRED to carry their class pair, unpinned becomes the stale posture that migrates to pinned, the two routing skeletons stop naming any model/effort and the 6 Codex skills regenerate from them, and D-687-01 records a real reversal. Owner-ruled, do not re-litigate: Luna/max is the standard tier (benchmark evidence held by the owner), the two roles resolve UPWARD to reasoning, and Claude Code changes only its default model. Kimi is exempt by capability (no tier axis) and needs no change. Verification owes all four chains plus a live post-install Codex spawn reading effective model/effort — measured evidence and the reusable probe recipe are in the issue body.
