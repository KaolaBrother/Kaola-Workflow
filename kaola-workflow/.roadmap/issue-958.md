issue: #958
title: docs/architecture.md says the additive editions are not wired into the routing-surface propagation set — true of the render targets, false of the derivation
status: open
workflow_project: issue-958
next_step: From the #952 audit (D9); one line, ranked third by value in the whole audit because the cost is behavioural. docs/architecture.md:295-297. Three of its four sub-claims hold (absent from npm test, edition-sync.js, install.sh); the fourth does not — the generator writes 18 surfaces and neither .opencode nor .kimi is among them, so the doc is right about RENDER TARGETS, but both sync scripts DERIVE their command list from that registry. The code says the precise thing ("the six routing surfaces"); the doc generalized it to "the propagation set". Operationally proven for opencode - --check clean at exit 0, append one comment line to commands/workflow-next.md, --check exits 1 "stale — regenerate"; negative control, commandSources returns 0 for classifier.js and sink-merge.js. Why it matters - a reader who believes the sentence edits a routing skeleton and never regenerates the opencode tree. Honest limit carried from the audit - the kimi leg of that A/B has NO signal (.kimi untracked, red in both legs), so kimi's coupling rests on the weaker direct listCanonCommands() measurement.
