issue: #759
title: feat(adaptive-node): expansion transaction — open-time frontier composition recorded append-only in the ledger, resume-safe
status: ATTEMPTED AND REJECTED 2026-07-23 (adversarial pass, 23/30 mutants killed, 4 blocking findings) — branch wave-a-759 NOT merged; rework required before Wave B can start, because #761 consumes this shape
workflow_project: —
next_step: New adaptive-node subcommand expand-open: takes units[] {role, tier, write surfaces, mode serial|co_open, dependency edges} + the derivation lines (grain/path/join/probe/serializer), appends an EXPANSION RECORD (append-only, monotonically numbered per point) to the ledger, then opens via the EXISTING running-set scheduler. Guard prologue applies. Crash between append and open reconciles via reconcile-running-set roll-forward (idempotent). next-action.js ready-set derivation reads spine + expansion records. Review journal rebinds to (spine plan_hash, expansion id) so re-expansion never orphans a completed journal. Re-expansion is the SAME transaction — no separate machinery. DELTA 2026-07-22: the six workflow-init surfaces are IN SCOPE (their generated CLAUDE.md describes freeze + Node Ledger + resume-reads-the-ledger) plus docs/workflow-state-contract.md and the resume/frontier-batch plan-run cards; model-tier resolution moves from next-action.js plan-lookup to units carrying tier at open time — name the single resolver owner. REWORK REQUIRED 2026-07-23 — four blocking findings, all reproduced end to end on a clean tree; the attempt is on branch wave-a-759 and is worth reworking rather than discarding (AC4 four chains genuinely green, AC5 spine validation-policy fail-close well built, the transaction skeleton is sound). B1 HARD WEDGE: a composed code-reviewer unit is a LEGAL composition (validateComposition reserves only finalize / main-session-gate / expansion-point) but OPENS through the freeze view with a schema-1 dispatch (no contract_version, no review_context_hash) and CLOSES through the execution view's schema-2 review path — close-node refuses review_context_mismatch permanently, expand-close refuses scheduler_active, reconcile-running-set no-ops; one live node, no ready work, no legal transition. The byte-identical SPINE reviewer in the same plan closes fine. Fix is compose-time (reserve the gate roles) or widen the review-context seam. B2 AC2 HALF-DONE: the Phase-2-to-3 crash sub-window (frontier opened, open() proof block missing) is NOT repaired — reconcile-running-set returns not_opening and never restores the proof, while expand-open/expand-close refuse expansion_open_incomplete carrying repair: 'run reconcile-running-set', an operator instruction that is a NO-OP in exactly the state it is emitted for. The reconciled-path arm sits BELOW the not_opening early return, so it is unreachable for a stable running set. B3 TEST INSUFFICIENCY on the load-bearing crash-repair logic: 5 of 7 survivors re-confirmed against the FULL walkthrough (231 scenarios) AND full test-adaptive-node (2932 assertions) — expand-close's ENTIRE guard prologue is unpinned (integrity, consent-halt fence, scheduler/serial exclusion all removable invisibly), expand-open's integrity + consent-halt fence are unpinned (only excl is pinned), the reconciled-path roll-forward arm is dead code in every test, and AC2's idempotence assertion passes via an unrelated not_opening early return rather than the guard it claims to pin. B4: AC1's close->sink tail is SIMULATED by literal ledger string replacement (simulate-workflow-walkthrough.js content.replace of pending->complete), so no test runs the real close lifecycle of a schema-2 review gate downstream of an expanded milestone — which is precisely why B1 was not caught. BLAST RADIUS is bounded: no shipped surface authors plan_form: spine or calls expand-open, so none of this is reachable from a real run today — that bounds urgency, not correctness. DESCOPE NOTE CORRECTION: the producer's claim that composed gate units 'fail CLOSED — simply invisible' is factually wrong (see B1); the corrected item moved back here from #762.
