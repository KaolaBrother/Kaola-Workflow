issue: #755
title: bug(claim): abandon authority downgrade masks non-downgradable faults behind a first-failure short-circuit
status: ready — filed 2026-07-22 from adversarial verification of the #735 abandon carve-out; proven with 5 probes, NOT a work-loss defect
workflow_project: —
next_step: verifyCurrentEpochAuthority returns on FIRST failure and the snapshot tier only runs when the current-epoch tier passed, so an abandon whose first failure is downgradable skips the rest of the ladder. Proven: staging residue alone refuses snapshot_staging_incomplete, but residue + missing compliance archives the folder, removes the worktree and DELETES the branch; same for a corrupt replan transaction and a symlinked .cache/epochs. FIX: collect ALL failures and downgrade only if EVERY one is in the downgradable set. Also cover cmdWatchPr's PR-CLOSED sweep (a second, AUTOMATIC caller the operator-initiated consent argument does not cover; mitigations hold but untested with a downgradable window), and pin the emitted warnings[] entry rather than the result-spread field. Not work-loss: a healthy abandon already git branch -D's an unmerged branch and the archive preserves all state intact.
