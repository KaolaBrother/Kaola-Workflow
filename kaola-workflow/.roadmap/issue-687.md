issue: #687
title: enhancement(codex): subagent dispatch inherits the main session model and reasoning effort — reasoning/standard tier vocabulary retained as declarative prose
status: open
workflow_project: —
next_step: User directive 2026-07-14: current Codex cannot guarantee subagent quality below the main session's strength — every Codex subagent must inherit the main session model + reasoning effort. Keep reasoning/standard tier tokens everywhere (plan grammar, dispatch cards, wait budgets, profile prose) as declarative runtime-aligned metadata. Surfaces: TOML profile pins drop to inherit (prove via child-session probe), preflight/installer enforce + autofix the inherit posture, 3 Codex SKILL packs move to a single inheritance proof, REASONING_FLOOR_ROLES gets an explicit tested resolution, profile-parity + contract validators updated; Claude Code/opencode untouched; four-chain. Full body on GitHub #687.
