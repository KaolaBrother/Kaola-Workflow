issue: #968
title: workflow-next defaults a run to one issue, and gates widening on a test that selects against parallelism
status: open
workflow_project: issue-968
next_step: Prose-only change to templates/routing/next.skeleton.md:58-60 and :268, rendering to 8 consumer surfaces (6 via generate-routing-surfaces, plus .opencode/command/workflow-next.md:47 and .kimi/skills/workflow-next/SKILL.md:48) with README.md:883-884 and :1298 restating it by hand. Nothing in the machinery caps bundle width — claimExplicitBundle (scripts/kaola-workflow-claim.js:1886-1911) retired KAOLA_BUNDLE_MAX_ISSUES with its enforcement and BUNDLE_SIZE_ADVISORY = 8 is a non-blocking note — so the limiter is the sentence "A run normally carries one issue" plus an admission test, "share a coherent scope", that admits the bundles which parallelize worst. Measured over the last 26 archived runs: median 1 issue, mean 2.6, 14 of 26 singles, with the two most recent singles supply-limited rather than policy-limited. Proposal is four prose items: default of three to five, admission on independent closure rather than sameness, a runs-alone test keyed on blast radius rather than diff size, and filing follow-ups as independent slices to keep the next bundle wide. ANSWER THE VALUE CALL FIRST — item 2 partially reverses 328's stated same-scope intent and is a judgement about what a run is, not a fact settleable from the tree. Do not build a per-member closure valve: keep-open is whole-set (claim.js:4911, kept_open uses issueSet) and that cost has never been observed, so it belongs on ADR 0017's watch list with the row drafted in the issue body. Edition-touching prose, so it carries the four-chain receipt at finalize; no script changes.
