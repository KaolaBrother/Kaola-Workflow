issue: #756
title: bug(finalize/reopen): finalize done-gate surfacing a required file outside a completed writer's frozen write set wedges a PASSED-review serial run (reopen strands, amend orphans journal, replan needs a failed attempt)
status: queued — epic #757 WAVE D; acceptance HARNESS for #761 + #762, not an independent fix
workflow_project: —
next_step: RESHAPED 2026-07-22 under epic #757 — this issue is now the ACCEPTANCE HARNESS for #761 (local re-expansion: removes dead-ends 3/4/6, no failed-attempt prerequisite) + #762 (declared-not-walled surfaces: removes dead-ends 1/2/5); do NOT batch with the #745 reopen-remedy half (superseded by #761). Of the three candidate directions, direction 1 was chosen and generalized; direction 2 (tail-first cascade) is unnecessary (reopen stays rollback-only); direction 3 (completeness lint) is downgraded to an optional planner aid. A run whose review PASSED wedges at finalize when a project done-gate (not modeled as a DAG node) surfaces a required code-relevant file outside every frozen write set: barrier-check refuses write_set_overflow, verdict-check refuses verdict_not_pass (the code-relevant file stales the certifier candidate; docs correctly do not), reopen-node on the writer refuses would_strand_completed_dependent, on the gate refuses would_orphan_in_progress then would_strand, write-set amend + freeze --repair refuses review_journal_plan_hash_mismatch, and replan refuses replan_source_journal_missing because it requires a FAILED-review attempt a passed review never produces. Three candidate directions in the issue: (1) a sanctioned expand-and-re-review primitive that does not require a prior failed attempt, (2) reopen-node tail-first cascade instead of a hard would_strand refusal, (3) a planner companion-file completeness lint. Acceptance requires the recovery keep the new file REVIEWED, not merely attributed. WAVE C CARRY-FORWARD (from #762, landed 2026-07-23): #762 shipped the BARRIER half — an attributable out-of-surface companion file now appends an append-only exact-file `amend(<point>)` block, clears the whole-plan write_set_overflow barrier once its owning point is complete, and ROUTES to re-review via #761 reexpand-open (wall→pending, gate-verify obligation raised). What #762 explicitly did NOT do and handed here: COMPLETING the wall's in-plan re-review after re-open still hits `review_repair_delta_unavailable` (settled-pass attempt, no fold boundary to derive a repair delta from) — the same epoch-transition / repair-delta problem this harness must exercise. So the harness must prove the recovered file ends REVIEWED (not merely attributed): amend-surface → reexpand-open re-opens the owning milestone's writer+wall, and the wall re-discharge must produce a real review verdict, which needs the settled-pass fold boundary resolved. Acceptance = #756 dead-ends 1/2/5 gone (barrier, via #762) AND 3/4/6 gone (re-open, via #761) AND the wall actually re-reviews to PASS in-plan. MINOR RIDER (from #763 MPfused, non-blocking): the fused-advance (close-and-open-next) dispatch site's `context_packet` attach lacks a direct coverage test — shipped code verified correct by the #763 verifier, only the assertion is missing; fold a one-line dispatch-card assertion into this harness while it exercises the fused-advance path.
