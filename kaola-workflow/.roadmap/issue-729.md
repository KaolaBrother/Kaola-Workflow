issue: #729
title: bug(replan): empty or uncovered failed-review findings can activate a review-only child epoch
status: PARTIALLY SHIPPED — 4 of 9 acceptance criteria met (case (a) empty-frontier refusal 8370e577; AC2 finding frontier in the planner packet 7f2340c3); IN FLIGHT as WAVE A of epic #757
workflow_project: —
next_step: SHIPPED (case a): reviewSourceCarriesFrontier + typed refusal replan_source_findings_missing, bound at every reachable frontier (source read, authority verification, planner packet, stored-transaction entry at both phases, candidate re-author); 8/8 mutants killed; recovery row added to the repair-routing card. SHIPPED (AC2): the source finding frontier is carried into the planner packet, indexed under packet.source.finding_index — the key was renamed from an earlier colliding form rather than editing an agent surface; verified across 48 mutants with 40 killed and 8 low/non-blocking survivors. TWO LOAD-BEARING SCOPING CALLS to preserve in any follow-up — the guard is scoped to authority_kind review_outcome because diagnosis_to_build legitimately carries findings [] (pinned by a killed mutant), and the predicate is non-empty FINDINGS not non-empty OPEN frontier, because a progress-only failure legitimately carries an all-resolved frontier whose record of what failed is intact. REMAINING (5 of 9 ACs): AC3 child carry-forward reporting uncovered uid/path/node; AC4 review-only child cannot activate for an unresolved path-based finding; AC5 correctly mapped writer-to-certifier still activates; AC6 N-1 of N coverage refuses; AC7 crash-prefix/idempotent retry at the new coverage seam. FAIL-DIRECTION WARNING for the remaining work: this invariant asks whether a source packet is SAFE to spend an epoch on, so it must answer NO unless the packet is PROVABLY covered — absence of a field is the corruption signature it exists to catch, never an implicit pass. Do NOT reuse unresolvedInScopeFixes (the fail-OPEN gate predicate); adaptive-schema.js now also carries repairResponsibleFindings (fail-CLOSED); read both comments before choosing, and write a third predicate if neither answers this question. Case (b) SEMANTIC COVERAGE deliberately unimplemented with the soundness argument recorded inline: every mechanical proxy at this seam is unsound (an anchor records where a defect was OBSERVED and may legally carry no path; fix_role is advisory not contractual; "child has any writer" does not even catch the live repro). A sound check needs the child plan to DECLARE which inherited finding uids each writer owns — a plan-grammar addition in the validator; AC2 was its prerequisite and has now landed. Minor: repairCandidateReauthorPrefix performs durable writes before the guard on one resume path (epoch is NOT consumed and phase does not advance, so AC1 holds, but "zero mutation" is not literally true there). SEQUENCING — co-opens with #759 in Wave A on named evidence: this work is confined to replan.js while #759 is confined to adaptive-node.js and next-action.js. EPIC NOTE (#757): the finding-frontier planner packet is the brief substrate for #761 — the shape is already consumable outside the replan seam, so #761 is not blocked on the remaining ACs. HARDENING BEFORE MERGE (Wave A, from the adversarial pass — verdict ACCEPT with 14/21 mutants killed, blocking=[]): two survivors are genuine coverage gaps rather than defence-in-depth and are closed IN THIS WAVE, because #761 rewrites replan.js in Wave B and the tests get much more expensive to add after the file moves. (1) M7 — duplicate finding_owners: meta lines are entirely untested, although refusing a duplicate is the stated reason metaFieldOccurrences exists; the existing check('F1=w,,F1=w') case pins an empty token inside ONE line, not a repeated line. (2) M18/M19 — the coverage wall is pinned only in AGGREGATE: the early pre-freeze check in resumeReplanUnlocked and the call in validateChildPlan each survive individual removal, so neither placement is independently regression-protected and the documented ordering property (docs/api.md: the verdict is taken on the attested image before any child byte is written) has no assertion. The remaining survivors (M13/M13b certifier reachability and every/some, M17 source-identity guard, M20 unresolvable-certifier) are accepted as defence-in-depth on the verifier's reasoning: each is unreachable through the canonical lane, and M13b was proven independently refused by the validator G1 rule before the coverage wall runs. NOTE the verifier CONTRADICTED the implementer here — the implementer claimed M6 certifier-reachability-removed was KILLED and it was not.
