issue: #758
title: feat(plan-validator): spine grammar — freeze a coarse milestone spine with typed expansion points; retire interior whole-plan shape proofs
status: queued — epic #757 child 1
workflow_project: —
next_step: plan-validator gains the SPINE shape: ordered milestones + unique sink; nodes are concrete single-role OR expansion_point (frontier composed at open time). Spine-level proofs stay global (unique sink, sink post-dominance on the spine, reviewed-before-sink wall, closed library, caps, governance); interior post-dominance/antichain proofs are NOT applied to expansion points. Each expansion_point declares milestone goal, coarse ADVISORY expected surfaces, join constraints, and its review obligation class. plan_hash covers the spine only. Dual-accept legacy plans, regression-pinned. Known seam: review-journal binding moves to (spine hash, expansion id) in #759 — coordinate, don't solve here. DELTA 2026-07-22: write set must also carry adaptive-handoff.js (spine freeze flows through it), agents/workflow-planner.md + the three T17-byte-matched tomls (canonical = github toml), the six adapt-topic surfaces, docs/api.md, docs/workflow-state-contract.md.
