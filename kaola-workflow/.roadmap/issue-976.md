issue: #976
title: The relative-TMPDIR escape survives at seven more installer sites outside #975's enumerated set
status: open
workflow_project: bundle-976-977-978
next_step: Measured by the adversarial review of #975 during the bundle-973-974-975 run, not derived. #975 half B closed the escape where a fixture root resolves against the current directory, at two Node sites (scripts/test-install-all.js:175, :137) and three shell sites (install-all.sh:233, :303, :339). The same property survives at seven more sites, all outside the set that issue enumerated — install.sh:24, :399, :427, :433; install-opencode.sh:305, :309, :600; install-kimi.sh:322. The property, stated precisely, because two plausible fixes were measured and both are wrong — it is not "make the root absolute", since realpathSync(mkdtempSync(...)) absolutises the returned string after the directory already exists and path.resolve(".") IS the current directory; the property is "does not resolve against the current directory". Why it is invisible today: the sandbox suite added in #975 (scripts/test-fixture-sandbox.js) drives test-install-all.js and watches the checkout root live, because the escaping directories self-delete at cleanup and no before/after git status can see them, but it is stub-driven and so does not reach these seven sites. Under a relative TMPDIR the #975 measurement put 81 fixture roots into a checkout root, each containing plugins/kaola-workflow/. Bounded honestly: nothing in this repository sets a relative TMPDIR and the reviewer had to induce it by hand, and run-chains.js:326-333 already immunises chain children by absolutising before handing TMPDIR down, so the exposure is a bare node scripts/... or a direct installer invocation. SPECIFY THE RESULT: no fixture or installer temporary root is created inside the working checkout, whatever TMPDIR holds — whether that is one shared helper, a per-site guard, or an assertion at creation is the implementer's call, but note the two idioms above are measured NOT to reach it.
