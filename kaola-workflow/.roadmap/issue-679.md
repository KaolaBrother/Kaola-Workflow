issue: #679
title: fix(scripts): gap-sweep explicit --output at a same-named leftover archive still clobbers its run-gaps.json (post-#675 edge)
status: open
workflow_project: —
next_step: Surfaced by the #675/#676 bundle adversary (R2, deferred). #675's project_archived refusal fires only when the ACTIVE project folder is gone but an archive exists (!existsSync(projectDir) && existsSync(archiveDir)). Residual edge: when a live project dir AND a same-named leftover archive both exist, and a scan runs with an explicit --output at the archive's run-gaps.json, the refusal does NOT fire (projectDir exists), the scanner reads the live dir, and the explicit --output still clobbers the archived run-gaps.json. Pre-existing explicit-output behavior, not introduced by #675, low severity (narrow trigger) but can silently overwrite a prior cycle's archived gap evidence. Fix (S): guard the explicit --output write — refuse or re-anchor when --output targets an archive path belonging to a different project/cycle than the one scanned, or when it would overwrite an existing run-gaps.json outside the scanned project's own .cache/. Regression: a live scan with --output at a same-named archive's run-gaps.json must not clobber it. Full body on GitHub #679.
