issue: #730
title: bug(repair-dispatch): reopened fixer receives IDs but not a digest-bound canonical finding brief
status: ATTEMPTED AND REJECTED 2026-07-22 (adversarial review, blocks_commit) — rework required; the #727 commit it was stacked with shipped separately
workflow_project: —
next_step: REWORK. The rejected attempt built the brief but left the OWNERSHIP-ABSENT path telling the authorized fixer to fix NOTHING: when no open finding routes to an owner (anchor-less schema-2 evidence_observation findings, findings anchored at a file no node declares, or a pre-#701 journal), the ownership bridge stays inert and the reopen proceeds on the graph-maximal proof alone — so the reopened writer IS responsible for the whole open frontier, yet buildRepairBrief emits an empty assigned-uid set. The flagship test asserted the exact behavior the issue forbids: §1 requires that if ANY open finding is unowned, ambiguously owned, or owned by another writer, direct repair must REFUSE and route to replan rather than silently omit it. Also correct on the redo: an evidence claim was false (the walkthrough was cited as a second frontier but greps zero for repair_brief), and for schema-2 the "reviewer's verbatim statement" is vacuous — describe() prefers row.raw and schema2RouteCandidates synthesizes raw as 'uid=<uid>', while canonical findings carry no human-readable statement field at all (only failure_class / trigger digests / anchors / proof_digest), so that claim holds only for schema-1 flat rows. Keep the audited scope trim: an existing backstop (closure re-review carries prior_findings forward and refuses without a per-uid resolution_json) already re-fails the gate on an unaddressed finding, so do NOT rebuild it and do NOT add a close-time proof that merely re-verifies it. The un-backstopped core is that consumed_by is set when the selected writer owns AT LEAST ONE open finding, not all of them. EPIC NOTE (#757): the digest-bound canonical brief becomes the fixer brief for #761 — keep the brief constructor callable outside repair-dispatch; no scope change.
