issue: #767
title: feat(planner): author plan_form: spine plans — make progressive elaboration user-reachable (epic #757 Wave E1)
status: OPEN — epic #757 WAVE E1, gates the #765 cutover; NOW (dispatched investigation of the authoring path)
workflow_project: —
next_step: The spine/expansion machinery (#758–#763) is validator/executor-side only; NOTHING authors plan_form: spine (grep of agents/ templates/ commands/ for plan_form is empty; absent → dag → legacy full-DAG). Teach the planner (agents/workflow-planner.md + 3 byte-matched forge tomls + six workflow-init embeds) to author a valid spine: milestone spine + finalize sink, each node a CONCRETE single-role node (frontier provable at freeze) or an expansion_point (composed at open time) declaring milestone goal / coarse expected surfaces / join constraints / review-obligation class; plan_hash covers the spine; validation_policy_required still applies (an expansion point is a code producer). The spine SUBSUMES full-DAG expressiveness (all-concrete spine == legacy full-DAG), so author-spine-always is the target that lets #765 retire dag without losing any shape. Freeze (adaptive-handoff.js) + executor (adaptive-node.js expand-open/expand-close/open-ready/reexpand-open) already run a frozen spine — confirm authoring is the ONLY gap and fix any gap real authoring exposes. ACCEPTANCE: a real run authors plan_form: spine, freezes, executes an expansion point end-to-end (open→compose→close→advance→sink), finalizes — the concrete "full run on the spine shape" #765 needs; cross-edition (planner md↔toml parity, six init embeds regenerated, six routing surfaces intact, four chains green); provenance out of agent-facing surfaces. Then #765 (Wave E2) retires dual-accept, rewrites the twelve generated-surface descriptions + six init surfaces, sweeps retired-language pins, records the cutover ADR; epic closes. USER DECISION 2026-07-23: chose "build planner spine-authoring first, then full cutover" over deferring the retirement.
