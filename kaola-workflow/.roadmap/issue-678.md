issue: #678
title: fix(scripts): open-ready group baseline strands on group-form aborts (post-#674 symmetric gap) + pre-journal hard-crash window
status: open
workflow_project: —
next_step: Surfaced by the #674 adversary (R1) + code-review (R1/R2), both execution-confirmed. Two residuals in the same open-ready lane-group baseline lifecycle, adjacent to #674's member-baseline-drop fix. R1 (symmetric gap, low): #674 drops each recorded MEMBER baseline (barrier-base-<member> file+ref+freshness) on every post-baseline group-form abort, but the GROUP baseline (barrier-base-lg-<...> file + refs/kaola-workflow/barrier/<project>/lg-<...> ref), recorded before the member loop, is NOT dropped on those same 5 aborts (adversary observed file=true ref=true). Harm bounded (default legged close uses the --merge-commit group barrier anchored at the merge commit; reconcile-running-set drops the group baseline when the descriptor persisted). Fix (S): extend the #674 abort cleanup to also drop the group baseline on the same aborts; regression asserting the lg-* file+ref are gone. R2 (pre-existing crash window, low): a SIGKILL-class hard crash between recording baselines and writing the Phase-1 running-set journal (running-set.json state:opening) strands baselines with no opening marker for reconcile to find — unreachable via any refusal return, predates #674. Fix (M): reorder so the crash-recoverable journal/opening marker is durable BEFORE any baseline is recorded, OR add an orphan-baseline reconciliation sweep. Cross-edition (adaptive-node GENERATED aggregator) → four-chain. Full body on GitHub #678.
