issue: #724
title: bug(plan-validator): whole-plan --barrier-check ignores the epoch lineage — a schema-2 child plan's accumulated candidate refuses write_set_overflow at finalize
status: ready — unblocked (#725 closed, v6.24.0 shipped 2026-07-21)
workflow_project: —
next_step: BATCH 8 — write-set union across the epoch lineage in whole-plan barrier-check (write_set_overflow at finalize)
