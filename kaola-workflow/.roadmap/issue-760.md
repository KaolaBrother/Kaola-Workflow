issue: #760
title: feat(protocol): serialization-trigger inversion — co-open by default at open time; serial requires a named serializer
status: queued — epic #757 child 3 (depends on #759)
workflow_project: —
next_step: Invert the burden of proof: mode defaults to CO-OPEN in isolated per-leg worktrees; serial only on a NAMED serializer — S1 true data dependency, S2 shared irreversible external effect, S3 no worktree support. UNCERTAIN overlap is NOT a serializer (co-open isolated + mandatory synthesizer reconcile; extends the existing containment+reconcile correctness net from proven-disjoint to uncertain). Grain adjusts unit COARSENESS, never mode. --write-overlap-consent narrows to the S2 class. Propagate to the SIX routing surfaces via skeletons+slots regeneration (never hand-edit the 12 generated surfaces); zero uncertain-implies-serial language may remain; route-reachability + all four contract validators green.
