issue: #760
title: feat(protocol): serialization-trigger inversion — co-open by default at open time; serial requires a named serializer
status: CODE VERIFIED-GOOD on branch wave-b-760 (3 commits, NOT merged) — the adversarial verdict was REJECT for ONE reason only: the mandated cross-edition four-chain receipt was never witnessed (a completeness/process blocker the orchestrator discharges at merge, not a code defect). 14/15 mutants killed; the lone survivor is an equivalent mutant (an unreachable defensive exact-overlap branch pre-filtered upstream by the freeze antichain + parallel-safe pair-loop). Merges with #761/#763; the four chains run once over the combined tree. INTERPRETATION QUESTION RESOLVED BY RESTRAINT: the impl declined to make PROTECTED files (lockfiles, CHANGELOG, ROADMAP, the ×4 schema anchor) newly co-openable under --write-overlap-consent, reading the issue's "consent narrows to the S2 class" as naming consent's only remaining scope (plain source-tree overlap no longer needs it), NOT a mandate to flip PROTECTED from an absolute block to consent-gated. This is correct-by-default: flipping them would be a LOOSENING, which the tighten-only boundary forbids as a default, and three existing regression tests pin PROTECTED as refusing even with consent. If PROTECTED-under-consent is genuinely wanted it is a deliberate follow-up, not this issue.
workflow_project: —
next_step: Invert the burden of proof: mode defaults to CO-OPEN in isolated per-leg worktrees; serial only on a NAMED serializer backed by PRESENT-TENSE EVIDENCE (user directive 2026-07-22) — S1 true data dependency (name the concrete artifact B consumes from A, or co-open), S2 shared irreversible external effect (name the shared resource — resource identity, not a conflict forecast), S3 no worktree support (the failed probe result). Predictions/anticipations/uncertainty are INADMISSIBLE grounds for serial. Evidence lines are audit-only derivations — the only mechanical check permitted is that a line EXISTS; no validator parses content. Governs MODE only: width stays with faithful decomposition (grain merges under-sized units). UNCERTAIN overlap is NOT a serializer (co-open isolated + mandatory synthesizer reconcile; extends the existing containment+reconcile correctness net from proven-disjoint to uncertain). Grain adjusts unit COARSENESS, never mode. --write-overlap-consent narrows to the S2 class. Propagate to the SIX routing surfaces via skeletons+slots regeneration (never hand-edit the 12 generated surfaces); zero uncertain-implies-serial language may remain; route-reachability + all four contract validators green. DELTA 2026-07-22: taxonomy completion — proven same-file overlap = MERGE the units (grain response), serial-with-named-path as fallback; KAOLA_PARALLEL_WRITES=0 survives as operator serializer (classify with S3); PROTECTED files fold into S2/consent, post-dominating-gate condition discharged by the spine review wall; rewrite surfaces include slots.js frontmatter descriptions + token pins. Posture block already in the skeleton at ad196273 — this child replaces the mechanics prose around it.
