issue: #673
title: fix(scripts): locateSection heading match uses startsWith vs classifier anchored ^##\s+H\s*$ — post-#670 residual
status: open
workflow_project: —
next_step: Surfaced by the #670 adversarial-verifier (R1). #670 closed the fence-INDENT divergence between schema.locateSection and the hash-covering classifier (both now anchor fences at ^\s{0,3}). The adversary then found a sibling divergence in the same parity family but on HEADING MATCHING: locateSection (adaptive-schema.js:1149 and :1165) selects a section via ln.startsWith('## ' + heading) / ln.startsWith('## '), a loose prefix match, while the classifier (classifier.js sectionBodyState / headRe) uses an anchored ^##\s+<heading>\s*$. Forward: looking for '## Node Ledger', locateSection matches a decoy '## Node Ledger Extra' the classifier does not (adversary-confirmed live with zero fences). Reverse: '##  Node Ledger' (two spaces) / '##\tNode Ledger' (tab) are matched by the classifier's \s+ but missed by locateSection's single-space prefix; a heading on line 0 is missed by locateSection's i>0 guard. Same plan_hash wedge class as #670, different channel. Fix (S-M): make locateSection's heading match byte-parity with the classifier's anchored ^##\s+<heading>\s*$ (exact heading, \s+ after ##, trailing-whitespace-only tail; reconcile the line-0 guard), apply byte-identically to all 4 adaptive-schema copies (byte-anchor group, NOT sync:editions), add a suffix/multi-space/tab/line-0 heading-parity regression. Do NOT touch the classifier (read-only oracle). Cross-edition four-chain. Note: the CRLF residual R2 is NOT a defect (both parsers are identically CRLF-blind; #670 removed a pre-fix trim CRLF divergence). Full body on GitHub #673.
