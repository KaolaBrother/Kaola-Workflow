issue: #979
title: The main-root preflight still exempts a backslash-named file the worktree guard now refuses over
status: open
workflow_project: TBD
next_step: Measured during bundle-976-977-978 by the #978 premise check and named by its implementer as a deliberate follow-up; NOT a regression. #978 closed the worktree-side hole in the shared guard (a decoded rel containing a backslash is kept as dirt before the classifier is consulted, since porcelain emits only "/"), verified by driving the legacy route end-to-end on all four editions. The main-root preflight around sink-merge.js:1697 was left alone and still classifies foreign dirt by the same name-shaped reasoning. It was left because the premise record listed the seam as "not measured" and the implementer chose a porcelain-fed guard repair over a classifier one to leave the byte-identical cross-edition kernel untouched. NOT KNOWN: whether the preflight exemption destroys anything or merely mis-names foreign dirt in a report — establish that before building. SPECIFY THE RESULT: the main-root preflight does not treat a literal-backslash filename as lane bookkeeping; the same two-record rule at the preflight, a kernel classifier change (four-tree propagation, four-chain run), or a demonstration that it is harmless, are all open.
