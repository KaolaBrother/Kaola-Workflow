issue: #763
title: feat(adaptive): shared context packet + per-expansion efficiency evidence line
status: PARTIAL — branch wave-b-763 carries 761+760+763-impl merged clean (HEAD from 4f58da8b, walkthrough green). First continuation agent DIED mid-response (connection closed) before committing; its coherent partial (readContextPacket + context_packet on the open-ready/open-next/fused-advance dispatch cards, canonical only) was reviewed, salvaged, and committed as WIP 64a756a7. RESUME DISPATCHED (w3j6h10hw) to verify that WIP, then finish: edition propagation, the plan-run skeleton relay prose, the RED-without-it dispatch-card test, and Gap 2 (archive-rollup pin killing M18/M19), then re-verify.
workflow_project: —
next_step: Two lightweight mechanisms, explicitly not a subsystem. (1) Orientation emits .cache/context-packet.md (goal, constraints, key files, conventions, join expectations); every frontier unit dispatch includes it verbatim — lowers per-agent setup cost, raising profitable width under the grain rule. (2) On expansion close, ONE evidence line: expansion <id>: width=<N> mode=<serial|co_open> serializer=<none|S1|S2|S3> rework=<K>; one rollup line per run in the archive summary — makes the fan-out turning point empirical. Non-goals: no token metering, no timing infra, no dashboards, no new durable files beyond the packet. CARRY-FORWARD FROM #759 (Wave A descope, owner named by the producer): workflow-tasks.json (the derived UI mirror, kaola-workflow-task-mirror.js) does not list composed units — it reads parseNodes (freeze view). Cosmetic and non-authoritative (the ## Node Ledger is the correctness truth and unit rows ARE in it), one-line fix via planNodesWithExpansions. It belongs with this child because this is the observability child; take it or explicitly decline it.
