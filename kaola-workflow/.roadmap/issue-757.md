issue: #757
title: epic(adaptive): progressive elaboration — coarse frozen spine + just-in-time expansion, parallel-by-default
status: IN PROGRESS — Wave A DONE (#758/#729/#759 CLOSED); Wave B DONE 2026-07-23 (#761 local re-expansion, #760 serialization inversion, #763 context packet — ALL CLOSED; merges 6ef8752c, 348670c6, ab825a5f; four edition chains green on the merged tree). Wave C DONE 2026-07-23 (#762 declared-not-walled surfaces, #730-AC9 unified repair feedback — both CLOSED; merges d7aefce1/4c2a137a, edition-sync regenerated the forge ports, all four chains green on the synced tree). NOW: Wave D (#766 opencode/kimi port sweep ∥ #756 acceptance harness for #761+#762 ∥ #745 disposition close) — co-open, file-disjoint. REMAINING: Wave E (#765 cutover ALONE → close epic).
workflow_project: —
next_step: EXECUTION SCHEDULE — five waves, run STRICTLY IN ORDER. WAVE A (DONE): #759 expansion transaction (adaptive-node.js, next-action.js) ∥ #729 remaining ACs (replan.js). WAVE B (DONE 2026-07-23): #761 local re-expansion (adaptive-node.js, replan.js) ∥ #760 serialization inversion (adaptive-schema.js, routing skeletons+slots, adaptive-node.js) ∥ #763 context packet + efficiency line (adaptive-node.js) — merges 6ef8752c/348670c6/ab825a5f, four chains green. WAVE C (NOW): #762 declared-not-walled surfaces (plan-validator.js, adaptive-schema.js, adaptive-node.js) ∥ #730 AC-9 unify direct-repair and replan-child feedback delivery (replan.js) — file-disjoint, co-open; #762 also carries #761's M7b integration-coverage and #763's MPfused fused-advance-site carry-forwards. WAVE D: #766 opencode/kimi port sweep (additive trees only) ∥ #756 acceptance harness for #761+#762 (walkthrough) ∥ #745 disposition close (docs only). WAVE E: #765 cutover ALONE, then close this epic. CRITICAL PATH is #759 → #761 → #762 → #766 → #765 and every edge on it is a NAMED S1 data dependency, so the wave count cannot be compressed below five: #761 consumes the expansion-record shape from #759; #762 routes amendments to re-review THROUGH #761's re-expansion record; #766 ports the settled prose of all core children; #765 ends dual-accept only after a full run has completed on the spine shape. Everything off that path is scheduled at the earliest wave its own dependency allows — #760 and #763 need only #759, so they co-open with #761 in WAVE B rather than waiting on it. CO-OPEN EVIDENCE — #761/#760/#763 all write adaptive-node.js, but shared-file-ness is a conflict FORECAST, not a serializer: S1 requires naming a concrete artifact one unit consumes from another, and none of the three consumes anything from the other two. They co-open in isolated legs and reconcile at the join; merge order is #761 (largest, region-replacing) then #763 (smallest, additive) then #760, so each reconcile is against a settled base. #730 AC-9 is held to WAVE C on real S1 evidence, not caution: it rewrites the replan-child feedback path in replan.js, and #761 retires replan_source_journal_missing in that same file — the artifact is named, so it waits. CARRY-FORWARD RULE (binding for every wave) — a wave is not finished when its code merges; it is finished when everything it DISCOVERED has a named owner. Each producer must return descoped items explicitly, and each descoped item must be written into the roadmap source of the child that will own it BEFORE the producing wave merges. Never leave a discovery only in a workflow journal or an agent transcript: those are ephemeral and this epic has already generated four such items. If a discovery fits no existing child, file a GitHub issue and add its .roadmap source in the same action. A verifier finding that is argued non-blocking still counts as a discovery and still needs an owner — the #758 spine validation-policy escape was carried into #759 this way and closed there. WAVE A CARRY-FORWARD, already assigned: #759's four descopes went to #761 (expansion_id as a validated review-journal attempt field), #762 (composed gate units invisible to --gate-verify / --verdict-check, fails closed), #763 (workflow-tasks.json mirror omits composed units, cosmetic), and #765 (six workflow-init surfaces + resume/frontier-batch cards move as one write set with the cutover); #729's surviving mutants are hardened in Wave A itself before merge. Per-child planning deltas live as 2026-07-22 comments on each issue. Principle prose was propagated ahead of mechanics at commit ad196273 (axioms+embeds, planner md+tomls, plan-run skeleton ×12) with chains DEFERRED — that debt is DISCHARGED as of the #758 ship, which ran all four chains green. Migration is dual-accept (legacy full-DAG plans keep byte-identical behavior); the cutover decision is an ADR at epic close. Hard anchors that must NOT loosen: consent valve, guard prologue, atomic freeze, reviewed-before-sink wall, finalize receipts, running-set ledger atomicity, unattributed-write tamper refusal. STRATEGY (user directive 2026-07-22): evidence-backed serialization — serial requires present-tense nameable evidence for S1/S2/S3; predictions and uncertainty are inadmissible; mode is evidence-governed, width stays with faithful decomposition (see the #760 addendum).
