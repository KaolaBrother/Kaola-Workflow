issue: #648
title: finalize-tail chain re-runs: stamp-last sequencing rule (x6 surfaces) + chains_stale culprit-path emission + consumer final-validation citation-instead-of-rerun
status: open
workflow_project: —
next_step: Live runs re-execute the four chains several times in the review/finalize tail: the five SELF_HOST_TEST_CONSUMED prose files (README/CHANGELOG/docs-api/workflow-state-contract/agents-source, PV:295-301) stay inside codeTreeHash, and the finalize tail is exactly when they get edited — every post-stamp edit flips the hash to chains_stale whose only remedy is a full re-stamp (the #547/#551 ~30-min waste), while the refusal prints only two hashes so agents re-run blind; independent consumer arm: All-done re-runs validation_command purely to materialize final-validation.md even when the terminal change-gate node ran the identical command green at the identical tree. Fix (one M run, 3 items): (A) stamp-last sequencing rule prose on all six routing surfaces (plan-run All-done + De-Dup, finalize chains_stale remedy, +SKILL mirrors x3 packs) incl. the does-NOT-stale clarification (kaola-workflow/** state + inert docs); (B) hint-only chains_stale culprit emission in plan-validator --finalize-check (~PV:3153/3158): best-effort delta vs receipt.headSha (clean-stamp only) filtered by isValidationInvisible with the receipt's own validationTestConsumes, additive stale_paths/stale_kind payload fields, chains_red-ctx-style hint, fail-open to the generic message — refuse decision byte-unchanged in all paths; walkthrough cases a-e beside the existing #432/#547 blocks (~3564-3660); (C) consumer final-validation.md by citation (source: cited:<node-id> + validated_command/validated_at_head/reuse_boundary fields; column-0 verdict:pass stays the gated token; consumer gate branch PV:3183-3196 zero code diff — #475 trust model preserved). Deferred non-goal: per-chain scoped receipt refresh (needs a machine-verified per-chain prose-consumption map). plan-validator is GENERATED class (edition-sync.js:54) → sync:editions + four-chain. Full design on GitHub #648.
