issue: #953
title: The code-producing roles carry no solution ladder: implementer has zero minimalism guidance, planner's one hit is incidental, code-architect holds a thin second wording — author it once, render to every carrier
status: open
workflow_project: issue-953
next_step: Premise pass first — enumerate the real carrier surfaces for implementer / code-architect / planner across all four runtimes (canonical agents/*.md, the codex agents/*.toml propagation surface, whatever the opencode/kimi syncs render for these roles; grep the role names, do not assume), recording which carriers are byte-renders vs transformed. Then author the solution-ladder section once (speculative brief parts are findings not builds; reuse-in-repo first; stdlib; already-installed dependency — new deps stay an escalation; then minimum working code; no single-implementation abstractions; deliberate corner-cuts marked with ceiling + revisit trigger) and render it to every enumerated carrier, harmonizing code-architect's existing two-line wording into the shared one. Agent-facing text names no vendor or origin. Verify: test-agent-profile-parity.js, edition sync --check, walkthrough at full scope, CHANGELOG under [Unreleased]. The ladder governs the solution, never the reading — comprehension and verification tiers in these files are untouched.
