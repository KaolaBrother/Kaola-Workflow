issue: #736
title: detectReviewRuntime misclassifies a self-dev checkout named kaola-workflow as opencode — #712[self-dev] red in default-named clones; blocks sink-merge FF-race gate
status: open — deterministically reds the main-root sink FF-race gate
workflow_project: —
next_step: small lightweight-path fix; slot BEFORE the Phase D sink if convenient, else first post-epic alongside #739
