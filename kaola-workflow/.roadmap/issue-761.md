issue: #761
title: feat(adaptive-node): local re-expansion — any finding re-expands its owning expansion point; no failed-attempt prerequisite
status: IN FLIGHT — epic #757 WAVE B, CRITICAL PATH (Opus; owns adaptive-node.js re-expansion + replan.js retirements; consumes #759 expansion-record shape, now merged; carries the #759 expansion_id-as-validated-attempt-field descope)
workflow_project: —
next_step: A finding from ANY source — failed review, PASSED-review candidate staleness, finalize done-gate (#756 trigger), out-of-surface attribution (#762) — routes to local re-expansion of the owning expansion point: a new expansion record with at minimum {fixer unit, re-review unit}; reviewed-before-sink preserved by construction. Fixer brief = digest-bound canonical brief (#730 shape) built from the finding frontier (#729 shape). Ownership by declared+amended surface; spanning findings re-expand each owner; a finding outside every milestone's scope is the ONLY remaining epoch-replan trigger. Retire as primary remedies: unique-maximal-producer escalation to replan for interior findings, the reopen strand/orphan family (reopen = rollback only), the replan_source_journal_missing failed-attempt prerequisite. AC: the #756 repro reaches a passing REVIEWED finalize with no hand-edits; the #745 reopen dead-end resolves. DELTA 2026-07-22: finalize re-expansion is admissible only BEFORE the sink's first irreversible step (commit/push/merge), determined from DERIVED sink-progress evidence (sink writes no in-band marker); after that it is a follow-up claim. The #729 empty-frontier refusals bind unchanged at spine-level replans. CARRY-FORWARD FROM #759 (Wave A descope, owner named by the producer): make expansion_id a VALIDATED field inside review-journal attempts. #759 shipped the pairing (expand-open emits review_binding {plan_hash, expansion_id}; each record carries the spine plan_hash it was composed against; the walkthrough pins that hash invariant across a re-expansion) but did NOT add the field to the attempt schema, because that schema lives in adaptive-schema.js — the byte-identical-x4 drift anchor — and gates every existing schema-1 and schema-2 review, so it would have to be optional-and-ignored to avoid invalidating in-flight journals. THIS child is the first consumer that actually needs to ask which record an attempt reviewed, so it lands here. Do not re-litigate the #759 scoping; implement the field.
