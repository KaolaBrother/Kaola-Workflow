issue: #761
title: feat(adaptive-node): local re-expansion — any finding re-expands its owning expansion point; no failed-attempt prerequisite
status: PARTIAL — routing PRIMITIVE verified-good on branch wave-b-761 (3 commits, NOT merged); adversarial REJECT 2026-07-23 (keystone BEHAVIOR incomplete). CONTINUATION IN PROGRESS on the same branch (Opus): building the discharged-point re-open cascade + retire-as-primary lifecycle wiring + finalize done-gate/derived-sink-progress admissibility + the M10 journal-validator test, then end-to-end re-verification.
workflow_project: —
next_step: SHIPPED on branch wave-b-761 (not merged) — a well-engineered, PURE, both-direction-pinned ROUTING PRIMITIVE: routeFindingReExpansion (default-LOCAL; escalate to spine_replan ONLY on positive proof the files lie outside every declared+amended surface; anchorless+no-context refuses reexpansion_owner_unresolved — no silent guess either way), resolveExpansionOwnership + surfaceCoversFile (spanning finding => multi-owner set), the FAIL-CLOSED re-review wall assertReExpansionReviewed (a point with no review_class or no reachable downstream spine gate REFUSES reexpansion_review_wall_missing — never sink unreviewed), buildReExpansionComposition (fixer unit = the SAME #759 expand-open transaction; brief = #730 digest over the #729 finding_index), a read-only route-reexpansion subcommand, and the CARRY-FORWARD expansion_id as an OPTIONAL-validated review-journal-attempt field (concluded NEEDED — a re-review must durably name WHICH record it reviewed; candidate_digest distinguishes but gives no lookup key). Provenance clean, CHANGELOG untouched, 100% additive (0 deletions), four editions byte-identical by enumeration, ALL FOUR CHAINS GREEN with real $? (claude/codex/gitlab/gitea EXIT=0, AC4 met), 13/14 mutants killed. REJECTED because three of four ACs are END-TO-END scenarios and none is reachable, and the issue's own retire-as-PRIMARY contract is entirely unwired. THREE PIECES REMAIN, all to be built ON THIS BRANCH before merge: (1) DISCHARGED-POINT RE-OPEN CASCADE — the real #756/#745/AC3 milestones are already discharged when the finding surfaces, and runExpandOpen fails closed expansion_point_discharged on a complete/n.a point (REPRODUCED at adaptive-node.js:12118); build a primitive that re-opens the discharged expansion point, re-activates its post-dominating spine review wall, and selectively re-verifies ONLY downstream milestones whose surfaces intersect the fix. (2) RETIRE-AS-PRIMARY WIRING — the router is consulted by NO lifecycle path (grep-confirmed across 4 editions); dependent_producer_replay_required (:8182/:8539), would_orphan_in_progress (:7495/:8518), would_strand_completed_dependent (:7515/:8559) and replan_source_journal_missing (replan.js ~680, not even in the diff) still fire as the PRIMARY route at byte-unchanged call sites; wire close-and-open-next / repair-node / reopen-node / the finalize done-gate to route to local re-expansion FIRST, keeping the old families reachable only for genuine rollback / spine-change. (3) FINALIZE DONE-GATE + DERIVED SINK-PROGRESS ADMISSIBILITY (the 2026-07-22 DELTA) — no sink-progress mechanism exists (grep empty); admit finalize re-expansion ONLY before the sink's first irreversible commit/push/merge, from DERIVED evidence (no marker file). Plus fix surviving mutant M10: expansionIdFieldOk is pinned in isolation but its INTEGRATION into validateReviewJournalV2 is untested (walkthrough fixtures OMIT the field, exercising only the absence-passes path), so the present-malformed fail-closed guard can be deleted with zero signal — add a journal fixture that CARRIES a malformed expansion_id. CARRY-FORWARD TO #762 (named by the verifier): routeFindingReExpansion currently routes a file outside every declared surface LOCAL to a named contextPoint and emits a fixer whose write_set is that out-of-surface file — unsatisfiable-by-declared-surface today; #762's append-only surface amendment is what makes it legal. ORIGINAL CONTRACT: a finding from ANY source routes to local re-expansion of the owning expansion point (a new expansion record with at minimum {fixer unit, re-review unit}; reviewed-before-sink preserved by construction), ownership by declared+amended surface, spanning findings re-expand each owner, and a finding outside every milestone's scope is the ONLY remaining epoch-replan trigger.
