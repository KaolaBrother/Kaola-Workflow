issue: #765
title: feat(adaptive): epic #757 cutover — retire legacy full-DAG grammar, rewrite generated-surface descriptions, sweep retired-language pins, record the ADR
status: IN PROGRESS — epic #757 WAVE E2, FINAL; #767 (E1) done. EXPANDED 2026-07-23 into a comprehensive retired-path/historical-residue sweep (see the GitHub issue body): not just grammar retirement + 12 surfaces + pins + ADR, but a whole-repo cleanup so every current-state statement reads as if the current version is the only version that ever existed. D1 removes LIVE fast/full opt-in CODE from install-opencode.sh/install-kimi.sh; D2 cleans legacy-v1/schema-1 prose AS the grammar retires; D3 six-phase vocab in scope; D4 history surfaces (CHANGELOG/decisions/investigations/audits/generated mirror) OUT of scope; D5 strip provenance from agent prompts. Now large + multi-surface + cross-edition. EXECUTION (three file-disjoint legs): LEG 3 (Section A docs + Section B install, D1) MERGED 4b70509e/b3cc27ff + recon 90077b95. LEG 1 (grammar retirement: dag→plan_form_dag_retired FREEZE-ONLY, SPINE-1 relaxed for all-concrete spine, 112 canonical fixtures) adversarial-ACCEPT, MERGED+PUSHED (523a3581, all four chains green). RECONCILIATION en route: the four-chain gate (not run after Leg 3) revealed origin was red — Leg 3 over-applied the D1 --enable-adaptive deletion to install.sh (restored the shim) + left stale install-config/validator pins + over-scrubbed the LIVE fast-summary.md D2 back-compat doc (restored present-tense); Leg 1 also missed test-barrier-base-integrity.js #724 + all 5 edition walkthroughs (0 plan_form refs each — migrated via a delegated Workflow, 39 spine insertions, negatives preserved). All folded into the pushed green tree. LEG 2 (planner author-spine-always flip [CORRECTNESS-CRITICAL: after dag retires the planner MUST author an all-concrete spine when the whole shape is known, else every run refuses] + Section C/D agent-prompt residue + 12 routing regens + token pins + cutover ADR) DISPATCHED (Workflow, iterates all four chains + fixes token pins in lockstep). Then merge → four chains → close #765 → close epic #757.
workflow_project: —
next_step: Precondition: all core children shipped + at least one full run completed on the spine shape. Then: end dual-accept in the plan-validator (legacy shape → typed spine-grammar refusal); rewrite the twelve generated routing surfaces' frontmatter descriptions and remaining freeze-time-proof prose (slots.js "serial as the fallback for overlapping/uncertain writes"); sweep required-blocks.js / test-route-reachability.js / four validate-*-contracts.js for retired-language token pins; record the cutover ADR. Four chains green. CARRY-FORWARD FROM #759 (Wave A descope, owner named by the producer): the SIX workflow-init surfaces plus the resume / frontier-batch plan-run cards were deliberately not touched. Their current text (the planner authors and freezes a task-shaped DAG; resume reads the ledger) is still ACCURATE for every plan any install can currently produce, because the shipped planner does not author plan_form: spine plans — #758 proved the shape at freeze/resume only. Documenting expansion records there before authoring exists would describe a shape no user can reach. #759 DID update what is authoritative today: docs/workflow-state-contract.md gained the ## Expansion Records contract and docs/api.md gained the full transaction contract. Move the six init surfaces + the two plan-run cards as ONE write set with this cutover.
