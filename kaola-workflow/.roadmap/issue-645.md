issue: #645
title: surfaces: ship a first-principles axiom layer to consumers — canonical axiom block, rule-gap tie-breaker protocol, tighten-only boundary
status: open
workflow_project: —
next_step: The design axioms (accuracy > automation > cheapest-sufficient; escalate values not facts; own your own verdicts) live only in this repo's CLAUDE.md — no consumer-shipped surface carries them (audit @ 2c45d5ba: commands/agents hold only local precedence ladders; the workflow-init template lacks the hierarchy), so an agent hitting a rule gap guesses by nearest-rule pattern-match, unauditably. Proposed three-layer design: Layer 0 canonical five-axiom block (ONE file, byte-identical across editions per the adaptive-schema pattern, drift-guarded in the walkthrough); Layer 1 existing rules unchanged (new-rule discipline: must trace to an axiom); Layer 2 empirical constraints protected from principle-pruning. Only behavioral change: rule-gap tie-breaker — resolve by axiom order + record a one-line derivation in .cache evidence (adversary-auditable; recurring derivations become rule candidates). Hard boundary: tighten-only — an axiom can never be cited to skip a typed gate/refusal/barrier. Ships as prose + drift guard only; no scheduler/validator/gate changes. Six routing surfaces + workflow-init template + #307 four-chain; opencode additive. Filed by user 2026-07-08; full design on GitHub #645.
