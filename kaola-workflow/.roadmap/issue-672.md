issue: #672
title: fix(scripts): residual porcelain-probe fail-opens deferred from #669 — leg-dirty-commit probe + worktreeDirtyState
status: open
workflow_project: —
next_step: Surfaced by the #669 review (R1, R2) and its change-gate adversary. #669 flipped ONLY the mandated plan-validator dirty-fence (plan-validator.js:3253) to fail-closed and capped every in-scope status --porcelain content site. Two OTHER porcelain empty-catch sites remain fail-open in their own consumers, both verified byte-identical pre-#669 (genuinely pre-existing, strictly improved by the cap). Site 1 (medium): adaptive-node.js:4522 synthesizeLevel leg-dirty-commit probe catches to dirty='' → a porcelain overflow/error reports the leg clean and real leg content is silently omitted from the octopus merge; the leg_omitted_from_merge guard (plan-validator.js:3181-3196) is ancestry-only and doesn't cover the working-tree probe; leg provisioning is LIVE since #542 (adaptive-node.js:7110), so live-but-low-probability merge-correctness. Fix: treat overflow/error as cannot-prove-clean → include the leg or refuse, not silently skip. Site 2 (low): claim.js:483 worktreeDirtyState catches to 'missing', conflating unprobeable with absent; consumer cmdSweepLegacyWorktrees (claim.js:3455) treats non-'dirty' as removable → a probe failure could feed a destructive worktree removal (ENOBUFS-immune since #669 but still fail-open on other errors). Fix: distinguish unprobeable from missing, fail closed before any destructive sweep. Note (cap-completeness, non-blocking): closure-audit.js:166/173 and release.js:44 carry uncapped status --porcelain content probes outside #669's site list; both catch fail-CLOSED so robustness only, cap for uniformity when convenient. Full body on GitHub #672.
