issue: #692
title: adaptive: write-leg dispatch briefs upstream_evidence at a leg-relative path, but a leg worktree carries no sibling .cache evidence
status: open
workflow_project: —
next_step: Absolutize dispatch.upstream_evidence[].path to the parent worktree when a node opens into an isolated leg, so leg agents can bind the upstream nonce without leaving the briefed cd-leg discipline.
