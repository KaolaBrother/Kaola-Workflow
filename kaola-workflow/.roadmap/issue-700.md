issue: #700
title: fix(sink): collision-suffixed archive must commit and persist closure metadata
status: open
workflow_project: —
next_step: Use the actual archiveProjectDir destination in --sink, persist cmdFinalize-equivalent closure/attestation metadata, and fail resumably unless the collision-suffixed archive plus roadmap cleanup are committed and pushed.
