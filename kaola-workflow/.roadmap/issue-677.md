issue: #677
title: fix(scripts): worktree-cleanup existsSync stat-failure still fails open + stale-sweep unprobeable-keep untested (post-#672)
status: open
workflow_project: —
next_step: Surfaced by the #672 review (R1) and adversary (A1, A2), live while verifying #672's worktreeDirtyState fail-closed fix. Two residuals in the same claim.js legacy/stale worktree-cleanup surface, OUTSIDE #672's porcelain-probe family. A2 (defect): worktreeDirtyState (claim.js:~480) hoists fs.existsSync OUT of the porcelain try to split 'missing' from 'unprobeable', but existsSync returns false for an EXISTING path when the parent dir is unreadable (chmod 000), so a present worktree routes to 'missing' → destructive consumers prune its registry entry + delete its merged-branch ref (content-safe — dir survives — but a stat-failure fail-open in the same family). Adversary-confirmed live via a chmod-000 parent. Fix (S): fail-closed the stat path too — lstatSync in try/catch, classify EACCES/ENOTDIR-on-parent as unprobeable not missing; regression on the parent-unreadable path asserting worktree+branch kept. A1 (test coverage): cmdStaleWorktreeCleanup's 'unprobeable' keep has no shipped regression (only cmdLegacyWorktreeCleanup tested); removing the guard on a scratch copy keeps the suite green while an ENOBUFS/unprobeable stale sweep deletes worktree+branch (behavior verified correct by #672 review + adversary, but untested). Add a mirrored cmdStaleWorktreeCleanup unprobeable-keep regression across --execute/--force/--archive/--export. Cross-edition (claim.js COMMON + divergent forge ports) → four-chain. Full body on GitHub #677.
