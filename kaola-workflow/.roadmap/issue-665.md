issue: #665
title: bug(parsing): #660 fence semantics missing from schema.locateSection and the release Unreleased parser
status: open
workflow_project: —
next_step: Two #660 residual consumers, both reproduced in the 2026-07-12 audit. (1) schema.locateSection (adaptive-schema.js:1127-1162, byte-anchor ×4 editions) closes fences family-only — no run-length/empty-suffix, first-hit heading, no duplicate refusal; a 5-backtick fence hiding a fenced "## Node Ledger" decoy makes the runtime ledger splices (adaptive-node.js:702/794/3159/3215), validator reconcile (:2349/:2387), consent-halt and Meta reads pick the decoy while the classifier picks the genuine section → hash-mismatch wedge; violates #660's "every consumer" AC. Implement the transition check locally (no classifier import allowed). (2) release unreleasedSection() terminates at /^##[ \t]+/m without fence awareness — a fenced "## " line truncates Unreleased refs (live-demonstrated; spurious changelog_incomplete + narrow fail-open lane for unknown refs). Fix (M): both consumers adopt family+run-length+empty-suffix semantics; decoy-plan end-to-end regression + fenced-Unreleased regressions; sync:editions + four-chain. Full body on GitHub #665.
