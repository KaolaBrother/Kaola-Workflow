issue: #646
title: scout: govern the issue-scout model tier — higher-profile reasoning variant + explicit dispatch-side model wiring (close the inherit gap)
status: open
workflow_project: —
next_step: issue-scout's tier is sonnet in all three intent surfaces (source frontmatter, resolver DEFAULT_AGENT_MODELS, install manifest) with no agents/profiles/higher/ variant — AND the effective dispatch is ungoverned: install.sh rewrites frontmatter to model:inherit, the scout is not a plan node (next-action.js never resolves it), and ISSUE_SCOUT_MODEL is absent from the render-placeholder + resolve_agent_model_for_install lists, so the router dispatch carries no model= at all (manifest entry consumed by nothing). Evidence for the raise: #524 live frontier mis-rankings on the standard tier; since #545 the scout is the SOLE backlog reader with no post-dominating verifier; whole-run blast radius at once-per-run cost (planner-class shape). Fix: (1) agents/profiles/higher/issue-scout.md with model:opus (common profile keeps sonnet; resolver default unchanged); (2) ISSUE_SCOUT_MODEL placeholder + install-rendered model= in the workflow-next scout dispatch prose across the SIX routing surfaces (#400), validator-pinned; (3) NOT a REASONING_FLOOR_ROLES entry. Codex TOML twins carry no model line (prose-only there). #307 four-chain. Filed by agent (user-approved) 2026-07-09; full design on GitHub #646.
