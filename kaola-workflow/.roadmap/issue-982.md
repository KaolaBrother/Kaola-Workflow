issue: #982
title: The mktemp shim's -p/-t precedence is a latent false-green for any future fix that uses -p
status: open
workflow_project: TBD
next_step: Found by the #976 adversarial review during bundle-976-977-978 while auditing the test harness rather than the diff; latent, and cannot mislead any site that exists today. scripts/test-relative-tmpdir-escape.js verifies the installer half of #976 through a GNU-shaped mktemp PATH shim because the defect is invisible on macOS, where /usr/bin/mktemp reads _CS_DARWIN_USER_TEMP_DIR and ignores TMPDIR even when absolute — so an unshimmed shell-site test authored on a Mac passes whether or not the fix exists. The shim was audited against real GNU coreutils 9.1 in a container and matched all 13 load-bearing rows including modes and the exact "too few X's in template" abort; two divergences were recorded, both STRICTER than real GNU: mid-template X handling and -p/-t precedence. The latent risk is that a future repair written with mktemp -p could go green under the shim while still escaping on real GNU. No current site uses -p; every one passes an explicit template under the KW_TMPDIR absolute-or-/tmp guard. SPECIFY THE RESULT: the shim cannot report green over a -p-based fix that would escape on real GNU — aligning precedence, refusing outright on -p so the gap is loud, or pinning that no installer site uses -p are all admissible.
