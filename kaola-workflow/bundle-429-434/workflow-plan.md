# Adaptive Workflow Plan — bundle-429-434

<!-- plan_hash: 5e798e35ef33be1f09ddad73239d1ddb79a9fb318cdf0656e8ec59f0a04a26ae -->

## Meta
issues: 429, 434
labels: enhancement, area:scripts
bundle_id: bundle-429-434
sink: merge
conflict_note: |
  #435 (parallel session, in-progress) owns these prose regions and they are AVOIDED by every node here:
  - commands/workflow-next.md "## Goal-Driven Autonomy" section (gap-filing-is-the-goal rule) — NO node here writes workflow-next.md.
  - the finalize "## Run gaps" / `gaps_unswept` Step-8 gate region (in finalize commands/SKILLs + contractor Step-8) — the doc nodes here route the manual SINK choreography and the resume/repair RECOVERY-HINT prose into NEW, structurally-distinct subsections, and are sequenced LAST so they factor in #435's areas.
  - the new scripts/kaola-workflow-gap-sweep.js + COMMON_SCRIPTS/install-manifest lines #435 adds — NO node here touches gap-sweep, COMMON_SCRIPTS, or install.sh.
  Doc nodes (n5→n6→n7) are SEQUENCED last and edit only the sink/resume/repair RECOVERY surfaces, never the gap-capture surfaces, so the sink merge stays conflict-free regardless of #435's landing order.

## Nodes

| id | role | depends_on | declared_write_set | cardinality | shape | model | notes |
|----|------|-----------|--------------------|-------------|-------|-------|-------|
| n1_design | code-architect | — | — | 1 | sequence | opus | Read-only design (no write set — design is evidence in .cache; the decision records are authored by n_decisions). #429: the resumable `sink` transaction over kaola-workflow-sink-merge.js — preflight clean-gate with typed remediation (auto-stash named claim-time roadmap source; verify-byte-superset-then-remove untracked project-state duplicates; REFUSE `sink_blocked` listing exact foreign-dirt paths, the #328 surface-don't-overwrite lesson), push -u, --no-ff [no-close]-safe merge anchored to mainRoot with the origin-advance --force-with-lease-on-workflow-branch-only path, worktree→main folder sync, cmdFinalize-internals invocation, per-#427 closure, stash restore + #428 roadmap-residue check, archive commit + push; `.cache/sink-receipt.json` step-receipt for idempotent crash-resume (#296 pattern); decide cmdWorktreeFinalize's fate (fold-and-delete vs test-only). #434: `revert-overflow` (reads barrier outOfAllow, git checkout <baseline> -- <exact paths> in barrier root, lifecycle-log, re-runs barrier-check), `repair-node` (refuse unless safe-point; writer complete→pending; KEEP writer original barrier-base — NO re-snapshot; delete invalidated downstream baselines; emit re-dispatch contract role+write-set+nonce), and the resume contract (orient classifies in_progress+absent-evidence as `requires_redispatch`; `inline_execution_suspected` informational flag). Opus: decisions constrain both downstream implements. |
| n2_impl_sink | tdd-guide | n1_design | scripts/kaola-workflow-sink-merge.js, plugins/kaola-workflow/scripts/kaola-workflow-sink-merge.js, plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-sink-merge.js, plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-sink-merge.js, scripts/simulate-workflow-walkthrough.js | 1 | sequence | sonnet | #429 implementation. Failing-test-first in scripts/simulate-workflow-walkthrough.js: (a) blocked preflight with FOREIGN dirt refuses `sink_blocked` listing the exact paths and mutates nothing, (b) kill-between-merge-and-finalize → re-run completes via sink-receipt.json without double-applying, (c) clean run sinks end-to-end with the receipt listing all steps. Canonical root script is a COMMON_SCRIPT: codex byte-copy mirrors verbatim; the two forge ports are hand-ported per the rename map (gitlab=MR/glab nouns, gitea=PR/tea nouns) — canonical spec = "mirror the root sink transaction modulo forge nouns". The validate-workflow-contracts pin is in n_pins (kept here under FILE_CEILING). Sequenced before n3 (shared scripts/ lane). |
| n3_impl_repair | tdd-guide | n2_impl_sink | scripts/kaola-workflow-adaptive-node.js, plugins/kaola-workflow/scripts/kaola-workflow-adaptive-node.js, plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-adaptive-node.js, plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-adaptive-node.js, scripts/test-adaptive-node.js | 1 | sequence | sonnet | #434 implementation. Failing-fixture-first in test-adaptive-node.js: (a) overflow fixture → `revert-overflow` clears outOfAllow and close passes, lifecycle-log shows the revert; (b) reviewer-finding fixture → `repair-node` reopens the writer with its ORIGINAL barrier-base, repair writes close cleanly inside the writer window, NO re-snapshot; (c) resume fixture → orient says `requires_redispatch`; inline completion without attestation flags `inline_execution_suspected`. adaptive-node.js is a GENERATED_AGGREGATOR: edit the CANONICAL root, then the codex byte-copy + 2 forge ports are regenerated by edition-sync.js --write (they change as bytes → declared; edition-sync.js --check is in the gitlab/gitea chains). The validate-workflow-contracts pin is in n_pins. Depends on n2 (shared scripts/ lane). |
| n_pins | implementer | n3_impl_repair | scripts/validate-workflow-contracts.js, plugins/kaola-workflow/scripts/validate-workflow-contracts.js | 1 | sequence | sonnet | non_tdd_reason: mechanical contract-pin (assertIncludes lines) — the pin IS the test; no natural failing unit test. Pin both the new #429 `sink`-subcommand token and the #434 `revert-overflow`/`repair-node`/`requires_redispatch` tokens into the contract validator and its byte-identical codex peer (COMMON_SCRIPT pair, #274/#301). Depends on n2+n3 (transitively) so the pinned tokens exist. code-producing (non-docs) → post-dominated by n_review (G1). |
| n5_doc_planrun | doc-updater | n_pins | commands/kaola-workflow-plan-run.md, plugins/kaola-workflow-gitlab/commands/kaola-workflow-plan-run.md, plugins/kaola-workflow-gitea/commands/kaola-workflow-plan-run.md, plugins/kaola-workflow/skills/kaola-workflow-plan-run/SKILL.md, plugins/kaola-workflow-gitlab/skills/kaola-workflow-plan-run/SKILL.md, plugins/kaola-workflow-gitea/skills/kaola-workflow-plan-run/SKILL.md | 1 | sequence | sonnet | #434 plan-run prose ×6 (#400 all six surfaces: 3 commands + 3 SKILL packs). Add the resume/recovery-hint rule: resume of an in_progress node with absent/incomplete evidence = `requires_redispatch` (re-dispatch the role agent with the SAME nonce); the main session completing node work inline requires a consent-halt or an explicit logged --attest-inline; every refusal that today suggests drop/re-record points instead at `revert-overflow`/`repair-node`. Canonical spec = "mirror the root command's wiring tokens; SKILLs carry the same wiring (route-reachability contract) modulo forge nouns — gitlab MR/glab, gitea PR/tea; forge validators FORBID `gh`/pull-request/`plugins/kaola-workflow/scripts` in SKILLs". Plan-run prose ONLY — #435 does not touch plan-run, zero conflict. docs-only (.md). |
| n6_doc_finalize | doc-updater | n5_doc_planrun | commands/kaola-workflow-finalize.md, plugins/kaola-workflow-gitlab/commands/kaola-workflow-finalize.md, plugins/kaola-workflow-gitea/commands/kaola-workflow-finalize.md, plugins/kaola-workflow/skills/kaola-workflow-finalize/SKILL.md, plugins/kaola-workflow-gitlab/skills/kaola-workflow-finalize/SKILL.md, plugins/kaola-workflow-gitea/skills/kaola-workflow-finalize/SKILL.md | 1 | sequence | sonnet | #429 finalize-sink prose ×6 (#400). Route the manual 8-step worktree-sink choreography to the new resumable `sink` transaction (replacing the hand-described stash/rm/merge/cp/finalize/close/archive/push). CONFLICT-AVOIDANCE: write ONLY a NEW sink-routing subsection (near the existing sink-merge wiring); do NOT touch the "## Run gaps" / `gaps_unswept` Step-8 gate region that #435 is adding — keep the sink prose structurally separate so the sink merge is conflict-free regardless of #435's landing order. Sequenced AFTER n5; canonical spec = mirror root command wiring into all six surfaces modulo forge nouns. docs-only (.md). |
| n7_doc_contractor | doc-updater | n6_doc_finalize | agents/contractor.md, plugins/kaola-workflow/agents/contractor.toml, plugins/kaola-workflow-gitlab/agents/contractor.toml, plugins/kaola-workflow-gitea/agents/contractor.toml | 1 | sequence | sonnet | Contractor prose for the new flows (#429 sink routing in the Step-8a/8b/7/8 finalization procedure; #434 `inline_execution_suspected` verifier note). contractor.md is the canonical; the 3 tomls are byte-mirrors modulo forge nouns — keep them edition-neutral (no `gh`/`glab`/`tea` binary names, no forge brand). CONFLICT-AVOIDANCE: add a DISTINCT subsection for the sink-routing/resume note; do NOT touch the #435 gap-sweep Step-8 invocation region. The 3 .toml files are NON-docs → this node produces code → post-dominated by n_review (G1). Sequenced LAST among doc nodes so it factors in #435's known write areas. |
| n_review | code-reviewer | n_pins, n7_doc_contractor | — | 1 | sequence | opus | G1 gate — post-dominates every code-producing node (n2, n3 implements; n_pins validator; n7 contractor tomls). Mutation-aware review: the sink_blocked preflight never touches foreign files; the receipt makes resume idempotent (no double-apply); repair-node truly REUSES the original baseline (no laundering re-snapshot); revert-overflow scopes git checkout to exactly outOfAllow paths in the barrier root; edition ports faithful (no forge-noun leak in scripts/SKILLs; root↔codex byte-identical); the contractor tomls stay forge-neutral. Emit lowercase `verdict: pass` + `findings_blocking: 0`. Opus: strong gate over the sonnet writers; high-blast-radius git-surgery primitives. |
| n_decisions | doc-updater | n_review | docs/decisions/D-429-01.md, docs/decisions/D-434-01.md | 1 | sequence | sonnet | Author the two decision records (next-free at authoring: docs/decisions/ has no D-429-* or D-434-*). D-429-01: the script-owned resumable worktree-sink transaction + receipt + sink_blocked preflight. D-434-01: the sanctioned repair primitives (revert-overflow / repair-node original-baseline-reuse / requires_redispatch resume contract). docs-only (docs/decisions/). After n_review so the records reflect the reviewed shape. |
| n8_finalize | finalize | n_decisions | CHANGELOG.md | 1 | sequence | — | Unique docs/state sink (no model — never dispatched as a subagent). Run all four chains (npm run test:kaola-workflow:{claude,codex,gitlab,gitea}) sequentially — this is a cross-edition diff (#307): scripts/ COMMON_SCRIPT + GENERATED_AGGREGATOR + forge ports + ×6 prose surfaces. Add the [Unreleased] CHANGELOG entry covering #429 (script-owned resumable worktree sink) and #434 (sanctioned repair primitives). Docs/state-only write. |

## Plan Notes

- **Conflict avoidance with #435 (in-progress, primary instruction).** #435 owns: `commands/workflow-next.md` Goal-Driven Autonomy; the finalize `## Run gaps`/`gaps_unswept` Step-8 gate (in finalize commands/SKILLs + contractor Step-8); a NEW `scripts/kaola-workflow-gap-sweep.js` + its COMMON_SCRIPTS/install-manifest lines. This plan touches NONE of workflow-next.md, gap-sweep, COMMON_SCRIPTS, or install.sh. The two prose surfaces shared with #435 — finalize commands/SKILLs (n6) and contractor (n7) — are entered ONLY in new sink-routing/resume subsections structurally separate from #435's gap-capture region, and are sequenced LAST (n5→n6→n7, all after the code chain) so they factor in #435's known areas. Result: the sink merge stays conflict-free regardless of which session sinks first.
- **File-lane sequencing (not fan-out).** n2 and n3 are genuinely independent features but both write under `scripts/` (top-level-dir-granular disjointness would refuse a fan-out) → SEQUENCED. The validate-workflow-contracts pin is hoisted into a dedicated n_pins node (root + byte-identical codex peer, #274/#301) so n2/n3 stay under FILE_CEILING=6. The three doc nodes are sequenced for deterministic #435-conflict-avoidance ordering.
- **Edition-port mechanics.** kaola-workflow-sink-merge.js is a COMMON_SCRIPT (codex byte-copy + 2 hand-ported forge ports per rename map) — n2 declares all four. kaola-workflow-adaptive-node.js is a GENERATED_AGGREGATOR (edition-sync.js --write regenerates the codex byte-copy + 2 forge ports; edition-sync.js --check gates the gitlab/gitea chains) — n3 declares the canonical + the three regenerated ports. validate-workflow-contracts.js is a COMMON_SCRIPT with a byte-identical codex peer — n_pins declares both. No new agent role is introduced, so the 22-path agent-registration surface (#340) does NOT apply.
- **Decision records.** docs/decisions/ has no D-429-* or D-434-* at authoring; D-429-01 and D-434-01 are the next-free numbers (the series is per-issue, each issue starts at -01). They are written by n_decisions (code-architect is read-only and cannot declare a write set).
- **Cross-edition diff (#307).** This bundle touches the edition trees (forge sink-merge ports, adaptive-node forge ports, ×6 prose surfaces, contractor tomls) → all four `npm run test:kaola-workflow:{claude,codex,gitlab,gitea}` chains must be green (run sequentially) before finalize; a green claude chain alone is insufficient.
- **Gates.** G1: n_review (code-reviewer) post-dominates every code-producing node — n2, n3 (implements), n_pins (validator, non-docs), n7 (contractor tomls, non-docs). No sensitive label → no G2 security gate. No GPU/device/human-sign-off acceptance → no main-session-gate (G3). The doc-updater chain precedes finalize because docs/public-interface prose changed.

## Node Ledger

| id | status |
|------|--------|
| n1_design | complete |
| n2_impl_sink | complete |
| n3_impl_repair | complete |
| n_pins | complete |
| n5_doc_planrun | complete |
| n6_doc_finalize | complete |
| n7_doc_contractor | complete |
| n_review | complete |
| n_decisions | complete |
| n8_finalize | in_progress |
## Required Agent Compliance

| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| code-architect (n1_design) | subagent-invoked | evidence-binding: n1_design 0fa58782000d | |
| tdd-guide (n2_impl_sink) | subagent-invoked | evidence-binding: n2_impl_sink 3c9cd2db0d1b | |
| tdd-guide (n3_impl_repair) | subagent-invoked | evidence-binding: n3_impl_repair afd4bf8a4acf | |
| implementer (n_pins) | subagent-invoked | evidence-binding: n_pins 0e0e0c8e1ce3 | |
| doc-updater (n5_doc_planrun) | subagent-invoked | evidence-binding: n5_doc_planrun 4222a5918b60 | |
| doc-updater (n6_doc_finalize) | subagent-invoked | evidence-binding: n6_doc_finalize e96dd0258bba | |
| doc-updater (n7_doc_contractor) | subagent-invoked | evidence-binding: n7_doc_contractor e0829e5474aa | |
| code-reviewer | subagent-invoked | evidence-binding: n_review 2b5922824354 | |
| doc-updater (n_decisions) | subagent-invoked | evidence-binding: n_decisions 38a65f17c0e0 | |
