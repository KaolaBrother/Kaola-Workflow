# n3 · finalize — docs/state sink (#275)

Added a `### Fixed` entry under `## [Unreleased]` in CHANGELOG.md describing the clearAdvisoryClaim
marker-deletion fix: bug, root cause (issueHasRemoteClaimComment never superseded by the cleared
comment), the option-(a) fix (project-scoped marker delete via repo-level
`issues/comments/{id}` endpoint), all four threaded call sites, the OFFLINE invariant, byte-identical
canonical+Codex copies, the three new walkthrough regressions, and the GitLab/Gitea + null-project
follow-ups.

Write set: CHANGELOG.md only. No code/state changes in this node.
