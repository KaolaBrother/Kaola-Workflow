# Documentation Docking — issue-275

## Changed files reviewed (git diff)
- `scripts/kaola-workflow-claim.js` — `clearAdvisoryClaim` gained a `project` param + project-scoped marker-comment deletion; 4 call sites threaded.
- `plugins/kaola-workflow/scripts/kaola-workflow-claim.js` — byte-identical Codex mirror of the above.
- `scripts/simulate-workflow-walkthrough.js` — 3 new RED→GREEN regressions.
- `CHANGELOG.md` — `### Fixed` entry under `[Unreleased]` (written by node n3).

## Documents checked
| Document | Impact | Disposition |
|----------|--------|-------------|
| CHANGELOG.md | yes | DOCKED — entry added (n3). |
| README.md | none | No feature/usage/env-var change. `clearAdvisoryClaim` is an internal function; the `discard`/`release`/`finalize`/`watch-pr` CLI contracts and `--json` output shapes are unchanged (behavior only improves: the stale marker is now cleaned). |
| docs/api.md | none | No CLI subcommand signature, schema, event, or `--json` field changed. The fixed function is not part of any documented external contract. |
| docs/architecture.md | none | No structural/data-flow change. |
| docs/workflow-state-contract.md | none | No durable-state field added/changed. |
| .env.example | none | No new environment variable. |
| docs/conventions.md | none | No convention change. |
| roadmap (.roadmap/ + ROADMAP.md) | yes | Handled by closure (roadmap source removed, mirror regenerated by cmdFinalize). |

## Gaps found and fixed
None. The only doc surface impacted is CHANGELOG, already docked by node n3.

## doc-updater
SKIPPED — explicit no-impact reason: no public behavior, API/CLI, setup, architecture, environment, or roadmap-impacting change beyond the CHANGELOG entry already written by node n3. (Skip-with-reason preferred over a drift-prone duplicate doc.)

## Verdict: DOCKED
