# Finalization - Summary: issue-515

## Delivered
Reciprocal switch-ON path guard (#515) — adaptive is now a non-bypassable, zero-deliberation default under an ON switch:
- **Script guard** in `claimProject` (all 4 claim.js editions): refuses a *defaulted* fast/full claim under an ON switch with typed `path_requires_explicit_opt_in` (the reciprocal of `authoring-allowed`, #235). Predicate `pathWasDefaulted = !args.workflowPath && !process.env.KAOLA_PATH`, fires on `adaptiveEnabled && pathWasDefaulted`.
- **Prose floor** on the 12 fast+full-entry surfaces (`<!-- PIN: adaptive-default-contract -->` + `path_requires_explicit_opt_in`), machine-pinned by a fail-closed T11 block in `scripts/test-route-reachability.js`.
- **Test-hermeticity fix** to all 6 walkthrough harnesses (default `KAOLA_ENABLE_ADAPTIVE='0'`) since the guard refuses every defaulted bare startup under ON.

## Files Changed
27 deliverable files (committed as 5135fac0):
- 4× claim.js editions (guard) + `scripts/test-claim-hardening.js` (6 RED→GREEN cases)
- 6× walkthrough harnesses (hermeticity)
- 12× fast+full-entry surfaces (prose) + `scripts/test-route-reachability.js` (T11)
- `docs/decisions/D-515-01.md` (new) + `docs/conventions.md` (§ Switch-ON path guard)
- `CHANGELOG.md` ([Unreleased] → Fixed)

## Test Coverage
`scripts/test-claim-hardening.js` 87 assertions; `scripts/test-route-reachability.js` 170 assertions (+24 for T11). Behavior-change covered by RED→GREEN in the hardening suite.

## Final Validation Evidence
4-chain green via `kaola-workflow-run-chains.js` → `.cache/chain-receipt.json` (headSha 5135fac0 = HEAD, workTreeHash clean): claude=0 (590s), codex=0, gitlab=0, gitea=0. Also independently run by the orchestrator with the full diff before commit (all 0). Adaptive barrier gates: resume=0 gate=0 barrier=0 verdict=0. Validation reuse covers code/test impact through node n6; the finalize-node bookkeeping (archive + roadmap) is non-code and outside the rerun trigger.

## Documentation Docking
DOCKED — D-515-01 decision record + conventions § Switch-ON path guard + CHANGELOG cover every public-interface/routing-contract change; the guard's typed token and the prose pin are cross-referenced. n5-doc evidence: `.cache/n5-doc.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- n4-review LOW (non-blocking, cosmetic): the T11 block comment in `scripts/test-route-reachability.js` says "3 Claude commands" while the surface list correctly spans root + gitlab + gitea command editions of the Claude command surface. Wording-only; the asserted surface list is correct. Deferrable.

## Closure Decision
No deferred items, unresolved conflicts, or partial implementation. All 6 plan nodes complete; gate verdict pass (findings_blocking: 0). One cosmetic follow-up noted above. Issue closes.

## Commit And Push
pending final Git gate.

## GitHub Issue
#515 — to be closed on sink.

## Roadmap
to be regenerated by cmdFinalize (removes `.roadmap/issue-515.md`).

## Archive
pending (cmdFinalize).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked (n5-doc node) | .cache/n5-doc.md | |
| documentation docking | invoked | this summary § Documentation Docking | |
| final-validation fix executors | N/A | no failures | no routed fixes needed |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | cmdFinalize |
| archive completed folder | pending | | cmdFinalize |
| final commit and push | ready | git status/receipt/upstream check | final gate after this file |

## Status
ARCHIVED AFTER FINAL GIT GATE
