# Finalization - Summary: issue-543

## Delivered
Ported the #538 `--with-fast`/`--with-full` install-time opt-in partition to **Codex + opencode** so a default install on every runtime (Claude, Codex, opencode) ships **adaptive-only**, with fast/full reachable only via explicit opt-in. Also fixed the folded **#544-run-discovered** opencode `.claude/` path-leak (opencode is now standalone). Shipped as a 10-node adaptive DAG (two parallel implementation lanes → dual opus code-review gates → doc sink).

- **Codex (Option A — installer config-writer):** the runtime legality gate (`kaola-workflow-claim.js:849-863` → `path_not_installed`) was already shipped; this run added the missing install-time writer. `install-codex-agent-profiles.js` (×3 byte-identical triplet) gained `--with-fast`/`--with-full` arg parsing + a pure-JS `seedKaolaConfig` UNION writer byte-semantically mirroring `install.sh:712-734` D4. No `plugin.json`/version change.
- **opencode (partition + standalone fix):** `install-opencode.sh` gained the same partition; `sync-opencode-edition.js transformCommandBody` rewrites the `kaola_script()` resolver to opencode-native paths → regenerated `.opencode/` tree has ZERO `CLAUDE_PLUGIN_ROOT`/`.claude/kaola-workflow` tokens (146→0).

## Files Changed
- Codex: `plugins/kaola-workflow{,-gitlab,-gitea}/scripts/install-codex-agent-profiles.js` (triplet); `plugins/kaola-workflow/scripts/simulate-kaola-workflow-walkthrough.js`; `plugins/kaola-workflow-{gitlab,gitea}/scripts/test-{gitlab,gitea}-workflow-scripts.js`; `scripts/test-install-model-rendering.js`.
- opencode: `install-opencode.sh`; `scripts/sync-opencode-edition.js`; `docs/opencode-edition.md`; regenerated `.opencode/**` (14 files: 12 commands + contractor + workflow-planner agents).
- Tests: `scripts/test-opencode-edition.js` (n4 RED block, +45 assertions).
- Docs: `docs/decisions/D-543-01.md` (new); `README.md` (+8/-2, pinned version lines intact); `CHANGELOG.md`.

## Test Coverage
All four cross-edition `npm run test:kaola-workflow:{claude,codex,gitlab,gitea}` chains green (#307). opencode suite `test-opencode-edition.js` 363 assertions green (incl. absence assertion A). 10 Codex `installed_paths` partition sub-cases pass.

## Final Validation Evidence
- Chain receipt: `.cache/chain-receipt.json` — claude/codex/gitlab/gitea all exit 0 (no accepted_red).
- Adaptive barrier gates: `--resume-check` (RC=0), `--gate-verify` (GV=0), `--barrier-check` (BC=0), `--verdict-check` (VC=0) — all PASS.
- Code-review gates: n7 (opencode) PASS 0-blocking; n8 (Codex) PASS 0-blocking.
- Note: one transient environmental flake during the first chain run (claim-hardening #538(b) faults on GitHub TLS handshake timeouts / API rate limits — the classifier subprocess ETIMEDOUT). Not a regression (no change to `kaola-workflow-claim.js`/classifier); cleared on retry.

## Documentation Docking
DOCKED — `docs/decisions/D-543-01.md` records both mechanisms; `README.md` reflects cross-edition behavioral parity; `CHANGELOG.md` entry under `## [Unreleased]`; `docs/opencode-edition.md` documents the partition (no longer "scoped out"). Pinned README version lines undisturbed (validator green).

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (transient) claim-hardening #538(b) classifier ETIMEDOUT | environmental flake (GitHub TLS/rate-limit) | orchestrator retry | chain receipt (re-run green) | resolved (not a product defect) |

## Follow-Up Items
- #549 — Codex `kaola-workflow-init/SKILL.md` opt-in discoverability prose ×3 (deferred from this run; see Run gaps).

## Run gaps
- deferred-docs (Codex SKILL.md opt-in prose ×3): filed: #549

## Closure Decision
Implementation complete; all acceptance criteria of #543 met (default Codex+opencode install = adaptive-only; opt-in reachable; four chains green; opencode standalone). One run-discovered doc gap filed (#549). No unresolved user-decision items.

## Commit And Push
pending final Git gate (sink) — awaiting user authorization for the irreversible merge-to-main + issue close.

## GitHub Issue
#543 — close pending sink authorization.

## Roadmap
to be regenerated by cmdFinalize at archive (Step 8b).

## Archive
pending sink.

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater (n9) | invoked | .cache/n9-doc-updater.md (D-543-01 + README) | |
| documentation docking | invoked | this file (DOCKED) | |
| final-validation fix executors | N/A | (transient flake resolved by retry, no routed fix) | environmental |
| code-reviewer opencode (n7) | invoked | .cache/n7-code-reviewer-opencode.md (PASS) | |
| code-reviewer codex (n8) | invoked | .cache/n8-code-reviewer-codex.md (PASS) | |
| finalize sink (n10) | invoked (main-session-direct) | .cache/n10-finalize.md (CHANGELOG) | |
| roadmap refresh | pending | cmdFinalize Step 8b | |
| archive completed folder | pending | | sink |
| final commit and push | ready | git status/upstream check | runs at sink |

## Status
ARCHIVED AFTER FINAL GIT GATE (sink) — awaiting user authorization
