# Documentation docking — #1004

**task**: dock the `appendSummarySection` fill-if-empty fix into `CHANGELOG.md` (`[Unreleased]`) and
`docs/api.md` ("### The three reports"). Write set: those two files only.

**verification tier**: `build-green` — prose-only edits, no executable behaviour. Verified by
re-reading the spliced regions, checking wrap width against the surrounding files, and confirming
`git diff --stat` touches only the two assigned files. The suite was NOT run (instructed not to;
another agent holds `scripts/`).

**files changed** (worktree `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/issue-1004`):

- `CHANGELOG.md` — +42 lines, one entry at the head of `[Unreleased]` → `### Fixed` (line 209).
- `docs/api.md` — +10 lines, one paragraph in `### The three reports`, immediately after the
  envelope-field/durable-heading table (line 287).

`git diff --stat` also lists `scripts/simulate-workflow-walkthrough.js` and
`scripts/test-finalize-door.js`; those are the concurrent agent's, untouched by me.

## Figures — independently re-measured, not copied

Every figure in the brief was re-derived here before use, over
`kaola-workflow/archive/*/finalization-summary.md` at `maxdepth 2` (the `.cache/` copies at depth 3
are excluded, which is what makes the corpus 157 rather than 162), using the same
heading-to-next-`## `-or-EOF slice the writer itself uses:

| | present | empty |
|---|---|---|
| `## Validation` | 49 | 15 |
| `## Changed Paths` | 48 | 17 |
| `## Mission List` | 12 | 3 |

corpus = 157. All six figures match the brief exactly. (The memory index's "16 of 49" is off by one
against this measurement; the brief's 15 is right.)

Also confirmed first-hand before citing:

- The Step 6 quote is exact — `templates/routing/finalize.skeleton.md:233` reads "do not delete
  them, and do not soften them", above the three headings in the summary skeleton.
- `docs/decisions/D-653-01.md` really does accept the pre-seed residual (its "Does not close the R1
  pre-seed residual … accepted as fenced by the `## Closure` block + stdout receipt"), and the
  `## Attestation` producer is gone: `validate-workflow-contracts.js:730` asserts
  `scripts/kaola-workflow-claim.js` does **not** contain the string `## Attestation`.
- `appendClosureBlock` writes `archived_at`, `issue_disposition`, `claim_label_removed`,
  `worktree_removed`, `closure_invariants`, `issues_closed`, `follow_ups_filed`,
  `follow_up_numbers`, `net_backlog_delta` — none of `validation`, `changed_paths`, `mission_list`.
  So the D-653-01 fence genuinely does not transfer.

Two figures from the brief I used without being able to re-measure: "exactly three door assertions
red under a relocating fill", and "no test anywhere pre-created one of the three headings in a
fixture". I sampled the second at `3380cafe` — every planted `finalization-summary.md` in
`test-finalize-door.js` and `simulate-workflow-walkthrough.js` I found writes `# Finalization
Summary`, `# summary` or an `Evidence:` line, none of the three `## ` headings — consistent, but not
exhaustive. The **846** assertion denominator was deliberately dropped (see below).

## CHANGELOG.md — exact text added

Placed first under `[Unreleased]` → `### Fixed` (which already existed at the file's conventional
position, after `### Added`), ahead of the #1002 entry, matching the section's newest-first order:

```
- **The three finalize sections are now filled when the heading is already there — #1004.**
  `appendSummarySection` declined to write whenever its heading already existed, so a
  `finalization-summary.md` that already carried `## Validation`, `## Changed Paths` or
  `## Mission List` had the measurement computed for it and then dropped, silently, at exit 0. What
  put the heading there is the finalize surface's own Step 6, which tells the orchestrator to
  pre-create exactly those three and says "do not delete them, and do not soften them" — so obeying
  the surface literally is what defeated the mechanism the surface exists to feed. Measured over this
  repo's **157** archived summaries: `## Validation` present 49, **15 empty**; `## Changed Paths`
  present 48, **17 empty**; `## Mission List` present 12, **3 empty**. The filled ones are the runs
  whose summary did not pre-create the heading, so the append path was taken; the two outcomes
  differ by nothing else.
  **The loss is asymmetric, which is why an empty heading reads as harmless.** On a green run the
  dropped section restates a receipt that was fine anyway. On a run whose chain receipt was stale,
  red, empty or absent, `## Validation` is the only durable place that finding survives — the
  envelope is gone when the process exits, and the empty heading is what a successor reads instead.
  The writer is now **fill-if-empty, in place**. A heading that is absent is appended at the tail,
  unchanged from before; a heading present with an empty body is filled where it already sits,
  keeping its position relative to its neighbours; and **a heading already carrying content is left
  exactly as written and never overwritten**. It is therefore idempotent by **content rather than by
  heading**, which is what preserves the guard's original purpose — a crash-resumed re-entry still
  cannot stack a second copy of a section that already says something. The `replace: true` caller
  (`## Finalize Findings`) is **untouched**, including that it still restates its section at the
  tail. All **four** shipped `kaola-workflow-claim.js` copies move together: canonical, the
  byte-identical Codex plugin copy, and the GitLab and Gitea hand-ports. No prose surface changed
  and nothing was regenerated — Step 6 still ships the three headings, and that is now correct
  rather than harmful.
  **The guards are mutation-proven, not assertion-counted.** New behavioural legs in
  `test-finalize-door.js` drive real finalize subprocesses over planted summaries, and a new
  four-copy pin in `simulate-workflow-walkthrough.js` lifts each copy's function out of its own file
  and drives it against real bytes, one copy mutated at a time. A fill that relocates the section to
  the tail instead of filling it in place reds exactly three door assertions, and all three are the
  new ones. That coverage was owed: before this change, **no test anywhere pre-created one of the
  three headings in a fixture**, so the entire durable-half contract was green over the
  create-if-absent branch alone, and the declining branch — the one that was wrong — was untested
  in every suite.
  `docs/decisions/D-653-01.md` accepted this same declining behaviour as a known residual in July,
  and is **not contradicted**: it ruled on a `## Attestation` section a contractor could pre-seed,
  accepting it as fenced by the `## Closure` block and the stdout receipt. That producer no longer
  exists in any copy — `validate-workflow-contracts.js` asserts `claim.js` does not contain the
  string — and the reasoning does not transfer, because `## Closure` carries none of `validation`,
  `changed_paths` or `mission_list`.
```

## docs/api.md — exact text added

Inserted in `### The three reports`, between the envelope/heading table and the
`changed_paths_probe` paragraph:

```
**The durable write is fill-if-empty, and it never overwrites prose.** All three land through one
writer, `appendSummarySection`, and what it does turns on what the heading already holds: absent,
and the section is appended at the tail; present with an empty body, and it is filled **in place**,
keeping its position relative to its neighbours; present with content, and it is left exactly as
written. So the transaction never overwrites prose an orchestrator wrote, and the section is
idempotent by **content rather than by heading** — a crash-resumed re-entry still cannot stack a
second copy of a section that already says something, while a summary that pre-created the three
headings, as the finalize surface's Step 6 instructs, receives the measurements instead of dropping
them. `## Finalize Findings` is written by the same function under a different rule (see below).
```

## Surrounding sentences judged, and what was done

1. `docs/api.md:277` — "None refuses, and each lands in two places — the emitted envelope and,
   durably, `kaola-workflow/{project}/finalization-summary.md`. The durable half is not optional: a
   conversion that emits a finding and drops the state the refusal was freezing is a deletion, not a
   conversion." **Left byte-untouched.** It was the aspirational sentence; it is now descriptive, and
   the new paragraph three lines below is what makes it so. Rewriting it would have removed the
   normative claim that justifies the fix.
2. `docs/api.md` `## Finalize Findings` paragraph (now ~line 407) — "The write is **restating, not
   append-once** … an append-once flush would instead have dropped every fault recorded after it".
   **Left untouched, and my text is scoped so it cannot be read onto it**: my last sentence says
   `## Finalize Findings` is "written by the same function under a different rule (see below)", and
   nothing in my paragraph claims restating for the three reports. Its counterfactual also survives
   the fix unchanged: under the new rule a second flush still finds a section carrying content and
   still declines, so "would have dropped every fault recorded after it" remains exactly true.
3. `docs/api.md:~931` — the sink's `## Sink Findings` writer, "idempotent across a crash-resumed
   re-entry". **Untouched and unaffected**: that is `persistSinkFindingsToSummary` in
   `kaola-workflow-sink-merge.js`, a separate function still guarding on heading presence. My
   paragraph is scoped to the three reports and to `appendSummarySection` by name, so it does not
   reach it.

## Deliberately left out

- **The 846-assertion denominator.** "reds exactly 3 of 846" became "reds exactly three door
  assertions". A suite-wide total decays every time a leg is added and cannot witness anything about
  this pin; the arming claim is carried by *which* three red, which is what I kept.
- **The 157/162 discrepancy** and the corpus-depth rule. It is a measurement artefact of how the
  census was taken, not a user-visible fact; recorded here instead so the figure can be reproduced.
- **A "future work" or migration note.** Nothing to migrate: existing summaries with a filled section
  are left alone by construction, and existing empty ones simply get filled on the next finalize.
- **`docs/workflow-state-contract.md` and `README.md`.** Outside the assigned write set; I have no
  evidence either states the declining rule, and did not go looking beyond `docs/api.md`.

## Prose checked against the shipped code, after the implementer landed

The code change was not yet in the worktree when I began (only the two test files were modified), so
I documented from the brief and re-checked afterwards. `scripts/kaola-workflow-claim.js` has since
landed and the shipped `appendSummarySection` matches what both edits state, branch for branch:

- heading absent → falls through to the unchanged tail append;
- heading present, body blank → spliced in place ("Fill-if-empty, SPLICED WHERE IT SITS … the
  document order is the orchestrator's, not this writer's");
- heading present, body non-blank → `return false`, nothing written;
- `replace` → still cuts and re-appends at the tail, so `## Finalize Findings` still relocates.

Reach confirmed by diff: all four `*claim.js` copies (canonical, `plugins/kaola-workflow`, gitlab,
gitea). `templates/` and `commands/` are untouched, so the CHANGELOG's "no prose surface changed and
nothing was regenerated" holds.

Two comment-level findings I had raised are already resolved by the implementer and need no action:
`claim.js:4237` and `:5202` now read "idempotent BY CONTENT (#1004)", and the function header no
longer says "the heading is checked first". `grep -rn "idempotent by heading" scripts/ plugins/`
returns nothing.

`kaola-workflow-sink-merge.js` also changed in that diff — comment-only, and it confirms rather than
contradicts item 3 above: it now says `## Sink Findings` "parts company with them on idempotence:
this one is guarded on the HEADING's presence". My `docs/api.md` paragraph names
`appendSummarySection` explicitly for exactly this reason, so it cannot be read onto the sink writer.
