# #1004 + #1005 + #1006 — stop the finalize summary silencing its own findings, then make the axiom layer single-sourced and give the premise check a rule surface

Claim record note: the claim was made for #1004 alone and is NOT being widened — owner ruled at the
midpoint that #1005 and #1006 join this run without a re-claim, because releasing would have removed
a worktree with a live agent in it and would have coupled #1004's finished fix to #1005's pending
values call under all-or-nothing closure. Branch `workflow/issue-1004` and folder `issue-1004` stand.
Finalize closes #1004; **#1005 and #1006 are closed explicitly after the sink, verified CLOSED rather
than trusted on exit 0.** Owner-ruled sequencing from #1005's own comment: **#1005 lands before
#1006 starts** — they edit the same lines of the same files. #1004 shares only
`scripts/simulate-workflow-walkthrough.js` and `CHANGELOG.md [Unreleased]` with them.

- item: Re-verify #1004's premise at HEAD `3380cafe` — that `appendSummarySection` still returns without writing when the heading exists (claim.js:3940-3946), that all three producers (`## Validation` :3976, `## Changed Paths` :3988, `## Mission List` :4057) still call it without `replace`, and that the corpus figure holds (49 archived summaries carry `## Validation`, 16 empty). The issue's evidence was measured at `b52939f6`, one commit back.
  status: done
  dispatched: investigator subagent, read-only over the main root, writes to kaola-workflow/issue-1004/findings/premise-recheck.md
  result: kaola-workflow/issue-1004/findings/premise-recheck.md — premise SURVIVES; nothing FALSE. Guard holds; Step 6 template holds (9 live copies: skeleton + 6 tracked renders + 2 untracked additive-edition renders). Two things moved: (a) the enumeration is materially incomplete — 12 affected call sites across 4 shipped copies of claim.js (canonical, byte-identical github plugin copy, gitlab and gitea hand-ports), plus a 4th caller per copy (`## Finalize Findings`, :4266) that already passes `replace: true` and is immune; one distinct function body across all four, sha=1b706fc826fe; the `false` return is read by nothing anywhere. (b) the corpus figure is 15 of 49 empty at HEAD, not 16 — 16 existed only in the uncommitted Leg-A tree; bundle-1001-1002's own summary is filled, confirming the hand-recovery. Also measured by EXECUTING the shipped bytes: `replace: true` MOVES the section to end of file (cut then append). Unestablished: whether any test arms this behaviour (declined to infer).

- item: Map the blast radius of both remedy shapes — which authoring skeleton in templates/routing/ owns the Step 6 summary template and which generated surfaces + runtimes it reaches, which tests pin `appendSummarySection` or the three headings, and whether a non-destructive middle path (fill a heading that exists but is empty; decline only when it carries content) is available. Facts only, no recommendation.
  status: done
  dispatched: Explore subagent, read-only over the main root, writes to kaola-workflow/issue-1004/findings/blast-radius.md
  result: kaola-workflow/issue-1004/findings/blast-radius.md — the agent had no write tool (dispatch error, Explore is read-only), returned inline; orchestrator transcribed it to that path in full. Headlines: (a) the third shape IS mechanically available with NO new parsing — the `after` endpoint is already computed one line below the early return, so fill-if-empty is a hoist; (b) ZERO tests pre-create any of the three headings in a fixture, so the entire durable-half contract is green over the create-if-absent branch only — the `if (existing)` arm is untested everywhere; (c) gitlab/gitea `appendSummarySection` is guarded by NOTHING (edition-sync excludes claim.js by design; the one 4-copy pin covers `persistValidationToSummary`'s body, not this function); (d) `docs/decisions/D-653-01.md:65-72` already ACCEPTED this exact class for `## Attestation` in 2026-07-11, but its subject is gone and its two-channel-fence reasoning does not transfer — `appendClosureBlock` carries none of the three fields; (e) 12 rendered surfaces carry the Step 6 template, md5-identical, and the "do not delete them" sentence is pinned WHOLE at required-blocks.js:235; (f) 4 in-source prose comments assert "idempotent BY HEADING" and go false under shapes 1/2.

- item: Put the values call to the user with both measurements in hand — `replace: true` makes the transaction authoritative and overwrites whatever an orchestrator wrote under those headings; dropping the headings from the Step 6 template touches only prose but leaves the guard armed for anyone writing them by hand. This is an irreversible-semantics choice about whose text wins, so it is the user's, not mine.
  status: done
  dispatched: self, asked in conversation with all four shapes and their measured costs
  result: OWNER RULED **fill-if-empty** (shape 3). The transaction fills a heading that exists but carries nothing, and still declines when the section has content — an empty heading gets filled, a filled one is left exactly as the human wrote it, an absent one is appended as before. Code-only: 1 function × 4 copies, 0 surfaces regenerated, 0 pinned sentences reworded. `replace: true` was DECLINED (it would discard human prose and relocate the section to end of file); prose-only was DECLINED (leaves the mechanism armed). I ruled out "always write" on facts, not values: it yields duplicate headings and `test-finalize-door.js:384` reads the FIRST match, so it would report worse than today.

- item: Author the tests for the fill-if-empty behaviour and prove them RED on current code — a behavioural pair in test-finalize-door.js (empty headings get filled; a heading carrying content is preserved verbatim) plus a four-copy source pin on `appendSummarySection` in the walkthrough, since gitlab and gitea ride on nothing today. Test custody: this role never writes production code.
  status: done
  dispatched: tdd-guide subagent, working in the worktree at .kw/worktrees/issue-1004 with absolute paths; edits scripts/test-finalize-door.js + scripts/simulate-workflow-walkthrough.js only; writes its RED transcript to kaola-workflow/issue-1004/findings/red-baseline.md in the MAIN root
  result: kaola-workflow/issue-1004/findings/red-baseline.md, baseline 3380cafe. Two artifacts, both RED, no production code touched. `T17` a/b/c in test-finalize-door.js drives real finalize subprocesses over planted summaries ("`## Validation` was planted EMPTY ... the finding must be IN it; got \"\""); door suite went 791 assertions passing -> 16 failures / 827 passed, and a candidate fix applied to a SCRATCH COPY of the tree took it to 843 passing, exit 0 -- so T17 is satisfiable and carries no pre-existing regression. `testFillIfEmptySummarySectionPortedToAllEditions1004` in the walkthrough is MUTATION-PROVEN one copy at a time against a scratch tree: gitea-unported reds naming gitea, canonical-unported reds naming canonical, gitlab+gitea unported reds naming gitlab, all four patched PASSES. It deliberately departs from #1002's regex idiom -- this rule has an observable, and `if (!replace) return false;` is CORRECT nested inside a non-empty branch, so a regex would forbid a valid fix; instead each copy's function is lifted from its own file and driven against real bytes, behind a harness-control leg (absent heading -> appended) that proves the sandbox evaluates the shipped function rather than silently swallowing into `catch (_) { return false; }`. Left unpinned and flagged rather than decided: section ORDER.

- item: Apply the fill-if-empty change to all four claim.js copies and correct the in-source prose that asserts "idempotent BY HEADING" (claim.js:3929-3939, :4237-4238, :5202 and sink-merge.js:264-266, each ×4/×3 copies). Implementer reads and runs the tests but never edits them.
  status: done
  dispatched: implementer subagent in the worktree at .kw/worktrees/issue-1004; write set is the 4 claim.js copies + canonical sink-merge.js comment ONLY, tests strictly off-limits; runs test-finalize-door.js, the walkthrough pin by name, validate-script-sync.js and generate-routing-surfaces --check; writes to kaola-workflow/issue-1004/findings/implementation-1004.md
  result: kaola-workflow/issue-1004/findings/implementation-1004.md. SIX files, not five — it also mirrored `plugins/kaola-workflow/scripts/kaola-workflow-sink-merge.js`, correctly and with the reason flagged: sink-merge is in validate-script-sync's COMMON_SCRIPTS, so the canonical comment edit FORCES the codex mirror or the guard reds. The change is the specified hoist (`after` lifted above the early return and reused, `block` hoisted so the fill branch can write it), no second scan, no second read, no new argument; `replace: true` still cuts and re-appends at the tail. Four comment sites rewritten from "idempotent BY HEADING" to by CONTENT. Verification with each exit code read directly: door suite 1 -> 0, `846 assertions`, the exact number the test author measured against a correct candidate; walkthrough pin 1 -> 0; validate-script-sync 0 (14 common scripts, 27 byte-identical groups); generate-routing-surfaces --check 0 (18 surfaces byte-match). `cmp` exit 0 canonical<->codex on both claim.js and sink-merge.js. It ALSO mutation-proved the two hand-ports itself, one at a time, snapshot-and-restore: gitea mutant -> pin reds naming gitea; gitlab mutant -> pin reds naming gitlab. Left as found and reported: the gitlab/gitea sink-merge stale `## Attestation` comment (pre-existing rot, out of scope). Corner cut recorded: the fill path rewrites only the section being filled, not whole-file blank-run normalization.

- item: Prove the new four-copy pin is actually armed by mutation, one call site at a time — an assertion count cannot witness a pin, and a mutant that reds nothing is more likely broken than redundant.
  status: done
  dispatched: folded into the tdd-guide dispatch above; evidence in the same transcript
  result: DONE and one-at-a-time, in kaola-workflow/issue-1004/findings/red-baseline.md. Mutants run against a SCRATCH COPY of the tree, never the worktree: gitea-unported reds naming gitea; canonical-unported reds naming canonical; gitlab+gitea unported reds naming gitlab; all four patched PASSES. This is the arming proof the project's own rule demands — the assertion count alone could not have witnessed it.

- item: Decide the one thing the test author flagged and stopped on rather than deciding — section ORDER. A cut-and-append fill would relocate `## Validation` below `## Status: ARCHIVED AFTER FINAL GIT GATE`; filling in place keeps it where Step 6 put it. Orchestrator ruled FILL IN PLACE (the ruling says the script fills the heading, not that it moves it), with the existing `replace: true` tail behaviour left untouched since that is a working path and not this issue's business. Needs the assertion added by test custody, not by the implementer.
  status: done
  dispatched: the SAME tdd-guide agent resumed with its context intact (it offered this assertion and asked for the ruling); adds the order pin to scripts/test-finalize-door.js in the worktree and re-reds; appends to kaola-workflow/issue-1004/findings/red-baseline.md
  result: addendum in kaola-workflow/issue-1004/findings/red-baseline.md. `headingSequence()` + `assertPlantedOrderUnchanged()` in all three T17 legs, `headingSeq()` + order assertions on the two walkthrough fill legs that can witness relocation. It reported HONESTLY that this assertion CANNOT be red at baseline — nothing is written, so nothing moves; it constrains the fix, not the defect — and armed it by mutation instead, in a scratch tree: baseline exit 1 (fill) / 16 door failures; RELOCATING fill (cut-and-append, all four copies) exit 1 on ORDER with exactly 3 of 846 door assertions failing, all 3 the new ones; IN-PLACE fill exit 0 at 846. The relocating candidate is the same one that PASSED the earlier version of both pins — that is the arming proof, and it establishes nothing else in the suite can tell the two fills apart. Boundaries honoured and MEASURED, not just intended: `replace: true` still relocates under the in-place candidate and its leg stays green; the EOF leg deliberately carries NO order assertion because its target is already last, and an assertion that cannot fail on the property it names is not watching it.

- item: Dock the docs for #1004 — CHANGELOG under [Unreleased] and the durable-half semantics in docs/api.md's "The three reports". No surface regeneration: the chosen shape moves no prose. Note CHANGELOG.md is in the self-host codeTreeHash consume list, so this lands BEFORE the chain receipt is stamped.
  status: done
  dispatched: implementer subagent in the worktree, write set CHANGELOG.md + docs/api.md ONLY (disjoint from the code implementer running concurrently); writes to kaola-workflow/issue-1004/findings/docs-1004.md
  result: kaola-workflow/issue-1004/findings/docs-1004.md. CHANGELOG.md +42 at the head of [Unreleased] -> ### Fixed (newest-first, matching the section); docs/api.md +10 in "### The three reports". It RE-MEASURED the census independently rather than copying the brief and got 157 / 49-15 / 48-17 / 12-3, all six matching — and noted the memory index's "16 of 49" is off by one against it. It verified the Step 6 quote, D-653-01's actual wording, and that `appendClosureBlock` writes nine fields none of which are the three, before citing any of them. Judged three neighbouring sentences and left all three byte-untouched with reasons: api.md:277's "not optional ... a deletion, not a conversion" was the ASPIRATIONAL sentence the new paragraph now makes descriptive, so rewriting it would have deleted the claim that justifies the fix; the `## Finalize Findings` "restating, not append-once" paragraph is scoped away explicitly so the new rule cannot be read onto it; the sink's `## Sink Findings` sentence is a different function. Deliberately dropped the 846 denominator from the prose — a suite-wide total decays with every added leg and cannot witness a pin, so it kept WHICH three red. Named as taken on authority rather than verified: the 3-of-846 figure, and the zero-fixtures claim (sampled, not swept).

- item: #1005 — resolve the issue's own hypothesis before choosing a remedy, because it says which way this resolves changes the remedy. Trace the history of root `CLAUDE.md`'s `## First Principles` block against `templates/axioms.md`: was the divergence a deliberate condensation for the 200-line cap, or accumulated drift? The issue argues against condensation on the grounds that axioms 4 and 5 ADD text rather than remove it, and that no run has traced the introducing edit. Read-only archaeology; establish what happened, do not pick the remedy.
  status: done
  dispatched: investigator subagent, read-only over the MAIN root (git history only, no worktree writes), writes to kaola-workflow/issue-1004/findings/premise-1005-history.md
  result: kaola-workflow/issue-1004/findings/premise-1005-history.md — VERDICT **MIXED**, and the issue's own tell is confirmed: adding text is not what a line budget makes you do. (a) The two files AGREED BYTE-FOR-BYTE for 22 days; axioms.md is older by 19 hours and root CLAUDE.md acquired the block by an UNDECLARED COPY-IN — #645's founding CHANGELOG entry and its own evidence leg both name the consumer set as the six workflow-init surfaces, and root CLAUDE.md is in neither list. **Exactly ONE commit in repo history ever touched both files**, and its CLAUDE.md half landed as a bullet in a different section in different words. (b) D2/D3 (the two missing standing paragraphs) are pure DRIFT — introduced at 153 lines against a 198 ceiling, 45 lines of headroom, so a budget motive is arithmetically impossible. (c) A/B/C (intro + axioms 4 and 5) are DELIBERATE AUTHORSHIP but NOT condensation: they ADD 125 characters, axiom 5's clause was pre-written as a design argument in archive/issue-877/step3-extraction-spec.md:85 four days earlier, and canonical was rewritten for the same reason TEN MINUTES LATER by a commit that did not touch CLAUDE.md — two independent rewrites of one axiom, minutes apart, neither aware of the other. (d) D1 alone is partial condensation, but the ONE cap-driven commit in history (95c4a38f, "the rewrite came in at 205") left the block entirely untouched — its seven lines came from elsewhere. (e) CORRECTION to a standing memory line: the 200-line cap WAS a hard build failure (validate-workflow-contracts.js:342) until e4522be9 on 2026-08-12 — advisory today, binding on 2026-07-31. (f) CORRECTION to the issue: init.skeleton.md is NOT an unguarded hole, it is transitively pinned by generate-routing-surfaces --check. (g) **NEW AND NOT IN THE ISSUE: `README.md:22-28` is a FOURTH wording, unguarded and user-facing, never byte-identical, and it disagrees in DIFFERENT places than CLAUDE.md — its intro AGREES with canonical where CLAUDE.md's disagrees, so the three surfaces are pairwise inconsistent.** Unestablished: whether any human ever intended the root block to be a copy — no commit message, CHANGELOG entry, ADR or archived summary states a position anywhere, which is itself the finding but cannot disprove an intent formed in conversation.

- item: #1005 — put the remedy direction to the user. The issue explicitly declines to pick: make root `CLAUDE.md` canonical and extend `testAxiomBlockByteIdentity`'s derived surface set to a 13th surface (costs about +7 lines onto 198 against a recommended 200-line cap), or declare it a named divergent region carrying its reason. Values call, and it gates #1006.
  status: done
  dispatched: self, asked in conversation with the archaeology in hand and the README finding surfaced
  result: OWNER RULED **canonicalize everything, no declared divergent region anywhere.** My first recommendation — canonicalize CLAUDE.md, declare README divergent on a register argument — was CORRECTLY REJECTED by the owner and does not survive the actual text: of README's three differences from canonical, two ("outcome there is", em-dashes for parens) have no reason at all, and the third is not register but a SUBSTANTIVE LOSS — README's axiom 4 drops "and ask, in conversation", the clause this project calls the whole consent mechanism. The rule's own exception is "where capabilities genuinely differ"; audience register is not a capability difference, so declaring that region divergent would have recorded a reason nobody ever formed — the same objection I had already raised against declaring the CLAUDE.md drift intentional. Owner's second directive: cover them ALL, with consistency, rather than diverging from the original goal.

- item: #1005 — the ruling in full, so the implementation cannot drift from it. (1) `templates/axioms.md` stays the sole canonical wording and its CONTENT is untouched here — content edits belong to #1006. (2) Root `CLAUDE.md`'s `## First Principles` block becomes byte-identical to the WHOLE canonical file: intro line, five numbered axioms, and all three standing paragraphs. (3) `README.md`'s axiom list becomes byte-identical to the same canonical block. (4) `testAxiomBlockByteIdentity`'s surface set goes 12 -> 14, each new surface mutation-proven INDIVIDUALLY on that surface. (5) Any second wording of a standing paragraph surviving elsewhere in those two files gets folded, or the fix just moves the duplicate instead of removing it — CLAUDE.md:90 `### Concurrency carries no machinery` and the `Dispatch production; keep decisions` restatement at :96 are the known instances. (6) NO declared divergent regions anywhere. Out of scope by the issue's own instruction: init.skeleton.md:134's hand-inline, which the archaeology also proved is transitively pinned by generate-routing-surfaces --check and is NOT a hole.
  status: done
  dispatched: self — this item is the ruling RECORD, not a task; it exists so the implementation could not drift from it
  result: all six clauses executed and verified in e82adb6d. (1) templates/axioms.md byte-untouched. (2) CLAUDE.md carries the whole canonical file. (3) README likewise. (4) guard 12 -> 14, each new surface mutation-proven individually. (5) both known restatements folded, plus a third the convergence escalated (README:46). (6) zero declared divergent regions anywhere in the repo.

- item: #1005 — extend the guard FIRST and prove it red, because the guard is the test artifact and test custody says whoever implements does not author it. `testAxiomBlockByteIdentity` currently derives its 12 surfaces from the routing registry with an anti-vacuity floor whose own comment is honest about which term is independent; adding two NAMED non-derived surfaces must preserve that anti-vacuity rather than quietly widen a formula until it cannot fail. It should red immediately naming both new surfaces, since both are divergent today.
  status: done
  dispatched: tdd-guide subagent in the worktree at .kw/worktrees/issue-1004, write set scripts/simulate-workflow-walkthrough.js ONLY; appends to kaola-workflow/issue-1004/findings/red-1005-guard.md
  result: kaola-workflow/issue-1004/findings/red-1005-guard.md, baseline 2d57c604. BLINDNESS MEASURED FIRST: same commit, before the edit, `PASSED (12 surfaces)` exit 0 while `CLAUDE.md.includes(axioms)` and `README.md.includes(axioms)` were both false. Now RED naming BOTH stale surfaces and, for each, the first canonical line absent from it. Surface set 12 -> 14 via a NAMED_SURFACES list, existence-asserted, commented as named-not-derived in the same terms as INIT_TOPIC. Comparison idiom untouched (`s.body.includes(axioms)`, one whole-block byte match); what changed is only reporting — drifted surfaces are COLLECTED rather than fail-fast, so the second stale file is not hidden behind the first. **The floor is `... + 2`, a LITERAL, not `NAMED_SURFACES.length`** — and mutant M8 is the counter-proof that this matters: written the derived way, dropping README from the list and re-staling it yields `PASSED (13 surfaces)`, green with a fully stale surface, because expectation and measurement shrink in lockstep. That is the FORGE term's known failure mode, now avoided rather than repeated. Ten mutants, one at a time: M1/M2 prove the two named surfaces fail INDEPENDENTLY (neither rides on the other); M4 (a missing standing paragraph) and M5 (one word, verdicts->verdict) prove it is still byte-identity, not a loose substring; M3 (canonical itself reworded) reds all 14 with `(EVERY surface — templates/axioms.md itself is what moved)`; M6/M7 red the floor; M9 catches a rename. Six things it still cannot witness are named honestly, including: the named list can GROW SILENTLY (a third file that starts stating the axioms is invisible until typed in), it sees the block and NOT the prose around it (README's "all twelve workflow-init surfaces" would stay green at a now-wrong twelve), and it enforces AGREEMENT, never correct wording.

- item: #1005 — canonicalize root CLAUDE.md and README.md against templates/axioms.md until the extended guard goes green, and fold the surviving second wordings so the fix removes the duplicate instead of relocating it. README:30's sentence about "all twelve workflow-init surfaces, with a machine-enforced drift guard" also stops being the whole truth once the guard covers 14. Out of scope by the issue's own instruction: `templates/routing/init.skeleton.md:134` hand-inlines the canonical text, and that duplication is deliberate per docs/decisions/D-645-01.md §2 — do not convert it to a splice; the archaeology separately proved it is transitively pinned and not a hole.
  status: done
  dispatched: implementer subagent in the worktree, write set CLAUDE.md + README.md + CHANGELOG.md ONLY; the guard and templates/axioms.md strictly off-limits; writes to kaola-workflow/issue-1004/findings/implementation-1005.md
  result: kaola-workflow/issue-1004/findings/implementation-1005.md; committed as **e82adb6d**. Guard 1 -> 0, `PASSED (14 surfaces)`; validate-workflow-contracts 0; generate-routing-surfaces --check 0; harness self-check 0. **CLAUDE.md 198 -> 195** — the fold gives back MORE than the canonical block costs, so the line-count worry that framed the whole issue was misplaced. Folded as restatement: "decompose to genuine independence and dispatch that wide", "You decide, uninspected.", the two mechanisms canonical already enumerates, and the whole first sentence of the local Dispatch-production paragraph. KEPT with reasons, because canonical says none of them: the frontier's DEFINITION (`list minus done minus in-flight` — pinned vocabulary in three other files), the three rejected mechanisms canonical does not enumerate, and the declinability rule from ADR 0016. Five more mutants, one site at a time, each reding exactly ONE surface and never the other — including a re-wrap of CLAUDE.md axiom 3 to ~100 columns with NO WORD CHANGED, which proves the guard sees wrapping. It also corrected README's out-of-block surface-count sentence, which the guard is structurally blind to. **It CAUGHT AN ERROR IN THE ARCHAEOLOGY I had relayed to the owner: the agreement window is 12d20h, not "22 days" — the 22 was the day-of-month transcribed as a duration.** The underlying evidence was sound; the CHANGELOG carries 13 days with both SHAs and exact dates. One thing it declined to decide and escalated instead: README:46 was left as a competing wording of canonical's Parallel-by-default paragraph, since its brief scoped the fold to CLAUDE.md.

- item: Rule on the README:46 duplicate the convergence escalated rather than folding unasked — it is the same defect class one file over, and the owner's standing directive is cover them ALL with consistency.
  status: done
  dispatched: self, inline — one paragraph, cheaper than a dispatch
  result: FOLDED by the same method the implementer used on CLAUDE.md, and included in e82adb6d. Dropped what canonical now states eight lines above (the near-verbatim independent/serial sentence, "nothing inspects the decision", "no evidence line", "no fan-out cap", "the agent can already tell the difference"); kept what it does not (the frontier's definition, and the disjointness proof / serializer taxonomy it names specifically). Guard re-run after the fold: still `PASSED (14 surfaces)`, exit 0. Left alone deliberately: README's other belief paragraphs, which EXPLAIN the axioms in README's own voice and carry content canonical does not state (the enumerated irreversible actions, the mission-list shape, the accountability argument) — folding those would gut the README to cure a defect they do not have.

- item: #1006 — author the fourth standing-default paragraph (check the premise before it shapes the work) into `templates/axioms.md` after the tie-breaker protocol and before Dispatch production, plus the companion intro edit at `:3` that distinguishes the numbered tie-breakers from the standing defaults, or the paragraph lands inverted. Then regenerate: the skeleton at init.skeleton.md:134 inlines by hand, generate-routing-surfaces.js renders 6 tracked init surfaces, the opencode/kimi syncs render 6 more, and testAxiomBlockByteIdentity now verifies **14** — the two extra being root CLAUDE.md and README.md, which are NOT generated and must be hand-updated, a consequence #1005 created and mutant M3 already proved (canonical moving alone reds all fourteen). STARTS ONLY AFTER #1005 LANDS — owner-ruled, same lines same files. The issue's four claims should survive any sharpening of its prose: measure now, carry the measurement rather than a verdict, correct the issue on disagreement, nothing inspects it. Explicitly NOT in scope per the issue: any standard location, filename or schema for the premise artifact, and any gate or close-time check — refusal count stays zero.
  status: done
  dispatched: implementer subagent in the worktree at .kw/worktrees/issue-1004; write set templates/axioms.md + templates/routing/init.skeleton.md + the 6 regenerated tracked init surfaces + CLAUDE.md + README.md + CHANGELOG.md; the guard is off-limits; writes to kaola-workflow/issue-1004/findings/implementation-1006.md
  result: kaola-workflow/issue-1004/findings/implementation-1006.md; committed as **d7b29e01**. 11 files: 4 hand-authored, 6 regenerated via --write (no rendered surface hand-edited), CHANGELOG. Guard `PASSED (14 surfaces)` exit 0, verified INDEPENDENTLY by me afterwards; gen --check 0 (18 byte-match); contracts 0; route-reachability 0 (368 assertions); harness 0. CLAUDE.md 195 -> 197 as forecast. Two judgement calls it made and reported rather than burying: it chose **colon-plus-lowercase** for the new paragraph's lede over the draft's period, because period-after-lede belongs exclusively to the five NUMBERED axioms and taking their punctuation would have visually filed the paragraph with the list it is deliberately outside — the placement argument and the punctuation now agree; and it declined to add a `next`-surface pointer, since D-645-01 §3's pointer rule is a six-surface obligation #1006 does not grant. All four required claims survive in the shipped text. It also surfaced a finding outside its write set and left it unedited: D-645-01's stale six-surface count, now **filed as #1007**.

- item: Close the run — stamp the four-chain receipt (the diff is edition-touching, so it fails closed to all four), write the summary with the three headings deliberately EMPTY so this run's own #1004 fix fills them in production, finalize with the WORKTREE's claim.js, push, sink from the MAIN root, then close #1005 and #1006 explicitly and verify CLOSED rather than trusting exit 0.
  status: in-flight
  dispatched: self, inline. Gap sweep already run: `sweptClasses: []`, artifact at kaola-workflow/issue-1004/.cache/run-gaps.json. Summary written with `## Validation` / `## Changed Paths` / `## Mission List` bare at lines 54-56. Follow-up #1007 filed. Four-chain receipt running in background -> /tmp/chains-1004.json.
