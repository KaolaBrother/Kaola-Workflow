evidence-binding: n2-node-678 ed023adc11cd
<!-- RED: paste RED here -->
RED: new test in scripts/test-adaptive-node.js (#678 block, forced leg_provision_failed via the #674b decoy-worktree repro) run PRE-FIX (source reverted via git stash) — `node scripts/test-adaptive-node.js` reported: "FAIL: #678: the SHARED GROUP baseline FILE must be dropped as part of the group-form abort (was stranded pre-fix), got file exists=true" and "FAIL: #678: the SHARED GROUP baseline REF must be dropped as part of the group-form abort (was stranded pre-fix), got ref exists=true" — adaptive-node tests FAILED (2 failures, 1818 passed). Confirms the adversary-observed `file=true ref=true` strand on all 5 abort paths (this repro exercises the per-node leg_provision_failed path).
<!-- GREEN: paste GREEN here -->
GREEN: post-fix (dropGroupBaseline() added, invoked at all 5 abort paths in `runOpenReady`'s group-form leg-provisioning block, guarded on groupBaselineSha) — `node scripts/test-adaptive-node.js` → "adaptive-node tests passed (1820 assertions)" (0 failures; the 2 new file/ref asserts now pass plus the pre-existing 1818 unaffected — a same-run harness stderr blip unrelated to my change, `.git/index` corruption from an isolated mkdtemp fixture in an unrelated pre-existing makeLaneRepo --freeze --repair try/catch, present identically in both the RED and GREEN runs, never surfaces as an assertion failure). `node scripts/edition-sync.js --write` then `--check` → "edition-sync: 10 forge aggregator ports, 24 COMMON_SCRIPTS mirrors, and 27 byte-identical groups in parity with canonical." (clean). `node scripts/simulate-workflow-walkthrough.js` → exit 0, "Workflow walkthrough simulation passed". R2 (crash-window journal-ordering redesign) DEFERRED by default per the task brief — not implemented; the Phase-1 durable `opening` write happens strictly after baseline recording + leg provisioning (git worktree adds, stub commit) in `runOpenReady`, so reordering it earlier is an M-effort crash-recovery-blast-radius change, not a trivially clean one-line move — must be filed as #680 at finalize.
