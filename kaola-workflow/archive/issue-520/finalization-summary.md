# Finalization - Summary: issue-520

## Delivered
Fixed the `--sink` `archive_commit` step so it no longer commits the transient crash-resume
journals (`sink-receipt.json` / `sink-fallback.json`) into the default branch on every direct
sink cycle. Both the `git add` and the `git commit -- <pathspec>` in archive_commit now carry
`:(exclude)` pathspecs for the two journals, across all four sink-merge editions. The on-disk
journals are untouched so #429 crash-resume and the #484 freshness guard still work.

## Files Changed
- scripts/kaola-workflow-sink-merge.js (canonical)
- plugins/kaola-workflow/scripts/kaola-workflow-sink-merge.js (codex twin, byte-identical)
- plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-sink-merge.js (gitlab port)
- plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-sink-merge.js (gitea port)
- scripts/simulate-workflow-walkthrough.js (RED→GREEN tracked-status assertion)
- plugins/kaola-workflow-gitlab/scripts/test-gitlab-sinks.js (#520 test block)
- plugins/kaola-workflow-gitea/scripts/test-gitea-sinks.js (#520 test block)
- CHANGELOG.md ([Unreleased] entry)

## Test Coverage
Hand-rolled assert suites (no coverage %). New RED→GREEN tracked-status assertions added in the
three chain-test homes; the codex chain is covered by the byte-identical twin + validate-script-sync.

## Final Validation Evidence
All four chains green (#307), real exit codes captured directly:
codex=0, gitlab=0, gitea=0, claude=0. Evidence: .cache/final-validation.md.
The contractor regenerates a fresh chain-receipt against the impl commit before the chain-receipt gate.

## Documentation Docking
DOCKED — .cache/doc-docking.md. Only CHANGELOG had doc impact (internal sink-hygiene fix; no
public API / README / architecture / env change).

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | | | | |

## Follow-Up Items
- #521 — harden the #520 regression tests to also bite an add-only half-fix (commit-side exclude
  currently uncovered by the fresh-repo fixtures). From the G1 review (R1, non-blocking).

## Run gaps
- review_followup (R1: #520 tests don't bite an add-only half-fix): filed: #521
- manual:leftover-foreign-receipts (7 pre-existing committed sink-receipt.json under archive/ for
  foreign projects bundle-429-434/bundle-440-441/bundle-445-446/issue-443/issue-451/issue-453/issue-455):
  noise: pre-existing, foreign-project archives explicitly out of #520's barrier-scoped write set and marked
  "(Optional housekeeping)" in the AC; untracking them risks the FOREIGN_ARCHIVE guard (#261), so they are a
  conscious deferral, not a #520 regression. (Automated sweep sweptClasses: [] — neither item is a
  machine-detected swept class; recorded here for goal-contract completeness.)

## Closure Decision
No deferred items requiring user decision beyond the filed follow-up (#521). The 7 leftover receipts
are explicitly optional per the AC. Issue #520's acceptance criteria are met (no committed receipt
after a clean sink; crash-resume preserved; all four chains green). Proceed to close #520.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#520 — to be closed at sink (acceptance criteria met).

## Roadmap
To be regenerated by cmdFinalize (removes .roadmap/issue-520.md).

## Archive
pending (cmdFinalize, Step 8b)

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | skipped | .cache/doc-docking.md | no public behavior/API/setup/architecture/doc impact beyond CHANGELOG; entry added directly |
| documentation docking | invoked | .cache/doc-docking.md | |
| final-validation fix executors | N/A | | no validation failures |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | runs in cmdFinalize Step 8b |
| archive completed folder | pending | | runs in cmdFinalize Step 8b |
| final commit and push | ready | | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE
