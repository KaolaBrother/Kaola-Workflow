# docs — doc-updater evidence (issue #357)

Dictated edit applied verbatim: docs/conventions.md §Testing — Cross-Edition Validation gains the "Single-scenario dev loop (#357)" bullet immediately after the #358 recording bullet — documents --list/--only (shared-tmp group semantics, full-run-only sentinel), fail-closed gh shim, runNode 120s timeout + KAOLA_* scrub + git-config isolation, and the edition runners' CHILD FAILURE tail block.

Verification: docs/conventions.md diff = 1 insertion (the bullet at line 40, inside the #307 section); other worktree diffs are the sibling implement nodes' pre-existing state, untouched by this node.
