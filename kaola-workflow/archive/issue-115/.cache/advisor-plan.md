# Advisor: issue-115 Plan

Blueprint is dependency-safe and complete. The two tasks (manifest version + install.sh case) are
independent and trivially ordered. The gitea) skip-guard pattern mirrors gitlab) exactly.
No missing integration points. The plan is implementable as written.
