evidence-binding: n1-fix 40d1617d9ce7
<!-- RED: paste RED here -->
RED: Wrote failing tests in scripts/simulate-workflow-walkthrough.js for both issues; a scratch probe (validatePlan + verifyVerdictBlock) confirmed each observed the buggy PASS. #589 evidence line — test "#589: fanout 1/2 refute (even tie) must break toward refuted": a 2-instance fan-out [refuted, pass] returned ok:true (majorityRefute `refutes*2 > n` = `2 > 2` = false → PASSED, outvoting the lone refuter) — AssertionError expected ok:false + majority-refute. #587 also RED pre-impl: glob `**/*.md` → in-grammar (expected glob_in_path refuse); parallel legs `Src/x.js` vs `src/x.js` → in-grammar (expected exact-overlap refuse); antichain `Lib/App.js` vs `lib/app.js` → in-grammar (expected both-write refuse); two parallel legs CHANGELOG.md + README.md → in-grammar (expected parallel_allowband_collision); independent antichain docs/a.md + docs/b.md → in-grammar (expected parallel_allowband_collision).
<!-- GREEN: paste GREEN here -->
GREEN: Implemented all fixes. #589 (plan-validator verifyVerdictBlock): `refutes*2 > verdicts.length` → `refutes*2 >= verdicts.length` (tie→refute; odd-width unchanged: 2/3 refutes, 1/3 passes; even 0/2 still passes). #587-1 (plan-validator): new isAllowbandDocsSurface/declaresAllowbandSurface helpers + parallel_allowband_collision refusal in the fan-out-group loop and the antichain-pair loop (≥2 legs declaring CHANGELOG.md/README.md/docs/** refuse; one-leg allowed; serial unchanged). #587-2 (classifier disjointWriteSets case-folds cross-node exact+coarse compare; plan-validator antichain exact clobber check case-folds too). #587-3 (plan-validator hygiene arm: new glob_in_path arm for `* ? [ { ] }`). Cross-edition: classifier hand-ported to codex twin (byte-identical) + gitlab/gitea forge classifiers; plan-validator copied to codex twin byte-identical + forge ports regenerated via `npm run sync:editions`. Test "#589: fanout 1/2 refute (even tie)" now ok:false, reason `fanout majority-refute: 1/2 skeptics refuted` — passing. Verification (all exit 0): simulate-workflow-walkthrough.js → "Workflow walkthrough simulation passed"; test-adaptive-node.js → 1082 assertions; test-commit-node.js → 119 assertions; edition-sync.js --check → 12 forge ports in parity; validate-script-sync.js → all byte-identical/superset families in sync.
