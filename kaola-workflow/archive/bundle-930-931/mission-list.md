# Close #930 (archive relocates a reserved directory at exit 0) and #931 (sink commits a collision-suffixed archive silently)

- item: Premise-check #930 by driving it at HEAD — claim a run whose project is `.roadmap`, finalize it, and see whether `kaola-workflow/.roadmap/` and its sources are actually relocated into the archive at exit 0; also drive the `archive` control the issue claims dead-ends at EINVAL. The issue cites `active-folders.js:240` (skip before `isSafeName`) and `claim.js:1189` (reclaim-on-EEXIST fallthrough). Read-only, scratch fixture, no tracked file touched.
  status: done
  dispatched: investigator agent "premise-930" — report to kaola-workflow/bundle-930-931/.cache/premise-930.md
  result: kaola-workflow/bundle-930-931/.cache/premise-930.md — the DESTRUCTION is CONFIRMED and worse than filed. Driven across three lanes and two reserved directories (.roadmap and .origin), all at exit 0. The worst lane is the one a real run actually uses: `finalize --keep-worktree` from the worktree destroys BOTH roots' copies, reports closure_invariants {ok:true} with no findings, AND authors a commit `chore: archive .roadmap` deleting tracked files on the feature branch, from where the sink merges it to main. `.origin` relocates 6 tracked files with zero signal of any kind. The `archive` control refuses correctly in both lanes (archive_exception EINVAL exit 1; archive_incomplete naming 8384 entries). Root cause (a) confirmed by POSITIVE CONTROL — deleting only active-folders.js:240 flips enumeration from [] to ['.roadmap'], so the workflow-state.md check at :243 does NOT save a claimed reserved dir. Refuted: "the mirror regenerates to No active work" is FALSE in the main lane with a non-empty backlog (regenerateRoadmap's :225 guard throws instead), and "nothing reported" is false in that same leg. Also refuted: "a closed predicate already written at five sites" is a ROOT-EDITION-ONLY count of one of at least four disagreeing wordings — per-edition counts are 5/6/7/7, and the wordings measurably disagree (compact-context.js sees a .roadmap project that readActiveFolders does not; NON_PROJECT_FOLDERS carries `exports`, which no other site knows). Guard coverage mutation-proven on four legs: only the root<->github pair is machine-enforced; a gitlab-only or gitea-only change is invisible to BOTH validate-script-sync.js and edition-sync.js --check, so a 4-copy fix has nothing catching a miss.

- item: Premise-check #931 by driving the collision — make `archiveProjectDir`'s destination already exist and confirm `claim.js:2518-2519` appends `.archived-<ts>`, that `sink-merge.js:2272` stages only its own destination, and that neither `archived_paths` nor `## Sink Findings` records the collision. Confirm the reporting surface the issue says already exists. Read-only, scratch fixture.
  status: done
  dispatched: investigator agent "premise-931" — report to kaola-workflow/bundle-930-931/.cache/premise-931.md
  result: kaola-workflow/bundle-930-931/.cache/premise-931.md — core premise HELD, driven end-to-end twice (collision → suffixed archive committed at exit 0, complete archive left untracked at the unsuffixed name). Two claims fail: (a) "nothing invokes closure-audit's archiveRequiredContent" is REFUTED — invoked at closure-audit.js:317 from :503, pinned by three suites, and it actually FIRED on the real incident commit 02471029; (b) "nothing in the durable record says a collision happened" is PARTLY refuted — the `.archived-<ts>` token sits in the committed record 3x and is uniquely produced by the destination-exists branch, so the FACT is lexically encoded and what is missing is a STATEMENT. "Nothing says the real archive is elsewhere" HELD fully. Key implementer constraints: test-sink-merge.js:414 (test #1) pins `!('findings' in out)` and no stderr FINDING line over a fixture that IS a collision, so routing this through recordSinkFinding turns a green test red; closure-audit.js:341-359 scans for bare `.cache/...` tokens, so the new text must use repo-relative paths or it manufactures a false archive_summary_citation_missing; there are TWO suffix sites (claim.js:2519 linked-run and :2615 in-place), not the one cited; the fix lands in FOUR script copies (canonical + codex byte-identical, gitlab and gitea hand-ported); every existing parser is includes/Array.isArray/anchored-regex so a purely additive line breaks none.

- item: Put #930's value call to the user in conversation — build the archive-side guard, or watch-list it. The issue itself says the call is the owner's: the failure class has zero occurrences in 2844 revisions, and the project rule says do not build speculatively; against that, #929's derivation rested on a clause this measurement refutes and the cost of being wrong is the backlog.
  status: done
  dispatched: self — asked in conversation, with both premise measurements in hand
  result: OWNER RULED: **build the archive-side guard**. Archive side ONLY — the wider option that also closes the claim side (active-folders.js:240) was offered and NOT taken, so the claim continues to admit a reserved directory as a project and that stays out of scope exactly as the issue asks. Rationale recorded at the time: this is not a new refusal class — the destruction carve-out already legalizes it and the sibling name `archive` already refuses twice (archive_exception, archive_incomplete); the gap is that the existing legal refusal does not cover dot-prefixed reserved names.

- item: Implement #930's demanded result — archiving must never relocate a directory that is not a project folder — at whatever shape the owner's answer settles. Explicitly out of scope per the issue: the `active-folders.js:240` skip (measured non-load-bearing), any general project-name validator, anything for the invisibility itself.
  status: done
  dispatched: implementer agent "impl-930" — edits the four kaola-workflow-claim.js copies ONLY (canonical, codex byte-identical, gitlab and gitea hand-ports), judged by tests-930's two suites which it may run but never edit; report to kaola-workflow/bundle-930-931/.cache/impl-930.md. Must cover BOTH archive lanes and more than one reserved name; must leave the `archive` control losing nothing.
  result: kaola-workflow/bundle-930-931/.cache/impl-930.md — 4 claim.js copies, +132 lines, two hunks each, no test touched. MECHANISM: refuse at the TOP of archiveProjectDir, above the isLinkedRun split, so a one-lane fix is structurally impossible; returns {archived:false, reason:'archive_reserved_directory', ...} and no caller changed because cmdFinalize:4192, cmdDiscard:5013 and both abandon sweeps already render result.reason verbatim. THE SUBTLE PART: the guard is ordered BEFORE the `source-missing` early return on purpose, because {skipped:'source-missing'} reads as SUCCESS to closureContract.archiveSucceeded (closure-contract.js:131), so a guard placed after it would still let closure proceed — issue close, roadmap-source removal, worktree teardown — against a name that can never be a project folder. WORDING PICKED: the enumeration form `name === 'archive' || String(name).startsWith('.')`, chosen from four candidates because it is what decides whether a directory is ever SURFACED as a project and it is already written twice inside claim.js itself in all four editions — reuse, not a fifth vocabulary. Rejected: isSafeName (shared path-safety predicate, would throw not report), the literal .roadmap/ROADMAP.md form (misses .origin, a driven instance), `!== 'archive'` (would not have fixed the filed case), and NON_PROJECT_FOLDERS (module-private, reusing it means widening the export surface of the byte-identical cross-edition anchor). The `archive` control changed mechanism and kept its result: same uniform refusal at exit 1, and it no longer leaves a partial archive/archive/ behind because nothing is copied. Verified: test-forge-archive-scoping 88/44 -> 132/0, FULL-SCOPE walkthrough 203/203, validate-script-sync, edition-sync --check, node --check x4, plus the two adjacent suites that call archiveProjectDir directly (test-bundle-finalize 149, test-claim-hardening 766). Mutation-proven on two legs from a scratch APFS clone: neutering the predicate to `false` in all four reproduces the baseline 88/44 byte-for-byte, and restoring root+github only gives 110/22 with every FAIL line being [gitlab or [gitea — independently reproducing the test author's predicted numbers from a different clone.
    DECLARED GAP, ACCEPTED BY ORCHESTRATOR: the predicate does not cover `exports`. Correct call — no observed failure demands it, and adding it would settle a live disagreement between two production sites on the implementer's own authority. I checked the hazard myself and it is benign: `exports` is reserved under archive/ (claim.js:334), not directly under kaola-workflow/, and `kaola-workflow/exports/` does not exist at HEAD; a project legitimately named `exports` would archive onto kaola-workflow/archive/exports/, which is the COLLISION shape — nothing destroyed, dest suffixed, and now disclosed by #931's own fix. The two changes interact benignly.

- item: Implement #931's demanded result — when the sink archives to a suffixed destination because the unsuffixed one existed, the committed record names that fact, discoverable from the record alone without inspecting the filesystem. No refusal, no new detector; `## Sink Findings` and `archived_paths` are the surfaces.
  status: done
  dispatched: implementer agent "impl-931" — edits scripts/kaola-workflow-sink-merge.js plus the codex/gitlab/gitea copies, judged by tests-931's suite which it may run but never edit; report to kaola-workflow/bundle-930-931/.cache/impl-931.md. Orchestrator decisions handed down: infer the collision AT THE SINK (both routes cost 4 copies, but the claim.js route costs 4 copies of TWO files), and do not hang the disclosure on persistArchivedPathsToSummary's empty-staged-list early return.
  result: kaola-workflow/bundle-930-931/.cache/impl-931.md — four *sink-merge.js copies changed, no test file touched. New `describeArchiveCollision(mainRoot, project, archiveDestRel)` returns a repo-relative sentence naming the pre-existing directory, or NULL when there was no collision so absence stays informative; it PROBES existence rather than inferring from the path string, so a plain path since removed yields nothing instead of pointing a reader at a directory that is not there. Rides persistSinkFindingsToSummary (no empty-staged-list gate) as a third argument, NOT recordSinkFinding, so #700 c stays green. Orchestrator independently verified the soundness premise: archiveProjectDir has exactly ONE sink call site and its suffix argument is literally `undefined`. Verified green: test-sink-merge 693 assertions, validate-script-sync (canonical and codex byte-identical at bc6b8490...), edition-sync --check, generate-routing-surfaces --check, and the FULL-SCOPE walkthrough 202/202 from a real git clone. Mutation-proven on two legs plus one accidental real one: all four reverted reds n1+n2+n4; gitlab alone reverted reds only `n4 (gitlab)`; and a mid-work run with gitea unedited red only `n4 (gitea)`. Cleared a FALSE RED honestly rather than assuming — its first walkthrough ran from a `git archive` export, which has no `.git`, and validate-workflow-contracts.js:665 gates the whole tag check on `.git` existing, so the contracts script exits 0 and testContractValidatorMissingTag fails; proven with a two-way probe on a byte-identical script.
    THREE THINGS IT FLAGGED. (1) Docs owed — docs/api.md:847-857 and CHANGELOG; nothing there is now false but `archive_collision` is documented nowhere. (2) ONE COLLISION SHAPE REMAINS SILENT BY SCOPE: the disclosure fires only when THIS SINK archived (receipt.archive_dest set). In the keep-worktree posture cmdFinalize does the archiving and the sink has no dest, so a collision produced there is undisclosed. ORCHESTRATOR RULING: RECORD, DO NOT BUILD — the 2026-08-03 incident is the sole-archiver shape and IS covered (finalize archived first, then the sink hit the collision on the resurrected folder), while the finalize-side shape has never been observed; `derive additively` governs, and closing it means writing claim.js. Not filed as an issue either, per the rule that a permanently-open issue which is explicitly not work invites scheduling it. (3) The forge ports are read-and-ported, not driven — glab and tea are both absent on this box, so those two copies rest on node --check, the (n4) static sweep and hand-checked wiring.

- item: Author the test for #931 and prove it fails on the pre-change baseline — custody says the implementer never writes it. Acceptance: after a sink whose archive destination already existed, the committed record carries a STATEMENT naming the pre-existing unsuffixed path, discoverable without touching the filesystem. Hard constraints from the premise check: test-sink-merge.js:414 (test #1) pins `!('findings' in out)` and no stderr FINDING over a fixture that IS a collision, so this must not route through recordSinkFinding; closure-audit.js:341-359 flags bare `.cache/...` tokens, so use repo-relative paths.
  status: done
  dispatched: tdd-guide agent "tests-931" — test lands in scripts/test-sink-merge.js, baseline-failure proof to kaola-workflow/bundle-930-931/.cache/tests-931.md. WAVE 1 LANDED: 25 assertions over 4 scenarios, RED on baseline with exactly 2 failures (both the headline clause), 671 passing, test #1's #700 c untouched and green; satisfiability driven on a scratch mirror of scripts/. Disclosure pinned UNCONDITIONAL — tracked-ness deliberately not pinned, so a report firing only on the untracked shape fails scenario n2. WAVE 2 re-dispatched to the same agent: extend the pin across all four sink copies using the file's existing z1/z2/z3/z4/#912 four-edition idiom, and mutation-prove that sweep is armed — because a three-copy fix passes everything wave 1 wrote and premise-930 measured that nothing else catches a port-only omission.
  result: kaola-workflow/bundle-930-931/.cache/tests-931.md (+ "Addendum — (n4)"). Wave 2 landed: (n4) sweeps all four sink copies plus adaptive-schema.js and closure-contract.js, deriving its marker AT RUN TIME from the disclosure (n1)/(n2) actually observed in the committed record, reduced to longest verbatim fragments (19/46/94 chars from the implementer's sentence) — so it names no field and no function and stays a result pin. Test files deliberately excluded: a guard reads what ships. ARMED on five mutation legs through a scratch mirror: unmodified mirror 693 assertions exit 0; codex-alone, gitlab-alone and gitea-alone reverts each red EXACTLY ONE assertion and it is that port's own leg; all-four reverted reds n1+n2+n4 with (n4) failing on its own terms rather than skipping. Three limits stated rather than papered over: (1) it reads TEXT not behaviour, so a port carrying the sentence but never reaching the code that writes it passes — it catches omission, not miswiring; (2) a wording that already existed verbatim in a port would make that leg vacuously green, so THE MUTATION LEGS MUST BE RE-RUN IF THE DISCLOSURE WORDING IS EVER REWRITTEN; (3) it requires the ports to share the canonical wording, which "one rule, one wording" already imposes, but a genuinely divergent port would need this revisited as a declared named region. Suite cost 56s/648 -> 60s/693 assertions, no new subprocess.

- item: Author the test for #930 — owner ruled BUILD, archive side only. Acceptance: archiving must never relocate a directory that is not a project folder; construct a run whose project is a reserved directory under kaola-workflow/ and finalize it, and .roadmap plus its sources must still be in place afterwards. Must cover the worktree --keep-worktree lane, which is where the destruction also authors a commit.
  status: done
  dispatched: tdd-guide agent "tests-930" — test lands in scripts/simulate-workflow-walkthrough.js, baseline-failure proof to kaola-workflow/bundle-930-931/.cache/tests-930.md
  result: kaola-workflow/bundle-930-931/.cache/tests-930.md — RED on baseline, no production code touched. Two files: simulate-workflow-walkthrough.js gets testArchiveNeverRelocatesReservedDir930 (3 reserved names x 2 lanes, root edition, 4 red legs + 2 green control legs), and test-forge-archive-scoping.js gets a #930 section driving the linked-worktree lane PER EDITION across all four (88 passed / 44 failed, every failure theirs). Four method-neutral assertions: content present byte-identical in every checkout, no archive/<reserved>/, not a silent success, and the feature-branch HEAD still carries the files. Arming proven on 6 mutation legs in a scratch clone — a .roadmap-only fix leaves .origin red, an in-place-only fix leaves the worktree lane red, and assertions 2/3/4 each fire alone. THE CROSS-EDITION GAP IS CLOSED BEHAVIOURALLY: that suite already runs in all four chains, and the sweep is mutation-proven at 88/44 baseline -> 110/22 with a root+github-only fix (gitlab and gitea still red) -> 132/0 with all four. Three picture-changers: no existing test pinned the destruction so nothing needs rewriting; gitlab and gitea are now DRIVEN rather than read (KAOLA_WORKFLOW_OFFLINE=1 plus that suite's stateForgeSection fixture is sufficient, no forge needed) and the destruction is real in both ports; and the `archive` control was deliberately pinned at result strength (nothing lost + not a silent success) rather than the `exit != 0` I asked for. ORCHESTRATOR RULING: deviation ACCEPTED — my brief specified a mechanism, the project rule is to specify the result, and a uniform fix that turns `archive` into an explicit reported skip at exit 0 destroys nothing and must be free to pass. Full-scope walkthrough (not the 1/12 shard) 166 scenarios pass.

- item: Adversarially verify #931's landed change — reachability of the disclosure in all four ports as WIRING rather than text, the vacuous-green risk in (n4)'s marker derivation, whether persistSinkFindingsToSummary's `## Sink Findings` idempotency check can silently swallow the collision line on a crash-resumed re-entry, and whether any comment in the four copies is a prose absolute true only of the root edition.
  status: done
  dispatched: adversarial-verifier agent "adv-931" — report to kaola-workflow/bundle-930-931/.cache/adv-931.md
  result: kaola-workflow/bundle-930-931/.cache/adv-931.md — VERDICT: REFUTED, 1 blocking. R1 CONFIRMED and driven on three editions: the disclosure fires when NOTHING COLLIDED, committing and pushing a false statement — the exact harm class #931 exists to remove. Chain, measured with an fs call trace on unmodified shipped modules: resolveSinkReceiptPath falls through to the ARCHIVE receipt path when main holds no live kaola-workflow/<project>/ at sink start (the #832 fallback), the first stepDone -> writeSinkReceipt mkdir -p's kaola-workflow/archive/<project>/.cache/, archiveProjectDir's existsSync then sees that and suffixes the destination, and describeArchiveCollision's statSync().isDirectory() calls the sink's own skeleton a pre-existing archive. Then disposeSinkJournals PRUNES it, so the committed record points a reader at a directory that is not there. THE FILE ALREADY NAMES THIS TRAP 1000 LINES ABOVE, at sink-merge.js:1167-1172, in the #832 comment — "an existence-only audit read it as a satisfactory archive". describeArchiveCollision IS an existence-only audit; this is a repeat of #832's defect class in new code. R2 CONFIRMED: the (n3) no-fabrication control CANNOT FAIL in that shape — buildSoleArchiverFixture leaves an empty untracked .cache/ that survives `git checkout main`, so the live folder is still present, no skeleton is manufactured and the destination stays plain; proven single-axis by a control/n3clone pair differing only in whether the live .cache carries files. R3 PLAUSIBLE (reasoned, not driven): a crash between the in-memory archive_dest write and stepDone('finalize') loses it, so the disclosure is silently dropped for a run that really did collide. R4 CONFIRMED: the gitlab and gitea comments assert a test pin that exists only in the canonical suite — the "prose absolute true only of the root edition" class. R5: impl-931's claim that the port suites cannot run here is WRONG — both drive their forge through KAOLA_GLAB_MOCK_SCRIPT / KAOLA_TEA_MOCK_SCRIPT rather than the CLI, and test-gitlab-sinks.js was measured EXIT=0 here with no glab installed; that is why the weakest joint stayed untested. R6 docs — already written by the orchestrator after this agent started; stale. NOT refuted: the wiring attack found describeArchiveCollision genuinely called and reaching the committed summary in both ports, driven end to end; and (n4)'s three derived fragments are absent from every swept file at HEAD, so that sweep is not vacuously green.

- item: Repair #931's R2 — the no-fabrication control cannot fail in the shape R1 exploits, so add one that drives the sole-archiver posture with the live .cache/ genuinely gone from main. Test custody: this is the test author's, not the implementer's.
  status: done
  dispatched: tests-931 (re-dispatched, has full context) — scripts/test-sink-merge.js only; must be RED against the currently-shipped producer; append to .cache/tests-931.md
  result: .cache/tests-931.md "Addendum 2" — (n5) is a BICONDITIONAL over one assertion set: the property pinned is not "a collision is disclosed" but that the statement appears when a prior archive really was there AND NOT when it was not, one axis, both preconditions MEASURED at sink start rather than assumed — which is precisely what (n3) failed to do. Confirmed the R2 diagnosis independently first: untracked-empty-cache leaves liveDir PRESENT after checkout, tracked-cache leaves it ABSENT. DRIVEN BEHAVIOURALLY PER EDITION through each port's own mock hook, which closes (n4)'s declared text-only blind spot. RED as demanded against the shipped R1-defective producer, all four editions: EXIT=1, 4 failed / 741 passed, every failure a `.../none` leg, with `plain path on disk afterwards: ABSENT` proving the committed and pushed record sends a reader to a directory that is not there; all four `collision` legs passed in that same run, so the reds are attributable to the axis alone. Bounded in the other direction too: with all four sink AND all four claim copies reverted to HEAD, 7 fail — n1, n2, n4 and all four `collision` legs — while the four `none` legs correctly PASS, because a producer that says nothing fabricates nothing. The two directions red on opposite producers, which is what makes it a biconditional rather than two assertions pointing the same way. Deliberately does NOT pin absence of the suffix in the no-collision leg: the skeleton pushing the destination off the plain path predates #931 and pinning it would reach outside the claim. CAUTION RECORDED AND ACCEPTED: impl-931 was rewriting the four copies throughout, so one transient `n5 gitea/collision` red coincided with gitea's sha changing MID-RUN and is NOT a defect, and an observed 745/EXIT=0 was also measured on a moving tree and is NOT a verdict. Any green must come from a settled tree — that is the orchestrator's final verification item.

- item: Repair #931's R1 (blocking) and R4 — stop the sink reporting a collision against a directory it manufactured itself, and strip the mechanism claim from the two port comments. Also run the gitlab and gitea sink suites, which R5 proved are runnable here via the mock-script hooks.
  status: done
  dispatched: impl-931 (re-dispatched, has full context) — the four *sink-merge.js copies only; report appended to .cache/impl-931.md
  result: .cache/impl-931.md rounds 2. THE SHAPE OF THE FIX: the disclosure no longer asks whether something EXISTS at the plain path, it asks whether a REAL ARCHIVE stands there — the gap between those two questions was the whole defect. describeArchiveCollision no longer touches the disk at all (a function that formats a sentence must not also decide whether the sentence is true); a new realArchiveAtPlainPath makes that call, treating a lone `.cache/` holding nothing but sink journals as not-an-archive, and REUSING the existing SINK_STAGE_SKIP set rather than restating what a journal is. Deliberately did NOT reuse pruneSinkArchiveSkeleton's predicate verbatim, because that one demands an EMPTY .cache/ — it runs after journal disposal, whereas the journal is present throughout the transaction being reported on, which is exactly the mis-fire adv-931 predicted for "route two". MY STEER WAS WRONG AND THEY PROVED IT: they BUILT the capture-at-transaction-start route I and the verifier both favoured, drove it, and removed it after measuring — it is not independently load-bearing (no drivable shape distinguishes the timings), and it ANSWERS TOO EARLY TO BE RIGHT, because archiveProjectDir makes the suffix decision at the finalize step AFTER the merge, so an archive the merge brings to main after the transaction opened is a genuine collision that an early capture reports as none. Stale by construction. Deviation ACCEPTED. R4 done: the `(#700 c)` pin claim is gone from both port comments, which now carry the durable fact alone; grep -c '#700 c' is 0 in both ports and canonical keeps it where it is real. R5 acknowledged and corrected — both port suites now RUN and are green, and they are what would have caught R1's port half in round 1. R3 explicitly NOT closed, with the reasoning that the early capture would not have closed it either: R3 is the loss of receipt.archive_dest in the crash window, and a persisted did-a-prior-archive-exist flag does not restore a destination path. Verified: test-sink-merge 745 assertions, BOTH port sink suites green, validate-script-sync, node --check x4, full-scope walkthrough 202/202 from a fresh clone carrying only its own four files. (n5) armed on all four editions — red against the mutation naming every `.../none` leg, green against the shipped code. Diffstat 4 files, +356/-12.

- item: Adversarially verify #930's landed change once impl-930 reports — same discipline, plus whether the chosen reserved-name wording agrees with the other sites or adds a fifth disagreeing one.
  status: done
  dispatched: adversarial-verifier agent "adv-930" — report to kaola-workflow/bundle-930-931/.cache/adv-930.md
  result: kaola-workflow/bundle-930-931/.cache/adv-930.md — VERDICT: REFUTED, 1 blocking. R1 CONFIRMED and driven: THE PREDICATE IS EXACT-MATCH AND THE FILESYSTEM IS CASE-INSENSITIVE. `isReservedWorkflowDirName` tests `name === 'archive'`, but this volume and os.tmpdir() are both case-insensitive (measured: `touch AbC && test -e abc` true on both), so kaola-workflow/Archive IS kaola-workflow/archive on disk while 'Archive' === 'archive' is false. `finalize --project Archive --keep-worktree` reproduces the FULL #930 destruction at exit 0. R2 CONFIRMED low: the refusal detail says "nothing was moved, copied, stamped or deleted", which is true of archiveProjectDir in isolation but FALSE on the finalize envelope it is emitted on — step 8a mirrorFinalizationArtifacts copies main's .roadmap over the worktree's, and persistValidationToSummary/persistChangedPathsToSummary CREATE finalization-summary.md inside the reserved directory, before the archive call is reached; measured, all four editions. Neither suite catches it because neither asserts the reserved directory is unchanged as a SET — both only assert enumerated files still exist. R3 pre-existing, recorded not actioned: claimProject's transaction rollback at :1263 is rm -rf over projectDir, which for a reserved name is the reserved directory itself; the owner ruled the claim side out of scope. R5 prose: the impl report AND the new walkthrough comment both characterise the `archive` control's prior behaviour wrongly — at HEAD a linked-lane `--project archive` exits 0 and destroys the band, so premise-930's measured refusal was FIXTURE-DEPENDENT (its archive band was big enough for verifyArchiveComplete to fail; adv-930's smaller one copied fine). The issue's own "control, same class, correctly safe" premise was therefore never a guarantee.
    NOT REFUTED, all driven: the refusal stops the WHOLE closure end-to-end in all four editions with a baseline positive control — no roadmap-source unlink, no archive staging, no worktree or branch removal, NO FORGE CALL AT ALL — and the implementer's ordering argument about the source-missing early return is confirmed load-bearing. My own expectation was WRONG: every fs.rmSync/renameSync/unlinkSync/rmdirSync/copyDir/mergeCopyDir in all four claim.js copies plus gap-sweep, sink-pr, closure-audit, active-folders and compact-context was enumerated, and no other mover is reachable — cmdRelease, both watch-pr arms and cmdWorktreeFinalize resolve through readActiveFolders, which skips reserved names, so "no caller change needed" is true and for those three also vacuous. R4: MY `exports` REASONING HOLDS, driven both ways — with archive/exports/ present the run collision-suffixes and the salvage patch survives byte-for-byte; nothing is destroyed.

- item: Repair #930's R1 (blocking) and R2 — make the reserved-name predicate agree with what the filesystem will actually do, and stop the refusal detail asserting something the same transaction already contradicted.
  status: done
  dispatched: impl-930 (re-dispatched, has full context) — the four *claim.js copies only; report appended to .cache/impl-930.md
  result: .cache/impl-930.md "round 2". Predicate is now `n.toLowerCase() === 'archive' || n.startsWith('.')`, verified present in all four copies, body hashing identically (cecd3024a7964415) with exactly one definition and one call site each; diff 4 files +232/-0, canonical and github byte-identical at 732809be471d535c. M3 IS THE LEG THAT MATTERS and it is correctly shaped: it reverts ONLY the case-fold, leaving the guard, its call site, the dot arm and every comment in place, so it isolates the round-2 repair from the round-1 work — and it carries a same-mutant lowercase control proving the mutation did not simply disable the guard. Under M3, `--project Archive` goes back to exit 0, archived:true, `chore: archive Archive` committed, BOTH bands destroyed, while `--project archive` on that same mutant still refuses and loses nothing. M1 (predicate -> false) gives 140/48 and M2 (root+github only) gives 164/24 with every failure now on BOTH arms per port. Shipped: scoping 188/0, walkthrough scenario PASSED (4/4 names x 2 lanes), full scope 203/203, plus validate-script-sync, edition-sync --check, node --check x4, test-bundle-finalize 149 and test-claim-hardening 766. Captured a transient red (`testSinkTransactionCrashResume`: `priorArchiveExisted is not defined`) and correctly attributed it to impl-931's lane caught mid-edit rather than claiming or ignoring it — established by showing the symbol occurs 0 times in its own four copies, does not exist at HEAD, and lives only in the working-tree sink copies. Repeated my own correction back on the record: its round-1 "nothing is lost either way" justification for the `archive` control was FALSE for the linked lane, inherited from the premise report and never driven. USEFUL FOR THE DOCS ITEM: the case-fold changes neither the reason token nor the envelope shape, only which names reach it, so the #930 documentation I already wrote remains accurate and only #931's needs revising.

- item: Repair #930's test gaps — no suite asserts the reserved directory is unchanged as a SET, so R2's stray finalization-summary.md passed both unseen, and the new walkthrough comment repeats the refuted claim that `archive` already dead-ends EINVAL today.
  status: done
  dispatched: tests-930 (re-dispatched, has full context) — scripts/simulate-workflow-walkthrough.js and scripts/test-forge-archive-scoping.js only; append to .cache/tests-930.md
  result: .cache/tests-930.md "Round 2". (1) CASE-VARIANT ARM: verified the case-insensitivity premise itself first, and caught its OWN first probe being mis-pathed and reading "case-sensitive" before correcting it. impl-930's fix had already landed (`n.toLowerCase() === 'archive' || n.startsWith('.')`) so the red is shown against the REVERTED pre-fix predicate in a scratch clone: 184 passed / 4 failed, exactly one per edition, `Archive` only, `.roadmap` arms green; restoring the fold gives 188/0. The arm splits `dir` (directory on disk) from `given` (caller's string) and RUNS ONLY WHERE THE ALIASING IS REAL, gated on a live CaseProbe/caseprobe probe rather than an assumption, skipping with a printed reason and a ran/total count on a case-sensitive host so the skip cannot go silent — which deliberately keeps the over-refusal trade I chose free, since the suite then says nothing about `Archive` either way. Honest limit stated: pre-fix, `Archive` on their fixture ends in archive_incomplete with a partial self-copy — bypass and residue, no loss — so it is the SET assertion that catches it, not preservation; adv-930's destroying variant needs a repo-wide *.md ignore rule and is mutually exclusive with assertion (4) in one fixture. (2) SET-EQUALITY: exact equality against a pre-run snapshot in every checkout, with a NAMED allowance KNOWN_ADDITIONS = {'finalization-summary.md'} — failing on it would red a writer I said not to fail on, ignoring additions leaves the hole, so naming it puts the write on the record and fails any OTHER addition immediately. Armed: emptying the allowance goes red naming that file in both suites and all four editions, so it is the ONLY entry either suite's reserved directory gains in any lane or edition. (3) PROSE corrected — the "archive is the CONTROL, its rename dead-ends EINVAL" claim is gone, replaced by the durable fact that none of the four names is a control and that whether `archive` survived depended on the FIXTURE.
    CORRECTION TO THE ADVERSARIAL FINDING, ACCEPTED: adv-930's R2 second measurement — the .cache/final-validation.md overwrite — does NOT occur where the two roots agree, which a realistic fixture makes them do. Instrumented before/after digests are identical (c5a93e233cc8 -> c5a93e233cc8) in every edition and both specs. The overwrite is real as a mechanism but observable only where the roots disagree, and rigging a fixture to make them disagree would be less realistic and would pin a content fact about the run's own artifact. The R2 repair still stands on the finalization-summary.md creation, which is real and independently confirmed load-bearing by the allowance mutation.
    *** THIS ACCEPTANCE IS REVERSED. *** On re-verification adv-930 refused the correction and is RIGHT: the measurement was sound but the inference was not. A fixture whose two roots hold IDENTICAL digests cannot observe an overwrite — copying a file over a byte-identical copy leaves no trace — so that control is VACUOUS for this question rather than evidence the overwrite does not happen. The overwrite is the shipped rule, readable without any fixture: mergeCopyDir at claim.js:3462 keeps only FINALIZE_MIRROR_DEST_OWNED and FINALIZE_MIRROR_TREE_BOUND, and .cache/final-validation.md is in NEITHER keep-set, so main's copy overwrites the worktree's unconditionally whenever both exist. The two copies DO differ in a real linked run because that file carries validated_candidate_hash over the tree the recorder's shell was in, and finalize's own operator hint says so. Re-measured on the repaired tree, all four editions: d7548b691726 -> c5a93e233cc8. No code consequence — pre-existing machinery, unrelated to #930, and the reworded detail no longer claims otherwise — but I accepted a true fact's retirement on the strength of a control that could not have detected it, which is the exact failure this project keeps warning about.
    DELIBERATE GAP, ORCHESTRATOR RULED: a change that MODIFIES an existing file inside the reserved directory stays invisible for the RUN'S OWN paths only (foreign paths are byte-compared, and any new path anywhere is now caught). Leave it unpinned. Modification of the run's own artifacts is not relocation, it is pre-existing finalize behaviour that writes into the project folder regardless, and no observed failure demands it — pinning it would freeze content facts about machinery outside #930's scope.

- item: Dock the documentation — `CHANGELOG.md`, and `README.md` / `docs/api.md` / `docs/architecture.md` / `docs/conventions.md` only where the change is actually user-visible or structural. Write CHANGELOG before the receipt run so the receipt is not staled by it.
  status: done
  dispatched: self — kept inline rather than delegated because both implementers correctly refused to widen past their write sets and flagged the docs debt instead
  result: three surfaces written, all before any chain run so the receipt is not staled. (1) CHANGELOG.md — a new `[Unreleased]` section (none existed; prepare consumes it) with a Fixed entry per issue plus a closing paragraph stating the ASYMMETRY in edition coverage honestly: #930 is pinned per edition behaviourally with the 88/44 -> 110/22 -> 132/0 numbers, #931's is a static sweep that catches an omitted port but not a miswired one. (2) docs/api.md — a new `archive_reserved_directory` row in the archive-refusal table, and an `archive_collision` paragraph after the archived_paths contract. (3) docs/workflow-state-contract.md — the workflow_project hazard paragraph at :138-141 named the INVISIBILITY but not the destruction; it now records that such a run can still be claimed and can no longer be archived. Verified myself before writing that the new reason propagates through the SINK too, not just cmdFinalize: sink-merge.js uses `archiveResult.reason || 'archive_incomplete'` on one arm and `(archiveResult && archiveResult.reason) || 'archive_not_performed'` on the other, so the token surfaces verbatim rather than being flattened to archive_not_performed.

- item: Get the work onto the feature branch — ORCHESTRATOR ERROR discovered mid-run. The claim recorded `run_posture: worktree` with the branch living in .kw/worktrees/bundle-930-931, but every agent was dispatched against main's checkout, so all eleven modified tracked files are UNCOMMITTED on branch `main` while workflow/bundle-930-931 is clean at the same base 68cb48f4. The sink merges the feature branch, so as it stands there is nothing on it to merge. Both refs are at the identical base and the worktree is clean, so moving the diff across is mechanical and destroys nothing. Do this only once BOTH implementers have stopped editing, or it moves a moving target. The run record folder stays in main, which is where the claim put it.
  status: done
  dispatched: self
  result: moved by patch rather than by copying, so the transfer was verifiable rather than trusted. `git diff` in main -> `git apply --check` in the worktree (OK) -> apply -> BOTH patches regenerated and compared byte-for-byte (IDENTICAL) and all 14 files sha256-compared across the two checkouts (14/14 OK) BEFORE main's copy was destroyed. Only then `git checkout --` in main, which was safe precisely because every sibling agent had already stood down and a verified second copy existed — the ordering the recorded lesson about never reverting over concurrent work demands. Committed as 5ccc2819 on workflow/bundle-930-931, one commit above 68cb48f4. Main is clean but for the untracked run record folder; the worktree is clean.

- item: Re-verify BOTH R1 repairs adversarially once they land — a repair that turns a red green is not thereby correct, and this project has shipped a repair round that introduced its own blocking regression. Re-dispatch the two verifiers, which already hold the context, scoped to the repairs and to whether the original refutations are genuinely closed rather than moved.
  status: done
  dispatched: adv-930 and adv-931 both re-dispatched (each holds its own prior context) — reports appended to .cache/adv-930.md and .cache/adv-931.md
  result: BOTH R1 refutations CLOSED, verified on the instruments that found them. adv-931 round 2: R1, R2, R4 and R6 all status=resolved — the phantom disclosure is gone on every drivable edition, the true positive intact, mutation-proved armed, (n5) confirmed a real biconditional, and both port comments' `(#700 c)` claim gone. It found R7 NEW (routed to impl-931, tracked as its own item) and R8, which was my documentation. adv-930 round 2: R1 CLOSED with the bound now measured, the next-bypass hunt run EXHAUSTIVELY AGAINST THE FILESYSTEM rather than against toLowerCase, the over-refusal trade I chose measured and found not to bite, R2's repair correct on the surface it is emitted on, and nothing broken. It also filed TWO DOCUMENTATION DEFECTS AGAINST ME, both confirmed: R6 — all three shipped doc surfaces enumerated the refused set as `archive` plus dot-prefixed, OMITTING THE CASE-FOLD THAT IS THE WHOLE SUBSTANCE OF THE REPAIR, so a reader would learn that `Archive` is archivable; and R7 — the CHANGELOG carried three claims measured false: that the `archive` control "kept its result" (false for the linked lane at baseline), that "nothing under the refused directory is touched" (false — finalization-summary.md is created there, which is the very R2 defect I had just told the implementer to fix and then repeated in my own prose), and a stale 132/0 where the shipped suite reports 188/0. All three surfaces rewritten.

- item: Close #931's R7 — the phantom disclosure returns over a torn atomic-writer temp file left in the sink's own skeleton .cache/, and the residue is PERMANENT because nothing removes it, so every later sink for that project repeats the false line. Treated as completing the R1 repair rather than building for an unobserved class: the class is the one just repaired, and the predicate simply does not cover this door.
  status: done
  dispatched: impl-931 (re-dispatched again) — the four *sink-merge.js copies; append to .cache/impl-931.md
  result: closed with an `isAtomicWriteResidue` arm in the same predicate, derived from the writer that produces the form rather than a third hand-maintained list. Driven on the PERMANENT end state — no real archive anywhere and the skeleton .cache/ holding nothing but one stranded temp, which is what survives after disposal has removed the receipt and pruneSinkArchiveSkeleton has refused the non-empty directory. Shipped: disclosure absent on canonical, gitlab and gitea; with the new arm removed, the false statement returns on three of three. archive_dest stays SUFFIXED on every shipped row, which is the proof the trigger is still fully live and only the false statement is gone. Regression clean — control and resume silent, collision still present. Final diffstat 4 files +440/-12, canonical and codex byte-identical.

- item: Revise the documentation I already wrote — it describes the PRE-REPAIR behaviour and one sentence states the defect itself as if it were the design. docs/api.md's archive_collision paragraph says the line is "written only when the plain path is still present on disk", which IS the existence-only probe adv-931 refuted; and CHANGELOG's #931 entry says the same in other words. Both need rewriting to whatever the R1 repair actually settles, plus refreshed per-edition numbers for #930 once the new arms land. Do this AFTER the repairs and BEFORE the chain receipt run.
  status: done
  dispatched: self
  result: R8 was exactly this item, independently confirmed by the verifier — both surfaces stated "written only when the plain path is still present", which is the pre-repair existence-only rule the repair deliberately replaced. Rewritten in docs/api.md and CHANGELOG.md to state the actual test — whether a REAL ARCHIVE stands at the plain path, not whether anything exists there — and to say WHY that distinction is load-bearing rather than pedantic: the sink manufactures that directory itself when main holds no live folder, so an existence-only test reports a collision against its own skeleton and names a directory journal disposal is about to delete. #930's documentation needed no revision, confirmed by impl-930: the case-fold changes neither the reason token nor the envelope shape, only which names reach it. Per-edition numbers in the CHANGELOG still need a final refresh once R7 settles the suite counts.

- item: Verify at full scope — `node scripts/simulate-workflow-walkthrough.js` exit 0 at full scope (the fast gate only samples a 1/12 shard), plus the chain receipt with `--project bundle-930-931`.
  status: done
  dispatched: self — run from the worktree at 5ccc2819, on a settled tree with every agent stood down, because every green measured while those files were moving was provisional
  result: walkthrough FULL SCOPE 203/203 exit 0, with testArchiveNeverRelocatesReservedDir930 reporting PASSED (4/4 names x 2 lanes) — not the 1/12 shard. test-sink-merge exit 0, test-forge-archive-scoping 188/0, test-bundle-finalize 149, test-claim-hardening 766, validate-script-sync and edition-sync --check both exit 0 with 4 Oracle Kernel copies identical. BOTH PORT SINK SUITES RUN AND GREEN (gitlab 577 spawns, gitea 573) — the suites impl-931 had claimed could not run here. Chain receipt at kaola-workflow/bundle-930-931/.cache/chain-receipt.json: headSha 5ccc2819 bound to the commit by exact equality, scope.changedFileCount 14 matching the diff exactly, scope.selection `edition_coupling` so all four chains ran, and claude/codex/gitlab/gitea all exit 0. The chain run COMPLETED before finalize was invoked rather than outliving it, which is the specific error that manufactured #931's own false-red receipt.
