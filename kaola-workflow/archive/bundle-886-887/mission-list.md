# Close #886 and #887 with verdicts, then cut the release

- item: strike the four orphan references to files the ADR 0017 demolition deleted — `templates/routing/rename-table.js:22` (`repair-state.js`), `scripts/measure-site-execution.drivers.txt:20,29` (`test-plan-validator.js`, `test-replan.js`), `scripts/test-spawn-classification.js:102` (a row for `test-replan.js`; the file carries no total over its rows, so removing one has no blast radius), `scripts/test-opencode-edition.js:306-308` (two dead A14 entries whose universal now lives in `assertNoBadgeResidue`). All three target files confirmed absent from the tree. #886 item 7.
  status: done
  dispatched: self — edits land in the four named files on `workflow/issue-886`
  result: #886 undercounted this by 7x, and one entry was not debris at all. Swept every entry rather than the named ones: 14 of 49 drivers dead (not 2), 10 ceiling rows dead (not 1). `rename-table.js` comment corrected; A14's two entries removed with their subject (neither phrase appears in any canonical command; the `MUST pass` entry stays because its subject is live) and the block comment now states where the universal actually lives. **`scripts/measure-site-execution.js` crashes on load** — it lifts its census from `test-refusal-route-sweep.js`, deleted when ADR 0017 took the refusal count to zero, so the instrument has not run since. `.origin/877/surfaces.md:75` predicted the call and deferred it. Owner ruled: retire. Both files deleted. Green after: opencode-edition 492, suite-registration 472, generate-routing-surfaces 430, route-reachability 325, spawn-classification 597 sites/60 files.

- item: add the two doctrine sentences to `CLAUDE.md`'s Validation Policy — "a guard reads what ships, not what was authored; when its universe is derived from the artifact under test, one absolute count lives in a different file" and "a derivation with a membership threshold cannot see a rule beneath it; small reciprocal obligations get an explicit pin". Both describe repairs already shipped in a9ca2a61 (rendered-content ban, ROLE_PINS) — this records the rule so the next guard's author inherits it. #887(b).
  status: done
  dispatched: self — one rule in `CLAUDE.md` Validation Policy, derivation in `docs/conventions.md`
  result: `CLAUDE.md` is hard-capped below 200 lines by two contract validators and was already AT the cap, so the derivation went to `docs/conventions.md` — "Aiming a guard — what it reads, and how wide (#887)" — with a two-line rule in `CLAUDE.md` pointing at it. Lines reclaimed by tightening the Running Tests paragraph and two Validation Policy bullets; file now 199. The new section carries the five-defect table, both corollaries, the routing anchor's mutation proof, and what is deliberately NOT built: the generic anti-vacuity harness, which catches 1 of the 5 observations it was derived from.

- item: close the reviewer prompt-defense gap with ONE bullet, not the 6-bullet baseline — the three reviewers already carry a 3-bullet `Prompt defense` in `templates/reviewers/behavior-contracts.json`; the only coverage genuinely missing that matters is the obfuscation-vector enumeration (homoglyphs, zero-width, encoded tricks, window overflow, urgency, authority claims). Executable-output and harmful-content bullets are declined on the record. Enforce presence in `validateBehaviorContracts` (closed-schema idiom already in that file), NOT the parity guard — generated roles are correctly excluded from the consensus corpus. Bumps `behavior_contract_version` 2→3, refreshes both hashes, regenerates 12 surfaces. #886 item 1.
  status: done
  dispatched: self — `templates/reviewers/behavior-contracts.json` + `generate-reviewer-profiles.js`, then `--write`
  result: One identical bullet on all three roles (nothing about the roles differs here, so one wording). Pinned ABSOLUTELY as `PROMPT_DEFENSE_VECTOR_LINE` in `validateBehaviorContracts` — exact text, because a fragment cannot carry a polarity and exact-match is what survives a uniform rewrite of all three roles at once. Mutation-proven armed on five separate mutations: deletion, replacement with innocuous prose, a one-byte reword, the uniform three-role rewrite, and version left at 2. **The version bump had to move 11 files** — the constant has ~10 homes (generator, codex-preflight ×4 incl. edition twins, install-codex-agent-profiles ×3, validate-vendored-agents ×2 assertions, validate-*-contracts ×3, `install.sh` heredoc, and 3 literals in test-install-model-rendering); `edition-sync --write` propagated only 3 of them. That sprawl is a "one rule, one wording" finding in its own right, recorded not fixed — restructuring it would touch the cross-edition byte anchor mid-release.

- item: audit the 16 of 40 `scripts/test-*.js` that derive part of their universe from a generator or schema module they also test, asking each one question — does anything render to a consumer that this guard never reads? This is the reading that found all five of #887's defects, applied to the rest of the corpus. Either it finds more #887-shaped defects, or silence retires the question. #887(c).
  status: done
  dispatched: self — read-only sweep, findings land as edits in `scripts/` or as a recorded decline
  result: **One real gap found and closed.** The ADR 0017 retired-vocabulary ban (`node-id`, `gate_*`, `change_gate`, …) lived in exactly ONE generator and covered only its 12 reviewer surfaces; the 18 routing surfaces had no scan at all. Clean today, but the same class has shipped twice — `node-id` through a generator's render (#885) and retired node/DAG wording in the plugin manifests (#882) — so this is observed, not speculative. Closed by exporting the one list and scanning `renderedAll` + `committedAll` in `test-generate-routing-surfaces.js` (432 assertions, up from 430), with an anti-vacuity floor pinning the scan at 36 surfaces. Mutation-proven: injecting `node-id` into `next.skeleton.md` reds it on all three forges by name; skeleton restored byte-identically.
  **Declined, recorded:** the opencode and kimi renders also carry no retired-vocabulary scan. Their surfaces derive from canonical, which IS now scanned on both render families, and no token has ever entered through one of their transforms. No observed failure, so nothing is built — silence is an answer.
  **Scope, stated honestly:** the render-family question applies to 4 of the 16 (reviewer, routing, opencode, kimi) and all four were checked. The other 12 guard behaviour — claim, finalize, run-chains, kernel conformance, the allowband — whose consumers are JSON envelopes and state files rather than rendered prose, so "what renders to a consumer that this guard never reads" does not apply to them. They were not audited to that depth and this is not a claim that they were.

- item: post the verdicts to #886 and #887 and close both — including the correction that #886 item 1's headline claim ("carry no prompt-defense block") is wrong, and the mutation proof for item 3 (delete `gitea` from both edition tables: `test-route-reachability` green at an unchanged 325 assertions, `test-generate-routing-surfaces` red first at `registry derives 18 surfaces` 18→12). Items 2, 4, 5, 6 close as deliberate declines with their reasons.
  status: done
  dispatched: self — two comments via `gh api`, closure rides the sink
  result: https://github.com/KaolaBrother/Kaola-Workflow/issues/886#issuecomment-5148211015 and https://github.com/KaolaBrother/Kaola-Workflow/issues/887#issuecomment-5148211128. Both carry the corrections rather than just the verdicts: #886 item 1's headline was wrong, item 7's count was wrong by ~7x and hid a dead instrument, and #887's own candidate mechanism catches 1 of the 5 observations it was derived from.

- item: CHANGELOG under `[Unreleased]` plus any doc surface the above touches, then finalize, chains, sink to main.
  status: todo

- item: cut the release at 9.0.0 — 97 bullets since 8.0.1 and the span contains the ADR 0017 demolition, so a frozen `workflow-plan.md` has no executor. `--prepare` → commit release files only → `--release-check` → `--tag` → `--push`, per the settled sequence.
  status: todo
