# Close #950 (stale assertion absolute in docs/conventions.md) and #951 (A30 footer-line blind spot in the opencode edition)

- item: Measure #950's premise — read `docs/conventions.md:325` verbatim, run `node scripts/test-route-reachability.js` for its plain total (issue claims 331, doc claims 325), and re-run the forge-deletion mutation the clause actually describes (delete a forge from both edition tables; the doc says the *other* check fails 18→12 while this one stays green) on a scratch mirror so we know what that mutation really yields. The 325 is a count-under-mutation, not the plain total, so 331 must not be substituted blindly.
  status: done
  dispatched: investigator subagent, read-only measurement; report lands at kaola-workflow/bundle-950-951/.cache/premise-950-route-count.md
  result: .cache/premise-950-route-count.md — issue's DIAGNOSIS REFUTED. The defect is not an off-by-6: the load-bearing clause "`test-route-reachability` stays green" is FALSE. Under the forge-deletion mutation that suite now exits 1 (`T19b universe: … 6 … found 4`), because commit 97df0d6f gave the forge term its own hand-typed absolute (`codexEditions`, a literal at test-route-reachability.js:141) — i.e. the doc's counter-example was REPAIRED and the prose still describes the pre-fix world. Trap: under mutation the run executes 324 passed + 1 failed = 325 assertions, the doc's exact numeral, reached for an unrelated reason while RED — so "verifying" the doc by its count confirms a false sentence. Plain total 331, confirmed. Sentence was exactly true when written at 40486659 (plain 325, mutated green at 325); broke at 97df0d6f, before the #945–#948 base, as filed. THREE sites carry the claim and must move together: docs/conventions.md:325-326 (false), docs/conventions.md:315 table row (half false — 12→8 confirmed, "unchanged assertion count" false), scripts/test-route-reachability.js:757-759 source comment (false, and it sits inside the suite that now catches it).

- item: Measure #951's premise — on a scratch mirror, apply the issue's `remediationLines` mutation in `scripts/sync-opencode-edition.js` (gate `sourceEdits` behind `flag ? [] : ...`) and confirm the opencode suite still exits 0 at ~563 assertions; then apply the contrast mutation (prefer the stronger `--write-config` when write-clearable and flag-irreducible mismatches coexist) and confirm it reds exactly 1 assertion on the new A30 `{WRITE + SOURCE_EDIT}` scenario. Both legs are read-only measurement; never leave the mirror's edits in the real tree.
  status: done
  dispatched: investigator subagent, read-only measurement on a scratch mirror; report lands at kaola-workflow/bundle-950-951/.cache/premise-951-a30-blindspot.md
  result: .cache/premise-951-a30-blindspot.md — claims 1 and 2 CONFIRMED to the digit (563 green under the mutation; contrast reds exactly 1 on the new scenario). Claim 3 QUALIFIED: "information-preserving" is REFUTED as stated — the footer's unique content is the NEGATIVE SCOPE claim that the `Fix:` line above does not cover this file; the deletion is path- and remedy-preserving but not scope-preserving. Three findings the issue does not carry: (i) the UNCONDITIONAL deletion (`sourceEdits = []`) is ALSO 563 green — zero assertions in the whole opencode suite observe that line, so the blind spot is wider than filed; (ii) severity is lower than it looks for the filed mutation — the footer REAPPEARS verbatim on the re-check after following the advice, though not for the unconditional variant; (iii) `docs/opencode-edition.md:362` carries the line verbatim and its rule at 348-352, and no script consumes that doc — a second, independent copy of the blind spot on a prose surface. Option (b) restate-for-a-drivable-outcome is IMPOSSIBLE without a new capability (no flag clears the class; the remedy is a source edit to PLUGIN_SCRIPTS). NEW option the issue did not consider, measured: a non-wording occurrence-count discriminator ("the flag-irreducible path is named at least twice") catches the filed mutation on 2 of 3 scenarios and the unconditional one on 3 of 3, invariant to rewording, +3 assertions (563→566). A prose-sentence pin would be the file's first; a TOKEN-level output pin already has precedent at line 2818 (`PLUGIN_SCRIPTS`).

- item: Put #951's value call to the user in conversation — the issue explicitly declines to propose a fix because whether the report's contract guarantees the source-edit footer line is a decision about what the report promises. Options are: pin the footer wording as a declared exception to the band's no-wording-pin design, restate the report so the footer carries an outcome A30 can drive, or accept the blind spot and record it as an ADR 0017 watch-list row. First Principle 4 — do not decide this alone.
  status: done
  dispatched: self, asked in conversation with the measured option space (the issue's option (b) is impossible; a non-wording occurrence-count discriminator was offered as a fourth)
  result: USER RULED — accept the blind spot, record a watch-list row, ZERO code change. Both the code surface and the prose surface (docs/opencode-edition.md:362 + its rule at 348-352) go in the SAME row, not a separate issue and not a new doc-consuming guard. Rationale matches the governing rule: the class was produced by deliberate mutation, never observed as an escape, and #951 itself opens "Not a defect". No wording pin, no structural pin.

- item: Fix #950 with whatever the measurement yields — either write the count the re-run mutation actually produces, or restate the clause so it carries no absolute (the suite prints its own total). `docs/conventions.md` is not test-consumed so this stales no receipt. Verify the surrounding rule still reads correctly: the point of the sentence is that one absolute count belongs in a different file.
  status: done
  dispatched: self, inline — the reasoning is the work and all three sites are small, surgical prose
  result: three sites repaired. (1) docs/conventions.md prose — the anchor claim is KEPT (18→12, measured true) and the counter-example is RE-POINTED to the walkthrough's `testAxiomBlockByteIdentity`, which passes at 12→8 because its width `FORGES.length × (2 + runtimeEditionCount)` shrinks in lockstep; the paragraph now records that `test-route-reachability` held that green side until `codexEditions` gave its forge term a hand-typed absolute, and adds an explicit "quote no assertion total in this paragraph", naming the coincidence that made the stale numeral still reachable under the mutation. (2) docs/conventions.md:315 table row — the table is a historical record of five OBSERVED defects and is true as history, so it was NOT rewritten; it gained a `*(since re-anchored)*` marker and a past-tense "was", so a present-tense reader is not misled. (3) scripts/test-route-reachability.js:755-762 comment — narrowed from "this suite stays green" to "THIS FLOOR stays green", with an explicit do-not-widen paragraph naming T19b and the codexEditions literal as the reason the suite now reds.

- item: Implement #951's ruled outcome — a watch-list row in `docs/decisions/0017-the-mission-list.md` § The watch list, adjacent to the existing "nothing asserts a mismatch has a remedy" row, naming the observation that would arm it and carrying the measurement (both the conditional and the unconditional deletion leave test-opencode-edition.js green at 563/563) plus the prose-surface copy at docs/opencode-edition.md:362 / :348-352 that no script consumes. NO code change, no pin — that is the ruling, so resist the pull to add a guard while writing the row. If the kimi gap measurement comes back positive, the row names that too.
  status: done
  dispatched: self, inline — a decision record carrying a ruling I took to the user, so the wording is mine to own
  result: one row appended to the watch-list table in docs/decisions/0017-the-mission-list.md, carrying both mutation legs (563/563 green conditional AND unconditional, unconditional byte-identical to baseline), the mechanism of the blindness (A30 quantifies over ADVICE_RE runnable invocations; the footer is prose so never enters `advised`), the prose-surface copy at docs/opencode-edition.md:362 / :348-352 that no script consumes, the bounded severity (line returns on re-check for the conditional form only), and BOTH sized-but-unbuilt mechanisms — the occurrence-count discriminator and K12's outcome-pin shape — with the sharpest arming fact recorded plainly: opencode is the edition missing a guard its sibling kimi already has. Zero code change, as ruled. Also closed a stray blank line that had split the watch-list table in two, so the new row lands in the same table rather than starting a third.

- item: Close the two gaps the premise reports left explicitly unmeasured, because both decide how wide the repairs must be. (a) For #950: `simulate-workflow-walkthrough.js` around line 12010-12021 carries a sibling comment claiming "this floor stays green" under the same forge-deletion mutation; the investigator inferred it is still true (its expectation and measurement are both registry-derived) but did NOT run it and said explicitly not to repair it on that say-so — so run the walkthrough under the mutation on a scratch mirror and settle it. (b) For #951: `scripts/sync-kimi-edition.js` was never examined and may share the `remediationLines` shape; if it does, the same blind spot exists in the kimi edition and any remedy that lands in opencode alone would violate one-rule-one-wording.
  status: done
  dispatched: investigator subagent, read-only measurement on scratch mirrors; report lands at kaola-workflow/bundle-950-951/.cache/premise-gaps-walkthrough-kimi.md
  result: .cache/premise-gaps-walkthrough-kimi.md — (a) the walkthrough comment at simulate-workflow-walkthrough.js:12012-12021 is TRUE and is NOT a fourth site: at full scope, unmutated and mutated both give 209/209 and exit 0, and the full-log diff differs on exactly ONE line — `testAxiomBlockByteIdentity: PASSED (12 surfaces)` vs `PASSED (8 surfaces)`. Its expectation is `routing.FORGES.length`, derived from the edition tables, so both sides shrink in lockstep exactly as its comment claims. Known-bad set stays at THREE. That also makes it the LIVE counter-example the repaired prose should point at, since it is registry-derived, stays green by design, and says so in place. (b) kimi has NO equivalent: no `remediationLines`, no REMEDY taxonomy, no plugin/user-config class — one unconditional `Fix:` line covering a universe `--write` clears entirely. An opencode-only fix is COMPLETE and owes no twin; `ls scripts/sync-*-edition.js` is exactly two. Sharpest new fact: kimi's own line IS pinned, by K12 — deleting it takes 521/exit 0 to 2 failures/518 passed. So opencode is the edition MISSING a guard its sibling has, which is the observation that would arm the watch-list row. Two environment faults recorded: a `git archive` mirror false-reds the contract validator (no `.git`), and the full walkthrough (~9 min) outlives a 600s tool cap — a reaped log shows EXIT=1 with no error text and is trivially misread as a real red.

- item: Adversarially verify everything that lands — read what ships, not what was authored; a green suite is not proof a guard is armed, and a docs claim is only closed if the number in the file matches a measurement someone actually took. Check the #950 clause against a fresh run and the #951 artifact against its mutation.
  status: done
  dispatched: two subagents concurrently on the committed working-tree diff — an adversarial-verifier falsifying every factual claim in the new prose (report to kaola-workflow/bundle-950-951/.cache/verify-950-prose.md) and a code-reviewer on the whole diff incl. table rendering and scope (report to kaola-workflow/bundle-950-951/.cache/review-diff.md); BOTH LANDED and both returned verdict=fail, 7 in-scope findings between them, all now corrected — a third adversarial-verifier is in flight on the CORRECTIONS themselves, report to kaola-workflow/bundle-950-951/.cache/reverify-corrections.md
  result: THREE review rounds, 14 defects found and fixed; reports at .cache/review-diff.md, .cache/verify-950-prose.md, .cache/reverify-corrections.md. Round 3 (on the CORRECTIONS) found six more, which is the round that justified itself: my repaired sentence still mis-stated the rule ("sits inside the suite it rescues, NOT IN A DIFFERENT FILE" — refuted, because `codexEditions` IS in a different file from the registry; the rule never says different from what, and the sanctioned exemplar satisfies both readings, so the disqualifier had to be restated as measurable facts); REVERTING the table row reintroduced the stale claim, leaving docs/conventions.md:315 the last live carrier in the repo, now dated `(325→325)` instead; the ADR's "cannot see the unconditional form at all" was refuted by building both probe variants (scoped catches it on 2 of 3, measured 565 vs 566); and the CHANGELOG carried three of its own — an ambiguous "predates it" referent (50 days under the natural reading, 60 under the intended one), a claim that all three sites moved when the diff is a single hunk and the row was untouched, and "the paragraph quotes no assertion total" sitting in a paragraph containing three of them. Fixed each, then caught three grammar breaks of my own making in the rewrites. Suites after all corrections: route-reachability 331, generate-routing-surfaces 434, finalize-door 490, ledger-compare 40, opencode-edition 563 with 3 trees in parity, both contract validators, and generate-routing-surfaces --check — all exit 0. Full detail below from the first two rounds.

  PARTIAL — first two rounds recorded here so the work is not lost if this run dies. Both reviewers independently converged on the same sharpest defect: my prose credited a RE-ANCHORING THAT NEVER HAPPENED. Measured: the floor at test-route-reachability.js:782 is still `ROUTING_FORGES.length`-derived (probe: expected=12 actual=12 unmutated, expected=8 actual=8 mutated, passes both), `codexEditions` is not new but dates to f61f4ce5 2026-06-11 — two months earlier — and 97df0d6f was +232 insertions with ZERO deletions, i.e. a new unrelated band for #944, not a repair of the guard. My table marker `*(since re-anchored)*` directly contradicted the source comment landing in the same diff. Also refuted: the nine-day interval was on the wrong side of the break in the code comment (true for nine days, wrong for one); the ASSERTION TOTAL went stale the SAME DAY it was written (325 at 40486659 07:08 → 323 at 6fdbf714 11:51, four hours), so only the CLAIM survived nine days; "byte-identical to baseline" is true of BOTH mutation legs, not just the unconditional one; the ADR's guard sizing named a condition (flag-advised, 2 scenarios) inconsistent with its own arithmetic (+3 over 3 scenarios); and the opencode rule bullet is :349-353, not :348-352. All seven fixed. I then caught one of my own in the rewrite: I had written that the literal "sits in the same file as the registry it checks", which is false — codexEditions is in the test file, the registry in generate-routing-surfaces.js; the rule's actual point is that it self-anchors INSIDE the suite it rescues. Confirmed and unchanged: the anchor claim 18→12, the walkthrough re-pointing (209/209 both legs, differing on one log line), the crux that the floor stays green, the ADR table merged to one table with consistent columns, and the #951 no-code ruling honoured with sync-opencode-edition.js untouched.

- item: Dock documentation and finalize — `CHANGELOG.md` under `[Unreleased]` before the chain receipt run, then `/kaola-workflow-finalize bundle-950-951`. Chain scope: the diff touches root `docs/` (#950) and possibly the opencode edition (#951); run-chains diff-scopes at finalize and an edition-touching diff fails closed to all four, so let it decide rather than pre-selecting. opencode has its own suite that `npm test` does not run.
  status: done
  dispatched: self — CHANGELOG.md `[Unreleased]` written and twice corrected under review (#950 under Fixed, #951 under Added), full-scope walkthrough green at 209/209 with the terminal marker and exit 0, then handing off to /kaola-workflow-finalize bundle-950-951
  result: Docked and finalized. Caught at finalize that all four edits were sitting UNCOMMITTED IN THE MAIN CHECKOUT ON `main` rather than on the run's own branch — the worktree was provisioned at claim time and never entered. Moved them by patch, verified all four byte-identical between trees before restoring main, copied the run folder across, and committed on `workflow/bundle-950-951` as 8b6eeb48 (`Refs #950 #951`, per this repo's convention that the sink owns closure, plus the Co-Authored-By trailer the last two impl commits carry). Chains: scope decision **all-four** on `edition_coupling`, all green at headSha 8b6eeb48. doc-updater made NO edits and returned a verified no-impact reason per checklist item, independently re-deriving the 12→8 arithmetic and confirming no fourth carrier of the stale claim — the two near-miss hits in docs/opencode-edition.md:402 and docs/kimi-edition.md:364 are a different, still-true claim about an edition addition not perturbing the baseline, and the dated D-5xx/D-6xx decision records are historical by the same convention this change applied to the table row. Docking DOCKED, gap-sweep gate pass (0 mapped, 0 filed, 0 noise), finalize --check ok:true with reasons:[] and validation chains_green. Closure: both issues closed — ADR 0017 is explicit that a watch-list observation must not linger as an open issue.
