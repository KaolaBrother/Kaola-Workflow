# Docs Lookup — issue-160

docs-lookup: N/A — internal patterns sufficient.

All relevant behavior involves internal Node.js code and CLI argument parsing. No external library, framework, or API documentation needed. The discrepancy is between internal docs (docs/api.md, README.md) and internal implementation (claim scripts). Fix uses only existing patterns already established in the codebase.
