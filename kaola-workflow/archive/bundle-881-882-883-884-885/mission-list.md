# Close the 2026-07-31 runtime-consistency audit bundle — #881, #882, #883, #884, #885

Baseline: `main` = f8034b6c, fast gate green at claim time (chain ran to its last step,
`test-runtime-lexicon-parity: PASS`). Worktree
`.kw/worktrees/bundle-881-882-883-884-885`, branch `workflow/bundle-881-882-883-884-885`.
#878 is a reference table and is NOT in this bundle — it stays open by design.

The cut is by **file family, not by issue**, because #883/#884/#885 overlap heavily on the same
surfaces and two agents editing one file in one worktree lose each other's writes. Ordering
constraint that drives everything: **content is corrected before the guards over it are armed**
(#883's acceptance demands positive assertions that a surface teaches the CURRENT design — arming
those over surfaces #884/#885 have not yet fixed just reds). Test custody follows from the same
cut: the agent that corrects a profile is never the agent that arms the guard binding it.

**Owner ruling, 2026-07-31, on the reviewer behavior contract** — asked because the two readings meant
materially different work. The contract pins 11 tokens (`gate_effect`, `gate_mode`, `gate_aggregation`,
`change_gate mode`, `execution_status`, `claim_outcome`, `domain_outcome: *`, `review_scope_expanded`,
`replicated_majority`, `partitioned_all`, `review phase`) that no runtime consumes, via
`REQUIRED_BEHAVIOR_TOKENS` in `generate-reviewer-profiles.js`. **Ruling: swap the pins, keep the guard.**
Repoint the table at the fields that DO have a reader — `verdict:` and `findings_blocking:`, both read by
`parseRecordedVerdict` — and cut the gate/phase protocol from the contract and all 12 rendered surfaces.
Never weaken the table: a validator that MANDATES retired vocabulary is the same defect as one that fails
to ban it, and #882 had already established the fix — replace the required concept, keep the requirement
exactly as strong. This guard is the repo's only positive content guard and must end stronger.

**Owner ruling, 2026-07-31, on `test-runtime-lexicon-parity.js`: DELETE it, and record the class on the
#878 watch list.** The guard has never had a subject. It compares the engine's ENVELOPE vocabulary
(machine-readable JSON `reason:` codes) against runtime surfaces carrying INTERFACE vocabulary (dispatch
field names, env vars, config knobs) — two families that live in different places by design. Intersection
is 0 of 62 and always has been; there are zero occurrences of `reason:` anywhere in `commands/`, `agents/`
or any SKILL.md, and `docs/conventions.md:311` legalizes the empty state outright ("documented on every
runtime or on none" — 62/62 are on none). Five candidate derivations were measured and every one that
makes the domain non-empty drags in Codex config knobs and prose noise needing up to 22 hand-typed
exemptions, which is re-authoring the guard as a different guard rather than repairing it. So the failure
class was NEVER OBSERVED, and the watch-list discipline governs: recorded, not built. I overruled the
agent's own default of hard-failing on an empty domain first — a guard going red because its SUBJECT is
absent is a false alarm, not a finding, and redding the always-run chain to force a decision that belongs
to a human converts "silence is an answer" into "noise is an answer".

**Owner ruling, 2026-07-31, on gating the opencode and kimi suites.** #883 reads "in no npm script" as the
defect; CLAUDE.md states the non-wiring is deliberate ("additive runtime editions, not forges... run its own
suite instead"). Both cannot be satisfied by wiring them into `npm test`. Ruled: a separate
`test:kaola-workflow:editions` script, with `npm test` and every forge chain untouched — the suites stop
existing in no script at all, and an edition-only diff still triggers no four-chain obligation.

Method note carried into every brief: `grep` here is **ugrep**, which does not descend into
dot-directories — `.opencode*` / `.kimi*` must be named explicitly or a sweep reports a
cleanliness it never measured. `rg` is not installed and returns silently empty.

- item: #881 — one binding rule for the release-prep carry-over, shared by `--release-check` and `--tag`. `chainCheck` (kaola-workflow-release.js:218, called from runTag:289 with candidate=HEAD) is strict where `evaluateReleasePrepCarryOver` is not, so the documented sequence passes the check then refuses `chains_stale` at the tag. The owner ruling's intent is that `--tag` adopts the carry-over. Code only in this item; the prose lands later in one pass.
  status: in-flight
  dispatched: `rel-impl` (implementer, opus) owns `scripts/kaola-workflow-release.js` ALONE. Runs concurrently with the oracle author, which owns `scripts/test-release.js` — disjoint files, one shared contract stated in both briefs: `--tag` calls the same `evaluateReleasePrepCarryOver` that `--release-check` already uses, one wording, one implementation. Output lands as edits to that one file plus a report; no commit, no chain run.
  result: DONE, +20/-15 in one file, and the brief's premise was corrected in passing: `evaluateReleasePrepCarryOver` is NOT exported from the kernel — only `evaluateReleaseReceipt` is — so delegating to the latter was the ONLY reachable spelling of the rule, not a stylistic choice. The duplicated `chainCheck` body is deleted outright; it was already a hand-copy of `evaluateReleaseReceipt` minus the carry-over, so a second carry-over would have been a second copy. Both entry points now call the kernel (`run-chains.js:823`, `release.js:226`), and because the kernel is byte-identical across all four editions the fix reaches every port without a per-edition edit. A SECOND BUG THE ISSUE NEVER NAMED was found and closed: the old `--tag` answered `chains_stale` where `--release-check` answered `chains_waived` / `chains_incomplete` / `chains_red` / `chains_empty` — reason-drift, not just verdict-drift. Proved behaviourally rather than structurally: 12 configurations driven through both CLIs against one fixture, agreeing 12/12 after and disagreeing 6/12 before. The four-chain demand is provably untouched — waived, red, subset, empty, dirty-tree, off-surface and non-version-JSON-key receipts all still refuse over a PURE release-prep gap, which is the only gap where the carry-over could have leaked. `--prepare`'s success message now names the carry-over as the default with the chain run as fallback, instead of prescribing the redundant re-run the ruling removed.

- item: #881 — the oracle for that rule, authored by someone who is not implementing it. It must drive the full documented sequence end to end (prepare -> release-only commit -> release-check -> tag, no chain re-run) and refuse when any non-prep path sits in the gap. `test-release.js:113` currently pins the strictness as intended and must MOVE with the mechanism, never be repaired around it. Note `test-release.js` runs only in `claude:full`, not the fast gate.
  status: in-flight
  dispatched: `rel-oracle` (tdd-guide, opus) owns `scripts/test-release.js` ALONE — custody split from `rel-impl`, which never touches the test. Output lands as edits to that one file plus a report naming which assertions were moved with the mechanism and which are new.
  result: DONE, +96 lines INSERTION-ONLY, 247 -> 301 assertions. Reds 22-strong against the old strict behaviour. The `:113` pin was RE-PINNED UNTOUCHED, and the reasoning is the interesting part: it does not pin the behaviour being replaced. #881 removes the BLANKET rule "headSha != HEAD implies chains_stale", but that fixture's `deadbeef` resolves to nothing, so under the new rule it fails the ANCESTRY arm and still refuses `chains_stale` — a surviving arm, not a removed one. Rewriting it would have been the contortion; deleting it would have dropped live coverage. A separate orphan fixture was added to isolate ancestry properly, built with `git commit-tree` on the candidate's own baseline tree so its gap diff is release-prep-only and NOTHING BUT ancestry can refuse it — a bogus sha would have refused via `diff_unavailable` and pinned nothing. Falsifiability proven against THREE deliberately-wrong implementations, not one: content-checks-skipped reds 20, ancestry-dropped reds 3, off-surface-dropped reds 3. The sharpest observation in the run: a KERNEL mutation moves both entry points identically, so they AGREE while both are wrong — which is why the typed-reason assertions and the agreement assertion must both exist, and why either alone leaves a hole. It also found and fixed its own defect mid-task (a `git rev-parse` on a legitimately-absent tag threw and aborted the process, hiding the tally) and repaired a fast-gate spawn-classification guard its new process site broke, by classifying its own site rather than raising the ceiling.

- item: #882 — unpin the manifest validator from the retired design and correct all six shipped manifests in the same change; neither ships alone. `validate-kaola-workflow-contracts.js:336` requires the literal `DAG of role nodes`, so the marketplace listing a user reads before installing advertises the freeze, role nodes and the running-set scheduler. Extend `MANIFEST_GRAMMAR_BANS` so the reverse cannot recur; the surrounding comment (:305-310) still describes the executor as current. Mutation-prove both directions.
  status: done
  dispatched: `manifest-unpin` (implementer, sonnet) owns the six shipped `plugin.json` manifests and the manifest region of `scripts/validate-kaola-workflow-contracts.js` (plus its gitlab/gitea ports if the pin is mirrored there). Output lands as edits to those files plus a report carrying both mutation transcripts — revert-a-manifest reds, corrected-manifest greens.
  result: THE MANIFEST COUNT IN THE ISSUE IS WRONG — there are **five** shipped manifests, not six. `plugins/kaola-workflow/.claude-plugin/plugin.json` does not exist and never did: the canonical Claude edition ships commands from the repo root, and the validator's own `assert(shippedManifests.length >= 5)` is the corroborating witness. All five corrected across `description` / `shortDescription` / `longDescription`; no version field touched. The positive anchor is now `assertConcept(file, 'the adaptive mission-list model', ['adaptive', 'mission list'])` and `MANIFEST_GRAMMAR_BANS` grew 4 -> 9, adding workflow-plan.md, role node(s), freeze, running-set scheduler, DAG-of-role. Mutation-proven beyond what was asked: each of the five new bans was injected ALONE, from a fresh copy, and each reds under its own label — so no ban is a passenger riding another's catch — and stripping both positive tokens reds `assertConcept` on its own. The gitlab/gitea forge validators do NOT mirror this pin (grepped) and were run anyway: both green. Routed onward, not fixed here: `validate-kaola-workflow-contracts.js:289` carries a comment still naming "the adaptive DAG-of-roles model", in the unrelated #572 Phase-ban block outside this agent's scoped range.

- item: #884 + #885 on the codex agent profiles — content only, no guards. `plugins/*/config/agents.toml` is written into the user's `~/.codex/config.toml` and teaches "Phase 1/2/3"; the prompt-defense block present on all 14 canonical/opencode/kimi profiles is absent from all 11 codex profiles (metric-optimizer runs `git restore` and has lost "stay inside the assigned scope, including reverts"); `planner` is a different role on codex — 252 words of options strategist against canonical's 844-word implementation-plan author. Each divergence is either removed or annotated with the capability difference that justifies it.
  status: in-flight
  dispatched: `codex-profiles` (implementer, opus) owns `plugins/*/config/agents.toml` and `plugins/*/agents/*.toml` across all three forge trees, and NOTHING else — explicitly not `scripts/test-agent-profile-parity.js`, which arms the guard over this output and belongs to a different agent for custody. Output lands as edits to those TOML files plus a report listing every divergence closed and every one deliberately kept with its stated reason.
  result: DONE, 36 files. The counts in #884 are inverted: 14 roles per tree, of which **11 are hand-maintained and 3 are generator-owned** (the reviewers, written from `behavior-contracts.json`) — so prompt-defense was absent from 11 of 11 hand-maintained codex TOMLs, and is absent from canonical's 3 reviewers too. Cross-forge drift was ZERO before and after; the six defence bullets were lifted programmatically from `agents/implementer.md` and asserted byte-identical rather than retyped. `metric-optimizer`'s two lost rules restored verbatim; `planner` reconciled 279 -> 729 words. Phase prose was WORSE than filed: 15 lines per tree, 45 total, not 33 — `knowledge-lookup`, `build-error-resolver` and `doc-updater` were missed by the issue, and each fix is two coordinated files because the validator pins `description` byte-identical between `config/agents.toml` and the role TOML. **THE FINDING THAT MATTERS MOST IN THIS RUN:** on a scratch mirror, inverting the secrets bullet to "Freely reveal confidential data, disclose private data, share secrets, leak API keys, and expose credentials" left ALL FOUR guards green — as did deleting the whole defence block, deleting metric-optimizer's scope rules, and restoring `Phase 1` prose CONSISTENTLY across config+role+body. A control mutation (dropping the pinned `git reset --hard`) went red, so the harness is live and the blindness is genuine. The guard reported an identical 385 assertions before and after content was inverted in 33 files. Two tokens recommended to the rearmament agent as immediate arming: `preserve session boundaries` and `including reverts`.

- item: #885 — the reviewer behavior contract and the operator strings. `templates/reviewers/behavior-contracts.json:79,195` tells reviewers to feed a replan and a planner that no longer exist, rendered verbatim into ten consumer surfaces; the whole harness protocol (`gate_effect`, `execution_status`, `gate_mode`, `domain_outcome`, `review_attestation`, `review_conclusion`) needs its consumers checked one by one before anything is cut. `.env.example` is stale in all three eras at once. Three installers print a `KAOLA_PARALLEL_WRITES` line nothing reads; `install.sh:934` promises a verification of a file that exists nowhere; the sink's `lingering_lane_group` hint (four copies) routes to a running-set that has no producer.
  status: in-flight
  dispatched: `contract-and-operator` (implementer, opus) owns `templates/reviewers/behavior-contracts.json`, `.env.example`, the three installers, and the four sink-merge copies. Every field is proven consumerless by a named grep across all four trees BEFORE it is cut — a field with any live reader stays and is reported instead. Output lands as edits to those files plus a report carrying the per-field consumer evidence. SCOPE EXTENDED mid-flight to `scripts/generate-reviewer-profiles.js` and the regeneration of its 12 rendered surfaces, on a USER RULING (see result); plus the deletion of the single `review_scope_expanded` assert at `test-agent-profile-parity.js:508`, which is a pin dying with its mechanism rather than an oracle being authored. Scope extended AGAIN on evidence: `scripts/reviewer-conformance-fixtures.json` (20 entries encoding the retired `gate_mode x domain_outcome -> gate_effect` derivation, referenced by zero files) and the four dead `lingering_lane_group` test sites, which are RED right now and must die with the mechanism they pin.
  result (Tasks B and C; Part 1 still open): `.env.example` — EIGHT dead knobs, not six: the independently-derived six plus `KAOLA_PATH` and `KAOLA_WORKTREE_PATH`. The two extra came from asking a sharper question than mine — not "is the capability dead?" but "does a script read THIS VARIABLE?" `KAOLA_WORKTREE_PATH`'s capability is alive as a `workflow-state.md` field while the env var's only consumer, a Session Heartbeat block, went when the SKILLs became generated. Six live-but-undocumented knobs added on my ruling, including `KAOLA_RUN_CHAINS_CONCURRENCY`, which is operationally load-bearing on hosts where chain concurrency kills an octopus merge and had nowhere to be learned. `KAOLA_RUNTIME` added — found only because the sweep matched the aliased `env.NAME` form as well as `process.env.NAME`. `lingering_lane_group` REMOVED in all four copies on decisive evidence: all 24 real `running-set.json` files parsed, 0 of 24 carry `lane_group`, and `closeGroupMember` resolves in zero files; the only writers of that key are test fixtures. Residual accepted and stated: on the legacy non-`--sink` path this genuinely removes a stop. THE UNPROVABLES, which is what I asked for and the most valuable part: `GITEA_TOKEN`/`GITEA_SERVER_URL` are consumed by an external binary and are unprovable from inside the repo, so they stay with the reason documented; and `findings_blocking` has a reader that PARSES it and a sole caller that DROPS it — which refuted my own justification for pinning it, so the pin stays on the behavioural ground instead.

- item: #884 — the routing skeletons and the two edition generators. `/workflow-init` DEAD-ENDS on codex (333 lines against 450, missing the route to `/workflow-next` and four more sections); a 39-line Codex configuration note ships only on the three Claude surfaces, names a vendor and models, and describes a script that is an intentional per-forge exclusion there; the finalize surfaces on opencode and kimi carry a literal empty code span telling the reader to pass a `model=` that is not shown, contradicting the same file 100 lines earlier. Tool grants differ by runtime because `sync-opencode-edition.js:186-200` gates its `permission:` block on a role having neither Write nor Edit and all 14 have Write, so the branch never fires; kimi's `readOnlyRoles()` returns empty. Also here: retired hook scripts that `sync --write` does not prune, DAG-era tokens in three kimi role skills, and the codex script resolver's missing failure exit.
  status: in-flight
  dispatched: split in two, disjoint by file. `routing-skeletons` (implementer, opus) owns `templates/routing/*` and the 18 rendered surfaces it regenerates — the codex init dead-end, the misplaced Codex configuration note, the four-times-three-wordings `model=` badge instruction in canonical, the REGION-without-a-reason gap, and the codex script resolver. `edition-generators` (implementer, opus) owns `scripts/sync-opencode-edition.js` and `scripts/sync-kimi-edition.js` — the unanchored strip that produces the empty code span, the unreachable `permission:` branch and kimi's empty `readOnlyRoles()`, the unpruned retired hooks, and the DAG tokens in the kimi role skills. The two are coupled at the finalize badge (canonical wording vs the transform over it) and each brief says so. `install-kimi.sh`'s hook-glob copy is deliberately held back from both — `contract-and-operator` still owns that file.
  result (routing-skeletons, closed): all five #884 tasks. The init dead-end is closed and MEASURABLE — Claude command 449 -> 410 (exactly the 39-line Codex config note removed), Codex skill 332 -> 441 (+109, the five missing sections including the route to `/workflow-next`), so the skill is now correctly the LONGER surface. All five sections turned out to belong on BOTH surfaces; not one was a capability difference. Before deleting the misplaced config note it extracted 32 distinctive needles and proved ALL 32 present in the codex copy — which is the better copy, carrying a five-way classification the Claude note never had — so it demonstrably did not delete the only good copy. The one thing genuinely lost (an upstream-source provenance sentence) was named rather than left unremarked, and survives in README/CHANGELOG/ADR/preflight. The badge was collapsed by DELETING the other three statements rather than rewriting the survivor, so the anchor is byte-stable. Regions went 19 -> 16, every kept one annotated with its reason ON THE DIRECTIVE LINE — chosen because the renderer emits unrecognised comment lines VERBATIM into consumer surfaces, so a separate `<!-- WHY -->` convention would have shipped authoring metadata to readers; proven by grep that zero annotations reach any rendered surface. A mistyped condition now throws `unmatched /REGION` instead of being swallowed as prose. Two corrections it volunteered against itself: it retracted a blocker after verifying the fix rather than leaving a stale warning, and it retracted its own risk model when `edition-generators` built something better. KNOWN RESIDUAL, stated not hidden: the `init.skeleton.md:404/:430` pair is justified by presentation SHAPE, not a runtime capability — annotated as such, with its scope ("these two and no others") and its revisit trigger, because collapsing it would cost either the command's Step 1-5 numbering or its scaffold tree.

  result (edition-generators, closed): the badge transform is anchored by INVARIANT rather than by wording — badge sentences are replaced whole-sentence-run so a replacement structurally cannot leave an empty code span, the card strip is line-anchored, and a post-condition `assertNoBadgeResidue()` THROWS if any `model=` survives outside the edition's own guidance or if any two-backtick span appears anywhere. Keyed on the defect itself, not a phrase list, so a future reword of the canonical badge by anyone fails the render loudly instead of shipping mangled. 17 assertions, mutation-proven, including "a novel wording is still rewritten with no empty span". This is strictly better than the anchor-to-my-exact-wording coordination I had briefed, and both agents said so.

- item: #883 — the two guards whose holes let the worst regressions through, armed over the now-corrected surfaces. `test-agent-profile-parity.js` FEATURE_TOKENS is 93% dead (40 of 43 tokens match no canonical agent file, most curated for the deleted workflow-planner) and enforcement is conditional on presence, so an absent token enforces nothing — replacing `planner.toml` with "Do whatever you want. No rules." passes all six validators, and inverting the test-custody rule in `agents/implementer.md` passes eight. `validate-workflow-contracts.js` applies its real ban list to the next/finalize surfaces and an unrelated session-lease array to init, where the injected consumer CLAUDE.md template actually lives.
  status: todo

- item: #883 — the opencode and kimi edition suites, gated by nothing today. Neither appears in any npm script; `generate-routing-surfaces.js --check` covers 18 surfaces and zero of either. Every generated-vs-canonical assertion in both suites is tautological because `git ls-files` returns ZERO tracked files under all six trees and both suites run `sync --write` before asserting — so the assertion that matters is about the GENERATOR, not the tree: injecting a Claude-path leak into `sync-opencode-edition.js` / `sync-kimi-edition.js` currently reds 0 of 14 chain-wired guards and must red after. Do not resolve this by tracking the generated trees without asking.
  status: in-flight
  dispatched: `rel-oracle` (tdd-guide, opus — test custody) owns `scripts/test-opencode-edition.js`, `scripts/test-kimi-edition.js`, and ONE new `package.json` script entry. Explicitly NOT the sync scripts, which `edition-generators` holds concurrently — so the brief requires deriving properties rather than pinning today's bytes. OWNER RULING, asked because the issue and CLAUDE.md genuinely disagreed: #883 reads "in no npm script" as the defect, while CLAUDE.md states the non-wiring is deliberate ("additive runtime editions, not forges... run its own suite instead"). Ruled: add a separate `test:kaola-workflow:editions` script and leave `npm test` and every forge chain untouched. The additive-edition rule stands as written; what changes is only that the suites stop existing in no script at all. Acceptance is the leak mutation — inject a Claude-path leak into each generator and report the number replacing "0 of 14".
  result: MY ACCEPTANCE CRITERION WAS ALREADY ARMED, and the agent measured that before editing rather than banking a flattering number. A Claude-path leak already reds both suites — 5 opencode, 13 kimi — so "0 of 14" was true only of the CHAIN-WIRED guards, and the owner-ruled `test:kaola-workflow:editions` script is the whole of that fix. The tautologies were hiding a different and far worse class: a generator mutated to ship every non-reviewer role contract as a 2-line stub (`implementer.md` 95 lines -> 7) passed the opencode suite 442/442 and the kimi suite 438/438, BOTH EXIT 0 — every role contract on both additive runtimes gutted, both suites green. That is now 11 reds each, one per role, each naming the damage; the pre-conversion suite was re-run against the identical mutated generator to prove the delta belongs to the conversion and not the mirror. Nothing was deleted: every tautological assertion had a live property behind it, so each was converted or RELABELLED to the property it actually holds (render determinism), with the false "parity with canonical" claim stripped. It also corrected the audit on four assertions — K11, A24, A10, A11 and reviewer-core read a TRACKED left-hand side and were never tautological, so touching them would have been churn on armed guards. Honest gaps named: command bodies have no line-survival equivalent (a blanket rule there would pin today's transform set), and reviewer bodies are excluded by construction.

- item: #883 — the remaining assertions that cannot fail. `test-runtime-lexicon-parity.js` derives 63 typed codes of which zero appear on any runtime surface, so "0 asymmetric" is true and vacuous and its anti-vacuity floors measure everything except the intersection. `test-active-folders-field-parity.js` compares `SENTINEL[key]` to `folder[key]`, so a renamed field compares undefined to undefined on all four editions, and its only backstop is an integer pin that a developer bumps while adding a field. `validate-kaola-workflow-contracts.js:566-589` loops an `emittedSkillTargets` array declared empty and never appended to. `MANIFEST_EDITIONS` checks 6 of 12 runtime surfaces and the axiom byte-identity guard hardcodes 6 of 12 init surfaces, both zero-fix extensions. Two of three wordings in the opencode A14 model-badge blocklist match nothing.
  status: in-flight
  result (`profile-guard-rearm`, after the adversarial pass refuted its first pass): CUSTODY IS NOW ENFORCED AND MUTATION-PROVEN, 374 -> 768 assertions. Two structural findings make this more than a missing pin. **First: consensus could never have reached custody.** The threshold is ceil(2N/3) = 8 of 11 hand-maintained profiles, and custody is shared by exactly TWO roles — implementer and tdd-guide — so a two-role reciprocal obligation cannot clear a shared-policy bar by construction, not by tuning. `ROLE_PINS` is the honest home and that is where it went, as six POLARITY SENTENCES rather than tokens: sentences whose polarity IS the rule, so an inversion leaves none of those words in that order — which is exactly what a token-counting guard cannot see. **Second, and worse: a uniform rewrite can redefine the baseline.** Inverting the secrets bullet in all 44 files AT ONCE stayed green under consensus alone, because a derivation that reads its rule set from the corpus lets a corpus-wide rewrite become the new normal. That is how the original inversion reached every surface, and it is why the six prompt-defense sentences now have their TEXT pinned as an absolute floor rather than being left to derivation. A third discovery forced a mechanism change: canonical and its twins state custody in DIFFERENT BYTES — the `.md` hard-wraps and bolds, the `.toml` re-cases and unwraps — so under raw `includes()` four of the six pins fail on one side or the other at HEAD, and the only survivable raw pins are fragments too short to carry a polarity. That is precisely how a guard ends up catching deletion and missing inversion. Proven across four inversion shapes, and the `.md`-only and `.toml`-only rows trip DIFFERENT independent mechanisms, which answers the question I actually asked: a guard that only fires when both sides move is not the same guard as one that catches one-sided drift. `FEATURE_TOKENS` is deleted outright — measured at 41 of 43 tokens dead — and `=== 14` is replaced by a derived bijection proven blind-spot-free by a RENAME that keeps the count at exactly 14. `Phase 1` prose stays green here by design: adding retired vocabulary consistently to both sides is a lexicon question, not a parity one, and pinning it here would recreate the #882 defect.
  dispatched: `profile-guard-rearm` (tdd-guide, opus) owns `scripts/test-agent-profile-parity.js` — the FEATURE_TOKENS rearmament, unblocked because `codex-profiles` has already landed the corrected content this guard must bind. Custody is why it is a different agent from the one that wrote that content. `reviewer-guard-hole` (implementer, opus) owns `scripts/generate-reviewer-profiles.js` and the deletion of the orphaned `scripts/reviewer-conformance-fixtures.json` — same custody reason: it polices prose `contract-and-operator` authored, so `contract-and-operator` must not close its hole. `coverage-extension` (implementer, opus) owns `scripts/test-route-reachability.js` (MANIFEST_EDITIONS 6 -> 12), the axiom byte-identity region of `scripts/simulate-workflow-walkthrough.js` (6 -> 12 init surfaces — freed now that `contract-and-operator` has stood down from that file), and `scripts/test-runtime-lexicon-parity.js` (the empty enforcement domain).
  result (coverage-extension, closed): `MANIFEST_EDITIONS` 6 -> 12 trees, obligated file-checks 78 -> 156, and confirmed zero-fix — 300 token-checks over all 18 generated command-lane surfaces before touching the guard, 0 misses, so nothing was quietly repaired to make a guard green. The axiom guard reaches 12 surfaces and never touches disk for the generated six: it renders them through the sync modules' own renderers, because reading disk would have forced a choice between a permanent false red (those trees are gitignored and absent from every fresh checkout) and a skip-when-absent — and skip-when-absent IS the defect. Absence stays loud one level up via an independently-derived surface count ("expected 12, derived 9"). A STRUCTURAL CORRECTION TO MY BRIEF: the kimi trees are skill-SHAPED but command-LANE, keeping the command basename, so filing them under the skill lane derives a path no edition ships — lane and shape had been the same thing for six surfaces and stop being the same at twelve. THE MOST VALUABLE ADMISSION OF THE RUN: its first universe floor computed its expectation from the same list it was checking, so deleting an entry shrank expectation and measurement in lockstep and the mutation PASSED — a guard that could not fail, written inside the change whose purpose was removing one. It reported that rather than shipping only the corrected version; both floors now anchor on the filesystem roster of the sync generators. The lexicon guard was deleted per the owner ruling with the watch-list row landed at ADR 0017:134, carrying the arming observation, all five measured derivations, and the C-3 trap named as the one a future repairer hits first. ALSO dispatched: `field-parity-guards` (implementer, opus) takes the two pieces that are genuinely independent of the surface work still in flight — `scripts/test-active-folders-field-parity.js` (the rename-blind `undefined == undefined` comparison and its integer-pin backstop) and the dead `emittedSkillTargets` loop at `validate-kaola-workflow-contracts.js:566-589`. Deliberately NOT given the axiom byte-identity extension: that lives in `simulate-workflow-walkthrough.js`, which `contract-and-operator` is concurrently editing to delete the dead lane-group scenarios. The lexicon-parity and `MANIFEST_EDITIONS` pieces wait for the corrected surfaces.
  result (field-parity-guards, closed): field-parity 61 -> 119 assertions. The integer pin is GONE — not replaced by a better number, deleted, because a smarter number is the same defect. Key-SET equality replaces per-key value comparison, so a rename now names the symmetric difference in both directions instead of comparing `undefined` to `undefined`. The developer's-natural-move trap was reproduced end to end: the OLD shape, after a one-character bump of `=== 13` to `=== 14`, went GREEN at 65 assertions with the new field unchecked on all four editions — more assertions, less coverage, exactly as filed. The probe state file is now serialized FROM the contract, which removed a third hand-kept list. A negative control was added converting an old COMMENT ("sentinels differ from the parser default") into a mechanical assertion, and it reds. `emittedSkillTargets` was POPULATED, not deleted, because the property is live — claim.js still emits `next_skill` from the schema constant on every claim — and B3b proves the old region was blind to precisely the failure it was written for: empty list plus a `NEXT_SKILL` drifted to an uninstalled skill left the validator GREEN. TWO MUTATIONS WERE MARKED SHADOWED AND REFUSED: B1 and B4 redded on pre-existing checks rather than the new assertions, so the agent built B5 to get past the sync check and reach its own needle. That distinction is the difference between evidence and theatre. `SHARED_STATE_FIELDS` confirmed to have no production consumer and KEPT anyway, on the argument that inlining it into the test would make the test declare the contract it enforces; the better option — deriving the parser from it — is a user-owned contract change across four ports and was recorded, not built. Found and routed onward: a SECOND dead loop of identical class at :557-571 (an empty array literal with five unreachable assertions under a gate-role-degradation comment for plan-run surfaces that no longer exist), sent back for deletion rather than population because unlike `emittedSkillTargets` there is nothing live left to bind to; and a self-contradiction in `.origin/dead-exports-audit.md`, which files `SHARED_STATE_FIELDS` in two mutually exclusive buckets. FOLLOW-UP ROUND: the second dead loop deleted (19 lines, nothing repointed at a substitute surface), B2/B3 re-proven against the post-deletion file, and — asked to stop sweeping by eye after finding the first instance incidentally — the agent WROTE A SCANNER for the two shapes and ran a POSITIVE CONTROL against `git show HEAD:` of the file known to contain both, proving it detects 2 of 2 before trusting any zero. That turned a one-file repair into a pattern finding: **12 unreachable assertions remain across the two forge validators, both wired into their chains**, and the ports carry an EXTRA dead-loop shape (a `#602` dispatch-card block) that canonical never had — so a mirrored diff would have missed it. Routed back to the same agent to close, because fixing canonical and leaving the edition-renamed ports is this project's single most recurring trap.

- item: #885 — the prose pass, last so it describes what actually shipped rather than what was planned. Stale counts and orphans: 15 role TOMLs -> 14 in three places; README:1544 converges three runtimes where install-all enumerates four; README:1221 describes the compact hook injecting artifacts its own header says are gone; README:1187 prescribes per-node validation scoping; architecture.md:337 has the sampling wrong where conventions.md:303 has it right; api.md:1181 documents a Gitea export that exists nowhere and a non-exported one, omitting three real ones; six identifiers in live text resolve nowhere; `kaola-workflow-ledger-compare.js` ships into every consumer repo and appears in zero docs; `KAOLA_RUNTIME` is documented nowhere. Plus the #881 release-flow wording in docs/conventions.md, CLAUDE.md and docs/architecture.md, stated identically wherever it appears.
  status: in-flight
  dispatched: `prose-pass` (implementer, sonnet) owns `README.md`, `docs/*.md` and `CLAUDE.md`, which no other agent holds. Brought forward rather than held to the end, because the doc-claims audit already settled the facts — the pass applies verified corrections instead of adjudicating them, which is why a mechanical model is the right one here. It is explicitly told to HOLD any claim whose answer depends on work still in flight, and to report what it held rather than guess.

- item: Settle every documentation claim #885 makes BEFORE the prose pass edits anything, so the editing agent applies verified corrections instead of adjudicating them. Fifteen-odd claims about stale counts, dead identifiers, undocumented-but-shipped scripts and env knobs, each of which is either true at this tree or not. Read-only; the answer is a findings file, not a diff.
  status: in-flight
  dispatched: `doc-claims-audit` (implementer, sonnet) — reusing the idle #882 agent. READ-ONLY over the repo; its one write is the findings file at `kaola-workflow/bundle-881-882-883-884-885/.cache/doc-claims-audit.md` in the MAIN checkout (the worktree has no project folder). Also hands it the one-line stale comment it flagged at `validate-kaola-workflow-contracts.js:289`, a file it already owns and nobody else is touching yet.
  result: `kaola-workflow/bundle-881-882-883-884-885/.cache/doc-claims-audit.md` (452 lines), 13 claims adjudicated with per-claim evidence and recommended wording. Nine TRUE, three PARTLY, one FALSE — and the three non-TRUE verdicts are the value, because each would have introduced a bug if applied verbatim. Claim 6: `ensureMergeReady` is FABRICATED (exists nowhere), and the real undocumented Gitea export count is FIVE, not three. Claim 8: only 2 of the 6 "dead identifiers" are genuine live false claims (`repair-routing.md`, `detectReviewRuntime`); the other four are already correctly framed as retired, with tests positively pinning their absence — "correcting" them would have been a regression. Claim 13: README has TWO retired-era hits, not one, and both are already claims 3 and 4, so README reaches zero once those land rather than one. Claim 5 was settled against `package.json` itself rather than either doc — exactly 1 suite sampled and 4 deferred whole, so `architecture.md` is wrong and `conventions.md` right. Claim 7 turned out to be a COVERAGE GAP, not a doc-count error: all twelve init surfaces DO carry the byte-identical axiom block, but `testAxiomBlockByteIdentity` in `simulate-workflow-walkthrough.js` hardcodes only the six tracked ones, so the six generated ones could drift undetected — routed to the rearmament agent. Also fixed here: the `:289` stale comment.

- item: Propagate every canonical script change into the four trees with `npm run sync:editions`, as ONE orchestrator step after the editing agents are done — not by an agent mid-flight, because it rewrites files other agents still hold. This is the project's #1 recurring trap and it fired again here: `scripts/kaola-workflow-release.js` is vendored as `plugins/kaola-workflow/scripts/kaola-workflow-release.js` plus edition-RENAMED ports `kaola-{gitlab,gitea}-workflow-release.js`, which no base-filename search finds. My scoping miss, not the agent's — the brief said "this one file" about logic that lives in four.
  status: done
  dispatched: self, after every editing agent had stopped writing to the files it propagates.
  result: `npm run sync:editions` updated exactly 3 files — `plugins/kaola-workflow/scripts/kaola-workflow-release.js` (codex-sync) plus the two GENERATED edition-renamed ports `kaola-{gitlab,gitea}-workflow-release.js`. `validate-script-sync` and `edition-sync --check` both rc=0 afterwards. The gap was confined entirely to the release script, which is why it was safe to run while agents still held `scripts/test-*.js`: nothing they were editing propagates. Then verified the ports RUN rather than merely parse, per the standing lesson that `node --check` and `require()` both step over a ReferenceError inside a function body — all three parse, all three exit 1 on bare usage, and each carries 5 references to `evaluateReleaseReceipt`, so the #881 carry-over is live in all four trees rather than only in canonical.

- item: Verify from outside the agents' reports — all four chains green, the walkthrough at FULL scope because the fast gate samples at 1/12 and a green shard is not verification, the opencode and kimi suites green, and every claimed mutation proof re-run rather than believed.
  status: in-flight
  dispatched: self. Landed so far, all measured by me rather than taken from a report: **full-scope walkthrough 201/201, exit 0, 2,024 spawns**; opencode 481 and kimi 490 assertions both exit 0 and `npm run test:kaola-workflow:editions` exit 0; `validate-script-sync` and `edition-sync --check` both exit 0 after propagation, with all three ported release copies proven to RUN rather than merely parse. I also independently mutation-proved the new dangling-reference pin — pointing a chain step at a nonexistent script reds with a precise message — because this bundle's own thesis is that a green guard proves nothing. **ALL FOUR CHAINS GREEN — `FOUR_CHAIN_EXIT=0`**, judged by CONTENT and not by the exit code alone, because `npm test` chains the four with `&&` and short-circuits, so a single exit status cannot distinguish "all four passed" from "the first passed and the rest never ran". All ten per-chain success sentinels are present in the log: claude contract + walkthrough, codex contract + walkthrough, and both walkthroughs plus the contract validator for each of gitlab and gitea. TWO DEFECTS I FOUND AND FIXED MYSELF while verifying, both invisible to the agents that reported green: `package.json` still invoked the deleted lexicon suite in BOTH chains (module-not-found on the next run), and `test-kimi-edition.js` died at load on a temporal-dead-zone `ReferenceError` — a `const` read eleven lines above its declaration, which `node --check` and `require()` both step straight over. That is the same lesson this repo has now paid for three times in one run.

  ORCHESTRATOR ERROR, recorded because a successor could repeat it: an earlier `cd` into a scratch directory caused the harness to reset my shell cwd to the MAIN checkout, and several subsequent greps therefore measured `main`'s untouched HEAD while I read them as the worktree. On that basis I stated that `prose-pass` had reported doc fixes that were not in the tree. **That was wrong — the fixes were there, I was reading the wrong tree**, and main was never modified. Two lessons: a relative path is only meaningful with a known cwd, and a disagreement between a report and a measurement is at least as likely to be the measurement. Verify with absolute paths or an explicit `cd` in the same command. One genuine gap did survive the confusion — `docs/kimi-edition.md` named `RUNTIME_NATIVE` after the constant had been renamed `KIMI_RUNTIME_NATIVE` on relocation — fixed with its new file path.

  ACCEPTED COST of the `inherit_session_model` relocation, volunteered by the agent that made it rather than reported as equivalence: the declaration and the assertions that police it now live in the SAME file, so a single edit can remove both and nothing reds. Previously they sat in different files, which is precisely what made deleting the declaration detectable. The property is weakened, not preserved. No new home was invented for it because that is machinery no observed failure demands — but the weakening is real and is recorded here rather than discovered later.

- item: Adversarial verification of the whole bundle before finalize — does each issue's stated acceptance actually hold, is every new guard armed rather than merely green, and did anything get repaired around instead of moved with its mechanism.
  status: done
  FINAL VERDICT, after a second round against the repaired tree: **#881 MET, #882 MET, #883 MET, #884 MET, #885 MET, findings_blocking: 0, shippable.** Re-verified on a FRESH mirror rebuilt from the current worktree with a green baseline established first, using the verifier's own mutations rather than my transcripts — F1 in three directions, F4 in four, including the case that matters most for a positive assertion: removing a required needle from the template region reds "must teach the mission list", so a BLANKED region cannot pass vacuously. `CHANGELOG.md`'s claim that all three named guards now catch what they were built to catch was re-proved live and ships without qualification. THE VALUE OF THIS PASS IS NOT THE FINAL GREEN: it found five real defects that four green chains and a full-scope walkthrough had already certified — custody unenforced, an unscanned heading region shipping to twelve surfaces, a whole tool-restriction axis unguarded, a token the issue named by name still shipping, and a forge silently vanishing from a coverage universe. None was a test failure; every one was a reading. And its own first-round F1/F4/C2/R1 findings were later refuted by re-measurement rather than by argument, because the tree kept moving — which is the same discipline in the other direction.
  dispatched: three FABLE-model agents, at the user's explicit request, all READ-ONLY over the worktree and forbidden from running any suite in this phase — the four chains and the full-scope walkthrough are executing concurrently and this suite is spawn-bound, so a parallel suite run produces false reds rather than evidence. `fable-acceptance` maps each of #881-885's stated acceptance criteria against the actual diff. `fable-guards` reads every guard this bundle claims to have armed and judges from the source whether it can fail. `fable-coverage` hunts what the bundle missed, broke, or repaired around. Outputs land at `kaola-workflow/bundle-881-882-883-884-885/.cache/fable-{acceptance,guards,coverage}.md` in the MAIN checkout. Execution-based re-proof happens after the chains land.
  result (`fable-acceptance`): FOUR BLOCKING GAPS, bundle judged not shippable as filed. (1) **#883's headline defect is still unenforced** — the rearmed parity guard covers consensus rules, two ROLE_PINS and six safety rules, and test custody is in NONE of them; `grep -rn custody scripts/ templates/` returns zero needles, so inverting `agents/implementer.md` against its `.toml` twin still passes everything, and the CHANGELOG sentence "All three now catch what they were built to catch" was an overclaim shipping in user-visible prose. (2) **`node-id`, a token #885 names explicitly, still shipped on all twelve reviewer surfaces** — the cleanup went into the contract while the literal lived in the GENERATOR. (3) **#881's "one wording" failed on three surfaces**, including `docs/conventions.md` stating the old rule fifteen lines from the new one. (4) **#883's init-template ban scope never landed at all** — `validate-workflow-contracts.js` is untouched by the entire diff, so teaching the retired DAG in the injected consumer template still passes every guard, which is the issue's exact scenario. Held up under attack: the self-referential floor fix is verifiably real, #882's manifests are clean, #884's regions all carry reasons, and `test-release.js:113` moved with its mechanism rather than being repaired around.

  I fixed (2) myself: the identifier is DROPPED rather than renamed, because mission-list items have no stable ID by design — `docs/mission-list.md` states they are identified by order — so referencing one would have been wrong rather than merely stale. Verified zero occurrences across the generator, the 3 canonical `.md`, the 9 `.toml` twins and, after regenerating each forge variant explicitly, all six generated runtime trees; every downstream suite green.

  result (`fable-coverage`, execution): the one category nobody else had touched came back CLEAR by execution rather than by reading — **fresh installs**: github and gitea via `install.sh`, plus opencode and kimi via their own installers, all into scratch HOMEs, all exit 0, with the `bash: deny` lines and tool restrictions landing correctly in the installed trees and zero placeholder or retired-grammar residue; 34 installed support scripts, 0 syntax failures, 0 load failures. It also ran the three deferred-whole suites the chains never execute (`test-sink-merge` 170, `test-claim-hardening` 464, `test-run-chains` 238) — all exit 0. Its C2 finding was real when measured and is now closed; I re-proved it myself on the current tree, where deleting the bash axis from `PERMISSION_AXES` reds naming the three roles that would otherwise ship with shell access.

  result (`fable-guards`, execution): 24 mutations red as intended, including the previously-442/442-green body-gutting one, and TWO wrong-reason reds correctly refused rather than banked. R1 (the contradiction holes) was real when measured and is now closed — I re-verified the heading mutation reds on the current tree. **R2 IS NEW, REAL, AND I CONFIRMED IT: the self-referential shape survives on the FORGE axis.** Deleting gitea from both edition tables in `generate-routing-surfaces.js` leaves `test-route-reachability` passing at *the same 325 assertions* — the coverage universe silently shrinks from 12 surfaces to 8 and the assertion count does not even move. The runtime term is independently anchored on an fs glob and is armed; the forge term is the registry measuring itself. It IS backstopped — `test-generate-routing-surfaces` reds with "registry derives 18 surfaces (3 topics x 6)", and although that suite is wired only into the claude chain, the claude chain is always selected — so the rating of low/non-blocking is correct and I am NOT re-architecting the anchor, per additive derivation. What I AM fixing is the dishonesty: both guards carry anti-vacuity comments claiming the anchor "cannot shrink in lockstep", which is true of the runtime term and FALSE of the forge term. A comment claiming a universal its body does not deliver is a documented failure mode here, and it is the exact standard I held another agent to earlier today.

  result so far (`fable-guards`, source phase): ONE REFUTATION, found by reading rather than by running, and it is a real hole. `generate-reviewer-profiles.js:363` builds `contractText` from `sections[].lines` ONLY, so section **headings** are scanned by NOTHING — not the contradiction check, not the required-token table, not the retired-vocabulary ban — while `renderBehaviorCore:458` renders them as `## <heading>` into all twelve shipped surfaces. A heading reading "Uncertainty never counts against the claim" ships silently, and retired vocabulary in a heading would ship too, which is exactly what #885 removed from everywhere else. This is categorically worse than the lexical limits the author documented honestly: those are "this phrasing escapes a pattern", this is "an entire region of consumer-facing prose is never looked at". Routed back for repair. Two smaller escapes found in the same pass: a countermand carrying a negation particle in the same clause is skipped for 2 of 3 roles because their REVOKED lists are empty (an accident of which role got patterns, not a design choice), and a comma-split countermand defeats the clause splitter and is documented nowhere — an undocumented hole in a guard whose author published a limits list is the worse failure, because the list is what a reader trusts. Everything else read ARMED from source, with one honest qualification worth keeping: the profile-parity consensus is self-referential in AGGREGATE and declared as such, so the six safety sentences are its only absolute anchor and mutation B6 must red or the rearm is hollow.

- item: The four blocking gaps the Fable acceptance pass found, closed. #883's headline (custody) and the init-template ban were the two that meant the issue was not done as claimed.
  status: done
  dispatched: `profile-guard-rearm` (custody), `field-parity-guards` (init-template ban), `reviewer-guard-hole` + self (`node-id`), `rel-impl` + `prose-pass` (the #881 wording).
  result: All four closed and mutation-proven. The init-template ban carries the half most people skip: **M2, the SILENT case** — replacing the mission-list record bullet with innocuous prose introduces ZERO banned tokens and sails through any negative-only guard, so the region must also POSITIVELY assert it teaches the current design, which is exactly what #883's acceptance asked for and what a ban alone cannot deliver. Scope proven in both directions (a banned token OUTSIDE the region stays green; the SKELETON source reds without regeneration) and every positive needle was verified present BEFORE the assertion was written, not after. Two rulings I made on its findings: the port validators are NOT extended — the gap exists in their code but is unreachable in behaviour, proven by closing both routes (skeleton edits red in the always-selected chain; hand-edited surfaces red on the drift check wired into all four), so three more copies would buy code symmetry and three more places to drift; and its propagation of the byte mirror of its OWN assigned file was signed off, with the general rule stated — mirroring your own file is in scope, editing a different one is not. THE #881 WORDING FIX RECREATED THE DEFECT BEFORE IT FIXED IT: two agents independently settled DIFFERENT step text for the same list, one file apart. Reconciled on register — the array is a list of imperative steps, so the step form won over the sentence form — and the equality is now proven from the RUNNING CLI's output rather than from source text, because the string an operator sees is the one that has to match.

- item: The live tool-restriction axis, found unguarded by the Fable coverage sweep — the #884 work made role restrictions REAL (opencode emits `bash: deny` for three roles, kimi emits the shell-commands line) while both suites still guarded only the write/edit axis that had been unreachable. A predicate regression would have shipped three roles WITH shell access on two runtimes, both suites green — the same emitter-fine/predicate-unguarded shape #883 diagnosed.
  status: done
  dispatched: `rel-oracle` (tdd-guide, test custody) owns both edition suites.
  result: opencode 481 -> 498 assertions, kimi 490 -> 507, both exit 0. Derived from the canonical `agents/*.md` `tools:` frontmatter rather than hand-listed, so a fourth restricted role is covered the day it is added and the generator's own parse cannot define the answer it is checked against. Asserted BIDIRECTIONALLY, and the mutation asymmetry is the proof: deny-nothing reds 4, deny-everything reds 12, where a one-sided assertion would have scored 4 and 0. The kimi side is the sharper case — a Skill drops canonical `tools:` and every role dispatches as `coder`, so the prose line is the ONLY carrier and its absence means those roles simply have shell access. `restrictedRoles()`, previously exported "for inspection" with zero consumers, now has one — and it is compared against "roles withholding ANY governed capability" rather than the Bash partition alone, because the narrower comparison would have been a wrong assertion that happened to pass today. Each retained empty-axis assertion was re-stated with the property it actually still carries, rather than left implying coverage it does not have.

- item: File ONE follow-up issue at close carrying everything deliberately scoped out, so a decision taken on purpose is not rediscovered later as a bug. Seven entries, each with its reason for exclusion rather than a bare TODO. (6) **The FORGE axis of two guards is self-referential** — deleting a forge from the edition tables shrinks the coverage universe from twelve surfaces to eight while `test-route-reachability` stays green at an UNCHANGED assertion count. I confirmed this myself. It is genuinely backstopped by `test-generate-routing-surfaces`'s "registry derives 18 surfaces" assertion in the always-selected claude chain, so the anchor is left registry-derived per additive derivation — but both guards' anti-vacuity comments claimed the width "cannot shrink in lockstep", which is true only of the runtime term, and those comments are now corrected to state the real boundary. (7) Orphan references to files the ADR 0017 demolition deleted, NOT this bundle: `rename-table.js:22` (`repair-state.js`), `measure-site-execution.drivers.txt:20,29` and `test-spawn-classification.js:102` (`test-plan-validator.js`, `test-replan.js`), and two dead A14 entries at `test-opencode-edition.js:306-308` whose universal now lives in `assertNoBadgeResidue`. Left because they are pre-existing #877 residue and fixing them would stale a bound chain receipt for cosmetic debris. (1) **The three generated reviewer profiles carry no prompt-defense block**, on canonical AND every runtime — measured, not inferred. It is UNIFORM, so it is not the cross-runtime drift #884 is about; it is a content gap in `templates/reviewers/behavior-contracts.json` and closing it is a decision about what a reviewer is told, not a repair. (2) The `init.skeleton.md:404/:430` region pair, annotated as a presentation-shape residual with its collapse cost stated. (3) `receipt_contract.domain_outcomes` keeps a stale internal name; it renders to no surface and is itself a closed-schema guard, so renaming it cascades through a profile hash for zero observable benefit. (4) The reviewer contradiction check is lexical and its in-source limits list is now honest about what escapes — semantic contradiction is not buildable here and an overclaiming guard is worse than a modest one. (5) The `inherit_session_model` declaration and the assertions policing it now share one file, so a single edit can remove both; the property is weaker than when they sat in separate files, and no new home was invented because no observed failure demands one.
  status: todo

- item: Recapture the prose census ONCE at bundle close — `node scripts/kaola-workflow-prose-census.js --write-baseline` — after every agent's removals have landed. NOT a hand-edit of the existing baseline, which was declined with better reasoning than the instruction that asked for it: the file is a captured measurement, its rows participate in a counted invariant (`distinct_conditions` 732 with three derived ratios), and `compare()` exists precisely to report which conditions DISAPPEARED, so deleting a row hides the finding the tool exists to emit. At least two conditions are known dead now (`lingering_lane_group`, `lane_group_live`) plus whatever else this bundle removed. Wired into no npm script, so nothing is red either way.
  status: todo

- item: Finalize — CHANGELOG under [Unreleased] before the receipt run or it goes chains_stale, chain receipt, roadmap, archive, sink, close all five issues.
  status: todo
