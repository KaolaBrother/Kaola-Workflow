# Close the whole open backlog — #963 (stale comments), #964 (yagni symbols), #966 (vacuous validate-remote)

- item: #963 — rewrite the three stale comment sites in `scripts/test-opencode-edition.js` (761-762, 869-871, 883-884) so they state what is true now; `rewriteClaudeModelNouns()` exists nowhere and no sync script emits `opus-tier`/`sonnet-tier`. Do not touch the assertions — they stay as canonical-drift canaries, exactly as #962 did for A22. Also inspect `scripts/test-kimi-edition.js` for mirrored prose.
  status: done
  dispatched: self — edits land in `$WT/scripts/test-opencode-edition.js` (worktree `.kw/worktrees/bundle-963-964-966`, branch `workflow/bundle-963-964-966`). Premise VERIFIED before editing: `rewriteClaudeModelNouns` survives in the tracked tree ONLY at `test-opencode-edition.js:761` and `:883`, both comment text; `CHANGELOG.md:2172` records #812 deleting it from both `sync-opencode-edition.js` and `sync-kimi-edition.js`. `opus-tier`/`sonnet-tier` appear in `scripts/` only in this file (758, 870, 872-873) — 872 is the live assertion and STAYS. `scripts/test-kimi-edition.js` has ZERO hits for all three needles, so the issue's "may mirror some of this" resolves to NO and the scope is one file.
  result: DONE — three comment sites rewritten in `scripts/test-opencode-edition.js`, comment text only, zero assertions moved. They now state that nothing rewrites on the way out and that the sweep passes because canonical `commands/` + `agents/` carry zero capitalized nouns (measured: empty result). Verified: `node scripts/test-opencode-edition.js` exit 0, **570 assertions**. First run was a partly vacuous green — it reported `drift-check: NO tree verified; 3 ABSENT` because a fresh worktree has no gitignored `.opencode`; re-ran after `sync-opencode-edition.js --write` and got `3 tree(s) in parity`, which is the run that actually read the surface.

- item: #964 — re-run the zero-consumer search the repo's way (stem not basename; per-file; `git grep -P` over tracked plus `find` over the gitignored rendered edition trees; positive control against a known-live module) for `runScenario` (`scripts/test-shard-lib.js`) and `makeShimSpawnFn` (`scripts/test-parallel.js`). If the zero holds, cut the symbols and whatever exercises them — never the files; `test-parallel.js --self-test` is a live step in both claude chains and covers `test-shard-lib`.
  status: done
  dispatched: self — cuts land in `$WT/scripts/test-shard-lib.js` and `$WT/scripts/test-parallel.js`. **The zero HOLDS, and the positive control is the part that makes it mean anything:** run the identical per-symbol method over all nine `test-shard-lib.js` exports and eight of them have 1–76 live consumer lines in `scripts/` (`owns` 76, `selector` 12, `MARKER` 10, `auditShardCoverage` 5, `parseCoverage` 3, `parseShard` 2, `coverageLine` 1, `reportCoverage` 1) — `runScenario` alone has 0. (My FIRST control was worthless and discarded: `shardOf` is not an export of that file, so its zero proved nothing.) `makeShimSpawnFn` is defined at `test-parallel.js:195` and never called; `nodeShimSpawn` (:238) is the shim actually wired at :249 and :301. Gitignored rendered trees swept by `find` in the MAIN tree where they exist (`.opencode` 3458 files, `.kimi` 19) — zero hits; the worktree has neither, so sweeping it would have been the false zero. Scope grows by one mechanism: `TIMING_ON` and its `KAOLA_TEST_SCENARIO_TIMING` env flag (`test-shard-lib.js:121-124`) exist ONLY for `runScenario` and reach no doc surface, so they die with it rather than being left as a knob wired to nothing.
  result: DONE — cut `runScenario` + its export line + `TIMING_ON` + the `KAOLA_TEST_SCENARIO_TIMING` comment from `scripts/test-shard-lib.js`, and `makeShimSpawnFn` from `scripts/test-parallel.js`. Both FILES kept. Verified: `node scripts/test-parallel.js --self-test` exit 0 at **23 assertions, 0 failed** — identical to the post-#960 baseline recorded in `archive/bundle-956-.../review-b-subtractions.md`, so the cut removed dead code and no assertion. `validate-script-sync.js` exit 0 (15 common scripts, 27 byte-identical groups).

- item: #966 — the acceptance is an explicit values fork the issue refuses to presume: does `ROADMAP.md`'s "active work" mean claimed runs or the open backlog? Measure what `validateRemote()` (`scripts/kaola-workflow-roadmap.js:294`) actually compares and what `CLAUDE.md` promises it covers, then put the fork to the owner with the evidence and implement the ruling. Closing with no code beyond a docs correction is a legitimate outcome.
  status: done
  dispatched: self — measurement done and it CONFIRMS the issue: `validateRemote()` iterates `readRoadmapIssues(dir)` and its only test is `status: open` locally AND `issueIsClosed(n)` remotely, so an empty dir means an empty loop and `cmdValidateRemote` prints `ok`. Consumers of the promise: `CLAUDE.md:43` (names it the hand-check for a left-behind file) and `README.md:1010`. Port cost for any behaviour change is 3 forges (github/gitlab/gitea roadmap scripts) plus both contract validators. Fork now with the owner in conversation; ruling lands back in this item's result. **OWNER RULED (in conversation, this run):** (1) `ROADMAP.md`'s "active work" means CLAIMED RUNS ONLY — `No active work` was TRUE at 85209757 and the mirror needs no change; the mirror-is-wrong reading is DECLINED. (2) Repair is *report what it compared* — `ok` must name its domain so an empty source set is visibly vacuous — ported to all three forge scripts, plus the docs correction. The reverse-direction option is DECLINED. Implementation lands in FOUR script copies under `$WT` — `scripts/kaola-workflow-roadmap.js` + `plugins/kaola-workflow/scripts/kaola-workflow-roadmap.js` (byte-identical, enforced at `validate-script-sync.js:50`), `plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-roadmap.js` (`issueIid`) and `plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-roadmap.js` (`issueNumber`) — plus `CLAUDE.md:43` and `docs/api.md:1416`. **Return shape is FROZEN:** both forge suites pin `validateRemote(root)` with `assert.deepStrictEqual(..., [996]/[9960])` and `docs/api.md` documents it, so the count rides an optional out-param and the array return is untouched. Nothing pins the `ok` stdout (only `skipped: offline` is pinned, at `simulate-workflow-walkthrough.js:6952-6955` and both forge suites), so the success line is free to change. The count must be entries that actually REACHED the remote check, not open files — reporting unparseable entries as checked would be a fresh instance of the very lie this issue is about.
  result: DONE — `validateRemote(root, stats)` gained an optional out-param carrying `checked` (entries that actually reached the remote call), array return untouched; `cmdValidateRemote` now prints a domain-naming success line. Ported to all four copies + `CLAUDE.md` + `docs/api.md` (subcommand row and all three export signatures). **The issue's own reproduction, run live against the real forge in the worktree:** zero sources → `ok: nothing compared — no open local sources. This checks .roadmap/issue-*.md outward and cannot see an issue open on the remote with no local source` (exit 0); one open source (#963, open remotely) → `ok: 1 open local source compared against the remote, none closed there` (exit 0); one stale source (#965, CLOSED remotely) → `roadmap drift: issue-965.md marked open but closed on remote` (exit 1, drift path unregressed). Probe files removed; `git status` clean of them. Both forge `deepStrictEqual` pins on the return still pass.

- item: Dock the run — `CHANGELOG.md` under `[Unreleased]`, plus any doc surface the three changes reach; regenerate routing surfaces only if a skeleton was touched.
  status: done
  dispatched: self — `CHANGELOG.md` `[Unreleased]` → `### Fixed`, plus `CLAUDE.md` and `docs/api.md` for #966. No `templates/routing/` skeleton touched, so no surface regeneration is owed.
  result: DONE — three `### Fixed` entries (#963, #964, #966) written above the `## [9.7.0]` heading. `CLAUDE.md` line budget was the one real catch of this run: the guard is `read('CLAUDE.md').split(/\r?\n/).length < 200`, which counts the trailing empty element, so the true ceiling is **198 by `wc -l`**, not 200 — main sits at 197, giving ONE line of headroom, not two. My first edit took it to 199 and threw `CLAUDE.md must stay below the 200-line target` on require, which reds the whole contracts validator; condensed to 198 and it passes.

- item: Verify — `node scripts/simulate-workflow-walkthrough.js` at full scope, the opencode and kimi edition suites for the #963 diff, `test-parallel.js --self-test` for the #964 cut, and the chain selection finalize computes.
  status: done
  dispatched: self — run sequentially in the worktree, never parallel-sharded, each exit code echoed on its own.
  result: DONE, all exit 0 — walkthrough at FULL scope (not the 1/12 gate shard): **209/209 scenarios, 0 failed**. `validate-workflow-contracts.js` PASS (after the CLAUDE.md fix above). `test-opencode-edition.js` 570 assertions with 3 trees in parity. `test-parallel.js --self-test` 23/0. `validate-script-sync.js` PASS. `test-gitlab-workflow-scripts.js`, `test-gitea-workflow-scripts.js`, `test-forge-roadmap-rules.js` (90 passed), and both forge contract validators PASS. Diff touches `plugins/kaola-workflow-{gitlab,gitea}/`, so finalize's diff-scoping will fail closed to all four chains — expected, not a fault.
