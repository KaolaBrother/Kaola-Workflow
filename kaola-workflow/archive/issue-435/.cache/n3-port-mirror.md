evidence-binding: n3-port-mirror 3ee36b2daec1
implementer created 3 edition ports of gap-sweep.js.
non_tdd_reason: mechanical edition byte-mirror + rename-normalized forge ports; parity enforced by validate-script-sync (sibling n4) + forge validators, no unit test.
build-green: cmp canonical vs codex IDENTICAL; gitlab+gitea --forbidden-only on the ports exit 0. renameNormalize applied 3 self-name tokens/port (header/usage/help); kaola-workflow/ state-dir paths preserved (slash-terminated, not matched). validate-script-sync green deferred to n9/finalize (needs n4 family entry).
Scope: only the 3 declared port files.
