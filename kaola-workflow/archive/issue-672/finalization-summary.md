# Finalization - Summary: issue-672

## Delivered
Two pre-existing porcelain-probe fail-opens (deferred from #669) made fail-CLOSED. (1) `synthesizeLevel`'s
leg-dirty probe (`adaptive-node.js`) now carries the 64 MB `GIT_MAX_BUFFER` cap and, on any probe
failure, loudly refuses (`leg_dirty_probe_failed`) instead of collapsing to `dirty=''` and silently
omitting real leg content from the octopus merge. (2) `worktreeDirtyState` (`claim.js`) now returns a
distinct `'unprobeable'` state (probe threw) vs `'missing'` (path absent); both destructive sweep
consumers (`cmdStaleWorktreeCleanup`, `cmdLegacyWorktreeCleanup`) unconditionally KEEP `'unprobeable'`
via a `skipped_unprobeable` bucket before every removal branch, with no `--force`/`--archive`/`--export`
override.

## Files Changed
- scripts/kaola-workflow-adaptive-node.js (+ codex twin + gitlab/gitea @generated ports) — synthesizeLevel leg-dirty probe cap + fail-closed refuse
- scripts/kaola-workflow-claim.js (+ codex twin + gitlab/gitea forge hand-ports) — worktreeDirtyState 'unprobeable' + both sweep consumers keep it
- scripts/test-adaptive-node.js — S5-PROBE-FAILED-REFUSE regression
- scripts/test-claim-hardening.js — #672 unprobeable-keep regression (legacy sweep)
- CHANGELOG.md — `[Unreleased]` entry for #672
- kaola-workflow/.roadmap/issue-677.md — new follow-up source (run gaps A1/A2)

## Test Coverage
Not a coverage-gated repo. `node scripts/test-adaptive-node.js` → 1789 assertions;
`node scripts/test-claim-hardening.js` → 173 assertions; `node scripts/simulate-workflow-walkthrough.js`
→ "Workflow walkthrough simulation passed"; `node scripts/edition-sync.js --check` → clean.

## Final Validation Evidence
Self-host four-chain receipt (`.cache/chain-receipt.json`) stamped by `run-chains.js --project issue-672`
after the feature commit. Per-node evidence: `.cache/n1-fix.md` (RED→GREEN both sites), `.cache/n2-review.md`
(verdict pass, 0 blocking), `.cache/n3-adversary.md` (NOT-REFUTED, verdict pass; live-verified both sweep
consumers across every flag + a >64 MB ENOBUFS shape).

## Documentation Docking
DOCKED — sole doc surface is the CHANGELOG `[Unreleased]` entry; no public interface/API/env/architecture
changed. Evidence: this summary + `.cache/n4-finalize.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- #677 — worktree-cleanup residuals in a DIFFERENT family than #672's porcelain probe: A2 the `existsSync` stat-failure path still fails open (chmod-000 parent → existing worktree routed to `'missing'` → registry+branch prune, content-safe); A1 the `cmdStaleWorktreeCleanup` `'unprobeable'` keep is behavior-verified but lacks a shipped regression test. Both adversary-live-confirmed.

## Run gaps
- manual:sweep-consumer-residuals (worktree-cleanup existsSync stat-failure fail-open plus untested stale-sweep unprobeable-keep both outside the porcelain-probe family): filed: #677

## Closure Decision
Two out-of-scope residuals (A1, A2) filed as #677 (see Run gaps) — both in a different failure family
than #672's porcelain probe. No unresolved conflict, partial implementation, or value/standing decision
for #672 itself — both porcelain fail-opens are fixed and verified (review + live adversary). Close #672.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#672 — to be closed by the merge sink after the four-chain merge gate.

## Roadmap
Regenerated by cmdFinalize: removes .roadmap/issue-672.md, adds .roadmap/issue-677.md, retains #673.
(#674/#675/#676 are GitHub-only, unmirrored.)

## Archive
pending (cmdFinalize Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| n1-fix (tdd-guide) | invoked | .cache/n1-fix.md | |
| n2-review (code-reviewer/fable) | invoked | .cache/n2-review.md | |
| n3-adversary (adversarial-verifier/fable) | invoked | .cache/n3-adversary.md | |
| doc-updater | skipped | CHANGELOG-only, no checklist doc impact | sole doc surface is CHANGELOG, updated inline in the finalize node |
| documentation docking | invoked | this summary + .cache/n4-finalize.md | |
| final-validation fix executors | N/A | — | no final-validation failure |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | cmdFinalize Step 8b |
| archive completed folder | pending | | cmdFinalize Step 8b |
| final commit and push | ready | git status/upstream check | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE

## Attestation
claim_planner_attested: attested
finalize_contractor_attested: attested
