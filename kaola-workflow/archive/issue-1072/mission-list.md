# Subtract global-contract duplicates and decision narrative from AGENTS.md (#1072, owner-authorized)

- item: Rewrite `AGENTS.md` in the worktree so it carries local repository facts only: drop the ADR 0017 "design of record" narrative and the restated Mission List / backlog-truth rules (`:8-15`), compress the instruction-file meta-rule (`:29-34`) to one sentence, remove the two second-round Change-discipline bullets (`:59-60` read-before-edit/scoped-to-measured-failures, `:68` never-claim-unexecuted), keep `:61-67`, Commands, Documentation, provenance note, and all seven pinned tokens; trim the CLAUDE.md narration line while keeping exactly one `@AGENTS.md` and the word "Claude". Acceptance: <= 60 lines; none of "design of record", "exactly four fields", "three write", "backlog truth", "Do not impose a template", "immediately before editing", "scoped to measured failures", "that was not executed".
  status: done
  dispatched: self — edits land in `.kw/worktrees/issue-1072/AGENTS.md` and `.kw/worktrees/issue-1072/CLAUDE.md` (uncommitted on `workflow/issue-1072`)
  result: AGENTS.md 75→59 lines / 558→401 words; CLAUDE.md 9→8 lines. Removed `:8-15` rules (kept a one-line `docs/decisions/0017-the-mission-list.md` pointer), `:29-34` meta-rule (compressed to one Change-discipline bullet), `:59-60` and `:68` bullets, and the `## Product and design authority` heading that no longer labelled anything; CLAUDE.md narration line dropped. Grep-verified: 0/8 forbidden phrases, 7/7 pinned tokens, exactly one `@AGENTS.md`. Uncommitted in the worktree; commit is the next mission's output.
- item: Validate and dock: `node scripts/test-runtime-agent-architecture.js` (A1/A2), `node scripts/validate-workflow-contracts.js`, `node scripts/test-route-reachability.js` (PROJECT-INSTRUCTIONS duplicate gate), then `npm test`; add a `CHANGELOG.md` `[Unreleased]` entry; confirm README/docs/api.md need no change (they do not describe AGENTS.md prose) or update them; commit on `workflow/issue-1072` and record the candidate commit. Stop at readiness — no install, sink, closure, or release.
  status: done
  dispatched: sidekick — runs the four validation commands inside `.kw/worktrees/issue-1072`, adds the CHANGELOG `[Unreleased]` entry I authored, greps README/docs for references to the removed AGENTS.md prose, and commits `AGENTS.md`, `CLAUDE.md`, `CHANGELOG.md` on `workflow/issue-1072`; output lands as one commit on that branch plus test logs under `/tmp/kw-1072/`
  result: Candidate commit `2c5e66f5` on `workflow/issue-1072` (3 files: AGENTS.md −19/+4 net 23→, CHANGELOG.md +7, CLAUDE.md −1; worktree clean after commit). First validation pass caught an over-subtraction: `validate-workflow-contracts.js:355-361` pins `kaola-workflow/.roadmap/_rules.md` + "only optional local roadmap file" in AGENTS.md (the issue's pinned-token list came only from `test-runtime-agent-architecture.js`). Restored that as a one-line path fact — the rule "do not create a local issue mirror" and the phrase "backlog truth" stay removed; AGENTS.md finalizes at 60 lines (≤ 60 acceptance; the mission-1 result's 59 predates this fix). Gates at pre-commit tree (identical bytes except one CHANGELOG digit): `test-runtime-agent-architecture.js` 0, `validate-workflow-contracts.js` 0, `test-route-reachability.js` 0, `npm test` 0 (`/tmp/kw-1072/*-2.log`). Re-bound at the exact commit: `test-runtime-agent-architecture.js` 0, `validate-workflow-contracts.js` 0 (`/tmp/kw-1072/*-2c5e66f5.log`). Docs residue grep: no `Product and design authority` or `AGENTS.md:` references in README/docs; `design of record` hits (`docs/README.md:57,110`, `docs/task-quality.md:78`) describe ADR 0017 itself, not quotes of removed AGENTS.md prose — no README/docs/api.md change needed. Commit executed by orchestrator (background dispatch cannot commit). Stopped at readiness per owner instruction: no install, sink, closure, or release.
