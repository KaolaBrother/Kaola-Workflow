# Finalization - Summary: issue-544

## Delivered
Generalized the opencode edition's two-tier effort mapping to be **contract-keyed** (top/second ↔ opus/sonnet resolve by the model's API contract, not its brand name), gave unknown providers a safe top/second default (no silent de-tier), and made model switches a documented re-sync rather than a silent collapse. Closes #544.

## Files Changed
- `scripts/kaola-workflow-adaptive-schema.js` (+ `plugins/kaola-workflow{,-gitlab,-gitea}/scripts/kaola-workflow-adaptive-schema.js` — ×4 byte-identical) — PROVIDER_EFFORT_TABLE → CONTRACT_EFFORT_TABLE; +contractForProvider; effortForProvider rewrite (falsy→null, unknown→default); exports.
- `scripts/sync-opencode-edition.js` — renderAdaptiveConfig comment (contract label + re-sync ⚠); ref flip.
- `scripts/test-opencode-edition.js` — S1 flip + S1-contract block + A12-unknown flip (300 assertions).
- `docs/opencode-edition.md` — contract-keyed table + callout, GLM thinking example, Switching-models subsection, ref updates.
- `docs/decisions/D-544-01.md` — NEW decision record.
- `install-opencode.sh` — seed_config comment + echo.
- `CHANGELOG.md` — #544 entry.
- `README.md` — line 361 pointer fix (R1).
- `opencode.json` — UNTOUCHED (byte-identical neutral template).

## Test Coverage
N/A percent (Node scripts, no formal coverage tool). Behavior verified by: test-opencode-edition.js (300), test-adaptive-node.js (1030), validate-script-sync.js (×4 byte-identity), sync --check (parity), and the four #307 cross-edition chains (all green).

## Final Validation Evidence
Chain receipt `.cache/chain-receipt.json` (headSha afad79ec == HEAD; claude/codex/gitlab/gitea all exit 0). Scope-relevant suites re-verified GREEN in node n4 + .cache/final-validation.md.

## Documentation Docking
DOCKED — `.cache/doc-docking.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none — final candidate clean; an earlier transient rate-limit burst re-ran clean) | environmental | n/a | chain-receipt.json | resolved |

## Follow-Up Items
None open. R1 (README pointer) fixed inline. R2 (regex bare 'z.ai' token) advisory/noise — see Run gaps.

## Run gaps
- manual:r2-regex-bare-zai-dot (code-reviewer advisory R2): noise: contractForProvider regex /zhipu|^zai|z-?ai|glm/ does not match the bare dotted 'z.ai' token (→default); NOT a real defect — the real opencode provider id 'zhipuai-coding-plan' resolves correctly to anthropic, and aliases zai/z-ai/zhipu*/glm* are all covered. Theoretical edge case only; out of scope for #544.

## Closure Decision
None needed — no deferred items, partial work, or user-decision items. R1 resolved; R2 noise.

## Commit And Push
pending final Git gate; final hash reported after push.

## GitHub Issue
#544 — to be closed (sink: merge).

## Roadmap
to be regenerated by cmdFinalize (no `.roadmap/issue-544.md` source existed locally; closure is no-op on the roadmap source).

## Archive
pending — performed atomically by cmdFinalize (Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked (main-session-trivial-inline for README) | .cache/doc-updater.md | docs otherwise authored by n3 |
| documentation docking | invoked | .cache/doc-docking.md | |
| final-validation fix executors | N/A | .cache/final-validation.md | no final-validation failures on the final candidate |
| roadmap refresh | invoked | kaola-workflow/ROADMAP.md | no local issue-544 source to remove |
| archive completed folder | pending | | cmdFinalize Step 8b |
| final commit and push | ready | git status/git diff/upstream check | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE
