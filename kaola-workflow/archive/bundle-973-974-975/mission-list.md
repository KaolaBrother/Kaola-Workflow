# Close #973, #974, #975 — the whole open backlog; reviewers run on the Fable model this run

- item: File the missing local roadmap sources for #973, #974 and #975 — all three are open on the forge with no kaola-workflow/.roadmap/issue-*.md, so the mirror reads "No active work" and validate-remote compares nothing at all; this is drift the one-directional check structurally cannot see.
  status: done
  dispatched: self, in the worktree; three source files land at .kw/worktrees/bundle-973-974-975/kaola-workflow/.roadmap/issue-97{3,4,5}.md and the outcome is recorded inline on this item
  result: issue-973.md, issue-974.md and issue-975.md written in the worktree's .roadmap/ carrying each issue's full body as next_step, workflow_project bundle-973-974-975, and the mirror regenerated with `roadmap.js generate` (explicit subcommand — the bare no-arg invocation mutates). Active Work now lists all three where it read "No active work". The measurable gain: validate-remote went from "ok: nothing compared — no open local sources" to "ok: 3 open local sources compared against the remote, none closed there", so the only forge-facing drift check in the repo was inert against the entire backlog and is now armed. Worktree diff: M kaola-workflow/ROADMAP.md + 3 new sources.

- item: Premise-check #973 — verify that copy_skills in install-kimi.sh really removes deployed skill directories before it copies, reproduce the 17-to-0 loss on a hermetic target, and establish what the blanket remove-then-recopy is actually load-bearing for (which retired directories no manifest lists) so a fix does not silently drop the self-heal; also confirm the issue's claim that #969's heal-or-abort now precedes the copy and check whether the same ordering exists in install-opencode.sh or install.sh.
  status: done
  dispatched: investigator subagent, read-only, running in the MAIN checkout, may install into a throwaway HOME but must not touch the live ~/.kimi or ~/.claude; report to land at kaola-workflow/bundle-973-974-975/premise-973.md
  result: kaola-workflow/bundle-973-974-975/premise-973.md — ordering claim SURVIVES verbatim (prune :181-186, copy :187-199, zero-guard :200-206, i.e. after BOTH); 17→0 REPRODUCED as leg D. Live $HOME proven untouched by before/after diff. FOUR findings that resize the work. (1) THE SILENT PARTIAL LOSS IS THE BIGGER DEFECT AND IS NOT THE ONE FILED — leg B (canonical agents/ emptied) is 17→3 at EXIT 0 with "Installed workflow skills" printed and no warning; leg E is 17→14, exit 0. The skill_count -eq 0 guard cannot see either, because 3 > 0. A fix scoped to the filed result — "an install that cannot deploy" — leaves both exactly where they are; the report's proposed truer result is "an install does not remove a deployed skill it is not going to replace." (2) THE ISSUE'S MECHANISM CLAIM IS REFUTED: #969 added no heal-or-abort before the copy; the check-or-write refresh is byte-identical at 9b6fac01^ and dates to f6dbf40d, and what #969 actually added is the POST-copy guard plus the --print-tree-root repoint. The conclusion (tree-root route closed) still holds. (3) THE PRUNE IS LOAD-BEARING FOR CORRECTNESS, not just hygiene — measured: `cp -R src dest` onto an existing dir NESTS (dest/X/X/SKILL.md) and leaves the old SKILL.md live, so removing or deferring the prune past the copy of that name makes reinstalls stop updating skills entirely. That is a hard constraint on every candidate. (4) BOTH SIBLINGS CARRY IT AND BOTH ARE WORSE THAN KIMI: install-opencode.sh has NO zero-guard at all (3 commands → 0, exit 0, success message), and install.sh's gitlab/gitea branch (:619-626) empties ~/.claude/commands and prints "skeleton installed" at exit 0 — measured on a hermetic HOME. Candidate table with measured obstacles at §6; the retired-sweep cost of narrowing the prune is exactly the four RETIRED_ROLE_SKILLS names, no more (§1 positive control + history at 63443589). Test surface: scripts/test-kimi-edition.js (589 assertions green, NOT in npm test), install probes are the P-series :907-1180 spawning the real installer, P1b :1031-1076 is the fixture shape to mirror, and the zero-deploy guard is currently asserted NOWHERE repo-wide.

- item: Premise-check #974 — reproduce the vacuous green: stand a tree carrying a pre-#971 stray kaola-workflow/<project>/ folder, run the gap sweep from it, and confirm the resolver stops at the stray folder, sweeps an empty .cache, maps zero classes and exits 0 while a real gap sits in the main checkout; locate the resolution site and the vacuous-pass branch (the #971 premise report put the latter near gap-sweep.js:370) and say which of the issue's four candidate shapes the code can actually carry.
  status: done
  dispatched: investigator subagent, read-only, MAIN checkout, reproduces in a throwaway clone or fixture tree and never pollutes this repo; report to land at kaola-workflow/bundle-973-974-975/premise-974.md
  result: kaola-workflow/bundle-973-974-975/premise-974.md — core claim REPRODUCES exactly at 69264936, every link confirmed, stray produced by the shipped scanner under pre-#971 conditions rather than hand-crafted; controls isolate the stray as the only differing variable. FIVE facts the issue lacked, three of which change the work. (1) The false green does NOT need a failed sweep: scan correctly FROM MAIN, then --check from the polluted worktree, still pass/mapped:0/exit 0 — and that IS the real flow, because commands/kaola-workflow-finalize.md:210 runs only --check at Step 7 and never re-scans. (2) A bare EMPTY DIRECTORY suffices — :476 is a naked existsSync — so the issue's "bounded and shrinking population" is REFUTED: the residue shrinks, the reachable population does not. (3) THE SAME DEFECT SITS IN A SECOND, UNFIXED RESOLVER — validation-runner.js:1286 resolveRecordFolder, which run-chains.js:823-829 uses to place the CHAIN RECEIPT; #971 fixed one of two co-derived resolvers and gap-sweep re-derives a rule validation-runner already owns. (4) The detection primitive for candidate (a) already ships — otherProjectRoots (validation-runner.js:1311) walks `git worktree list --porcelain` and its consumer at :1535-1552 already emits the exact report-not-refuse shape #974 asks for; it is simply not exported. (5) The vacuous-pass branch cannot tell the cases apart from the artifact — run-gaps.json is {project, sweptClasses} only (:209) and is a REGISTERED durable record (adaptive-schema.js:760, docs/workflow-state-contract.md:50), so a fix probes fs/git at check time or widens a contract-documented schema. Candidate (d) survives but is constrained three ways (needs a cwd tie-break, would red T25e's 3 assertions without a fallback, and introduces a NEW wrong answer for a legitimate worktree-resident chain receipt); (b) cleanup is a user call, not an agent call, since git cannot recover an untracked stray; (c) marker is blind to the very pre-fix population it targets. Blast radius: 151 assertions in test-gap-sweep.js (the brief's 127 is right but attached to the wrong noun — 127 ride on KAOLA_GAP_ROOT keeping tier-1 precedence), 24 in the T25 block, ~9 on the vacuous pass; 4 edition copies, gitlab/gitea GENERATED so edit canonical and regenerate. Repo cleanliness claim SURVIVES; nothing changed.

- item: Premise-check #975 — MEASURE which invocation writes plugins/plugins into the working checkout rather than assuming test-install-all.js, per the issue's own warning; run the candidate suites against a pristine checkout and watch git status for untracked escapes, and separately establish whether the recursive comparison walk added to install-all.sh has any cycle guard at all.
  status: done
  dispatched: investigator subagent, read-only on tracked files, MAIN checkout, runs candidate suites in a throwaway clone so any escape it provokes lands there and not here; report to land at kaola-workflow/bundle-973-974-975/premise-975.md
  result: kaola-workflow/bundle-973-974-975/premise-975.md — the OBSERVATION is accurate; the two claims the issue leans on hardest are BOTH REFUTED, and the truth is worse in one direction and empty in the other. (1) CULPRIT: no suite wrote it and NO CODE IN THIS REPO CAN — ten suites run one at a time leave zero untracked escapes, fs.symlinkSync throws EEXIST rather than creating DIR/basename (one measurement excluding ~35 call sites), and there is not one `ln -` in scripts/, install*.sh or templates/. The writer was an ad-hoc `ln -s` in an agent's own verification shell; the dispatch log puts tests-972 and impl-972 live at 17:59Z = 01:59 local and both reports independently describe symlink work. So a sandbox assertion in the suites would NOT have caught this. (2) CYCLE GUARD: REFUTED twice over — Dirent uses lstat semantics so a symlink answers false to BOTH isDirectory() and isFile(), the if/else-if takes neither branch, and symlinks are skipped by construction; three loop shapes including `loop -> .` all returned `same` immediately inside a 30s bound. And plugins/plugins was never in the walked tree anyway — the live row's source.path is plugins/kaola-workflow, making the link a sibling. This is the watch-list's "recorded, not built" case. (3) "NOTHING DETECTED IT" REFUTED, AND THE TRUTH IS WORSE: four sites already see it by name, and the live behaviour is not silence but SILENT ADOPTION — `git add -A -- plugins/plugins` exits 0 and stages mode 120000, and the finalize residue probe (claim.js:5211-5225 → :5272) feeds exactly that call, so had the link still been present finalize would have COMMITTED the loop into the repository. It was removed by hand first; that was luck, not a guard. (4) A SEPARATE LIVE ESCAPE the issue did not have: TMPDIR=. puts 81 fixture roots into the checkout root during test-install-all.js, each holding plugins/kaola-workflow/, ALL self-deleted at cleanup so no before/after git status can ever see them — and install-all.sh:233 (production) participates. run-chains already immunises chain runs via realpathSync(mkdtempSync(...)) at :326-333, so the exposure is a bare `node scripts/test-*.js`. (5) The receipt evidence the issue cites does NOT survive — the archived receipt's 24 touchedEditionPaths do not include plugins/plugins, it was re-run after removal — though the MECHANISM reproduces exactly against the real exported functions (isEditionCouplingPath true, classifyScope all-four/edition_coupling). Split verdict: the issue's predicted pair is wrong; the two surviving halves are the relative-root escape and foreign-vs-own dirt classification at finalize, genuinely independent, and the cycle guard is dead. Repo verified clean, HEAD unchanged, plugins/ holds 3 dirs and no symlink.

- item: Put the two scope calls to the user in conversation — #973's filed result is provably too narrow (the silent partial loss walks past the guard) and both siblings carry the shape, and #974's defect sits in a second resolver that places the chain receipt; both are value calls about what ships, not facts I can settle.
  status: done
  dispatched: self, in conversation; two questions put together, carrying the measured legs and the second-resolver finding
  result: BOTH RULED BY THE USER. (1) #973 — "All three installers now": fix install-kimi.sh, install-opencode.sh AND install.sh in this bundle, against the truer result "an install does not remove a deployed skill it is not going to replace", covering the silent partial-loss legs B and E, not only the filed total-zero leg D. (2) #974 — "Fix both resolvers": gap-sweep.js resolveRunRoot AND validation-runner.js resolveRecordFolder, the latter being what run-chains uses to place the chain receipt.

- item: Author the failing tests for #973 across all three installers — the acceptance surface is "an install does not remove a deployed skill it is not going to replace", so the pins must cover the SILENT partial-loss legs (canonical agents/ emptied → 17→3 exit 0; commands renamed out of the allowlist → 17→14 exit 0) and not just total-zero, must pin that the four RETIRED_ROLE_SKILLS are still swept, and must pin that a reinstall still UPDATES rather than nesting (cp -R onto an existing dir nests and leaves the old SKILL.md live).
  status: done
  dispatched: tdd-guide subagent, working in the WORKTREE .kw/worktrees/bundle-973-974-975; tests land in scripts/test-kimi-edition.js, scripts/test-opencode-edition.js and the install.sh suite it selects, and a note on what it pinned and its measured red baseline lands at kaola-workflow/bundle-973-974-975/tests-973.md
  result: kaola-workflow/bundle-973-974-975/tests-973.md — RED on all three installers at 69264936, no production file touched. P5a kimi leg B (14 of 14 role skills destroyed, exit 0) at test-kimi-edition.js:1189 and P5b kimi leg E (3 command skills destroyed, exit 0) at :1233 → 2 failures / 607 passed; P7a opencode leg E at test-opencode-edition.js:1405 → 1 failure / 642 passed; install.sh pinned across ALL THREE FORGES in one probe at test-install-upgrade-rewrite.js:451 (github 3 removed exit 1, gitlab 3 removed exit 0, gitea 3 removed exit 0). Leg D is deliberately NOT a separate probe — it is B and E simultaneously, so a repair satisfying P5a+P5b satisfies it while a D-only repair (the skill_count==0 class the issue actually filed) reds on both, which is exactly the trap the bundle needed to avoid. No red probe asserts an exit code or a message, so a refuse-before-pruning repair is equally admissible. Both preservation constraints pinned AND armed: the retired-sweep probe plants the 4 retired names plus workflow-goal, kaola-something-else, my-own-skill and the non-directory kaola-role-notadir.md with anti-vacuity in both directions, and the no-nesting probe asserts source-byte equality, absence of an X/X directory, and that an orphan inside a replaced dir is cleared. MUTATION PROOF: two structurally different repairs (narrow-prune-plus-retired-list, and namespace-prune-with-a-refusal-before-it) both go fully green, and five counter-mutations each red exactly their intended pin — including narrow-prune-without-the-retired-list, which reds naming all four retired dirs. Live install verified intact after the run (~/.kimi-code/skills 17, ~/.claude/commands 3, ~/.claude/agents 14, ~/.config/opencode/command 3).

- item: Implement #973 across install-kimi.sh, install-opencode.sh and install.sh against those tests, keeping the retired-directory self-heal alive and the per-name prune ahead of the per-name copy; the mechanism is the implementer's call. The sharp edge tests-973 routed up: read literally the acceptance surface would forbid removing a RETIRED skill too, since a retired skill is also one the install will not replace — the tests pin both halves, so the fix must DISTINGUISH "retired on purpose" from "failed to deploy", and a bare prune-narrowing does not.
  status: in-flight
  dispatched: FIRST DISPATCH DIED — implementer subagent in the WORKTREE, killed by an ECONNRESET (the second agent lost to one this run) before writing its report. The IMPLEMENTATION landed: install-kimi.sh +61/-, install-opencode.sh +35/-, install.sh +31/- (93 insertions, 34 deletions across the three), and I verified by hand rather than trusting the worker that all three suites now pass — test-kimi-edition 609 assertions exit 0, test-opencode-edition 643 exit 0, test-install-upgrade-rewrite exit 0, each run serially with the real exit code captured, not through a pipe. Live install checked immediately because it died mid-install-spawn: ~/.kimi-code/skills 17, ~/.claude/commands 3, ~/.claude/agents 14, ~/.config/opencode/command 3 — intact, no damage. What is MISSING is the project's actual standard of evidence: no report, no mutation proof per installer, no statement of how the fix distinguishes retired-on-purpose from failed-to-deploy, and no CHANGELOG. Green suites are not proof a guard is armed. One observation for whoever reviews: the edition suites print "[tree root: /Users/ylpromax5/Workspace/Kaola-Workflow, not this checkout]", so the #969 --print-tree-root resolution points SOURCE_TREE at MAIN's edition trees even when the worktree's installer is the one under test. RE-DISPATCHED: a fresh implementer to audit the landed diff as a stranger's work, mutation-prove it, and report.
  status: done
  result: kaola-workflow/bundle-973-974-975/impl-973.md — audit verdict SOUND, repaired in one place, and the repair was found BY READING, not by any suite: install-opencode.sh:48-63 carried a header comment describing the mechanism the fix had just removed, every clause of it false (copy_tree no longer prunes kaola-owned files, the prune is not namespace-complete, a retired command no longer self-heals by namespace). The kimi twin HAD been updated, so it was a single-file omission — this is the same defect class the last bundle shipped (a comment whose measured claim was the opposite of true), caught this time. THE SHAPE: each installer now declares its retired set explicitly (RETIRED_ROLE_SKILLS / RETIRED_WORKFLOW_COMMANDS / RETIRED_COMMANDS) and removes exactly two things — the names it retired on purpose, and each name it is about to write, immediately before writing it. It distinguishes retired-on-purpose from failed-to-deploy BY DECLARATION AND ONLY BY DECLARATION, on the reasoning that "retired" is not a property the installer can infer from a name — both look identical on disk — so it must be a fact someone recorded. SEVEN MUTATIONS across the three installers, each surgical: restoring the namespace glob verbatim reproduces the premise agent's hand-measured legs from a different session and harness (kimi 17→3 exit 0, kimi 17→14 exit 0 with 3 warnings, opencode 3→0 exit 0, install.sh github exit 1 / gitlab exit 0 / gitea exit 0), and emptying a retired list reds the sweep pin alone — no single mutation satisfies both directions, which independently re-measures the test author's claim. It also closed BOTH of premise-973's open items by measuring --global and the gitlab/gitea forge axis, exercising the newly-added retired entries for the first time. Suites all green with real exit codes including the walkthrough at FULL SCOPE 210/210, and it states plainly that green was never the evidence: the inherited tree was already green before its repair, which moved no number. Tree-root observation CHECKED and does not weaken any probe — every probe spawns the WORKTREE's installer via REPO, and the mutated-source probes throw unless --print-tree-root realpath-equals their throwaway copy. THE COST STATED HONESTLY: retiring a surface now requires a list entry and NOTHING DETECTS A FORGOTTEN ONE; the failure mode is benign (a dead dir lingers, nothing is destroyed), so it is recorded as a maintenance obligation rather than built against. ROUTED TO ME: docs/kimi-edition.md:150-151 and docs/opencode-edition.md:147-148 both still assert the removed self-healing behaviour and are now factually wrong; correct wording supplied. Four things flagged-not-built with reasons, incl. a PRE-EXISTING hole where install.sh strands claude-workflow*.md that the old glob also missed.

- item: Author the failing tests for #974 across both resolvers — gap-sweep.js resolveRunRoot and validation-runner.js resolveRecordFolder — pinning that a leftover or empty run folder stops silently satisfying the resolver and that the pass stops being vacuous, WITHOUT adding a refusal, and preserving KAOLA_GAP_ROOT tier-1 precedence that 127 existing assertions ride on.
  status: done
  dispatched: tdd-guide subagent, working in the WORKTREE .kw/worktrees/bundle-973-974-975; tests land in scripts/test-gap-sweep.js and scripts/test-validation-runner.js, and a note on what it pinned and its measured red baseline lands at kaola-workflow/bundle-973-974-975/tests-974.md
  result: kaola-workflow/bundle-973-974-975/tests-974.md — RED established, production untouched. test-gap-sweep.js T26 block :1249-1532 (22 assertions, 8 red / 14 green controls) across three variants: T26a check-only-after-a-correct-main-scan (the shape finalize actually takes), T26b scan-from-polluted, T26c bare empty directory. test-validation-runner.js :533-611 (4 assertions, 1 red at :588) for the second resolver, measured byte-identical JSON across the two legs at HEAD. The surface is pinned as two mechanism-free properties — DIFFERENTIAL (a leftover and a legitimately worktree-resident folder must stop producing byte-identical output) and ACTIONABILITY — and NO assertion demands a non-zero exit; :1390 explicitly forbids a NEW refusal class while still allowing the pre-existing gaps_unswept. MUTATION PROOF, three families against a scratch mirror: detect-and-report PASSES both suites, teach-the-resolver PASSES both suites, and a constant-report negative control FAILS both — two opposite mechanism families closing the tests is the evidence that no mechanism is pinned. A fixture correction fell out of that: the first draft omitted workflow-state.md from main's folder, which silently FORBADE the claim-signature family, so markClaimCreated (:1322) now writes it. T26d pins the legitimate post-mirror both-trees window that a naive fix would break, and deliberately does NOT require silence there. Preservation confirmed: all 151 pre-existing assertions still pass, KAOLA_GAP_ROOT tier-1 intact, additions only. FLAG RAISED FOR ME, NOT DECIDED BY THE AUTHOR: run-chains resolveOutputPath returns a bare string with no report channel, so a report-only fix to resolveRecordFolder would leave THE CHAIN RECEIPT STILL LANDING IN THE LEFTOVER — pinning it would have forced one mechanism family, so the author correctly refused to decide it by assertion and routed it up.

- item: Implement #974 against those tests, reusing validation-runner's existing otherProjectRoots detection primitive (it already emits the report-not-refuse shape at :1535-1552 and only needs exporting) rather than reinventing it; canonical is the edit site and gitlab/gitea are generated, so regenerate rather than hand-editing them. ONE RESULT ADDED BY ME on the tests-974 flag, because a report-only fix would satisfy every assertion and still leave the defect that motivated the user's ruling: the chain receipt must LAND in the tree holding the run's real record, never in a leftover — resolveOutputPath has no report channel, so reporting alone does not reach it.
  status: done
  dispatched: implementer subagent, working in the WORKTREE .kw/worktrees/bundle-973-974-975 on scripts/kaola-workflow-gap-sweep.js and scripts/kaola-workflow-validation-runner.js plus a regenerate of the gitlab/gitea ports; it reads and runs the tests but never edits them; report to land at kaola-workflow/bundle-973-974-975/impl-974.md
  result: kaola-workflow/bundle-973-974-975/impl-974.md — 10 non-comment lines across the two co-derived resolvers (gap-sweep.js:488, validation-runner.js:1301), deliberately the SAME shape in both so they cannot drift apart again the way #971 left them. The rule is subtler than either candidate I anticipated: workflow-state.md — written by the claim transaction and nothing else — is a TIE-BREAK BETWEEN TWO CANDIDATE TREES, never a requirement on one. This tree wins when it carries the signature (post-mirror, both do, so T26d is safe) and when NEITHER carries it (first-run receipt-only folder, so T25e is safe); main is preferred in exactly one case, where it carries the signature and this tree does not. KAOLA_GAP_ROOT stays the function's first statement, so tier-1 is untouched. It chose retarget over detect-and-report because report-only closes Result 1 but NOT Result 2 — resolveOutputPath consumes .dir as a bare string, so the ANSWER had to move — and then declined to export otherProjectRoots or add a report at all, on the ground that the retarget closes both results and a report would be an addition no observed failure demanded. RESULT 2 MEASURED DIRECTLY against the real run-chains binary, not inferred: pre-fix the receipt landed in wt/.../issue-974r2/.cache/chain-receipt.json (THE LEFTOVER), fixed it landed in main/.../chain-receipt.json (THE REAL RECORD), both exit 0 with claude=0, placement the only difference. FOUR mutations on scratch mirrors: m1 reproduces exactly the HEAD failure set (8/165), m2 ("just read main whenever main holds it") reds T26d ALONE, proving the tie-break is precisely what protects the post-mirror window, m3/m4 red the record resolver at :588 and m3 puts the receipt back in the leftover. Suites serial with real exit codes: gap-sweep 8-fail → 173 passed exit 0; validation-runner → PASSED exit 0; run-chains 283 passed unchanged; walkthrough 210/210 FULL SCOPE exit 0; edition-sync --check, generate-routing-surfaces --check (18 surfaces), validate-script-sync (4 kernel copies identical), validate-workflow-contracts all exit 0. Six ports regenerated via edition-sync --write, none hand-edited; adaptive-schema.js untouched. OWED AND ROUTED TO ME: no CHANGELOG entry — the change is user-visible and there is no [Unreleased] section today, so creating one was a structural call it correctly declined to make alone.

- item: Put the two #975 design calls to the user in conversation — the residue probe's treatment of dirt it cannot attribute to the run, and whether the measured-but-hand-induced relative-TMPDIR escape gets built or recorded; both follow from premise-975 refuting the issue's mechanism, and both are value calls rather than facts.
  status: done
  dispatched: self, in conversation; two questions put together, carrying the staging measurement and the 81-fixture-root measurement
  result: BOTH RULED BY THE USER. (1) Residue — "Adopt own, report foreign": finalize keeps staging the run's own implementation dirt, and a path it cannot attribute to the run becomes a TYPED FINDING on the envelope and in finalization-summary.md instead of being committed. No door; the established measure-and-report idiom. (2) TMPDIR — "Build it, reuse the run-chains idiom": apply realpathSync(mkdtempSync(...)) as already proven at run-chains.js:326-333 to the fixture roots and to install-all.sh:233. The cycle guard is NOT built — refuted twice over, and it goes on the watch list as recorded-not-built.

- item: Author the failing tests for #975's two surviving halves — that finalize reports rather than silently stages a path it cannot attribute to the run (the measured harm: `git add -A` stages a foreign symlink at mode 120000), and that a fixture root cannot land in the checkout root under a relative TMPDIR (measured: 81 self-deleting dirs that no before/after check can see, so the pin must observe DURING the run, not after).
  status: in-flight
  dispatched: FIRST DISPATCH DIED — tdd-guide subagent in the WORKTREE, killed by an ECONNRESET before writing its note. Work PARTIALLY landed and I verified it by hand rather than trusting the worker: scripts/test-forge-finalize-findings.js +213 lines (Half A) and a NEW untracked scripts/test-fixture-sandbox.js (189 lines, Half B) which runs RED at a real exit 1 (4 passed, 1 failed) — I first misread this as exit 0 by gating on a pipeline through `tail`, the exact trap this project records. No tests-975.md note exists, so there is NO red-baseline proof for Half A and the item cannot close on what landed. Two facts the dead agent surfaced that change the work: (1) its failure text REFUTES the realpathSync idiom my own brief prescribed — realpathSync absolutises the returned STRING after the directory already exists, so with a relative TMPDIR the fixture root is still created inside the checkout; the root must be absolute at CREATION, and the value handed to children absolute too. run-chains.js:326 therefore protects children from inheriting a relative TMPDIR but does NOT protect its own directory. (2) scripts/test-suite-registration.js is armed and reds on the new file: "UNREGISTERED SUITE ... is in no test chain, so it NEVER RUNS", so the successor must register it in test:kaola-workflow:claude and :claude:full or exempt it with a reason. RE-DISPATCHED: a fresh tdd-guide, same worktree, inheriting both findings, to audit and complete what landed and write the note.
  status: done
  result: kaola-workflow/bundle-973-974-975/tests-975.md — inherited work KEPT and repaired in four places; RED on both halves. Half A: test-forge-finalize-findings.js part D :578-787, two legs per edition across all four editions, 13 red per edition = 52 red / 201 passed. Half B: the new test-fixture-sandbox.js, 5 passed / 1 failed exit 1. IT MEASURED THE HARM END TO END, closing premise-975's own open item — driven in a throwaway clone, `chore: finalize` COMMITTED plugins/plugins at mode 120000 with finalize exit 0, and `git status --porcelain` afterwards was EMPTY, which is precisely why nothing noticed. Four repairs to the dead agent's work: (1) verified its unverified "MEASURED" comment, which turned out true; (2) killed a silent parse bug — its helper read porcelain through a trimmed stream, so the leading space of the FIRST record was eaten and slice(3) then ate the first character of that pathname, invisible while every foreign path was untracked but corrupting the moment a ` M` record sorted first, which would have red a CORRECT fix; (3) added FOREIGN_TRACKED, a foreign MODIFICATION to a tracked file, because the residue probe lists it identically and `git add -A` takes it identically — measured in that same commit; (4) added the anti-vacuity checks the brief asked for and nothing had, which caught bug 2 within a minute. MUTATION PROOF, five families: R1 (exclude-before-add, attribution from branch history) and R2 (stage-then-un-stage, attribution from the net diff) are OPPOSITE transaction shapes and BOTH go 253/0 green, so a result is pinned and not a mechanism; C1 constant-report, C2 over-reach and C3 tracked-shortcut all stay red on DISTINCT assertions, and C3 reds on exactly and only the four FOREIGN_TRACKED assertions, which is the measurement proving that third shape earns its place. Half B mutation: B-REALPATH (the idiom that LOOKS like the fix) stays red, B-NODE half-fix stays red with the escape MOVING to the shell half at install-all.sh:233, B-BOTH goes 7/0 green. Registered in both claude and claude:full (46 files, 43 registered, 3 exempt, 538 assertions exit 0), chosen over exemption because the class is defined by nothing noticing. MY RULING ON THE ONE THING IT ROUTED UP: FOREIGN_TRACKED STAYS — it is not a widening past the user's ruling but an instance of it, since "a path it cannot attribute to the run" covers a modified tracked file, the mechanism reach is measured rather than speculated, and C3 proves that without it a plausible fix ships that silently adopts foreign edits to tracked files, which is harder to spot than the symlink was. NOTE FOR THE IMPLEMENTER: both R1 and R2 also had to update docs/api.md (the findings row and the "seven → eight" count sentence) before part B went green.

- item: Implement #975's two halves against those tests — foreign-vs-own dirt classification at finalize, and a fixture root that does not resolve against the current directory (NOT "absolute at creation", which tests-975b measured to be the wrong property since path.resolve(".") is the cwd) across both the Node sites and install-all.sh's three shell sites — and add the watch-list row recording the cycle guard as refuted-not-built with the Dirent/lstat measurement that would have to change to arm it.
  status: in-flight
  dispatched: implementer subagent, working in the WORKTREE .kw/worktrees/bundle-973-974-975 on the four hand-ported claim.js copies, install-all.sh, test-install-all.js's fixture-root construction (which is the IMPLEMENTATION here, judged by test-fixture-sandbox.js, so custody is intact), docs/api.md and the ADR 0017 watch list; it reads and runs the tests but never edits test-forge-finalize-findings.js or test-fixture-sandbox.js; report to land at kaola-workflow/bundle-973-974-975/impl-975.md
  status: done
  result: kaola-workflow/bundle-973-974-975/impl-975.md — both halves green: test-forge-finalize-findings 201/52-red → 253/0 exit 0, test-fixture-sandbox 5-passed/1-failed → 7 passed exit 0 (5→7 because the :212 assertion arms once the escape is gone, exactly as the test author predicted). Chose R1 exclude-before-add over R2 because R2 puts the artifact in the index first, so a process dying between the add and the reset leaves it staged — the precise outcome the issue is about. THE FINDING THAT MATTERS MOST IS ONE THE SUITE COULD NOT HAVE CAUGHT: it rejected TOP-SEGMENT attribution in favour of OWN-DIRECTORY attribution, and measured why — the 2026-08-12 bundle committed under plugins/kaola-workflow-gitea/scripts/ and friends, 24 paths under plugins/ and none directly in it, so `plugins` was in the run's own top-segment set and plugins/plugins WOULD HAVE BEEN CALLED THE RUN'S OWN WORK. Both designs pass the whole suite at 253/0 because the fixture branch touches only src/ and kaola-workflow/, so it stated the gap rather than letting a reader assume the pins covered it. Attribution reads git HISTORY not the net diff (work committed then reverted is still the run's), with two fail-open reads that stage everything exactly as before — git unaskable, and a branch carrying no commits of its own, since with no evidence of what the run authored "all of it is foreign" is not a finding. Neither forbidden design was used: no mtime read anywhere, no claim-time manifest. Half B moved BOTH language halves. Watch-list row added for the refuted cycle guard. Regression sweep all exit 0 incl. walkthrough 210/210 FULL SCOPE, claim-hardening 766, bundle-finalize 179, edition-sync --check, generate-routing-surfaces --check 18 surfaces. ONE RED ROUTED TO ME AND CORRECTLY NOT SELF-REPAIRED: test-finalize-door.js T9b, 490 → 487/3-failed, caused by this change but NOT a behaviour regression — T9b's own comment declares "either outcome is acceptable", the file IS named on the envelope, and the assertion still fails because envelopeNames compares a RAW needle against JSON.stringify(item) while JSON escapes exactly the newline/quote/backslash the three hazard names carry. I VERIFIED THIS MYSELF rather than accept it: raw needle unfindable for all three, JSON.stringify(needle).slice(1,-1) findable for all five. So the test's OBSERVATION is broken, not the behaviour it declares acceptable, and it was latent until something took the other branch. It also rejected the green-buying alternative — exempting the repository root from attribution — because the repo root is precisely where the Half-B escape class lands, so that carve-out would silently commit the very class being fixed. I agree and am not reversing it.

- item: Repair test-finalize-door.js T9b's OBSERVATION, which impl-975 found broken and correctly refused to touch — envelopeNames compares a raw needle against JSON.stringify(item), so a name carrying a newline, quote or backslash can never be observed on the "named" branch, which makes the assertion effectively demand the hazard be COMMITTED for exactly three of its five legs. This is repairing a measurement bug, not weakening a pin: T9b's declared acceptance ("either outcome is acceptable — being SILENT about dropping it is not") is still exactly right and must not change.
  status: in-flight
  dispatched: tdd-guide subagent, WORKTREE, owns scripts/test-finalize-door.js alone; must prove the repaired observation actually observes by mutation in both directions rather than just going green; outcome recorded inline on this item
  status: done
  result: kaola-workflow/bundle-973-974-975/tests-t9b.md — repaired in envelopeNames only (29+/5-); the T9b assertion, its acceptance wording, all five legs and all five hazard names are UNCHANGED, and test-finalize-door is back to exit 0 at 490 assertions, exactly the HEAD count, now all passing. IT REJECTED THE OPTION I SUGGESTED, and was right to: escaping the needle with JSON.stringify(needle).slice(1,-1) compensates for one serializer while keeping the same category error — measuring a rendering rather than the data — and is strictly LOOSER, since a stringified haystack can match across the `","` seam between two entries. It walks the parsed structure instead. Three proofs. RIGHT REASON: on all five legs disjunct 1 is false so nothing short-circuits, every pass is carried by the envelope term, and each hit locates to exactly finalize_transaction.residue_unattributed[0]; its instrumentation independently reproduced my own measurement that the old matcher was true only for `notes.md ` and `nöte.md`. MUTATION: deleting the whole reporting block from a mirrored claim.js — path still withheld, nothing names it — reds ALL FIVE legs including the two that were green before the repair. NOT VACUOUS: an absent needle, a one-char near miss, and `src/feature.js` (which IS on the envelope under changed_paths) all return false, so the band exclusion survived the rewrite. Adjacent suites green: bundle-finalize 179, claim-hardening 766, forge-finalize-findings 253. One behavioural note it surfaced for the reviewers: T9c's `locked.md` is unattributable under #975, so the report naming it now arrives from the residue_unattributed block rather than the staging-failure block.

- item: Write the bundle's CHANGELOG entry once all three implementations have landed — one entry covering all three issues, and it must be written BEFORE any chain-receipt run because editing test-consumed docs stales a receipt; there is no [Unreleased] section today (prepare consumes it), so creating one is part of the job.
  status: done
  dispatched: self, in the worktree; four entries under the existing [Unreleased] ### Fixed section of CHANGELOG.md, plus the two falsified doc paragraphs, and the outcome recorded inline
  result: An [Unreleased] section DID already exist, contrary to what I expected — the previous bundle cut no release, so it still holds #968's Changed entry and #969-972's Fixed entries, and mine append to that same ### Fixed block. Four entries written: #973 (all three installers, stating the silent partial legs rather than only the filed total wipe, and stating plainly the NEW obligation that retiring a surface now needs a list entry and nothing detects a forgotten one), #974 (both resolvers, the tie-break rule, and the chain-receipt consequence), and #975 split into its two genuinely separate halves (finalize reporting rather than committing unattributable dirt; fixture roots no longer resolving against the current directory, including the correction that absolutising after the fact does not fix it since path.resolve(".") IS the cwd). Also corrected the two doc paragraphs impl-973 routed up — docs/kimi-edition.md and docs/opencode-edition.md both still asserted the self-healing namespace prune the fix removed, every clause of which was false; both now state what the installer actually removes and that retiring means adding a name to the list. Written BEFORE any chain-receipt run, so the receipt will not be stale on these.

- item: Adversarially review all three implementations on the Fable model, one reviewer per issue, hunting for the defect that survived a green suite — every recent bundle has had one, including a fix that shipped the very defect it fixed.
  status: done
  result: review-973.md / review-974.md / review-975.md. #974 PASS — 9-cell truth table x both resolvers x both arms, exactly one cell changed and it is the intended one, Result 2 re-measured through the real run-chains binary, 8 degenerate predicate shapes agreeing, all four ports verified; it also found a cell the fix quietly IMPROVES (a plain file where the run folder should be returned the worktree at HEAD and died ENOTDIR). #973 FAIL — one real REGRESSION THIS BUNDLE INTRODUCED: kaola-role-issue-scout stranded forever where the old glob swept it, reproduced hermetically, refuting the implementer's "verified complete against the full shipping history"; I re-verified the history myself and it holds. #975 FAIL — the attribution rule itself survived every attack (byte-identical across all four copies, -z stream clean on hazard names, both fail-open reads measured failing OPEN, and the reviewer independently drove the real 2026-08-12 incident directory shape against the shipped code and it CAUGHT it), but F1 is the defect that survived every green suite: THE REPORT'S SHELF LIFE IS ONE STEP. Finalize now deliberately leaves unattributed paths UNTRACKED, and sink-merge Step 0's guard at :520 probes with --untracked-files=no, so it cannot see them, and `git worktree remove --force` then destroys them. I verified this myself: the flag is deliberate (:425-429 exists to ignore the workflow's own untracked lane dirs), so the naive flip to normal would break every run. The blindness was DORMANT before #975 — unattributed paths used to be staged and committed, so nothing untracked survived finalize — and #975 MANUFACTURES the state that makes it live. CLAUDE.md's "a sink over a tree carrying uncommitted work still fails loudly" is now false for exactly that state. Combined with R3 (own work in a BRAND-NEW subdirectory is reported foreign and left uncommitted, where pre-#975 it was committed) the destroyed thing can be the run's own work. Also F2: the CHANGELOG absolute I wrote — "the run's own work still stages exactly as before, including files it created but never committed" — is REFUTED by that same measurement, and docs/api.md carries the correct "beside a committed sibling" qualifier that I dropped. R6: the watch-list row's line anchors were shifted stale BY THIS SAME DIFF. R4: the repo ROOT is nearly always attributed (any branch committing CHANGELOG.md), so root-level foreign files are adopted — measured. R5: the relative-TMPDIR class survives at seven more installer sites outside the issue's enumerated set.

- item: SUPERSEDED RECORD of the first review dispatch, kept because it explains six agent deaths a successor would otherwise puzzle over.
  status: done
  dispatched: FIRST DISPATCH OF ALL THREE DIED ON THE FABLE WEEKLY QUOTA ("You've hit your weekly limit · resets 10am Asia/Shanghai"), none of them reaching a report — the locator is empty, so nothing is closeable and all three are simply re-run. Re-dispatched at 10:04 CST, four minutes after the stated reset. Three code-reviewer subagents ON THE FABLE MODEL as the user asked, one per issue, reading the WORKTREE diff; each is barred from running a full suite because the suites are spawn-bound and three concurrent reviewers plus my own verification pass would manufacture false reds — targeted node -e and git probes only, and a suite run is requested rather than taken. Reports to land at kaola-workflow/bundle-973-974-975/review-973.md, review-974.md, review-975.md. My own verification is ALREADY GREEN and was run serially while nothing else ran: walkthrough 210/210 at FULL SCOPE (total:1, not a shard) exit 0, and the whole claude chain exit 0 including the newly-registered test-fixture-sandbox suite. THIRD DISPATCH FOR #974 ONLY: review-974b died on a connection drop having written just its skeleton, and because the brief made it stamp a "STATUS: IN PROGRESS — if this line is still present the review died mid-flight" marker, the file itself proves nothing was completed rather than leaving me to guess; re-dispatched. That is now six agent deaths this session — three connection drops and three on the Fable weekly quota — and the write-early marker is the only reason any of the recoveries were unambiguous.

- item: Repair the regression the Fable review found — kaola-role-issue-scout is missing from install-kimi.sh's RETIRED_ROLE_SKILLS, so this bundle's own fix strands it forever where the old namespace glob swept it on every reinstall. I verified the history myself rather than take it on report: agents/issue-scout.md was present at f6dbf40d among 17 agents, the generator renders kaola-role-<agent> at :118/:433, and :327 of that same generator hardcodes "the kaola-role-issue-scout Skill" into rendered prose, so it demonstrably shipped; it was then deleted at d663fd66 on 2026-07-25, eight days into the kimi edition's life. The census missed it because .kimi/ was never tracked and default rename detection hides retirements. The installer's own comment ("the two roles retired since") is wrong — there were three.
  status: in-flight
  dispatched: tdd-guide first for custody — a P5c-shape assertion planting kaola-role-issue-scout and pinning that a reinstall sweeps it, proven RED against the current worktree; then an implementer to add the name and correct the false comments. Note lands at kaola-workflow/bundle-973-974-975/tests-973b.md and the repair at impl-973b-repair.md. SECOND DEFECT FOLDED IN, from review-974: the tie-break comment I helped propagate asserts "workflow-state.md is written by the claim transaction and nothing else", which is LITERALLY FALSE — the finalize mirror (claim.js:3466/3479) and sink-pr.js:160 also write it. The tie-break premise survives every producer the reviewer found, so this is a wrong absolute rather than a wrong mechanism, but it is the THIRD false comment this bundle and I repeated it in my own run record above, which is exactly how a false absolute propagates. One-clause rewording plus a regenerate of the six ports, since the comment lives in a generated-port file.
  status: done
  result: BOTH HALVES DONE. Implementation at kaola-workflow/bundle-973-974-975/impl-repairs.md — all three repairs green: test-kimi-edition back to 609 assertions exit 0 from 1-failure/608, mutation-proven (deleting kaola-role-issue-scout from the array again reds P5c naming it, with the control green first so the red is attributable to that single element), edition-sync --write regenerated 6 ports and --check is exit 0 with 8 forge aggregators in parity, `git grep "and nothing else writes that file"` now returns NO MATCHES so the false absolute is gone from every copy, and the walkthrough is 210/210 FULL SCOPE exit 0. Test half at tests-973b.md. RED at "still on disk: kaola-role-issue-scout", exit 1, 1 failure / 608 passed. The re-census I asked for paid off in the negative direction, which is the useful direction: EXACTLY ONE name was missing, no second — the complete historical retired set over f6dbf40d..HEAD with --no-renames is 11 names and install-kimi.sh held 10. It also excluded agents/profiles/higher/* on evidence rather than assumption (that era's generator lists TOP-LEVEL *.md only, per its own comment, and HEAD's expectedSkillDirs is unchanged in that respect), confirmed the D census is identical with and without --no-renames, and confirmed all three deleted agents are absent at HEAD. The pin is now stronger than the defect required: P5c's RETIRED array went from a 4-name SAMPLE to the full censused 11, with a note recording that the list is derived from HISTORY and deliberately NOT read back from install-kimi.sh — a pin that read the installer's own array could never catch an omission. Anti-vacuity proved in both directions in one run: adding issue-scout while dropping phase3 made it report phase3 alone, so it is a real predicate rather than a hardcoded case. EXTRA FINDING ROUTED TO ME AND DELIBERATELY NOT BUILT: the uninstall path has no coverage, because probe U1 installs before uninstalling so only the 17 just-written names are ever on disk and it structurally cannot see a retired-name residue. The one-line fix closes install AND uninstall since :421 iterates the same array, so no observed failure demands the probe; recorded, not built, per additive derivation.

- item: Put the two review-975 adjudications to the user in conversation — F1's sink blindness (a data-loss path this bundle manufactures) and the attribution rule's two measured weaknesses; both are value calls about what ships, and F1 concerns a destructive operation.
  status: done
  dispatched: self, in conversation; two questions together, carrying my own verification of sink-merge.js:520 and the reason the naive flip breaks every run
  result: BOTH RULED BY THE USER. (1) F1 — "Fix the sink guard in this bundle": Step 0's probe must see untracked work while still ignoring the workflow's own lane dirs, so it refuses loudly as CLAUDE.md already claims. (2) R3/R4 — "Document both, change the rule for neither": every alternative rule reintroduces the defect the implementer rejected on measurement, since nearest-committed-ancestor attribution would swallow plugins/plugins exactly as top-segment did; both go in the CHANGELOG and the watch list as known limits, and R3's danger comes from F1 rather than from itself, so fixing the sink degrades it to a run left visibly unfinished.

- item: Author the failing test for the sink guard — a worktree carrying an untracked path that is NOT one of the workflow's own lane dirs must not be force-removed silently; the guard at sink-merge.js:520 declares it "must FAIL CLOSED" yet probes with --untracked-files=no, and the flag is deliberate (:425-429 ignores untracked lane dirs), so the pin must distinguish those two populations or it will simply break every run.
  status: done
  result: PIN COMPLETE AND NON-VACUOUS ON ALL FOUR EDITIONS — established by my own run, not by a report, because tests-sink stopped writing without updating its note (suite mtime 12:14:53, note still 11:59, then 10 minutes of no writes; I checked ACTIVITY before concluding anything this time). `node scripts/test-sink-merge.js` exits 1 at 24 failed / 910 passed, and the reds decompose as arm (e) x2, arm (h) symlink x2 and arm (i) legacy x2 on EACH of root, codex, gitlab and gitea. The green-vacuity is gone: i/gitlab now reports `exit=0 status="merged" reason=undefined worktree_still_present=false survivors=[]`, which is the strong red tests-sink2 measured — the destructive step RAN past both gitlab-only preconditions and destroyed the work, so survival is false AND told is false, rather than the earlier shape where an early exit made both clauses pass while measuring nothing. (i-control) is now green, and tests-sink fixed the terminal-token defect better than asked: instead of asserting the literal `merged` it asserts a generic successful terminal (:131) and documents at :110 why `sinked` is wrong for this path, so the control cannot rot against a future token change. Arms f and g stay green as designed — f is the must-not-break ordinary run, g the ignored-files control. Full arm set is e, f, g, h, i, i-control. tests-sink then DID finish and rewrote tests-sink.md in place with one authoritative red set (STATUS: DONE), and its numbers match my independent run exactly — 104 new assertions introducing exactly these 24 reds and nothing else, pristine baseline 830 green. Two things it produced that my implementer brief did not have, both relayed on: COUNTER-MUTATION 3 is the one that earns arm (h) — a preserve repair written with copyFileSync alone FOLLOWS the link and throws ELOOP on plugins/plugins, so the rescue silently preserves nothing for the very artifact that motivated #975, and (h) reds on all four editions while (e) and (i) go GREEN; a repair that handled files and forgot links would have looked complete. COUNTER-MUTATION 4 instruments the hole I predicted: a repair covering only the --sink transaction reds (i) alone while all five other arms go green. IT ALSO CORRECTED AN INSTRUCTION OF MINE, rightly: I had asked for "an arm whose envelope is null has measured nothing and must red", which is WRONG here — the legacy terminal is try/catch at :3275 so a thrown refusal emits NO envelope, and a legitimate Family-A refusal repair emits none either, making envelope-presence unusable as a non-vacuity premise; (i-control) is (i)'s control instead. Bare-flag-flip is 17 reds not 13 — (f) AND (i-control) on all four plus #707 h x9 — so both must-not-break controls fire on both entry points. Coverage discovery worth keeping: finalValidationPassed is gitlab/gitea-only and requires /Final Validation/i AND /pass/i, and because the legacy tests drove the root script only, NOTHING IN THIS REPO HAD EVER RUN THAT GATE. TWO CORRECTIONS TO WHAT I RECORDED ABOVE, both from tests-sink and both accepted. FIRST, the green-vacuity tests-sink2 reported was REAL FOR THE REVISION IT READ BUT ALREADY SUPERSEDED: its snapshot md5 634f0ffc was stale against tests-sink's 235703cb, which already carried the in-place posture, the "## Final Validation / verdict: pass" summary body and an (i-control) no longer asserting `sinked`. Its measurements of the SINK'S BEHAVIOUR (M1, M2, the ELOOP/lstat facts) are sound and were used; its measurements of the TEST FILE were of a version that no longer existed. The archive-on-main it recommended was not needed — the untracked live dir alone defeats the early exit, because finalValidationPassed resolves the live summary first. SECOND, IT REFUTED MY NON-VACUITY INSTRUCTION WITH A MEASUREMENT rather than just declining it: injecting the literal "null envelope → red" rule into (i) and running it against a WORKING family-A repair gives 4 failed / 942 passed — red on all four editions on a repair that works, because the work survived and the operator WAS told and only the envelope was absent. That rule pins the mechanism, which is exactly what this project forbids. What shipped instead implements the intent: (i-control) now returns whether it completed, runs FIRST, and its result is (i)'s premise (:4482), plus result.status !== 3 (:4490) for the archived early exit — so under the bare flag flip (i-control) reds AND (i)'s premise reds naming it, instead of (i) going silently green. Authoritative frozen-snapshot baseline is 24 failed / 918 passed (my own earlier 24/910 predates its last assertions); mutation matrix: family A 942 green, family B zero #973 reds, counter-1 flag flip 21 reds, counter-2 --ignored 4, counter-3 symlink-blind preserve 9 with (e)/(i) green, counter-4 --sink-only 9 with all else green, counter-5 my own rejected rule 4 reds on a correct repair. OPERATIONAL WARNING I MUST HONOUR AT FINALIZE: an implementer wrote the sink scripts WHILE its suite was running and produced a torn read describing no version of the tree, so ANY suite result taken during an implementer write is VOID — hash the production files before and after a verification run, as it did. FIRST ROUND LANDED RED and I sent it back for two more arms it had offered rather than let the offer lapse. What it established: 8 reds at baseline across ALL FOUR sink copies (root :520, codex :520, gitlab :488, gitea :503 all carry the identical blind probe), 44 new assertions, and the lane/work boundary drawn from EVIDENCE rather than assertion — lane is under kaola-workflow/ and .kw/, because the sink itself stages <wt>/kaola-workflow/<project>/ before the removal precisely so untracked .cache evidence survives (sink-merge.js:2100-2130) and #707 h already pins that end to end; under the naive flag flip #707 h reds 9 times, which is independent corroboration of the trap. It measured that ignored files are unreachable by any --untracked-files setting, so arm (g) is the control against a repair reaching for --ignored, and its counter-mutation reds (g) on all four editions AND NOTHING ELSE IN THE SUITE — no pre-existing test catches that. Both repair families admitted (widen-and-subtract-lane 874 green; preserve-and-report with no path classification at all, 1 unrelated red), bare-flag-flip counter-mutation reds 13. Critical warning it surfaced for the implementer: isParkedLanePath CANNOT be reused as-is because it returns false for the run's OWN project (adaptive-schema.js:429-431), which in the linked worktree is exactly the population that must be ignored. SECOND ROUND ASKED FOR: (a) the SYMLINK survivor shape with an lstat oracle — not optional, because the observed #975 artifact WAS a symlink and a guard proven only on regular files is unproven against the shape that motivated the issue; existsSync returns false on a self-referential link while lstatSync resolves, and Dirent answers false to both isDirectory and isFile, so a survivor check built on either would pass silently over the artifact it exists to catch. (b) the legacy non---sink entry point, because a probe-side repair covers both entry points but a family-B repair placed in the --sink merge step would not, and I declined to close that by telling the implementer where to put the fix since a mechanism claim in a brief rots. Its third offered arm — untracked lane content belonging to ANOTHER project — I declined: no evidence it occurs, and destroying another run's state is a user value call, not an assertion. THE SEND-BACK DID NOT TAKE: the agent went idle without acting on it, and I caught that by checking the LOCATOR rather than believing its note, which was marked complete while still listing both items as unpinned — `git grep "#973"` shows only arms e/f/g in the suite and both lstat hits at :2354/:2391 are pre-existing archive tests. Re-dispatched as a fresh tdd-guide scoped to exactly those two arms, told not to redo the finished work and pointed at the in-file lstat precedent it should reuse. THAT RE-DISPATCH WAS MY ERROR AND THE SECOND AGENT CAUGHT IT: tests-sink had received the send-back and was mid-write; the file changed under tests-sink2 between two reads 90 seconds apart and what appeared was scaffolding for exactly the two arms — plantWorktreeUntracked973 at :886, an opts.symlinks knob on both fixture builders, and runSinkLegacyAt at :384 split out so the legacy path can be driven per edition. tests-sink2 refused to write, escalated instead of racing, and no work was lost. The lesson against my own resume rule: "look for the work, not the worker" is right, but ONE SAMPLE OF THE TREE IS NOT EVIDENCE OF ABSENCE while a writer may be live — the rule says re-dispatch unless you can show the dispatch is still alive, and I never checked mtime or recent activity before concluding it was dead. RESOLVED: tests-sink keeps the file and finishes both arms; tests-sink2 stands down to read-only measurement of two facts the arms depend on, landing at measure-sink-shapes.md — whether `git status --porcelain -uall` reports a self-referential symlink AT ALL (if not, that constrains the repair family, not just the test, since widening the flag alone would not see the artifact #975 actually observed), and whether the legacy entry point truly reaches assertWorktreeClean or stops earlier at assertNoLiveWorkflowFolder (an arm going green because an earlier guard fired would certify coverage that does not exist). THAT SECOND MEASUREMENT LANDED AND IT WAS THE SHARPEST CATCH OF THE RUN — record at kaola-workflow/bundle-973-974-975/measure-sink-shapes.md (its earlier scratch note tests-sink-hi-measurements.md was removed and replaced by that file — do not go looking for it). tests-sink DID land (h), (i) and an (i-control) of its own, and (h) is sound: RED x2 on all four editions, its lstat oracle independently confirmed, including that existsSync answers FALSE on a self-referential link while lstat resolves, statSync/readFileSync throw ELOOP, and a Dirent answers false to both isFile and isDirectory — so arm (e)'s nonce byte-search structurally cannot cover the symlink shape, which is exactly why I refused to ship without (h). But (i) IS GREEN-VACUOUS ON GITLAB AND GITEA: those sinks carry two legacy-path preconditions root and codex lack — finalValidationPassed (gitlab :1079) and an archived early-exit returning exit 3 (gitlab :1103) — and both stop the run BEFORE the destructive step, so nothing is destroyed and the survival clause passes while the non-zero exit makes the "told" clause pass. Both clauses pass while measuring NOTHING, and the arm's own non-vacuity guard cannot see it because there is no envelope at all (out is null). THIS IS THE SAME DEFECT CLASS AS #974 ITSELF — a vacuous pass, one layer up: #974 was a gate exiting 0 having mapped zero classes because it was pointed at the wrong tree, and this is an arm going green because an earlier precondition fired. Second defect: (i-control) asserts the terminal token `sinked`, but the legacy path emits `status: 'merged'` (sink-merge.js:1131) — a control asserting a token the path never emits reds for the wrong reason, the same disease inverted. tests-sink2 measured a posture that fixes both at once (archive on main, a summary satisfying finalValidationPassed, plus an untracked live dir in the main root) under which all four editions reach exit 0 / status "merged" and silently destroy the genuine untracked file — survival false AND told false, the strongest red available, uniform across editions. Sent to tests-sink with the exact changes; I have told it I treat (i) as NOT coverage until that lands, asked for a per-edition red set that establishes rather than asserts non-vacuity, and asked it to add the guard that would have caught this: an arm whose envelope is null has measured nothing and must red rather than pass. Full suite currently 24 failed / 910 passed at a stable md5. BOTH MEASUREMENTS THEN LANDED IN FULL. M1 came back the REASSURING way and rules no repair family out: `git status --porcelain -uall` DOES report a self-referential symlink (`?? plugins/plugins`, and on --untracked-files=normal, empty only on =no), so a probe-side widening can see the exact #975 artifact. What it constrains is the TEST, not the fix — git sees the link while Node's ordinary idioms do not, so a walk shaped `if (isDirectory) recurse; if (!isFile) continue;` skips it in silence, which is arm (e)'s filesContaining973 exactly and the reason (h) needed lstat+readlink rather than a parameter on (e). M2 confirmed the hypothesis and measured it rather than reasoning it: against the shipped root sink, a branch tip that is LIVE exits 1 at report/not_merged/run_not_finalized with the destructive step NEVER RUNNING and the work surviving, while a FINALIZED tip exits 0 at status "merged" and destroys it — so on root and codex (i) reaches assertWorktreeClean only because the fixture sits in the archived posture, now confirmed necessary AND sufficient. Two smaller items from the same run: the legacy terminal token is `merged` not `sinked` (so (i-control) reds on the token on root/codex too), and removeWorktree's archive rescue (claim.js:588) aborts Step 4 on any legacy fixture that commits the archive only on the branch — tests-sink had already fixed that one independently.

- item: Take the prose repairs I own from the reviews — my own refuted CHANGELOG absolute, the R3/R4 limits the user ruled should be documented, and the watch-list row anchors this same diff shifted stale.
  status: done
  dispatched: self, in the worktree, on CHANGELOG.md and docs/decisions/0017-the-mission-list.md; outcome recorded inline
  result: F2 fixed — my CHANGELOG sentence claimed the run's own work "still stages exactly as before, including files it created but never committed", which review-975 REFUTED by measurement, and docs/api.md had carried the correct "beside a committed sibling" qualifier all along that I dropped when writing the entry; the qualifier is restored. R3 and R4 documented as known limits in the same entry, with the reason they are not closed stated so a later reader does not "fix" them into a regression: attributing by top-level segment OR by nearest committed ancestor would BOTH have called the original plugins/plugins artifact the run's own work, since the run committed under plugins/<edition>/scripts/. R6 fixed — the watch-list row cited install-all.sh:387-406 and test-install-all.js:479-489, both shifted stale by this same diff's Half-B fix; I measured the real positions rather than guessing an offset (the walk is now :400-407 inside codex_cache_content_state at :395, and the second copy is in dirsEqual at :496-502) and corrected them. Worth noting against the row itself: a NEIGHBOURING row in that same table deliberately cites "#524 by issue, not by line: this table outlives any line number" — the new row uses line anchors heavily, which is precisely why it went stale inside a single bundle.

- item: Implement the sink guard against the 24 reds — a worktree carrying genuine untracked work must not be silently force-removed, on both the --sink and legacy entry points and on all four editions, while an ordinary run carrying only the workflow's own untracked lane content still sinks unchanged.
  status: in-flight
  dispatched: implementer subagent, WORKTREE, owns the four kaola-workflow-sink-merge.js copies; report to land at kaola-workflow/bundle-973-974-975/impl-sink.md

- item: Adversarially review the sink guard on the Fable model — a gap I nearly shipped past: all three Fable reviews ran BEFORE this guard existed, so the most destructive change in the bundle (the one that decides whether `git worktree remove --force` proceeds) is the only one nobody has attacked. My own read says it is good — the probe widens to -uall rather than =normal for a stated reason (=normal collapses a wholly-untracked directory into ONE record while the classifier reads a path's project segment, so a collapsed kaola-workflow/ would classify as work and refuse every run), only `??` records are filtered, tracked records are untouched, and an undecodable record is kept fail-closed — and it uses isParkedLanePath with an EMPTY ownedProjects, which is the exact right answer to the warning rather than an avoidance of it: in the main-root call the run's own lane folder is the live record about to be published and must not be exempt, while here it is the worktree's throwaway copy that every run carries and no run means to keep. But my read is not a review.
  status: in-flight
  dispatched: code-reviewer subagent ON THE FABLE MODEL, read-only, barred from running any full suite because a suite read during another agent's write already produced a failure set describing no version of the tree this session; pointed at the isParkedLanePath truth table and the porcelain C-escaping surface as the two places a hole would hide; report to land at kaola-workflow/bundle-973-974-975/review-sink.md
  status: done
  result: REVIEW DONE — kaola-workflow/bundle-973-974-975/review-sink.md. VERDICT SOUND IN BOTH DIRECTIONS, reproduced with its own fixtures against the REAL sink CLI rather than the suite or inherited numbers: a HEAD-mirror sink silently destroys an untracked file at exit 0 "merged", the shipped sink refuses at exit 1 with the worktree and file intact and the path NAMED, for a genuine file AND for the #975 self-referential symlink; lane-only content still completes at exit 0; and its own over-broadening mutation refuses the ordinary lane-only run, so the [] subtraction is independently confirmed load-bearing. Porcelain decoding exact on 18/18 hostile names (spaces, quotes, newlines, octal UTF-8, trailing space) with fail-closed on degenerates; ignored files unreachable via all three ignore mechanisms; -uall does not collapse quoted-name directories; four copies consistent with root==codex cmp, kernel byte-identical x4, edition-sync --check and validate-script-sync both exit 0; no torn read (hashes stable, and the test file still at tests-sink's custody md5). ONE DEFECT, RS1 low and candidate-caused — THE FOURTH FALSE COMMENT THIS BUNDLE: the #579 note at :517-518 still claimed the probe passes --untracked-files=no AND that ownedProjects is "passed through to the inner status check", both false after the change, and the second is exactly backwards from the filter's whole subtlety, which is that this call must use []. I FIXED IT MYSELF rather than dispatch: rewrote the note to say what is true, re-synced the codex copy by cp and proved it with cmp, and confirmed by grep that the stale clause is gone from all four copies. Re-verified after: validate-script-sync exit 0 with 4 kernel copies identical, edition-sync --check exit 0, test-sink-merge exit 0. THREE RESIDUALS it measured, all destroyed EQUALLY by the pre-change code so residual rather than regression, and all now written into the CHANGELOG because my entry read as an absolute and this bundle has already shipped four of those: a file whose NAME contains backslashes (legal on POSIX) can normalise onto a lane prefix and be classified lane; an embedded git repo under a lane prefix is ONE collapsed record even under -uall, so it is exempted by directory segment and everything uncommitted inside goes with it; and the legacy route removes the run's own .cache/ journal where the --sink route stages a best-effort rescue first. It judged CLAUDE.md's "a sink over a tree carrying uncommitted work still fails loudly" TRUE as the repo means it, bounded by those three.

- item: SUPERSEDED — the sink implementation record, kept for provenance.
  status: done
  result: IMPLEMENTATION SIDE — impl-sink.md — impl-sink.md. Family A chosen and placed in the shared assertWorktreeClean so it covers both entry points; test-sink-merge 24-failed/910 → 942 assertions exit 0; walkthrough 210/210 FULL SCOPE; mutation-proven both directions (reverting the guard reds all 24 again, over-broadening reds the must-not-break controls at 21). It reworked the fast-gate section into something better than I asked: rather than just running the gate it MEASURED which suites the gate defers — test-claim-hardening, test-sink-merge, test-run-chains and the full walkthrough, which the gate samples at --shard auto/12 — ran three of the four at full scope, and justified the fourth by measurement (test-run-chains has zero references to sink-merge). It ran claim-hardening on its own initiative because that suite references sink-merge ten times and leaving it would have been a gap it could see. Two honest observations: it hit the phantom LITERAL-NULL-ENVELOPE-RULE because the shared scratchpad's obvious name `mirror` collided with another agent's mutation experiment, redid every measurement under uniquely-named directories, and correctly FLAGGED RATHER THAN FIXED the phantom, noting that if such a rule ever landed its legacy repair would need a JSON envelope on main()'s catch — a change to that entry point's terminal, outside its write set, that it wanted assigned deliberately rather than smuggled in. TWO FURTHER THINGS FROM ITS FINAL REPORT. It deliberately did NOT run edition-sync --write, on the grounds that the sink forge ports are hand-ported and that --write would have propagated other agents' in-flight canonical edits — I VERIFIED BOTH HALVES rather than accept them: edition-sync.js:30-34 explicitly names claim / sink-merge / sink-pr / active-folders / classifier / roadmap as HAND-PORTED data-layer ports that generation does not own, and --check is exit 0 with 8 aggregator ports in parity plus committed kernel parity, so skipping --write was correct and the codex copy was made by cp + cmp instead. And it flagged a user-visible consequence I have now written into the CHANGELOG: an untracked file that is neither lane content nor ignored — a stray .DS_Store in a repo that does not ignore it being the likely case — now REFUSES the sink where it was previously destroyed without mention. That is the intended direction rather than a regression, but it is a real change in what an operator will experience, and burying it would have been the kind of undisclosed behaviour change this project keeps catching in review. FINAL EXCHANGE, no code change needed: the copyFileSync/ELOOP hazard does not apply to Family A because it copies nothing and so has no dereference to get wrong — the worktree is left standing, links and all — and it recorded that as the strongest argument against the family it did NOT pick, with the lstat+readlink+symlink floor noted for anyone revisiting the seam with a preserve-shaped repair. My correction of the envelope rule closed its only open flag with no action; it verified the terminal itself and CORRECTED MY LINE NUMBER — the legacy catch is canonical :3314, not the :3275 I cited, which was the pre-edit position before its change added 45 lines above. It reconciled the two mutation counts exactly rather than waving at them: my corrected 17 is (f)x4 + (i-control)x4 + #707 h x9, and its 21 is those 17 plus the four (i)-premise assertions that fire once (i-control) stops completing. And on the gitlab/gitea legacy path having had NO prior coverage anywhere in this repo, it did not lean on surrounding green: its mutation runs prove both directions for those two editions specifically — reverting the guard reds (i/gitlab) and (i/gitea) x2 each, over-broadening reds (i-control/gitlab) and (i-control/gitea) — which is the difference between an armed arm and the vacuous green that existed before the fixture posture was fixed.

- item: Verify the whole bundle at full scope — the walkthrough at full scope (the fast gate only samples a rotating 1/12 shard), plus whichever suites the diff actually touches — then hand off to /kaola-workflow-finalize bundle-973-974-975.
  status: done
  dispatched: self, serially in the worktree with every real exit code captured directly rather than through a pipe, and with a hash over every modified .js/.sh taken BEFORE and AFTER the whole pass — the torn-read discipline this run learned the hard way, after an implementer wrote the sink scripts during a suite run and produced a failure set describing no version of the tree
  result: ALL SIX GREEN AND THE TREE PROVEN STABLE — PRE_HASH == POST_HASH == b43a0d11e9fa44376b2eca97c45cc8c3, so nothing wrote during the pass and the numbers describe a real version of the tree. claude chain (fast gate) exit 0; walkthrough exit 0 at FULL SCOPE with spawn-census 2432, byte-identical to the full-scope run I took before any sink work landed, which is the tell that it was not a shard; test-sink-merge exit 0 at 942 assertions; test-claim-hardening exit 0 at 766; test-kimi-edition exit 0 at 609 with 3 trees in parity; test-opencode-edition exit 0 at 643 with 3 trees in parity. The last four matter precisely because the fast gate does NOT cover them: sink-merge holds every assertion protecting the change to the destructive path, claim-hardening is deferred, and the two edition suites are in no chain at all and are the ONLY coverage install-kimi.sh and install-opencode.sh have — a green gate would have said nothing about any of them.
