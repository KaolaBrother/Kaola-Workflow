# Run gaps — bundle-973-974-975

Hand-recorded gaps this run discovered that the scanner's own heuristics did not observe. Each is a
measurement made during the run, not a derivation. The full measurements live in the filed issues
(#976, #977, #978) and in the review reports beside this file.

gap: installer-tmpdir-escape — the relative-TMPDIR escape survives at seven more installer sites outside #975's enumerated set

gap: retired-name-incomplete — install.sh strands seven claude-workflow* names, opencode's uninstall ignores its retired list, and probe U1 structurally cannot observe a retired residue

gap: sink-lane-exemption-residual — three measured shapes the lane exemption still destroys silently — backslash filenames, an embedded git repo collapsing to one record, the legacy route's own .cache journal

gap: sink-staging-dir-leak — the sink's kw-wtsync-* staging directory is never removed
