# Close the four post-#940–#944 audit observations: #945 vacuous sandbox assertions, #946 dead {INVESTIGATOR_MODEL}, #947 dangling Codex profile-gate reference, #948 A30 mixture gap

- item: Verify #945's premise independently — that 7 `--check exits 1 on a hand-edited <topic> surface` assertions in `scripts/test-generate-routing-surfaces.js` pass vacuously when the sandbox cannot start, because a dead sandbox exits 1 for every invocation; issue claims proof by neutering `cmdCheck`'s `process.exit(1)` in a mirror (3 finalize, 2 init, 2 next go red, 426 passed). Confirm the count, the mechanism, and whether the `clean.status` baseline control really catches them in aggregate.
  status: done
  dispatched: investigator (read-only, re-measures via scratch mirror) → report at kaola-workflow/bundle-945-946-947-948/.cache/premise-945.md
  result: .cache/premise-945.md — CONFIRMED with 2 qualifications. Core claim exact: under a dead sandbox all 7 V3 `exits 1` assertions PASS against a process that died at module load (Node module-load failure exits 1, byte-identical to cmdCheck's drift exit). Block is 37 assertions (2 baseline + 7 victims x 5), not a triple. QUAL-1: issue's "426 passed" is wrong, measured 425 of 432. QUAL-2 (matters): the issue's own framing is REFUTED — `clean.status` is a ONE-SHOT PRE-LOOP control that PASSES under a mid-loop sandbox death (LEG C), so the load-bearing controls are V4 (names-as-drifted) and V5 (exits-0-after-revert), which catch 7/7 in both legs. V3 is not useless: it is the SOLE catch when drift prints but the exit code is wrong (LEG B). Adjacent unfiled defect: V3's message names only the topic, not the path, so 3 identical "finalize" lines print with nothing distinguishing which surface failed.

- item: Verify #946's premise — `{INVESTIGATOR_MODEL}` registered twice in `install.sh` (~:544 `model_for_placeholder()`, ~:576 `render_command_file` list) and consumed by no template. Check ALL install scripts and ALL four editions (install.sh, install-all.sh, install-opencode.sh, install-kimi.sh) plus every prompt surface, not just `commands/ templates/ skills/`; prose absolutes in this repo have repeatedly been root-edition-only.
  status: done
  dispatched: investigator (read-only) → report at kaola-workflow/bundle-945-946-947-948/.cache/premise-946.md
  result: .cache/premise-946.md — QUALIFIED. Mechanical claim CONFIRMED: sites at install.sh:544 and :576 unchanged; removing both changes ZERO rendered bytes (A/B install into two sandbox $HOMEs, 37 files path-normalised hash-identical, render test + contract validator green on leg B). Three qualifications: (1) 8 of 11 registered placeholders are dead, not 1 — only TDD_GUIDE/BUILD_ERROR_RESOLVER/DOC_UPDATER have consumers, so singling out investigator misdescribes the shape; (2) the issue's grep pathspec was defective — no `skills/` dir exists at repo root, and `plugins/*/commands/` was omitted, so the conclusion survives but its method never established it; (3) the broader claim "investigator's tier reaches no rendered surface" is REFUTED — it reaches shipped bytes via 4 carriers (resolve-agent-model.js:15 + 3 byte-mirrors, six Codex SKILL tier tables, CODEX_PINNED_* / opencode frontmatter derivation, EXPECTED_ROLE_MODELS + README). Agent flags whether the 8-way deadness is a deliberate capability surface or residue as a values question for the user.

- item: Verify #947's premise — the installed Codex `kaola-workflow-next/SKILL.md` (~:251-253) points at a "Codex Profile Freshness Gate" section that does not exist. Determine whether that gate survives under another name anywhere (authored skeleton `templates/routing/next.skeleton.md`, rendered surfaces, installed trees, git history); that answer decides restore-vs-retire. Codex skills install at `~/.codex/plugins/cache/.../<ver>/skills/`.
  status: done
  dispatched: investigator (read-only, incl. git history search) → report at kaola-workflow/bundle-945-946-947-948/.cache/premise-947.md
  result: .cache/premise-947.md — CONFIRMED, and the design call is SETTLED BY EVIDENCE as RETIRE. The gate is GONE: introduced valid at ea84673d, orphaned at 17296a65 (#926, "fix: verify Codex profiles at install time") which deleted 78 lines from each skeleton with zero insertions, leaving the cross-reference standing by omission. Its responsibility relocated deliberately to the install/upgrade transaction (install-codex-agent-profiles.js postVerify) and to an explicit --doctor step on the init surface; nothing bears a name "above" could resolve to. RESTORE IS REFUTED: the removed section contains 6 of T19's 9 forbidden tokens, so restoring reds test-route-reachability.js and reverses #926 plus four doc surfaces (README:580, architecture:319, conventions:54, api:1488). SCOPE CORRECTION: 5 live carriers, not 1 — templates/routing/next.skeleton.md:245-246 (prose) AND :237 (the REGION:skill comment whose sole recorded justification IS the dead gate), plus the 3 rendered Codex SKILL.md (github/gitlab/gitea) at :257-259. Codex-only, but all three forge editions. UNANTICIPATED: T19 at test-route-reachability.js:315 forbids lowercase 'profile freshness gate' via case-sensitive String.includes, while the survivor ships as 'Profile Freshness Gate' — the guard built to prove the gate was gone cannot see the reference that outlived it, and is green today with the dangler in place.

- item: Verify #948's premise — `A30.SCENARIOS` in `scripts/test-opencode-edition.js` covers the #941 remediation-footer classes but omits the `{WRITE + SOURCE_EDIT}` mixture (a `--write`-clearable stale generated artifact alongside an unregistered plugin, which no flag clears). Confirm the omission, and confirm by hand that today's footer is correct for that mixture so the new pin encodes real behaviour rather than a wish.
  status: done
  dispatched: investigator (read-only, drives the footer by hand) → report at kaola-workflow/bundle-945-946-947-948/.cache/premise-948.md
  result: .cache/premise-948.md — QUALIFIED, substance CONFIRMED. `{WRITE + SOURCE_EDIT}` is absent from A30.SCENARIOS (scripts/test-opencode-edition.js:2708-2714, 5 scenarios over 3 classes). Footer driven by hand for the mixture: advises `--write`, separately names the source-edit path, and states the negative outright ("No flag of this script clears ...") — CORRECT, so it is a coverage gap, not a defect; all 7 mixtures driven, every leg held. Issue's "the one mixture" is false: TWO subsets are absent, but the three-way {W+WC+SE} has a branch profile identical to the already-covered {WC+SE}, so {W+SE} (profile E: `--write` advised alongside a no-flag-clears line) is the only branch-distinct gap — the issue picked the right one and miscounted the rest. BLOCKER FOUND: the opencode suite cannot run on main — `.opencode/agent/synthesizer.md` is stale because commit 97df0d6f touched `agents/synthesizer.md` without regenerating the gitignored tree, so D0 fails before A30. Baseline taken in a clean clone instead: 555 assertions, exit 0. Correction to my brief: test-opencode-edition.js has NO `--shard` handling.

- item: Settle the two design calls the issues deliberately left open — #945 (assert on rendered output vs. accept the aggregate `clean.status` baseline control as sufficient) and #947 (restore the named section vs. retire the sentence). Both are value-laden; ask the user in conversation with the measurements in hand, do not pick unilaterally.
  status: done
  dispatched: self — asked the user in conversation with all four premise reports in hand
  result: #947 needed no value call: evidence settled it as RETIRE (restore reds T19 on 6 tokens and reverses #926 + 4 doc surfaces). User ruled on the other three. (1) #945 → derive the hand-typed sandbox copy list from the real require graph AND make V3's message name row.path. (2) #946 → remove ALL 8 dead registrations from both install.sh lists, keeping the 3 live (TDD_GUIDE, BUILD_ERROR_RESOLVER, DOC_UPDATER); the 11-entry list is residue, not a capability surface. (3) T19's case-sensitivity IS in scope for this run — fix the predicate to match case-insensitively. Baseline established serially in the worktree before any edit: opencode suite 555 assertions, exit 0 (.opencode absent there, so D0 SKIPS — main's copy is stale and cannot run it).

- item: Implement the settled fixes for #945/#946/#947/#948 across their disjoint file sets; test custody applies — whoever implements a behaviour does not author its tests, and #945/#948 are test artifacts belonging to tdd-guide.
  status: in-flight
  dispatched: WAVE 1, three concurrent agents, all editing in the WORKTREE /Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-945-946-947-948 on branch workflow/bundle-945-946-947-948, disjoint file sets, reports to main's kaola-workflow/bundle-945-946-947-948/.cache/. (A) tdd-guide → #945, scripts/test-generate-routing-surfaces.js only → .cache/impl-945.md. (B) tdd-guide → #948, scripts/test-opencode-edition.js only → .cache/impl-948.md. (E) tdd-guide → T19 case-insensitivity, scripts/test-route-reachability.js only, must prove it goes RED against today's still-dangling prose → .cache/impl-947-t19.md. WAVE 2 after E lands (D depends on E: E's guard must be red first, then D turns it green): (C) implementer → #946, install.sh only → .cache/impl-946.md; (D) implementer → #947 retire, templates/routing/next.skeleton.md + regenerate the 3 rendered Codex SKILL.md → .cache/impl-947.md. Suite is spawn-bound and cannot be parallelized without false reds — every agent told to re-run any unexpected failure serially before reporting, and I re-verify everything serially at the end.
  partial-result: (E) T19 DONE — .cache/impl-947-t19.md. scripts/test-route-reachability.js predicate now folds case (+7/-1); suite went 331/exit 0 → 4 failures/327 passed/exit 1, which is the INTENDED red. Proven three ways: V1 reverting to case-sensitive returns exactly 331/exit 0 (so the change is the sole cause), V2 re-runs the existing mutation proof with every token UPPERCASED and all 72 pairs (8 files x 9 tokens) still red the check, V3 neutralizing the predicate produces exactly 72 failures proving the loop is armed, not vacuous. Delta is exactly 4 (file, token) pairs, all token #7; zero innocent matches from the other 8 identifier-shaped tokens; finalize skeleton and all 3 finalize SKILLs stay clean. (D) dispatched after E on that evidence.
  partial-result: (D) #947 DONE — .cache/impl-947.md. next.skeleton.md: REGION:skill directive KEPT (deleting it would have changed what renders on six surfaces) with only its dead justification rewritten to the surviving reason (the live Codex Per-Spawn Model Routing contract); clause 1 (the dangling pointer) removed, clause 2 ("profile drift is not tool unavailability") kept as its own sentence since it states a live rule and needed no new cross-reference. Regenerated via `generate-routing-surfaces.js --write` (18 surfaces), never hand-edited; 4 files, +9/-13. T19 now 331 assertions / exit 0 (327+4=331, nothing lost). Load-bearing detail the brief only half-anticipated: the non-rendering REGION comment was itself one of the 4 flagged carriers. No provenance written into the prose.
  partial-result: (C) #946 DONE — .cache/impl-946.md. install.sh -16/+4: 8 case arms + 8 array entries removed, 4-line comment added recording the coupling invariant. Both lists mechanically extracted and diffed → identical, exactly the 3 live names. A/B sandbox install (isolated $HOME, never --global, no `--global`): 39 files each leg, 36 byte-identical raw, the 3 differing are install-run nondeterminism only (firstStartTime/machineID/epoch-ms filename/$HOME path, identical after normalisation). Zero unsubstituted {*_MODEL} in the installed tree, with a POSITIVE CONTROL proving the scan finds the 6 real source hits. The 3 live placeholders still render sonnet/opus/sonnet at :87/:96/:155. All 8 roles still ship. Seven install-reading suites green. Extra finding: all 8 removed roles carry `model: inherit`, so they were dead twice over — no consumer, and no usable value if one had appeared.
  partial-result: (B) #948 DONE — .cache/impl-948.md. One scenario added to A30.SCENARIOS, no assertion authored or edited (the band already generalises), no footer prose pinned. 555 → 563 assertions, exit 0, delta accounted for exactly. TWO corrections to my brief: (i) `.opencode` is PRESENT in the worktree so D0 ran for real (3 trees in parity), not skipped as I stated; (ii) the mutation I proposed is NOT exclusive — it also reds the pre-existing `[unregistered canonical plugin]` scenario. The agent built a sharper one (when write-clearable and flag-irreducible coexist, name the stronger flag) which reds ONLY the new scenario: 1 failure, 562 passed. Declined blind spot, and I agree: dropping the footer's source-edit line stays green, but catching it needs a WORDING pin, which A30's outcome-not-wording contract forbids and which would be a mechanism for a never-observed failure class.

- item: (impl item closed above) Regenerate routing surfaces if any skeleton changed (`node scripts/generate-routing-surfaces.js --check` must print the surface count and pass), then verify with the full walkthrough at full scope (`node scripts/simulate-workflow-walkthrough.js` exit 0) plus the opencode edition suite; the fast gate SAMPLES a 1/12 shard so green there is not verification.
  status: done
  dispatched: self — re-ran everything SERIALLY in the worktree with absolute paths and real exit codes, taking no subagent's green on faith (own your own verdicts; the suite is spawn-bound and false-reds under concurrency)
  result: all green in the worktree. generate-routing-surfaces --check → "all 18 surfaces byte-match the skeleton", exit 0. test-route-reachability → 331 assertions, exit 0. test-generate-routing-surfaces → 434 assertions, exit 0. test-opencode-edition → 563 assertions, exit 0, drift-check 3 trees in parity. test-install-model-rendering → exit 0. validate-workflow-contracts → exit 0. simulate-workflow-walkthrough at FULL scope → 209/209 scenarios, `"total":1` (unsharded), exit 0, 2400 spawns.

- item: Dock documentation for whatever the fixes made user-visible — CHANGELOG under `[Unreleased]`, and `docs/conventions.md` only if an observation earned a convention; then hand off to `/kaola-workflow-finalize bundle-945-946-947-948`.
  status: in-flight
  dispatched: self — CHANGELOG written BEFORE any receipt run (a receipt taken first would be staled by the doc edit)
  result-so-far: CHANGELOG.md +55 lines under [Unreleased]: #948 under Added; #947 (dangling pointer) + #947 (the case-blind guard) + #945 under Fixed; #946 under Removed. No README/api.md/architecture.md change is owed — the only doc references to the placeholder machinery are in docs/decisions/ and docs/investigations/, which are dated historical records and sanctioned residue by this repo's own precedent. PROCESS CORRECTION: I first wrote CHANGELOG.md into the MAIN checkout, which sits on `main`, not on the run's branch — the worktree/main split trap. Verified both HEAD copies were byte-identical, copied the edited file into the worktree, confirmed the sha matched, and restored main to HEAD. Main is now clean apart from the untracked run folder; the branch carries the edit.
