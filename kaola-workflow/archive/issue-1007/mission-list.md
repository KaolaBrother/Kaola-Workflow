# #1007 — D-645-01 states the axiom block reaches six surfaces; the guard now checks fourteen

- item: Check the premise at HEAD `66ac0442`, and close the gap the filing itself declared unmeasured. The three citations (`D-645-01:34`, `:39`, `:69-70`) were verified by reading before filing, but the body says plainly "Not measured: whether any other count in this ADR or in `docs/` carries the same stale six." Measure that now. Note especially that `:69-70` carries TWO counts in one sentence — "any of the six embeds" AND "a missing axiom pointer on any of the six `next` surfaces" — and the additive opencode/kimi editions may have staled the second one too, which the issue never considered.
  status: done
  dispatched: investigator subagent, read-only over the MAIN root at 66ac0442, writes to kaola-workflow/issue-1007/findings/premise-1007.md
  result: kaola-workflow/issue-1007/findings/premise-1007.md — **HOLDS**, all three citations accurate, guard confirmed by RUNNING it (`PASSED (14 surfaces)`, exit 0; width at simulate-workflow-walkthrough.js:11482 is `FORGES.length * (2 + runtimeEditionCount) + 2` = 14). But the issue UNDERSTATES the problem on two axes. (a) **Three more stale sites, all in `docs/conventions.md`** — `:849` and `:852` say TWELVE (true: 14), and `:335` quotes the guard's width MISSING the `+ 2` and then says "passes at 12->8" where it should be 14->10. `README.md:42` is CORRECT at fourteen, so one subject carries a three-generation gradient: D-645-01 = 6 -> conventions.md = 12 -> README = 14. Scale is otherwise small and reassuring: of ~60 "six-surface" statements across docs/decisions/, only the three in D-645-01 concern the axiom embed — the rest cite the #400 routing-prose rule and are TRUE and deliberately forge-scoped; every other count class checked (4 editions, 4 runtimes, 3 forges, 3 commands, 18 surfaces, 14 roles, 16 support scripts) is true at HEAD. (b) **`D-645-01:69-70` is wrong TWICE, and the second is a FALSE MECHANISM CLAIM, not a stale count**: there are 12 `next` surfaces (not 6) and all 12 carry the pointer, but "reds `npm test` immediately" is FALSE — the `nx-first-principles` required-blocks entry was DELETED by `ea84673d`. MUTATION-PROVED in a git-archive sandbox: pointer stripped from all 12 surfaces leaves generate-routing-surfaces --check, test-route-reachability (368), testAxiomBlockByteIdentity (14), test-generate-routing-surfaces (434) and test-kimi-edition (642) ALL exit 0. The only guard that fires is `test-opencode-edition.js` A25, which reads exactly ONE file (`:998`) and lives only in `test:kaola-workflow:editions`, absent from `npm test`. `docs/conventions.md:855-856` is already ahead of the ADR here. (c) Decisive for the values call: `validate-workflow-contracts.js:952-956` excludes `docs/decisions/` **by explicit design** — "History is out of scope by construction... rewriting them would falsify the record" — and docs/** is invisible to the chain-receipt band, so editing either file costs no receipt. `conventions.md` carries no such exclusion. (d) The durable fix has precedent: `conventions.md:141` already says "Read the count off `node scripts/generate-routing-surfaces.js --check`, which prints it, rather than from this sentence." Unestablished: the "nothing in the four chains catches this" claim rests on per-suite mutation plus reading package.json composition, not a full chain run; docs/investigations/ and docs/audits/ (34 files) were out of scope and unswept.

- item: OWNER RULINGS, recorded so the work cannot drift from them. (1) **conventions.md is CORRECTED in place** — `:849` and `:852` twelve -> fourteen, `:335` restore the missing `+ 2` and 12->8 becomes 14->10 — and the corrected sentences POINT AT THE PRODUCER, following the precedent already at conventions.md:141 ("Read the count off ... which prints it, rather than from this sentence"). (2) **D-645-01 is ANNOTATED, not rewritten** — counts left exactly as written, a dated note appended, because the repo's own validator excludes docs/decisions/ as history by construction and rewriting them would falsify the record. (3) **The guard gap IS fixed in this run**, not filed. That inverts what the note must say: the `npm test` enforcement named in `:69-70` was deleted by `ea84673d` and is RESTORED here, so the sentence becomes TRUE again modulo six -> twelve, rather than being documented as false. The note therefore records deletion AND restoration.
  status: done
  dispatched: self, asked in conversation with the full measurement in hand
  result: as recorded above. The decisive fact for ruling (2) was not opinion but code: `validate-workflow-contracts.js:952-956` excludes `docs/decisions/` by explicit design.

- item: Restore enforcement for the axiom pointer across the `next` surfaces, so stripping it cannot leave `npm test` green. The pin is the oracle, so test custody holds: whoever wires it does not author it. It CANNOT be red at baseline — the pointer is present on all 12 surfaces today, so this fixes an ABSENT GUARD rather than a live defect, and the arming proof must be by mutation, one surface at a time.
  status: done
  dispatched: tdd-guide subagent in the worktree at .kw/worktrees/issue-1007, write set templates/routing/required-blocks.js + scripts/test-route-reachability.js ONLY; writes to kaola-workflow/issue-1007/findings/red-1007-pointer.md
  result: kaola-workflow/issue-1007/findings/red-1007-pointer.md; shipped in **05c95d56**. It said plainly the pin CANNOT be red at baseline and measured the BLINDNESS instead: scratch copy of 66ac0442, pointer stripped from commands/workflow-next.md -> route-reachability `passed (368 assertions)`, exit 0; same tree, same mutation, pin installed -> RED naming the surface and the missing token, exit 1. `nx-first-principles` restored content-led over five tokens, each measured present exactly once on all 12 and absent from all 12 once the paragraph is removed. Mutation table, ONE surface at a time: each of the 6 tracked surfaces reds naming itself, and a tracked COMMAND additionally names its 2 transitive additive-edition renders — which is how it established the 6 edition renders are transitively protected rather than assumed so. Machinery mutants too: deleting the manifest block reds; narrowing `surface_type_tag` to 'command' reds both width asserts at "got 9"; a token list replaced by prose that survives the strip reds as unwitnessed. **It independently reproduced the literal-vs-derived lesson**: dropping the gitea forge (12 -> 8) fires the LITERAL while the DERIVED comparison goes green over four unchecked surfaces. Chain placement verified: test-route-reachability.js is a step in test:kaola-workflow:claude, not sharded, not hoisted, so `npm test` runs it. Five things it cannot witness are named honestly, including a pointer that is present but WRONG, and a pointer whose referent never existed on a repo initialised before the axiom layer. I re-ran the suite myself afterwards: 376 assertions, exit 0.

- item: Put the remedy shape to the user. The issue names three and picks none: correct the counts (a live reference in `docs/decisions/`, which this project treats as design of record rather than an archive), leave them as the record of what was decided then, or leave them and add a dated note that the set has since grown. It also suggests a durable fix for the class — state the reach as what the guard derives rather than as a number. Editing an ADR's factual claims retroactively is value-laden, which is exactly why this was filed rather than fixed in the run that found it.
  status: done
  dispatched: self
  result: superseded by the rulings item above; both questions answered in one exchange.

- item: Correct the three conventions.md sites and point them at the producer; append the dated note to D-645-01 AFTER the guard lands, so it describes what is true rather than what is planned; dock the CHANGELOG under [Unreleased].
  status: in-flight
  dispatched: self, inline in the worktree at .kw/worktrees/issue-1007, write set docs/ only — disjoint from the tdd-guide working in scripts/ and templates/. TWO of the three sites are DONE: the axiom-layer paragraph (was "twelve workflow-init CLAUDE.md-template surfaces", which was wrong twice over — the count AND the description, since 2 of the 14 are repo-root prose surfaces that are not workflow-init templates at all) now says fourteen, enumerates all three groups, points at the producer per the conventions.md:141 precedent, and states that root CLAUDE.md and README.md are HAND-MAINTAINED so --write does not reach them; and the :335 analytical passage now carries the corrected width `FORGES.length * (2 + runtimeEditionCount) + 2`, passes at 14->10 rather than 12->8, and gained the contrast that is the actual lesson — the derived term is derived on purpose, the trailing literal is literal for the OPPOSITE purpose, and a count is safe to derive only from something the mutation cannot move. Also corrected an understatement in the same paragraph: the canonical file is the source for the standing-default paragraphs too, not only the five axioms. ALL THREE SITES NOW DONE, shipped in **05c95d56**: the held pointer sentence was rewritten last, once the guard's shape was known — twelve, and it IS a required-blocks entry again, with the literal-12 width and the reason for it. D-645-01 carries a dated NOTE, not an edit. The note covers all three false claims, including one the tdd-guide surfaced that neither the issue nor I knew about: **§5 quotes a tighten-only clause ("never cite an axiom to justify skipping a typed gate, refusal, or barrier") that ships NOWHERE.** It was offered to me as a values call; I settled it as a FACT instead and recorded the evidence — `typed gate` and `barrier` return ZERO matches across templates/, commands/ and root CLAUDE.md, because that machinery was removed by later decisions and the refusal count is now zero, so restoring the clause would add a rule about mechanisms that no longer exist. Suites re-run by me: route-reachability 376/0, gen --check 0 (18 byte-match), contracts 0, testAxiomBlockByteIdentity 14 surfaces/0, test-generate-routing-surfaces 434/0.
