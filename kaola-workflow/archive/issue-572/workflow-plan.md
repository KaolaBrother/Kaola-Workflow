# Workflow Plan — issue-572

<!-- plan_hash: 337cbc414d8290aee789cf15873632b87653f4ff80903dbc9b0f481b3be60582 -->

## Meta
labels: documentation, enhancement, area:workflow-phases
validation_command: npm test

## Plan Notes

Issue #572 is a **stale-content + missing-enforcement fix**, not a behavior change. The
`workflow-init` template injects a `## Kaola-Workflow` block (the `KW-CLAUDE-TEMPLATE-START/END`
region) into a consumer's `CLAUDE.md` that still teaches the retired **6-phase-as-default** model
(`Phase 1/2/3/4`, "phase files", "current phase file"), contradicting **#538 (adaptive is the
unconditional default)**. Re-ground the block on the adaptive DAG-of-roles model and lock it with a
phase-number ban + cross-forge content parity + opencode-surface parity.

**Why this is ONE write node (#309 + #546 coupling), not a per-surface or content-vs-enforcement
split.** The deliverable couples two file groups that CANNOT move independently without leaving the
tree red mid-run:
1. The **6 markdown template surfaces** (3 `commands/workflow-init.md` + 3 init `SKILL.md`) carry the
   KW-CLAUDE-TEMPLATE region byte-identically *modulo two lines* — the forge-noun line (`GitHub`→
   `GitLab`/`Gitea`) and (for opencode only, generated) the runtime-noun line (`Claude Code agents`→
   `subagents`). Per #309 a single semantic prose change spanning N editions MUST be one node with a
   shared canonical spec, or parallel implementers diverge the prose (#254). So all 6 surfaces stay
   in one node.
2. The **contract validators** PIN durable-state needles on those surfaces via `assertConcept`
   (each pins `fast-summary.md` + `workflow-state.md` + `.cache/`): `scripts/validate-workflow-
   contracts.js` (claude chain, on `commands/workflow-init.md`; **byte-mirrored** into the codex
   plugin at `plugins/kaola-workflow/scripts/validate-workflow-contracts.js` — a #274 COMMON_SCRIPTS
   pair, so both copies move together), `scripts/validate-kaola-workflow-contracts.js` (codex chain,
   on the github/codex `SKILL.md`), and the gitlab/gitea forge contract validators (on their init
   `SKILL.md`). Rewriting the durable-state bullet (drop "phase files", drop `fast-summary.md` from
   the default, repoint to `workflow-plan.md` / `## Node Ledger` / `.cache/{node-id}.md`) BREAKS those
   needles unless the validators move in the SAME commit. So the needle updates are inseparable from
   the content rewrite — one node.

This is a genuine **tdd-guide** cycle (not `implementer`): the new **`Phase <n>` ban** assertion is a
meaningful failing test — it REDs against the current phase-laden template and GREENs only after the
rewrite. Write the ban (RED) → rewrite the 6 surfaces phase-free (GREEN) → update the moved needles.
(Contrast #570, which was pure prose with no writable failing test → `implementer`; here the
enforcement assertion is the driving RED, so tdd-guide.)

**Shared canonical spec for the 6 surfaces (#309 — mirror verbatim modulo the two noun lines).** The
re-grounded `## Kaola-Workflow` block must: (a) contain **no `Phase <n>` token** and no "phase
file/artifact" framing; (b) describe the adaptive model in one line — a task-shaped DAG of role nodes;
the planner authors and freezes `workflow-plan.md`; the executor runs it node-by-node via the
running-set scheduler; (c) point durable-state bullets at `workflow-plan.md` (its `## Node Ledger`) +
`.cache/{node-id}.md`, NOT "phase files"; (d) name roles by **function**, not phase — read/research →
`code-explorer`/`knowledge-lookup`; strategy/blueprint → `planner`/`code-architect`; execution →
`tdd-guide` (test-first) or `implementer` (refactors/scaffolding/config with no natural failing
test); gates → `code-reviewer`/`adversarial-verifier`; docs → `doc-updater`; (e) demote
`fast`/`full`/`fast-summary` to an explicit opt-in aside or omit them. Preserve the existing
non-template prose (`MANDATORY` sentinel, redirect blocks, the Codex-note OUTSIDE the markers) and the
exact `<!-- KW-CLAUDE-TEMPLATE-START/END -->` markers (the byte-parity extractor keys on them).
Forge-neutral plugin prose (#341): the SKILL/forge-command surfaces must not name a forge-specific CLI
binary or brand inside new prose.

**Enforcement placement (AC4/AC5).**
- **Phase-number ban + cross-forge content parity** → `scripts/validate-kaola-workflow-contracts.js`
  (codex chain): it already reads all 6 tracked surfaces (`initFiles`, lines ~459-466) and all three
  forge trees, so one phase-ban loop over `initFiles` + a cross-forge equivalence check (github vs
  gitlab vs gitea, modulo the single forge-noun line) covers AC4 + the cross-forge half of AC5 in one
  place. Keep the existing within-forge-pair byte checks (lines ~474-488).
- **opencode-surface parity** → `scripts/test-opencode-edition.js`. `.opencode/` is **fully
  gitignored** (`git check-ignore .opencode` → `.opencode`; 0 tracked files) — it is generated by
  `install-opencode.sh`/`sync-opencode-edition.js`, NOT committed and absent from a fresh checkout, so
  the four-chain contract validators MUST NOT read it. `test-opencode-edition.js` regenerates the
  opencode edition from canonical and is the correct home for the opencode phase-ban + parity (modulo
  the runtime-noun transform). It is opencode's own suite (additive, D-530-02 (existing)), kept green per the
  issue's validation note.
- **Needle updates** → all validators' init durable-state `assertConcept` only: drop the moved tokens
  (`fast-summary.md` if removed), ADD the adaptive tokens (`workflow-plan.md`, `## Node Ledger`,
  `.cache/{node-id}.md`). **Do NOT touch the unrelated fast-path `fast-summary.md` needles** on the
  classifier / `workflow-next` surfaces in the forge validators (gitea ~618/~636) — the fast path
  still exists as an opt-in.
- `scripts/sync-opencode-edition.js` is in the write set **precautionarily** (#546 edition-aggregator
  class): the existing transform already swaps `Claude Code agents`→`subagents` and strips the
  Codex-note, so a careful rewrite needs no transform change; declared so a discovered transform tweak
  never stalls the run (over-declaration is barrier-safe).

**Cross-edition obligation (#307/#400).** This diff touches the edition trees (3 plugin command +
3 plugin/forge validator surfaces) → all four `npm run test:kaola-workflow:{claude,codex,gitlab,gitea}`
chains green (run sequentially, the `validation_command`) + `simulate-workflow-walkthrough.js`, plus
`node scripts/test-opencode-edition.js` for the regenerated opencode edition. opencode is additive
(D-530-02 (existing)): no committed opencode file is in any write set.

**Decision record:** next free number is **D-572-01** (no D-572 record exists in `docs/decisions/`).

**Acceptance mapping.** AC1/AC2/AC3 → the 6-surface rewrite (phase-free; roles by function;
`implementer` named alongside `tdd-guide`; fast/full demoted). AC4 → phase-ban in
`validate-kaola-workflow-contracts.js` (+ opencode in `test-opencode-edition.js`). AC5 → cross-forge
parity in `validate-kaola-workflow-contracts.js` + opencode parity in `test-opencode-edition.js`. AC6
→ all six surfaces updated identically modulo nouns (one node); opencode regenerated/verified.

## Nodes

| id | role | depends_on | declared_write_set | cardinality | shape | model | non_tdd_reason |
| --- | --- | --- | --- | --- | --- | --- | --- |
| n1-init-template-adaptive | tdd-guide | — | commands/workflow-init.md, plugins/kaola-workflow/skills/kaola-workflow-init/SKILL.md, plugins/kaola-workflow-gitlab/commands/workflow-init.md, plugins/kaola-workflow-gitlab/skills/kaola-workflow-init/SKILL.md, plugins/kaola-workflow-gitea/commands/workflow-init.md, plugins/kaola-workflow-gitea/skills/kaola-workflow-init/SKILL.md, scripts/validate-kaola-workflow-contracts.js, scripts/validate-workflow-contracts.js, plugins/kaola-workflow/scripts/validate-workflow-contracts.js, plugins/kaola-workflow-gitlab/scripts/validate-kaola-workflow-gitlab-contracts.js, plugins/kaola-workflow-gitea/scripts/validate-kaola-workflow-gitea-contracts.js, scripts/test-opencode-edition.js, scripts/sync-opencode-edition.js | 13 | sequence | opus | — |
| n2-review | code-reviewer | n1-init-template-adaptive | — | 1 | sequence | opus | — |
| n3-finalize | finalize | n2-review | CHANGELOG.md, docs/decisions/D-572-01.md | 2 | sequence | — | — |

## Node Ledger

| id | status |
| --- | --- |
| n1-init-template-adaptive | complete |
| n2-review | complete |
| n3-finalize | complete |
## Required Agent Compliance

| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| tdd-guide (n1-init-template-adaptive) | subagent-invoked | evidence-binding: n1-init-template-adaptive 6672b3bc33ad | |
| code-reviewer | subagent-invoked | evidence-binding: n2-review 4501a5398c8b | |
| finalize (n3-finalize) | main-session-direct | evidence-binding: n3-finalize 78e4fe73631b | |
