# Adversarial verification — issue #1013

behavior_contract_version: 3
behavior_contract_hash: efb8f28ba39b96d87ad7986705629c1c133e71747fa6c30d9270e57003f3883c
resolved_profile_hash: 80e8b4b4df55c5379dfa5c62f8f341f6b173966018a4da09851d11a0cf75fe2d

## Context and candidate

- Worktree: `/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/issue-1013`
- Active evidence: `/Users/ylpromax5/Workspace/Kaola-Workflow/kaola-workflow/issue-1013/.cache`
- Candidate baseline: `5d12821db236ce0601d6804e9d78df31a9576f65`
- Candidate surface: the eight-path uncommitted diff in `CHANGELOG.md`, `README.md`, `docs/README.md`, `docs/architecture.md`, `docs/cursor-edition.md`, `install-cursor.sh`, `scripts/sync-cursor-edition.js`, and `scripts/test-cursor-edition.js`, plus the issue #1013 body/comments, mission list, `tdd-red.md`, `implementation.md`, `docs.md`, and `live-cursor.md`.

## Exact claim

generated Cursor agents derive raw unquoted medium/high Grok 4.6 frontmatter pins from canonical classes, reject unknown/absent tokens, Task commands omit per-call model, docs/additive boundary stay correct, and a fresh xhigh Cursor parent resolved implementer medium and code-reviewer high.

## Analytical result

`not_refuted` — execution succeeded. Confidence: high.

No concrete counterexample broke the supplied claim. The retained human-readable `live-cursor.md` is a summary rather than a raw stream artifact, but the named session's persistent Cursor chat store was still present and independently exposed the relevant runtime-created envelope records; this removed the evidentiary uncertainty rather than requiring trust in the summary.

## Counterexamples attempted and observed

### Candidate, callers, and exact generated bytes

- Read the complete issue body and all three comments, the mission list, full eight-path diff, and the four assigned evidence files before testing.
- `git diff --check` passed. The candidate does not touch canonical `agents/`, `commands/`, `templates/`, `CLAUDE.md`, or `AGENTS.md`.
- Exact scans of `.cursor`, `.cursor-gitlab`, and `.cursor-gitea` found all 42 generated agent files carrying exactly one raw line at column zero: standard roles use `model: grok-4.6[effort=medium]`; reasoning roles use `model: grok-4.6[effort=high]`. No generated agent has a quoted model line or a separate `effort:` / `reasoning_effort:` field.
- Read-only parity commands passed for github, gitlab, and gitea: each reported 14 agents, 3 commands, and 2 hook files in parity with canonical.
- Scans of every generated command and hook across all three trees found no `model` assignment and no Grok model literal. Task-card counts and role names are checked against canonical command cards by the suite.
- Scans of canonical consumer prompt surfaces found no `grok-4.6` or `cursor-grok-4.6` literal. The candidate diff leaves `package.json`, `install.sh`, `scripts/edition-sync.js`, and chain-selection machinery untouched; top-level `npm test` still names only the four forge chains, while Cursor remains in the separately invoked additive-editions script.

### Canonical-class boundaries and invalid state

- Direct `renderAgent` probes covered `sonnet`, `standard`, `opus`, and `reasoning`; all rendered the expected exact medium/high raw line.
- Case and surrounding-whitespace probes (`SONNET`, `REASONING`, and padded tokens) normalized to their correct known class.
- Absent, empty, `inherit`, arbitrary unknown, prefix-like (`sonnet-extra`), quoted-known (`"sonnet"`), and homoglyph (`сonnet`, Cyrillic first character) inputs all threw the agent-specific canonical-token error rather than rendering a fallback.
- An isolated copy of the candidate ran `node scripts/test-cursor-edition.js`: exit 0, `550 assertions`. The copy started without generated trees, so all suite mutations and its planted drift probe remained outside the issue worktree; the temporary copy was sent to Trash afterward.
- A synthetic current-shape placeholder was stripped and a single-quoted `model=` instruction was rejected/rewritten. A non-current `model = "..."` spacing variant can pass production `assertNoModelDispatchResidue`, but the changed suite's broader `model\s*=\s*["']` assertion rejects such generated output. No current canonical or generated command contains that form, so this did not produce an in-scope candidate failure.

### Live Cursor attribution and alternative explanations

- The installed isolated-project bytes at `/tmp/kw-cursor-1013.UBJHl1/.cursor/agents/` still carry medium for `implementer` and high for `code-reviewer`. Their mtimes precede the fresh session.
- Session `9c0b65bf-b333-43af-861e-1ddaa15cdb9f` remains in Cursor's persistent chat store at `/Users/ylpromax5/.cursor/chats/b5703ec72288dceb12377d61a8bbff58/9c0b65bf-b333-43af-861e-1ddaa15cdb9f/store.db`; `meta.json` binds its cwd to `/private/tmp/kw-cursor-1013.UBJHl1` and its creation to the recorded cold run.
- The parent record identifies `cursor-grok-4.6-xhigh`. Its two Task-call arguments name `implementer` and `code-reviewer` and contain no `model` argument.
- Blob `8e65169fb150f26f535144a7c628f6d7637c0cf21c7ab7421772a67a8e06cd89` binds the standard custom-agent envelope to `implementer`, `cursor-grok-4.6-medium`, launch id `d21cadf4-5643-4047-b683-83ebb2fa405f`, and the completed `STANDARD_CHILD` result.
- Blob `63069f2d4b700638ceb61b379035bab2578b2770917a1981e90cd098f37c9b2f` binds the reasoning custom-agent envelope to `code-reviewer`, `cursor-grok-4.6-high`, launch id `d5688f07-30fe-4bc6-9f54-0eca76b1edff`, and the completed `REASONING_CHILD` result.
- Same-name global agents existed, but both global files carried `model: inherit`; from an xhigh parent they cannot explain the distinct medium/high envelope values. The Task arguments also rule out a per-call override. The generic child display identity (`Cursor Grok 4.6`) and empty nested allowlists are therefore non-oracles, not counterexamples to the runtime-created envelope resolution.
- No concrete evidence showed that the envelope's resolved model field was merely copied but ignored by execution. The claim is specifically resolution to the two effort slugs, and the runtime-created custom-agent envelope is the direct carrier for that resolution.

## Findings

No blocking findings.

verdict: pass
findings_blocking: 0
