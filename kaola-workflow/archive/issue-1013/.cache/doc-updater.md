# Issue #1013 — finalize documentation docking

## Verdict

`DOCKED` — every user-visible Cursor behavior, setup statement, architecture
pointer, and validation claim in the eight-path candidate is traceable to the
current source, generated output, issue comments, or retained live evidence.

The pre-existing receipt is green for all four chains, but its code-tree hash is
stale after the README scope clarification below. The receipt records
`39ef0d1f171607439b15930977d7c48f60507cc44c2e867d96c3090f9b002389`; the
current candidate computes
`935fd29a2d0f9dbe2f1d4aaa33cc6066840cac47c2ead28a10a066abb69e6ed8`.
The orchestrator must rerun the four-chain producer before finalization. This
is validation freshness, not an unresolved documentation gap.

## Candidate and changed files reviewed

Working directory:
`/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/issue-1013`

The complete candidate remains these eight tracked paths:

- `scripts/sync-cursor-edition.js` — production generator now derives strict
  canonical class pins and emits the raw model line.
- `scripts/test-cursor-edition.js` — suite and declaration guards cover exact
  raw lines, model-free Task cards, and fail-closed classes.
- `install-cursor.sh` — installer comment now describes generated per-tier
  frontmatter and unchanged absence of user-specific config seeding.
- `README.md`, `docs/README.md`, `docs/cursor-edition.md`,
  `docs/architecture.md`, `CHANGELOG.md` — documentation surfaces.

## Documentation reconciliation

- `README.md` already documented the Cursor pins, model-free `Task`, additive
  boundary, install commands, and runtime limits. Final review found two older
  unqualified paragraphs that described inherit-only frontmatter and
  `Agent(model=...)` as runtime-wide. I qualified both to Claude Code and
  changed “other runtimes retain their existing model routing” to
  “edition-specific model routing”. This prevents the new Cursor behavior from
  being contradicted by the general role section.
- `docs/README.md` documents the Cursor edition index entry with canonical
  class-derived, unquoted medium/high frontmatter pins, model-free `Task`,
  resume injection, and runtime limits.
- `docs/cursor-edition.md` is the detailed source of truth: it names both raw
  unquoted pins and all four canonical aliases, says unknown classes fail
  closed, says `Task` omits `model`, names the declared resume injection, and
  records cold-start, one-family picker clamp, resume, and cloud/local product
  limits. It explicitly says no config seeding, inline override, workaround,
  or second pin path is added.
- `docs/architecture.md` keeps the runtime capability matrix factual: Cursor
  model/tier handling is `partial`, pointing to the detailed tier-pin and
  runtime-limit section rather than duplicating mechanism prose.
- `CHANGELOG.md` has the `[Unreleased]` #1013 entry with raw unquoted model
  lines, model-free Task cards, no separate effort field, product limits, and
  no workaround/second pin path; the historical initial-Cursor entry explains
  the transition without presenting inherit-only behavior as current.

## Deliberately unchanged documentation surfaces

- `docs/api.md` — no exported API, CLI signature, option, envelope schema, or
  config key changed. Its additive-edition sync row remains accurate, and the
  Cursor generator’s model-class rendering is an edition behavior documented in
  `docs/cursor-edition.md`, not a new public script contract.
- `.env.example` and environment/setup documentation — no environment
  variable, installer flag, install path, hook path, or config-seeding behavior
  changed. The installer comment change describes generated bytes only; it
  does not add a setup knob.
- `CLAUDE.md`, `AGENTS.md`, `agents/`, `commands/`, and `templates/` — no
  vendor model literal was added to canonical consumer prompt surfaces.

## Evidence traced

- `scripts/sync-cursor-edition.js:113-140` maps `sonnet`/`standard` to
  `grok-4.6[effort=medium]`, `opus`/`reasoning` to
  `grok-4.6[effort=high]`, rejects unknown/absent classes, and emits the model
  line directly rather than through `yamlScalar`.
- `scripts/sync-cursor-edition.js:163-178` states model-free `Task` guidance;
  `scripts/test-cursor-edition.js:340-375` and `:495-529` guard exact raw
  lines, no separate effort fields, no per-call model assignments, and the
  declared runtime entries.
- Generated output at the shared tree root has 14 agents and 3 commands in
  each of `.cursor`, `.cursor-gitlab`, and `.cursor-gitea`; each tree has 7
  medium and 7 high raw pins, zero quoted model lines, zero separate effort
  fields, and zero command `model=` assignments.
- The retained `.cache/live-cursor-stream.ndjson` has 25/25 JSON-parseable
  events. Its two started/completed envelope pairs resolve `implementer` to
  `cursor-grok-4.6-medium` with `STANDARD_CHILD_2` and `code-reviewer` to
  `cursor-grok-4.6-high` with `REASONING_CHILD_2`, under a distinct xhigh
  parent. The record proves runtime resolution only; it does not treat child
  self-introduction or nested allowlists as effort oracles, and the docs make
  no such claim.
- The canonical prompt scan found no `grok-4.6` or `cursor-grok-4.6` literal
  in `CLAUDE.md`, `AGENTS.md`, `agents/`, `commands/`, or `templates/`.

## Commands run

From the issue worktree:

- `node scripts/test-cursor-edition.js` — PASS, 550 assertions; all three
  present generated trees in parity.
- `node scripts/generate-routing-surfaces.js --check` — PASS, all 18 surfaces
  byte-match their skeletons.
- `node scripts/sync-cursor-edition.js --forge=github --check` — PASS, 14
  agents + 3 commands + 2 hooks.
- `node scripts/sync-cursor-edition.js --forge=gitlab --check` — PASS, 14
  agents + 3 commands + 2 hooks.
- `node scripts/sync-cursor-edition.js --forge=gitea --check` — PASS, 14
  agents + 3 commands + 2 hooks.
- `git diff --check` — PASS.
- Canonical vendor-literal scan — PASS, no matches.
- Retained-stream JSON parse and envelope extraction — PASS, 25/25 events,
  two expected starts, two expected completions, and both expected markers.
- Chain receipt inspection — all four recorded exit 0 with no accepted red;
  code-tree freshness mismatch is recorded above for the required rerun.

## Remaining documentation risks

- The fresh-session, family-clamp, resume, and cloud/local statements remain
  typed product limits. The current retained live stream proves both standard
  and reasoning frontmatter resolutions in one fresh path; it does not turn
  the documented limits into universal guarantees.
- The architecture matrix points to the stable `Tiered frontmatter pins` /
  `runtime limits` section. If that heading changes later, update the pointer
  instead of duplicating the mechanism in the matrix.
- No documentation blocker remains. The only required follow-up before the
  finalize transaction is regeneration of the stale four-chain receipt after
  this tracked README clarification.

Result landed at:
`/Users/ylpromax5/Workspace/Kaola-Workflow/kaola-workflow/issue-1013/.cache/doc-updater.md`
