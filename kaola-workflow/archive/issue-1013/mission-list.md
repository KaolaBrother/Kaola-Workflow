# Finish #1013 — pin Cursor subagents to Grok 4.6 medium/high through unquoted generated frontmatter

- item: Establish the current Cursor generator and suite premise, then add acceptance pins that fail on HEAD for missing or quoted tiered model frontmatter while command cards remain model-free.
  status: done
  dispatched: `tdd-guide` owns `scripts/test-cursor-edition.js` in the issue worktree; it will derive the issue acceptance pins, prove RED on the claimed baseline, and record exact evidence at `kaola-workflow/issue-1013/.cache/tdd-red.md` in the main root.
  result: `scripts/test-cursor-edition.js` now derives both rosters from canonical model classes, pins exact unquoted medium/high frontmatter, rejects separate effort fields and per-call model dispatch, and fail-closes a synthetic unknown class; `kaola-workflow/issue-1013/.cache/tdd-red.md` records baseline `5d12821d`, exit 1, and 29 intended failures with 521 passes after all three generated trees first passed drift parity.

- item: Make the Cursor generator derive each unquoted Grok 4.6 effort pin from the canonical agent class, fail closed for unknown classes, and regenerate the additive edition with truthful dispatch guidance.
  status: done
  dispatched: self verified the claimed inherit-only premise; `implementer` owns `scripts/sync-cursor-edition.js` plus the now-false model-routing comment in `install-cursor.sh`, and will land strict class-token derivation, raw model emission, truthful model-free dispatch guidance, GREEN evidence, and its changed-path note at `kaola-workflow/issue-1013/.cache/implementation.md`.
  result: `scripts/sync-cursor-edition.js` now maps sonnet/standard to raw Grok 4.6 medium and opus/reasoning to raw high, rejects absent/unknown classes, and keeps generated `Task` cards model-free; `install-cursor.sh` has truthful comment prose, all three present trees are in parity, and `kaola-workflow/issue-1013/.cache/implementation.md` records the suite moving from 29 failures/521 passes to 550/550 green.

- item: Dock the user-visible Cursor tier binding and its documented runtime limits without leaking vendor literals into canonical consumer prompts.
  status: done
  dispatched: `doc-updater` owns the Cursor documentation surfaces in the issue worktree and will record its changed-path and verification decisions at `kaola-workflow/issue-1013/.cache/docs.md` in the main root.
  result: `README.md`, `docs/README.md`, `docs/cursor-edition.md`, `docs/architecture.md`, and `CHANGELOG.md` now describe canonical-class-derived raw medium/high pins, model-free `Task`, and the cold-start/picker/resume/cloud limits; `kaola-workflow/issue-1013/.cache/docs.md` records clean focused scans and `docs/api.md`/environment no-impact decisions.

- item: Produce cold-session Cursor close evidence for one standard and one reasoning role, or record the issue-defined typed family-clamp deferral with the child allowlist evidence.
  status: done
  dispatched: self — probe the installed Cursor CLI read-only, then use an isolated generated Cursor home after production lands to run fresh no-override role dispatches; exact transcripts and the pass-or-typed-deferral verdict will land at `kaola-workflow/issue-1013/.cache/live-cursor.md`.
  result: `kaola-workflow/issue-1013/.cache/live-cursor.md` records two fresh Cursor CLI 2026.08.11 sessions whose xhigh parent model-free Tasks resolved `implementer` to medium and `code-reviewer` to high; after review found the first summary lacked durable outer events, `live-cursor-stream.ndjson` retained all 25 raw rerun events and parsed cleanly with both successful envelope pairs. No family-clamp deferral was needed; forge evidence is https://github.com/KaolaBrother/Kaola-Workflow/issues/1013#issuecomment-5379788311 and the review fix is https://github.com/KaolaBrother/Kaola-Workflow/issues/1013#issuecomment-5379813811.

- item: Independently review the finished diff, mutation-prove the new guard, and run the Cursor suite plus required workflow validation before finalization.
  status: done
  dispatched: `code-reviewer` and `adversarial-verifier` independently inspect the finished issue diff and live evidence, returning `kaola-workflow/issue-1013/.cache/review.md` and `kaola-workflow/issue-1013/.cache/adversarial.md`; `tdd-guide` mutation-proves the new Cursor declaration/raw-line guard in an isolated copy and records `kaola-workflow/issue-1013/.cache/mutation.md`; self owns executable validation and the full walkthrough evidence.
  result: Mutation legs for declaration deletion, quoted pins, unknown fallback, and Task model injection all failed on their intended guards; adversarial verification returned `not_refuted`; independent review's initial durable-stream gap was repaired with a 25-event raw rerun and re-review passed with no findings. `validation.md` records Cursor 550/550, routing 18/18, full walkthrough 186/186 with 2,173 spawns, clean diff, and canonical vendor-literal absence.

- item: Complete the finalize-time documentation checklist and dock every changed surface and repaired runtime-evidence claim against issue #1013 before the archive transaction.
  status: done
  dispatched: `doc-updater` at the standard-tier `gpt-5.6-luna` / `max` pair reviews the final eight-file diff, root `CLAUDE.md` checklist, issue/comments, green four-chain receipt, and repaired raw runtime evidence; it owns docs-only corrections and records `kaola-workflow/issue-1013/.cache/doc-updater.md`.
  result: Finalize-time `doc-updater` returned `DOCKED`, repaired two general README paragraphs that could still be read as runtime-wide inherit behavior, and confirmed every changed surface plus API/environment no-impact decisions in `doc-updater.md`; the resulting receipt-staleness finding triggered a fresh all-four chain run before finalization.
