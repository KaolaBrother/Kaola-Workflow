# Resolve the build-error-resolver / adversarial-verifier tier divergence UPWARD on every runtime, and delete install.sh's dead model fallback (#935)

Run posture: worktree at `/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/issue-935` on branch
`workflow/issue-935`, based on `254e667f`. **All code edits happen in the worktree**; this folder
(`kaola-workflow/issue-935/`) lives in main's checkout and holds the run record only. Subagent reports
land in `kaola-workflow/issue-935/.cache/` — note that folder exists on both sides of the split; write
to main's copy.

`CODEX_PINNED_*_ROLES` are already correct and DO NOT CHANGE — every other surface moves to agree with
them. Owner ruling: upward, re-affirmed on capability grounds; do not resolve downward, do not reopen
on cost. Stage B (Luna/`max`) is dropped. Stage C is refuted — its guard already exists and is armed.

- item: Flip both roles to the reasoning tier at the source — generator adapter at `scripts/generate-reviewer-profiles.js:185` from `claude-standard` to `claude-reasoning` then `--write` (never hand-edit the rendered `agents/adversarial-verifier.md`; measured to change 1 file / 2 lines and leave `behavior_contract_hash` and the 9 Codex TOMLs untouched), `agents/build-error-resolver.md:5` sonnet→opus by hand (vendored, but `validate-vendored-agents.js:69-87` only checks `source-sha256` is well-formed, never that it matches this file — measured safe), `DEFAULT_AGENT_MODELS` in all four byte-identical resolver copies (today `c22f3c81`, must still hash equal after), and rewrite the false-after-this comment at `kaola-workflow-resolve-agent-model.js:27-31` stating the real reason — NOT the dead wait-budget wording — leaving the `metric-optimizer` comment at `:32-35` alone. This is the root item; items 2 and 5 read its result.
  status: done
  dispatched: implementer subagent, working in the worktree `.kw/worktrees/issue-935`; edits land as tracked changes on branch `workflow/issue-935`, and its report lands at `kaola-workflow/issue-935/.cache/item1-tier-flip.md` in MAIN's checkout.
  result: `kaola-workflow/issue-935/.cache/item1-tier-flip.md`. All four edits landed across 7 files. Regeneration matched prediction exactly — 1 file, 2 lines, `behavior_contract_hash` unchanged, 9 Codex TOMLs byte-unchanged. Four resolver copies share ONE new hash `d5ece6342e6fcbdbf1310be90faaa18bd59bb1bb5d48979a840e4650c2cb7adc` (was `c22f3c81`). Green: reviewer `--check`, `validate-vendored-agents`, walkthrough 209/209 at FULL scope, routing surfaces 18/18, 4 contract validators. TWO test-custody reds by design — `test-agent-model-resolver.js:68` (aborts on the FIRST assert, not `:72` as predicted; text is `build-error-resolver Claude dispatch tier changed`) and **`test-install-model-rendering.js:2945`, which was NOT on the work list** → new item 9. Also surfaced: a pre-existing false wait-budget claim at `kaola-workflow-resolve-agent-model.js:18-19`, nine lines above the rewrite → new item 10.

- item: Regenerate the opencode edition rather than editing it — `node scripts/sync-opencode-edition.js --write-config` (`--write` only seeds when absent and will NOT fix a stale config), confirm `opencode.json` carries all 7 reasoning roles instead of 5, and hand-update the prose count at `docs/opencode-edition.md:122` ("five" → "seven") which the generator does not own. Staleness reds at `sync-opencode-edition.js:864-866`, so `--check` is the oracle.
  status: done
  dispatched: implementer subagent in the worktree `.kw/worktrees/issue-935`; report lands at `kaola-workflow/issue-935/.cache/item2-opencode.md` in MAIN's checkout.
  result: `kaola-workflow/issue-935/.cache/item2-opencode.md`. Generator self-corrected 5→7 as predicted; two files changed, `opencode.json` (regenerated, never hand-edited) and `docs/opencode-edition.md`. Oracle went red→green: `--check` `opencode.json — stale` (1) → `14 agent(s) + 3 command(s) + 1 plugin(s) in parity` (0); `test-opencode-edition.js` 1 → 0 (516 assertions); `npm run test:kaola-workflow:editions` 0. **The brief named one doc defect and there were two** — `docs/opencode-edition.md:122` said "five" AND `:123` enumerated the five roles by name, so fixing the numeral alone would have shipped a correct count above a wrong list. THREE findings, none fixed, all out of scope → follow-ups: (a) `sync-opencode-edition.js:870` prints an unconditional remediation footer advising `--write`, which CANNOT fix a stale `opencode.json` (`:661-666` preserves the existing file and tells you to use `--write-config`) — the per-mismatch reason at `:865` is correct, only the footer misleads; (b) the edition suite MATERIALIZES `.opencode-gitlab`/`.opencode-gitea` as it runs, so a first run reports them `SKIPPED — absent` and a second reports `in parity` — the banner reads as coverage when a fresh clone has none; (c) `install-opencode.sh` PRESERVES an existing user `opencode.json`, so this machine's installed copy keeps the stale 5-role scaffold unless `--adopt-config` is passed — this bites item 8's reinstall step. Archived run artifacts under `kaola-workflow/archive/issue-927/` still say "five" and were deliberately left; rewriting a historical record would falsify the archive.

- item: Correct the tier column at `README.md:152` and `:156` from `standard` to `reasoning`. This is load-bearing, not cosmetic — `docs/conventions.md:215` records the table as NOT machine-checked so nothing will catch an error, and it is the ONLY role→tier enumeration on any surface a Codex orchestrator can actually read, which is what makes the Codex half of this fix reach dispatch at all.
  status: done
  dispatched: self — a two-line edit, cheaper to make than to hand off; lands in the worktree `.kw/worktrees/issue-935/README.md`.
  result: `README.md:152` and `:156` now read `reasoning`. **The two-line estimate was wrong** — grepping both role names across the file found a FOURTH stale enumeration nobody had anticipated: the badge-visibility blockquote at `:209-213` sorts all 14 roles into "Sonnet-dispatched" and "Opus-dispatched" lists, and both target roles sat in the Sonnet list. Moved them; the lists are now 7/7. Checked and correct as-is, no edit needed: `:179-183`, which binds the tier table to Codex dispatch, still accurately states `standard`→`sol/medium` and `reasoning`→`sol/xhigh` because the pairs are unchanged. Remaining hits at `:52`, `:186`, `:563`, `:608`, `:797`, `:801`, `:806` are role descriptions with no tier claim.

- item: Delete `install.sh`'s `default_agent_model()` (`:506-518`) and its sole call site (`:543`), leaving `extract_agent_model` the only authority — it is a fourth tier table covering 12 of 14 roles and already wrong on `code-architect`/`code-reviewer`/`security-reviewer` today, independent of this issue. Prove the deletion rather than asserting it: drop `model:` from one vendored and one local `agents/*.md` in a scratch mirror and confirm `test-agent-model-resolver.js:87-94` reds naming each role (do NOT look for this guard in `validate-vendored-agents.js:92-101` — measured, it covers only the 8 `localAgents`), then confirm a full install resolves all 14 models with the function gone. If the proof fails, fall back to correcting its three stale values plus the two missing roles and say why deletion was unavailable. Independent of item 1.
  status: done
  dispatched: implementer subagent, working in the worktree `.kw/worktrees/issue-935` (edits `install.sh` only, disjoint from item 1); report lands at `kaola-workflow/issue-935/.cache/item4-install-fallback.md` in MAIN's checkout.
  result: `kaola-workflow/issue-935/.cache/item4-install-fallback.md`. DELETED, −17/+2; `extract_agent_model` is now the only authority and `inherit` handling is untouched. All three mutation legs ran off a clean `git archive HEAD` mirror: baseline 0, local control (`investigator`) 1, decisive vendored (`build-error-resolver`) 1 at `test-agent-model-resolver.js:91`. Extra control: on the SAME vendored mutant `validate-vendored-agents.js` exits 0 — proving it is blind and the resolver test is the sole armed guard. Step 3 shown by execution, not reading: real installs across all three forges before/after (6× exit 0), installed trees byte-identical but for `.claude.json` timestamps, and all 14 roles resolved through the shipping function by sourcing `install.sh` — before-column shows `code-architect`/`code-reviewer`/`security-reviewer` at `opus`, which the fallback could not produce, so it demonstrably never fired. `bash -n` 0. 12 install-adjacent checks green before AND after.

- item: Delete the `CLASS_DIVERGENCE` object and the branch it feeds — `scripts/test-agent-model-resolver.js:42-56` and the `if (declared)` arm at `:65-74` — leaving the `else` assert as one unconditional rule for all 14 roles. It must be REMOVED, not emptied: `:72-73` exists precisely to red on a stale declaration, and this change makes the tables converge. TEST CUSTODY — this is a test artifact, so whoever did item 1 must not do this; route it to a separate author who must show the surviving unconditional assert still reds when a role is moved in one table only.
  status: done
  dispatched: tdd-guide subagent (test custody — did NOT make the production change), in the worktree `.kw/worktrees/issue-935`; carries item 9 in the same dispatch since both are test artifacts made stale by the same flip. Report lands at `kaola-workflow/issue-935/.cache/item5-9-test-custody.md` in MAIN's checkout.
  result: `kaola-workflow/issue-935/.cache/item5-9-test-custody.md`. `CLASS_DIVERGENCE` REMOVED, not emptied — object, comment block, key-validation loop, the whole `if (declared)` arm and the orphaned `codexClass` are gone, and the surviving assertion's MESSAGE was also rewritten since it named the deleted mechanism. Mutation-proven armed in a scratch mirror (reverted by re-copying one file, never `git checkout --`): baseline 0; M1 moving `tdd-guide` between the two Codex lists with the map untouched → 1 naming the role; M2 changing `implementer`'s map value with the lists untouched → 1. Neither one-table-only mutation passed. Premise re-checked before editing: the divergence is genuinely stale, not a wrong production change.

- item: Show, don't assert, that no new refusal path appears — the reasoning floor still gates only `synthesizer` (`kaola-workflow-resolve-agent-model.js:46`; `isReasoningClass`'s one caller `enforceReasoningFloor` short-circuits every non-floor role at `:245`), and no wait budget, timeout, concurrency cap or dispatch gate keys on tier anywhere in live code. Read-only; produce the evidence, not a claim.
  status: done
  dispatched: investigator subagent, read-only against the worktree `.kw/worktrees/issue-935`; evidence lands at `kaola-workflow/issue-935/.cache/item6-no-refusal-path.md` in MAIN's checkout.
  result: `kaola-workflow/issue-935/.cache/item6-no-refusal-path.md`. **SUPPORTED as "nothing GATES on tier", REFUTED as "nothing KEYS on tier"** — the issue's A7 wording is wrong and must be corrected in item 7's CHANGELOG prose. Three things legitimately key on tier and change behavior, all intended: `sync-opencode-edition.js:146` (5→7 reasoning roles), `install.sh:526-549` rendering the finalize dispatch block `sonnet`→`opus`, and the Codex skeleton's tier→`reasoning_effort` mapping. Real A/B (HEAD `c22f3c81` vs worktree `d5ece634`): 56 `enforceReasoningFloor` calls → only `synthesizer@sonnet` refuses, in BOTH legs; widened to 11 tier tokens, both target roles return ok on every input. Positive control armed AND a mutation control proves the ok-column is not vacuous. Hook is fail-open, four `exit 0`s, no `exit 1` in the file. **Biggest finding, out of scope to fix:** `--enforce-floor` has ZERO production consumers — the reasoning-floor refusal is not live for ANY role including `synthesizer`, so "no new refusal path" holds partly because there is no live refusal path at all. File as a follow-up at finalize, do not fix here (scope fence puts `REASONING_FLOOR_ROLES` out of scope). Method note carried forward: `git grep -E` lacks `\b`/`\s` on this box and produced a false negative that would have confirmed the claim for the wrong reason — negatives were re-run with `-P`.

- item: Update the user-visible record — `CHANGELOG.md` under `[Unreleased]` (no such section exists today; prepare consumes it, so add one), plus any architecture/API doc the tier change reaches. State the intended behavioral consequence plainly: Claude dispatches both roles at reasoning, Codex moves sol/medium → sol/xhigh once the README table lands, opencode goes 5→7, Kimi unchanged (no tier axis). Write this BEFORE the chain receipt run — editing docs afterwards stales the receipt.
  status: done
  dispatched: self — the entry has to summarize all eight closed items and I hold that context; briefing an agent on it would cost more than writing it. Lands in the worktree at `.kw/worktrees/issue-935/CHANGELOG.md`.
  result: `CHANGELOG.md` gains an `[Unreleased]` section (none existed — prepare consumes it) with `### Changed` for the tier move and `### Removed` for the install.sh fallback, both naming the per-runtime consequence and the cost increase explicitly. **No other doc needed editing, checked rather than assumed**: `docs/api.md:1533-1538` and `docs/architecture.md:345-348` describe the tier→pair MAPPING, which is unchanged, not role→tier membership; `docs/opencode-edition.md:122` already says "seven" via item 2. Every remaining `sonnet`/`standard` claim about these two roles lives in dated `docs/investigations/*` records — deliberately left, on the same principle as the archive: rewriting a historical design record would falsify it. A7's wording was corrected per item 6's refutation — the entry claims nothing about tier not being read, only about what dispatches where.

- item: DISCOVERED MID-RUN by item 1, not on the original issue's list — `scripts/test-install-model-rendering.js` pins the finalize routed-fix dispatch block to render `build-error-resolver` as `sonnet`, and is now red at `:2945` with stale expectations also at `:2936`, `:2946`, `:3029`, `:3033`. This red is GOOD evidence: it proves the tier flip reaches the rendered finalize command instead of sitting inert. Update the pins to `opus`. TEST CUSTODY — a test artifact, so it goes to the same test author as item 5 and not to whoever made the production change; a test is repaired WITH its mechanism, never rewritten merely to keep passing, so confirm the rendered value genuinely should now be `opus` before touching the expectation.
  status: done
  dispatched: same tdd-guide dispatch as item 5; report lands at `kaola-workflow/issue-935/.cache/item5-9-test-custody.md` in MAIN's checkout.
  result: `kaola-workflow/issue-935/.cache/item5-9-test-custody.md`. Render path traced end-to-end BEFORE editing any expectation — `commands/kaola-workflow-finalize.md:96 {BUILD_ERROR_RESOLVER_MODEL}` → `install.sh:545` → `resolve_agent_model_for_install` → `agents/<role>.md` frontmatter → `opus`, so the new value is verified, not assumed. All 14 `sonnet` hits enumerated, 4 stale, fixed at `:2936` (comment), `:2946` (assertion), `:2947` (message), `:3029`/`:3033` (`EXPECTED_ROLE_MODELS`); the other 10 correctly left. **Judgement call I reviewed by hand and ACCEPT**: `EXPECTED_ROLE_MODELS` carries an explicit "do not fix a failure here by editing this table to match the resolver". They edited it anyway and were right to — the rule forbids silencing a failure, not recording a ruling. They kept the prohibition intact, added a paragraph naming the two moved entries and the ruling behind them ("a decision, never a green-suite convenience"), and corrected the table's provenance sentence, which claimed the pre-axis-removal resolution and would otherwise have gone false. Sweep found `EXPECTED_ROLE_MODELS` is the ONLY role→tier table on the whole test surface; 9 implicated suites run, all 0. **Pre-existing gap I found reviewing this, out of scope → follow-up:** that table has 13 entries, not 14 — `investigator` appears nowhere in `test-install-model-rendering.js`. Unaffected by this flip (standard tier either way), but it means one role is unpinned.

- item: DISCOVERED MID-RUN by item 1 — `scripts/kaola-workflow-resolve-agent-model.js:18-19` still carries a pre-existing comment claiming the map preserves each role's "declarative reasoning/wait-budget class", nine lines above the comment item 1 just rewrote. That is the same retired mechanism item 1 was forbidden to re-introduce, so leaving it contradicts the fix. Rewrite it to describe what the map actually does; keep it byte-identical across all four resolver copies, which must still share one hash.
  status: done
  dispatched: self — a comment fix in the file item 1 had just finished with; serializing it inline was cheaper than handing off a 4-copy byte-identity constraint.
  result: Rewritten in the canonical copy and propagated by `cp` to the three plugin copies; all four now share ONE hash `49e8c1fc646f` (was `d5ece634` after item 1). The comment carried a SECOND stale claim beyond the wait-budget one the item named — "Codex named profiles inherit runtime strength from the parent session; this map never selects a child pair" is also false post-#925, since the Codex contract now sets model and effort explicitly per spawn from the role's tier. Both are gone; the replacement states that the tier is what every runtime reads (Codex per-spawn effort, opencode's derived reasoning list) and that a change here reaches all of them.

- item: Full verification — reviewer-profile `--check`, `validate-vendored-agents.js`, `test-agent-model-resolver.js` mutation-proven armed after the CLASS_DIVERGENCE deletion, equal hashes across the four resolver copies, `sync-opencode-edition.js --check`, the walkthrough at FULL scope (not the rotating 1/12 shard), the opencode and Kimi suites, and all four chains with `--project issue-935` (every edition is touched, so a claude-only receipt does not qualify; use `KAOLA_RUN_CHAINS_CONCURRENCY=serial`). Then after reinstall, read the EFFECTIVE model and effort back from a live spawn per runtime — do not infer them from the authored files.
  status: done
  dispatched: self — verification is the deciding, not production. Running in the worktree `.kw/worktrees/issue-935`; chain receipt lands via `run-chains.js --project issue-935`, suite output recorded inline in this item's result. CHANGELOG landed BEFORE this runs, so the receipt is not stale by construction.
  result: ALL GREEN. Six targeted validators exit 0 (reviewer `--check`, `validate-vendored-agents` 14 agents, `test-agent-model-resolver`, `test-install-model-rendering`, `sync-opencode-edition --check` in parity, routing surfaces 18/18). Byte-identity holds: ONE distinct hash across the four resolver copies AND across the four `adaptive-schema` copies (the latter correctly unchanged, since `CODEX_PINNED_*` did not move). Diff is exactly 14 files. Editions: opencode 516, kimi 507, edition-sync 30, all 0. Walkthrough 209/209 at FULL scope, shard 1/1. **Four-chain receipt GREEN** at `kaola-workflow/issue-935/.cache/chain-receipt.json` in MAIN's checkout (correct — NOT in the worktree): claude/codex/gitlab/gitea all `exitCode: 0`, none waived, none accepted-red; `scope.decision: all-four` via `reason: edition_coupling`, `changedFileCount: 16`, `base: 660fec1d`, `headSha: 254e667f`, `workTreeHash: 7b95bc86`. **MY ERROR, caught and corrected:** the first full-scope walkthrough ran from MAIN's checkout because cwd resets between calls, so it exercised the PRE-change tree and proved nothing — confirmed by main still reading `'build-error-resolver': 'sonnet'`, then re-run inside the worktree with `pwd` and an `opus` grep as preconditions. NOTE for release: `headSha` is the re-scope commit and the tree is uncommitted, so this receipt does NOT satisfy the pre-tag gate — a release must re-run chains at the release commit.

- item: SPLIT OUT of item 8 — A10's live readback cannot run from the worktree, because reinstalling from an unmerged branch would install code that is not yet on main. After the sink lands, reinstall all four runtimes (`./install-all.sh --global --yes`) and read the EFFECTIVE model and effort back from a live spawn per runtime rather than inferring them from the authored files: Claude should resolve both roles to the reasoning tier, and a Codex spawn should report `gpt-5.6-sol` / `xhigh` for them instead of `medium`. KNOWN TRAP, measured during item 2: `install-opencode.sh` PRESERVES an existing user `opencode.json`, so this machine's installed copy will keep the stale 5-role scaffold unless `--adopt-config` is passed — check it explicitly rather than assuming the reinstall carried it.
  status: todo
  note: This is why #935 is KEPT OPEN. `issue_action: comment_keep_open` is recorded in `workflow-state.md`, so the sink merges, comments and archives but does not close the issue. The implementation commit must therefore carry NO closing keyword, or the merge would auto-close #935 and defeat the decision.

- item: File the follow-up issues this run surfaced but deliberately did not fix, all outside the scope fence. (1) `--enforce-floor` has ZERO production consumers — the reasoning-floor refusal is not live for ANY role including `synthesizer`, so "no new refusal path" held partly because there is no live refusal path at all; this is the biggest finding of the run and `REASONING_FLOOR_ROLES` was explicitly out of scope. (2) `sync-opencode-edition.js:870` prints an unconditional remediation footer advising `--write`, which cannot fix a stale `opencode.json` — `:661-666` preserves the file and directs you to `--write-config`; the per-mismatch reason at `:865` is already correct, only the footer misleads. (3) The edition suite MATERIALIZES `.opencode-gitlab`/`.opencode-gitea` as it runs, so a first run on a fresh clone reports them `SKIPPED — absent` while the drift-check banner still reads as coverage. (4) `EXPECTED_ROLE_MODELS` in `test-install-model-rendering.js` holds 13 of 14 roles — `investigator` appears nowhere in that file, so one role's rendered tier is unpinned. Each needs its own issue with the measurement that found it.
  status: done
  dispatched: self — filing is a forge action and the measurements are mine; user approved all four.
  result: Filed #940 (reasoning floor enforced for no role — `--enforce-floor` has zero production consumers), #941 (`sync-opencode-edition --check` footer advises a flag that cannot fix a stale config), #942 (edition suite materializes the trees it drift-checks), #943 (`EXPECTED_ROLE_MODELS` pins 13 of 14 roles; `investigator` unpinned). All four reconciled through `.cache/run-gaps-manual.md` and the gap-sweep gate: `{"result":"pass","mapped":5,"filed":4,"noise":1}`.
