# Settle the three claim-artifact issues #936 left behind — #937 (slug-keyed marker deletion), #938 (offline finalize reports closed having released nothing), #939 (six finalize refusals return before the claim loop)

All three were filed by the #936 run against `main` at `ecdb2c88`; HEAD is now `42559b1c` and #936's
own fix has landed since, so every cited line number is re-verified before it is built on. All three
explicitly permit closing with no code. Worktree: `.kw/worktrees/bundle-937-938-939`, branch
`workflow/bundle-937-938-939`.

- item: Re-verify the three issues' cited line numbers and code shapes against HEAD 42559b1c — `clearAdvisoryClaim` (cited 958-975), the `cmdFinalize` claim-clearing loop (cited 4598-4612), the six refusal returns (cited 4248/4277/4328/4343/4379/4414), the null-folder fallback (cited 4578-4596), `activeByProject` (cited 1123-1125), `classifier.js:215` detector and `:216-217` expiry and `:371-374`/`:390` blocked message. #936 edited this file, so drift is expected; produce a corrected table.
  status: done
  dispatched: code-explorer "MAP-A" (read-only, no drives) — corrected anchor table + Direction-2 call-site census → `kaola-workflow/bundle-937-938-939/.cache/map-a-anchors.md`
  result: `.cache/map-a-anchors.md`. Shapes all byte-unchanged; only line numbers moved. **#939's key number survives: still exactly six refusal returns**, and all six cited lines still land inside the block they named (`archive_refused` :4379 is byte-exact). `clearAdvisoryClaim` is now at :962-986 and is **4-arg** (`opts` added by #936) — any 3-arg quote is stale. `activeByProject` is at :1128-1130 and the cited :1123-1125 now points at a DIFFERENT function (`activeByIssue`). Confirmed strict `===` over dir entry names. **The null-folder fallback recovers `issue_number`/`issue_numbers` but NO slug — and the slug never came from the folder at all, it is `args.project` at :4610/:4616.** classifier.js `:215`/`:216-217`/`:371-374` byte-exact; blocked message is :392 not :390. `closure-audit.js:628` byte-exact, and `kw:claim` appears NOWHERE in that file — no marker sweep on either axis.

- item: Drive #937 Direction 1 on the real scripts — a finalize whose `--project` differs from the folder name only in case: confirm exit 0 / `result: closed` / comment LIST issued / ZERO DELETE calls, and confirm the stated mechanism (path ops succeed on case-insensitive APFS while `activeByProject`'s strict `===` returns null). Mock forge, temp fixture, never the shared root.
  status: done
  dispatched: investigator "DRIVE-937" via KAOLA_GH_MOCK_SCRIPT in a scratchpad fixture repo → `kaola-workflow/bundle-937-938-939/.cache/drive-937-direction1.md`
  result: **CONFIRMED as symptom, PARTIAL on mechanism — the issue's causal story is REFUTED.** Two-leg ledger, only axis the argument's case: control deletes 2 markers, variant deletes 0, both exit 0 / `status: closed` / `claim_label_removed: "removed"`. Positive control proves the mock is wired. **Leg C is the refutation**: with the folder STILL unresolvable but the stored marker spelled the operator's way, both markers DELETE — so `activeByProject`'s null is NOT on the causal path. The real one-liner: `cmdFinalize` is the only caller passing operator-supplied `args.project` instead of resolved `folder.project`, into an exact-substring marker. Leg D: **not keep-open-specific**, close mode misses too. Legs E/F: `release` REFUSES on the variant (it needs the folder), which is why finalize is the exposed door. Case-sensitive APFS image: variant **refuses** with `finalize_gate_unverified`/`archive_authority_missing` — the issue's host-dependence claim CONFIRMED by measurement. `"removed"` reports only the label call; the marker delete sits in a fully swallowed try, so **no envelope field distinguishes the broken run from the good one** (`closure_invariants.ok` is `true` in the broken leg). **New secondary defect**: the archive dir is named from `args.project` too, producing `archive/Bundle-936-940` whose inner state says `name: bundle-936-940`. Disclosed: one unintended read-only `gh issue list` from a first probe that set OFFLINE after module load; re-run correctly with 0 calls.

- item: Settle #937 Direction 2 — statically enumerate every call site reaching `clearAdvisoryClaim` at HEAD and record whether any can pass a falsy `project`; the issue counted eight sites all passing non-empty. If the arm still has no producer, the question of what the falsy branch should do (match-nothing vs match-everything) is a disposition to record, not a bug to chase.
  status: done
  dispatched: folded into code-explorer "MAP-A" above — census lands in the same `.cache/map-a-anchors.md`
  result: **The count is 11, not 8** — #936 added three sink-merge sites (`sink-merge.js:971/985/2892`). All 11 are PROVABLY-NON-EMPTY (`args.project` dominated by an assert; `folder.project` is a dir entry name past `isSafeName`), so #937's conclusion stands and only its arithmetic was stale. **But the falsy arm is NOT producerless: `test-gitlab-forge-helpers.js:214` and `test-gitea-forge-helpers.js:281` pass `null` deliberately and assert match-everything BY NAME.** Canonical pins nothing there — so a canonical-only change to that arm is invisible to every suite, and a four-edition change reds two forge suites and is therefore a test-DELETION decision, not a one-line edit. `clearAdvisoryClaim` is already exported on all editions (#936 did it).

- item: Drive #938's measurement at HEAD — `KAOLA_WORKFLOW_OFFLINE=1` finalize on a bundle: confirm exit 0, `status: closed`, `claim_label_removed: skipped_offline`, zero forge calls, and that BOTH the `workflow:in-progress` label and the `<!-- kw:claim project=… -->` marker survive on every member. Also record what `finalization-summary.md` says today, since candidate shape 1 would write the finding there.
  status: done
  dispatched: investigator "DRIVE-938" via KAOLA_WORKFLOW_OFFLINE=1 + mock forge in its own scratchpad fixture → `kaola-workflow/bundle-937-938-939/.cache/drive-938-offline.md`
  result: **CONFIRMED.** Five legs, artifacts posted by REAL production claim code then finalized. Control: 11 forge calls, both artifacts gone. Offline: **0 calls** (log is zero bytes), both artifacts survive on BOTH members, in BOTH the keep-open and close lanes. **Correction to the issue: the LABEL is the first and sufficient blocker and has NO expiry** — it short-circuits the marker probe entirely (1 forge call vs 3). A 2×2 isolation with a both-absent positive control proves it: label-only blocks, marker-only-aged-25h acquires. So the durable half of the leak is the label, permanently. **The `skipped_offline`≡`removed` sameness is DELIBERATE, EXPLICIT and PINNED — not an accidental truthy read**: exactly two consumers, both string-equality allow-lists (`claim.js:3250`, `:5274`), zero truthy tests anywhere, and it is contracted at `docs/api.md:1023`/`:1145`. **So "make it violate" is a CONTRACT CHANGE** that reds `simulate-workflow-walkthrough.js:7079` and `:7930` by name. **A typed-finding fix reds NOTHING** and the wiring already exists: `recordFinalizeFinding`/`flushFinalizeFindings` (`claim.js:4190-4209`), and the clearing sites at :4610/:4616 already sit after `result` is assigned and before the unconditional flush at :5154 — no new plumbing. `finalization-summary.md` is BYTE-IDENTICAL online vs offline today. **No detector catches the residue**: `audit-labels`/`repair-labels` scope to `--state closed` and the leaked members are OPEN (driven, with a closed positive control that DID fire). Offline-claim asymmetry confirmed — offline claim creates nothing; offline finalize abandons state a prior online claim made minutes earlier. **Open fork (inference 3): nothing local records whether the claim was posted online** — `writeState` persists no `remote_claim` token — so a finding can only be conditional unless claim time starts persisting it.

- item: Answer #939 question 1 with a driven path, not a reading — is there a finalize refusal where the work is genuinely complete and MERGED and the claim is therefore stranded over nothing? `:4379`/`:4414` (archive refused / archive incomplete) are the candidates because the merge can already have landed by then. A negative answer closes #939 with no code and is an expected outcome.
  status: done
  dispatched: investigator "DRIVE-939" using KAOLA_WORKFLOW_FORCE_ARCHIVE_REFUSAL + a merged-branch fixture → `kaola-workflow/bundle-937-938-939/.cache/drive-939-refusal-paths.md`
  result: **NO — question 1 answered negative, driven, so #939 closes with NO CODE.** Door count at HEAD is still **6**; all six driven, all fire with the mainline untouched and both artifacts held, and **none performs a mutating forge call** (door 1 makes no call at all; the other five make 2 pure reads). All six precede `archiveProjectDirSafely`, so `cmdFinalize` runs entirely before the merge by construction — and after an archive refusal BOTH sink forms refuse to merge (`run_not_finalized`; `sink_blocked` on the untracked main-side duplicate a refused finalize necessarily leaves). D7 built the most hostile ordering — merged AND pushed to origin/main by hand before finalize, then door 6 fires — and it still does not qualify: unreachable from inside the workflow, and live run state exists in three places, so a re-claim there would be the actual defect. **Three recovery paths, all driven green**: re-run finalize after fixing; `cmdRelease` from the main root (clears BOTH artifacts even on a condition that permanently blocks finalize, because release renames in place); re-run `--sink`. **The residual gap is ROUTING, not code** — no refusal envelope and no command surface names `release` as the give-the-claim-back route; `docs/api.md:833` names it for exactly one unrelated archive reason. Also recorded: the sink's own archive is step 4 AFTER merge, so its archive stop leaves the branch merged into LOCAL main — but nothing published, no forge call, journal resumable. Three post-claim-release refusals (4982/5103/5132) are the INVERSE hazard, unmeasured. #936's keep-open fix confirmed ARMED.

- item: Close DRIVE-937's one named gap — the three sink-merge `clearAdvisoryClaim` sites (`sink-merge.js:971/985/2892`, all added by #936) take `args.project` in the same shape as the finalize sites but were NOT driven, and the sink is the path the workflow actually takes. Drive a case-variant `--project` through the sink's keep-open terminal and find out whether it misses the same way or normalises the slug upstream.
  status: done
  dispatched: investigator "DRIVE-937-SINK" — case-variant slug through sink-merge keep-open, mock forge, scratchpad fixture → `kaola-workflow/bundle-937-938-939/.cache/drive-937-sink.md`
  result: **CONFIRMED — all three sites REACHED-AND-MEASURED, and the sink half is STRICTLY WORSE than the finalize half.** Attribution is proven, not inferred: a byte-identical mirror of `scripts/` plus exactly three env-gated one-line probes (diff shows only lines 970a971, 984a986, 2891a2894), with a control proving the mirror reproduces all four legs identically. `:971`/`:985` fire on the LEGACY path, `:2892` on `--sink`; the two terminals never reach each other, so two fixtures were needed. **The normalisation hypothesis is REFUTED twice over**: the only validation is `isSafeName` at `:3036`, which tests SHAPE and never touches the filesystem, and the in-process probe read back the operator's raw `Issue-93702`. Ledger, only axis the case: control 2 DELETEs / variant **0**, on both entry points, on every bundle member, with `REJECTED-wrong-cwd` = 0 in all four legs (so this is NOT #936's cwd trap — `forgeOpts` is working and a fix must not regress it). **Beyond the surviving markers, the `--sink` case-variant leg PUBLISHES CORRUPTION**: because git's index is case-sensitive while APFS is not, the archive move found the content but the removal pathspec never matched, so a fresh clone of the remote carries `archive/Issue-93702/` AND the never-removed live `kaola-workflow/issue-93702/` — the run record duplicated on the remote, main left dirty with two unstaged deletions, all reporting `result: ok, status: sinked`, exit 0, `closure_invariants.ok: true`. **So the exposed set is 5 sites taking `args.project` (claim.js:4610/:4616 + sink-merge.js:971/:985/:2892); the other 6 take `folder.project` and are safe.** Unmeasured: case-sensitive hosts, the legacy archive-naming leg, worktree-postured runs (`:2109`/`:2980` do a fourth exact `===` on `args.project`), and the gitlab/gitea sink ports.

- item: Ask the owner the value calls, with the measurements in hand — #938's core question (report / defer / accept-and-record) is explicitly the owner's; #937 Direction 2's disposition and anything #939 turns up that is a values fork go in the same ask. Do not implement ahead of the answer.
  status: done
  dispatched: self — asked in conversation after all five investigations landed
  result: **OWNER RULINGS, do not re-litigate.** (1) **#938 → TYPED FINDING**: report the unreleased-claim fact on the envelope and in `finalization-summary.md` through the EXISTING `recordFinalizeFinding`/`flushFinalizeFindings` channel. The finding stays **CONDITIONAL** — the owner did NOT ask for a `remote_claim` token at claim time, so do not add one. (2) **#937 D1 → RESOLVE AND REPORT**: resolve `args.project` to the actual on-disk folder name once, early, in both `cmdFinalize` and `sink-merge`; report the correction. Refusal DECLINED (consistent with the #933 precedent). (3) **#937 D2 + both adjacent observations → WATCH-LIST ALL THREE, NO CODE**: the falsy arm stays as-is (the two forge-edition tests pinning match-everything are NOT deleted), the `sess=`/whitespace gap and the missing marker sweep are recorded in ADR 0017's watch list with the observation that would arm each. (4) **Both cheap adjacents ARE in scope**: name the blocking artifact in the classifier message, and route to `release` in the refusal hints. **Standing constraint given mid-run: add no new thing without necessity — revise and amend existing mechanisms instead.** No new env var, no new file, no new abstraction, no new reporting channel; reuse what exists.

- item: #938's adjacent, "probably worth doing regardless" change — the classifier's `blocked` verdict says `has a remote workflow claim` whichever artifact caused it, so an operator cannot tell which to clear or that one self-heals in 24h and the other never does. Name the artifact in that message across all four classifier copies; it is a prose surface, so check whether it is generated before editing.
  status: todo

- item: #937's two adjacent observations — the deleter is strictly narrower than the detector (`sess=`, inner whitespace, case) so a marker in the gap blocks re-claim for 24h and can never be cleared; and nothing sweeps a stranded marker (`closure-audit.js:628` repairs stale labels only). Decide whether either is forced by an observed failure or is watch-list material under additive derivation.
  status: todo

- item: Author the tests for the finalize/sink lane FIRST and prove them RED on the current tree — the slug resolve-and-report across all 5 exposed sites (`claim.js:4610/:4616`, `sink-merge.js:971/:985/:2892`), and the #938 conditional typed finding on the offline claim-release skip. Test custody: the author of these never writes the production code.
  status: done
  dispatched: tdd-guide "TDD-LANE" — tests only, proven red at HEAD, → `scripts/` suites + a note at `kaola-workflow/bundle-937-938-939/.cache/tdd-lane.md`
  result: **10 pins, all proven red, no production file touched, no new test file / env var.** #937 sink — `test-sink-merge.js:5124/:5126/:5131/:5133` covering BOTH entry points (`--sink`→:2892; legacy→:971+:985) with EXACT-slug positive controls GREEN at baseline; suite reds 9/821. #937 finalize — `test-bundle-finalize.js:1736/:1741/:1746` bundle+scalar+control; reds 7/172. #938 — `simulate-workflow-walkthrough.js:7108 testFinalizeOfflineReportsSkippedClaimRelease`, type `claim_release_skipped_offline`, red on `findings; got: []`, and it also pins that `claim_label_removed` STAYS `skipped_offline` and `closure_invariants.ok` STAYS `true` so an over-reaching fix cannot pass. ONLINE control passes at baseline. **"Reported" is pinned METHOD-FREE** — `slugCorrectionSentences` walks every envelope value for one STRING naming both spellings, so a note field, a finding detail or a receipt field all satisfy it; silence and two-spellings-in-separate-fields do not. Deliberately NOT pinned (correctly): the conditional WORDING of the #938 finding, and conditionality of the #937 report. **Implementer heads-up (pre-existing guards, not new pins): `test-forge-finalize-findings.js` part B reds on a canonical-only finding type — the new type MUST reach all four editions plus `docs/api.md`'s findings row and count sentence; `docs/api.md:385` ("a good finalize … with no findings") goes stale for offline runs.**

- item: Author the tests for the two prose/routing corrections and prove them RED — the classifier naming WHICH artifact blocked (6 emitters across 4 files, gitlab/gitea carry a port-only second site), and `release` named as the give-the-claim-back route in the six refusal hints. Disjoint from the lane tests above.
  status: done
  dispatched: tdd-guide "TDD-PROSE" — tests only, proven red at HEAD, → `scripts/` suites + a note at `kaola-workflow/bundle-937-938-939/.cache/tdd-prose.md`; then re-dispatched to extend T12 to the two doors that carry no `operator_hint`
  result (first pass): **15 red findings across 4 editions.** Classifier — `test-bundle-state.js:329/:351/:376` (canonical, which by COMMON_SCRIPTS byte-identity also covers the claude-plugin mirror), plus 6 gitlab and 6 gitea findings reported through ONE aggregate assert each so a fix landing on one emitter and missing its sibling is visible in a single run. Pins the RESULT not a wording: label arm contains `workflow:in-progress`, marker arm contains `kw:claim`, and **the two arms must not emit the SAME sentence** — that third assertion catches the near-miss where a message names both artifacts on both arms. **Measured correction to my brief: both arms must drive the SAME issue number**, since the defect sentence interpolates it and a two-number pair differs already — the differ-check went green against the defect on the first draft. Refusal routing — `test-finalize-door.js` T12, 4 doors × 4 editions, all 16 reachability assertions PASS at baseline so the 28 message reds are attributable. **Generated-surface verdict: none of the pinned text is generated** — but if the route is also added to command/SKILL prose, that IS generated and must be edited at `templates/routing/finalize.skeleton.md` / `slots.js`, never a rendered surface.
  result (extension, after my ruling to amend `reasoning` rather than add a field): **T12b adds 12 reds with all 8 new reachability assertions PASSING** — suite now `40 failures, 450 passed`; the pre-existing 394-assertion footer and original T12 are unchanged at `28 failures, 430 passed`. Two measured corrections worth keeping: door 4384 under the force knob emits **`archive_forced_refusal`, not the literal `archive_refused`** (it returns `result.reason || 'archive_refused'`); and door 4419 is driven by a **symlink** in the live run folder's `.cache/` — `verifyArchiveComplete` (`claim.js:5925`) cannot reduce it to bytes so it seeds `mismatched[]`, and unlike the force knob that condition PERSISTS across a re-run. The main-root cue is mixed on purpose: `archive_incomplete` is already green on it and stays as a regression pin while `archive_refused` is red — hence 12 reds, not 16. `test-spawn-classification.js` is GREEN again (644 sites, 212 classified) after TDD-LANE moved its marker prose above the token.

- item: Implement the canonical production code for the finalize/sink lane — resolve the slug and report it, plus the #938 conditional finding — against the red tests, never authoring them. Reuse existing channels only; add no new mechanism.
  status: done
  dispatched: implementer "IMPL-LANE" — canonical `scripts/kaola-workflow-claim.js` (cmdFinalize region) + `scripts/kaola-workflow-sink-merge.js` only, against TDD-LANE's 10 red pins → code in the worktree, note at `kaola-workflow/bundle-937-938-939/.cache/impl-lane.md`. **Holds the claim.js edit lock** — released.
  result: **All 10 pins GREEN; full-scope walkthrough 209/209 (not a shard); `test-claim-hardening.js` 766 assertions unchanged.** +168/-21 across the two canonical files, no new env var/file/script. Shape: one pure `resolveProjectSlug(requested, names)` at `claim.js:334-370` (exported at :6698 so the resolution and its sentence exist ONCE and are read twice), called early in both scripts so `args.project` is rewritten in place and all ~40 downstream reads — archive path, roadmap paths, removal pathspec, marker, receipt — see the resolved spelling. **The two callers necessarily read different stores**: cmdFinalize reads directory entries (NOT `readActiveFolders`, which does a `gh issue list` prefetch and filters folders whose issue is closed — i.e. exactly the runs finalize is for), while the sink reads `git ls-tree` of the BRANCH, because the sink runs from the default-branch checkout which does not carry the run folder until the merge step. Report rides the existing conditional-field idiom (`resolved_project_note`) and, in the sink, the existing `sinkEmit` accumulator so one edit covers both terminals and every later refusal. **Unanticipated find, caught by the full walkthrough**: `flushFinalizeFindings` runs AFTER both commits, and every pre-existing finding fires only when a git step already failed — so a finding on a HEALTHY run left the tree dirty and broke `testFinalizeIncompleteWorktreeReentryFix`. Fixed by making the flush repeatable (an optional 4th `replace` arg to `appendSummarySection`; every existing caller passes three and is byte-identical) and flushing once before the residue probe. An early once-only flush was rejected because it would drop `residue_stage_failed`, which is recorded after that probe.

- item: Implement the canonical production code for the two prose/routing corrections against their red tests, never authoring them.
  status: done
  dispatched: implementer "IMPL-PROSE" — `scripts/kaola-workflow-classifier.js` (all editions, 6 emitters across 4 files) + the six refusal messages in canonical `claim.js`, against TDD-PROSE's 15+12 red pins → code in the worktree, note at `kaola-workflow/bundle-937-938-939/.cache/impl-prose.md`. IMPL-LANE has released the claim.js lock.
  result: **ALL GREEN.** `test-bundle-state.js` 29/29, both forge suites pass, `test-finalize-door.js` **exit 0 at 490 assertions** (was 40 failures), full walkthrough still 209/209, `edition-sync.js --write` + `validate-script-sync.js` exit 0, routing surfaces 18/18 untouched. Classifier: the arm was already known at the emitter, so the fix hoists it into a local `blockedByLabel` and branches the message — no signature change, evaluation order preserved so #519 transient routing is untouched. Ports interpolate `forge.CLAIM_LABEL` rather than the literal, matching how those files already match it. **Wording is deliberately precise about the expiry**: because `if (!comment.updated_at) return true`, the message says *a timestamped marker* stops blocking after 24h rather than asserting markers expire. Refusal routing: ONE sentence byte-identical at all six doors × four editions (`grep -c` = 6 per edition), naming the route, the main root, why not from inside the folder, and what `release` actually does — worded as the exit, with re-running finalize left primary. Door 1's hint has FOUR arms and the fixture drives one; all four amended via a shared `route` local so they cannot drift into four wordings. **One site deliberately NOT touched: the second `staging_guard_multi_project` (`finalGuard`, canonical :5224) fires AFTER the claim is released, so the route would be false advice there.** Two mutation probes confirm the guards are armed, both reverted byte-exactly. Intermediate measurement: canonical-only edits took finalize-door 40→30 with `root` fully green and 10 reds each on the other three — the four editions are genuinely independent legs, and codex's 10 cleared by regeneration rather than an edit.

- item: Settle the one deviation IMPL-LANE surfaced and stated plainly — resolving the slug EARLY necessarily changes behaviour on a case-SENSITIVE volume, where `kaola-workflow/Issue-N` genuinely does not exist and the run refuses today with `archive_authority_missing`. After the change the name resolves before that gate reads it, so the run completes and reports instead. Unmeasurable on this box (every volume is case-insensitive APFS).
  status: done
  dispatched: self — put to the owner in conversation, since it removes an existing refusal on the majority platform
  result: **OWNER RULED: uniform resolution.** Resolve, proceed and report the correction on every filesystem; the case-sensitive refusal is NOT preserved. Rationale on record: the refusal it removes is not a designed door but an incidental `archive_authority_missing`, which is the same class of refusal already declined for this defect; and gating it would give `cmdFinalize` and the sink different semantics for the identical operator mistake, since the sink resolves from a git tree and has no filesystem to probe at that point. **No code change — IMPL-LANE's implementation stands as written.** Do not re-litigate; do not add an existence probe.

- item: Port every canonical change to the three other editions — `edition-sync.js --write` regenerates the codex/claude mirror by `cp`, but gitlab and gitea are HAND ports whose `clearAdvisoryClaim` takes the slug as the FOURTH argument (`issueIid, reason, projectInfo, project, opts`), and whose classifier carries a second blocked emitter canonical lacks. Finish with `validate-script-sync.js` green.
  status: done
  dispatched: split by file so the two implementers run in parallel — IMPL-PROSE owns the classifier ports + canonical refusal messages + the single `edition-sync.js --write`; IMPL-LANE owns the gitlab/gitea `claim.js` + `sink-merge.js` ports of its own lane change (`resolveProjectSlug` export, the `claim_release_skipped_offline` finding, the sink's tree-based resolution) → note at `kaola-workflow/bundle-937-938-939/.cache/port-lane.md`
  result: **`validate-script-sync.js` exit 0** — 15 common scripts, 27 byte-identical groups, 6 forge export-superset families, 4 Oracle Kernel copies identical at HEAD. The two implementers shared both forge claim ports without collision: every edit used an exact-string anchor so a clash would have failed rather than clobbered, and the hunks are disjoint (15 lane hunks vs 12 prose hunks per file). **The forge signature difference needed NO special handling** — resolution rewrites `args.project` upstream of every call site, so all five per-edition sites get the resolved slug untouched, and gitea's `releaseClaimArtifacts` wrapper forwards it straight through. One structural divergence, declared: the forge sinks split their asserts between `main()` and an independently-exported `runDirectMerge`, so there is no single line every path crosses — the resolution is a small named local called at both, with `resolveProjectSlug` still the only place the sentence exists. **Green suites were proven insufficient and driven instead**: both forge suites were ALREADY green at baseline, so two scratch drives exercised all four editions × {exact, mis-cased} — the finding fires, the durable section names BOTH bundle members, `claim_label_removed` stays `skipped_offline`, `closure_invariants.ok` stays `true`, the mis-cased run archives under the RESOLVED name and carries the note while the exact run carries none, and the worktree is CLEAN after every run. `test-forge-finalize-findings.js` part B: all three edition-delta legs GREEN (forge count 6→7 restores the delta to exactly `["archive_unstage_failed"]`); only three `docs/api.md` statements remain red.

- item: Record the three watch-list rows in ADR 0017 — the falsy-`project` arm, the `sess=`/whitespace detector-vs-deleter gap, and the absent marker sweep — each naming the observation that would arm it. Recorded, not built.
  status: done
  dispatched: self — three rows appended to the existing table in `docs/decisions/0017-the-mission-list.md` (worktree)
  result: Three rows added, table-only, no new prose. Each names its arming observation and sizes its mechanism. The falsy-arm row records that it is NOT dead code — the two forge-edition tests pin match-everything by name, so arming it deletes those pins with the mechanism. The marker-sweep row records that it is larger than it looks: a marker cannot be told from another session's LIVE claim without a project-scoped active-folder cross-check.

- item: Land the docs the change owes — `CHANGELOG.md` under `[Unreleased]`, and any of `docs/api.md` / `docs/workflow-state-contract.md` / `docs/conventions.md` the behaviour touches; a decision recorded with no code still owes the CHANGELOG or an ADR entry.
  status: done
  dispatched: self — `docs/api.md` + `CHANGELOG.md` in the worktree
  result: `docs/api.md` — findings row gains `claim_release_skipped_offline`; the per-edition counts go 6→**seven** (forge) and 7→**eight** (canonical/codex); `resolved_project_note` documented on the finalize envelope with why the resolution happens once and up front, and that `--check` and the sink carry it too (the sink resolving from the branch's git tree, not the filesystem); the healthy-run sentence now excepts the offline lane. **One staleness caught that no test named**: the paragraph asserting the durable write "happens **once** … `appendSummarySection` is idempotent by heading" was made false by the repeatable-flush change — rewritten to restating-not-append-once, with the reason an append-once flush would drop `residue_stage_failed`. `test-forge-finalize-findings.js` now **133 passed, 0 failed** (was 5 then 3 failures). `CHANGELOG.md` — four `### Fixed` entries under `[Unreleased]` covering #937, #938 (finding + classifier message) and #939, each stating the defect and what was deliberately left unchanged.

- item: Full-scope verification before finalize — `node scripts/simulate-workflow-walkthrough.js` at full scope (the fast gate samples a 1/12 shard) plus `npm test`, then `/kaola-workflow-finalize bundle-937-938-939`.
  status: in-flight
  dispatched: self — full walkthrough **209/209 exit 0** (not a shard); **`npm test` all four chains exit 0** incl. `test-forge-finalize-findings` 133/0, reserved-directory 188/0, claim-rollback 120/0, reserved-project 364/0 across 20 legs, roadmap-prose 90/0, active-folders parity 119, and `generate-routing-surfaces --check` 18/18 byte-matching. Implementation committed in the worktree as **`705ab4a1`** (22 files, +2236/-128), message using `Refs #937, #938, #939` with NO closing keyword so finalization owns the closing rather than the push doing it silently. Next: `/kaola-workflow-finalize bundle-937-938-939`.
