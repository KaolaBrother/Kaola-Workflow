# Finalization - Summary: bundle-675-676 (closes #675 + #676)

## Delivered
Archive-evidence-integrity pair. **#675** — `gap-sweep`'s `runScan` refuses `project_archived` when the
active project folder is gone but an archive exists, before any `mkdir`/read/`--output` write, so a
post-archive scan no longer recreates a stray active dir or clobbers the archived `run-gaps.json`.
**#676** — `verifyArchiveComplete` is now SOURCE-RELATIVE: the archive DEST must preserve every evidence
file the live SOURCE actually had (top-level plan/state/summary/fast-summary + every `.cache/*.md`
node-evidence file of any node-id shape, minus fixed-name machinery sidecars), refusing `archive_incomplete`
before any live-copy deletion otherwise; `workflow-state.md` is the unconditional anchor and the atomic
`renameSync` path stays ungated. Source-relative was the operator's value call over the issue's absolute
per-path floor (decision record `docs/decisions/D-676-01.md`).

## Files Changed
- scripts/kaola-workflow-gap-sweep.js (+ codex twin + gitlab/gitea rename-normalized ports) — project_archived refusal
- scripts/kaola-workflow-claim.js (+ codex twin + gitlab/gitea forge hand-ports) — source-relative verifyArchiveComplete
- scripts/test-gap-sweep.js — #675 T12/T13 regressions
- scripts/simulate-workflow-walkthrough.js — #676 testArchiveCompleteSourceRelative676 (source-relative, incl. free-form node-id cases)
- CHANGELOG.md — two `[Unreleased]` entries; docs/decisions/D-676-01.md — decision record
- kaola-workflow/.roadmap/issue-679.md — new follow-up source (run gap R2)

## Test Coverage
Not a coverage-gated repo. `test-gap-sweep.js` 68; `test-bundle-finalize.js` 135; `test-claim-hardening.js`
173 (at HEAD, zero fixture edits); `test-adaptive-node.js` 1797; `simulate-workflow-walkthrough.js` passed;
`edition-sync.js --check` + `validate-script-sync.js` clean.

## Final Validation Evidence
Self-host four-chain receipt (`.cache/chain-receipt.json`) stamped by `run-chains.js --project bundle-675-676`
after the feature commit. Per-node evidence: `.cache/n1-fix.md` (RED→GREEN both fixes + the R1 repair),
`.cache/n2-review.md` (opus, verdict pass — re-review after repair), `.cache/n3-adversary.md` (opus,
NOT-REFUTED after repair — re-ran its own refuting case). NOTE: reviewer/adversary gates ran on OPUS
(fable was out of usage credits mid-run; operator chose opus).

## Documentation Docking
DOCKED — CHANGELOG (2 entries) + the D-676-01 decision record; no public interface/API/env/architecture
change. Evidence: this summary + `.cache/n4-finalize.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| n3-adversary R1 refutation (in-run) | evidence-loss (glob too narrow) | reopen-node → tdd-guide (n1) | .cache/n1-fix.md + n3-adversary.md | resolved (broadened enumeration; re-gated pass) |

## Follow-Up Items
- #679 — pre-existing gap-sweep explicit-`--output`-at-a-same-named-archive clobber (adversary R2, out of #675's archived-project scope).
- N1 (theoretical sidecar/node-id name collision — no shipped trigger) and N2 (non-`.md` artifacts carried faithfully by copyDir — no loss) are non-defects, not filed.

## Run gaps
- in_run_repair (n1-fix): noise: n1 reopened to repair the adversary-refuted #676 node-evidence glob, broadened to every .cache .md minus fixed-name sidecars; re-verified pass
- in_run_repair (n2-review): noise: re-gated after the n1 repair, code-review verdict pass 0 blocking
- in_run_repair (n3-adversary): noise: re-gated after the n1 repair, NOT-REFUTED reran its own refuting case
- manual:gapsweep-explicit-output-archive-clobber (a live scan with explicit --output at a same-named leftover archive run-gaps.json still clobbers it outside the archived-project scope of #675): filed: #679

## Closure Decision
The in-run adversary refutation was repaired and re-gated (both gates pass). One out-of-scope residual R2
filed as #679; N1/N2 are non-defects. No unresolved conflict or user-decision item remains for #675/#676 —
both fixes complete and verified (review + live adversary after repair). Close BOTH #675 and #676.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#675 + #676 — both closed by the bundle merge sink (all-or-nothing) after the four-chain merge gate.

## Roadmap
Regenerated by cmdFinalize: adds .roadmap/issue-679.md, retains #673/#677/#678. (#675/#676 were GitHub-only,
unmirrored, so nothing to remove.)

## Archive
pending (cmdFinalize Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| n1-fix (tdd-guide) | invoked | .cache/n1-fix.md | |
| n2-review (code-reviewer/opus) | invoked | .cache/n2-review.md | fable out of credits; operator chose opus |
| n3-adversary (adversarial-verifier/opus) | invoked | .cache/n3-adversary.md | fable out of credits; operator chose opus |
| doc-updater | skipped | CHANGELOG + D-676-01 authored in the finalize node | sole doc surfaces are CHANGELOG + the decision record |
| documentation docking | invoked | this summary + .cache/n4-finalize.md | |
| final-validation fix executors | invoked | reopen-node → n1 repair (.cache/n1-fix.md) | in-run repair of the adversary refutation |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | cmdFinalize Step 8b |
| archive completed folder | pending | | cmdFinalize Step 8b |
| final commit and push | ready | git status/upstream check | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE

## Attestation
claim_planner_attested: missing
finalize_contractor_attested: attested
ATTESTATION WARNING: no workflow-planner dispatch found in dispatch-log — claim/author seam may have been run inline by main session
