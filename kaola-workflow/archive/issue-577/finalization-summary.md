# Finalization - Summary: issue-577

## Delivered
A tracked canonical source for the opencode-edition hook adapter, closing two pre-existing gaps:
fresh-clone `install-opencode.sh` silently deploying no hooks plugin, and
`scripts/test-opencode-edition.js` (A11/P1/G1/H1) failing in any clean checkout / fresh git worktree.
The plugin now has a git-tracked source under `templates/opencode/plugins/`, regenerated into the
gitignored `.opencode/plugins/` by `sync-opencode-edition.js` and deployed from the tracked source by
`install-opencode.sh` (no self-referential `.opencode/` copy; a missing plugin is now a loud error).

## Files Changed
- `templates/opencode/plugins/kaola-workflow-hooks.js` (NEW, tracked canonical source; byte-identical to the live adapter)
- `scripts/sync-opencode-edition.js` (`writePlugin()` byte-copy wired into `--write`; parity assertion in `--check`)
- `install-opencode.sh` (deploy from tracked `templates/opencode/plugins/`; dropped silent `2>/dev/null || true`)
- `scripts/test-opencode-edition.js` (self-provision via `sync --write`; new `A11-canon` byte-identity assertion)
- `docs/opencode-edition.md` (canonical-source + regeneration path)
- `docs/decisions/D-577-01.md` (NEW decision record)
- `CHANGELOG.md` ([Unreleased] #577 entry)

## Test Coverage
opencode edition suite: `node scripts/test-opencode-edition.js` → 496 assertions, exit 0, GREEN from
tracked sources in a fresh git worktree with no manual `.opencode/` seeding (the bug's exact failing
environment). RED→GREEN verified in n1 (tdd-guide) and independently re-run by the n3 review gate
(clean-clone simulation: moved `.opencode/` aside, suite still green). Coverage is assertion-based
(no %); the opencode suite is the recorded `validation_command`.

## Final Validation Evidence
- Plan `## Meta` `validation_command`: `node scripts/test-opencode-edition.js` — PASS (496 assertions,
  exit 0), evidence `.cache/n1-opencode-plugin-source.md` (RED→GREEN) and `.cache/n3-review.md` (gate verdict: pass).
- Self-host four-chain receipt: generated by the orchestrator post-impl-commit at
  `.cache/chain-receipt.json` (the self-host finalize machine gate). opencode is additive (D-530-02);
  no opencode file is in the four chains, so the chains are unaffected by this diff and the opencode
  suite is the real correctness gate.
- Validation reuse boundary: the opencode-suite pass covers code/test impact through node n1, re-verified
  at n3; the n4 `CHANGELOG.md` edit and the n2 docs edits are docs-only and outside the suite's rerun trigger.

## Documentation Docking
DOCKED — evidence `.cache/doc-docking.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- n3 reviewer LOW note R2 (non-blocking, out-of-scope): `install-opencode.sh` deploys
  `templates/opencode/plugins/*.js` via a glob while `sync-opencode-edition.js` uses an explicit
  `PLUGIN_SCRIPTS` allowlist. They agree today (single plugin file); a hypothetical future second
  plugin `.js` added without updating `PLUGIN_SCRIPTS` could diverge. Future-proofing only.
- n3 reviewer LOW note R1 (non-blocking, out-of-scope): the `A11-canon` byte-identity sub-assertion is
  partly tautological inside the self-provisioning test (the load-bearing guard is the `exists()` check
  on the tracked source + install-time `--check`). Harmless.

## Run gaps
- manual:reviewer-note-R2 (install glob vs sync PLUGIN_SCRIPTS allowlist divergence): noise: dormant future-proofing note, not a current defect — install and sync agree for the single existing plugin file; would require a future unrelated second-plugin addition to manifest. Reviewer rated non-blocking/out-of-scope.
- manual:reviewer-note-R1 (A11-canon byte-identity sub-assertion partly tautological): noise: harmless — the real regression guard is the `exists()` check on the tracked source plus install-time `--check`; no defect.

## Closure Decision
No deferred items, unresolved conflicts, or partial implementation. The only run-discovered items are
the two LOW non-blocking reviewer notes above, both recorded as noise (neither is a current product
defect). Issue #577 is fully resolved (AC1–AC4 met). Proceed to close.

## Commit And Push
Implementation commit authored by the orchestrator (code + docs + CHANGELOG) before the chain receipt;
the `chore: finalize issue-577` bookkeeping commit is authored by the contractor. Final hash reported
after the sink; not written back here.

## GitHub Issue
Pending close via sink (full close; `sink: merge`).

## Roadmap
Pending — `cmdFinalize` removes `.roadmap/issue-577.md` and regenerates `ROADMAP.md` once at archive.

## Archive
Pending — `cmdFinalize` archives `kaola-workflow/issue-577/` → `kaola-workflow/archive/issue-577/`.

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked | .cache/n2-docs.md (node n2) | |
| documentation docking | invoked | .cache/doc-docking.md | |
| final-validation fix executors | N/A | — | no validation failures |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | runs in cmdFinalize at archive |
| archive completed folder | pending | | runs in cmdFinalize |
| final commit and push | ready | git status/diff/upstream | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE
