# Run-gap manual seeds — issue-674

Adversary R1 (group baseline strands on the same aborts) + review/adversary R2 (pre-journal hard-crash
window): two residuals in the same open-ready baseline lifecycle, out of scope for #674's member-baseline
fix, filed together as #678.

gap: open-ready-baseline-lifecycle-residuals — group baseline strands on the same group-form aborts where member baselines now drop plus a pre-journal hard-crash window strands baselines both out of scope for the member-baseline fix
