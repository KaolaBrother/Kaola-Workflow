# Finalization - Summary: issue-674

## Delivered
Two coupled defects in `open-ready`'s lane-group provisioning transaction (parallel-write engine), both
live-observed in a consumer repo. (a) The seeded member evidence stubs are now `git add -f`'d (were
`git add` without `-f`), so a consumer that gitignores `.cache/` co-opens successfully instead of
`stub_commit_failed` + silent serial-degrade; the `-f` add stages ONLY the script-enumerated stub paths.
(b) Every post-baseline group-form abort (`stub_commit_failed`, mid-loop `baseline_failed`, three
`leg_provision_failed` variants) now transactionally drops the recorded member baselines (base file +
gc-anchor ref + freshness token) via the existing `--drop-base` primitive, so a later serial member open
re-records a fresh baseline instead of reusing the stale one — eliminating the `write_set_overflow`
misattribution of a sibling's landed diff.

## Files Changed
- scripts/kaola-workflow-adaptive-node.js (+ codex twin + gitlab/gitea @generated ports) — git add -f + dropRecordedBaselines on all 5 group-form aborts
- scripts/test-adaptive-node.js — #674a (gitignored-.cache co-open succeeds) + #674b (post-abort serial open no write_set_overflow) regressions
- CHANGELOG.md — `[Unreleased]` entry for #674
- kaola-workflow/.roadmap/issue-678.md — new follow-up source (run gaps R1/R2)

## Test Coverage
Not a coverage-gated repo. `node scripts/test-adaptive-node.js` → 1797 assertions;
`node scripts/test-parallel.js --self-test` → 13 passed; `node scripts/simulate-workflow-walkthrough.js`
→ "Workflow walkthrough simulation passed"; `node scripts/edition-sync.js --check` → clean.

## Final Validation Evidence
Self-host four-chain receipt (`.cache/chain-receipt.json`) stamped by `run-chains.js --project issue-674`
after the feature commit. Per-node evidence: `.cache/n1-open-ready-baseline.md` (RED→GREEN, 5 abort
paths audited), `.cache/n2-adversary.md` (NOT-REFUTED — every abort path executed, partial-drop ruled
out, git add -f scoped), `.cache/n3-review.md` (verdict pass, 0 blocking).

## Documentation Docking
DOCKED — sole doc surface is the CHANGELOG `[Unreleased]` entry; no public interface/API/env/architecture
changed. Evidence: this summary + `.cache/n4-finalize.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- #678 — open-ready baseline-lifecycle residuals: R1 the GROUP baseline strands on the same 5 group-form aborts where #674 now drops MEMBER baselines (symmetric gap, bounded harm); R2 a SIGKILL hard-crash window before the Phase-1 journal write strands baselines (pre-existing, unreachable via any refusal return). Both adversary/review-confirmed, out of scope for #674's member-baseline fix.

## Run gaps
- manual:open-ready-baseline-lifecycle-residuals (group baseline strands on the same group-form aborts where member baselines now drop plus a pre-journal hard-crash window strands baselines both out of scope for the member-baseline fix): filed: #678

## Closure Decision
Two out-of-scope residuals (R1, R2) filed as #678 (see Run gaps) — in the same subsystem but out of
scope for #674's stated fix (member baselines + git add -f), and neither introduced by this change. No
unresolved conflict, partial implementation, or value/standing decision for #674 itself — both defects
are fixed and verified (RED→GREEN + adversary every-abort-path audit + G1 review). Close #674.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#674 — to be closed by the merge sink after the four-chain merge gate.

## Roadmap
Regenerated by cmdFinalize: adds .roadmap/issue-678.md, retains #673/#677. (#674 was GitHub-only,
unmirrored, so nothing to remove; #675/#676 remain GitHub-only.)

## Archive
pending (cmdFinalize Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| n1-open-ready-baseline (tdd-guide) | invoked | .cache/n1-open-ready-baseline.md | |
| n2-adversary (adversarial-verifier/fable) | invoked | .cache/n2-adversary.md | |
| n3-review (code-reviewer/fable) | invoked | .cache/n3-review.md | |
| doc-updater | skipped | CHANGELOG-only, no checklist doc impact | sole doc surface is CHANGELOG, updated inline in the finalize node |
| documentation docking | invoked | this summary + .cache/n4-finalize.md | |
| final-validation fix executors | N/A | — | no final-validation failure |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | cmdFinalize Step 8b |
| archive completed folder | pending | | cmdFinalize Step 8b |
| final commit and push | ready | git status/upstream check | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE

## Attestation
claim_planner_attested: attested
finalize_contractor_attested: attested
