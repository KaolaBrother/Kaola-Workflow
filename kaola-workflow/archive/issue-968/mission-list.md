# Land #968 — make the bundle the default shape of a run, and fix the admission test that fights it

- item: Verify #968's own claims against the tree before building on them — the 26-run size distribution (median 1, mean 2.6, 14/26 singles), the 8 rendered surfaces plus the skeleton and README.md:883-884/:1298, the cap retirement at scripts/kaola-workflow-claim.js:1886-1911 with BUNDLE_SIZE_ADVISORY=8 advisory-only, and whole-set keep-open at claim.js:4911; filed issues in this repo are frequently wrong about themselves.
  status: done
  dispatched: investigator, read-only premise pass over the five claims; report lands at kaola-workflow/issue-968/.cache/premise-968.md
  result: kaola-workflow/issue-968/.cache/premise-968.md — claims 1 and 2 CONFIRMED (distribution exact to the digit, cap genuinely retired and exercised), but my "nine-run single streak" sub-claim is REFUTED. Claim 3 CONFIRMED for the sentence and its SCOPE REFUTED: the rule is stated in six phrasings across THREE skeletons (next, init, finalize) reaching 40 consumer surfaces, and README restates it four times, not two. Claim 5 substantially REFUTED — #328's own design phase DROPPED target_set_not_same_scope on #44 grounds, for the same reason item 2 gives, its only intended carrier (issue-scout) is retired, and zero machinery reads scope; item 2 completes #328 rather than reversing it. Claim 4 CONFIRMED but QUALIFIED — closure_policy: all_or_nothing is a schema-validated, audit-consumed, four-edition-pinned contract (a stronger reason not to build a valve than I gave), and a per-member excludeIssues seam ALREADY exists and is already tested, so the watch-list row's "mechanism already sized" is half-built. New conflict found: README.md:1312 already recommends a bundle size, and it says 8.

- item: Find out whether any test, guard or generator pins the exact wording being replaced — retired-vocabulary bans, templates/routing/required-blocks.js, walkthrough prose assertions, edition sync transforms keyed on source text — so the edit does not red a chain on a sentence nobody knew was load-bearing.
  status: done
  dispatched: investigator, read-only pin sweep over tests/guards/generators/sync transforms; report lands at kaola-workflow/issue-968/.cache/pins-968.md
  result: kaola-workflow/issue-968/.cache/pins-968.md — the wording is UNPINNED (zero test assertions on any of the six phrases), so it is free text; the propagation is what bites. Surfaces are TWELVE not eight (the issue undercounts by 4: .opencode-gitlab/.opencode-gitea/.kimi-gitlab/.kimi-gitea exist too). The two sync --check scripts run in NO chain, so four green chains can ship stale edition trees — the sync scripts must be run explicitly per forge. Banned token hazard: "fan-out cap" REDs (use "nothing caps it"); also parallel_safe and bundle_requires_adaptive. No trailing whitespace anywhere in new prose — rule 5 of the sync transforms silently strips markdown hard breaks on all six edition trees while --check stays green. No new HTML-comment directive lines and no PIN: marker. Pre-existing untracked drift on the gitlab/gitea edition trees from e4522be9 will be swept up by any regeneration.

- item: Settle the one value call with the user — replacing "share a coherent scope" with "closeable on its own evidence" partially reverses #328's stated same-scope framing; the 3-5 default is already the user's own stated intent and is not in question.
  status: done
  dispatched: self — asked the user in conversation once the premise evidence was in hand, since claim 5's refutation changed the question
  result: All three settled by the user. (1) REPLACE the admission test — same-scope becomes one admissible route, disjoint write surfaces the preferred one. (2) Floor 3-5 and the existing advisory ceiling of 8 are DIFFERENT KNOBS and must read as such; BUNDLE_SIZE_ADVISORY stays, no code change. (3) Scope is ALL THREE skeletons plus README, explicitly including the KW-CLAUDE-TEMPLATE region injected into consumer repos' CLAUDE.md.

- item: Author the prose in templates/routing/next.skeleton.md — the default at :58-60 and the completion contract at :268 — then regenerate and confirm all 18 surfaces byte-match and that the opencode and kimi transforms carried it (those two trees do not exist inside the worktree; sweep them in main).
  status: done
  dispatched: self, in the worktree at .kw/worktrees/issue-968
  result: :58-60 replaced by four paragraphs — the 3-5 default with three as a floor on what the frontier already offers and eight kept as the recommended ceiling; admission on "closeable on its own evidence" with disjoint write surfaces preferred over shared scope; runs-alone keyed on blast radius with size explicitly refused as the test; and say what you bundled and what you passed over. :268 rewritten to "one explicitly selected set of issues — normally three to five, sometimes one". Trailing-whitespace and banned-token scans both clean; generate-routing-surfaces --write then --check reports all 18 surfaces byte-match at exit 0.

- item: Author the matching prose in templates/routing/init.skeleton.md — :153 carries the same one-issue-or-same-scope-set rule, and :167 flatly asserts "Each workflow run targets one issue; finishing it is the terminal event" with no bundle escape clause, which a 3-5 default makes FALSE; both sit inside the KW-CLAUDE-TEMPLATE region injected verbatim into consumer repos' CLAUDE.md, so this is a consumer-facing contract change, not an internal one. Also weigh :176 ("one selected item"). Keep the seven required mission-list tokens, and no Phase <n>, no vendor names, no /workflow-next inside that region.
  status: done
  dispatched: self, in the worktree
  result: :153 now reads "A run claims an explicitly selected set of issues — normally three to five, sometimes one — each open, unclaimed, and closeable on its own evidence" and carries the runs-alone test; :167's flat absolute became "targets one selected set of issues; finishing the set is the terminal event"; :176 "one selected item" became "one selected set". All three sit inside KW-CLAUDE-TEMPLATE and reach consumer CLAUDE.md files.

- item: Propagate to all TWELVE next surfaces and TWELVE init surfaces — generate-routing-surfaces --write then --check for the 18 tracked, then sync-opencode-edition.js and sync-kimi-edition.js --write for github, gitlab and gitea each, then npm run test:kaola-workflow:editions; no chain runs the two sync checks, so four green chains would otherwise certify stale edition trees.
  status: done
  dispatched: self — the 18 tracked surfaces are already regenerated and byte-matching; the six edition trees remain. Running the syncs inside the worktree rather than main, because that renders from the NEW command surfaces and so actually tests the transforms against changed prose, which the pin sweep flagged as the risky shape (they fail silently on a changed anchor). Verification lands inline here.
  result: All 18 tracked surfaces byte-match (generate-routing-surfaces --check, exit 0). Six sync runs, 19 files each, all exit 0. The transform risk is REFUTED by measurement rather than assumed: all six edition next surfaces carry both "three to five issues" and "closeable on its own evidence" and NONE still carries "coherent scope"; all six edition init surfaces carry "normally three to five, sometimes one" and none retains either the same-scope clause or the flat one-issue absolute. npm run test:kaola-workflow:editions green — opencode 570 assertions, kimi 528, all six trees reported in parity.

- item: Update README.md by hand at :883-884 and :1306 (near-verbatim copies of the two edited sentences, keeping the #multi-issue-bundle-lane anchor intact) and decide what to do about the softer single-issue framings at :131, :1176 and :1298 that will read as stale under a widened default, so the hand-maintained restatements match the new wording rather than contradicting it.
  status: done
  dispatched: doc-updater, editing README.md in the worktree at .kw/worktrees/issue-968; it reports back inline and the change is visible in git diff README.md
  result: README.md, +16/-11, all five restatements brought into line (:131, :883-884 with the #multi-issue-bundle-lane link preserved, :1176, :1298, :1306) plus the runs-alone paragraph added to the bundle-lane section. :1312 kept the 8 and sharpened it into a ceiling: "Only a set larger than 8 draws the note; the three-to-five norm is guidance for choosing a set, not a second threshold anything checks" — verified accurate against targets.length > BUNDLE_SIZE_ADVISORY. My own residual scan over the whole tracked tree finds no "coherent scope", "coherent in scope", "same-scope set", "normally carries one issue" or "targets one issue; finishing" outside #968's own roadmap entry, which is removed at closure.

- item: Author the matching prose in templates/routing/finalize.skeleton.md:439 — "closes exactly one issue, or every issue in one explicitly selected set — all of them, or none" is the closure half of the same rule on 8 more surfaces, and it stays TRUE under a widened default, so check whether it needs anything beyond leaving alone.
  status: done
  dispatched: self, in the worktree
  result: It did need a change — leading with "closes exactly one issue, or every issue in one explicitly selected set" makes the single issue the headline case. Now reads "closes every issue in one explicitly selected set — all of them, or none; a run carrying a single issue is that set with one member", which keeps the all-or-nothing contract wording verbatim while making the set the norm.

- item: Reconcile the number with README.md:1312, the only live prose recommending a bundle size, which says 8 and matches BUNDLE_SIZE_ADVISORY in all four editions; a 3-5 floor and an 8 ceiling are different knobs and must read as such, or one rule ends up with two numbers.
  status: done
  dispatched: self, folded into the next.skeleton wording rather than handled separately
  result: Settled per the user's answer as two knobs in one sentence — "Three is a floor on what you take from what already exists — never a reason to invent work — and eight remains the recommended ceiling." BUNDLE_SIZE_ADVISORY is untouched, so no code changes and the envelope note still advises at eight. README.md:1312 needs no edit; it describes the ceiling, which is unchanged.

- item: Correct the issue's own errors on the record before closure — the surface count (8 to 12 for next alone, 40 across all three skeletons), the refuted nine-run streak, the excludeIssues seam already being built and tested, and the stronger all-or-nothing reason; a closure comment that leaves the filed claims standing would preserve the false halves.
  status: done
  dispatched: self, posted to the issue after the chains went green rather than before, since three counts moved under me during the run and I wanted to post once
  result: https://github.com/KaolaBrother/Kaola-Workflow/issues/968#issuecomment-5267912276 — five filed claims corrected on the record: the surface count (wrong three ways), the nine-run single streak that does not exist since bundle-930-931 and bundle-937-938-939 sit inside the range, the excludeIssues seam that is already built and already tested, the weak "not observed" reason where the real one is that closure_policy is a validated audit-consumed contract, and the blocking value call that measurement substantially refuted. Also names what survived unchanged, so the comment is not read as retracting the whole issue.

- item: Add the watch-list row for a bundle member that cannot close while its siblings are finished to docs/decisions/0017-the-mission-list.md, naming the observation that would arm it and the sized mechanism (per-member issue_action, or a finalize --drop-issue), and build nothing.
  status: done
  dispatched: doc-updater, adding one row to the watch-list table in docs/decisions/0017-the-mission-list.md in the worktree at .kw/worktrees/issue-968; the change is visible in git diff docs/decisions/0017-the-mission-list.md
  result: One row added, 20 to 21, 3 columns, no trailing whitespace, no existing row touched. It CORRECTED my brief twice and I verified both: closure_policy is asserted in THREE of the four walkthroughs, not four — the codex copy carries it only as fixture input building a workflow-state.md string at :1802 and :1862 — and the all-or-nothing prose sits on SEVEN surfaces, one skeleton and its six renders, not eight (the eighth grep hit is this run's own CHANGELOG). Its own surface count was then wrong in the other direction and I corrected the shipped row by hand: it reported seven surfaces from the workflow-next family, my earlier count found seven from the finalize family, and the measured answer is that BOTH exist — the closure rule is stated in two different sentences across two skeletons and their six renders each, so the row now says fourteen. README.md:1307's "all-or-nothing" is correctly excluded from both counts: it is claim-time target validation, not closure policy. The Fable review then made the same sizing point a third way, and the row now also names the hand-maintained restatement at README.md:1332 that no generator reaches, so a reader sizing a change to the closure contract sees the whole footprint rather than the generated half. It also found something I had not asked for and that strengthens the row: selection now routes around this case, because the runs-alone test written this run sends an issue needing a user value call to run alone, so the arming case narrows to a member whose non-closeability was not visible at selection time.

- item: Write the CHANGELOG.md entry under [Unreleased], keeping provenance out of the agent-facing surfaces.
  status: done
  dispatched: self — no [Unreleased] section exists (prepare consumes it), so one is created above [9.8.0]; lands in CHANGELOG.md in the worktree. De-hash any background citation: the release --verify gate refuses changelog_unknown_reference on a hashed reference that is not the issue being closed.
  result: CHANGELOG.md — new [Unreleased] section above [9.8.0] with two entries: the default-shape and admission-test change, and the ADR 0017 watch-list row. The bundle lane's original design issue is cited in words, never hashed, so #968 is the only hashed reference in the block. Trailing-whitespace scan clean. Records the provenance the routing surfaces deliberately do not carry — that the same-scope test was dropped before v1 on the grounds that a script refusing a user-named set overrides explicit user intent.

- item: Review the whole diff adversarially using the Fable model as the user asked, looking hardest for new wording that contradicts a neighbouring doc — that is this repo's recurring defect class.
  status: done
  dispatched: two reviewers on the Fable model as the user asked, both over the complete worktree diff — code-reviewer for correctness and contradiction, adversarial-verifier tasked to REFUTE the claim that the change is complete and self-consistent. Both told not to run npm test or the walkthrough, since the suite is spawn-bound and a concurrent run produces false reds. Reports land at kaola-workflow/issue-968/.cache/review-fable-968.md and .cache/refute-fable-968.md
  result: adversarial-verifier REFUTED the completeness claim with two blocking findings, both verified by me and both FIXED. (1) The CHANGELOG said the change reached 12 edition surfaces; measured 18 — the finalize family also renders into all six trees, and I had propagated that same wrong count into the dispatch brief and into what I told the user. (2) next.skeleton Step 3 still presupposed the single-issue run seventy lines below the new default: it recorded "which issue" where init.skeleton now says "which issues", and it led with KAOLA_TARGET_ISSUE, making the shape the same file calls the norm into the swap-in exception. Fixed by pluralizing the claim record sentence, leading with the set, and flipping the nx-claim-run splice in templates/routing/slots.js to --target-issues; both pinned flag substrings survive on all six next surfaces and validate-workflow-contracts passes. Also fixed: this repo's own CLAUDE.md:45 "which issue". Left alone deliberately: the bundle_size_note string ("8 or fewer is the recommended shape") is compatible rather than contradictory, since a three-to-five bundle IS eight or fewer, and the user ruled no code change.

- item: A live comment in scripts/test-bundle-claim.js:1233 calls a bundle "the guarded exception the ranking rules allow", which is stale prose describing the rule this run replaces and is exactly what a future grep for the old rule will find.
  status: done
  dispatched: tdd-guide, which holds the test artifact — I implemented this change so the test file is not mine to write; one comment in the worktree at .kw/worktrees/issue-968, visible in git diff scripts/test-bundle-claim.js
  result: scripts/test-bundle-claim.js:1232-1236, comment only — the parenthetical now reads "a bundle is the norm — a run normally carries three to five issues, each closeable on its own evidence". The sentence explaining why the selection-origin record must be wired into the bundle path is byte-identical; no assertion, fixture, test name or behaviour touched. 196 assertions before, 196 after, exit 0 captured to a file rather than through a pipe. Its own sweep of every test script for stale bundle prose found nothing else: test-opencode-edition.js:1454 "the same scope" is install scope, and test-bundle-claim.js:422-424's "bundle size is ADVICE, not a ceiling" is still accurate since eight remains the recommended ceiling and nothing enforces it. It declined to issue any verdict on the production change, correctly — a green run it did not author is evidence it broke nothing, not certification of my work.

- item: Sweep the one-issue residue both Fable reviewers converged on and that my own sweep missed — init.skeleton.md:316 "A run claims its issue" in the skill region three lines from what I did edit, the finalize keep-open section written entirely in the singular where the mechanism is whole-set, the finalize sink narrative, README's per-issue worktree subsection which was left half-updated, and "which issue, which branch, which worktree" surviving in workflow-state-contract.md and architecture.md after I changed that exact phrase in the consumer template.
  status: done
  dispatched: doc-updater, editing the named anchors in the worktree at .kw/worktrees/issue-968; I regenerate and re-sync afterwards rather than letting it, so surface propagation stays with me. Visible in git diff.
  result: All five anchors fixed in authoring sources only — init.skeleton.md:316 now says "One file per run, not one per issue", the finalize keep-open section is rewritten into three paragraphs that state the whole-run consequence outright (no member closes, including finished ones; every claim released), the finalize sink narrative closes "every issue in the set", README's worktree subsection is per-claim with its heading renamed after a repo-wide check that no link or anchor pointed at it, and the three "which issue" divergences in workflow-state-contract.md and architecture.md now match the consumer template. It then flagged five more in the same rule's footprint and left them to me; I took all five, because they are one rule's wording, not new scope: the fz-issue-closure splice across three forges, README's two "per-issue worktrees" tool mentions, README:82, README:1010 whose sentence contradicted its own path, and README:1297. My own follow-up scan then caught two more it had not listed, README:49 and :869. README:1297 also carried a defect INDEPENDENT of this change — it claimed keep-open "releases the claim exactly like a normal close", and I verified in sink-merge.js:969-978 and :2885-2901 that keep-open releases BOTH artifacts on every member while the close arms deliberately remove the label alone, a marker on a closed issue being inert. Corrected rather than carried.

- item: Re-run the FULL walkthrough after the Step 3 and slots.js fixes — the green 209/209 was measured before those edits, and slots.js is a generator input whose change altered every rendered next surface, so that result no longer covers the tree.
  status: done
  dispatched: self, after every edit had landed
  result: GREEN on the final tree — 209 scenarios ran, 209 passed, 0 failed, shard 1/1, real exit 0 captured to a file rather than read through a pipe, and zero FAILED lines in the full output. This supersedes the earlier 209/209, which was measured before the Step 3 and slots.js edits and therefore covered different bytes.

- item: Run the walkthrough at full scope and the four chains; this is edition-touching prose, so the four-chain receipt is owed.
  status: done
  dispatched: self — walkthrough at full scope started first and alone, since the suite is spawn-bound and running anything else against it produces false reds. Started before the ADR row landed on purpose: the walkthrough reads only docs/api.md and never docs/decisions, measured, so the pending edit cannot change its result. Output at /private/tmp/claude-501/-Users-ylpromax5-Workspace-Kaola-Workflow/d6f5cac1-c850-4e19-99ce-78b4e794a09a/tasks/. Four chains run after every edit has landed, never before, so the receipt binds the finished tree.
  result: FOUR CHAINS GREEN — claude, codex, gitlab and gitea all exit 0, real exit code captured to a file, receipt at kaola-workflow/issue-968/.cache/chain-receipt.json bound to headSha 1d892a56 which IS this branch's HEAD, scope decision "all-four" with reason "edition_coupling", unwaived, and touchedEditionPaths naming CLAUDE.md, README.md and the command surfaces so the chains provably saw this diff rather than an unchanged tree. Trap avoided: I first read .cache/chain-receipt.json at the REPO ROOT and it showed four green chains bound to d5165f7c — this morning's receipt, untouched at 10:32. Passing --project writes into the run folder instead, so the root copy is stale and reading it would have certified someone else's run as mine. Earlier in the same item: walkthrough GREEN at full scope — 209 scenarios ran, 209 passed, 0 failed, shard 1/1, exit 0, 2400 spawns. Four chains still owed, and deliberately held until the two Fable reviews return rather than started now: the receipt must bind the final tree, and in this repo review finds real defects after green suites often enough that starting the chains before the verdict is the more expensive order.
