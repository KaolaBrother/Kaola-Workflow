# Run gaps — manual entries

gap: verification-blind-spot — Neither `sync-opencode-edition.js --check` nor `sync-kimi-edition.js --check` runs in any of the four chains; both live only under `npm run test:kaola-workflow:editions`, which is in neither `npm test` nor the full tier. A skeleton edit that regenerates the 18 tracked surfaces and stops therefore leaves the six edition trees stale with four green chains certifying it. Measured this run: after editing three skeletons, `generate-routing-surfaces --check` reported all 18 tracked surfaces byte-matching while every edition tree still carried the old wording, and only an explicit six-invocation sync propagated it. The installers self-heal (`install-opencode.sh:158-159`, `install-kimi.sh:128-129` run `--check || --write`) but only for the forge passed on the command line.
