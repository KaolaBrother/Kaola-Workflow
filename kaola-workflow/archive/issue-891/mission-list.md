# Remove the workflow's parallelism enforcement — parallel_mode, KAOLA_FORCE_CLASSIFY, and the file-set overlap gate (#891)

- item: cut the overlap axis out of the canonical `scripts/kaola-workflow-classifier.js` — the seven returns at lines 570/574/578/585 (`red`) and 589/594/598 (`yellow`), plus the `parallel_mode` bypass at ~619 and the `readOrCreateConfig` default that seeds it. Line 601's `green` becomes the unconditional outcome of the lane check. **`red` is overloaded: line 710 emits it for a CLOSED issue on the CLI path and must survive** — `claim.js:1125` and `:1749` both depend on it. Keep `owned`, `blocked` (530, 538), `target_unavailable`, `target_unverified`. Also drop whatever overlap-only helpers (scope parsing, area maps, curated root-file list) lose their last caller — but only those, checked by grep, not by assumption.
  status: done
  result: `scripts/kaola-workflow-classifier.js` 863 → 503 lines. `classify()` is now the depends-on check and nothing else. Grep-verified last-caller loss, then removed: `FILE_PATH_REGEX`/`AREA_PATH_REGEX`, `normalizeRepoPath`, `areaForPath`, `extractFilePaths`, `extractCoarseAreas`, `markdownFenceTransition`, `sectionBodyState`, `sectionBody`, `parseAreaLabels(FromText)`, `SHARED_INFRA`, `isSharedInfra`, `PROTECTED_BASENAMES`, `PROTECTED_PATH_MARKERS`, `isProtected`, `scanClaimedOverlap`, `isSafeName`, `readOrCreateConfig`, `CONFIG_PATH`, the `os` require. `claim.js` needed **no** change: it never referenced `yellow`, and both `red` consumers are the closed-issue path the comments already describe. `adaptiveSchema.extractCuratedRootPaths` is left in place — see the follow-ups below.

- item: propagate to the three forge ports — check first whether they are a byte-identical group or rename-normalized ports.
  status: done
  result: `plugins/kaola-workflow/scripts/kaola-workflow-classifier.js` is **byte-identical** to canonical (a `validate-script-sync` group) — copied. gitlab/gitea are **hand-ported and structurally divergent** (own forge module, own helper set) — edited by hand to the same shape, 706 → 459 lines each. `validate-script-sync.js`, `edition-sync.js --check` and `generate-routing-surfaces.js --check` (18 surfaces) all green.

- item: stop the three installers seeding the key — `install.sh`, `install-opencode.sh`, `install-kimi.sh`, plus `install-codex-agent-profiles.js` ×3. A user's existing config carrying the key must be IGNORED, never rewritten.
  status: done
  result: all six seeders removed rather than left writing `{}` — `parallel_mode` was the only field any of them still wrote. `~/.config/kaola-workflow/config.json` stays user-owned and is still read by `sink-pr.js` / `sink-mr.js` for `pr_auto_merge`/`mr_auto_merge`, which create it on demand with their own defaults. Nothing rewrites a user's file, so a stale `parallel_mode` is simply ignored.

- item: delete the tests with the mechanism, never repair them ahead of it. **Never rewrite an overlap assertion into an assert-green.**
  status: done
  result: 17 overlap scenarios out of `simulate-workflow-walkthrough.js` (incl. every fast-summary `## Scope` fence case and `testClassifierSectionBodyFenceIdentity`), the matching blocks out of both forge suites and both forge walkthroughs, the `readOrCreateConfig`/bypass pairs, and the four `seedKaolaConfig` staging scenarios out of `test-install-model-rendering.js`. None rewritten to assert green. **Two sites changed rather than went, both mutation-proven:** the install suites now assert the shared config is *not created* (mutant: a one-line seeder re-added to `install.sh` → AC1 red; restore verified byte-identical), and `testStartupExplicitTargetRedAnswers` keeps its subject and repoints its fixture at a closed issue via `runClaimOnline` + `writeBundleGhMockScript`, because `red`'s only producer moved (mutant: `verdict === 'red'` → `'red_MUTANT'` in `claimExplicitTarget` → scenario red at exit 1; restore verified byte-identical). Five contract validators drop the pins for the deleted exports; `test-kernel-conformance.js` drops the classifier's now-stale non-atomic-write exemption, which its own `dead` half caught.

- item: docs — `README.md`, `docs/api.md`, `docs/opencode-edition.md`, `docs/kimi-edition.md`; `CHANGELOG.md` under `[Unreleased]` as BREAKING.
  status: done
  result: README's "Classifier configuration" becomes "What the classifier decides" — a verdict table plus the rule that concurrency is the runtime agent's call. `docs/api.md` loses the `scanClaimedOverlap` section and the `parallel_mode` config key (keeping a one-line migration note), and two verdict-table rows stop describing overlap. Both edition docs stop claiming the install seeds the config. CHANGELOG `[Unreleased] ### Removed`, marked BREAKING.

- item: prove the gate is actually gone rather than merely defaulted-off, and that the kept refusals still fire.
  status: done
  result: hermetic sandbox, **no `~/.config` present at all**. (1) two projects with an exact file-path overlap on `scripts/kaola-workflow-claim.js` → `{"verdict":"green"}`, and no config file was created by the run. (2) already-claimed issue → exit 2, no stdout. (3) open `depends-on:#55` → `{"verdict":"blocked"}`. (4) closed issue via gh mock → `{"verdict":"red","reasoning":"issue #703 is already closed"}`; a sibling OPEN issue naming the same overlapping file → green.

- item: four chains (edition-touching diff), finalize, sink. Verify no needle survives.
  status: done
  result: four chains green and **unwaived** at `d05421b4`, serial, offline — claude 222s / codex 6s / gitlab 59s / gitea 57s, `scope.chains` all four, no `accepted_red`. The fast gate samples the walkthrough at 1/12, so it was also run at **full scope separately: 183/183, exit 0**. The two additive edition suites (absent from `npm test`) pass: opencode 490, kimi 505. Needle sweep clean — no `parallel_mode` / `KAOLA_FORCE_CLASSIFY` outside `CHANGELOG.md` history, `kaola-workflow/archive/`, the historical `docs/investigations|audits|decisions/D-*` records, and one deliberate migration note in `docs/api.md`.

## Recorded, not fixed — follow-ups

- **The shared-config CAS layer in `install-codex-agent-profiles.js` ×3 is orphaned.** `seedKaolaConfig` was the only caller of `captureAtomicTargetVersion`, and therefore of `atomicWriteSameDirectory`'s `expectedVersion` branch, `assertAtomicTarget`, and the `atomic_stage_conflict` path. Its four tests went with it. The 2-arg `atomicWriteSameDirectory` path stays live and tested (the profile-collision scenarios). Not deleted here: removing a hardening primitive from three installers is a deletion of working capability, which the project's own rule says to escalate rather than do unilaterally.
- **`adaptiveSchema.extractCuratedRootPaths` (+ `CURATED_ROOT_PATHS`, `CURATED_ROOT_LC`) lost its last caller.** It lives in `kaola-workflow-adaptive-schema.js`, the byte-identical ×4 drift anchor. Left in place for the same reason — touching the anchor for dead-code removal is a separate, four-copy change.
- **Coverage that left with the overlap scenarios and was not replaced:** `readActiveFolders`'s closed-issue and `released`-status exclusion was only ever asserted *through* an overlap verdict. That behaviour is unchanged by this issue, and re-covering it is test authoring outside this scope.
