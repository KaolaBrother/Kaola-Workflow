# Close #995 / #996 / #997 — tier what Step 7 files, stop a worktree regen mutating another checkout, and stop an unreadable gap section reading as a measured zero

Worktree: `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-995-996-997`
(branch `workflow/bundle-995-996-997`, base `8deb8eae`). Subagents get absolute `$WT/` paths.

Ordering that matters: #996 lands before #995's regeneration, because #995 must run the very tool
#996 reports as defective. #997 is independent of both.

- item: Trace how `scripts/generate-routing-surfaces.js` resolves its edition-tree root — #996 derived the effect only ("the resolution code was not traced") and guesses a path derived from the git common directory rather than the working tree; also settle its untested second hypothesis, whether `edition-sync.js` and the install scripts share the same seam. Read-only measurement, no fix.
  status: done
  dispatched: investigator — trace + reproduce the #996 resolution seam from the worktree; report lands at `kaola-workflow/bundle-995-996-997/.cache/996-resolution-trace.md` (main checkout)
  result: `.cache/996-resolution-trace.md`. **#996 IS MECHANICALLY A REQUEST TO REVERT #969.** The behaviour is deliberate, commented and tested — commit `9b6fac01` (Thu Aug 13, `Refs #969-#972`) introduced `TREE_ROOT`, `refreshPresentEditionTrees` and the purpose-built `A31` band together. Its body states the intent verbatim: "Canonical sources resolve to the invoking checkout; the generated tree resolves to main." **Both of #996's proposed remedies are refuted by measurement**: relocating the tree to the worktree fails `test-opencode-edition.js:3499` BY CONSTRUCTION (A31 asserts the worktree gains no tree) and A31's own header pre-names it "a no-op wearing a fix's name"; skipping the refresh re-opens #969's observed failure (trees that "died with the worktree", twelve files keeping prose for a renamed flag). **Premise correction: "invisible" is half wrong** — `D0` in BOTH edition suites detects the drift precisely, names the file, prints the remedy, and deliberately refuses to self-repair; it is RED right now. The true narrower claim is that **the detector is not in the four chains**, and that exclusion is itself deliberate (`generate-routing-surfaces.js:352-354`: a chain-resident check reading edition trees would put editions inside `npm test`). Sharpest characterisation (leg 5): the same `--check` exits 1 from main and 0 from the worktree over one tree at one moment — **the drift is invisible from the checkout that caused it and visible only from the one that did not**. Residual real gap (inference I2, medium-high): routing/timing — nothing schedules `D0` after a worktree `--write` driven by a NON-edition diff. Hypothesis 2 answered: exactly two writers exist (`sync-opencode-edition.js`, `sync-kimi-edition.js`); `edition-sync.js` and `install.sh` do NOT hit the seam; both edition installers hit it INTENTIONALLY and are required to (`install-opencode.sh:171` consumes `--print-tree-root` as a program interface).

- item: Make a `--write` run inside a linked worktree stop silently mutating a different checkout; the design fork — resolve the root to the invoking working tree, detect-and-skip with a printed note, or keep writing but say so loudly — is settled from the measurement above, and note `--check` deliberately does not read edition trees so no guard can live there.
  decision: **ANNOUNCE, do not relocate.** Resolution stays exactly as #969 designed it. `--write` from a linked worktree prints which checkout's trees it refreshed and how to verify. Derivation: the only OBSERVED failure is a human unable to tell another checkout changed — the write itself is correct and load-bearing (`install-opencode.sh:171` consumes `--print-tree-root` as a program interface). A printed note is a report, not a gate. Relocating fails A31 by construction and reverting #969 would require deleting a test band, which test custody forbids.
  status: in-flight
  dispatched: implementer — add the announcement to `$WT/scripts/generate-routing-surfaces.js` (+ the two sync scripts if that is where the write is known); lands as the committed diff on `workflow/bundle-995-996-997`
  landed: `sync-opencode-edition.js` +34 and `sync-kimi-edition.js` +32, in `runRefreshPresent`, gated `wrote > 0 && TREE_ROOT !== REPO`, printed to **stderr**. Orchestrator-verified: all five writers already returned counts pre-change (no `NaN` gate), `REPO`/`TREE_ROOT` both defined (`:52`, `:78`), and `--print-tree-root` stdout is exactly one clean path on BOTH scripts, so the installers' program interface is intact.
  dispatched: investigator (trace-996) — ARM the note: prove it fires on a real cross-checkout write and stays silent on an in-parity one. Implementer custody bars fix-996 from proving its own guard. Report appends to `.cache/996-resolution-trace.md`.
  note: **the pure-deletion case in trap 3 was REAL.** The first cut summed `pruneSkills`/`pruneRetired` — deletions — into a count labelled "written", so a prune-only refresh would have claimed it WROTE files it had DELETED. Implementer found it independently while answering the question and renamed `wrote` → `changed` in both scripts, with the comment naming the reason: a refresh can delete from the other checkout and write nothing, "the more destructive half of the same cross-checkout reach". Still to verify: whether `changed` genuinely sums removal counts, or is the same defect with a better label.
  landed-final: both sync scripts FROZEN and `node --check` clean. `changed` scoped to `runRefreshPresent` only (opencode `:809-845`, kimi `:777-812`); every surviving `wrote` is a correctly-scoped local inside a writer. **Q1 verified by READING both prune functions, not inferred:** every `fs.rmSync` in `pruneRetired` (opencode `:756-782`, four deletion loops) and `pruneSkills` (kimi `:647-660`, two loops) is immediately followed by `removed++`, both `return removed`, both feed the sum — so a prune-only refresh fires the note. **Q2:** the note's "now render THIS checkout's canonical sources" is a claim about the RESULTING STATE, not the operation; `runRefreshPresent` runs all writers AND the prune on every present tree, so under deletion-only the prune is precisely what makes the sentence true. No propagation obligation: neither script is in `COMMON_SCRIPTS`, `BYTE_IDENTICAL_GROUPS` or `GENERATED_AGGREGATORS`, and no codex/gitlab/gitea copy exists to propagate to (`validate-script-sync.js` exit 0, `edition-sync.js --check` exit 0).
  carried: **one imprecision, deliberately NOT fixed mid-verification.** kimi's first prune loop removes a retired skill DIRECTORY with `recursive: true` and counts it as 1, so a 5-file skill folder prints "1 file(s) changed". The gate is sound (never zero when something was deleted) but the number is units-removed, not files. Same class as the verb defect just fixed, far smaller. Deferred rather than dropped: changing the wording now would invalidate the verification matrix currently in flight. Revisit after trace-996 reports — a one-word change to "change(s)" makes it unconditionally true.
  status: done
  result: **#996 closed by announcement, fully verified.** Final wording `NOTE — N change(s) in a checkout that is not this one.` + root, trees, and the verify command. Re-verified on the frozen shas after the reword: fires with the new string, 0 `NOTE` occurrences on stdout, both negative postures silent — including the strong one, three files genuinely written same-checkout and correctly no note. Prune-only 5-file-directory posture fires and now reads `1 change(s)`. A31 not re-run, and that was VERIFIED not assumed: zero `scripts/test-*.js` reference `--refresh-present` or either NOTE string, with a live positive control (`print-tree-root` → 2 files) proving the search was not silently matching nothing; the note lives in `runRefreshPresent` (`:807`), disjoint from `runCheck` (`:873`) which is the mode D0/A31 drive. **AUTHORITATIVE closing state:** main's six trees render MAIN's canonical (`8deb8eae`), clause count 0 on all six; both `--check`s exit 0 from main; zero probe artifacts; MAIN `git status` clean but for the untracked project folder.
  ARMED: **the #996 note is mutation-proven.** Firing case on the frozen shas: both scripts exit 0, 3 `generated` lines, **0 `NOTE` occurrences on stdout**, 437/421 bytes on stderr, text matching the briefed string exactly. Negative cases both silent (`TREE_ROOT === REPO`; `changed == 0`). **Deletion claim verified by RUNNING, not by reading** — three prune-only probes, including a 5-file skill directory: the note fires in all three, never prints 0, never stays silent. **`A31` EXECUTED AND PASSED from BOTH checkouts** — first time this session — with proof of execution rather than inference: the verdict line is `test-opencode-edition.js:3781`, the last line of a 3781-line file, while the A31 band opens at `:3314`, so a printed verdict means the band ran. Suites: 663 + 627 assertions, drift-check armed, zero FAIL lines, from main AND from the worktree.
  ledger: **FINAL STATE, measured after the last repair — main's six trees render MAIN's canonical (`8deb8eae`), clause count 0 on all six.** This supersedes every earlier reconstruction, including mine. I was wrong about this ledger three times, each time by reasoning about ordering from agents' reports instead of measuring at the moment I needed the answer, against a mutable resource two agents were writing. The lesson is not "the reports were unreliable" — each was accurate about its own action — it is that **a shared mutable state is only knowable by measuring it at point of use.** After the sink, these trees need regenerating so they pick up the merged clause.
  ledger-superseded: **corrected ordering, reconciled from both agents' evidence.** (1) trace-996 repaired main's six trees from MAIN's canonical — clause absent, both `--check`s green from main. (2) trace-996 then ran its FIRING measurement from the worktree: 3 files changed per script, which is what wrote the tier clause into main's trees. (3) fix-996's `--write` probe ran after that and emitted exactly ONE `generated` line per script — its planted markers only — which proves main's trees were already a byte-exact render of the worktree's canonical at that instant; had they been at main's canonical, the finalize surface would have been rewritten in all three trees per edition and the note would have said 4, not 1. (4) Orchestrator measured: all six carry the clause. So the repair was overwritten by the verification's own firing test, NOT by the implementer's probe — my earlier attribution was wrong and fix-996's refusal to accept it was correct. Current state stands: main's six trees render the WORKTREE's canonical; self-corrects at merge.
  carried: fix-996's first residue check was `awk '/\<wrote\>/'`, which returns EMPTY on macOS awk (no `\<` support) and would have certified any rename state as clean. Caught only because empty was implausible. Fresh instance of "your check can silently not check"; re-run with `grep -nw`.
  note: SERIALIZATION — orchestrator error, second of this run. Asked the implementer about the deletion case while the investigator was measuring the same two files; a run at 20:49:32 against a file written at 20:49:30 produced a transient `ReferenceError: wrote is not defined` at `sync-kimi-edition.js:805`, mid-rename. Investigator correctly diagnosed it as a live-edit race and refused to file it or to report stale firing text. Implementer frozen; investigator holding for an all-clear. Banked as still valid: main's six trees repaired from MAIN, both `--check`s green from main, negative case (a) measured (run from main ⇒ stderr exactly 0 bytes), `--print-tree-root` post-write still one clean 58-byte path on both scripts.

- item: ~~Independent test custody for #996 — prove a worktree-run `--write` leaves main's six edition trees untouched.~~ **OBSOLETE — this item specified the opposite of what shipped.** It was written before the trace established that writing main's trees is #969's deliberate, A31-pinned design; a test asserting the trees stay untouched would fail by construction and would be a test authored against a reverted fix. Superseded by the item below. Recorded rather than deleted, because a successor reading only the surviving items would not know the inversion happened.
  status: done
  result: withdrawn on measurement; see the `996-resolution-trace.md` headline.

- item: Pin the #996 announcement so it cannot silently vanish — the note is the ENTIRE deliverable of #996, and an unpinned behaviour decays exactly the way the ADR wording did. Natural home is the edition suites (`test-opencode-edition.js` / `test-kimi-edition.js`), alongside the `A31` band that already pins the two-root design; the additive-edition rule means this owes its own suite, not a four-chain run. Must cover: fires on a genuine cross-checkout change, silent when `changed == 0`, silent when `TREE_ROOT === REPO`, stderr-only so `--print-tree-root` stdout stays a clean single path. Author must not be fix-996.
  status: done
  dispatched: tdd-guide (guard-995) — author the pin in `$WT/scripts/test-opencode-edition.js` and `$WT/scripts/test-kimi-edition.js`; custody clean, it never touched the note. Unblocked: trace-996 finished and released both suites. Lands as the committed diff on `workflow/bundle-995-996-997`.
  result: **`A34` / `K16`, six legs each**, placed at the end of the existing A31/A32/A33 fixture block — the only place in either suite owning both candidate roots, so no fourth `copyRepo`. Legs: in-parity refresh silent; changing refresh fires; the note names the other root; stderr-only with zero `NOTE` on stdout; **silent from main even though files WERE written**; deletion-only refresh fires. Reached not skipped, proven by count: 663 → 678 and 627 → 642, both exactly +15. **6/6 mutations red exactly their aimed leg**, one at a time, `otherFails=0` and `restored=IDENTICAL` every time. Matches on the literal `NOTE`, the absolute root path (compared against BOTH `/var` and `/private/var` spellings, since the fixture lives under a macOS tmpdir symlink and a bare `===` would red on the platform rather than the property), and one control word. **The count is never asserted** — a prune removes a directory in one call and counts it once. Run hermetically (rsync copy, no `.git`, so `TREE_ROOT` collapses onto `REPO`): opencode 678 assertions exit 0, kimi 642 exit 0.
  refined-by-mutation: two changes the mutations forced, both worth keeping. (1) M5 originally reddened TWO legs, because the deletion leg asserted `includes(NOTE) && namesTheOtherRoot(...)` — a compound. Narrowed to presence only, root-naming pinned once elsewhere. That is "an N-site mutant proves ≥1, never N" applied at ASSERTION granularity, and it was only visible because the mutation over-fired. (2) Its last wording dependency — a `/refreshed \d+ present tree/` control — was loosened to the single word `refreshed`, removing the last place a prose reword could red the suite for no reason. Battery re-run after both changes. Declined the `--print-tree-root` leg with reasoning: a fifth property about a different mode, already owned by A31/A33.

- item: Add the priority-tier clause to Step 7's filing paragraph in `templates/routing/finalize.skeleton.md` (`:237-248`, already inside `<!-- PIN: forge-is-the-backlog -->`, so it needs no new marker) — Step 7 today returns zero matches for `P0|P1|tier|label|priorit` — then regenerate all 18 surfaces through the fixed generator, never editing a rendered surface.
  status: in-flight
  dispatched: self — skeleton clause written now at `$WT/templates/routing/finalize.skeleton.md:238`; the regeneration half is deliberately HELD until #996 lands, because regenerating through the defective generator is the very act #996 reports. Outcome: the committed diff on `workflow/bundle-995-996-997`, and `generate-routing-surfaces.js --check` clean at the end.
  note: the hold did not hold. The #996 reproduction ran `--write` in this same worktree while the skeleton edit was live, so the SIX tracked finalize surfaces (`commands/kaola-workflow-finalize.md`, `plugins/kaola-workflow/skills/…/SKILL.md`, and the gitlab+gitea command+SKILL pairs) are ALREADY regenerated and carry the clause, byte-identical to the skeleton edit. KEPT deliberately, not reverted: "edit the skeleton and regenerate" is the mandated flow and reverting would leave the tree drifted with `--check` red. The #996 fix changes edition-tree ROOT RESOLUTION, not rendering, so the tracked output needs no re-render — only a `--check` before finalize. Outstanding side effect at the TIME OF WRITING: main's six gitignored edition trees carried the uncommitted sentence. **STALE — do not read as current state.** They were repaired from MAIN's canonical later in the run; clause count is now 0 on all six, per the authoritative measurement on the #996 item. The asymmetry consequently FLIPPED: the edition suites are now red in place from the WORKTREE (main's trees are stale against the worktree's canonical) and green from main — leg 5 seen from the other side. Sequencing error by the orchestrator, not by the agent.

- item: Independent guard custody for #995 — the `required-blocks.js` token pinning the new tier clause must not be authored by whoever wrote the prose, and a green suite is not proof it is armed, so mutation-prove it. Guard home is `templates/routing/required-blocks.js` exporting `REQUIRED_BLOCKS`, consumed by `scripts/test-route-reachability.js:670,1012,1099`.
  status: done
  dispatched: tdd-guide — author the `REQUIRED_BLOCKS` entry in `$WT/templates/routing/required-blocks.js`; the orchestrator wrote the prose, so custody is clean. Lands as the committed diff on `workflow/bundle-995-996-997`; mutation proof follows the regeneration in a second message to the same agent.
  result: `templates/routing/required-blocks.js` +34/-16. **Folded into `fn-forge-is-the-backlog`** (13 → 15 tokens) rather than kept as a separate block — orchestrator's call, on the agent's own argument that the block's comment already documents "one per obligation" for this exact span; the fold deleted the only content-led-block-inside-a-PIN-region exception and touched nothing in `test-route-reachability.js`. TWO tokens: the duty (`Give it a priority tier in the same breath`) and the measured consequence (`an issue filed without a P0–P3 label sorts **last** …`), because a block holding only the duty stays green while the consequence decays back to the ADR's wrong "invisible" wording. **Mutation-proven, armed:** 12/12 obligated surfaces red individually (each named in its own failure line); per-TOKEN controls confirm each token fires alone (3 failures each, naming that token); baseline re-proof by fresh `git archive 8deb8eae` extraction = exit 1, 25 failures / 330 passed; final green 331 assertions, all restores md5-IDENTICAL via snapshot-copy in a `finally` (never `git checkout`, which would have discarded the regen).

- item: Correct ADR 0018's own wording at `docs/decisions/0018-the-forge-is-the-backlog.md:264-265` — "invisible to the sorter" does not describe the code: `listOpenIssues` (`claim.js:276-290`) sorts on the tier and neither filters nor truncates, so an untiered issue (tier 99, `claim.js:267-274`) is listed and sorts LAST, beneath every tiered issue.
  status: done
  dispatched: self — edit `docs/decisions/0018-the-forge-is-the-backlog.md` in the worktree at `$WT/docs/decisions/`; outcome is the committed diff on `workflow/bundle-995-996-997`
  result: §8 item 8 rewritten, +7/-2. "invisible to the sorter" → "ranks it **last**, beneath every tiered issue", with a dated inline correction naming #995 and citing `claim.js:281-291` (sorts, never filters or truncates) and `:272-279` (the tier-99 default). Provenance belongs in an ADR, so the correction note stays; the same fact reaches the agent-facing skeleton as the RULE ONLY, with no origin.

- item: Give `parseGapSection` (`scripts/kaola-workflow-gap-sweep.js:234-294`) a way to report rows it attempted to map and could not, so the closure stamp degrades to `unknown` rather than a measured-looking `0`; it must not disturb the strict regex (`:265`, whose comment ends "Do not 'simplify' this quantifier"), the deliberate free-text-bullet silence (`:267-281`, pinned by `test-finalize-door.js` T14's `freetext` leg asserting `follow_ups_filed: '0'`), `samplesMatch`, or either refusal direction (`gaps_unswept`, `observed_gap_unseeded`) — read closed #848 first, the named precedent for absent-vs-present-but-unparseable.
  decision: **BROAD detector, stamping the existing `unknown`, shape (b), keyed on unaccounted `filed: #N` ONLY.** Degrade when the section carries `filed: #N` refs the parse did not account for — ≥1 is enough, not only all-rows-fail, or `issue-500` still stamps 1 against three recorded mappings. **REFINED by measurement after the first cut:** keyed literally on `filed:`/`noise:`/`#N` the rule flags **14** sections, not 6 — the extra 8 lose no filing (their unaccounted text is `noise:` prose) and are correct today; restricted to unaccounted `filed: #N` it flags exactly the measured 6. And the archive scan's own named refuter FIRED: its claim that no prose section mentions an issue number in passing is **false** — `bundle-952-953-954-955`, `issue-877` and `issue-967` all do, so a bare-`#N` key would degrade three correct sections. The harm being fixed is lost FILINGS, so the key is filings; `noise:` rows are dispositions and lose nothing. Derivation: additive derivation requires covering what WAS observed (18 refs, 3 shapes), and the narrow reading is built on a premise that measured zero. `unknown` not a new token, because a new token alters `docs/workflow-state-contract.md:291-296` and unilateral contract changes need escalation. Shape (b) because (c) misses tables entirely and cannot express partial, and (a) silently disarms `observed_gap_unseeded` at `gap-sweep.js:345`. NOT widening the strict grammar — the lazy quantifier is load-bearing on 7 measured rows; whether tables should be legal is a separate value call, filed with the scan as evidence.
  status: done
  dispatched: implementer — `$WT/scripts/kaola-workflow-gap-sweep.js` (1 authored edit) + `$WT/scripts/kaola-workflow-claim.js` stamp branch at `:2463` (3 authored edits: root+codex byte copy, gitlab and gitea hand-ported); lands as the committed diff on `workflow/bundle-995-996-997`
  result: `gap-sweep.js` +41, `claim.js` +19. `countFiledRefs` keys on `/filed:\s*#\d+/g` — the strict grammar's own tail, so a bare `#N` and a `noise:` disposition both read 0. Two accumulation points: non-bullet lines (table rows, wrapped continuations) and bullets that failed the strict regex; free text keeps its measured zero because prose carries no `filed: #N`. Exported as a **non-enumerable** `unaccountedFiled` on the returned array, chosen explicitly so `Array.isArray`/`.length`/`.find`/`.filter` and both refusal reads stay untouched by construction rather than re-audited. Consumer: `computeBacklogDelta` degrades on `filed === null || unaccountedFiled > 0`, reading it as `Number(...) || 0`. **Orchestrator-verified, not taken on report:** door exit 0 / 675 assertions (exactly the count the test author predicted from sandbox), gap-sweep 0 / 173, bundle-finalize 0 / 192; all EIGHT files across four editions carry the change (`scripts/` + all three `plugins/*/scripts/` pairs); `validate-script-sync.js` exit 0 and `edition-sync.js --check` exit 0. **False-positive check measured directly:** the three prose sections that mention issue numbers in passing (`bundle-952-953-954-955`, `issue-877`, `issue-967`) carry NO literal `filed: #N` inside their `## Run gaps` section, so the chosen keying flags none of them.

- item: Independent test custody for #997 — tests for the three-state stamp (absent / measured / unreadable), red before the fix, with T14's free-text leg still green afterwards.
  status: done
  dispatched: tdd-guide — author the fourth T14 leg in `$WT/scripts/test-finalize-door.js` (T14 spans `:2686-2836`); shape-independent because both surviving candidates stamp `unknown`, so it is authorable before the shape is cut. Red on the current tree. Lands as the committed diff on `workflow/bundle-995-996-997`.
  result: `scripts/test-finalize-door.js:2825-2958`, FOUR legs inside T14's four-edition loop, fixtures extracted from the archive **by script and emitted as escaped JS literals, not retyped**, then replayed through the shipped parser: `nosample` (`bundle-904-…-910`), `wrapped` (`issue-500`), `table` (`issue-725`), `prose` (`bundle-587-589`, the negative control). RED on the unfixed tree: exit 1, 40 failures (10 × 4 editions); the `wrapped` leg's `got "512"` is the reachability witness — that number can only come from row 2 of the fixture. Adjacent suites on the unfixed tree: door 1 · gap-sweep 0 (173) · bundle-finalize 0 (192); under the decided rule in a sandbox: door 0 (675) · gap-sweep 0 · bundle-finalize 0. **Two mutants, one at a time:** "degrade only when nothing mapped" reds 12, all `wrapped`; "degrade on any unparsed content" reds 32 including 3 `prose` + 2 `freetext` per edition — so category F is green under the decided rule and provably armed against the over-broad one. The first E-loose leg was replaced outright, not kept: `grep manual:typo` returns 0.

- item: Test #997's own stated hypothesis by scanning archived runs' `finalization-summary.md` for `## Run gaps` sections where the loose pattern matched but the strict one did not; closed #726 is the precedent that individually-unmappable rows do happen, but an all-rows-unmappable section has never been observed.
  status: done
  dispatched: investigator — scan all ~405 archived runs with the real regexes imported from gap-sweep.js, not retyped; report lands at `kaola-workflow/bundle-995-996-997/.cache/997-archive-scan.md` (main checkout)
  result: `.cache/997-archive-scan.md`. **#997 is confirmed in substance and REFUTED in its proposed test.** The test it names — "loose pattern matched but strict did not" — measures **ZERO** across all 154 consumer-visible summaries; the parser's own advisory at `:275` has never fired in the archive. So **any fix keying `unknown` off the existing advisory would change nothing.** But the underlying harm is real and ~2x larger than filed: **6 sections silently lose 18 `filed: #N` refs**, in three shapes the parser AND its warning are both blind to — (1) E-silent, rows with no `(<sample>)` group, e.g. `- manual:relative-plan-receipt-placement: filed: #911` ×5 in `bundle-904-…-910`, 9 refs total; (2) D-lossy, a correctly-shaped row WRAPPED across physical lines, `issue-500`, 1 ref, stamps 1 where the operator recorded 3 mappings; (3) B-table, the section written as a **markdown table** with zero bullets, `issue-725`, 8 refs — indistinguishable from an empty section, so the `0` measures nothing. Consequence for the shape call: **candidate (c) is dead** — it fires only when bullets were attempted, so it misses B-table (no bullets at all) and cannot express D-lossy (partial) by construction. The working discriminator the scan identifies is **"the section carries `filed:`/`noise:`/`#N` text the parse did not account for"**, which flags all 6 real cases and none of the 42 benign prose sections or 3 free-text sections. Also: #726 has zero survivors and its lazy quantifier is load-bearing on real data (7 rows across 3 runs parse today with a `)` inside the sample; a negated class would break all seven).

- item: Produce the design brief for #997's three-state stamp — map `parseGapSection`'s callers and every consumer of its `null`-vs-array contract, read closed #848 as the absent-vs-unparseable precedent, and lay out the candidate shapes with what each would break. Reading and mapping only; the shape call stays with the orchestrator.
  status: done
  dispatched: code-explorer — map the parseGapSection contract and its consumers; report lands at `kaola-workflow/bundle-995-996-997/.cache/997-design-brief.md` (main checkout)
  result: `.cache/997-design-brief.md`, 573 lines. Decisive findings: (1) **#848's code no longer exists** — it lived in `adaptive-node.js`/`plan-validator.js`, both deleted; it survives only as `CHANGELOG.md:2895`, and that entry is a SELF-CRITICISM ("the error class did not shrink, it moved"; "the truthful fix — distinguishing at the emit site — is NOT done"). So #848 is evidence FOR an in-parser distinction and AGAINST relabelling at a distance. (2) Candidate (a) `{rows,unmappable}` **silently disarms `observed_gap_unseeded`** at `gap-sweep.js:345` (`undefined > 0` is false) and throws uncaught at `:407` and `claim.js:2463` — rejected. (3) Candidate (d) caller-re-scan contradicts this repo's own written argument at `claim.js:29-32` ("a second spelling would count filings the gate does not") and "one rule, one wording" — rejected. (4) Live fork is **(b) array+property** — refusals untouched *by construction*, 1 gap-sweep + 3 claim.js authored edits — versus **(c) reuse `null`** — 1 edit, ZERO claim.js edits, but makes the gate's stderr say "section absent" for a present section and **can never express partial malformation**. (5) **The existing suite does not discriminate between any of the four shapes** — the new T14 leg goes green under all of them, so tests cannot decide this; design must.

- item: Verify the bundle — walkthrough at full scope (the fast gate samples a rotating 1/12 shard, so a green gate is not a verified suite), then the diff-scoped chains at finalize; this diff touches `scripts/` and `templates/routing/`, so expect all four.
  status: done
  dispatched: self — `node $WT/scripts/simulate-workflow-walkthrough.js` at FULL scope (not the 1/12 shard), run in background from the worktree. Held until now deliberately: the pin added test code, so any earlier run would have been stale. Outcome recorded inline here as its exit code.
  result: **walkthrough GREEN at full scope, exit 0**, and the scope is proven by the suite's own shard census rather than asserted: `##KW-SHARD {"suite":"simulate-workflow-walkthrough","index":1,"total":1,"scenarios":184,"ran":184,"passed":184,"failed":0}` — `total:1` is what distinguishes this from the fast gate's rotating 1/12 sample, and `ran == scenarios` rules out a silent skip. Zero FAIL lines in 154 log lines; spawn census 2171. Seven consolidated pre-finalize checks, each exit 0 read separately, never aggregated: `generate-routing-surfaces --check` (all 18 surfaces byte-match the skeleton — so the tracked regen is clean despite main's edition trees being deliberately behind), `test-route-reachability` 331, `test-finalize-door` 675, `test-gap-sweep` 173, `test-bundle-finalize` 192, `validate-script-sync` (4 Oracle Kernel copies identical at HEAD), `edition-sync --check`. The four chains run at finalize; this diff touches `scripts/` and `templates/routing/`, so it is edition-touching and owes all four.

- item: Step 7 run gaps — file each with a `P` tier, per this bundle's own new rule. Measured and ready: (1) `scripts/test-route-reachability.js:1008` reads "Blocks legitimately led by a plain content token (9 of the 30 today)"; measured at `8deb8eae` and identically on the folded tree, it is **10 content-led of 19 total** — BOTH figures wrong, denominator off by 11. Hedged "today" and checked by nothing, so descriptive drift, not a disarmed guard; this bundle changes neither figure. (2) Whether a `## Run gaps` section may legally be a **markdown table** — `issue-725` wrote one with seven filings and the parser cannot see it; the #997 fix degrades it to `unknown` rather than reading it, so "should the grammar accept what operators keep writing" is a live value call with the archive scan as evidence. (3) From the #996 trace, inference I2: nothing schedules the edition-drift detector `D0` after a worktree `--write` driven by a NON-edition diff — dispose as `noise:` with justification, since the announcement fix addresses the observed half and the remainder only bites a reader who ignores the note. (4) near-miss `## Run gaps` headings — **NO LONGER A HYPOTHESIS; MEASURED at finalize rather than filed unmeasured.** `/^## Run gaps\s*$/` requires the heading to stand alone, and a sweep of all 127 archived summaries carrying any `## Run gaps`-like heading finds **exactly one** that fails it: `bundle-625-626`'s `## Run gaps (reviewer-recorded, non-mechanical)`. That section carries **zero** `filed: #N` refs — all three rows are `noise:` dispositions — so it loses no filing. It classifies as section-absent and stamps `unknown` where the truthful answer is a measured `0`: a real mis-stamp, but in the CONSERVATIVE direction, the opposite of #997's. Worth filing precisely because `bundle-625-626` lost nothing only by happening to file nothing; the same heading form over a section that did file would be invisible too.
  status: in-flight
  dispatched: self — Step 7 of finalization; each filing carries a `P` tier per this bundle's own new rule.

- item: Restore the MAIN checkout's six edition trees, which currently carry this worktree's uncommitted clause and hold `D0` RED in both edition suites. Repaired from the MAIN checkout by `sync-opencode-edition.js --refresh-present` and `sync-kimi-edition.js --refresh-present` — never from the worktree, which would re-render the same drift. Do it AFTER the sink, so the trees regenerate from a canonical that already carries the merged clause; verify by reaching band `A31`, which never executed this run because both suites abort at the earlier `D0`.
  status: todo

- item: Dock the documentation — `CHANGELOG.md` under `[Unreleased]`, and `README.md` / `docs/api.md` / `docs/architecture.md` only where this bundle actually changes what a reader is told.
  status: in-flight
  dispatched: self — CHANGELOG entries for #995 and #997 written directly into `$WT/CHANGELOG.md` under the single existing `### Fixed`; #996's entry and the `docs/api.md` row were written by the implementer. Outcome is the committed diff.
  status: done
  result: four surfaces docked. `CHANGELOG.md` — #996 by the implementer, #995 and #997 by the orchestrator, all under the single `### Fixed`. `docs/api.md` — the `sync-*-edition.js` row, by the implementer. **`docs/workflow-state-contract.md:296` — a find I nearly missed: it said "a lane that could not LOCATE that section stamps `unknown`", which is the OLD two-state semantics, i.e. #997's exact conflation restated in the contract doc. We added no token, but we widened what `unknown` MEANS, and that widening is documented state. Now records present-but-unread and the partial case, and states that prose and free-text keep their measured `0`.** `README.md` and `docs/architecture.md` — CHECKED, no change owed: architecture has zero hits on any affected symbol, and README documents the SELECTION sort order (`P0`→tier 0 … other→tier 99, which is accurate and unchanged) while saying nothing about the filing duty #995 added.
  landed: both entries in. **Deliberately ONE `### Fixed` heading** — the duplicate-heading failure mode this section shipped with in 9.10.0 is avoided by adding to the existing heading rather than opening a second. #997's entry records that the hypothesis AS FILED measured zero, because a reader who later finds the loose advisory never firing should not conclude the fix was pointless. Still to check at docking: whether `README.md` or `docs/architecture.md` tell a reader anything this bundle changed — expected no for both, since no structure moved and the install surface is untouched.
