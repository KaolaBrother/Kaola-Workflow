# Run gaps observed by this run, seeded for the scanner

gap: finalize-surface-omits-scanner — the finalize surface splices only the --check gate, never the --json scanner run that produces run-gaps.json
gap: chains-stale-culprit-absent — finalize --check --json reports validation chains_stale as a bare token with no culprit paths, so the reason must be diagnosed by reading the hash producer
gap: docs-commit-stales-chain-receipt — on a self-host repo a docs-only commit changes the code-tree hash, so documentation docking after Step 1 validation stales a green four-chain receipt
gap: t14-t20-citation — T14 is cited as the free-text-bullet silence pin, but the silence is T20 in a different suite
gap: docs-index-omits-gap-sweep — docs/README.md indexes no gap-sweep section, leaving the only reader-facing grammar description unindexed
gap: manifest-assertion-count-insensitive — adding a four-token manifest block left the suite assertion count unchanged, so the count cannot witness a new pin
gap: scanner-owns-grammar-phrase — the finalize surface still defers with "whose grammar the scanner owns" a short way below the statement that now gives it
