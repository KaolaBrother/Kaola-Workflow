# #927 — make opencode subagent effort tiers actually reach the model, with both tiers still inheriting the main session's model

- item: Premise-check the issue's code anchors before building on them — that `CONTRACT_EFFORT_TABLE` in `kaola-workflow-adaptive-schema.js` really stores `{variant, options}` per tier, that `sync-opencode-edition.js` emits `variant` around :672 with the prose claims at :641/:651, that the `provider.*.variants` block at :657–:668 exists and is dead once options carries the tier, that `reasoningRoles()`/`standardTierRoles()` are the single source of tier assignment, that `plugins/kaola-workflow-hooks.js` exists and carries a `tool.execute.before` hook of the `(input, output)` shape the design reuses, and that the installer's `SWITCHED YOUR OPENCODE MODEL?` warning is where the issue says. Read-only; a refuted anchor changes the build.
  status: done
  dispatched: one `investigator` subagent over the Kaola-Workflow repo anchors; report to land at `kaola-workflow/issue-927/.cache/premise-repo-anchors.md`
  result: `kaola-workflow/issue-927/.cache/premise-repo-anchors.md`. 8 of 10 CONFIRMED byte-exact at commit c3938174. Two corrections that change the build: (a) the hook file is NOT `plugins/kaola-workflow-hooks.js` — the hand-written source is `templates/opencode/plugins/kaola-workflow-hooks.js`, byte-copied into THREE rendered surfaces `.opencode/`, `.opencode-gitea/`, `.opencode-gitlab/plugins/`, so the template is the only legal edit target; (b) the installer string is `Switched your opencode model?` at `install-opencode.sh:433` inside `seed_config`, not the all-caps form the issue quotes. Also found: `sync-opencode-edition.js` has a SECOND emit path `renderNeutralConfig` that does emit `model` when env-var pins are set — the issue only describes `renderAdaptiveConfig`. Tier names are really `top`/`second`; `CONTRACT_EFFORT_TABLE` at `:144-161` keys 4 contracts each `{variant, options}`; suite is `npm run test:kaola-workflow:editions`, and `test-opencode-edition.js:659-762` (block A12) asserts only `agent[role].variant`, nothing about `options` or absent `model`.

- item: Premise-check the load-bearing mechanism claim against the opencode 1.18.11 source on this box — that `agent.<role>.options` merges into every call with no `agent.model` gate, that `chat.params` receives the resolved model/provider and its `output.options` is actually consumed downstream, and that TaskTool's `b.model ?? {parent modelID, providerID}` fallback gives subagents the parent's model. The whole design rests on these; the issue read them from the binary and rows 3–7 measured the effect, so confirm the code path rather than re-measure. Read-only.
  status: done
  dispatched: one `investigator` subagent over the installed opencode 1.18.11 binary/source on this box; report to land at `kaola-workflow/issue-927/.cache/premise-opencode-mechanism.md`
  result: `kaola-workflow/issue-927/.cache/premise-opencode-mechanism.md`. Read from the real 1.18.11 Bun-compiled binary by extracting the embedded `__BUN` segment. A/B/C/D/E/F/G all CONFIRMED near byte-exact. **The load-bearing claim holds**: merge order is provider → `model.options` → `e.agent.options` → variant-slice options, and nothing in that function references `agent.model` at all; `output.options` was traced through to `providerOptions: a.params.options` on the real model API call, so it is consumed, not discarded. `agent.<name>.options` is schema-accepted (`options: D.optional(D.Record(D.String, D.Any))`, and `AgentConfig` is an open `StructWithRest` folding unknown keys into `options`) — the "schema might reject it" risk is refuted. Two things to build against: `input.agent` in `chat.params` is a plain STRING agent name (the issue's `TIERS[input.agent]` lookup is therefore correct as written), and `input.provider?.id` is valid. **Unresolved**: whether a THROWING `chat.params` hook kills the session — `Plugin.trigger` wraps hooks in a bare `v.promise(...)` with no `catch`, unlike every lifecycle call around it. Treat as hostile: the hook must not be able to throw.

- item: Author the test suite for the opencode edition proving the effort tier reaches a SUBAGENT and not merely a primary session — the acceptance surface is the four config-shape criteria (options payload emitted per tier, no `model` key on any role, `variant` gone rather than left beside `options`, sidecar tier map single-sourced from the role helpers) plus the plugin hook's per-call contract resolution. Test custody: whoever implements does not author this. Must be red on the current baseline. Design settled in `kaola-workflow/issue-927/.cache/design-brief.md` — one wording, every role reads it.
  status: done
  dispatched: one `tdd-guide` subagent, working in the worktree `.kw/worktrees/issue-927` on branch `workflow/issue-927`, authoring into `scripts/test-opencode-edition.js` (and any installer-drift test surface it finds); runs alone so its red-on-baseline proof is against untouched source; report to land at `kaola-workflow/issue-927/.cache/test-authoring.md`
  result: `kaola-workflow/issue-927/.cache/test-authoring.md` (+ red log `test-authoring-red-run.txt`). 326 assertions added to `scripts/test-opencode-edition.js` (+608/−38, only file touched); **232 red on baseline c3938174**, 563 passed, and `test-kimi-edition.js` still green at 507 — so the whole red is the new surface. Deleted 11 sites / 23 assertions of old A12 that pinned `agent[role].variant` and the `provider.*.variants` block: the mechanism is being removed, so its pins go with it, re-asserted against `agent.<role>.options`. Hook driven directly from the shipped plugin in an ESM child harness over 23 cases including the load-bearing same-tier/two-providers case and 5 no-throw cases. Installer drift tested via the repo's existing hermetic-installer convention and deliberately does **not** pin the opt-in flag's name. **The author raised a real design defect**: Layer 2's spread-merge cannot close the wrong-contract exposure, because Layer 1's payload is already inside `output.options` when the hook runs — folded into the brief as addendum 1, together with the badge-block false-mechanism claim (addendum 2) and the ruling that `CONTRACT_EFFORT_TABLE.variant` stays (addendum 3).

- item: Implement Layer 1 in `sync-opencode-edition.js` — emit the tier's `options` payload instead of the `variant` name, delete the now-dead `provider.*.variants` block, and correct the false mechanism claim at :651 and the prose at :641 so they state the result rather than the method.
  status: done
  dispatched: implementer A (see next item — Layers 1 and 2 are one coherent change in one file set and went out as a single dispatch)
  result: done as part of implementer A's dispatch — `renderAdaptiveConfig` now emits `agent.<role>.options`, the `provider.*.variants` block is gone, no `variant` or `model` key survives on any role, and the `:641`/`:650-656` prose plus 5 further stale mechanism-claim comments now state the result. `renderNeutralConfig` untouched, as scoped.

- item: Implement Layer 2 — generate the `effort-tiers.json` sidecar from `reasoningRoles()`/`standardTierRoles()` and add the `chat.params` hook to `plugins/kaola-workflow-hooks.js` that resolves `contractForProvider` + `CONTRACT_EFFORT_TABLE` against the model actually in use per call, then retire the installer's `SWITCHED YOUR OPENCODE MODEL?` regeneration warning and the staleness class behind it. Layer 1 alone ships a wrong-contract payload as a live provider error, so Layer 2 is not optional.
  status: done
  dispatched: one `implementer` subagent ("A") in the worktree `.kw/worktrees/issue-927`, owning `scripts/kaola-workflow-adaptive-schema.js` (+ its ×4 copies via `edition-sync.js --write`), `scripts/sync-opencode-edition.js` and `templates/opencode/plugins/kaola-workflow-hooks.js`; explicitly NOT `install-opencode.sh`, which is implementer B's; reads `.cache/design-brief.md` incl. its addendum; judged by the tdd-guide suite it may run but never edit; report to land at `kaola-workflow/issue-927/.cache/impl-a-generator-plugin.md`
  result: `kaola-workflow/issue-927/.cache/impl-a-generator-plugin.md`. Sidecar written by a new `--write-effort-tiers-to <path>` flag; `chat.params` hook added to the plugin template, reusing a generalized `deployedPath()` (was `hookPath()`) for the project-then-global walk rather than a second scheme; addendum 1 honoured — the hook clears every other contract's knob, derived from `CONTRACT_EFFORT_TABLE` itself, so a stale Layer 1 payload cannot ride along. The provider→contract regex chain became ordered DATA (`PROVIDER_CONTRACT_RULES`) that `contractForProvider` itself consumes and the sidecar serializes, so the function and the sidecar cannot diverge; a differential run proved `contractForProvider` behaviour unchanged over **205,832 inputs, 0 mismatches**. ×4 anchor re-synced. Mutation proofs both ARMED: deleting the emitted `options` → 76 red; breaking the other-contract clearing → exactly the 2 stale-knob assertions red. **Left open by A, deliberately**: the badge heading still reads `## Effort Variant Resolution` because renaming it would silently disarm S2, whose anchor is that literal string — routed as its own item below rather than disarmed quietly.

- item: Mutation-prove the guard — deleting the emitted `options` payload must turn the suite red, and prove arming and coverage separately; a green suite is not proof the tier is armed, and a mutation control can share the code's own assumption.
  status: done
  dispatched: self — judging the two implementers' own mutation proofs and re-running every suite myself rather than taking their word
  result: 7 mutation proofs across the two implementers, all ARMED, each on a scratch mirror with a positive control and a verified restore — never `git checkout --`, which would have destroyed the other's concurrent work in the shared worktree. Arming and coverage were proven separately: B's M4 (a flag named in the report but inert) and M1 (blinding one direction of the drift check while the other stayed green) are exactly the controls that separate *named* from *works*. Independently re-run by me in the worktree with real exit codes read directly, never through a pipe: opencode edition **811 assertions exit 0** (3 trees in parity), kimi **exit 0**, walkthrough **exit 0**, `validate-script-sync` **exit 0**, `generate-routing-surfaces --check` **exit 0**. Implementer B's transient `synthesizer`-in-`second` red did not reproduce once A had settled. One honest limit A recorded and I am not papering over: a hand-typed sidecar tier list that happens to be correct today would still pass — no static suite can see that.

- item: Settle the secondary defect — `seed_config` in `install-opencode.sh` preserves an existing `opencode.json` and never regenerates, so this box's config carries three retired roles and is missing `investigator` and `metric-optimizer`, and nothing detects the drift. Decide whether the fix is regeneration, drift detection, or both; rewriting a user's existing config is value-laden, so put the call to the user before taking it. **User ruled 2026-08-03: detect the drift, warn with what drifted, and act only behind an explicit opt-in flag — never overwrite the user-owned config silently.**
  status: done
  dispatched: one `implementer` subagent ("B") in the worktree `.kw/worktrees/issue-927`, owning `install-opencode.sh` ALONE — both the drift detection with its opt-in flag and the deployment of implementer A's `effort-tiers.json` sidecar across project and global layouts; the seam (`--write-effort-tiers-to <path>`, sidecar at `<config>/kaola-workflow/effort-tiers.json`) is fixed in `.cache/design-brief.md` so A and B never touch one file; report to land at `kaola-workflow/issue-927/.cache/impl-b-installer.md`
  result: `kaola-workflow/issue-927/.cache/impl-b-installer.md`. Opt-in flag is **`--adopt-config`**, spelled once in `ADOPT_CONFIG_FLAG` and read by both the parser and the drift report, so the report cannot name a flag the parser would reject. Drift reporting names extra and missing roles separately and the default path stays non-destructive; the `Switched your opencode model?` warning is retired with the staleness class behind it. Sidecar deployed in both layouts and removed on uninstall. Five mutation proofs, each with a positive control and a `cmp`-identical restore: blinding the missing-role half → 3 red with the extra half still green (the two directions are independently covered), suppressing the report → 7 red, removing the sidecar deploy → 21 red (measuring the `--no-scripts` hazard), a flag named but inert → 1 red (separating *named* from *works*), over-firing → the negative control red. B also caught its own first draft announcing a sidecar that was never written, because the generator prints usage and exits 0 for an unknown mode — now gated on a non-empty temp file. **Open, and a real disclosure question**: `--adopt-config` rewrites the whole config file, not only the `agent` block, so adopting discards a user's model pins.

- item: Satisfy the live acceptance oracle if it is reachable on this box — two subagents on one inherited model with nothing pinned, measurably running at different effort, read as `tokens_reasoning` in `opencode.db`. `opencode debug agent` is explicitly not sufficient: it stayed green throughout the entire failure. If a real provider call is not available, say so plainly rather than substituting a config assertion. **User ruled 2026-08-03: yes, run the live measurement** — it costs real provider tokens and that is accepted.
  status: done
  dispatched: self — measuring against a PROJECT-scoped install into a scratch dir with `OPENCODE_CONFIG_DIR` pointed at a temp path, so the user's own `~/.config/opencode/` is never touched to satisfy a test; raw evidence to land under `kaola-workflow/issue-927/.cache/live-oracle/`
  result: `kaola-workflow/issue-927/.cache/live-oracle/README.md`. **Criterion 1 is MET on the oracle it demands.** Probe A1 isolated Layer 1 by removing the sidecar (the hook no-ops by design) and setting `implementer` to thinking-disabled: two subagents, one parent, one inherited `glm-5.2`, nothing pinned on either — **planner 350 reasoning tokens vs implementer 0**, the only difference being the `options` payload. Probe A2 then set the static config and the sidecar to disagree on purpose — config said enabled 16000, sidecar said disabled — and measured **implementer 0**, so the `chat.params` hook applies on a real call and beats the sync-time payload, which is the whole reason Layer 2 exists. Probe B measured the shipped pair at 305 (top) vs 182 (second). **Stated as a limit, not buried**: probe B is a single stochastic sample and a budget is a cap, so the tiers are proven to APPLY but their numeric separation on ordinary prompts is not proven; the primary session reasons without any `options` at all, so a non-zero count alone would have proved nothing and only the controlled contrast is evidence.

- item: Adversarially review the finished diff — every previous bundle found real defects after every suite was green, and this one changes a config surface whose failure mode is silent. Check especially that no role emits a `model`, that no dead `variant` key survives anywhere, and that the plugin hook cannot throw and take the session with it.
  status: done
  dispatched: two independent lenses over the same diff, both read-only, both in the worktree — one `code-reviewer` for correctness/regression/scope across the whole change, and one `adversarial-verifier` tasked to REFUTE the run's headline claim rather than confirm it, with uncertainty counting against the claim; reports to land at `kaola-workflow/issue-927/.cache/review-code.md` and `.cache/review-adversarial.md`
  result: `.cache/review-code.md` and `.cache/review-adversarial.md`. **The adversarial lens REFUTED the run's headline claim** and its findings drove everything after: the plugin's named exports breaking opencode's loader, the hook replacing options opencode itself set, the contract resolved from brand id rather than API contract, and — decisively — that `zhipuai-coding-plan` routes through `@ai-sdk/openai-compatible`, so the shipped budget split had no demonstrated effect on the only provider ever measured. The code lens independently measured two installer regressions: `--adopt-config` run twice producing a config with zero role payloads while printing success, and the drift detector silent under `OPENCODE_CONFIG_DIR`. **It also caught my own error** — probe A2's "the hook demonstrably applies" was written against logs carrying a plugin-load failure I had not read. All findings were routed and fixed; one (`google-vertex-anthropic`) was later refuted by implementer A on measurement and not forwarded. Both installer regressions and all three plugin defects are fixed and re-measured.

- item: Dock the documentation — `CHANGELOG.md` under `[Unreleased]`, the opencode edition's own docs and README surfaces, and the design record at `docs/investigations/2026-08-03-opencode-inherited-effort-tiers-design.md` if the build diverged from it. Write CHANGELOG before any receipt run.
  status: done
  dispatched: one `doc-updater` subagent in the worktree owning `CHANGELOG.md`, `README.md`, `docs/opencode-edition.md`, `docs/kimi-edition.md` and the design record — the stale lines both implementers named; touches no script and no test; report to land at `kaola-workflow/issue-927/.cache/docs.md`
  result: four rounds, `.cache/docs.md`. Post-pivot: `CHANGELOG.md [Unreleased]` rebuilt around Removed rather than Added; `docs/opencode-edition.md` net −213/+293 with **154 lines deleted outright** rather than softened; `README.md`, `docs/kimi-edition.md` (whose whole "kimi inherits, opencode tiers" contrast is gone), the audit note, and the design record flipped to BUILT, MEASURED, THEN REMOVED with its body left unedited and probe C recording why. **The agent declined an instruction for the second time in this run and was right again**: I had told it the loader bug took the dispatch-log and compaction hooks down, and it refused, citing a reproduction showing the hooks registered before the throw. I reproduced it myself against this box's installed v9.4.2 plugin — namespace order `default`, `findRoot`, `hookPath`; `default` registers both hooks, then the named exports throw, one hook table collected. **The hooks worked all along**; they survive only by an accident of export sort order. My claim was an over-read of the adversarial report, which had said plainly that they survive. Corrected in `.cache/pivot-brief.md` and to the user. Fifth round repaired `D-610-01`'s four citation sites with **three different dispositions** — broken pointers re-cited by subject rather than by a title that had moved twice, present-tense claims about deleted functions past-tensed (a site it had judged "narrative past, leave it" in round 2 and reported getting wrong), and the decision's own consumer inventory kept verbatim but scoped, since the rule it encodes survived all four deletions and only the inventory moved. It verified `D-610-01` still stands rather than assuming (`normalizeTier` is live, called at `dispatchEffort:103`). On `D-544-01` it added a dated measurement note under three self-imposed restraints — no verdict on the decision, no overclaim past what was measured, and an explicit line for what the measurement does not reach — then found, beyond the brief, that **the replacement repeated the defect it was written to fix**: D-544-01's own words are "keyed on brand name, not on the API contract the provider actually speaks", and the shipped `contractForProvider` matched on the provider *id*. Same defect, different wrong answer, same provider — verified against `git show HEAD`, not inferred from prose.
  result: `kaola-workflow/issue-927/.cache/docs.md`, two rounds. `CHANGELOG.md [Unreleased]`, `README.md`, `docs/opencode-edition.md` (31 hunks + two new sections), `docs/kimi-edition.md`, the design record, `docs/audits/opencode-edition-audit.md` and a repaired citation in `docs/decisions/D-610-01.md`. Corrected beyond the brief: the top-tier role list wrongly named 6 roles instead of 5, and a documented merge recipe would have thrown on its own guard now that no `provider` block is emitted. **The doc agent declined one instruction and was right to.** I had said to rewrite the audit's stale measurement; it refused, because those lines are a dated June measurement against a June-fetched schema, and restating them would assert that the June audit validated an August key — fabricating provenance, a worse defect than the staleness. It wrote a supersession note instead, carrying the sentence that matters most in that file: **schema-accepted was never the same as applied**, and no key-presence check would have caught this. I upheld the refusal. It also caught its own round-1 claim going false when B's backup landed, and verified the difference at source rather than inferring it.

- item: The agent-facing badge heading still reads `## Effort Variant Resolution`, naming a mechanism that no longer exists, and it renders into every opencode agent file. Implementer A correctly refused to rename it unilaterally because S2's assertions anchor on that literal string and a rename would have disarmed them with no red. Custody says the test author re-anchors first, then the implementer renames — the heading should name the result the way its body now does.
  status: done
  dispatched: `tdd-guide` re-anchors S2 onto the result-named heading first (making it red), then implementer A renames the heading in `scripts/sync-opencode-edition.js`; sequential inside this item, both in the worktree; report to land at `kaola-workflow/issue-927/.cache/badge-heading.md`
  result: `kaola-workflow/issue-927/.cache/badge-heading.md`. Heading is now `## Effort is configured, not passed` — it states the block's two facts as a result and names no machinery, so it cannot rot the way `variant` did. **The test author found the real defect while re-anchoring**: S2's old `if (sec !== null)` deleted three assertions in silence whenever the heading moved, so the guard that was supposed to police this prose could evaporate with no red — exactly the failure shape of the bug this whole issue is about. Presence is now asserted in both directions against an expectation derived from the canonical source, with a corpus-vacuity guard, plus an anchorless body-wide sweep for the mechanism word across every generated agent and command file. Implementer A applied the rename and fixed the two comments that went stale with it. A measured the sweep independently of the suite: **51 generated files across all three opencode trees, 0 hits**, new heading present in all three. A also ran one extra mutation the natural red could not give — correct heading, mechanism word reintroduced in the body → 2 red with the locator assertion *passing*, proving the scoped check and the anchorless sweep each fire on their own.

- item: `--adopt-config` rewrites the whole `opencode.json`, not only its `agent` block, so adopting silently discards a user's model pins. The user's ruling was that an explicit opt-in flag may act, and it may — but a flag whose destruction is not disclosed is a trap, and the drift report currently names the flag without saying what it will cost. Make the report state what adopting replaces before the user runs it.
  status: done
  dispatched: implementer B resumed via SendMessage (agent `aa9a1799742c7415b`, keeping its `install-opencode.sh` context) to add the disclosure to the drift report; report appended to `kaola-workflow/issue-927/.cache/impl-b-installer.md`
  result: the drift report now states that adopting replaces rather than merges, that hand edits and model pins go, and where the previous file was copied to; `usage()` and the header comment say the same thing as the runtime output. Implementer B went further than asked and added the backup itself, then **caught a real defect in its own first cut by measuring instead of assuming**: a second-granularity timestamp meant two adoptions inside one clock second wrote the SAME backup name, so the second overwrote the user's original with the generated config — a reassuring `.bak` containing nothing worth recovering. `config_backup_path()` now never returns an existing path, re-measured with three adoptions inside one second. A backup that cannot be written exits 1 and leaves the config alone. B then reported honestly that its own mutation M6 (neuter the backup AND delete the disclosure) leaves the suite green — so the backup and the disclosure are unasserted, routed below to the test author who owns that artifact.

- item: The `--adopt-config` backup and its disclosure are unasserted — implementer B measured that neutering the backup and deleting the disclosure together leaves the suite green at exit 0, and said so rather than letting a green suite imply coverage. Pin the result, not B's wording: the backup exists and matches the pre-adopt config, a second adoption inside one second does not clobber the first, an unwritable backup aborts instead of replacing, and the report discloses the replacement before the user runs it.
  status: done
  dispatched: the same `tdd-guide` that holds `scripts/test-opencode-edition.js`, resumed via SendMessage (agent `aab0cc2d7354ed326`) with B's own M6 mutation as the positive control — if the new assertions do not turn M6 red they are not armed; report appended beside `kaola-workflow/issue-927/.cache/badge-heading.md`
  result: A28 added, four claims, none pinning B's mechanism — the flag discovered from the report, the backup found **by content** rather than by name, and the promised path matched as a shape lifted from the report itself, so a backup named by PID or hash would still pass; what it may not do is lose the file. The second-adoption case is testable only under an adversarial clock, so the author **froze `date` on `PATH`** with a format-agnostic shim, since two real installs are seconds apart and a clock-derived name would be unique by accident — the suite would otherwise pass against the very code that loses the file. Each case carries its own control, including one proving a non-adopting install into a read-only directory still exits 0, without which the abort test would prove nothing. **B's M6 now goes red** (9 failures), as do four narrower mutations, with `M-b` — B's exact first cut — reddening only because of the frozen clock.

- item: **OWNER RULING 2026-08-03 — option A.** The premise of #927 is refuted by measurement (probe C in `.cache/live-oracle/README.md`): opencode already hands a subagent the parent session's effort whenever the role pins no model, so flipping only the parent's `--variant` moved both subagents 0 → 560/641 with no `agent` block, no sidecar and no plugin action. The ~80 sessions at `default` were inheriting correctly from parents at `default`, not failing. The tier machinery this run built exists to OVERRIDE that inheritance, and no observed failure forces it to exist. Remove it. Before cutting, measure the blast radius — what actually becomes dead across all four editions, including whether `CONTRACT_EFFORT_TABLE`, `contractForProvider`, `effortForProvider` and `mapTier` on the ×4 byte-identical anchor still have any live consumer.
  status: done
  dispatched: one read-only `investigator` over the worktree; report to land at `kaola-workflow/issue-927/.cache/deletion-blast-radius.md`
  result: `kaola-workflow/issue-927/.cache/deletion-blast-radius.md`, plus `.cache/pivot-brief.md` which I wrote from it as the one wording for the pivot. It settled the delete/keep split with file:line evidence and found **three things guessing would have got wrong**. (a) A scope question: `renderAdaptiveConfig`, `--adapt`, `detectInheritModel` and the `variant` emission all PRE-DATE this run, so reverting #927 alone would restore the `variant` form the oracle measured inert — I ruled the deletion covers variant-era remains too, since leaving provably-dead configuration behind is the defect class that caused this bug. (b) **Blocker**: drift detection loses its subject entirely — with no `agent` block emitted, `install-opencode.sh:575` early-exits and the detector goes silent, measured twice with positive controls; reframed rather than deleted, its new subject being a config that still carries an obsolete per-role block. (c) **Blocker**: the tier-guarding refusal at `:627-635` would fire for every user with an agent block post-deletion (measured, exit 1). Also confirmed three survivors with consumers outside tiering — `reasoningRoles()` via `renderNeutralConfig`, `normalizeTier` via `dispatchEffort`, and `KAOLA_OPENCODE_INHERIT_MODEL` via `resolveRuntime()` in `kaola-workflow-claim.js` — each of which a careless cut would have taken.

- item: Delete the tier mechanism itself, tests falling out with it rather than being repaired ahead of it — the `agent.<role>.options` emission and whatever of `renderAdaptiveConfig` has no remaining purpose, the `effort-tiers.json` sidecar and its `--write-effort-tiers-to` flag, its deployment and uninstall in the installer, and the `chat.params` hook. Test custody holds: the author removes the assertions that pin the removed mechanism, the implementer removes the mechanism.
  status: done
  dispatched: three concurrent, disjoint by file, all reading `.cache/pivot-brief.md` as the single wording — implementer A (`a09460056174e4dbd`) on the generator, the ×4 anchor and the plugin; implementer B (`aa9a1799742c7415b`) on `install-opencode.sh` alone, deleting the sidecar and the always-firing refusal and reframing drift detection; the test author (`aab0cc2d7354ed326`) on `scripts/test-opencode-edition.js`, removing the pins whose mechanism is going and repairing the H1 import line, which is a legitimate repair because the loader fix it references SURVIVES. Reports append to their existing `.cache/impl-a-*.md`, `.cache/impl-b-*.md`, and a new `.cache/test-pivot.md`.
  result: all three landed; **six suites green and zero residue, verified by me in the worktree with real exit codes** — opencode 514 assertions, kimi, walkthrough, script-sync, routing-surfaces, install-all all exit 0, and `rg` over the whole worktree finds 0 hits for each of `effort-tiers`, `CONTRACT_EFFORT`, `contractForProvider`, `mapTier`, `renderAdaptiveConfig`, `write-effort-tiers`. Net −564/+135 across A's 7 files, including one contiguous 9,172-char excision from the ×4 anchor. **A verified before cutting and found a live consumer the blast radius missed** (`detectInheritModel` at `install-opencode.sh:526`), then measured the end state against the real installer rather than assuming B had handled it. **A settled the badge by measurement**: the task tool's schema, read byte-exact from the binary, takes `description`/`prompt`/`subagent_type`/`task_id`/`command` — no model, no effort — so the old "never pass a per-call `model=`" was warning agents against an argument that cannot be supplied; heading is now `## Model and effort are inherited`. A also swept 7 source + 63 generated files and found one survivor the delete list never named. The test author deleted each block **with** its mechanism leaving a note naming what it pinned, refused two S2 sweeps whose subject survives and whose claim is now *more* true, and closed both gaps the implementers had reported as invisible to the suite — A29 loads the plugin the way opencode does (mutation: re-adding `export { hookPath, findRoot }` reproduces the historical `paths[0]` failure exactly) and A27-quiet covers B's N4 over-fire. **The anchor-miss hardening proved itself live**: A's rewording landed and S2 went red naming its moved subject instead of silently asserting nothing.

- item: The agent-facing badge now states the wrong thing in a new way — `## Effort is configured, not passed` was true of the tiers and is false under inheritance. State what an agent actually gets: the subagent runs the session's model and effort. Check separately whether "never pass a per-call `model=`" is still accurate for opencode's task tool before keeping that sentence, rather than carrying it forward on momentum.
  status: done
  dispatched: implementer A, as part of its pivot dispatch; guard re-anchored by the test author before the source followed, same sequence as the first rename
  result: **A settled it by reading the binary rather than reasoning about it.** The task tool's schema takes `description`, `prompt`, `subagent_type`, `task_id`, `command` — **no model and no effort parameter exists**, so the old sentence warned agents against an argument that cannot be supplied. Heading is now `## Model and effort are inherited`, body stating what an agent actually gets. A flagged its own sequencing deviation rather than hiding it: every honest wording reds S2, because the old assertion required the block to contain `reasoning-tier` and `standard-tier`, and keeping those words to hold a test green would be repairing a pin ahead of its mechanism. Cost was one visible red for one round — and it proved the round-2 anchor-miss hardening live, since S2 went red naming its moved subject instead of silently asserting nothing.

- item: Re-point the documentation at inheritance — `CHANGELOG.md`, `README.md`, `docs/opencode-edition.md`, `docs/kimi-edition.md`, the audit supersession note, and the design record, which must now record that its own design was built and then removed on a measurement, with probe C as the evidence.
  status: done
  dispatched: the same `doc-updater` resumed for the pivot, rounds 4 and 5
  result: covered by the docs item above — `CHANGELOG.md [Unreleased]` rebuilt around Removed, `docs/opencode-edition.md` net −213/+293 with 154 lines deleted outright rather than softened, `README.md`, `docs/kimi-edition.md`, the audit note, the design record flipped to BUILT, MEASURED, THEN REMOVED with its body preserved, and `D-610-01` / `D-544-01` handled per disposition.

- item: Keep and re-verify what survives the pivot, none of which depended on tiering: the installer's drift detection and its two measured defects (adoption producing a role-less config while printing success; the detector blind under `OPENCODE_CONFIG_DIR`), `--adopt-config`'s disclosure and collision-proof backup, the plugin loader fix, and the S2 guard hardening that made an anchor miss red instead of a silent skip. Re-examine what config drift even means once the generated config carries no `agent` block, rather than assuming B's role-set comparison still has a subject.
  status: done
  dispatched: self — re-running every suite in the worktree and re-measuring the surviving installer behaviour against a scratch config dir, rather than accepting the implementers' reports
  result: all six suites green with real exit codes read directly (opencode 514, kimi, walkthrough, script-sync, routing-surfaces, install-all), and `rg` over the whole worktree returns 0 hits for every removed symbol. The drift question resolved by **changing the feature's subject rather than deleting it**: from "your role set differs from the generator's" — which post-deletion has no baseline at all — to "this config carries per-role effort settings that no longer do anything". Measured against a four-entry fixture: it names the three carrying `variant`/`options`, deliberately does NOT name the entry pinning only a `model` (that is the user's own), exits 0 and leaves the file byte-identical. That is the migration path for anyone who installed the tier version. The plugin loader fix survived and is confirmed by probe D. **B overrode one of my own "keep" instructions and was right**: the `OPENCODE_CONFIG_DIR` resolution existed only to feed the now-deleted `detectInheritModel()`, measured inert by byte-identical output with the variable set and unset — and the defect it was added for stays fixed for a better reason, since the reframed check needs no generator baseline and reads straight from the deploy path.

- item: The plugin loader bug is LIVE on this box today — `~/.config/opencode/plugins/kaola-workflow-hooks.js` at v9.4.2 still carries `export { hookPath, findRoot };`, so every opencode session logs a failed plugin load and the dispatch-log and compaction-resume hooks are dead there too, not just tiering. Verified by a real run: the fixed copy loads clean, the installed copy does not. Reinstall the runtimes once this lands.
  status: done
  dispatched: self — reproduced opencode's loader against this box's installed v9.4.2 plugin rather than reasoning about it
  result: **Severity corrected, and the correction matters more than the finding.** I twice told the user the load failure had killed the dispatch-log and compaction hooks. It had not. The reproduction: namespace order is `default`, `findRoot`, `hookPath`; `default` registers both hooks successfully and only then do the named exports throw, leaving one hook table collected. **Both hooks have been working all along** — they survive purely by an accident of export sort order, and any export name sorting before `default` would kill all of them with that one already-normalised error line as the only signal (implementer A's mutation 5 demonstrated exactly that). The doc agent refused to write my wrong version and was right. The fix ships; the fragility argument is untouched; the user-facing claim was not supported. **The reinstall itself is deliberately deferred to after the sink** — it acts on the user's installed runtimes rather than on the repo, so it is not a precondition for finalization: `./install-all.sh --global --yes` once this merges.

- item: **User request, 2026-08-03**: once the pivot lands, confirm by measurement that dispatching subagents actually works correctly in opencode — not that the config parses, but that a real session dispatches real subagents to the right roles, that each inherits the session's model and effort, and that the surviving plugin hooks fire without the loader error. The probe rig from the live oracle is still standing and is the cheapest way to do it.
  status: done
  dispatched: self — probe D, a fresh install of the post-pivot branch into a scratch project, config exactly as shipped, run at two different session efforts; evidence in `.cache/live-oracle/README.md`
  result: **Verified correct.** Both `task` calls routed to the role named in `subagent_type` as their own child sessions; both inherited the parent's `glm-5.2`; and both inherited the parent's effort — the `variant` is recorded on the subagent's OWN session row (`nothink` / `think`), which is direct evidence rather than the token-count inference the earlier probes had to rely on. The effort is real, not merely recorded: 0/0 in the `nothink` arm against **832 and 334** in the `think` arm. **Zero plugin load errors in both arms** against the shipped install, where this box's v9.4.2 copy still fails. One method note kept honestly: my first read showed `planner` at 0 in the `think` arm because the query returned one arbitrary message row per session instead of the session total — aggregating gave 832, and a single-row read is exactly how the original oracle overstated itself.
