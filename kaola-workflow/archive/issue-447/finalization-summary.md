# Finalization - Summary: issue-447

## Delivered
Codex lifecycle hooks now install **globally** into `~/.codex/hooks.json` + `~/.codex/kaola-workflow/{hooks,scripts}`, decoupled from per-repo `workflow-init`; every installer run (init or upgrade) force-refreshes the single global hooks file. Agent profiles stay project-local. `uninstall.sh` cleans the global hooks symmetrically. Docs (README + workflow-init SKILL/command ×3 editions) and install tests updated to the global model.

## Files Changed
16 product files (impl commit 60a7361):
- plugins/kaola-workflow{,-gitlab,-gitea}/scripts/install-codex-agent-profiles.js (byte-identical group)
- uninstall.sh
- plugins/kaola-workflow-{gitlab,gitea}/scripts/test-{gitlab,gitea}-workflow-scripts.js
- plugins/kaola-workflow/scripts/simulate-kaola-workflow-walkthrough.js
- scripts/test-install-model-rendering.js
- plugins/kaola-workflow{,-gitlab,-gitea}/skills/kaola-workflow-init/SKILL.md + commands/workflow-init.md (×3 pairs)
- README.md, CHANGELOG.md

## Test Coverage
All four chains green (sequential, AC7): claude / codex / gitlab / gitea. Adaptive barrier (resume/gate/barrier/verdict) all pass against the committed diff.

## Final Validation Evidence
`npm run test:kaola-workflow:{claude,codex,gitlab,gitea}` — all GREEN (run bqeij5112). validate-script-sync byte-identity OK. Barrier-check genuine pass post-commit (outOfAllow=[], unattributed=[]).

## Documentation Docking
DOCKED — README + workflow-init prose ×3 + CHANGELOG reflect the global-hooks model; n4/n5 doc-updater nodes + finalize CHANGELOG. No API/schema/CLI-output sections invented.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| test:kaola-workflow:codex (1st AC7 run) | behavior: codex walkthrough asserted old project-local hooks | tdd-guide (codex walkthrough fix) | a29f2e734a15aae7e | RESOLVED — re-green |

## Follow-Up Items
- **Process (run-gap, #435 class):** the planner under-scoped n1 — it omitted the codex-edition walkthrough `plugins/kaola-workflow/scripts/simulate-kaola-workflow-walkthrough.js` (and n1 was at the 6-file ceiling, so the file could not be added to n1). Corrected by adding it to n2's write set + re-freeze (plan_hash 1d4f923a → b72666c2). A planner heuristic "a Codex-installer behavior change touches the codex-edition walkthrough test" would prevent recurrence. (Recommend filing as a follow-up — pending user approval.)

## Closure Decision
One process follow-up surfaced (above), non-blocking for #447 (all ACs met). Surfaced to user; feature is complete and closeable.

## Commit And Push
[pending final Git gate]

## GitHub Issue
447 — to be closed by sink-merge after FF.

## Roadmap
to be regenerated by cmdFinalize (remove .roadmap/issue-447.md).

## Archive
pending cmdFinalize.

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked | n4/n5 nodes (.cache/n4-*.md, n5-*.md) | |
| documentation docking | invoked | this summary | |
| final-validation fix executors | invoked | a29f2e734a15aae7e (codex walkthrough) | |
| roadmap refresh | pending | cmdFinalize | |
| archive completed folder | pending | cmdFinalize | |
| final commit and push | ready | impl 60a7361 committed; barrier green | |

## Status
ARCHIVED AFTER FINAL GIT GATE
