# Finish #956–#962 — the subtraction-audit escalation set (all seven open issues)

Run facts every item depends on:
- Worktree (ALL edits land here): `.kw/worktrees/bundle-956-957-958-959-960-961-962/`, branch
  `workflow/bundle-956-957-958-959-960-961-962`. The main tree is NOT the work tree.
- **`.opencode` and `.kimi` are untracked and DO NOT EXIST in the worktree.** Any zero-consumer or
  edition sweep covering them must run against the MAIN tree, or it returns a false zero. `plugins/*`
  (3 trees, 86 .js) are tracked and present in both.
- `grep` here is ugrep and skips dot-directories; `git grep` cannot see gitignored trees. Two-part
  sweeps, always, and **on the module stem, never the basename**.
- `docs/api.md`, `docs/workflow-state-contract.md`, `docs/agents-source.md` are TEST-CONSUMED —
  editing them stales the chain receipt, so the receipt run comes after they are final.
- Every measurement backing these issues is in `docs/audits/2026-08-11-subtraction-audit.md`.
- Reviewers run on the Fable model, at the user's direction.

- item: Premise-check #956 — confirm FEATURE_TOKENS is absent tree-wide (removed at 523f1241) while the positive control CONFIG_HOOKS_FAMILY, twelve lines later at docs/conventions.md:281-293, still resolves 6x; then settle whether the 23-line block is deleted with its mechanism or rewritten against whatever replaced it, and settle the 9-vs-13 net-line discrepancy the audit left open.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-956.md
  result: premise-956.md — CONFIRMED-WITH-CORRECTION, and the correction changes the repair from a DELETE to a REWRITE. Core claim holds: FEATURE_TOKENS is absent from every code dir (I re-ran it: `git grep -l` over scripts/plugins/templates/agents/commands/hooks exits 1), removed at 523f1241, positive control CONFIG_HOOKS_FAMILY still resolves 6x. But (a) lines 287-289 (item 3, chain pinning) are TRUE TODAY — independently re-verified: test-agent-profile-parity.js is in the claude chain at package.json:40 and pinned by five validate-*contracts.js files — so the filed "23-line block" and the 13-line reading both OVER-DELETE; (b) the mechanism was not merely removed but REPLACED in the same commit and same file by ROLE_PINS + SAFETY_BASELINE_RULES + a 2/3 consensus baseline (re-verified live at lines 37 and 76 of a 743-line file). Truly dead = lines 281-286 plus the clause "then pin it in FEATURE_TOKENS" in 291-292. Repair = rewrite item 2 and the Workflow paragraph against the successor; "Three-part" at 273 stays true, no renumbering. Net deletion 0.

- item: Premise-check #957 — confirm the Codex tier pair at docs/api.md:1535-1538 and docs/conventions.md:45-47 is the sole unbound copy, with constants at kaola-workflow-codex-preflight.js:89-92 and bindings at test-route-reachability.js:530-545 and validate-kaola-workflow-contracts.js:444-452. It is NOT in adaptive-schema.js (that file holds role rosters only) and two prior attempts pointed at the wrong owner; note the binding pins EFFORT and accepts any model string.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-957.md
  result: premise-957.md — CONFIRMED-WITH-CORRECTION, and the correction kills the issue's headline word. Every mechanism claim verified: constants at codex-preflight.js:89-92 (sol/medium, sol/xhigh, all four copies md5-identical and byte-guarded), test-route-reachability.js:537-538 builds expected efforts from the constants while the model regex is literally `model: "[^"]+"` so ANY model string passes, validate-kaola-workflow-contracts.js:444-453 cross-binds preflight to installer, adaptive-schema.js holds role ROSTERS ONLY (sweep for model/effort literals exits 1 — the issue's warning not to send an implementer there is correct), and docs/api.md is test-consumed. REFUTED: "the SOLE exception is docs/api.md and docs/conventions.md". README.md:180-181 is a THIRD live unbound shipped copy — I re-verified it independently: normative prose ("Both mappings are fixed"), and it sits in the carrier list beside the two docs. Proven unbound two ways: every README-reading script enumerated (only role-catalog, coordination-concept, ECC and release readers — none touches the pair) and every xhigh-carrying check enumerated (none reads README). SECOND CORRECTION: both cited sites extend past their cited line ranges — colloquial "Sol/medium" restatements at api.md:1540 and conventions.md:49 — so a repair confined to 1535-1538 and 45-47 leaves a value standing at each site. Carrier census corrected: 27 non-archive tracked carriers, not the audit lineage's 22. Both doc copies currently MATCH the constants, so this is drift-class, not live-error. D-687-01 and CHANGELOG carry the pair as declared history and are correctly left alone. Repair shape for api.md/conventions.md settled: pointer-ize (D5 establishes pointing as this class's direction, and adding a check to conventions.md would make unbound verdict-affecting prose whose edits do NOT stale the receipt — a stale-green class). README's treatment is a user decision, batched below.

- item: Premise-check #958 — re-verify docs/architecture.md:295-297: three sub-claims hold (absent from npm test, edition-sync.js, install.sh), the fourth is false because both sync scripts derive their command list from the routing registry though neither .opencode nor .kimi is among the 18 render targets. Carry the honest limit: the kimi leg has NO signal (.kimi untracked, red in both legs), so kimi rests on the weaker direct listCanonCommands() measurement.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-958.md
  result: premise-958.md — PREMISE HOLDS, and the honest limit was SURMOUNTED rather than carried. All three conceded sub-claims verified true (npm test / edition-sync.js / install.sh all zero opencode-kimi mentions). Derivation coupling shown in code across three legs: both sync scripts require runtime-edition-forge, which requires generate-routing-surfaces and returns GENERATED_SURFACES rows. Generator writes 18 surfaces = 3 topics x 6 dirs, neither .opencode nor .kimi among them. Operational A/B in a disposable clone: one comment line appended to commands/workflow-next.md flips sync-opencode-edition --check from exit 0 to exit 1 naming .opencode/command/workflow-next.md. KEY UPGRADE over the audit: the kimi leg's "no signal" is a property of the BARE-CLONE setup, not of kimi — with .kimi copied in, baseline exits 0 and the same mutation reds exactly .kimi/skills/workflow-next/SKILL.md, so kimi's coupling is operationally proven and the replacement sentence needs NO kimi hedge. Negative control clean (commandSources returns 0 for classifier/sink-merge across all three forges). Repair text drafted in the report's section 8. Neighbour fact for the docs item: install-all.sh DOES invoke the standalone installers, so the repair must not widen "install.sh" to "the installers".

- item: Premise-check #959 — re-measure which forge CLI each of the four trees calls (expected gh 127 canonical, Codex gh 64/glab 0/tea 0, gitlab glab 34, gitea tea 41) with the positive control that glab appears only in gitlab and tea only in gitea; confirm the blast radius is one live line by checking "four forge edition" outside the archive, and confirm the load-bearing phrase "four editions" is untouched.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-959.md
  result: premise-959.md — PREMISE HOLDS structurally; two corrections to the issue's own prose. Decisive evidence: the Codex tree's six `gh` exec sites are the SAME six files at the SAME six line numbers with the same bytes as canonical's, while gitlab invokes glab (kaola-gitlab-forge.js:19,23) and gitea invokes tea (kaola-gitea-forge.js:23,31,41) — so Codex differs by RUNTIME, not forge CLI, and "against a different forge CLI" is false. Positive control passed: under invocation-grade methods glab is nonzero only in gitlab and tea only in gitea; all 21 cross-tree blunt-token hits were individually inspected and every one is a comment or a negative-guard regex (the gitlab/gitea contract validators actively ASSERT the absence of gh). CORRECTION 1: the issue's cited counts 127/64/34/41 did NOT reproduce under any of four counting methods — the structural claim they were cited for holds under all four, so the premise stands, but the arithmetic is unreproducible; the repair cites no counts so nothing changes. CORRECTION 2: "exactly three hits" is stale — now 8 hit lines in 6 files, the three extra being the audit doc, this issue's roadmap source and the generated mirror, all self-referential defect-tracking, none a live-doc assertion; docs/architecture.md:287 remains the ONLY live one. "four editions" measured at 243 lines across ~70 files and is untouched by the repair. APPLIED in the worktree: line 287 now reads "**Four editions** ship the same workflow across three forge CLIs:" — 86 chars, no rewrap, one-line diff, coherent with the #955 table preamble at line 313 ("three forges"). Reported out of scope, NOT fixed: lines 289-290's "Most scripts are rename-normalized copies" holds for gitlab/gitea only (Codex keeps canonical filenames) — pre-existing and hedged by "Most", so left alone rather than widening this issue's scope.

- item: Premise-check #960 — re-run the zero-consumer search for scripts/run-chain-pool.js (428 lines) in two parts on the STEM `run-chain-pool` (a basename search already produced a false zero here once: the real consumer is require('./run-chain-pool')), covering the tracked tree AND all rendered edition trees including .opencode/.kimi in MAIN; confirm the shard registry is empty and name the test that dies with the mechanism.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-960.md
  result: premise-960.md — SAFE WITH CONDITIONS, and it caught a build-breaking error in the filed issue. REFUTED clause: "its only consumer is the test that exists to test it". There is NO dedicated test. The sole consumer is scripts/test-parallel.js:349 — which is a LIVE step in BOTH claude chains (I re-verified: `test-parallel.js --self-test` is in test:kaola-workflow:claude and :claude:full) and whose self-test covers THREE modules. Executing the issue literally — delete the module and delete "its test" — would red both chains AND strand test-shard-lib, which survives independently via simulate-workflow-walkthrough.js:12712 (re-verified) and is registered at test-suite-registration.js:38. Corrected surgery: delete the module, remove ONLY line 349 and sections f6-f9 plus the pool half of the (f) comment, keep sections a-e and f1-f5 and the line-348 shardLib require. Everything else in the issue holds: 428 lines, no bin entry, no npm script, no installer/edition-sync/manifest reference, SHARDED_SUITES genuinely `{}` (re-verified), and all six scripts/ enumerators filter by patterns this filename cannot match. One copy in any tree, multiplier x1. Positive control returned 538 files for a live module vs 1 for this one. Stranded-but-harmless: simulate-workflow-walkthrough.js's KAOLA_TEST_TIMEOUT_SCALE reader is fail-open and correctly left alone.

- item: Premise-check #961 — same two-part stem search for scripts/fixtures-orphan-legality.js (102 lines), confirming both importers are gone and all 8 exports unreferenced; name any test existing only to exercise the fixture, which is removed with it and never repaired ahead of it.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-961.md
  result: premise-961.md — SAFE WITH CONDITIONS; every quantitative sub-claim reproduced exactly (102 lines, exactly 8 exports, exactly 2 importers, multiplier x1). Zero live consumers: stem search plus 8 separate per-export-name searches return archive/history prose only, two of the exports return zero hits anywhere, and no code builds a `fixtures-` path dynamically. Part B swept NINE edition trees (more than the six named) with per-tree file counts printed so a silently-empty sweep would be visible; the six .opencode*/.kimi* zeros are structural — those trees ship no scripts at all, and the positive control scores 0 there for a live module too. History nails the custody claim: importer 1 (test-parallel-batch.js) died at 1fc33c9d retiring the parallel-batch subsystem, importer 2 (test-adaptive-node.js) died at c0b48043; no third importer ever existed and neither was renamed. So NO test is deleted or repaired by this change — both custodians are already gone. Positive control discriminates hard: 80 files for kaola-workflow-adaptive-schema vs 0 for the fixture. Deletion is invisible to the install manifest (deliberately absent from SUPPORT_SCRIPTS) and to every scripts/ directory enumeration — all four filter by patterns this filename cannot match. CONDITION: the same change must delete the now-stale exclusion comment at line 55 of BOTH byte-paired kaola-workflow-install-manifest.js copies (root + plugins/kaola-workflow), preserving their sha256 pairing. Awaiting user consent before the deletion lands.

- item: Premise-check #962's eight sub-findings — S3 (three dead transformCommandBody strips, 87 lines, claim: removing all three renders BYTE-IDENTICAL output), S4 (runtime-edition-forge.js --commands-dir/--forges, no caller), S5 (8 comment lines naming deleted plan-validator.js, 0 net deletable), S6 (two legacy-alias exports + dead constant), D3 (kimi-edition.md credits a deleted renderer), D5 (LANE_STALENESS_MS in three live docs, TWO SITES IN A TEST-CONSUMED DOC), D7 (opencode-edition.md re-types a derived roster), D8 (architecture.md inlines PARKED_LANE_PREFIXES unnamed).
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-962.md
  result: premise-962.md — every sub-finding CONFIRMED as a defect; two carry corrections. S3: 87 lines across six blocks, all three trigger patterns match 0/9 real command sources, and byte-identity was PROVEN not asserted — three clones, 114 generated files per leg, diff -r exit 0, with a positive control (removing one LIVE transform instead) that correctly showed a difference, so the identity is a real result and not a vacuous one. S4: confirmed zero callers, with positive controls showing the two sibling flags ARE live; commandSources() must be kept. S5: confirmed corrective, 0 net deletable — the comments' referent is wrong but each carries a load-bearing fact, and the tombstone guards naming plan-validator on purpose must not be swept. S6 CORRECTION: undercounts by three — OUT_HOOKS_DIR (both scripts) and OUT_PLUGINS_DIR (opencode) meet the identical criterion; the audit's name-keyed sweep likely mistook the sibling file's definition for a consumer. I widened the cut to all six. D3, D7, D8 confirmed. D5 CORRECTION: "TWO sites in a test-consumed doc" is WRONG — it is exactly ONE (docs/workflow-state-contract.md:295); architecture.md and conventions.md are in neither declared list. The archived reader's own header contradicted its own table, and the slip propagated into the audit and the issue. Both recorded observations confirmed: the deletion transforms still have no over-match observer (narrower watch-list wording drafted in the report), and the repo declares no Node floor — no engines key, no .nvmrc, no CI config of any kind — so the native: class stays unmeasurable, and declaring a floor would be a user-owned contract change.

- item: Check whether audit findings D2 (docs/README.md:17 sells a removed opencode per-role model/effort mapping) and D4 (docs/api.md attributes behaviour to a function that never existed) were repaired in the #952-#955 bundle — they are in the audit but escalate in NO issue, so if still live they are unowned work the user should decide on rather than a gap to silently fill.
  status: done
  dispatched: adversarial-verifier (Fable), read-only → kaola-workflow/bundle-956-957-958-959-960-961-962/premise-d2-d4.md
  result: premise-d2-d4.md — BOTH STILL LIVE, BOTH UNOWNED. Verified against today's tree, not just the audit record. D2: docs/README.md line 20 (was 17; d521f1f0 shifted it, byte-identical) still sells the opencode edition on a "provider-open two-tier effort mapping"; the mechanism is gone — mapTier/CONTRACT_EFFORT_TABLE/effortForProvider/contractForProvider have 5 hits, ALL comments in test-opencode-edition.js recording the removal, opencode.json has no variants or effort key, and the linked doc opencode-edition.md:102 itself says it is "removed, not merely deprecated". Not test-consumed, freely editable. D4: docs/api.md:1002 attributes behaviour to `cmdSinkPr`, which NEVER existed — git log -S returns three commits, none adding it to a .js file; zero hits in scripts/ or plugins/ or any dot-edition tree; the only live identifier is cmdSinkFallback. The stated RESULT is true (the PR sink emits no closure receipt); only the method attribution is fiction — precisely the "specify the result, never the method" failure. api.md IS test-consumed, so the edit must precede the receipt run; no assertion pins the passage, so it cannot red a validator. Ownership independently confirmed: all seven open issue bodies captured in full and read; #962's docs list names D3/D5/D7/D8 and skips D2/D4. Coverage is 14 of the audit's 16 findings. SCOPE DECISION FOR THE USER — batched with the deletion consent, not decided unilaterally.

- item: USER DECISIONS taken in conversation 2026-08-12, binding on the items below. (1) Both deletions proceed on the CORRECTED plan, not the filed one. (2) D2 and D4 are folded into this bundle — the user chose to fix them here rather than file them. (3) README.md keeps its concrete Codex values and gets a BINDING built from the constants, rather than being pointer-ized; the two docs sites are still pointer-ized. Sub-question left to me: whether the gitlab/gitea contract validators mirror the new README assert.
  status: done
  dispatched: self — asked in conversation with the evidence for each
  result: recorded here; each is implemented in the items below

- item: DONE INLINE by me — the three settled docs repairs. #956: docs/conventions.md item 2 rewritten against the ROLE_PINS/consensus successor and the Workflow paragraph rewritten; item 3 and "Three-part" left intact because they are true. #958: docs/architecture.md:295-298 now says "the routing generator's render targets" and states the derivation coupling with its consequence. #959: docs/architecture.md:287 now reads "**Four editions** ship the same workflow across three forge CLIs".
  status: done
  dispatched: self, in the worktree
  result: verified — FEATURE_TOKENS and "propagation set" both now absent from the worktree copies; diffstat 2 files, +16/-10 at the time of writing.

- item: #962 S3+S4+S6 — the sync-script subtractions. Delete the six dead transformCommandBody strip blocks (87 lines) across sync-opencode-edition.js and sync-kimi-edition.js; drop runtime-edition-forge.js's uncalled --commands-dir/--forges CLI modes (~14 lines) while KEEPING commandSources(); delete the dead legacy aliases and OUT_* constants. I widened S6 from the filed 3 symbols to 6 on the verifier's evidence — OUT_HOOKS_DIR (both scripts) and OUT_PLUGINS_DIR (opencode) meet the identical zero-consumer criterion with positive controls, and leaving them while cutting their neighbours would be arbitrary.
  status: done
  dispatched: implementer → edits land in the WORKTREE (.kw/worktrees/bundle-.../scripts/sync-opencode-edition.js, sync-kimi-edition.js, runtime-edition-forge.js); report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-962-sync.md
  result: impl-962-sync.md — REGRESSION-GREEN, the only item in this run to reach that tier. Three files, -60/-34/-16. S3 total 87 lines, exactly the premise's count. Byte-identity PROVEN across all six rendered trees (22/19/19/19/19/19 files, 117 per leg, diff -r exit 0 and 0 bytes each, cmp on opencode.json exit 0), with a POSITIVE CONTROL against vacuous equivalence: with the cut in place it additionally removed one LIVE transform, re-rendered, and got exit 1 differing in exactly one file per opencode tree and nowhere else — so the zero-diff is a real result. The control was applied to a cp backup and restored with cmp exit 0, explicitly NOT via git checkout -- which would have destroyed other agents' concurrent edits. Both edition suites exit 0 before AND after with IDENTICAL assertion counts (opencode 563, kimi 521) — no assertion dropped or skipped; A22, which absence-asserts the stripped patterns, stays green and unedited. S4: live modes still work for all three forges, removed modes now take the unknown-argument path at exit 2 (a behaviour change with no caller), commandSources() kept and verified live at four call sites. S6: all six names confirmed zero-consumer per FILE, and the OUT_HOOKS_DIR name-collision trap I warned about REPRODUCED EXACTLY — the name-keyed grep returns 4 hits across two files, each pair being a definition plus its own export, neither file reading the other's; split per file each is a clean zero. Sweep covered 3554 files across seven dot trees with a positive control (kaola-workflow → 114 files/697 hits) proving it reads content. CORRECTION: the premise's START lines were off by one for four of the six S3 blocks (they excluded the first line of each lead comment) though every COUNT was right — cutting on anchor text rather than line numbers is what caught it; the filed numbers would have orphaned a comment line into two hunks.

- item: FINDING carried out of impl-962-sync, pre-existing and NOT caused by this bundle: the MAIN tree's four non-github rendered edition trees are one canonical edit stale — main/.opencode-gitlab/agent/code-architect.md and its gitea/kimi siblings still carry a line canonical agents/code-architect.md no longer has, while main's github trees are current. Gitignored artifacts only, so nothing to commit and the A/B was unaffected (both legs re-rendered from the same worktree canonical). Worth a --write for the non-github forges in the main tree before any install from it.
  status: done
  dispatched: self — recorded, not acted on; outside this bundle's scope
  result: recorded here and raised to the user; no tracked file involved

- item: #962 S5 — corrective, 0 net deletable. Eight comment lines across kaola-workflow-claim.js and kaola-workflow-run-chains.js name the deleted plan-validator.js; the referent is wrong but the fact each carries is load-bearing, so they are REWRITTEN against today's reader (evaluateChainReceipt) and not deleted. Ported x4, so all four trees must agree. Tombstone guards that name plan-validator on purpose must NOT be swept.
  status: done
  dispatched: implementer → WORKTREE kaola-workflow-claim.js + kaola-workflow-run-chains.js across all four trees; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-962-s5.md
  result: impl-962-s5.md — 8 files (2 canonical x 4 trees), 32 shipped comment lines, nothing deleted. PROPAGATION determined from the code, not guessed, and it DIFFERS PER FILE: run-chains.js is a byte copy to codex (COMMON_SCRIPTS) but GENERATED to gitlab/gitea (GENERATED_AGGREGATORS via renderForgePort); claim.js is a byte copy to codex but HAND-PORTED to gitlab/gitea, which edition-sync.js:30-34 states explicitly. It propagated by calling edition-sync's own exported renderForgePort/forgeRel from a scoped script rather than running --write, precisely so it would not overwrite the sibling files other agents were mid-edit on — and confirmed the result is what --write would produce via edition-sync --check. The forge rename pass renders the claim token correctly per tree (kaola-gitlab-workflow-claim.js etc.) while leaving the kaola-workflow/<project> state-directory token alone. TWO JUDGEMENT CALLS beyond the literal dispatch, both right: it extended "enforced"→"reported" to site 3 as well, because evaluateChainReceipt's own header at adaptive-schema.js:1210 says "IT REPORTS; IT DOES NOT REFUSE" and leaving "refuses" would have replaced a wrong referent with a wrong mechanism; and it KEPT "the only refusal is chains_config_missing" verbatim because that one IS a live typed refusal the "Nothing refuses" principle explicitly preserves. Site 1 gained the load-bearing clause the finding was about — that the tag shape is pinned by historical refs and not by any live caller, so a reader cannot "modernize" it away. FOUR-TREE AGREEMENT verified in-process with a positive control (sanitizeBarrierTag, 9 hits per copy); it also caught its OWN checker's wrong expectation (expected 4 occurrences, actual 5 — "the expectation was wrong, the files were not"). validate-script-sync.js and edition-sync --check both exit 0 BEFORE and AFTER with byte-identical output; all 8 files node --check clean; validate-script-sync's require() of all four claim copies makes its exit 0 a live module load, not just a byte compare. Tombstone guards confirmed untouched and still carrying their mentions (test-finalize-door.js 7, validate-workflow-contracts.js 1, both forge validators 1 each) — and it checked WHY they were safe rather than assuming: T1's regex matches a require(), not a comment.

- item: #960 — delete scripts/run-chain-pool.js (428 lines, the only copy in any tree). Mechanism only; the test surgery is a separate item under test custody.
  status: done
  dispatched: implementer → git rm WORKTREE scripts/run-chain-pool.js; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-960-module.md
  result: impl-960-module.md — scripts/run-chain-pool.js deleted (428 lines, staged D). Two comment-only follow-ups, both of which I diff-checked myself for behaviour change and verified factually: test-suite-registration.js:205 no longer names the now-deleted COST_HINT table and instead names the spawn-classification ceiling table — confirmed real, CEILINGS exists at test-spawn-classification.js:65, and COST_HINT is now absent tree-wide (git grep exits 1); simulate-workflow-walkthrough.js:44-51 comment now states the scale is permanently 1 since the pool was retired, with the fail-open `const timeoutScale = Math.max(1, ...)` code line BYTE-UNCHANGED, so no behaviour moved. CHANGELOG: a ### Removed subsection under the existing ## [Unreleased] (which does exist at line 3, alongside Added and Changed).

- item: #960 test custody — excise ONLY sections f6-f9 and the line-349 require from scripts/test-parallel.js, keeping a-e and f1-f5. These pins die with their mechanism; nothing is repaired to keep passing. Getting this wrong reds both claude chains.
  status: done
  dispatched: tdd-guide (test custody) → WORKTREE scripts/test-parallel.js only; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-960-test.md
  result: impl-960-test.md — three cuts, 1 insertion / 54 deletions, made on structure not blind line numbers: the (f) lead comment retitled to drop the dead mechanism, the line-349 pool require removed, sections f6-f9 removed whole. Line 348's shardLib require, sections a-e and f1-f5 all byte-identical; no assertion weakened, renumbered or added. MUTATION-PROVEN in a scratch mirror rather than in-tree (the mechanism file was still present when it ran, so an in-tree green would have proved nothing): RED control = baseline test-parallel.js + mechanism deleted throws "Cannot find module './run-chain-pool'" at line 349, exit 1 — proving the cut is load-bearing, not cosmetic; GREEN = edited file + mechanism deleted, exit 0, 23 assertions. Exact delta audited by comm of the two PASS sets: 36 → 23, the 13 lost are precisely (f6a-d)(f7)(f8a-g)(f9), all pool, and ZERO assertions present after but absent before. test-suite-registration.js exits 0 (45 files, 42 registered, 3 exempt) — the very guard the filed plan would have redded. Corrects premise-960 §6: test-suite-registration.js contains NO literal "test-parallel" string; registration derives from the package.json chain steps. Conclusion unchanged. CLOSED by me: with run-chain-pool.js confirmed absent from the worktree, the in-tree `node scripts/test-parallel.js --self-test` exits 0 with 23 assertions passed / 0 failed — identical to the mirror's prediction, including the surviving f1-f5 shard assertions. The agent rightly refused to grade its own implementation; the run is mine and it is green.

- item: #961 — delete scripts/fixtures-orphan-legality.js (102 lines) plus the stale exclusion comment at line 55 of BOTH byte-paired kaola-workflow-install-manifest.js copies. No test dies: both importers were already deleted at 1fc33c9d and c0b48043.
  status: done
  dispatched: implementer → WORKTREE git rm scripts/fixtures-orphan-legality.js + both install-manifest copies; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-961.md
  result: impl-961.md — done in the worktree. fixtures-orphan-legality.js git-rm'd (102 lines); the stale exclusion comment removed from BOTH install-manifest copies. Byte pairing preserved and proven at the git level, not just the working copy: both copies sha256-equal before (2a8224ef…, the digest premise-961 recorded) and after (df56e45b…), with git diff showing identical pre- and post-image blob ids on both. Independent check: requiring both copies and comparing supportScripts() gives identical 17/19/19 per forge with the fixture absent from every set, confirming it was never installed. git grep for the stem and all export prefixes over scripts/ and plugins/ exits 1. No test touched — none exists to touch. 17 remaining repo-wide hits are all history/prose and correctly left alone. CHANGELOG: new ### Removed section under [Unreleased].

- item: Docs group 1 — the four files carrying #957's pointer-ization, D4 and D5. docs/api.md (pointer-ize the Codex tier table AND the colloquial restatement below it, plus the cmdSinkPr fiction), docs/conventions.md (same pointer-ization, plus the LANE_STALENESS_MS numeral), docs/workflow-state-contract.md and docs/architecture.md (the numeral). api.md and workflow-state-contract.md are TEST-CONSUMED — these edits must precede the receipt run.
  status: done
  dispatched: implementer → WORKTREE docs/api.md, docs/conventions.md, docs/workflow-state-contract.md, docs/architecture.md; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-docs-1.md
  result: impl-docs-1.md — all six edits landed in exactly the four assigned files. #957: api.md's value TABLE and the "Sol/medium" restatement below it both removed (the R2 site a table-only repair would have left standing), conventions.md's two bullets collapsed to one pointer. D4: `cmdSinkPr` → "The PR sink", one token, the true result preserved. D5: the numeral dropped at all three sites with the name and "(24 hours)" gloss kept. D8: architecture.md:121 now names PARKED_LANE_PREFIXES and its home, matching the correct style already at conventions.md:790; architecture.md:198 correctly left alone as a different list. Verification: seven patterns grepped over the four files, ALL exit 1 — gpt-5.6-sol, Sol/medium, cmdSinkPr, "= 86400000", the bare numeral 86400000, xhigh, and even `gpt-`; a reader sweep shows NO script opens conventions.md or architecture.md at all. CORRECTS MY BRIEF: I passed down premise-962's caution that test-forge-finalize-findings.js opens docs/conventions.md — measured, it does not (grep exits 1); it reads docs/api.md and pins the finding-type table rows, and the agent confirmed its api.md edits fall outside every pinned region. The agent complied with the misdirected caution anyway, so nothing was harmed. It also caught its own zsh unquoted-file-list slip (ugrep exit 2 reading as a false clean) and re-ran. Honest limit recorded by the agent: static verification only, no chain run — that is mine to close, and for the two test-consumed files it must come AFTER these edits.

- item: Docs group 2 — the three standalone docs. docs/kimi-edition.md D3 (credits the deleted modelDisplay()), docs/opencode-edition.md D7 (re-types a derived roster), docs/README.md D2 (sells a removed two-tier effort mapping). No file overlap with group 1.
  status: done
  dispatched: implementer → WORKTREE docs/kimi-edition.md, docs/opencode-edition.md, docs/README.md; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-docs-2.md
  result: impl-docs-2.md — three files, +5/-9. D3 DEVIATED from my recommended text, correctly and under the brief's own condition: rather than replacing the bullet it DELETED it, with a clause-by-clause table showing every clause of my proposed replacement was already carried by the surviving section (:88 "no `model:` field", :90 "skipped entirely — meaningless under inherit"), and the bullet's actual subject — the planner's per-node tier "recorded in the dispatch packet and ledger" — is machinery ADR 0017 retired, so there is no planner node, packet or ledger left to describe. I read the section afterwards: it flows correctly without it, and modelDisplay now greps to zero. D7: roster replaced with a pointer, count word removed (`grep -nw seven` exits 1 file-wide), and BOTH halves of the pointer verified true before writing — reasoningRoles() derives it at sync-opencode-edition.js:565-574, and the scaffold comment really does carry the list at opencode.json:8, so "read the roles from the scaffold" resolves to something real. D2: verbatim as briefed, clause kept not deleted so the sibling kimi line keeps symmetry. Verified none of the three files is read for CONTENT by any script — the one apparent hit, test-kimi-edition.js:398, is a comment saying the OPPOSITE ("prose in docs/kimi-edition.md cannot satisfy that"). 18 relative links resolved, 0 broken. SECOND CORRECTION OF A PREMISE CLAIM OF THE SAME KIND: validate-kaola-workflow-contracts.js:468 is a COMMENT inside deriveCodexRoleCatalog(), not an executed check — premise-962 described it as a check, just as it described test-forge-finalize-findings.js as reading conventions.md. Neither error changed an outcome.

- item: #957 README binding — the user chose to KEEP README.md's concrete Codex values and bind them, so README.md itself is not edited; a guard is added that builds the expected text FROM the codex-preflight constants. Guard authorship is test custody. Must be mutation-proven, and must not create a one-rule-two-wordings divergence across the forge validators.
  status: done
  dispatched: tdd-guide (guard authorship = test custody) → WORKTREE validate-* contract validator(s), README.md NOT edited; report → kaola-workflow/bundle-956-957-958-959-960-961-962/impl-957-readme-guard.md
  result: impl-957-readme-guard.md — 15 lines at validate-kaola-workflow-contracts.js:504-518, README.md byte-unchanged (git diff --quiet exits 0), zero new requires since readmeText and codexPreflight were already in scope. MUTATION-PROVEN, and then some. Green unmutated; RED on model gpt-5.6-sol→gpt-5.6-luna; RED on effort xhigh→high, deliberately chosen as the WRAPPED fragment so the proof also exercises the normalization. Then a POSITIVE CONTROL I did not ask for and should have: both reds are equally consistent with a hardcoded literal, so it moved the constants AND README together to a new value across all four preflight copies, all three installers, the :444-453 literals and README:180-181 — the guard stayed GREEN, proving it reads the constant rather than a literal. That is the "a mutation control can share the code's own assumption" trap closed properly. PLACEMENT decided on measurement, not preference: the codex validator, not the fast gate, because validate-workflow-contracts.js's codex twin computes root=plugins/kaola-workflow where README.md does not exist (ls exits 1) — the rule would be authored where it is structurally FALSE — and because both drift sources already force all four chains anyway (README.md is in ROOT_EDITION_READ_FILES; codex-preflight.js has a plugin mirror caught by isEditionCouplingPath). MIRRORING settled: pinned ONCE, no gitlab/gitea copy, because both forge validators contain ZERO README occurrences and they mirror the :444-453 block only because each forge tree has its own preflight and installer — the SUBJECT is replicated there, not the rule; README is a single root file, so three asserts would be three wordings of one rule.

- item: Adversarially review the whole bundle on the Fable model — every claim written into a doc checked against the shipped tree, every deletion checked for a surviving consumer, prose absolutes checked across all four editions rather than root only.
  status: in-flight
  dispatched: THREE concurrent code-reviewers on Fable, read-only, partitioned by concern not by file (review has no write conflicts): (A) docs accuracy — every factual claim newly written into a doc verified against the shipped tree → review-a-docs.md; (B) the subtractions — surviving consumers, test custody, anything that should or should not have died with its mechanism → review-b-subtractions.md; (C) the new guard, one-rule-one-wording, and prose absolutes across all four editions → review-c-guard-editions.md. All three in kaola-workflow/bundle-956-957-958-959-960-961-962/.
  result: TWO confirmed defects across the three reviews, both repaired (see the two REPAIR items above), plus one addition folded in. (A) FAIL, 1 medium — my own "defined once"/"defined solely by" wording, contradicting architecture.md; fixed. Everything else in the docs verified true against the tree. (B) FAIL, 1 low — the A22 comment block still describing the deleted strip in the present tense; dispatched to tdd-guide. Its sharpest answer: A22 is NOT vacuous but MORE load-bearing after the cut, since a canonical reintroduction would previously have been eaten silently by the strip and will now reach the generated surface and red. (C) PASS, 0 defects, and it pushed past the brief: NINE mutations on the new guard — red on standard-only, reasoning-only, model value, effort value and outright sentence deletion; GREEN on an aggressive 6-line reflow, so norm() is not a maintenance trap; vacuous pass impossible (empty README dies at :481, undefined constants fail closed). It also SHARPENED the author's positive control honestly: a constant-side mutation never actually reaches the new guard because the pre-existing byte-parity and literal pins red first, so the guard's unique coverage is the README side — which is exactly what #957 filed. It re-proved the T1 tombstone armed by reintroducing a require (exit 1) and restoring (exit 0), which I had not asked for. It endorsed the no-mirroring decision with fresh evidence (validate-script-sync.js:34 documents the guard's home as deliberately Codex-only) and confirmed chain reach: codex chain only, but README is in ROOT_EDITION_READ_FILES and plugins/ is edition-coupling, so any drift-capable diff fails closed to all four at finalize — no green-while-wrong path. All eight rewritten S5 comments verified true across all four trees.

- item: CHANGELOG — the bundle-level entries are the orchestrator's, not any implementer's. The two deletions were changelogged by their agents; everything else was owed.
  status: done
  dispatched: self, in the worktree
  result: added under the existing ## [Unreleased] — one ### Added entry for the README binding guard (#957), five ### Changed entries covering #956, #957's pointer-ization, #958, #959 and the #962 docs half plus the two unfiled audit findings D2/D4, and one further ### Removed entry for #962's S3/S4/S6 subtractions. Coverage verified per issue with a -P grep (the earlier \b form silently matched nothing — the exact ugrep trap my notes record): #956 1, #957 2, #958 1, #959 1, #960 1, #961 1, #962 2, with a negative control (#999 → 0) proving the method discriminates. NOTE: CHANGELOG.md is TEST-CONSUMED, so this edit stales the chain receipt — the receipt run must come after it, which is the sequencing the next item already carries.

- item: REPAIR from review B — test-opencode-edition.js's A22 block still describes the strip transform this bundle deleted, in the present tense ("are STRIPPED at generation time by transformCommandBody", "This locks the strip-transform", "the strip is now keyed to the Path Intent TITLE"), plus the same claim at :515's declared-transform list and inside several assertion failure messages. I verified it: the strip greps to zero in sync-opencode-edition.js. Same defect class this whole bundle exists to fix, and the one place it missed. The ASSERTIONS must not change — review B measured that they are not vacuous but MORE load-bearing now, since a canonical reintroduction would previously have been eaten silently by the strip and will now flow through and red A22.
  status: done
  dispatched: tdd-guide (test artifact = test custody) → WORKTREE scripts/test-opencode-edition.js comments and message strings ONLY; report → kaola-workflow/bundle-956-957-958-959-960-961-962/repair-a22-comments.md
  result: repair-a22-comments.md — comments and assertion MESSAGE strings only; the diff shows every assert() condition as unchanged context. 563 assertions before and after, exit 0; kimi 521, exit 0. The A22 narration now says what is true: the strips are gone, so a canonical reintroduction flows through transformCommandBody UNTOUCHED and these assertions are the canary that reds on it. It found MORE than I flagged: the L515 declared-transform list had TWO stale items, not one — "comma collapse" is also gone (the only remaining trace is a comment describing the retired unanchored predecessor; no collapse regex exists in code) — and it verified each surviving item live before leaving it, adding "script-path rewrite" (rewriteClaudeScriptPaths, live) so the list stays representative. It dropped the "Mechanism B" note after judging it: it existed to explain why the now-deleted strip lived in the generator rather than canonical, and #538 landed long ago. It also fact-checked its own new wording, noting "full review" appears in two agent profiles but not in any canonical COMMAND, which is what the canary actually reads — so it wrote "no canonical command carries them", which is exact.

- item: The ninth plan-validator site review C found — test-run-chains.js:727 said the deleted script "derives" the plan dir, in the present tense. The audit counted eight and missed it; leaving it would ship a bundle that removed references to deleted machinery with one still standing.
  status: done
  dispatched: self, inline — one comment line, not an assertion, and I implemented nothing this test judges, so spawning an agent for it would have been disproportionate. (I did message the tdd-guide first; it had already finished.)
  result: comment now names today's reader (adaptiveSchema.evaluateChainReceipt) and the project dir it reads from. node scripts/test-run-chains.js exits 0, 283 assertions. Tree-wide sweep confirms the ONLY remaining plan-validator mentions outside the tombstones are the deliberate PAST-TENSE historical referents S5 kept in claim.js across all four trees ("both since deleted … USED to anchor", "the retired plan-validator.js"), which is correct — they explain why a live constraint keeps its historical shape. Tombstone guards untouched at 7 and 1.

- item: REPAIR from review A — MY OWN defect. The #957 pointer wording I passed down said the pair is "defined once" (api.md) and "defined solely by" (conventions.md) the preflight constants, and credited test-route-reachability.js with binding the SKILL prose. Both false: install-codex-agent-profiles.js:92-95 independently authors all four constants (which is exactly what the 444-453 cross-bind exists for), the routing skeletons author the shipped prose values, and that test pins only the EFFORT halves — the model regex is literally `model: "[^"]+"`. Worse, it CONTRADICTED docs/architecture.md:396-404, which a previous bundle's review had already repaired to say the right thing, so two shipped docs disagreed. Third recurrence of this same single-wrong-source defect, and premise-957 had recorded both facts before I wrote the wording.
  status: done
  dispatched: self, in the worktree — my error, my repair
  result: both sites rewritten to mirror architecture.md's existing wording — "authored twice", naming BOTH authoring sites (the preflight constants and the typed literals in templates/routing/*.skeleton.md that actually ship), and stating the binding's true shape (pins the effort, accepts any model string, so a model change is caught by the contract validator rather than the prose check). Verified: gpt-5.6-sol / Sol-medium / xhigh all still absent from both files (no values reintroduced), "defined once" and "defined solely" both gone, and all THREE docs — api.md, conventions.md, architecture.md — now carry "authored twice".

- item: Verify — walkthrough at FULL scope (the fast gate samples a 1/12 shard, so green there is not verified), then the chain receipt run after all test-consumed docs are final, with KAOLA_RUN_CHAINS_CONCURRENCY=serial and --project passed explicitly.
  status: in-flight
  dispatched: self, in the worktree, SERIALLY — nothing else is running, because this suite is spawn-bound and a parallel run produces false reds I could not attribute. Full scope, no --shard. Output → the run log; result recorded here.
  result: FULL-SCOPE WALKTHROUGH GREEN — exit 0, 209/209 scenarios passed, 0 failed, 2400 spawns. The shard envelope confirms it was genuinely full scope and not the fast gate's sample: {"index":1,"total":1,"scenarios":209,"ran":209}. Alongside it this run has green: test-parallel --self-test (23), test-opencode-edition (563), test-kimi-edition (521), test-run-chains (283), test-suite-registration, validate-script-sync, edition-sync --check, generate-routing-surfaces --check (18 surfaces), and validate-kaola-workflow-contracts. The four-chain receipt is deliberately NOT run here — it belongs to the finalize transaction, after every test-consumed doc (api.md, workflow-state-contract.md, CHANGELOG.md, README.md) is final, which they now are; running it early would only stale it.

- item: Finalize the bundle — /kaola-workflow-finalize bundle-956-957-958-959-960-961-962, all-or-nothing across the seven issues, removing all seven .roadmap/issue-N.md sources and regenerating the mirror once.
  status: todo
