# Build ADR 0018 — the forge is the backlog: retire the local backlog layer end to end, in the order §8 forces

> **Run note — where the evidence lives.** Every `result:` below cites `.cache/<name>.md`, and those
> resolve against **the main root's** `kaola-workflow/bundle-984-985/.cache/`, which is the folder that
> gets archived. I dispatched the first ten agents with worktree-relative report paths, so they wrote
> into a *duplicate* `kaola-workflow/bundle-984-985/` tree inside the worktree — a tree that is
> discarded when the worktree is removed at finalize, which would have taken the whole evidence trail
> with it and left every locator in this file pointing at nothing. Caught on a hygiene sweep, not by
> anything failing: I copied all ten to the main root, verified each byte-for-byte against its
> original **before** deleting anything, then removed the duplicate. The worktree now carries exactly
> one untracked path, `scripts/test-priority-list-open.js`, which is a real new suite and must be
> committed. This is the same class as everything else this run kept surfacing — **nothing was red,
> and the record was still on its way to being worthless.**

- item: The pick step reads shortlisted candidates' bodies and comments before claiming (#985, §8 step 1). `next.skeleton.md` today has ZERO occurrences of `comment` / `issue view` / `acceptance criteria`, and the `nx-issue-list` splice in `templates/routing/slots.js` fetches only number,title,state,labels,assignees,updatedAt,url — so work is selected from titles alone. Bind the read to the SHORTLIST (81 open issues unbounded would be ~160 round-trips per pick) and declare the per-forge divergence: `tea` has no porcelain comments view, but its `tea api /api/v1/...` transport in `kaola-gitea-forge.js` makes it expressible. Surfaces regenerate via `generate-routing-surfaces.js --check`.
  status: done
  dispatched: `implementer` (sonnet) in the worktree `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-984-985` — edits `templates/routing/next.skeleton.md` + `templates/routing/slots.js`, regenerates surfaces, and writes its report to `$WT/kaola-workflow/bundle-984-985/.cache/m985-impl.md`. Tests are NOT its to author.
  result: `.cache/m985-impl.md`; 9 files, +99/−0, uncommitted on the branch. New paragraph at the end of Step 2 binding the read to the shortlist ("the handful you are ranking for this claim, never the full list fetched above"), comments overriding the body, said aloud at selection; new `nx-issue-detail-fetch` splice; gitea divergence declared as a `REGION` because `tea` has no porcelain comments view and the read goes through `kaola-gitea-forge.js`'s `tea api` transport. **I verified rather than accepted**: the three forge exports it calls all exist with matching arity (`discoverProject(opts)`, `viewIssue(issueNum, opts)`, `listIssueComments(project, issueNum, opts)`) — no fabricated API; and my own runs gave `--check` 18/18 byte-match and both contract validators exit 0. It also caught a real self-inflicted pin break (the literal `Run it` in Step 2 broke `assertBefore('Write the mission list','Run it')`) and fixed it, and added a `[Unreleased]` CHANGELOG section that the brief did not ask for but `CLAUDE.md` mandates.

- item: Priority gets a forge home BEFORE anything is deleted. Propose the bare `P0`–`P3` labels for the owner to create (ruling 4: I propose, they execute) and connect the sorter dormant since it shipped — `claim.js:268-291`, `priorityTier` matching `/^P\d+$/i`, the `priority_top_tier_labels` knob, and `listOpenIssues` with zero production call sites — as ORDERING, never selecting, so `next.skeleton.md:39` ("You select the target. No script picks for you") stays true. This gates every deletion below: `next_step` is physically where 47 of 81 tiers live today.
  status: done
  dispatched: TEST FIRST — `tdd-guide` (sonnet) in the worktree, authoring the failing suite for a tier-ordering entry point on `claim.js` (new behavioural logic with a real oracle, so custody says the test is not the implementer's). Touches test files only; must not edit `claim.js`, `slots.js` or any skeleton. Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-tier-tdd.md`. The label-creation half is an OWNER action under ruling 4 and stays with me, unstarted. — TDD LEG DONE: `scripts/test-priority-list-open.js`, 17/17 RED at `8ca7e885` (`unknown subcommand: list-open`). I verified it is not vacuous: the `KAOLA_GH_MOCK_SCRIPT` seam sits in `ghExec` (`claim.js:211-216`) which is exactly what `listOpenIssues:279` calls, and the not-selecting assertion is genuine SET EQUALITY plus `length > 1`, not a count check. Second leg now out: `implementer` (sonnet) adding the `list-open` CLI entry point wrapping the dormant `listOpenIssues`, in canonical `claim.js` + the codex byte-mirror + both hand-ported forge copies — **explicitly NOT touching skeletons, slots.js, or regenerating surfaces**, because `region984` holds those and regenerating mid-edit would render its half-finished work. Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-tier-impl.md`.
  result (BOTH LEGS DONE): **Label leg closed 2026-08-15 on owner authorization** — created `P0`/`P1`/`P2`/`P3` on this forge and tiered the two open issues (#985 → P1, #984 → P2), six writes, all in the owner's own repo. **Proved the whole loop end to end rather than just that the labels exist**: `list-open` returns `#985 [P1]  #984 [P2]` — #985 sorts FIRST despite the higher issue number, so the ordering is genuinely by tier and not by number. A sorter that had been dormant since the day it shipped, with zero production call sites, is now doing real work on real data. That is ruling 2 landed, not merely implemented. Owner also authorized deleting this repo's tracked `kaola-workflow/ROADMAP.md` in this run (ruling 3's per-repo consent), and the fence in `impl984del`'s brief was lifted accordingly. — CODE LEG: `cmdListOpen` in all four `claim.js` copies (+52/−4), reusing `listOpenIssues` unchanged. Suite 18/18 green, `validate-script-sync` 0, both contract validators 0, full unsharded walkthrough 210/210. **I did not accept the green — I mutation-proved it.** The tdd reported 17 RED and the impl 18 GREEN, and that count gap is exactly what a quietly-edited test looks like, so I mutated the shipped call site (`listOpenIssues(root)` → `.slice(0, 1)` — ordering silently becoming selecting): the suite red on **6 assertions across 3 test functions** (`expected {1..7}, got {1}`). Restored byte-identical against a pre-mutation snapshot — never `git checkout`, since the file carries uncommitted work — and re-verified green + script-sync 0. Also confirmed the implementer's correction to my brief: `git show HEAD:` proves **both forge ports already shipped all 3 sorter functions before this run**, so the machinery was faithfully ported into four copies and then called from none of them; only the wrapper was owed.

- item: The injected `CLAUDE.md` guidance gets a named region, and the rule gets `<!-- PIN: forge-is-the-backlog -->`. A consumer `CLAUDE.md` carries no marker at all today — the `<!-- SPLICE:in-shared-001 -->` is authoring-side and resolves away at render — so init can only ADD and never retire, which is how `VRPCadCore`'s `CLAUDE.md:114` came to fuse tool text with a user amendment in one line. `test-route-reachability.js` derives a pin's universe from SHIPPED BYTES, as it already does for `consent-in-conversation`. Gates migration. The pin and the region are different mechanisms — do not conflate them, or a fail-closed test lands on a file the tool does not own.
  status: done
  dispatched: `implementer` (sonnet) in the worktree — owns `templates/routing/init.skeleton.md`, the PIN comment in `next.skeleton.md` + `finalize.skeleton.md`, and regenerated surfaces; must first MEASURE whether `test-route-reachability.js` derives its pin universe from shipped bytes (if it does, no test edit is owed at all). Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-region-pin.md`. Must not touch `claim.js`, `slots.js`, or anything under `.roadmap`.
  result: `.cache/m984-region-pin.md`. Region marker is `<!-- KW-CLAUDE-MANAGED-START/END -->` — deliberately NOT `REGION:`, which the renderer intercepts and strips; survival verified inside the rendered fence on all 6 init surfaces. Pin placed on all three skeletons (6 spans). **Its Task-A measurement REFUTED this record's own claim and I re-verified it myself**: the byte-derived universe is purpose-built for `codex-dispatch-model-routing` alone (`test-route-reachability.js:550`); `consent-in-conversation` is LISTED in the hand-authored `templates/routing/required-blocks.js` (16 entries). I had generalised a scoped sentence — the same error class as my §2 misquote. ADR §5 item 9 and §8 step 3 both amended. **Tree is RED by design**: 330 passing / 73 failing, every failure an `orphan-surface` naming the unregistered marker — the reverse sentinel proving the pin mechanism armed by mutation rather than by a green run. Two follow-ons appended below.

- item: `finalize.skeleton.md` has NO "comment what you corrected" prose — only "tag what you file" exists today. ADR §5 item 9's *after* moment requires both, since the next run's read is only as good as this run's write, and it is the write half that closes the loop. Author it inside the existing pin span at Step 7, beside the `filed: #N` paragraph. Discovered by `region984`, which correctly pinned what was there and refused to invent policy prose under a brief that did not authorise it.
  status: done
  dispatched: `implementer` (sonnet) in the worktree — owns `templates/routing/finalize.skeleton.md` + `slots.js` if a per-forge splice is needed + regenerated surfaces. Skeletons are free now (`region984` finished); `claim.js` is NOT (impl984tier holds it). Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-finalize-prose.md`.
  result: `.cache/m984-finalize-prose.md`; `finalize.skeleton.md` +5, lines 244-247, **verified by me to sit inside the pin span** (PIN opens 237, closes 248). No splice needed — the sentence names no forge invocation, matching the "result, not method" register of the paragraph above it. It ISOLATED the pre-existing red rather than reporting around it: with the marker present but its prose removed, still 73/330 with the identical 12 finalize lines, so its addition contributed zero new failures — the marker's presence is what the sentinel flags, not the text inside. My own re-check: `--check` 18/18 byte-match, reachability still 73/330 exactly.

- item: Register `forge-is-the-backlog` in `templates/routing/required-blocks.js` — one entry per surface topic that carries the marker, each with block_id/topic/runtime_tag/surface_type_tag/content_tokens, honouring that file's own non-vacuity floor (at least one token that is NOT a substring of the marker itself, or the entry proves only that the comment exists). **Test custody: this is the judge of the markers `region984` placed, so it is not `region984`'s to author.** Must be written AFTER the finalize prose above is final, or its `content_tokens` pin text that is about to change.
  status: done
  dispatched: `tdd-guide` (sonnet) in the worktree — owns `templates/routing/required-blocks.js` ONLY; skeletons are now FINAL and must not be touched. Goal is reachability green **for the right reason**, and it must mutation-prove each entry by deleting a pin span and confirming that entry alone reds. Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-required-blocks.md`.
  result: `.cache/m984-required-blocks.md`; 3 blocks (`nx-`/`in-`/`fn-forge-is-the-backlog`) + the marker added to `FOREIGN_MARKERS`, 6 single-site mutations each restored from snapshot. Reachability **331 assertions / 0 failures, exit 0**; `--check` 18/18 — both re-run by me. **Its real finding is a mechanism fact:** the reverse orphan-sentinel keys `marker text -> ONE block` via a plain `Map.set`, so three per-topic blocks for one marker silently overwrote each other and produced **61 FALSE `orphan-surface` failures** before the `FOREIGN_MARKERS` entry. I checked that this is not a disarm: `FOREIGN_MARKERS` exempts only the REVERSE sentinel, while the FORWARD per-topic presence obligation plus the manifest-wide non-vacuity floor still red on gutted prose — the file's own consent-in-conversation comment states exactly that division, and the three blocks' tokens are real rule sentences, one per physical span, not the marker alone. The `validate-kaola-workflow-contracts` exit 1 it reported is `impl984stop`'s in-flight `claim.js` mirror, not its own work.

- item: Stop reading the roadmap sources, one independently-green capability at a time: the classifier's offline arm (`classifier.js:292-305` — both the `target_unverified` evidence path and the `blocked by #N` → `depends-on:#N` inference, the two named accepted losses), `projectNameForIssue` (`claim.js:293-300`, whose roadmap door is also #933's second entry), closure-audit's roadmap classes with their finding types and per-edition registries, and the sink's stash bucket (`sink-merge.js:1827-1833`, `:1951`) plus keep-open retention, dual-root reconciliation (`claim.js:2450-2583`) and the `roadmap_source_removed` / `roadmap_regenerated` / `roadmap_regenerated_by_root` envelope fields. Each slice takes its own tests and doc rows with it — deleted WITH the mechanism, never repaired ahead of it. MEASURED PREREQUISITE (mine, verified not relayed): TWO separate registries are in play — `stale_roadmap_sources` / `mirror_lists_closed_issues` are closure-audit DRIFT CLASSES (`closure-audit.js:508-509`), while `main_roadmap_mirror_not_regenerated` is a FINDING TYPE, and `test-forge-finalize-findings.js` enforces the latter three ways: a `>= 5` per-edition floor (`:808`), a set-difference between forge and canonical (`:837`, deliberately not a count — its own comment says a type added on one side and removed on the other keeps counts intact), and — the trap — it parses an ENGLISH NUMBER WORD out of `docs/api.md` (`:877-883`) and compares it to the real set size. That sentence currently reads "The GitLab and Gitea ports raise **eight** finding types where canonical and Codex raise **nine**"; removing one takes it to seven/eight, so **`docs/api.md` must change in the SAME commit** or the assertion reds. Both floors survive (7 and 8 are ≥ 5). SECOND MEASURED PREREQUISITE, and §8 did not have it: **the sink's bucket-1 removal is COUPLED to the closure-side unlink and cannot go first.** `sink-merge.js:1827-1833` exists precisely to catch `kaola-workflow/.roadmap/issue-N.md` for this sink's issues and stash it; without that arm those paths fall to bucket-3 foreign dirt and `sink_blocked`. The paths appear there because closure unlinks them at `claim.js:2471`. So removing bucket-1 while closure still unlinks manufactures the half-migrated brick on any repo still carrying sources — closure-side first, or the same commit. Third fact: this cannot be fanned out. All slices delete scenarios from the SHARED `simulate-workflow-walkthrough.js` (613 roadmap mentions) and the two forge suites (~43 each), so concurrent agents would collide there; single dispatch, internally ordered.
  status: done
  dispatched: `implementer` (sonnet) in the worktree, ONE agent for all four capability slices in a fixed internal order — classifier offline arm, then closure-side unlink + `projectNameForIssue` + envelope fields, then the sink's bucket-1/keep-open retention, then closure-audit's drift classes + finding type. **`docs/api.md` is explicitly NOT its to edit** — I am reserving that file for the doc-dock item so the finding-type count sentence and the envelope rows land in the same commit as the code, without two agents contending for one file. Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-stop-reading.md`.
  result: `.cache/m984-stop-reading.md`. All four slices landed across all four editions, in the ordered sequence — and **all 8 module-load `require`s of `roadmap.js` are gone as a side effect**, which I proved by RUNNING rather than grepping (`claim.js status` → `{"active":[],"drift":[],"count":0}` exit 0, `list-open` offline → `{"issues":[]}` exit 0, `closure-audit` loads exit 0). It found a 4th roadmap touch-point my brief missed (the legacy `postMergeCleanup` read), correctly went slightly outside its file list to drop the `closure-contract.js` field family (leaving them would make `emptyReceipt()` assert `'failed'` forever — a receipt that lies), and **corrected my brief**: `main_roadmap_mirror_not_regenerated` lives in `claim.js`, not `closure-audit.js`. It measured before deleting that `mirror_lists_closed_issues` was always a strict SUBSET of `stale_roadmap_sources`. It stopped at the boundary three times rather than guessing, and each stop was worth more than the work it would otherwise have done: it caught `testFinalize`'s silent-vacuity trap, root-caused the `archive_stage` control that was green for the wrong reason, and — after I could not reproduce its first `git add` explanation — replaced a false general claim with a true narrow one. Eleven walkthrough tests HELD (bootstrap dead, subject alive) and handed to `tdd984fix`. Its own tail: the contract-validator cleanup, in its own item below.

- item: Delete the file and every sentence describing it as ONE movement — `roadmap.js` in all four editions, the `nx-roadmap-*` splices, init's `.roadmap/` bootstrap, the mirror itself, and the roadmap prose in all three skeletons plus `README.md` and `docs/api.md`, with routing surfaces regenerated in the same commit. **The eight module-load requires come out FIRST** (`claim.js:17` and `closure-audit.js:44`, canonical plus all three plugin copies) or claim/startup/resume/finalize/audit break at REQUIRE TIME in every edition — and this is the self-host, so the run doing the deletion is the one `claim.js` finalizes. Verify by RUNNING a claim and a finalize: a green suite loads the same module the same way and passes right up until the file is gone. **PRE-CLEARED AND PROVEN BY RUNNING, not by reading**: all 8 module-load requires are already gone as a side effect of slices 2 and 4 (`grep -rn "require.*workflow-roadmap"` over production scripts in all four editions returns EMPTY), and I exercised the tool rather than trusting that — `claim.js status` → `{"active":[],"drift":[],"count":0}` exit 0, `list-open` offline → `{"issues":[]}` exit 0, `closure-audit` loads exit 0. So deleting `roadmap.js` can no longer break the require graph, and this step's worst hazard is retired before it starts. **MEASURED DELETION SURFACE (survey, not recollection):** four `roadmap.js` copies — canonical, the codex mirror, and the two forge ports — plus **fourteen files that still name them**, several of which §8 step 5 does not: `kaola-workflow-install-manifest.js` (×2 — the `SUPPORT_SCRIPTS` count drops and the retired name needs a prune entry), `validate-script-sync.js` + `test-validate-script-sync.js` (`COMMON_SCRIPTS` / `RENAME_NORMALIZED_FAMILIES` registration), `test-kernel-conformance.js`, `test-forge-roadmap-rules.js` (whole file dies), both forge test suites, both forge contract validators (which check concept terms against their OWN roadmap module and so die with it — canonical's two are already clean), and the walkthrough. On top of that: the `nx-roadmap-*` splices, init's `.roadmap/` bootstrap and its ROADMAP template block, the `in-forge-is-the-backlog` manifest tokens (which today pin the very rules this step deletes), `README.md`'s script table, and the mirror file itself. THIRD PREREQUISITE, found while auditing the pin registration: **`in-forge-is-the-backlog`'s `content_tokens` currently pin the very rules this step deletes** — `'Do not purge \`kaola-workflow/.roadmap/\`; closure removes only the closed issue source file.'` and `'Roadmap/research sessions create or refine issues; ... refresh the mirror.'`. A pin named *forge-is-the-backlog* is today asserting *do not purge .roadmap/*, because `region984` could only pin the prose that existed. So this step rewrites init's pinned block AND its manifest tokens **together, in one commit** — and the tokens are REPLACED because their prose is gone, never "repaired" so the suite keeps passing. That is the exact move `CLAUDE.md` forbids: a test is deleted with its mechanism, never repaired ahead of it. **MEASURED, and one of them is worse than "will die":** of `in-forge-is-the-backlog`'s five `content_tokens`, three go — the two roadmap-lifecycle sentences, and `init.skeleton.md:179`'s *"In `.roadmap/issue-N.md`, `workflow_project` becomes a directory name verbatim … a placeholder like `unclaimed` or `TBD` produces a folder literally called that."* That third one is **already false in the tree right now**: slice 2 reduced `projectNameForIssue` to `return 'issue-' + issueNumber;`, so nothing reads that field at all. This run killed the behaviour and left the prose, and **the pin we added this run is actively holding the falsehood in place** — a third instance of the very pattern this ADR is about, authored by us, inside the mechanism meant to prevent it. Fix the prose and the tokens together in this step. The two survivors are the marker itself and the `priority_top_tier_labels` line. FOURTH, nearly missed: **`_rules.md` survives but its READER dies with the mirror.** `next.skeleton.md:47-48` reaches project rules through the generated `### Project rules` section that `roadmap.js:100-105` appends to `ROADMAP.md` — delete the mirror and the only path to `_rules.md` is gone silently, with nothing red to announce it. The skeleton must read `kaola-workflow/.roadmap/_rules.md` directly, in this same step. Also worth stating because it changes the risk here: **this repo carries ZERO `issue-*.md` sources** (`.roadmap/` holds only `_rules.md` and `.gitkeep`), so no local content is at stake in the tool's own tree — the 287,087 bytes and 47 tier tags the dry-run measured are all in the consumer, and reachable only through step 6.
  status: done
  dispatched: `implementer` (sonnet) as `impl984del`, ONE agent for the whole step — the tree is otherwise idle, and the surface is too interconnected to fan out (four `roadmap.js` copies, fourteen referencing files, the skeletons, the pin tokens, and the shared `simulate-workflow-walkthrough.js`). Carries the four measured traps as named hazards: the silent `_rules.md` reader death, tokens REPLACED not repaired, tests that die only with their subject (stop and report if only a bootstrap broke), and disk-and-index in one movement. **Owner authorized deleting this repo's tracked `kaola-workflow/ROADMAP.md` mid-flight**, so the fence in its original brief was lifted by message. Acceptance is a RUN (`claim.js status`, `list-open`), not a green suite, because a suite loads modules the same way an import does and stays green until the file is gone. Report to `.cache/m984-delete.md` in the MAIN root.
  result: `.cache/m984-delete.md`. All four `roadmap.js` copies gone, `test-forge-roadmap-rules.js` gone, and every live reference cleared across `package.json`, both install manifests, `validate-script-sync.js` + its test, `test-kernel-conformance.js`, all four contract validators, both forge test suites and **all four walkthrough files** — it went beyond my Gates list on the "every sentence describing it" clause and found the three forge/codex walkthroughs crashed on the same retirement once reached, plus two stray comments I never named (`adaptive-schema.js`, byte-identical across four editions and re-synced, and `rename-table.js`). **19/19 gates exit 0**, and I re-ran them all after adding the mirror deletion: surfaces 18/18, both contract validators, route-reachability, sink-merge, bundle-lane, finalize-findings, priority-list-open, and the full unsharded walkthrough. **Run-based acceptance, which is the one that matters here**: with `roadmap.js` deleted outright, `claim.js status` → `{"active":[],"drift":[],"count":0}` and `list-open` → the live two-issue list correctly tier-sorted (#985 P1 first despite the higher number), both exit 0. `_rules.md` and `.gitkeep` survive tracked, and `next.skeleton.md:49` now reads `.roadmap/_rules.md` **directly** — the silent link that would have died with nothing red to announce it. **I deleted the mirror myself** (`git rm`, never `--cached`, disk and index in one movement, `D ` staged with zero untracked residue) because my fence-lift message crossed with its work: 5,294 bytes, on the owner's authorization. Two corrections to my brief, both verified before I accepted them: canonical's contract validators were NOT already clean (both needed real repair), and the install manifest needs no retired-name list because `install.sh:220-231` already sweeps stale support scripts generically — I checked that line rather than taking it, since being wrong there means every consumer carries a dead script forever. Its bucket discipline held: 10 canonical `testRoadmap*` functions deleted outright (subject retired), the bundle-cleanup pair TRIMMED and renamed rather than deleted (its online-archive-integrity core is alive), and 5 offline-claim tests across three editions **ported to the online mock pattern** rather than dropped, because ADR 0018 retired their evidence path and not their subject.

- item: Measure what migrating a real consumer would actually do, without touching it. On `VRPCadCore` (81 sources / 81 open, every check calling it reconciled): enumerate what would be deleted, diff each `next_step` digest against `gh issue view N --comments` and the repo to find the genuinely homeless residue (20-file sample says ~2/20, so single digits repo-wide rather than 81), and confirm the `0 == 81` trigger point in its `CLAUDE.md:114`. REPORT ONLY — the actual migration is per-repo consent under ruling 3, and the mirror must leave disk and index in the same movement, never `git rm --cached`.
  status: done
  dispatched: `investigator` (sonnet), READ-ONLY, in the consumer repo `/Volumes/WorkspaceA/ylminiserver/workspace/vrpcadcore` — mutates nothing there, and writes its measurement to `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/kaola-workflow/bundle-984-985/.cache/m984-migration-dryrun.md` (main root, since it never enters the worktree).
  result: `.cache/m984-migration-dryrun.md`. Manifest 82 tracked files / 287,087 bytes. **Homeless = exactly 2 of 81** (#535, #536, both the "issue-497 family" attribution), measured over 652 atoms against 512 issues + the repo, NOT sampled — so preservation is two comments, not eighty-one. **It corrected ADR §1**: the repo-held-only class does not exist (0/81); #143's `0.1227` is in #393's thread, which a per-issue sample could not see. I re-verified that myself before amending. `0 == 81` trigger confirmed by direct simulation of both sides; sink `sink_blocked` refusal confirmed by code reading with no `.gitignore` entry that would change it. Two surfaces the ADR had not named: 11 further `ROADMAP.md` citations across five live consumer docs (only the consumer can repair those), and `oracle_495_498_fillet_landed_honesty.rs:62` already citing a `.roadmap/issue-495.md` deleted long ago — **the dangling-citation failure mode already exists today**. All three amendments landed in ADR §1, §8 and the method note.

- item: Re-bootstrap `scripts/test-forge-bundle-lane.js` (~700 lines, 40 legs, currently RED). It is NOT a roadmap test — it is the mutation-proven oracle for #862, where both forge claim scripts once silently dropped `selectionRecordBytes` on the bundle lane, and **that mechanism is not retired**. Its every leg bootstrapped an OFFLINE bundle claim on roadmap-file evidence, which slice 1 removed, so all legs now die at `target_set_unverified` before reaching their subject. Deleting it would break the rule from the wrong end: a test dies with ITS mechanism, and this one's is alive. The redesign keeps `KAOLA_WORKFLOW_OFFLINE=1` and keeps the hostile exit-97 forge shim (the file's whole point is proving the claim never shells the forge offline) and swaps the evidence source to the seam that already exists — `KAOLA_CLASSIFIER_MOCK_SCRIPT` (`claim.js:1096-1101`, #495), which replaces the classifier wholesale with a controllable green verdict. No new machinery, hermetic guarantee intact. **SCOPE GREW — this was the run's closest call.** `impl984stop` reported the same breakage in `simulate-workflow-walkthrough.js` (69 calls to one `plantRoadmapIssue` helper at `:896-908`) and classified it into three buckets, then began deleting bucket 2 — tests whose SUBJECT is alive (`testClaimStatusRelease` = claim→status→release) and which merely borrowed offline roadmap evidence to reach `acquired`. It argued this differed from bundle-lane; it does not. That is an argument about SCALE, not shape, and the shape test is only ever *is the mechanism under test retired?* I stopped it and measured the alternative: **`plantRoadmapIssue` is ONE function, so re-pointing it at `KAOLA_CLASSIFIER_MOCK_SCRIPT` keeps all 69 call sites working untouched** — one helper instead of dozens of deletions. Bucket 1 (subject IS the retired mechanism) still deletes; bucket 3 stays. Both re-bootstraps go to `tdd-guide` as one job, since re-implementing a test helper is test authoring and the party who broke it may not rebuild it. Watch the helper's `body` parameter: some callers pass classifier-relevant content, so the shim needs a per-test verdict. **NAMED TRAP, found by `impl984stop` and worth more than the deletions it prevented:** do NOT substitute a lighter fixture for a real `startup`. Its own worked example is `testFinalize` — swapping `startup` for the file's existing `plantActiveFolder` helper looks safe because no assertion touches the swapped part, but `startup` seeds `next_command` / `next_skill` and the test asserts those are NEUTRALIZED at archive time; `plantActiveFolder` never writes them, so those assertions go **green while checking nothing**. That failure mode is worse than the red it replaces. It is also precisely why the fix is to re-point the helper and leave every `startup` call in place: nothing downstream of the classifier changes, so nothing can go vacuous. **The per-test question is one line** — *if `plantRoadmapIssue` still produced a green verdict tomorrow, would this test still be meaningful?* Yes → untouched. No → bucket 1. By that test `testClaimStatusRelease` (claim→status→release, alive) was MIS-BUCKETED for deletion and I halted it; `testKeepOpenInvariantUnit` and `testClosureAuditKeepOpenExclusion` are true bucket 1 because the thing they pin is itself gone. **THIRD fixture job, and the most interesting: `test-forge-finalize-findings.js`'s linked-run legs were green for the wrong reason.** On a linked run the archive dest resolves outside the worktree by design, so `existingPaths` was ever only populated by `.roadmap`/`ROADMAP.md` — present in that fixture solely because `reconcileRoadmapForClosure` called `regenerateRoadmap(root)` and **incidentally wrote a file the test then staged**. So the leg asserted "a healthy linked finalize stages the archive" while actually observing "roadmap regeneration drops a file in the worktree". Retirement did not break it, it REVEALED it, and 4 of the 12 failures are this one root cause. RULING: `'skipped'` is the honest post-retirement answer and the assertion moves to say so — **do NOT plant roadmap-shaped content to restore `'staged'`**, which would rebuild the same accident. The fault-injection leg (`archive_stage_failed`/`archive_unstaged`) sits inside `if (existingPaths.length > 0)`, which a linked run can no longer enter, so it moves to a NON-LINKED fixture where the block is genuinely reached. `archive_stage` itself stays — still meaningful off the linked path.
  status: done
  dispatched: `tdd-guide` (sonnet) as `tdd984fix` in the worktree, ONE job covering all three fixtures — the `plantRoadmapIssue` helper re-point (69 call sites, one function), `test-forge-bundle-lane.js`, and the `test-forge-finalize-findings.js` linked-run legs. Owns those three test files; `docs/api.md` is docked and NOT its to touch. Required to mutation-prove every rebuilt fixture still reds on the defect it claims to pin. Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-fixture-rebuild.md`. Later correction sent: the file's one surviving `static:` failure (`archive_unstaged`) is ALSO its — `:537` reads that field off a live run's `bad.tx`, so it is downstream of the broken fixture, not a doc gap.
  result: `.cache/m984-fixture-rebuild.md`. **Walkthrough exit 0, 186/186** and **`test-forge-finalize-findings` exit 0, 253/0** (from 244/9) — both re-run by me unpiped, and the `static: archive_unstaged` failure resolved itself exactly as predicted once the rebuilt fault-injection leg genuinely enters `if (existingPaths.length > 0)`. `validate-script-sync` 0. The helper re-point worked as designed: all 69 call sites untouched, all 11 held tests alive. Four needed a SECOND fix beyond the bootstrap (`buildAuditReport` no longer folds roadmap-source issue numbers into its candidate set) and it found a **12th casualty on nobody's list** — `testWatchPrAbandonedClosureInvariantsClean` fed a still-live `roadmap.js generate` through the helper's old file write, a different consumer than the classifier. It also caught its OWN first pass by running it: `archive_stage` is edition-dependent (gitlab/gitea set `'staged'` unconditionally), so asserting `'skipped'` for all four redded two editions for the wrong reason. And it corrected one of my rulings with a measurement — a genuinely non-linked fixture for the fault-injection leg is **structurally impossible** (`result.dest` never resolves inside a linked worktree root), so it reused Part C's own working technique and flagged the deviation instead of silently satisfying the letter of my instruction. Concurrency hazard I created was a non-issue and the reason is worth keeping: it used `Edit` (string-match against live content) throughout, never `Write` (full replacement) — **`Edit` is inherently safe against a concurrent editor; `Write` is not.**
  → follow-up to file at finalize: a control that passes because of an unrelated side effect is a defect in its own right, independent of this ADR.
  → THIRD follow-up: `OverCapFallsBack` does not red under its mutation on this Node version — pre-existing, unrelated to this ADR, found while mutation-proving. A pin that cannot fail is not a pin.
  → FOURTH follow-up, found on a pre-chain tree-wide sweep: **production code still names the deleted mirror**, surviving only because its own defensive filters make it a no-op. `sink-merge.js:2520-2522` still builds `roadmapPathspecs` from `.roadmap/issue-N.md` per member plus `kaola-workflow/ROADMAP.md`, and `claim.js:4825`'s `archive_stage` candidate list still carries `ROADMAP.md` (its `.roadmap` sibling is still meaningful — `_rules.md` lives there). Neither breaks anything: the sink filters to paths that exist on disk or are tracked at HEAD, so an unmatchable pathspec cannot abort `git add`, and `stagedRoadmap` being permanently empty is the **already-understood** condition that exposed the `git add` prefix bug. **Deliberately not touched**, and the reason is consistency rather than caution: `tdd984fix` argued the same case for inert `plantRoadmap` writes in the forge walkthroughs — zero functional gain, nonzero regression risk on green suites — and I accepted it; I cannot hold a different standard for my own convenience minutes before a four-chain run. But a future maintainer reading `roadmapPathspecs.push('kaola-workflow/ROADMAP.md')` will believe the sink still stages a mirror that this same commit deletes, so it is dead weight that reads as a live claim.

- item: Port the `KAOLA_CLASSIFIER_MOCK_SCRIPT` seam into the two forge claim ports so `test-forge-bundle-lane.js` — the mutation-proven #862 oracle — can run there at all. Measured and confirmed by me: canonical spawns the classifier as a subprocess and honours the env var (`claim.js:1095-1101`, #495), while both ports call it IN-PROCESS (`kaola-gitlab-workflow-claim.js:876-880`, gitea `:880-884`) with **zero** `CLASSIFIER_MOCK` references, so the suite cannot control the verdict there and all 22 remaining failures are gitlab/gitea. **Narrowing the file's scope was the tempting fix and it is wrong**: #862's defect lived in *both forge claim scripts*, so dropping the forge editions would delete coverage of exactly where the bug was. Only the mocked branch is new; the shipped in-process path stays byte-identical.
  status: done
  dispatched: `implementer` (sonnet) as `impl984seam`, the two forge ports only — canonical and the codex mirror already have the seam and are fenced off. Judge is `test-forge-bundle-lane.js` reaching exit 0 without the test being edited or any assertion weakened. Report at `.cache/m984-forge-seam.md` (main root; it wrote to the worktree path from its original brief before my correction landed, and I rescued it byte-identically).
  result: `.cache/m984-forge-seam.md`; +19 lines in each forge port's `classifyIssue`, spawning the mock exactly as canonical does and matching its contract (env var, spawn form, empty-output fallback). The shipped in-process `classifier.classifyIssue(issueIid, root)` is byte-identical behind an early return — I confirmed it still present at gitlab `:900` / gitea `:904`. **`test-forge-bundle-lane.js` exit 0, verified by me.** The integrity check that matters: the assertion total is **unchanged at 59** (previously 37 passed + 22 failed), so the green came from the seam and not from assertions being dropped. It deliberately did NOT port canonical's retry/backoff (a mock cannot fault, so the retry would be unfalsifiable) or the `KAOLA_ISSUE_STATE_SNAPSHOT` injection (it feeds a roadmap-evidence read this ADR retired) — both are the right call and both were stated rather than silently skipped. It also flagged, without touching it, that `editionSupportsClassifierMock()` and its comment block are now dead and FALSE — the limitation they describe is exactly what this port removed; sent back to `tdd984fix` as a cleanup, since a comment asserting a constraint the tree no longer has misleads the next reader as surely as a stale test.
  → SECOND follow-up, same shape, found in `sink-merge.js`'s `archive_commit`: a **real `git add` pathspec bug, reproduced on git 2.54.0**. When an `:(exclude,glob)` names a directory that is a strict string PREFIX of the include's leaf directory (`kaola-workflow/issue-9500/**/sink-receipt.json` against include `kaola-workflow/archive/issue-9500-archived-XYZ/`), `git add` exits **0 and stages NOTHING**, with a real file sitting under the include. I ran the agent's verbatim repro (reproduces) and then a clean 2×2 varying one component at a time — only the prefix cell fails. **Its first explanation was too broad and it said so itself after I could not reproduce that version**; the narrow one is correct. My own added measurement: the glob TAIL is a second independent trigger — same non-prefix pair stages fine with `**/sink-receipt.json` but stages NOTHING with `**/x` or `**` — so the comment's "an unrelated subtree is safe" boundary is scoped to our tail, not general, and I had it add that clause. **This bug could always have fired**: #700's collision-suffixed dest always shared that prefix, and a second include merely masked it. Retirement removed the mask. Two independent cases this run of the retirement REVEALING something already wrong, which is worth an issue rather than a code comment.

- item: Collapse the now-dead `door: 'startup'` ternary at `simulate-workflow-walkthrough.js:3055` (`const argv = c.door === 'startup' ? … : …`). I ruled it dead and `impl984stop` reported the thread closed, but **the branch is still live** — the only other `door: 'startup'` in the file is its own retirement comment, so no surviving case can reach it. Held rather than done now because `tdd984fix` currently owns this file. Small, but it is exactly the residue-we-authored class, and it survived a "closes everything" report — which is the argument for checking rather than accepting closure claims.
  status: done
  result: `impl984stop` collapsed `argv` to its one surviving form and dropped the now-constant `door:` key from the three remaining cases; `--only testClaimNeverAdoptsReservedDir933` PASSED (3/3), sweep exit 0, walkthrough unchanged. I verified: the single surviving `door: 'startup'` match is its own retirement comment, not a reference. **My own error to record: I sent this ruling and dispatched `tdd984fix` onto the SAME file in one turn**, so two agents held `simulate-workflow-walkthrough.js` concurrently. No damage — `plantRoadmapIssue` was still pristine when I checked, so nothing had been clobbered — but the read-modify-write race was real and mine to have avoided. Warned `tdd984fix` to re-read before its first write rather than trusting a stale in-context copy.

- item: Dock `docs/api.md` against the retirement — reserved for me deliberately, so the test-parsed finding-type count sentence and the envelope/receipt rows land in one commit from one hand rather than two agents contending for one file.
  status: done
  dispatched: I hand-edited the six highest-risk sites myself (the guarded count sentence `nine/eight` → **`seven/eight`**, the `target_unverified` row, the finalize-envelope roadmap fields + their `roadmap_regenerated_by_root` bullet, the `findings` fault-name list, the three roadmap closure-invariant bullets, and the closure-receipt roadmap fields with their `roadmap_removed`/`roadmap_residue` bullets), then dispatched `implementer` (sonnet) as `impl984docs` for the mechanical remainder, owning `docs/api.md` ALONE, with acceptance = a final `grep` in which every survivor is categorised and **no fourth category is allowed**.
  result: `.cache/m984-apidocs.md`. It found **three more the itemized list missed**, the largest being a whole "Closure cleanup is automatic" paragraph still asserting `reconcileRoadmapForClosure`'s retired behaviour as current fact. It also renumbered the closure invariants 1-7 → 1-5 and repaired every downstream citation of the old numbers. Final grep: 30 hits, all category (a) still-live or (b) marked history, zero out-of-scope. It correctly LEFT `roadmap_staged` and the roadmap-staging step alone after verifying both against current `claim.js` — still accurate — and left the whole roadmap.js script-reference section, since that script is not retired until step 5. **My verification:** count sentence intact and its assertions now pass; the file moved 241/12 → **244/9**. One `static:` failure survives, and I traced it rather than assuming it was my gap — `:537` reads `archive_unstaged` off a LIVE run's `bad.tx`, so it is downstream of the broken linked-run fixture, not a doc error. Reassigned to `tdd984fix` with the evidence.

- item: Delete the retired concept tokens from the three contract-validator copies. Both `validate-workflow-contracts.js` and `validate-kaola-workflow-contracts.js` now crash at `assertConcept` — *"docs/api.md must document closure contract invariants and receipt schema; missing: roadmap_source_removed"* — because they pin a token list that still names retired fields. Bucket 1: delete with the mechanism, and do NOT re-add anything to `api.md` to satisfy them. **The timing is the finding**: these validators were GREEN for the entire period the documentation was wrong, because they only check that api.md *mentions* a token — nothing ever checked the token still had a live field behind it. Removing the field from the code could not have reddened them; only removing it from the doc did. Same shape as the run's two silent-vacuity finds: a check that passes for a reason unrelated to what it claims to enforce.
  status: done
  dispatched: `impl984stop` (sonnet, re-dispatched — it knows exactly what it retired), all three copies, fenced off `api.md` and the three files `tdd984fix` holds. Report to `$WT/kaola-workflow/bundle-984-985/.cache/m984-contract-validators.md`.
  result: `.cache/m984-contract-validators.md`. Exactly ONE stale token existed — `'roadmap_source_removed'` in the `assertConcept('docs/api.md', …)` list — in both canonical and its byte-identical plugin twin; it checked the other nine candidates and the four retired invariant IDs against current code and found none of them were ever referenced, rather than deleting on my say-so. The third copy is `plugins/kaola-workflow/scripts/validate-workflow-contracts.js`, byte-identical, and that identity is enforced TWICE independently (`validate-kaola-workflow-contracts.js`'s own `sharedScripts` loop, plus `validate-script-sync.js`) — which is what threw when only canonical was fixed, so it did not stop at the first green. It also checked the gitlab/gitea contract validators before assuming scope and found neither references `docs/api.md` at all, confirming exactly three copies. **My verification, unpiped:** all three exit 0, and `diff -q` confirms the plugin twin byte-identical.

  → **How I found it, recorded because the near-miss is the lesson:** my checkpoint ran the gates as `out=$(node ... | tail -1)` and read `$?` — which is **`tail`'s** exit, not node's. All seven printed 0. Two were actually crashing, and the giveaway was a last line reading `Node.js v24.18.0` — a stack-trace trailer, not a success message. This repo already carries that exact caution and I walked into it anyway. Re-ran without the pipe and got the real exits.

- item: Dock the REMAINING documentation — `CHANGELOG.md`, `README.md`, `docs/README.md`, `docs/architecture.md`, and (discovered late) `docs/api.md`'s script-reference section plus `docs/workflow-state-contract.md`.
  status: done
  dispatched: `CHANGELOG.md` written by me directly (four `Added` entries, one `Removed` for the retirement, one `Fixed` for the `git add` prefix bug) — kept inline because the house style leads with the observation that forced each change, and that reasoning is freshest now; recovering it later loses exactly the half a reader needs. `README.md` + `docs/README.md` + `docs/architecture.md` → `implementer` as `impl984readme` (report `.cache/m984-readme.md`). `docs/api.md`'s stale section + `docs/workflow-state-contract.md` → `implementer` as `impl984docs2` (report `.cache/m984-docs2.md`). I also updated ADR 0018's own Status to record steps 1-5 shipped and step 6 outstanding — per ADR 0017's precedent that a banner exists only while the decision and the code disagree.
  → **A sequencing gap I created, worth naming because step 6 can repeat it.** `docs/api.md` was docked BEFORE the deletion step. At that moment `roadmap.js` still existed, so `impl984docs` **correctly** left its script-reference section alone and said so in its report. Then the script was deleted and nobody went back — leaving one file internally inconsistent: current envelope and receipt sections beside a section documenting a deleted script as live API. Nobody erred; the order did. `docs/workflow-state-contract.md` was never in any brief at all. Both found by `impl984readme` reading past its own scope rather than stopping at its grep.
  → I also briefly suspected `list-open` was a CLI nothing called, since I promised to wire the pick flow myself and never did. **Unfounded** — `impl984del` wired it during its prose pass: `next.skeleton.md:48` names the tier-ordered list as a ranking source, `:101` is the call, and the old `nx-issue-list` splice is gone. Checked before reporting it as a gap.

- item: Run the validation chains and own the verdict. The diff is edition-touching, so `run-chains.js` fails closed to all four chains; the walkthrough must pass at FULL scope, not the rotating 1/12 shard, before any claim that the suite is verified.
  status: done
  dispatched: mine, not delegable — the finalize transaction only READS the receipt and must never produce it. Implementation committed first at `25054b07` (88 files, +2,074 / −10,415) because the receipt binds to a `headSha`, so a chain run over uncommitted work would bind to a commit that does not contain it. Receipt written via explicit `--output` to the MAIN root's `.cache/chain-receipt.json`, since `--project` resolves at the git top-level and from the worktree that would rebuild the duplicate project folder I had just cleaned out.
  result (ATTEMPT 1 — RED, one root cause): **`RUN-CHAINS EXIT: 1`.** Exactly ONE distinct failure across the whole run, 252 passed / 1 failed, attributed to three chains because `run-chains` hoists shared steps into a one-time preamble. `test-forge-finalize-findings.js`'s **positive control lost its witness**: `docsRow('migrate')` reads a table row out of `docs/api.md`, and that row went with the `## Roadmap Operations` section this run deleted → `Row: undefined`. The control is well-made and its comment states its purpose — without it a broken edition-name matcher would pass the "no qualifier" assertion and fail the "must be scoped" one **for the wrong reason**. Mechanism alive, witness dead: the third instance of that shape this run, so it is re-pointed by `tdd984fix`, never deleted. **The timing is the lesson**: I ran this exact suite directly earlier and got exit 0 — but that was BEFORE the api.md section was deleted, and nobody re-ran it after. Every single-suite gate was blind to it; the four-chain run was not. That is precisely why the chains belong after all code and all test-consumed prose has landed, and not before.
  → My own slip, twice in one day: I wrapped the run in `| tail -40`, so the background task reported **exit 0** — the exit of the final `echo`, not of the chains. The real verdict was in the `RUN-CHAINS EXIT:` line I printed myself. Do not pipe a gate whose exit code you intend to read.
  → **The attempt history is the finding, not the noise.** Five reds, each a DIFFERENT guard, and after attempt 1 every one of them was `claude`-only with codex/gitlab/gitea stable green:
    1. `4ae77702` — 7 fails, `test-forge-finalize-findings.js`: a positive control whose witness (`docs/api.md`'s `migrate` row) was deleted this run. Mechanism alive, witness dead → re-pointed to the install-manifest row. `tdd984fix` rejected `archive_unstaged`'s row for a reason worth keeping: **it is the row the downstream assertions already read, so using it would make the control depend on the thing it exists to independently verify.**
    2. `53aa2b23` — 7 fails, `test-finalize-door.js:567`: asserted closure REMOVED the roadmap source. Subject ("a finding does not withhold closure") alive and already asserted directly by `passedWithFinding`'s `out.status === 'closed'`; the roadmap check was a redundant secondary witness built on retired machinery. Deleted, coverage proven elsewhere first.
    3. `a8ebf58e` — **0 FAIL lines**, exit 1: `test-suite-registration.js`. `scripts/test-priority-list-open.js` was **in no chain, so it never ran**. My omission — `tdd984tier` flagged it at authoring time, I recorded it and did not act. **A suite nobody runs is not coverage, however good its assertions.**
    4. `f1577407` — 0 FAIL lines, exit 1: `test-spawn-classification.js`, 2 unclassified spawn sites in that same new file. Both are `cli-contract`. **My first fix made it worse** (2 violations → 3): I put the rationale on the marker line, and the guard parses everything after `spawn-class:` as the class token. Read the recogniser before guessing its format.
    5. `f1577407`+ — 6 fails, `test-generate-routing-surfaces.js:288`: `requiredByTopic.init` still pinned `kaola-workflow/ROADMAP.md`. Dropped, **and replaced** — the new backlog rule had inherited the old mirror rule's place in the prose but not its protection.
  → **What the pattern says.** Every single-suite gate I ran was green at the moment I ran it. Five separate cross-file couplings were invisible to all of them and visible only to the chains — a test reading another file's content as a witness, a suite absent from the runner, a spawn missing its declaration, a token list naming retired prose. **The chains do not re-check what the suites check; they check that the suites are wired into the system at all**, and that is a different question with a different failure mode.

- item: FOLLOW-UPS to file at finalize — settled to THREE, after dropping one on measurement rather than filing it on a hunch.
  status: todo
  → **FILE (1)** `OverCapFallsBack` does not red under its mutation on this Node version. Found while `tdd984fix` was mutation-proving something else; pre-existing and unrelated to this ADR. **A pin that cannot fail is not a pin**, and this run demonstrated the cost of that class five separate times.
  → **FILE (2)** Production code still names the deleted mirror — `sink-merge.js:2520-2522`'s `roadmapPathspecs` and `claim.js:4825`'s `archive_stage` candidate list. Harmless (both are filtered to paths that exist or are tracked), but a maintainer reading `roadmapPathspecs.push('kaola-workflow/ROADMAP.md')` will believe the sink stages a mirror this same commit deletes. Dead weight that reads as a live claim.
  → **FILE (3)** ADR 0018 §8 **step 6 — consumer migration**, the remaining and riskiest step, so #984 can close honestly on steps 1-5. Carries everything the dry run measured: 82 tracked files / 287,087 bytes, exactly 2 homeless lines needing a preservation comment (#535, #536), the consumer `CLAUDE.md:114` assertion that reads `0 == 81` the moment sources are deleted in a file this tool cannot edit, 11 further `ROADMAP.md` citations across five live consumer docs only the consumer can repair, and the mirror leaving disk and index in ONE movement.
  → **FILE (4)**, surfaced by `tdd984fix` volunteering it rather than being asked: `test-finalize-door.js`'s T11 `roadmap_staged === false` assertion **cannot exercise the `archiveAddOk &&` gate** that #907's own comment describes. It is not vacuous — hardcoding `roadmap_staged = true` is caught — but removing just that gate while keeping `existingPaths.some(...)` is **not**, because the fixture's repo builders never populate `.roadmap` / `ROADMAP.md`, so `existingPaths` is structurally empty there. **This is a FIXTURE gap, not a dead production path**: `.roadmap/` still exists in a real repo (it holds `_rules.md`), so `claim.js:4825`'s candidate list can still be non-empty in production. **RULED: accept the gap, do not plant roadmap-shaped content to close it** — that is precisely the move I forbade for `archive_stage`, and those paths no longer exist, so planting them would be planting a fiction to satisfy a test.
  → **DROPPED, and measured rather than assumed:** "audit other `git add` sites for the exclude-prefix shape." Swept every `:(exclude` construction in production across all four editions — **all of them are in `sink-merge.js`, and the only two real pathspecs are the `**/sink-receipt.json` / `**/sink-fallback.json` pair already fixed.** There is no second instance, so the issue would have been speculative work for a class of one.
