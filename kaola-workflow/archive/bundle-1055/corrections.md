# Corrections to the bundle-1055 records (appended 2026-09-10 by the bundle-1056 run; nothing below rewrites a completed Mission result)

Source: the owner's independent acceptance of #1055 on main `e72407b8`, and the orchestrator's re-read of the records.

| record | inaccurate statement | correction |
|---|---|---|
| `finalization-summary.md` › Delivered | "export key sets of all five sync scripts unchanged" | True only for the first candidate `8791ab55`. The second candidate `c0abadc9` retired the exported-but-unconsumed `ZCODE_MODEL_DISPATCH_BLOCK`, `ZCODE_MODEL_DISPATCH_GUIDANCE`, `OPENCODE_MODEL_DISPATCH_BLOCK`, `OPENCODE_MODEL_DISPATCH_GUIDANCE`; the export key sets of `sync-zcode-edition.js` and `sync-opencode-edition.js` shrank by two each. grok/kimi/cursor are unchanged. The render oracle and edition suites do not depend on those keys. |
| `finalization-summary.md` › Delivered, Acceptance walk; `mission-list.md` item 7 result | "103 real sink envelopes … measured unchanged" | The 103 envelopes were captured by instrumenting `runSinkAt` in `scripts/test-sink-merge.js` and running that suite in two clones: `gh` is the suite's mock, `KAOLA_WORKFLOW_SKIP_TESTGATE=1` skips the post-rebase test gate, and the remotes are local bare fixtures. This is a mock-forge, in-repo equivalence measurement of the sink transaction, not a real-forge verification. `adversarial.md` §B3 states these limits; the summary dropped them. |
| `audit.md` › dispositions (sink-pr → claim: `defaultBranch`) | "`OFFLINE`, `REMOTE_TIMEOUT_MS`, all of which adaptive-schema already defines" | Wrong. On `5cb85515` adaptive-schema defined neither; the implementer verified this and added per-module copies (`impl-section2.md`). The orchestrator misread a grep whose lines came from claim.js. |
| `finalization-summary.md` › Follow-Up Items; `mission-list.md` item 7 result | the load-order `OFFLINE` capture is a "latent" divergence recorded in #1056 as a follow-up | It is a behaviour change of a public export introduced by #1055: `defaultBranch` used to consult the env captured at claim's load; after the move it consulted the env captured at adaptive-schema's load, so any caller that loads the kernel first and sets `KAOLA_WORKFLOW_OFFLINE` / `KAOLA_GH_REMOTE_TIMEOUT_MS` afterwards got network probes and the default timeout. The owner reproduced it on main with an `execFileSync` mock. Fixed under #1056 (env consulted at call time), with regressions in `scripts/test-issue-1056-default-branch-env-contract.js`. |
| `adversarial.md` §S1, `acceptance.md` › Review follow-up | each file carried one raw NUL byte in its prose (the very byte they describe) | Replaced by the six-character escape text (backslash, `u`, `0000`); no other byte changed (`git diff --stat`: 1 line each). |

Kept as recorded: the legacy non-`--sink` pipeline and the 260 `refs/kaola-workflow/barrier/*` refs remain untouched; the two HUMAN_DECISION_REQUIRED items stay open for the owner.
