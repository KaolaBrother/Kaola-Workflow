# #1055 audit on the implementation baseline

Baseline: `5cb85515` (main = origin/main = v11.0.0 + bundle-1054 archive evidence). Drift from the audit candidate `db6af1be`: `scripts/test-issue-1054-*` only (+5 lines); production scripts unchanged; 33 files / 30,734 lines reproduce. Detail and verbatim references: `audit-generators.md` (§1, static candidates, §3 generators, mirror mechanics) and `audit-lifecycle.md` (§2, §3 host adapter, conditional items). The orchestrator re-ran the identity probe (`claim.getCoordRoot === schema.getCoordRoot` etc.: all four `true`) and the barrier-ref count (260) independently.

## Dispositions

| item | basis | disposition |
|---|---|---|
| `transformCommandBody` line loop ×5 | pure copy loop; normalisation is `split(/\r?\n/)` + `join('\n')`; zcode also carries an unused `block` local | delete loop, keep normalisation (§1) |
| `ZERO_HASH` ×5 (+ unused `agentGen.ZERO_HASH` binding where destructured) | declared, never referenced, not exported; generate-agent-profiles keeps its own live copy | delete (§1) |
| `lowerSet` in grok/cursor/opencode | never called, not exported | delete (§1) |
| `cursorModelPin` + `CURSOR_MODEL_CLASS_PINS` | `renderAgent` delegates to `renderRuntimeRole('cursor')`; real pins live in `templates/agents/runtime-capabilities.json` via generate-agent-profiles; only consumer is a source-regex read in `test-cursor-edition.js:674-686,838-842` | delete after the test author migrates the pin to the rendered output (§1) |
| sink-merge `getRoot`, `requiredArchiveFiles` | 0 callers, not exported; `scanArchiveTree(...).required` is called directly; `test-sink-merge.js` mentions the name only in prose | delete; the `#901 ignoredUntrackedUnder` comment block that follows stays verbatim |
| validation-runner `computeLandableBlobEntries` | exported, 0 callers in production or tests across all 4 copies; added for the DAG-era `plan-validator` gate (CHANGELOG, #740) whose producer and unit suite were deleted with ADR 0017/0018; `docs/api.md` documents validation-runner as a CLI only | delete (exported-but-orphaned residue of a removed producer) |
| codex-preflight `scopeIsFresh` | 0 callers; live gate calls `scopeIsStale` directly; the plugin walkthrough's test (c) drives the CLI as a subprocess and does not depend on the symbol | delete |
| sink-merge → claim: `getCoordRoot`, `mainRootFromCoord`, `resolveMainRoot`, `readActiveFolders` | `===` pure re-exports of adaptive-schema / active-folders | sink-merge imports from the owners; claim's re-exports stay (public export surface, other consumers) — pure relocation (§2) |
| sink-merge → claim: `removeWorktree`, `buildClosureReceipt`, `checkClosureInvariants`, `defaultBranch`, `appendClosureBlock`, `clearAdvisoryClaim`, `resolveProjectSlug`, `worktreePathFor`, `archiveProjectDir` | claim-native; consumed by claim + sink-merge only | keep in claim: a new closure/archive service module would add an installed file to 7 runtimes and 2 hand-ported forges for a single external consumer (§2, keep) |
| sink-pr → claim: `defaultBranch` | claim-native; the only claim symbol consumed by 2 modules; depends only on `execFileSync`, `OFFLINE`, `REMOTE_TIMEOUT_MS`, all of which adaptive-schema already defines; measured require cost 9.2 ms (claim) vs 1.6 ms (schema) | move `defaultBranch` into adaptive-schema (the shared kernel already required by sink-pr and sink-merge); claim re-exports the same object so `claim.defaultBranch` and the gitlab/gitea hand-ported sinks are untouched — pure relocation (§2) |
| Cursor host adaptation in claim (`1959-2236`) | single-sited, reachable only from `startup`/`resume`, pinned by `test-issue-1052-cursor-cli-startup-prep.js`, no duplicate elsewhere | keep: extraction would add an install-manifest file for one consumer and move the failure envelope across a module boundary for no second consumer (§3, keep with reason) |
| generator helpers `parseFrontmatter`, `parseTools`, `yamlScalar`, `listCanonAgents`, `listCanonCommands`, `canonCommandPath`, `commandRel` | byte-identical across the sync scripts that define them; all five already require `runtime-edition-forge`; install scripts run the generators from the repo checkout and copy none of them; `treeLabel`/`runCheck`/`runWrite` diverge by design | share via `runtime-edition-forge.js` exports where the body closes over only its arguments and forge layout; keep per-runtime write/prune/permission logic separate (§3, implement bounded) |
| non-`--sink` legacy pipeline (~732 lines: main() tail + 5 exclusive guards) | documented public interface (`docs/api.md:985-1013`, load-bearing guard differences); 11 legacy invocations in `test-sink-merge.js` across 4 editions; generated finalize surfaces call only `--sink` | keep; support policy is a HUMAN_DECISION_REQUIRED item (see below) |
| `--codex-dispatch-mode`, `--workflow-path`, `--attest-contractor-spawn` | accept-and-warn shims so old callers never hit `unknown_flag` | keep (documented deprecation shims) |
| legacy state blocks (`removeLegacyStateBlocks`), `workflow-plan.md`, `fast-summary.md`, `phaseN-*.md`, `headSha`-only receipts | explicit write-time strip / tolerant read paths | keep |
| legacy sibling worktree layout | consumed only by the `legacy-worktree-cleanup` migration command, dry-run by default | keep |
| barrier references (42 in claim) | archive-time reap is live on every archive; `barrier-ref-sweep` is a manual command; producer deleted; 260 stale refs (2026-07-07..07-15) exist on this machine, none younger | keep code; the refs are external state and are NOT touched (HUMAN_DECISION_REQUIRED) |
| adaptive-schema "barrier" hit | scratch-index filename, unrelated | no action |

## HUMAN_DECISION_REQUIRED (no action taken by this run)

1. **Legacy non-`--sink` sink-merge pipeline support policy.** Evidence: in-repo generated surfaces only call `--sink`; docs/api.md documents the legacy guards as public; external callers cannot be enumerated. Options: (a) keep indefinitely (current; zero change); (b) announce deprecation in CHANGELOG/README now, retire in a later major with a migration note pointing to `--sink`; (c) retire now. Recommendation: (b) at the earliest, and only by an explicit owner ruling; this run keeps it.
2. **260 stale `refs/kaola-workflow/barrier/*` refs on this machine.** Evidence: producer deleted; youngest ref 2026-07-15. Options: (a) leave; (b) the owner runs `node scripts/kaola-workflow-claim.js barrier-ref-sweep` (manual, keep-set based) themselves. The issue forbids this run from clearing external state; recommendation: (b) by the owner, at their convenience.
