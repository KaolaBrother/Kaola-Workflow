# Close the six open post-v9.2.0 audit defects: #911 #912 #913 #914 #916 #917

Worktree: `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-911-912-913-914-916-917`
Branch: `workflow/bundle-911-912-913-914-916-917`. Subagents get ABSOLUTE `$WT/` paths.

- item: #916 premise — reproduce a main-root roadmap regeneration FAILURE on the gated path (`claim.js:2389`, gated on `mainRoot && mainRoot !== linkedRoot && !keepRoadmapSource && !keepWorktree`), or show the branch unreachable. Reachable → record it; unreachable → DELETE the dead branch. Do not build on the premise before it is measured; found by reading, not by a run failing.
  status: done
  dispatched: investigator (read-only) — reproduce or refute the main-root regeneration failure on the gated path; output lands at `kaola-workflow/bundle-911-912-913-914-916-917/.cache/916-premise.md` in the MAIN root
  result: `.cache/916-premise.md` (22KB). VERDICT: **REACHABLE**, full defect reproduced end to end on real fs state, no patching or injected throws. Real throw sites in `regenerateRoadmap`: (C) main's `.roadmap/` absent + non-empty mirror → the source-loss guard throws; (D) a main `.roadmap/issue-*.md` at mode 000 → EACCES; (E) `.roadmap` dir mode 000 → EACCES on scandir; (F) `kaola-workflow/` mode 555 → EACCES on the tmp-file open. Controls that do NOT throw: unwritable `ROADMAP.md` with a writable dir, and a malformed issue source. Leg R: a concurrent unlink-vs-regenerate race across 2 OS processes threw 368/4000 (all ENOENT) — a fault the run produces itself, not exotic tree state. Legs C and D both: finalize **exit 0**, `roadmap_regenerated: "regenerated"`, `closure_invariants: ok`, stderr EMPTY (the `catch (_) {}` ate it), and main's mirror still lists the closed issue. The archived run folder mentions "roadmap" ZERO times — the issue's claim confirmed verbatim. TWO CORRECTIONS TO THE ISSUE: (1) its own suggested fault (unwritable `ROADMAP.md`) does NOT work — `writeFileAtomicReplace` writes a tmp and renames over it; nor does a malformed source. (2) The coverage claim is STRONGER than filed: walkthrough `:5405` also passes `--keep-worktree`, so the `:2389` gate is false and the block never executes there at all — across the whole test surface, ZERO tests execute it. Useful detail for the fix: `roadmap_removed_by_root` already reports `{"920":{"worktree":true,"main":true}}`, so per-root reporting is an EXISTING idiom in this receipt; only the mirror REBUILD lacks a field.

- item: #916 act on the premise — whatever lands must land in all four editions (canonical `claim.js:2390`, codex `:2390`, gitlab `kaola-gitlab-workflow-claim.js:2171`, gitea `:2170`), be RECORDED never gated (finalize still exits 0), and let a receipt reader tell WHICH mirror is stale. Coverage must red on current code against the reproduction, not an injected throw. Walkthrough `:5405` asserts the LINKED root's outcome and is structurally blind to main. DECIDED BY ME, now the premise is confirmed reachable: keep `roadmap_regenerated` (the linked root's scalar) UNCHANGED so the existing `:5877` warning and its consumers are untouched, and ADD a per-root field following the receipt's own existing `roadmap_removed_by_root` idiom, so a reader can tell WHICH mirror is stale. Record it durably in `finalization-summary.md` too — the investigator confirmed the archived folder currently says "roadmap" zero times. Recorded, never gated: finalize still exits 0.
  status: in-flight
  dispatched: tdd-guide — authored 4 walkthrough cases; VERIFIED RED BY ME via `--only`: `...UnreadableSourceIsRecorded916` FAIL, `...SourceLossIsRecorded916` FAIL, `...HealthyControl916` PASS, `...KeepWorktreeSkipsMainRoadmapRebuild916` PASS. Then implementer — `$WT/scripts/kaola-workflow-claim.js` + the 3 edition copies; output is the diff on the branch
  result: **DONE, VERIFIED BY ME — including the mutation A/B, which I ran myself after the implementer went idle without reporting it.** All four `--only` cases PASS; `cmp` claude<->codex `claim.js` IDENTICAL; 69 lines added per edition, all four carrying it; finding types 7/7/6/6. SHAPE, exactly as specified: `roadmap_regenerated` keeps its meaning (the invoking tree's outcome, so the `:5877` warning is not repurposed); `roadmap_regenerated_by_root: { worktree, main }` uses the SAME enum in the vocabulary `roadmap_removed_by_root` already established; `roadmap_regenerated_main_error` carries the caught message (capped 300 chars) and is attached ONLY on failure — a key holding `null` would still read as an error report to anything scanning the receipt, which is a nice catch; the finding `main_roadmap_mirror_not_regenerated` is raised; exit stays 0 and the run still archives. On a non-linked run `main` mirrors the scalar rather than inventing a second answer. REQUIREMENT 4 (durable, not receipt-only) genuinely met: the tests read the ARCHIVED run folder from whichever root it landed in and require a record naming the main root, the roadmap mirror and a failure TOGETHER — scoped to the artifact, so it cannot go green on unrelated text. MUTATION A/B, mine: I planted the original silent-swallow shape (failure re-typed as `'regenerated'`, error dropped) -> BOTH defect cases RED, healthy control still PASS -> restored, `cmp`-verified byte-identical, edition parity and type counts re-checked after. The recording is load-bearing, not decorative.

- item: #914 question — canonical/codex emit 6 `recordFinalizeFinding` types, gitlab/gitea 5; the delta is exactly `archive_unstage_failed`. Root is structural: canonical makes two archive-staging calls (`claim.js:4598` `git rm -r --cached`, `:4631` scoped `git add`), forges make one unscoped `git add -A 'kaola-workflow/'`. Answer whether the one-call shape has a failure mode the five types cannot name.
  status: done
  dispatched: investigator (read-only) — enumerate the forge one-call staging failure modes against the five existing types; output lands at `kaola-workflow/bundle-911-912-913-914-916-917/.cache/914-analysis.md` in the MAIN root
  result: `.cache/914-analysis.md` (26KB). ANSWER: **(c), with (b) inside it.** Narrow question — the forges do NOT need `archive_unstage_failed`: their own `archive_stage_failed` string already contains "or the removal of the live run folder from the branch", so the sixth type would name a fault the one-call shape cannot produce. Shape question — the unscoped `git add -A 'kaola-workflow/'` has uncovered modes that **no additional FAILURE type could ever catch, because they exit 0 and record `archive_stage: 'staged'`**: M3/M3b (a `.gitignore`d child is silently skipped — canonical's explicit pathspec exits 1, the forge exits 0 and records `roadmap_staged: true` while `.roadmap` never reached the index), M6 (live folder survives on disk: canonical's `git rm --cached` forces it out, the forge `-A` RE-ADDS it and the live folder stays on the branch at exit 0 — the exact outcome the sixth type announces, reached through SUCCESS), and **M4/M5, the genuinely new finding: the unscoped `-A` sweeps a FOREIGN project's live folder and a foreign archive band into `chore: archive <project>` past both staging guards at exit 0** — a scoping gap whose remedy is to scope the pathspec, not to add a type. M2 is the inverse: the forge fires a SPURIOUS `archive_stage_failed` (exit 128, pathspec matched nothing) where canonical is correctly silent. Counts verified 6/6/5/5, issue's headline table CORRECT; its "7/7/5/5 assignment sites" needs correcting — those are token-bearing lines, one of which is a schema default, not assignments. **AC-2 IS ALREADY SATISFIED AT HEAD**: `docs/api.md:359-365` already states five-vs-six accurately with the `archive_stage` semantics caveated; the "it said four-vs-five" half was discharged by the #904–#910 bundle. What IS wrong: `docs/api.md:330` documents `archive_unstaged`, measured 1× in canonical and **0×** in both forge ports. M8, a canonical-only message defect on this same surface: `archive_stage_failed` says "`git add` is all-or-nothing over its pathspec list, so NONE of these N path(s) was staged" — measured FALSE (exit 1 AND `ROADMAP.md` was staged); that is the invented-mechanism-in-a-message class.

- item: BLOCKER FOUND MID-RUN, not one of the six — `scripts/test-sink-merge.js`, `test-gitlab-sinks.js` and `test-gitea-sinks.js` are RED at the release commit `540f79a2` on any box whose global `init.defaultBranch` is unset. VERIFIED BY ME with a clean main root at 540f79a2: bare `node scripts/test-sink-merge.js` → **19 failed, 609 passed, exit 1**; `GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=init.defaultBranch GIT_CONFIG_VALUE_0=main` → **631 assertions, exit 0**. Single axis, no product regression. Cause: `test-git-fixture.js:154` and `mkFixture901` create the remote with `git init --bare` and NO `-b`, so the remote HEAD stays an unborn `master` while the fixtures push `main`; the clone checks out nothing and every fresh-clone witness reads `[]`. This is "the workflow does not own the consumer environment" pointed inward at our own tests, and it blocks honest verification of this run — a baseline that is red for unrelated reasons cannot tell my regressions from its own noise.
  note: OWNER RULED 2026-08-02 — fix the fixture in this run, flagged clearly as out-of-bundle. Test-fixture only, no product code.
  status: done
  dispatched: tdd-guide (test custody — the artifact IS a test fixture) — call sites in `$WT/scripts/test-sink-merge.js` and both forge sink suites; `test-git-fixture.js` deliberately untouched
  result: **DONE, VERIFIED BY ME ON BOTH LEGS.** Leg 1, no git config at all (`env -u`): all three suites PASS, `test-sink-merge.js` **649 assertions**. Leg 2, HOSTILE global `init.defaultBranch=trunk`: all three PASS, **649 assertions** again. Same count both legs and equal to the pre-fix workaround count, so nothing was made green by running fewer assertions — that was the regression I was actually worried about. The suites no longer read the operator's configuration. COMPLETENESS — I grepped rather than trusting the report, and three site classes still bare-init with no `-b`: `test-git-fixture.js:154` `initBare()` (×4 editions), `simulate-workflow-walkthrough.js:1644` `initGitRepoWithBareRemote`, and `simulate-kaola-workflow-walkthrough.js:786` (codex, and it is executed by the REAL chain `test:kaola-workflow:codex`). Site 2 is byte-for-byte the same shape as the bug — bare init, then `push -u origin main`. **But the diagnosis is different from what it looks like**: `simulate-workflow-walkthrough.js:1625` defines `GIT_ISOLATION_ENV = { GIT_CONFIG_GLOBAL: '/dev/null', GIT_CONFIG_NOSYSTEM: '1' }` and passes it to those calls, so the walkthrough ALREADY ignores the operator's config — which is exactly why the walkthrough was green while the sink suites were red on this same box. The sink suites simply lacked that isolation. So the real defect class is "a suite reads the operator's git configuration", the walkthrough had already solved it a different way, and a bare remote whose HEAD is an unborn `master` only BITES when something clones it expecting a checkout — which is what the `#901` fresh-clone witnesses do and what the walkthrough sites do not. Leaving sites 1–3 alone is therefore the correct call, not an oversight, and churning them would be change without an observed failure. The full four-chain verification settles it empirically, since site 3 sits inside a chain.

- item: file the four out-of-bundle findings as GitHub issues — OWNER RULED 2026-08-02, all four approved: (1) the forge roadmap `RULES_BLOCK` divergence from #917; (2) the gitlab/gitea `claim.js:2012` `persistExpansionRollupToSummary`/`parseExpansionRecords` that canonical deleted; (3) M8, canonical's `archive_stage_failed` message stating a measured-false mechanism; (4) `docs/api.md:330` documenting `archive_unstaged`, which is 0× in both forge ports. Evidence lives in `.cache/911-measurement.md` and `.cache/914-analysis.md`. Use `--body-file`, never `--body "$(heredoc)"` — backticks command-substitute.
  status: done
  dispatched: self — `gh issue create --body-file` (never `--body "$(heredoc)"`, backticks command-substitute)
  result: FIVE filed, not four — the owner also ruled M4/M5 filed-not-fixed, so it went with them. **#918** forge roadmap `RULES_BLOCK` divergence (4 bullets vs 5, only 2 overlapping; `git log -S` = zero commits so it is original divergence; `validate-script-sync.js:487` sees it only as a `module.exports` superset family and the case is NOT among that file's declared exclusions; the rendered `workflow-init` surfaces disagree with the forge generators' own output). **#919** gitlab/gitea `claim.js:2012` `persistExpansionRollupToSummary`/`parseExpansionRecords`, canonical grep-count 0. **#920** M8, `archive_stage_failed` asserting `git add` is all-or-nothing — measured false (exit 1 AND `ROADMAP.md` staged). **#921** `docs/api.md:330` `archive_unstaged`, 1×/0×/0×, with sibling `residue_unstaged` at 1/1/1 as the control showing the gap is specific. **#922** the foreign-project sweep — the forge unscoped `-A` pulls another project's live folder and archive band into `chore: archive <project>` at exit 0, past both staging guards; a scoping gap no failure type can reach because it SUCCEEDS. #922 is now wired into the `docs/api.md` paragraph that referenced it as untracked.

- item: #914 act — either the forge ports gain the sixth type or the reasoning for five is recorded; `docs/api.md` must state the TRUE counts (it said 4-vs-5, bundle corrected to 5-vs-6). Coverage BEHAVIOURAL per forge — hand-ported surface, parity checks cannot witness it. OWNER RULED 2026-08-02: the forges do NOT gain the sixth type; record the reasoning instead. AC-2 needs no work (counts already correct at HEAD). M4/M5 is FILED, not fixed here. `docs/api.md:330` is FILED as its own issue, not fixed here. So what lands is (a) the recorded result in `docs/api.md` and (b) behavioural per-forge coverage pinning the finding-type SETS so the hand-ported drift is caught.
  status: in-flight
  dispatched: (a) implementer, with the #911 close-out, dictated text -> `$WT/docs/api.md` + `$WT/CHANGELOG.md`. (b) tdd-guide -> new file `$WT/scripts/test-forge-finalize-findings.js`, wired into the chains.
  status: done
  result: **DONE, VERIFIED BY ME INCLUDING BOTH MUTATIONS.** (a) The recorded result is in `docs/api.md` under "**`archive_unstage_failed` is not owed to the forges**" — it states the forges lose nothing on a genuine failure, and that the one call is NOT otherwise equivalent, naming the three measured silent successes. `docs/api.md:330` and the counts paragraph were deliberately left for #921 and the drift repair respectively. (b) `scripts/test-forge-finalize-findings.js`, 65 assertions, exit 0, in a NEW file (the three sink suites had a concurrent writer). It pins the two claims holding the no-port decision up, kept explicitly separate: BEHAVIOURAL per edition — drive a real archive-staging fault on codex/gitlab/gitea and assert the finding names the live run folder, which is the claim that the forge message already says what the missing type would; and STATIC — the finding-type registries agree forge-to-forge and match `docs/api.md`'s stated counts. The static half is honestly labelled a registry-drift guard, not dressed as behavioural, because #914 rules a diff/byte check does not satisfy its AC-3 and only the behavioural half is offered against it. It is wired into ALL FIVE chain definitions in `package.json`, matching the existing `test-active-folders-field-parity.js` pattern, so it actually runs. MUTATION-PROVED BY ME, BOTH HALVES: planting a fake type in the gitlab port -> RED, "gitlab-only: [\"totally_fake_type_xyz\"], gitea-only: [\"residue_probe_failed\"]"; falsifying the docs counts to four/five -> RED, "docs/api.md says the forge ports raise four finding types; measured 6". Both product files byte-restored (`cmp`-verified) and the suite green again after. **This guard would have caught today's count drift**, which I had to find by hand.

- item: #912 — forge `sinkPreflight` (gitlab + gitea `kaola-*-workflow-sink-merge.js`) calls `assertWorktreeClean` unconditionally: no #711 branchless clause, no `[project]` argument, where canonical passes both. Make the three editions agree on when it asserts, or record each difference. `edition-sync.js:30-34` keeps these hand-ported, so BEHAVIOURAL per-forge coverage is the only witness. MEASURED BY ME: the `[project]` half is INERT — canonical `sink-merge.js:360` says `ownedProjects` was "added for API consistency… ignored". The live difference is the #711 branchless clause: on `branch === 'TBD'` canonical skips the call entirely while the forges call it, and `assertWorktreeClean` throws fail-closed if the `git worktree list` probe faults BEFORE any branch matching — so a forge refuses where canonical proceeds.
  status: in-flight
  dispatched: tdd-guide — 387 lines across both forge suites + 173 canonical; VERIFIED RED BY ME with the real `FORCE_WT_LIST_FAIL` hook (so the ports carry it): both forge suites red on `#912-<forge> (e)` branchless probe-fault, canonical PASSES at 649 assertions. Then implementer — `$WT/plugins/kaola-workflow-{gitlab,gitea}/scripts/kaola-*-workflow-sink-merge.js` only; output is the diff on the branch
  result: **DONE, VERIFIED BY ME.** Both forge suites exit 0, canonical `test-sink-merge.js` still 649 assertions, `scripts/kaola-workflow-sink-merge.js` untouched (empty diff). Change is +12/-3 per port, identical shape: `const branchless = branch === 'TBD'` as the first statement, the existing `assertWorktreeClean` try/catch wrapped in `if (!branchless)`, call left 2-arg (no `ownedProjects` ported — canonical ignores it). MUTATION-PROVED PER PORT, one site at a time with byte-restore between: gitlab-only mutant → gitlab suite RED, gitea suite green; gitea-only mutant → gitea suite RED, gitlab suite green. Neither port stayed green with its own clause removed and neither reacted to the other's mutant, so the coverage is non-vacuous AND genuinely per-port — an all-sites mutant would only have proven one. OUT-OF-SCOPE FINDING, reported not acted on: **#711 is otherwise ENTIRELY unported to both forges.** Canonical carries 12 `#711` references; each port now carries exactly one (the new comment). Missing on the forges: the `catKey`/`archiveKey` `branchless ? 'HEAD' : branch` fallbacks, `receipt.branch_mode = 'branchless'` (`grep branch_mode` returns nothing in either port), the branchless skips for `push_upstream`, `merge`, the publish-ancestry probe, worktree removal and feature-branch cleanup, and the `--branch TBD is only valid with --sink` typed refusal. So a forge `--branch TBD --sink` run now passes preflight but would then hit push/merge against a branch literally named `TBD`. NOTE — this fix does NOT introduce that: without a probe fault the old unconditional call already matched no worktree on 'TBD' and returned cleanly, so a branchless forge run already proceeded past preflight. Only the probe-fault case changed. #912's AC is satisfied as written. Making the forges branchless-capable end to end is a much larger port — raise with the user.

- item: #913 — `kaola-workflow-validation-runner.js` `buildScrubbedEnvironment` silently discards `--env-allowlist HOME` / `TMPDIR` (`deterministic.has(key) → continue`). Make an allowlisted deterministic key either take effect or be REPORTED; never silently discarded. `command_id` determinism must survive (that is why the scrub exists), the four copies stay byte-identical, and every place `--env-allowlist` is documented gets updated. DECIDED: the outcome is REPORT, not take-effect — letting HOME through would make `command_id` machine-dependent, which AC 2 forbids.
  status: in-flight
  dispatched: tdd-guide — 286 lines in `$WT/scripts/test-validation-runner.js`; VERIFIED RED BY ME: fails naming HOME, and the assertion deliberately EXCLUDES the request echo and the digest key column because both are byte-identical in a run that reports nothing. Then implementer — the 4 byte-identical `kaola-workflow-validation-runner.js` copies only, NO docs (impl-docs owns `docs/api.md`); output is the diff on the branch
  result: **DONE, VERIFIED BY ME.** `node scripts/test-validation-runner.js` → exit 0, "test-validation-runner: PASSED" (285 executed assertions). All four copies `cmp`-identical. The surface is a new always-present top-level receipt field `env_allowlist_ignored`, a sorted array naming exactly the requested keys the sandbox writes for itself: `--env-allowlist HOME,CARGO_HOME` -> `["HOME"]` with CARGO_HOME still passing through; no flag -> `[]`. The key still does NOT take effect, which is correct: letting the real HOME through is exactly what would move `command_id`. Determinism preserved STRUCTURALLY, verified by me at source rather than via the suite alone: `computeCommandId` hashes an explicit field list (`schema_version`, `policy`, `effective_environment`, `runner_node`, `execution_shell`, `executables`, `toolchains`) that does not include the new field, so it cannot move the digest. Nothing on stderr. MUTATION-PROVED TWO WAYS by the implementer: deleting the reporting line -> RED at `test:1428`; replacing it with a CONSTANT `["HOME","TMPDIR"]` banner -> RED at `test:1473` ("must VARY with the request") — that second mutant is the one ruling out a vacuous constant. I wrote the `--env-allowlist` documentation myself into `$WT/docs/api.md` (a new subsection ahead of `--keep-output`), because impl-docs owned that file while the implementer ran.

- item: #911 — `run-chains.js` relative `--plan` from a linked worktree resolves against the invoking tree and lands the receipt where the finalize gate does not read it — identical shape to #910, whose rule was "the hash follows the invoking tree, the receipt follows the run folder". Either land it where the following `finalize --check` reads, or document the resolution as deliberately caller-relative with the reasoning. Absolute `--plan`, the `--output` > `--plan` > `--project` > cwd precedence (`run-chains.js:748-762`), and plain non-worktree repos all stay unchanged. MEASURED BY ME: the real resolver is `resolveOutputPath` at `run-chains.js:836-845`; `:834` records the deliberate choice ("`--plan` is an EXPLICIT path the caller supplied and is left resolving against cwd unchanged"); `resolveProjectRecordDir:821` already implements the #910 rule with a load-bearing fallback. OPEN: no prompt or command surface passes `--plan` at all, and ADR 0017 retired `workflow-plan.md` as the run record — so whether the flag has a live producer decides fix-vs-document.
  status: done
  dispatched: investigator (read-only) — measure the mis-landing and whether `--plan`'s target artifact is still produced; output lands at `kaola-workflow/bundle-911-912-913-914-916-917/.cache/911-measurement.md` in the MAIN root
  result: `.cache/911-measurement.md` (21KB). **Q2: the mis-landing REPRODUCES** — 6-leg matrix, chains really run (mocked green). Only leg L1 diverges: relative `--plan` from a linked worktree lands the receipt in the worktree while the gate reads main → `chains_unverified`. Absolute `--plan` (L2), `--project` (L3), plain repo (L4/L5), main-root invocation (L6) all `chains_green`. Control A: the L1 receipt copied byte-for-byte into main's folder flips the SAME gate to `chains_green` — identical bytes, placement is the entire difference. Control B: the #910 split is intact and live (receipt in main's folder carries the worktree's HEAD). Second-order: the relative arm CREATES `<worktree>/kaola-workflow/<P>/` as a side effect, the exact hazard #910 settled for `--project`, and the outcome-telemetry dir at `run-chains.js:1063` has the same cwd-relative arm. **Q1: `--plan` has NO LIVE PRODUCER** — zero surfaces pass it in any of the four editions (the one rendered invocation slot `fz-runchains-run` passes `--project` on all three forges); every `workflow-plan.md` reference is classified and NONE produces one — `docs/workflow-state-contract.md:406-409` already states these "are read by nothing, and are never newly authored". **THIS REFRAMES THE ISSUE:** AC 1's second branch ("documented as deliberately caller-relative with the reasoning recorded") is **already shipped verbatim** at `docs/api.md:430-432`, which describes this exact behaviour, plus the reasoning at `run-chains.js:834`; `docs/api.md:417` already labels the flag "A legacy path-derivation alias; `--project` is the flag to use". COVERAGE: `test-run-chains.js` T23 (`:708-776`) is a PURE unit test with `cwd='/work/repo'`, a nonexistent non-git path, so `resolveRecordFolder` returns `dir:null` and the #910 fallback yields exactly what T23 asserts — T23 pins the plan-dir DERIVATION, not the TREE, and would NOT red if the relative arm moved. What it does pin is the absolute arm (AC 2) and precedence (AC 3), which must survive. No test in ANY edition drives `--plan` under a worktree topology. TOPOLOGY: `run-chains.js` is COMMON_SCRIPTS claude↔codex (sha256 verified identical) + GENERATED_AGGREGATORS for the forges; any change is edition-touching → all four chains. BONUS DEFECT, unrelated to #911, do not fix here: the gitlab/gitea `claim.js:2012` ports carry `persistExpansionRollupToSummary`/`parseExpansionRecords` reading `workflow-plan.md` that **canonical deleted** (grep count 0 in canonical) — live hand-port drift, inert only because no plan is ever authored. Same unpoliced family as #912/#914.

- item: #917 — "advances one item per cycle" is a dropped constraint stated as a rule, and ships to every consumer repo. Remove or restate the cadence claim across all 9 renderings from the 3 authoring surfaces only (`scripts/kaola-workflow-roadmap.js:41`, `plugins/kaola-workflow/scripts/kaola-workflow-roadmap.js:41`, `templates/routing/slots.js:104` slot `in-shared-004` with 6 variants), regenerate, and confirm `generate-routing-surfaces.js --check` passes and a regenerated `ROADMAP.md` carries the new wording. Verified: the 9 renderings are exactly as filed; the gitlab/gitea `roadmap.js` ports carry no rules block with this sentence — confirm what they DO emit so "one wording" is true across all three forges.
  status: done
  dispatched: implementer — edits the 3 authoring surfaces in `$WT` and regenerates; output is the diff on branch `workflow/bundle-911-912-913-914-916-917`
  result: Landed in `$WT`, VERIFIED BY ME independently (not from the agent's report). New wording: "`workflow-next` fetches GitHub issues and mirrors active implementation work here." — the cadence clause dropped, the sentence's real information kept, no replacement prescription. 11 files: the 3 authoring surfaces + the 6 generated renderings + `kaola-workflow/ROADMAP.md` + `CHANGELOG.md`. `generate-routing-surfaces.js --check` → "all 18 surfaces byte-match the skeleton", exit 0; `cmp` of the claude↔codex `roadmap.js` pair → IDENTICAL; all 9 renderings confirmed by grep, each keeping its own forge noun and command token; the only surviving "advances one item per cycle" string is the CHANGELOG provenance entry, which is correct. OUT-OF-SCOPE OBSERVATION, deliberately not fixed: the gitlab/gitea `roadmap.js` FORGE PORTS never carried this sentence — their `RULES_BLOCK` (`kaola-gitlab-workflow-roadmap.js:42`, gitea `:42`) is a different, shorter block with no research-session bullet and no cadence bullet at all. So #917 introduced no divergence, but the forge mirror's rules-block difference predates this and is worth its own issue. Raise with the user; do not file unilaterally. SHARPER EVIDENCE (agent-measured, spot-checked by me): github block = 5 bullets, ports = 4, only 2 overlap; `git log -S"advances one item per cycle"` over both port paths returns ZERO commits, so it is ORIGINAL divergence, not drift; `validate-script-sync.js:487` covers the roadmap ports only as a `module.exports` SUPERSET family, so RULES_BLOCK prose is invisible to it, and this case is NOT among that file's declared non-sync exclusions — undeclared, not declared-and-accepted. THE REAL CONSEQUENCE: `slots.js` `in-shared-004` DOES render gitlab/gitea rules bullets into their `workflow-init` surfaces, so on those two forges the surface prose and that forge's own `roadmap.js generate` output disagree about what the roadmap body says — a genuine one-rule-one-wording break that my change cannot close, because the forge generators were never wired to this wording. Fixing it changes what a consumer repo's mirror says on two forges: user-owned contract, escalate. Also verified by me: `validate-script-sync.js` exits 0 ("15 common scripts, 27 byte-identical groups… 4 Oracle Kernel copies identical at HEAD"), and the worktree `ROADMAP.md` diff is exactly 1 insertion / 1 deletion with the `### Project rules` section preserved.

- item: #911 close-out — OWNER RULED 2026-08-02: close as already-documented, no behaviour change. `docs/api.md:430-432` already states the caller-relative resolution and `run-chains.js:834` already records the reasoning, so AC 1's second branch is satisfied at HEAD; ACs 2, 3 and 4 are satisfied by not touching anything. What lands is ONE recorded fact so the next auditor does not re-file it: that the flag has no producer in any edition. Shares `docs/api.md` with the #914 act item — same agent, no concurrent writer.
  status: in-flight
  dispatched: implementer (with #914's docs edit, dictated text) — `$WT/docs/api.md` + `$WT/CHANGELOG.md`; output is the diff on branch `workflow/bundle-911-912-913-914-916-917`
  status: done
  result: **DONE, VERIFIED BY ME.** `docs/api.md`'s `--plan` row now carries the one fact nowhere on record: the flag has NO producer — no prompt, command, skill or routing surface in any of the four editions passes it (the rendered invocation slot passes `--project`), and nothing outside a test fixture authors a `workflow-plan.md` for its argument to name. Falsifiable by grep, which is the point. No script, no test and no behaviour changed, so ACs 2/3/4 hold by construction and AC 1's documentation branch was already satisfied at HEAD. CHANGELOG entry says plainly it is documented, not changed. Confirmed by me that the agent did NOT touch `docs/api.md:330` or the counts at `:359-365` — both were reserved, the first for #921 and the second for the drift repair.

- item: COUNT-DRIFT REPAIR, discovered by me mid-run — #916's fix ADDS a `recordFinalizeFinding` type, `main_roadmap_mirror_not_regenerated` (canonical `:4555`), which is the correct idiom for "recorded, never gated" but MOVES the finding-type counts that #914's docs work just froze. Measured mid-flight: canonical now has 7 call sites, gitlab still 5 (propagation pending). Once #916 lands in all four, the true counts are 7/7/6/6, and BOTH of these become false: `docs/api.md:359-365` ("**five** ... **six**") and the `[Unreleased]` CHANGELOG entry for #914 ("raise five finalize finding types where canonical and Codex raise six"). This is the two-concurrent-agents version of "the fix's own summary is the next instance" — each was accurate when written and a sibling change invalidated it. MUST be repaired after #916 lands and BEFORE any commit; re-count all four editions from the actual call sites, do not trust these numbers.
  status: done
  dispatched: self — `$WT/docs/api.md` + `$WT/CHANGELOG.md`
  result: RE-COUNTED from the actual call sites after #916 propagated to all four: **7 / 7 / 6 / 6**, delta still exactly `archive_unstage_failed`. Repaired FIVE things, four of which nothing would have caught: (1) `docs/api.md` "**five** ... **six**" -> "**six** ... **seven**"; (2) the CHANGELOG #914 entry, same counts; (3) the heading "**The sixth type is not owed**" -> "**`archive_unstage_failed` is not owed to the forges**" — the ordinal was itself count-coupled and would have rotted again on the next added type, so it now names the type instead of counting it; (4) `docs/api.md:340`'s `findings` list enumerated the six type names and was MISSING `main_roadmap_mirror_not_regenerated` — a user-visible surface #916 added and nothing else documents; (5) the finalize envelope had no `roadmap_regenerated_by_root`, so #916's whole point (telling a reader WHICH mirror is stale) was undocumented — added to the JSON shape with prose covering the enum, the `skipped` case, `roadmap_regenerated_main_error`, and that the exit stays 0. Also added the missing #912, #913, #916 and fixture CHANGELOG entries. LESSON, worth keeping: two agents each wrote something true and a sibling change falsified it inside the same diff — no chain recounts finding types against prose, which is the very gap #914 was filed about.

- item: verification — walkthrough at FULL scope (`node scripts/simulate-workflow-walkthrough.js` exits 0) plus `npm test` four chains from the worktree; the fast gate samples a 1/12 walkthrough shard and proves only the slice that came up. Mutation-prove each new guard is armed; a green suite is not proof.
  status: done
  dispatched: self — a script running each chain SEPARATELY and echoing its OWN exit code, with NO git config exported
  result: **FULL WALKTHROUGH (unsharded) exit 0**, and all four chains exit 0 — but only after a real red. Run with no `GIT_CONFIG_*` on purpose: verifying the environment-independence claim while exporting the workaround would have proved nothing, and it also settled empirically that the codex walkthrough's un-branched bare remote does not bite (codex chain green). FIRST PASS: `claude_exit=1` while codex/gitlab/gitea were 0. **The background task reported "exit code 0" overall** because the script's last statement was an `echo` — the exact compound-green trap, avoided only because each chain echoed its own code. CAUSE, a real guard doing its job: `test-spawn-classification` found 4 unclassified spawn sites in `scripts/test-sink-merge.js` against a ceiling of 3, and says outright that raising the ceiling is not a fix. tdd912 HAD classified its new site, but wrote the marker trailing prose on the same comment line (`// returning it. spawn-class: cli-contract`) where the guard requires a comment whose WHOLE payload is the marker. FIX BY ME: split it onto its own line. Re-ran: `spawn-classification passed (10 mutation assertions; 616 spawn sites across 61 files, 184 classified, 432 grandfathered)`, `test-sink-merge.js` still 649 assertions with no config, and `claude_exit=0`. Worth noting the guard mutation-tests ITSELF — 10 mutation assertions — which is the standard the rest of this run tried to meet.

- item: finalize — `/kaola-workflow-finalize bundle-911-912-913-914-916-917`; all-or-nothing closure of all six issues, one roadmap regeneration, one archive.
  status: in-flight
  dispatched: self — branch `workflow/bundle-911-912-913-914-916-917` committed at `36f41ab9` (29 files, +2096/-68); next is the run-chains receipt bound to that commit, then finalize, push, and the sink from the MAIN root
  result: **COMPLETE.** All six issues verified CLOSED on the forge (state read back, not inferred from exit 0). `main` = `origin/main` at `8c6fda7e`, tree clean, folder archived, no active folders, sink journals disposed, worktree and branch removed. Closure audit: `current_project_clean: true`, all seven scoped classes zero; the drift it reports outside scope belongs to older archives (issue-455, issue-891) and is correctly partitioned so it cannot contaminate or hide behind this verdict. Receipt bound `41b4a10d` with four chains exit 0, scope `all-four`/`edition_coupling`. TWO THINGS CAUGHT ONLY BY VERIFYING AFTER THE PUSH: (1) **my own commit message closed #918.** The line `Filed, not fixed: #918 ...` contains the substring `fixed: #918`, which GitHub's keyword matcher acts on — the negation means nothing to it, and only the first reference after the keyword was hit. Reopened with an explanation; the later commits deliberately write bare numbers with no `#`. (2) **`ROADMAP.md` was stale and still carried the OLD cadence sentence**, i.e. #917's own AC was unmet on main: the mirror is regenerated during finalize, which runs BEFORE the sink merges, so it was built by the pre-merge generator while the merged generator no longer emits that sentence. Regenerated with the script and pushed as `8c6fda7e`; `validate` and `validate-remote` both `ok`.
