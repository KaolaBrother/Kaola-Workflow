# #1078 — Droid CLI becomes the ninth runtime as an additive native_only skills edition with no hooks and no sub-agent profiles

- item: Register the Droid capability adapter and its evidence. `templates/agents/runtime-capabilities.json` gains `droid` evidence entries (official Factory docs for AGENTS.md/skills/commands/droids/hooks plus a live 0.220.0 probe) and a `runtimes.droid` adapter in the zcode/opencode native_only shape: `named_roles`/`deterministic_profiles` false, `instruction_loading` direct, `role_dispatch` native_only, `hook_scope` user_and_project (measured; nothing installed), `delegation_guidance` naming the built-in `worker`/`explorer` Task routes, `compact_protocol.events` empty (no hook installed), `install_scope` with measured global discovery. Append `droid` to `RUNTIMES` in `scripts/generate-agent-profiles.js` and regenerate `agents/generated-agent-manifest.json`.
  status: done
  dispatched: self; edits land in the worktree at .kw/worktrees/issue-1078 on branch workflow/issue-1078
  result: `templates/agents/runtime-capabilities.json` gained six droid evidence entries (droid_agents_md, droid_skills, droid_commands, droid_subagents, droid_hooks from docs.factory.ai/harness pages observed 2026-09-16, plus droid_live_probe_20260916 citing this issue) and the `runtimes.droid` adapter in the zcode/devin native_only shape with `compact_protocol.events: []` and a measured install_scope. `scripts/generate-agent-profiles.js:38` gained `droid`; `node scripts/generate-agent-profiles.js --write` regenerated `agents/generated-agent-manifest.json` (native_only_runtimes now ends opencode/kimi/zcode/devin/droid) and `--check` exited 0.

- item: Make the claim surface accept droid everywhere it enumerates runtimes. `scripts/kaola-workflow-claim.js` usage enum gains `|droid` and `node scripts/edition-sync.js --materialize-kernel` propagates it to the three plugin claim copies. Update the B0-claim assertion in `scripts/test-zcode-edition.js` (enum now ends `|droid`) and confirm `scripts/test-devin-edition.js` still passes.
  status: done
  dispatched: self
  result: `|droid` appended to the claim USAGE runtime enum in canonical `scripts/kaola-workflow-claim.js` and both hand ports (`plugins/kaola-workflow-gitlab` and `plugins/kaola-workflow-gitea`); `node scripts/edition-sync.js --write` byte-synced the codex mirror. `test-zcode-edition.js` B0-claim now pins the `|devin|droid` tail on every claim copy; `test-devin-edition.js` pins the same eight-runtime enum. edition-sync --check reports 6 forge ports in parity and both focused suites pass (zcode 463 assertions).

- item: Add the `droid-local` machine-global contract target. `templates/global/runtime-contract-adapters.json` gains a target in the zcode-local shape: discovery `command` on `droid`, carrier `managed_region` markdown at `~/.factory/AGENTS.md`, precedence/reload/compatibility_reads measured from Factory docs (reload after compaction stays honestly unknown). Update the enumerating registry suites (`scripts/test-issue-1046-global-contract.js`, `scripts/test-issue-1051-global-contract.js`) for the new target.
  status: done
  dispatched: self
  result: `templates/global/runtime-contract-adapters.json` gained `droid-local` (discovery command `droid`, carrier `managed_region` markdown at `DROID_HOME`/`.factory`/`AGENTS.md`, carrier_group `droid-global-agents`, honest reload noting post-compaction reload unmeasured). `test-issue-1046-global-contract.js` extended to the eleventh surface / ninth runtime family: EXPECTED_SURFACES, fake `droid` executable, DROID_HOME env, reload pattern `/new session/`, droid owner-file in the managed-insertion survival check, and 10-installed/10-NOT_INSTALLED counts. 1046 passes 162 assertions; 1051 passes 50 (its enumerations are compact-capable runtimes, unchanged at four).

- item: Author `scripts/sync-droid-edition.js` rendering the Droid skills trees from the shared routing registry, modeled on `sync-devin-edition.js`: treeLabel `.factory<suffix>`, skills at `.factory/skills/<name>/SKILL.md` with `name`/`description` frontmatter (user-invocable and model-invocable by Droid defaults), `kaola_script()` rewritten to resolve `$DROID_HOME:-~/.factory` then self-dev `./scripts`, `--runtime droid` stamped, native dispatch prose replacing `Agent(...)` cards, `--write|--refresh-present|--check|--tree-root=` CLI. Wire it into `refreshPresentEditionTrees()` in `scripts/generate-routing-surfaces.js` and the `runtime-edition-forge.js` header roster.
  status: done
  dispatched: self
  result: `scripts/sync-droid-edition.js` authored modeled on `sync-devin-edition.js`: `treeLabel('.factory'+forge outSuffix)`, skills at `.factory<suffix>/skills/<name>/SKILL.md`, `renderSkill` with description verbatim + invoke sentence via `yamlScalar`, triggers user+model, `transformCommandBody` defers the dispatch block (pointer once via `agentGen.deferRuntimeDispatchBlock`), rewrites the `kaola_script()` resolver to probe `$DROID_HOME:-$HOME/.factory` then the forge self-dev dir, stamps `--runtime droid`, and swaps Agent(...) cards for native `Task` worker/explorer route prose. Wired into `refreshPresentEditionTrees()` in generate-routing-surfaces.js and both roster comments in runtime-edition-forge.js. Smoke-tested: three-forge `--write` into a temp root, `--check` green, pointer count 1 in workflow-next, no CLAUDE/token leaks (the only "CLAUDE" hit is the canonical CLAUDE.md reference in workflow-init, which is legitimate prose).

- item: Author `install-droid.sh` modeled on `install-devin.sh` minus every hook/config mutation: `--global` (skills into `~/.factory/skills/`, support scripts into `~/.factory/kaola-workflow/scripts`, managed `~/.factory/AGENTS.md` via the shared global-contract CLI), `--project[=DIR]` (skills into `<DIR>/.factory/skills/`), `--forge=github|gitlab|gitea`, `--check`, `--uninstall` (ownership-safe: only Kaola-rendered skill dirs and support scripts), no hooks.json, no settings.json, no droids, no MCP. Add `.factory/` and `.factory-*/` to `.gitignore`; wire droid into `install-all.sh` (RUNTIMES, usage, scopes, DROID_CMD, run_one) and `scripts/test-install-all.js`; update `package.json` (description nine runtimes, files, keywords, `bash -n install-droid.sh`, edition chain registration).
  status: done
  dispatched: self
  result: `install-droid.sh` authored minus every hook/config mutation (no hooks.json/settings.json/droids/MCP), with `--global`/`--project[=DIR]`/`--forge`/`--check`/`--uninstall`. `~/.factory` via DROID_HOME; `droid-local` target in the global-contract wrapper; `--uninstall` removes only rendered skill dirs by source-tree name plus manifest-listed support scripts and never the shared carrier (zero-residue rmdir chain). `.gitignore` gained `.factory/` and `.factory-*/`; `install-all.sh` gained droid in RUNTIMES/usage/scopes/DROID_CMD/run_one (incl. "other eight" prose); `test-install-all.js` gained the droid row in KNOWN_INSTALLERS and the stub map; `package.json` updated (nine runtimes descriptions, `droid` keyword, files list, `bash -n install-droid.sh` in both chains). Hermetic run: install → 3 skills + support scripts + AGENTS.md, `--check` passes, `--uninstall` removes edition artifacts and preserves AGENTS.md. `test-install-all.js` passes 285 assertions. (Editions-chain registration for test-droid-edition deferred to M6 so every commit stays green.)

- item: Author the Droid edition suites and bring every enumerating surface green: `scripts/test-droid-edition.js` (adapter assertions, renderSkill contract — description verbatim plus invoke sentence, no Claude tokens, `--runtime droid` stamped, always-loaded carrier pointer once; hermetic install with a temp `DROID_HOME`; sync `--tree-root` roundtrip; registry target; install-all includes droid; claim usage includes droid) plus a no-hook/no-harness-additions protocol suite. Register both in `test:kaola-workflow:editions`. Update the enumerating suites: `scripts/test-route-reachability.js` (27 next surfaces), `scripts/test-runtime-agent-architecture.js` (runtime roster, native_only expectations, zero-profile counts), `scripts/test-issue-1044-runtime-adapters.js` and `scripts/test-issue-1044-prompt-bundle.js` (droid render/compact expectations), `scripts/test-issue-1055-render-subtraction-oracle.js` (COMMAND_RUNTIMES + regenerated baseline fixture), `scripts/simulate-workflow-walkthrough.js` (droid sync legs), and the vendor-name ban in `scripts/validate-workflow-contracts.js` if required.
  status: done
  dispatched: self
  result: `scripts/test-droid-edition.js` authored and green: six-evidence adapter pins (droid_live_probe_20260916 runtime_probe → #1078; five official_docs → docs.factory.ai), native_only capabilities with `compact_protocol.events []`, renderSkill contract (pointer once, user+model triggers, `--runtime droid`, resolver probes `$DROID_HOME:-$HOME/.factory`), hermetic install→check→uninstall against a temp DROID_HOME (skills + support scripts + AGENTS.md, zero harness files), sync `--tree-root` roundtrip, registry target, install-all + claim usage. Plus the no-hook/no-harness-additions protocol section: installer has zero hook/config/droid/MCP write machinery (manage_config_hook/UserPromptSubmit/ups_command/HOOK_MARKER/writeFileSync absent), generator output set is skills-only, and the hermetic install proves no config.json/hooks.json/settings.json/droids/commands/mcp in `~/.factory`. Registered in `test:kaola-workflow:editions`. droid joined `RECOVERY_FULL_DISPATCH_RUNTIMES` + the renderCompactRecoveryPrompt allowlist (its `~/.factory/AGENTS.md` is the always-loaded full-dispatch carrier), which made the earlier reds resolve: route-reachability 148 assertions (9 modules / 27 obligated trees), runtime-agent-architecture 722 (nine families, native_only expectations, zero-profile counts), prompt-bundle C2 scoped to rendered full-dispatch intersection. Walkthrough axiom sweep covers the droid init surface. Edition lane otherwise green (zcode 463, cursor 611, 1045 24, devin, droid); kimi S2.6 and validate-workflow-contracts reds are PRE-EXISTING tag fixtures (remote has v9.17.2 and v12.0.1; fetch --tags at validation time), confirmed identical on main root.

- item: Dock the documentation. New `docs/droid-edition.md` (zcode-edition section shape: runtime not forge, native_only, skills carrier, why no hooks). Update `docs/README.md` (edition bullet, ADR 0025 native_only list), `docs/installation.md`, `docs/runtime-capabilities.md` (capability, install, compact-recovery, subagent-binding tables and the nine-runtime counts), `docs/api.md` (sync-droid CLI, installer, usage enum), `docs/architecture.md`, `docs/prompt-size.md` (Droid token row plus raw counts), `docs/agents-source.md` (native_only adapter list), `README.md` (runtime table nine rows, token table, Runner paragraph, install entries), and `CHANGELOG.md` under `[Unreleased]` citing only #1078.
  status: done
  dispatched: self
  result: `23f6b22` adds the Droid edition guide and synchronizes the README, documentation index, installation/API/architecture/capability/source pages, ADR 0025, prompt-size measurements, related edition notes, and the Unreleased changelog entry for #1078. Droid is documented as the ninth native_only runtime with no Kaola profiles or harness additions; measured prompt counts are Next 1286, Finalize 1570, global 928 words (estimated 1930/2360/1390/5680 tokens). `node scripts/test-droid-edition.js`, `node scripts/generate-agent-profiles.js --check`, `node scripts/generate-routing-surfaces.js --check`, and `node scripts/test-route-reachability.js` (148 assertions) pass; a temporary Droid render tree also passes write/check parity. Historical eight-runtime wording remains only in prior ADR/changelog records.

- item: Live-verify the edition on this machine: run `install-droid.sh --global --yes --forge=github` against the real `droid` 0.220.0 install, then prove Droid discovers the three skills and the managed `~/.factory/AGENTS.md` carrier (skill listing and a read-only probe), and `--check` passes. Record first-party evidence for the capability matrix. Uninstall is not needed afterwards — the carriers are the deliverable.
  status: done
  dispatched: self
  result: `droid --version` returned 0.220.0. After fixing the newly added `install-droid.sh` mode from 0644 to executable 0755, `./install-droid.sh --global --yes --forge=github` installed three skills, 15 support scripts, and `/home/factory-user/.factory/AGENTS.md`; `./install-droid.sh --check --forge=github` passed. A bounded `droid exec --cwd <worktree> --auto low` read-only probe reported `kaola-workflow-finalize`, `workflow-init`, `workflow-next`, and the loaded machine-global AGENTS.md carrier. Commit `41ba1c9` records the live evidence and executable mode.
