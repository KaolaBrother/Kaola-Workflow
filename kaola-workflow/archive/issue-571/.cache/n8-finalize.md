evidence-binding: n8-finalize 3b3bb4c6652d

# n8-finalize (sink) — main-session-direct

Deliverable: CHANGELOG.md `[Unreleased]` entry for #571 (Codex agent profiles global by default; `--global` flag; preflight either-scope acceptance; init scaffolding-only; cross-edition #307 four chains green; ADR D-571-01).

Finalization performed main-session-direct per the plan-run contract (the finalize sink is non-delegable). Mechanical bookkeeping (archive + status close + roadmap regen + git-add staging + commit gate) delegated to the contractor; the four-chain receipt is regenerated by the orchestrator against the final tree (CHANGELOG is a chain-asserted doc, so the receipt is refreshed after this write).

Write set: CHANGELOG.md (only).
