# node3 — doc-updater evidence (issue #352)

Dictated CHANGELOG edit applied verbatim: new "### Fixed" subsection created under [Unreleased] (after the ### Added bullets) with the single #352 bullet (init: hardcoded maintainer machine path removed from workflow-init ×3 editions; relative sibling first preference + KARPATHY_SKILLS_PATH/ask-user fallback; byte-identical block ×3; SKILL partners + KW-CLAUDE-TEMPLATE untouched; four chains green).

Verification: this node's diff contribution = CHANGELOG.md | 4 ++++ only (the other three changed files in the worktree are node1's pre-existing uncommitted work). grep: line 12 contains "(#352)" inside [Unreleased]/### Fixed.
