# Finalization - Summary: issue-539

## Delivered
Two file-disjoint parts, both verified:
1. **finalize `--base` forwarding** — `cmdFinalize` (`scripts/kaola-workflow-claim.js` ×4 byte-identical) now forwards `--base <ref>` (and/or `KAOLA_FINALIZE_BASE` env, default unset → `main`) to the plan-validator's whole-plan `--finalize-check`, unblocking per-issue finalization on a shared/multi-issue branch. The per-node `--barrier-check` anti-laundering guard is unchanged.
2. **opencode edition path-flip (mechanism B)** — adaptive is the unconditional default on the opencode runtime; the Path Intent section + the adapt "downgrade to full path" wording are stripped at generation time by an opencode-only transform in `scripts/sync-opencode-edition.js`. Canonical `commands/*.md` untouched (runs file-disjoint from the concurrently-in-flight #538 on `main`). `install-opencode.sh` path-selection opt-ins scoped out (standalone installer; full parity rides a later issue).

## Files Changed
- `scripts/kaola-workflow-claim.js` (+ the 3 byte-identical plugin copies: `plugins/kaola-workflow/scripts/kaola-workflow-claim.js`, `plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js`, `plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js`)
- `scripts/simulate-workflow-walkthrough.js` (new `testFinalizeBaseFlagScopesAttributionSweep`, both `--base` directions pinned)
- `scripts/sync-opencode-edition.js` (opencode-only strip-transform in `transformCommandBody`)
- `.opencode/command/workflow-next.md`, `.opencode/command/kaola-workflow-adapt.md` (regenerated via `--write`)
- `install-opencode.sh` (scoped-out rationale), `scripts/test-opencode-edition.js` (A22 assertions), `docs/opencode-edition.md`
- `CHANGELOG.md`, `README.md` (doc-updater n4)

## Test Coverage
Node-scripts repo (no formal coverage pipeline). Correctness gated by: the new `testFinalizeBaseFlagScopesAttributionSweep` (both `--base` directions + env path), A22 opencode path-flip assertions (5/5), and the machine-verifiable four-chain receipt.

## Final Validation Evidence
- **#307 four-chain receipt** (`.cache/chain-receipt.json`, headSha `de272214` = HEAD, all `exitCode: 0`): claude ✓ codex ✓ gitlab ✓ gitea ✓. Generated by `kaola-workflow-run-chains.js` (concurrent, 18 cores).
- opencode additive suite: `sync-opencode-edition.js --check` (15 agents + 12 commands parity), `test-opencode-edition.js` (283 assertions incl. A22), `simulate-workflow-walkthrough.js` — all green.
- n3 opus code-review: verdict `pass`, `findings_blocking: 0` (anti-laundering guard intact; default-unset byte-equivalent; claim.js ×4 byte-identical; canonical 100% untouched; tests bite).
- Adaptive finalize barrier gates: resume-check ✓ gate-verify ✓ verdict-check ✓ barrier-check ✓ (the last with `--base HEAD` — see R2).

## Documentation Docking
DOCKED — `CHANGELOG.md` `[Unreleased]` + `README.md` (opencode bullet, `KAOLA_FINALIZE_BASE` env row, finalize `--base` synopsis) updated by n4 doc-updater; conventions preserved. Evidence: `.cache/n4-doc-update.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| _(none — all chains green first run)_ | — | — | — | — |

## Follow-Up Items
- **R1 → #540** (reviewer-deferred, n3): stale inline "(Step 0a-1)"/"switch-OFF" references in generated `.opencode/command/workflow-next.md` (Bundle Lane / Goal-Driven Autonomy / output template). Coherent dead prose; purge via the opencode transform or self-resolves when #538 flips canonical.
- **R2 → #541** (in-run discovery): the finalize command prose (`commands/kaola-workflow-finalize.md` ~L36) runs the whole-plan `--barrier-check` without `--base`; on a shared branch it refuses `foreign_archive`. The whole-plan barrier accepts `--base` (plan-validator.js:2161); the prose should forward it (canonical edit → coordinate with #538). For #539 the orchestrator passed `--base HEAD` manually.

## Run gaps
- in_run_repair (n1-finalize-base): noise: intentional TDD RED→GREEN cycle (test-first methodology — the RED is the new `--base` assertion before the fix landed); not a product defect.
- reviewer_deferred (n3 R1, stale inline refs): filed: #540
- manual:finalize-barrier-base-gap (R2): filed: #541

## Closure Decision
User-approved: commit the #539 work on `feature/opencode-support`, **defer the merge/PR** to a later final PR (after #538 lands — merging now would collide with #538's in-flight canonical edits), and close #539 (work complete).

## Commit And Push
Committed on `feature/opencode-support` (in-place, `run_posture: in-place`). **No push / no merge / no PR** — the main-merge is deferred to the user's later final PR per the Closure Decision. (The workflow's Step 9 sink was intentionally skipped.)

## GitHub Issue
#539 — to be closed (work complete; merge deferred to the later final PR).

## Roadmap
Updated at archive (`kaola-workflow/.roadmap/issue-539.md` removed, `ROADMAP.md` regenerated by `cmdFinalize`).

## Archive
`kaola-workflow/archive/issue-539/` (via `cmdFinalize --base HEAD --keep-worktree`).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked | .cache/n4-doc-update.md | |
| documentation docking | invoked | DOCKED (this summary § Documentation Docking) | |
| final-validation fix executors | N/A | — | no failing chains (all green first run) |
| roadmap refresh | invoked | kaola-workflow/ROADMAP.md (regenerated at archive) | |
| archive completed folder | invoked | kaola-workflow/archive/issue-539/ | |
| final commit and push | commit only | git (feature/opencode-support) | push/merge deferred to later final PR (user) |
| finalize (n5) | main-session-direct | this summary + .cache/n5-finalize.md | non-delegable sink node |

## Status
READY — commit on branch; sink deferred (user-directed later final PR).
