# Fix #929 — `projectNameForIssue` adopts a roadmap placeholder ("unclaimed") as the project name

Pre-claim measurement, already done, carried here so nobody re-derives it:
`projectNameForIssue` (`scripts/kaola-workflow-claim.js:293-300`) is verbatim as filed and guards
only the em-dash `'—'`. BUT no roadmap tool in this repo ever emits the literal `unclaimed`:
`cmdInitIssue:344`, `readRoadmapIssues:78`, `buildTableRow:89` all default to `'—'`, and
`cmdProjectName:369` rejects `'—'`. A repo-wide grep across all four plugin trees finds the literal
in no `.js` emission site. So the issue's evidence (a real KaolaVPN run whose folder/worktree/archive
were all named `unclaimed`) is sound, but its root-cause attribution to the generator is FALSE — the
sentinel was hand-authored into that repo's roadmap source. The filed one-line fix therefore guards a
magic string this codebase never produces, and would not catch `TBD`, `none`, or `n/a`.

- item: Settle what the observation actually demands — the adoption path is real but the generator is not the source, so decide whether the fix belongs at the claim's read (reject placeholder-class values), at the roadmap's write, or is a wording/contract fix; derive additively and name the observed failure each candidate answers, then bring the recommendation back for the shape call
  status: done
  dispatched: investigator (read-only, no edits), FIRST dispatch was interrupted before it ran and produced nothing; re-dispatched carrying the corrected facts from the surface map → report at kaola-workflow/issue-929/.cache/fix-shape.md
  result: kaola-workflow/issue-929/.cache/fix-shape.md (19 numbered measurements). DEFECT IS REAL — reproduced end-to-end at exit 0, and re-reproduced independently by the orchestrator driving the exported projectNameForIssue: unclaimed→unclaimed, TBD→TBD, —→issue-N, absent→issue-N. THE FILED DIAGNOSIS IS FALSE: `unclaimed` is KaolaVPN's own hand-authored vocabulary, 0 occurrences in 1395 committed values of the field across this repo's whole history, so the filed one-line fix would hardcode a foreign repo's private vocabulary into four hand-ported copies. CORRECTION TO THE SURFACE MAP AND TO WHAT THE ORCHESTRATOR FORWARDED: `TBD` never realized a failure — at ced64384 the derivation shelled out to a `project-name` subcommand that did not exist in that commit's roadmap.js (0 hits, verified), so the empty catch kept issue-N; realized failures in this repo = ZERO, the value class reached the field but the failure did not. DECISIVE STRUCTURAL RESULT: no lexical predicate can separate the classes — `unclaimed` and `pr-sink` are the same lexical object, so a kebab allow-list accepts `unclaimed`, a must-contain-hyphen rule rejects legitimate `sink` while still accepting `not-yet` (10/10 plausible placeholders adopted, 6/6 plausible names rejected), and a blocklist is unbounded by construction. Write-side fixes do not reach either observation (both hand-authored). WHAT IS ACTUALLY MISSING: `workflow_project` appears in 0 prompt surfaces, 0 lines of docs/workflow-state-contract.md, and its only spec hangs off `project-name`, a subcommand with 0 callers — verified. Recommendation: contract + vocabulary only, no runtime change. Escalated to the user as a value call, since a docs-only outcome for a `bug`-labelled issue is materially different from what the filer expected.

- item: Map every surface that carries projectNameForIssue or the workflow_project read, across root scripts, the three forge plugin ports and the codex byte-pair, since any fix must propagate identically and claim.js is a byte-paired/ported file
  status: done
  dispatched: Explore (read-only) → report at kaola-workflow/issue-929/.cache/surface-map.md
  result: kaola-workflow/issue-929/.cache/surface-map.md — the agent had no write tooling, so the orchestrator wrote the report from its returned text and re-verified the four load-bearing rows directly. Four claim.js copies: root↔codex byte-enforced (misses go red), gitea/gitlab HAND-PORTED with only an export-key-set check, so a body divergence there is SILENT. Three of the orchestrator's own pre-claim measurements were REFUTED: (1) isSafeName is in active-folders.js:14, not adaptive-schema.js, and is a path-traversal guard only — unclaimed/TBD/none all pass it; (2) the gitea/gitlab roadmap ports' issueRecordContent:222 defaults workflow_project to issue-N, not '—', so the guard is near-inert on those two editions; (3) a placeholder DID reach this repo's own .roadmap — issue-23.md carried `workflow_project: TBD` from ced64384 to 90ef58a1, so the failure class is observed HERE, not only in KaolaVPN. Also: the issue's cited fallback at 5196/5227 is a misattribution (that is collectStale, a separate derivation). Live path is claimExplicitTarget:1477, not claimProject:1115. Only ONE assertion in the whole repo proves the '—'→issue-N fallback (walkthrough:180), and plantRoadmapIssue plants '—' 40+ times, so widening must keep '—' working.

OWNER RULING (asked and answered in conversation): ship **contract + vocabulary only**. No runtime
change in any of the four claim.js copies. Consequence for the items below: there is no production
code to implement and no new behaviour to pin, so the test-custody item is struck rather than
deferred — a docs-and-skeleton change authors no test artifact, and inventing one would pin machinery
nobody built. Verification is the existing `generate-routing-surfaces.js --check` plus the walkthrough.

- item: Author the three edits in the worktree — a workflow_project entry in docs/workflow-state-contract.md (the field is absent from it entirely), an amended docs/api.md:1350 that specifies the FIELD and not only the callerless project-name subcommand, and one sentence in templates/routing/init.skeleton.md at the .roadmap bootstrap naming `—` as the sole unassigned token and warning that any other value becomes a real directory name; then regenerate the rendered surfaces, never editing a rendered surface by hand
  status: done
  dispatched: self, authored in the worktree .kw/worktrees/issue-929 on branch workflow/issue-929
  result: Three edits landed. docs/workflow-state-contract.md — new `### Roadmap issue-source fields` subsection at the end of `## Durable Sources`, stating that workflow_project names the project directory verbatim, that `—`/absent/empty are the unassigned token yielding issue-N, that any other value becomes the active folder + worktree + archive + sink-receipt name while the branch stays workflow/issue-N, that nothing validates it beyond path safety, and that a shared value is how a bundle directs several issues into one folder (so a claim cannot require the directory to pre-exist). docs/api.md:1350 — the project-name row now specifies the FIELD, not only the subcommand's exit rule, and cross-references the contract section. templates/routing/init.skeleton.md — one paragraph after the `.roadmap` bootstrap block. Regeneration is a POSITIVE CONTROL: --check before regen failed with exactly 6 drifted surfaces (3 commands + 3 SKILLs, github/gitlab/gitea) and the drift WAS the new paragraph, proving the skeleton really feeds them; after --write, all 18 surfaces byte-match, exit 0.

- item: Verify the change — generate-routing-surfaces.js --check must still print all 18 surfaces byte-matching, and the walkthrough must stay green at full scope, not the sampled fast gate
  status: done
  dispatched: self — surfaces already confirmed 18/18 byte-matching at exit 0; walkthrough run at full scope in the worktree, output to kaola-workflow/issue-929/.cache/walkthrough.log
  result: First pass green — walkthrough 202/202 scenarios at FULL scope (shard 1/1, not the sampled fast gate), exit 0, log at .cache/walkthrough.log; surfaces 18/18 byte-matching. Re-run after the prose repair at .cache/walkthrough2.log. Also green: test-opencode-edition, test-kimi-edition, test-route-reachability, all exit 0 — run because the skeleton feeds those additive editions at install time.

- item: Adversarially verify the WORDING itself, since the wording is the entire fix and a green suite cannot judge prose — check it is accurate against the measured behaviour (non-`—` adopted verbatim, `—`/absent/empty falling through to issue-N), that it actually reaches a consumer-repo roadmap author, that it names no vendor/model/unresolvable command, and that it does not contradict docs/api.md or claim the field is validated when it is not
  status: done
  dispatched: adversarial-verifier over the three authored surfaces + one rendered surface → report at kaola-workflow/issue-929/.cache/wording-verdict.md
  result: VERDICT FAIL, 1 blocking — and it was right. R1 (blocking): the sentence claiming a shared workflow_project value directs several issues into one folder described a mechanism that DOES NOT EXIST. Verified by the orchestrator directly: claimExplicitBundle derives `const project = 'bundle-' + targets.join('-')` at claim.js:1801 and calls projectNameForIssue ZERO times in that range; the second claim on a shared value is refused `target_occupied`; and the contract already says at :361 that bundle_id is the folder name and branch stem. I fabricated it, and the example I chose (`bundle-540-541`) was exactly the string the real lane generates by itself. R4: the same sentence contradicted the same file 220 lines down and the shipped next.skeleton.md bundle instruction. R2: "any other value is adopted as written" is false for `.`, `..`, `../evil`, `a\b`, which silently become issue-N. R7: the paragraph sat OUTSIDE the CLAUDE.md compact-template fence (verified: fence 86-188, paragraph at 324-329), so it was a one-shot bootstrap instruction fired when .roadmap is still empty and never copied into the consumer repo — cosmetic as an intervention. R3: `archive` and any dot-prefixed name are path-safe yet skipped by readActiveFolders, so such a run is claimed but invisible. Confirmed clean: Claim C (rendered surfaces carry a byte-identical paragraph, U+2014 intact, no vendor/model/command) and Claim E (no overclaim of protection).

- item: Repair the prose against the verifier's findings — delete the fabricated bundle sentence rather than rewriting it (subtraction is the remedy; bundle naming is already documented), qualify the path-safety absolute and say the failure is a silent substitution, name the archive/dot-prefix invisibility, and MOVE the consumer-facing rule out of the one-shot bootstrap step into the CLAUDE.md compact template where a consumer repo's later sessions actually read it
  status: done
  dispatched: self, in the worktree
  result: All four repaired. Contract: fabricated bundle sentence deleted and replaced with the measured fact that a bundle's name is derived from its issue set, cross-referencing `### Bundle project and branch naming` (verified present at :381); absolute now reads "any other path-safe value", with the silent issue-N substitution stated; archive/dot-prefix invisibility named. api.md: same qualifier. Skeleton: bootstrap paragraph REMOVED (also fixing the split between the bash fence and its own "skip this step" caveat) and one bullet added inside the compact template. Verified placement: bullet renders at commands/workflow-init.md:153, INSIDE the fence at 86-188; all 6 rendered surfaces plus the skeleton carry it; 18/18 byte-match at exit 0.

- item: Dock the CHANGELOG under [Unreleased], recording that the filed root cause was refuted and why no guard was added, written before any chain receipt run so the receipt is not staled
  status: done
  dispatched: self
  result: New `## [Unreleased]` → `### Documentation` entry at the top of CHANGELOG.md (none existed; release --prepare consumes the section). Records what the field is, the three surfaces now specifying it, that no script changed in any edition, and — as provenance, which belongs here and not in a prompt surface — that the filed root cause was refuted and the filed one-line guard not taken, with the lexical-indistinguishability reason. README needs no docking: it never documented the field's semantics, only listing `project-name` among the subcommands at :1016. Final change set: 10 files, 64 insertions, 1 deletion, ZERO .js files touched, matching the ruling exactly.

- item: Re-verify the repaired prose, since the repair itself introduced three new factual claims (the bundle cross-reference, the silent issue-N substitution, the archive/dot-prefix invisibility) and a new bullet in a new position, none of which the first verification pass covered
  status: done
  dispatched: same adversarial-verifier resumed from its transcript so it holds its own findings → overwrites kaola-workflow/issue-929/.cache/wording-verdict.md. Orchestrator independently confirmed R3 meanwhile: active-folders.js:240 reads `if (entry.name === 'archive' || entry.name.startsWith('.')) continue;`, verbatim as written into the contract.
  result: 7 of 8 prior findings RESOLVED and each re-measured, including R3 driven through the real `claim.js status` subcommand (archive and .hidden claim as `acquired`, then appear in neither active[] nor drift[] — count:1) and R7 fence-membership computed on all six rendered surfaces (bullet INSIDE the CLAUDE.md template in every one; Claim D flipped REFUTED→CONFIRMED because the rule now ships into each consumer repo's own CLAUDE.md, which AGENTS.md mandates reading before any tool call). It also checked all 11 factual assertions in my CHANGELOG entry and found them true. But it raised R9, BLOCKING, and admitted it was present in pass 1 and missed: my sentence "the branch is always `workflow/issue-{N}`" is FALSE on two of four editions. Verified by the orchestrator directly — gitlab-claim.js and gitea-claim.js emit `workflow/gitlab-issue-` and `workflow/gitea-issue-`, the contract's OWN table at :387-388 says so, and docs/api.md:166-167 already carried the correct wording, so I had introduced a second wording of one rule.

- item: Close R9 and sweep its whole failure class — a root-edition-only absolute in prose that the repo refutes elsewhere — across every remaining claim in the new section, then get a final per-edition verification since three instances of this one class have now surfaced
  status: done
  dispatched: self for the sweep, then the same adversarial-verifier resumed → overwrites kaola-workflow/issue-929/.cache/wording-verdict.md. Sweep already done and it found a THIRD instance the verifier had not: the opening field list was root/codex-only — gitea/gitlab `issueRecordContent` also writes `labels` and `url` (measured). Fixed. The em-dash guard, the issue-N fallback and the archive/dot skip were each confirmed 1:1 across all four editions, so they stand.
  result: VERDICT PASS, 0 blocking. Thirteen assertions in the section each checked against all four editions, by normalized-hashing every shared predicate across its copies instead of reading root and assuming — the specific mistake that made passes 1 and 2 necessary. No fourth instance of the class. Notable per-edition confirmations: isSafeName is normalized-identical across TWELVE production copies; the archive/dot skip precedes the isSafeName line in all four (root/codex :240 before :241, gitlab :219/:220, gitea :218/:219); `labels`/`url` are WRITE-ONLY, read back by nothing in any edition, so "only workflow_project is read back as an identifier" holds — the issue number comes from the FILENAME, not the `issue:` field. The reworded branch clause names three forms covering four editions with no over-generalization either way. Also green after the rewording: validate-workflow-contracts.js (this doc IS test-consumed, at :337/:360/:367/:399/:441) and generate-routing-surfaces --check 18/18. RECORDED NOT BUILT, per additive derivation: on gitlab/gitea the `refresh` path defaults workflow_project to `issue-{N}` rather than `—` (their cmdInitIssue still defaults to `—`), so a reader on those forges may never see `—` in a generated source; this makes no sentence in the section false, since the section states a read-back rule and authoring advice, both valid x4. R2's residual (the consumer bullet's unqualified "Any other value") stands cleared as a defensible one-line compression, non-blocking.
