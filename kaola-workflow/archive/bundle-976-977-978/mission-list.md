# Close #976 (installer temp roots escape into the checkout), #977 (retired-name handling incomplete on two axes, unobservable on a third), #978 (the sink's lane exemption still destroys three measured shapes)

Run facts: branch `workflow/bundle-976-977-978`, worktree
`/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-976-977-978`, this folder lives in
main. User directive for this run: **reviewers use the Fable model**. Test custody holds — whoever
implements a behaviour does not author its tests.

- item: Reconstruct the three missing roadmap sources — `kaola-workflow/.roadmap/issue-{976,977,978}.md` do not exist, so `ROADMAP.md` reads "No active work" while three issues are open and `validate-remote` reports "nothing compared — no open local sources"; write them from the forge bodies in the shape of the deleted `issue-969.md` (see `git show b7b75f58^:kaola-workflow/.roadmap/issue-969.md`), then regenerate the mirror. Closure removes them again.
  status: done
  dispatched: self; output lands as `kaola-workflow/.roadmap/issue-{976,977,978}.md` plus a regenerated `kaola-workflow/ROADMAP.md`
  result: The three sources now exist **on the run branch in the worktree** (not main — main was restored clean, since these travel with the bundle and closure deletes them). `ROADMAP.md` regenerated there and lists all three rows against `bundle-976-977-978`. `validate` = ok; `validate-remote` went from "nothing compared — no open local sources" to "ok: 3 open local sources compared against the remote, none closed there", so the mirror can now actually see drift.

- item: Premise-check #976 — measure, do not assume, that all seven enumerated sites (`install.sh:24,399,427,433`, `install-opencode.sh:305,309,600`, `install-kimi.sh:322`) still create a temporary root that resolves against the current directory under a relative `TMPDIR`, and that `scripts/test-fixture-sandbox.js` is stub-driven and cannot reach them. The property is **"does not resolve against the current directory"**; the issue records that `realpathSync(mkdtempSync(...))` and `path.resolve(".")` were both measured NOT to reach it. Record line-accurate findings; the line numbers are from the prior run and may have moved.
  dispatched: investigator subagent **on Fable**, read-only over the worktree, all TMPDIR induction confined to the scratchpad; output lands at `kaola-workflow/bundle-976-977-978/.cache/premise-976.md` in main. First attempt (default model) died instantly on an account-level auth failure and produced nothing — the `investigator` definition pins a subscription-gated model; an explicit model override is the workaround.
  status: done
  result: `.cache/premise-976.md`. **The defect is real but the filed claim is wrong in three load-bearing ways.** (1) **The property is PLATFORM-CONDITIONAL and the issue never says so.** On macOS `/usr/bin/mktemp` reads `_CS_DARWIN_USER_TEMP_DIR` and ignores TMPDIR **even when absolute** (measured six ways + man page), so **all eight sites are immune on macOS**; on GNU/Linux seven of eight escape, reproduced live through the real `install.sh` in a `node:22-bookworm-slim` container (2 `tmp.*` files observed in the checkout root mid-run, gone afterward). The filed generalisation from #975 is unsound because **those** sites interpolated `$TMPDIR` into an explicit template (escapes everywhere) while **these** rely on mktemp's own TMPDIR consultation (never happens on macOS). (2) **Enumeration wrong at both ends**: prose says seven, body lists eight; and `install-kimi.sh:323` escapes on NO measured platform — on GNU `mktemp -t kaola-kimi-hooks` errors "too few X's in template", exit 1, and under `set -euo pipefail` (`:46`) **aborts the install whenever a config exists**. That is a different, unfiled, worse defect at the same line. Line drift: install.sh 399→401, 427→429, 433→435; kimi 322→323. (3) **Harm understated**: a bare walkthrough under `TMPDIR=.` does not merely litter — it **modified one tracked file and created one untracked artifact** under `kaola-workflow/archive/` and failed spuriously. **CORRECTION**: premise-976's verdict paragraph says "modified two tracked files", but its own leg-1 transcript shows ` M kaola-workflow/archive/issue-210/workflow-state.md` and `?? …/finalization-summary.md` — one modified tracked file plus one NEW UNTRACKED file. The doc pass caught the discrepancy; I verified it against the transcript and the transcript wins. The CHANGELOG states the transcript's version. I had propagated the overstatement into this record and into a report to the user before it was caught — the harm is still real and still worse than "self-deleting litter", but it is one tracked file, not two. Also: the Node surface is missed entirely (497 `mkdtempSync(` sites, **4 in production**), including `run-chains.js:326` — *the very site the issue cites as the immuniser* — which builds its own root with the dead idiom. Confirmed accurate for fairness: both dead idioms, the sandbox suite's structural blindness, "nothing in-repo sets a relative TMPDIR", the child-side half of the run-chains sentence, and the #975 fix still holding (254 assertions, zero escapes, re-verified under `TMPDIR=.`).

- item: Premise-check #977's three parts — (1) `install.sh` `RETIRED_COMMANDS` omits `claude-workflow.md` and `claude-workflow-phase1..6.md`; (2) `install-opencode.sh`'s `uninstall_edition` removes by source-tree name and ignores its own retired list, where `install-kimi.sh:419-425` consults `RETIRED_ROLE_SKILLS`; (3) probe `U1` at `scripts/test-kimi-edition.js:1369+` installs before it uninstalls, so it structurally cannot observe a retired residue. A check that reads the installer's own array can never catch an omission in it — cross-check with `git log --no-renames --diff-filter=D --name-only` against what that era's generator rendered.
  status: done
  dispatched: investigator subagent **on Fable**, read-only over the worktree; output lands at `kaola-workflow/bundle-976-977-978/.cache/premise-977.md` in main. First attempt died on the same auth failure, produced nothing.
  result: `.cache/premise-977.md`, measured at 51db5d2d in a scratch clone. All three parts CONFIRMED. Part 1: stranded set is exactly the filed 7 (`claude-workflow.md` + `phase1..6`, renamed away 2026-05-10/13), pre-existing confirmed. **One refutation** — "only mechanism that could clear them" is false: `uninstall.sh:97-104` globs `claude-workflow*.md` and was watched removing all 7; true only of the install/upgrade path. Part 2 CONFIRMED live (planted `kaola-workflow-fast.md` survives opencode `--uninstall` at exit 0); kimi's lines are **419-426**, not the filed 419-425. Part 3 CONFIRMED **and mutation-proven** (a mutant with the retired loop deleted passes every U1-checked condition while stranding a planted dir); two wording caveats — "at all" is kimi/commands-scope true but repo-wide false (opencode R3 observes retired-AGENT residue), and "needs a NEW probe" is a design choice not a structural necessity (~5 lines inside U1 suffices, R3 is the in-repo precedent), though the plant must come AFTER the seed install. Blast radius for part 2: exactly 9 basenames, and **every reinstall already deletes those same 9 today** (P7b-pinned), so wiring changes *when* deletion happens, not *what* — kimi already ships this behaviour.

- item: Premise-check #978's three shapes against the real sink CLI — (1) a plain file literally named `kaola-workflow\proj\x.md` classifies as lane because `isParkedLanePath` normalises `\` to `/`; (2) an embedded git repo under a lane prefix is one collapsed `??` record even under `-uall`, and everything uncommitted inside goes with the removal; (3) the legacy route has no equivalent of the `--sink` route's best-effort rescue at `sink-merge.js:2100-2130`. All three are destroyed equally by pre-#973 code, so they are residuals, not regressions.
  status: done
  dispatched: investigator subagent **on Fable**, read-only over the worktree, any sink reproduction confined to a scratch clone in the scratchpad; output lands at `kaola-workflow/bundle-976-977-978/.cache/premise-978.md` in main. First attempt died on the same auth failure, produced nothing.
  result: `.cache/premise-978.md`. **All three shapes CONFIRMED**, plus the (f)/(g) constraint, the residual framing, and byte-identity — each measured, not argued. Real anchors (all filed line numbers drifted): classifier `adaptive-schema.js:410-433` (normalise at `:411`), `worktreeDirtRecords` `sink-merge.js:499-510`, probe `:561`, `--sink` rescue `:2156-2172`/`:2268-2274` (filed as 2100-2130), legacy removal `:3217-3230`, arms (e)-(i) `test-sink-merge.js:4177+`. **Two corrections to the filed text**: (i) shape 2's `.kw/worktrees/` example is the WRONG mechanism — `.kw/` is gitignored so a nested repo there yields *no record at all* (arm (g)'s ignored population); the collapsed-record mechanism is real under `kaola-workflow/<seg>/`. (ii) shape 2 UNDERSTATES the loss: the nested repo's own `.git` goes too, so committed-but-unpushed history is destroyed, not just uncommitted work. **Honest narrowing the fixer needs**: under a lane prefix the collapse does not change *classification* (plain files there are individually exempt anyway) — what it changes is that an entire foreign repository hides behind one exempted segment where no per-file test can see inside. Mutation A (delete the exemption) = 21 failed/919, arm (f) on all four editions plus more; mutation B (`--ignored`) = exactly 4 failed/938, **all four are arm (g)** — the filed exclusivity claim confirmed by exhaustive run, and (g) is therefore the fragile tripwire. Legacy route is NOT dead code but no shipped surface invokes it: reachable by a direct no-flag operator invocation (`--sink` is optional; the rendered finalize surface always passes it). Byte-identity confirmed by one hash across all four copies.

- item: NEW, found by the #977 premise check, not filed — `uninstall.sh`'s `RETIRED_AGENTS=("contractor")` omits `issue-scout`, `workflow-planner`, `docs-lookup`. Measured: planted managed+manifested, uninstall exits 0 leaving all three in `~/.claude/agents` **and** deletes the manifest, so a fresh reinstall cannot heal them — a permanent strand, strictly worse than #977 part 2's class. Squarely inside #977's stated result ("removed from a deployed install, on both the install and uninstall paths"); the fix is three names plus a probe.
  status: done
  dispatched: folded into the #977 implementation dispatch below, after the user ruled both axes in scope
  result: Closed by the #977 implementation — `uninstall.sh` `RETIRED_AGENTS` gained `docs-lookup`, `issue-scout`, `workflow-planner`; removal stays gated on the managed marker so a user-authored file at one of those names survives byte-intact. Pinned in `test-install-adaptive-config.js` (planted managed+manifested so a name-shaped OR manifest-shaped fix is observable) and mutation-proven by the #977 reviewer.

- item: NEW, found by the #977 premise check, not filed — retired hooks (`kaola-workflow-pre-commit.sh`, `write-lane.sh`, retired 2026-07-20, shipped in tracked `.opencode` as of 06-23) survive opencode `--uninstall` **and** reinstall: no retired list, no manifest, no healing path at all. kimi shares the uninstall shape. **User ruled 2026-08-14: IN SCOPE for this run** — asked, and overruled my recommendation to file it instead. Needs a real design (a retired-hooks declaration plus a healing path) across opencode and kimi both, where the other axes need only a name in an existing array.
  status: done
  dispatched: folded into the #977 implementation dispatch below, with my ruling (a) — blocklist model, never a namespace sweep
  result: Closed by the #977 implementation — a new `RETIRED_HOOKS` blocklist read by BOTH paths on opencode and kimi (sweep before the hook `cp` in `copy_tree`; explicit removal in `uninstall_edition`). Contents censused from the generators' `HOOK_SCRIPTS` history via `git log -L`, not memory: exactly {pre-commit, write-lane} per edition. `phantom-advisor` deliberately EXCLUDED — it died 06-11, before `.opencode/hooks` existed (06-19) and before kimi existed (07-17), so it never shipped on either edition. Pinned by opencode P8/U2 and the kimi U1 extension, plus the kimi install-path P8 added after the review.

- item: Scope call on #976's true blast radius, measured by me inline and NOT in the issue — `os.tmpdir()` returns `"."` verbatim under `TMPDIR=.` (it does not absolutise), and `fs.mkdtempSync(path.join(os.tmpdir(), 'probe-'))` creates the directory in the **cwd**, confirmed by `existsSync` against `process.cwd()`. So the property is not seven installer sites: it holds at **every Node `os.tmpdir()` call site in the repo** — validation-runner, the walkthrough sims, adaptive-schema's barrier index, sink-merge's `wtStageDir`, and more. `run-chains.js` absolutising for chain children means `npm test` is immune and the exposure is a bare `node scripts/…`. #976's stated result is scoped to "fixture or installer" roots; decide whether this run closes the property at the installer sites only, or everywhere it holds. Do not silently pick the narrow reading.
  status: done
  dispatched: self, inline measurement
  result: **User ruled 2026-08-14: installers PLUS one shared choke point** — meet the filed result in full, immunising the Node population at a single choke point rather than editing 487 call sites. Counts I measured: 8 unguarded installer `mktemp` sites (not 7), 487 test/fixture Node sites, 4 production Node sites; `install-all.sh`'s 3 sites already guarded via `KW_TMPDIR`, `uninstall.sh` clean. The premise check independently corroborated the property (`os.tmpdir()` → `"."` verbatim) and raised the Node count to 497 by a wider pattern. **The ruling survives the premise check — the new facts strengthen it — so it is not being re-asked.** Two precedents to extend, never invent: `KW_TMPDIR` (`install-all.sh:43-52`) and `tmpBase()` (`test-install-all.js:79-82`).

- item: Author the failing tests for #976 — an oracle proving no fixture or installer temporary root is created inside the working checkout whatever `TMPDIR` holds, reaching the **eight** sites (not seven) the stub-driven sandbox misses, plus the Node choke point the user ruled in. **The verification trap that dominates this item**: on macOS every one of the eight sites is IMMUNE (mktemp reads `_CS_DARWIN_USER_TEMP_DIR` and ignores TMPDIR even when absolute), so a shell-site test written and run only on this machine proves nothing — it must either drive Linux or assert the guard's presence in a way that cannot pass vacuously here. The Node half IS observable on macOS (`os.tmpdir()` honours a relative TMPDIR). Prove failure on the recorded baseline.
  status: done
  dispatched: tdd-guide subagent on Fable, tests only, into the worktree; output lands in the install/fixture suites plus a summary at `kaola-workflow/bundle-976-977-978/.cache/tests-976.md` in main

  **#976 REVIEW IN — PASS, 0 blocking.** `.cache/review-976.md`. **The shim audit I most wanted came back sound**: the GNU-shaped `mktemp` PATH shim was driven against REAL GNU coreutils 9.1 in a container and matched on all 13 load-bearing rows, including file modes and the exact "too few X's" abort — so the shell half of #976 does NOT rest on a false oracle. Two divergences recorded, both stricter-only (mid-template X's; `-p`/`-t` precedence), with the honest note that the `-p` precedence is the one latent false-green shape *if a future fix ever used `-p`* — no current site does. It also re-ran the mutation matrix itself rather than trusting the record, adding a novel single-site revert mutant that proves per-call sensitivity; drove the fixed installers through real GNU `mktemp` under `TMPDIR=.` (zero escapes, kimi install with an existing config now exit 0, doctor-reject restores byte-identically); probed the kernel choke point across absolute/trailing-slash/repo-interior/empty/unset and confirmed no production consumer reads a raw TMPDIR; and **ran two kernel-pinning suites the implementer had skipped** — `kernel-conformance` (254) and `oracle-kernel` (48), both green. It also caught that my recorded kernel hash was stale, which is corrected above.
  result: `.cache/tests-976.md`. New suite `scripts/test-relative-tmpdir-escape.js`, registered in **both** `test:kaola-workflow:claude` and `:claude:full` (`package.json` the only other edit); `test-suite-registration.js` passes; ~7s runtime. **RED: 39 passed / 9 failed**, identical on macOS, in a `node:22-bookworm-slim` container, AND in the live worktree with the concurrent #977/#978 edits present. Signature failures: `install.sh: 16 of 16 mktemp-created paths landed INSIDE the copied checkout`; `install-kimi.sh: exits 1 … too few X's in template`; walkthrough roots beside the observed root; `run-chains: private chain root ABSOLUTE but INSIDE the runner's cwd`. **The macOS-vacuity trap was taken head-on** rather than papered over: the installer scenarios drive the REAL installers under a GNU-shaped `mktemp` PATH shim with a live positive control every run, and shim fidelity was validated in-container against real GNU coreutils 9.1 across all six invocation rows, plus an independent replication of the real-GNU `install.sh` escape. What the shim does and does not prove is stated in both the summary and the suite header. Mutation matrix: simulated correct fix → 48/0 green on macOS AND Linux; three near-misses each red only their own pins (realpath idiom → walkthrough only; empty-only guard → installers only; delete-the-kimi-backup → the C2 restore pin only). **Two constraints for the implementer**: scenario E also caught `kw-barrier-idx-*` (validation barrier machinery) escaping, so the choke point must reach beyond `createIsolatedChainSpec`; and an over-eager fix that rejects an absolute TMPDIR merely for sitting inside a repo **will break `test-run-chains` T30** — the pinned property is only "never resolve against the cwd". Note: the fast gate is deliberately RED on this branch until the fix lands.

- item: Author the failing tests for #977 — an oracle for a retired surface being removed from a deployed install on **both** the install and uninstall paths, across all **five** axes (parts 1/2/3 + axis A `RETIRED_AGENTS` + axis B retired hooks). The probe must **plant** retired names first, and the plant must come AFTER the seed install, since the install sweeps retired names before writing. A new probe is one admissible shape but not structurally required — `R3` is the in-repo precedent for planting inside an existing probe. Every oracle must fail on the baseline and be mutation-proven, since the measured failure here is precisely a probe that cannot see a residue.
  dispatched: tdd-guide subagent on Fable, writing tests only (never production code) into the worktree at `.kw/worktrees/bundle-976-977-978`; output lands in `scripts/test-kimi-edition.js` / `scripts/test-opencode-edition.js` / the install suites, plus a summary at `kaola-workflow/bundle-976-977-978/.cache/tests-977.md` in main
  status: done
  result: `.cache/tests-977.md`. Four suites touched, +166 lines, **no new files** — every pin rides an existing registered suite. **Custody verified by me, not taken on trust**: `git status` shows only `test-install-adaptive-config.js`, `test-install-upgrade-rewrite.js`, `test-kimi-edition.js`, `test-opencode-edition.js` modified; no production file. All pins are disk-outcome-only and **no test reads any installer array** — the anti-pattern that let these bugs live. RED on baseline: axis 1 in the upgrade suite, axis A in the adaptive-config suite (planted managed+manifested so a name-shaped OR manifest-shaped fix is observable), axes 2+B via new probes U2 and P8 in the opencode suite (3 FAILs / 652 passed), axis B kimi leg via a U1 extension (2 FAILs / 610 — the second is the EXISTING no-residue pin now *armed* by the plant). Axis 3 is baseline-GREEN by design, since kimi's mechanism already ships and the defect was test blindness — proven by running the premise check's mechanism-removed mutant, where the extended U1 goes red with 5 FAILs. Satisfiability proven for every red pin against scratch fix-mutants. **Deliberately not pinned, and I accept each**: uninstall of the 7 `claude-workflow` names (already works), duplicate 9/11-name enumerations (would rot; P5c pins completeness on install), user-hook survival, and the kimi install-path hook sweep.

- item: Author the failing tests for #978 — oracles for the three shapes that do not break the two arms the guard is balanced against: arm `(f)` (an ordinary lane-only run must still complete — a bare widening of the exemption test refuses every sink) and arm `(g)` (reaching past `--untracked-files` into `--ignored`; nothing else in the 942-assertion suite catches that mutation). Both arms are already in `scripts/test-sink-merge.js`.
  dispatched: tdd-guide subagent on Fable, tests only, into the worktree; output lands in `scripts/test-sink-merge.js` plus a summary at `kaola-workflow/bundle-976-977-978/.cache/tests-978.md` in main
  status: done
  result: `.cache/tests-978.md`; `test-sink-merge.js` +249 lines, no production file touched. **Verified red by me, not on report**: `node scripts/test-sink-merge.js` → exit 1, **20 failed / 994 passed** (baseline was 942). The 20 are arms **j** (backslash name, ×2 assertions), **k** (embedded repo, ×2) and **l** (legacy journal, ×1) across all four editions — root, codex, gitlab, gitea — which is the coverage the ports needed since nothing else compares them. Every assertion specifies the RESULT and leaves the mechanism open, verbatim: *"Refusing, preserving it elsewhere and copying it aside all satisfy this; destroying it does not."* Each shape also pins a second, separate result — the sink must not report **unqualified success** over work it destroys: "a non-zero exit, a typed reason on the envelope, or the name in the output all count — silence does not." **Residual the author flagged, and I relayed to the implementer mid-flight**: (j) and (k) drive the `--sink` route only, so the pins cannot distinguish a guard-level repair (which covers the legacy route too, since both share `assertWorktreeClean`) from one confined to the `--sink` transaction body (which would leave legacy still destroying both shapes — green suite, defect alive on the default no-flag invocation). Also relayed: shape k's collapsed record carries a TRAILING SLASH in the porcelain string, so any keying must be checked against the real output.

- item: Implement #976 against its tests — no fixture or installer temporary root inside the working checkout under any `TMPDIR`; one shared helper, per-site guards, or an assertion at creation are all open, but the two measured-dead idioms are not.
  dispatched: implementer subagent on Fable, into the worktree, production code only; output is the 8 installer sites plus the Node choke point the user ruled in, with a note at `kaola-workflow/bundle-976-977-978/.cache/impl-976.md` in main. Sequenced AFTER #978 landed so only one agent is ever in `sink-merge.js` (a #976 production temp site at `:2166` is also #978's stage dir).
  **CUSTODY SPLIT, ruled mid-flight.** The implementer measured that scenario D **cannot** be turned green by any production-only edit: `simulate-workflow-walkthrough.js:22` calls `mkdtempSync(path.join(os.tmpdir(), 'kw-sandbox-home-'))` at MODULE LOAD, and the only modules loaded before it (`test-spawn-census:7`, `test-git-fixture:10`) are test infrastructure requiring no production module — so no choke point can execute in that process before the escape. The second escaper is `:13192`, same file. That file is a **test artifact**, so I ruled the implementer must not touch it and routed the edit to `tests976`, who holds custody and already edited it in their fixmirror. The implementer proceeds on everything else and leaves scenario D red, correctly attributed.
  status: done
  result: `.cache/impl-976.md`. Choke point landed as a require-time TMPDIR/TMP/TEMP normalisation in the kernel, propagated to all four editions — **I verified byte-identity myself**: all four `kaola-workflow-adaptive-schema.js` copies hash **`ac916beb…`** (was `0ac70c1d…`; I first recorded `3c13cc5f…`, which was taken BEFORE the constraint-3 comment reword — the #976 reviewer caught the staleness and I re-hashed to confirm. Byte-identity across the four editions, the property that actually matters, held throughout). `validation-runner` likewise updated in all four copies. **Suites I ran myself, not on report**: `test-relative-tmpdir-escape` exit 0 at **48 passed / 0 failed**; `test-run-chains` exit 0 at **283 assertions** (T30 and the hand-down contract intact, so the choke point did not over-reach); `test-install-all` exit 0 at **254** (the #975 fix not regressed); `test-sink-merge` exit 0 at **1014** (#978 undisturbed by the shared `sink-merge.js:2166` site); `test-validation-runner` and `test-validation-allowband` (17) both exit 0, so the load-order hoist is clean. Choke-point rationale: the kernel is the only module every temp-creating production script loads at module start (`run-chains:145`, `sink-merge:9`, `validation-runner` via a one-line hoist), and any caller of the kernel's own barrier-index site has necessarily required the kernel first — so one statement covers all four production sites, the `sink-merge` chdir hazard, and every spawned child via env inheritance. **All three of my constraints were executed and PROVEN, not asserted**: (1) the predicate is `val && !isAbsolutePath(val)` and an absolute value is never assigned — measured, an absolute symlinked path is NOT realpathed (`/tmp/x` stays `/tmp/x`) and a trailing slash survives byte-identical; (2) idempotence proven by a probe that deletes the require-cache entry so the block genuinely re-executes — byte no-op, and re-requiring after a run-chains-style hand-down leaves the private root untouched; structurally it cannot drift because the block's only output is `/tmp`, which its own predicate excludes; (3) **the comment audit FAILED one clause and it was reworded** — "'/tmp' is what `os.tmpdir()` itself falls back to when nothing is set" was a platform-general claim that had not been measured everywhere, now restated as the repo fact "the same floor the repo's existing absolute-or-`/tmp` guards normalise to". Shell-side comments state GNU behaviour as GNU-specific (coreutils 9.7/9.1 measurements), never generalised from this Mac. Its own mutation proofs: kernel guard degraded to empty-only → 46/2, exactly the two run-chains pins; shell guard degraded to bare `${TMPDIR:-/tmp}` → 41/7, all seven installer location pins. Sink-merge non-disturbance was verified by naming what was compared — sha256 of all four sink surfaces captured before the first `edition-sync --write` and after the second, diff empty. **Honest chronology disclosure**: the custodian's walkthrough fix landed in the shared worktree before its post-fix oracle run, so no run demonstrates its half in isolation; the mutation matrix serves that purpose instead.
  **Choke-point design approved with three bounding constraints**, since a require-time env mutation is a genuine global side effect: (1) normalise ONLY when TMPDIR is relative — an absolute value is left byte-for-byte alone, which is what keeps `test-run-chains` T30 alive; (2) idempotent, proven not assumed, so a second require or a post-hand-down require is a no-op; (3) the comment may state only what was measured on this machine and must not generalise to the shell sites or to unmeasured platforms. Hoisting a top-level kernel require into `validation-runner` (currently lazy at `:974`) is permitted only as a minimal edit with `test-validation-runner` and `test-validation-allowband` green, since load-order changes carry non-obvious effects.
  **Walkthrough custody edit DONE** (`tests976`): one env-normalisation block at the very top of `simulate-workflow-walkthrough.js` — TMPDIR/TMP/TEMP, if set and not absolute, become `/tmp`. Env shape chosen over a `tmpBase()` helper because the file holds **232** `mkdtempSync` sites and spawns children that inherit the env, so one statement covers the whole process tree where a helper would be 232 edits or partial coverage; it extends the same absolute-or-`/tmp` RULE at process granularity and uses neither dead idiom. Root-only is correct — the file is excluded from edition sync at `validate-script-sync.js:17`. **Sweep answered as asked**: exactly TWO unconditional escapers, measured empirically (an owns-nothing shard under `TMPDIR=.` produced exactly `kw-sandbox-home-*` at `:22` and `kw-active-folders-*` at `:13192`) AND cross-checked statically (of 232 sites, `:22` is the only module-top statement and `:13192` the only unconditional runner-level one; the other 230 are scenario-scoped and covered by the env shape anyway). **Verified**: `test-relative-tmpdir-escape.js` exit 0 at **48 passed / 0 failed** with both halves converged; full walkthrough exit 0 at **210/210** scenarios under a normal TMPDIR; and — the filed harm itself — the fixed tree on a clone under `TMPDIR=.` runs the FULL walkthrough at exit 0 with **zero live-polled escapes and `git status` byte-identical before and after**, so the leg-1 archive-band tracked-file mutation is gone. Scenario D proven still armed: stripping the normalisation from a scratch copy of the FIXED tree reds exactly the walkthrough scenario. No suite assertion was touched.
  **Edition walkthroughs closed too.** I routed the flagged sibling finding back rather than letting it evaporate, and the custodian found **five** edition entry points, not the three I briefed — the codex variants exist as well (`simulate-gitea-codex-`, `simulate-gitea-`, `simulate-gitlab-codex-`, `simulate-gitlab-`, `simulate-kaola-workflow-`), so it measured instead of taking my count. Six files, +89 lines total. **Verified by me**: `validate-script-sync.js` exit 0 (so editing several of a parity-compared set did not trip it) and `generate-routing-surfaces.js --check` reports all 18 surfaces byte-matching the skeleton. Each file was **measured before being touched**: the codex walkthrough was the worst at **22 distinct roots in the checkout plus exit 1**; gitlab and gitea one each at module top. The two codex-variant wrappers have **zero own temp sites** — their escape is TRANSITIVE, belonging to the `test-{forge}-workflow-scripts.js` child each spawns, and the wrapper-top guard covers the whole child tree by env inheritance, so no child file was edited. *"Nothing was guarded that did not escape; nothing that escaped was left open"* — additive derivation applied exactly. The block is **byte-identical across all six entry points, machine-checked**, and the root's earlier wording was rewritten to the shared text so there is one rule in one wording. Sync status verified rather than inferred: none of the five appears in `edition-sync.js`, and `validate-script-sync.js:17` explicitly excludes the walkthrough pair. **The load-bearing confirmation I asked for**: `edition-sync.js --write` on a copy of the current tree reports "0 file(s) updated — tree already in sync" with all six walkthroughs SHA-256 identical before/after, so the implementer's sync runs cannot overwrite these edits. **One probe was invalidated mid-round and honestly redone**: macOS openrsync's unanchored `--exclude='kaola-workflow'` silently skipped `plugins/kaola-workflow` by exact-basename match, so the codex file's first post-fix probe ran against a STALE copy and faithfully re-showed the baseline; anchored, re-overlaid, re-probed clean. A false negative from a tooling gotcha, caught by its own author. **The kernel-vs-never-escaped distinction I asked for, answered and stable**: none of the five is immunised *through* the kernel and none never-escaped — every escaper is a module-top `mkdtempSync` executing at require-time of the walkthrough file itself, BEFORE any production require (the gitlab walkthrough creates its sandbox at `:24` and only then requires sink-mr, which reaches the kernel). The kernel's require-time normalisation, confirmed at `adaptive-schema.js:36-39` in the same absolute-or-`/tmp` shape, structurally cannot run first. So the entry-point guard is the stable fix and the two mechanisms do not overlap — which is what makes this a real fix rather than an accident of load order.

- item: Close #978 review finding **R1** — a dangling/self-referential symlink or unreadable file inside `kaola-workflow/<project>/` makes the legacy stage throw, the `catch` swallow it, and the sink destroy the journal at exit 0 saying nothing. **My ruling: fix it, do not file it.** It is byte-identical to baseline so it is a residual rather than a regression — but #978 was filed precisely to close residuals of this class, its stated result is "the sink does not silently destroy uncommitted work", and a reviewer demonstrated it on all four editions with a constraint-safe repair available that flips no green arm. Leaving it would close #978 with its own stated result unmet on a trigger we have measured. Needs a pin first (custody), then the fix. **Sequenced after #976's implementation**, since both reach `sink-merge.js`.
  status: done
  result: Mechanism: **refuse-on-throw at each stage site, scoped to "journal directory exists AND the stage attempt threw"** — a missing worktree or absent journal proceeds exactly as before, which is what leaves (f)-(i), (i-control) and (l) untouched. Legacy: the stage error is set aside and checked OUTSIDE the swallowing catch, before `removeWorktree`. `--sink`: removal becomes `if (!wtStageErr) removeWt(...)`, and a set error emits a typed envelope (`result: refuse`, `reason: stage_failed`, `step: merge`, exit 1) using the transaction's own refusal pattern. **CORRECTION — the two routes refuse in DIFFERENT shapes, and I got this wrong upward before the doc agent caught it**: the typed `reason: 'stage_failed'` envelope is the `--sink` transaction's shape ONLY (`sink-merge.js:2190-2203`, via `sinkEmit`). The LEGACY route refuses by **throw** in its keep-guard shape (`:3279-3286`, `"sink-merge refused: could not stage …"`) — same capture-after-catch structure, same skipped removal, same source path and underlying error in the message, but **no typed reason field**. I verified both blocks myself after the catch. What is true of both: they refuse, they skip the removal, and they name the path and the error. **The clean partition I asked it to report rather than absorb**: mutation C (legacy throw disabled, `--sink` repair kept) → exactly the 4 (m) pins red with (n) GREEN; mutation D (`--sink` disabled, legacy kept) → exactly the 4 (n) pins red with (m) GREEN. Two separately load-bearing repairs; the "one mechanism reds both pins" case did not occur, so no escalation was owed. **A link-aware copy was considered and rejected with reasons stated for review** — it would fix only the fixtured trigger while leaving EACCES/mkdtemp faults silently destructive, and a faithful link copy would force changing `sinkLandStagedUnion`'s `existsSync` destination probe, whose landing semantics are #707-pinned. **It also caught its own earlier comment** — "a stage failure never stops the sink" — as now false, and rewrote it to the measured behaviour in all four copies.
  **Both routes repaired and verified by me**: `node scripts/test-sink-merge.js` → exit 0 at **1058 assertions**, exactly the count the test author's independent two-site repair mirror predicted. Two agents reaching the same number without coordinating is the cross-check that the fix landed at BOTH stage sites rather than one. The sink diff across all four copies grew from +166 to +326.
  dispatched: `tests978` (custody holder) authoring the pin into `scripts/test-sink-merge.js` — the RESULT pinned is "when the legacy stage cannot preserve the journal, the run must not report unqualified success over destroying it", accepting a non-zero exit, a typed reason, the path named in output, or survival; mechanism left to the implementer. Must be RED against the CURRENT candidate (the #978 fix is already in the tree), drive all four editions, and mutation-prove. The implementer fix follows once the pin lands.
  **Pin landed as arm (m)**: `buildLegacyWorktreeFixture973` + two worktree-only journal files + BOTH trigger shapes (dangling and self-referential symlink) in ONE fixture — sound reasoning, since whichever the copy meets first loses the whole stage, so one arm covers both without depending on readdir order. RED on the current candidate at **4 failed / 1034 passed**, one per edition, **zero non-R1 failures** — (j)(k)(l) and all 1014 prior assertions green against the shipped fix. Mutation-proven against a candidate-vs-repair mirror: with the legacy catch replaced by refuse-on-throw in all four editions, the suite reaches **1038, exit 0**, the pin flips green and **no green arm flips** ((i-control), (l), #937 c/d, #707 h all hold). The unreadable-file trigger was deliberately NOT fixtured and is named as an omission in the arm's comment — EACCES fixtures invert under root, and a named omission beats a pin that passes for the wrong reason.
  **Twin pinned as arm (n)**: (m)'s fixture shapes carried over exactly — both symlink triggers in one lane dir, journal NONCE with a pre-run positive control, the EACCES omission named again — driven through `runSinkAt`. RED on the candidate at **8 failed / 1050 passed**, exactly (m)×4 + (n)×4 with zero other failures, signature `exit=0 status="sinked" reason=undefined survivors=[]`. **Three-tree mutation proof**: mirror4 (refuse-on-throw at BOTH stage sites, four editions) → **1058, exit 0**, both pins green and no green arm flips; mirror5 (**legacy repair ONLY**) → 4 failed / 1054, the four being exactly (n)×4 with (m) GREEN. That second mirror does double duty — it is (n)'s remove-the-repair direction AND the proof the two pins independently track their own sites, so neither shadows the other and a legacy-only placement is provably insufficient. One deliberate premise difference flagged so it is not misread as drift: `worktree_dirty` is NOT on (n)'s unrelated-stop list, because on the `--sink` route a guard refusing over an uncopyable lane entry is a legitimate repair rather than an unrelated stop. **Blast-radius question answered by reading both stage blocks, not assumed**: IDENTICAL content scope, larger reach — both copy exactly `<wt>/kaola-workflow/<args.project>/` through the same `sinkCopyDir`, swallow identically to `wtStageDir = null`, land through the same `sinkLandStagedUnion`, and on failure both proceed to the same `git worktree remove --force`; anything outside the own-project lane dir is the GUARD's jurisdiction on both routes alike. What differs is reach and the terminal the loss hides under (`"sinked"` vs `"merged"`). So one repair shape can serve both sites.

  **NEW FINDING, and I ruled it IN immediately without asking**: the `--sink` merge step's stage carries the IDENTICAL swallow (`sinkCopyDir` in try/catch → `wtStageDir = null`; root/codex ~:2177, gitlab ~:2038, gitea ~:2047), so the same symlink silently loses the journal on the `--sink` route too, at `status:"sinked"` exit 0. **This is the more important half**: the premise record established the legacy route is reachable only by a direct operator invocation omitting the flag, while every shipped surface — the rendered finalize — always passes `--sink`. Repairing only the legacy stage would fix the route almost nobody runs and leave the defect live on the route everybody runs. Not a close call, and not a value judgement, so it did not go to the user.

- item: Implement #977 against its tests, now **five axes** — parts 1/2/3 plus axis A (`uninstall.sh` `RETIRED_AGENTS`) and axis B (retired hooks). **User approved the opencode uninstall wiring on 2026-08-14**, on the measured basis that reinstall already deletes those same 9 names and kimi already ships the behaviour. Correct the two filed wording errors while here: kimi's lines are 419-426, and a plant-mode inside U1 is admissible (the plant must follow the seed install).
  dispatched: implementer subagent on Fable, into the worktree, production code only — it reads and runs the tests but never edits them; output is the fix across `install.sh` / `install-opencode.sh` / `install-kimi.sh` / `uninstall.sh` plus a note at `kaola-workflow/bundle-976-977-978/.cache/impl-977.md` in main. My two rulings passed down with it: hooks use the **retired-list (blocklist)** model, never remove-what-isn't-shipped, since #973 deliberately moved away from exactly that; and the kimi install-path hook sweep is IN, but must be measured before it is built.
  status: done
  result: `.cache/impl-977.md`. Production files only — `install.sh` (+7), `install-opencode.sh` (+42), `install-kimi.sh` (+26), `uninstall.sh` (+6); **no test file touched**, custody intact in both directions. **I ran all four suites myself rather than trusting the report**: `test-install-upgrade-rewrite` exit 0, `test-install-adaptive-config` exit 0, `test-opencode-edition` exit 0 at **655 assertions**, `test-kimi-edition` exit 0 at **612** — matching the test author's independently-predicted post-fix counts exactly, which is the cross-check that the fix satisfies the pins rather than dodging them. Per axis: `install.sh` RETIRED_COMMANDS += the 7 `claude-workflow*` names; `uninstall.sh` RETIRED_AGENTS += docs-lookup, issue-scout, workflow-planner; opencode's RETIRED_WORKFLOW_COMMANDS wired into `uninstall_edition` in kimi's shipped shape; a new `RETIRED_HOOKS` blocklist read by BOTH paths on both editions. **Axis 3 took no production change — confirmed, not invented.** Ruling (b) was measured before it was built: unmodified `install-kimi.sh` reinstalled over a planted `kaola-workflow-pre-commit.sh` at exit 0 with the hook SURVIVING and zero removal lines; the edited installer removes it with a named line. That seam has no pin, so the live before/after IS its evidence. Good additive-derivation call recorded: `phantom-advisor` was deliberately EXCLUDED from RETIRED_HOOKS — retired from root 06-11, before `.opencode/hooks` existed (06-19) and before kimi existed, so it never shipped on either edition. The RETIRED_HOOKS contents were censused from the generators' `HOOK_SCRIPTS` history via `git log -L`, not from memory or from reading the installer's own array.

- item: Implement #978 against its tests — the sink stops silently destroying uncommitted work in the three shapes, without breaking arms (f) and (g).
  dispatched: implementer subagent on Fable, into the worktree, production code only; output is the fix across `kaola-workflow-adaptive-schema.js` and/or `kaola-workflow-sink-merge.js` **plus the three edition ports**, with a note at `kaola-workflow/bundle-976-977-978/.cache/impl-978.md` in main. **Sequencing decision**: #976's implementation is deliberately held until this lands, because both could reach `adaptive-schema.js` and that file is the byte-identical cross-edition anchor — two agents in it concurrently is a conflict I can avoid for free by ordering them.
  status: done
  result: `.cache/impl-978.md`. Four files, all sink copies — root, codex plugin (byte-identical to root, verified), and the gitlab/gitea hand-ports. **No test file and no kernel touched.** Mechanism: **j+k in the shared guard** — `worktreeDirtRecords` keeps two record shapes as dirt *before* consulting the classifier: a decoded rel containing a backslash (always a literal filename char, since porcelain emits only `/`) and a rel ending `/` (the collapsed embedded-repo record). Because that guard is the one BOTH entry points share, the legacy-route gap the test author flagged **does not arise** — the relay worked. **l reuses the `--sink` route's own rescue verbatim** (`sinkCopyDir` stage before `removeWorktree`, `sinkLandStagedUnion` after merge), rather than writing a second one. **Verified by me**: `test-sink-merge.js` exit 0, **1014 assertions**, and the kernel still hashes `0ac70c1d3fb8…` identically across all four editions. Both halves independently mutation-proven in a scratch mirror: fix-minus-j/k-guard → exactly 16 failed (j+k × 4 editions, l green); pristine-legacy-plus-guard-only → exactly 4 failed (l × 4). Neither half is load-bearing for the other's pins, which is what makes the proof meaningful. Probe untouched — no `--ignored`, so arm (g) is safe by construction rather than by luck. **Addendum, after I relayed the residual**: the guard-level placement was then *measured*, not merely argued — a fixture carrying all three populations, probed with the guard's own flag form, emits `?? kaola-workflow/crashed9/` (**trailing slash confirmed on the real porcelain string**, so `endsWith('/')` is keyed against reality), and the SHIPPED `worktreeDirtRecords` extracted verbatim from the edited file returns exactly the two #978 records as dirt while exempting the ordinary lane `.cache` file. Legacy Step 2 calls `assertWorktreeClean` immediately before Step 3's `removeWorktree`, and the `--sink` transaction calls it in its preconditions, so one classification governs whichever route arrives. The implementer also stated it derived the mechanism rather than copying the test author's witness, and gave its reasoning — refusal via the existing guard is this repo's sanctioned destruction-protection, and reusing stage/land is the only l-mechanism that satisfies survival *without* refusing, which `(i-control)` forbids.

- item: Close review-977's **R1** — the kimi INSTALL-path `RETIRED_HOOKS` sweep is the one #977 mechanism with no pin (the reviewer's mutant removed it and the kimi suite stayed green on the install side), while its opencode twin IS pinned by P8. Leaving it reproduces exactly the asymmetry #977 part 2 complained about.
  dispatched: `tests977` (custody holder for `test-kimi-edition.js`, and nobody else is in that file) — pin the RESULT on disk outcomes only, never by reading `RETIRED_HOOKS`; plant AFTER the seed install; include the user-authored-hook control, since the blocklist model exists precisely so a user's own hook survives. Proof direction must be a scratch mutant going red, not the already-shipped tree going green. **Explicitly authorised to decline**: if the pin cannot be written without duplicating a list that will rot, a stated omission beats a brittle pin — the same call it made correctly on the 9/11-name enumerations.
  status: done
  result: New `P8 (#977)` in `test-kimi-edition.js`, a twin of the opencode P8 — seed install, then plant BOTH a retired `kaola-workflow-pre-commit.sh` and a user-authored `my-own-hook.sh` **after** the seed install, reinstall, and assert disk outcomes only: retired hook gone, every current hook deployed (positive control that the deploy actually ran), user hook survives byte-intact. **No installer array is read**; the retired name is hard-coded from the census. Anti-vacuity assert included: neither planted name is in the deploy set. **Proof stated in both directions, as required** — the fix already ships, so green is the candidate (`test-kimi-edition.js` exit 0 at **618** = 612 + exactly 6) and RED is the mutant (fresh mirror of the CURRENT tree with only the install-path sweep excised, `bash -n` clean, uninstall side intact → exit 1, **exactly 1 failure / 617 passed**, and the user-hook and deploy controls stayed GREEN in the mutant run, so the red isolates the sweep rather than the fixture). opencode suite unchanged at 655. The list-rot escape hatch was not needed: one censused name pins the sweep's existence without duplicating any list.

- item: File the follow-ups this run measured but deliberately did not build, each with its evidence — (1) the **main-root preflight** still exempts a backslash name (`sink-merge.js:~1697`), the seam the #978 premise record listed as "not measured" and the #978 implementer named as a candidate follow-up, since it chose a porcelain-fed guard fix over a kernel/classifier one; (2) the `--sink`/legacy red-stop-after-removal corner, which parks the stage in the OS tmpdir un-landed — pre-existing, inherited not widened by #978; (3) `validation-runner.js:193/209` handing a possibly-RELATIVE `env.TMPDIR` to sandboxed children, plus two candidates I checked and **closed rather than filed**. Do not let the survivors evaporate — each is a measured fact with a named site.
  **Checked and CLOSED, not filed** (verified rather than assumed, so nobody wastes an afternoon on a fixed bug): (i) `validation-runner:214` still assigns `env.TMPDIR = isolatedTmp`, but `isolatedTmp` derives from `os.tmpdir()` and the new top-level kernel require normalises before it runs — the relative hand-down to sandboxed children is fixed as a side effect of the choke point. (ii) `adaptive-schema.js:1033`'s comment "the index lives OUTSIDE the repo (os.tmpdir)" was false under a relative TMPDIR and is **now true**, because the require-time normalisation happens in that very module before `snapshotWorktree` can run.
  **Still open, worth filing**: the main-root preflight backslash exemption; the red-stop-after-removal corner (pre-existing #619(4)); retired SUPPORT SCRIPTS surviving opencode/kimi `--uninstall` (review-977 R3, uninstall-only — reinstall heals); the `-p`/`-t` precedence latent false-green in the mktemp shim (review-976 R1, only bites if a future fix uses `-p`, which no current site does); and the EACCES/unreadable-file stage trigger, deliberately unfixtured because permission fixtures invert under root. **Not a follow-up, a recorded decision**: review-978 R2 — an in-lane basename containing a backslash now refuses every sink where it previously sank; loud and fail-closed, the safe direction, accepted deliberately.
  status: done
  dispatched: self, after the user ruled "file all five" on 2026-08-14
  result: **Filed #979–#983**, each carrying its measured evidence, what is NOT known, and a SPECIFY-THE-RESULT close: #979 main-root preflight backslash exemption · #980 red-stop-after-removal parks the staged journal under a generated name (same class as #619(4)) · #981 retired support scripts survive uninstall (mildest of the class — reinstall heals it, stated so it is not over-prioritised) · #982 the mktemp shim's `-p`/`-t` precedence as a latent false-green · #983 the EACCES stage trigger unfixtured because permission fixtures invert under root. Roadmap sources written for all five in the worktree, so the mirror lists them after finalize regenerates — **this is the drift I found at the START of this run** (three open issues with no local sources, `validate-remote` blind), and not repeating it for the successor was the point. `validate-remote` now reports "8 open local sources compared against the remote, none closed there".

- item: Adversarially review the whole diff with **Fable** reviewers, one per issue plus a cross-cutting pass, hunting the failure classes this repo keeps producing: prose and comment claims that are false or true only of the root edition, guards that cannot fail, tests that pin a mechanism rather than a result, and a fix that strands what it replaces. Mutation-prove every new guard via a scratch mirror — a green suite is not proof a guard is armed.
  dispatched: two adversarial reviewers **on Fable** (the user's directive for this run), launched now over the finished #977 and #978 halves rather than waiting for #976 — one per issue, read-only, each hunting this repo's recurring failure classes; reports land at `kaola-workflow/bundle-976-977-978/.cache/review-977.md` and `.cache/review-978.md` in main. A third Fable reviewer covers #976 once its implementation lands, plus a cross-cutting pass over the whole diff.
  status: done
  result: **All three Fable reviews PASSED with zero blocking findings**, each reproducing the implementers' mutation proofs rather than trusting them. Their per-issue detail is recorded inline above under each issue's own item; the non-blocking findings were then acted on rather than filed away — #977 R1 (the unpinned kimi install-path sweep) was PINNED, #978 R1 (the swallowed stage failure) was PINNED AND FIXED on both routes, and #978 R2 was accepted as a deliberate fail-closed decision. The single highest-value check was the #978 reviewer driving the LEGACY no-flag route end-to-end over both fixtures on all four editions, confirming the guard-level repair covers the entry point the pins structurally could not reach; and for #976, auditing the GNU-shaped `mktemp` shim against real coreutils 9.1 in a container, since the entire shell half of that issue rests on that oracle.

  **#977 REVIEW IN — PASS, 0 blocking.** `.cache/review-977.md`. All four RETIRED_* lists independently re-censused (`git log --no-renames --diff-filter=D` over all refs + `git log -L` over both generators' HOOK_SCRIPTS) and each is exactly complete. The `phantom-advisor` exclusion is decisively correct and now dated: `install-opencode.sh` was BORN 06-19 in the same commit as `.opencode/hooks`, eight days after phantom-advisor died; kimi was born 07-17. Every #977 pin mutation-proved in a scratch mirror — each mechanism excision reds exactly its own pins and no others. Axis 3's "no production change" verified true (the kimi retired-skill uninstall loop pre-exists at `HEAD:425`). Live-verified in scratch homes: a retired name is removed on reinstall AND uninstall while a **user-authored `my-own-hook.sh` survives** — the destructive-over-reach check my ruling (a) was meant to prevent.
  Three non-blocking: **R1** the kimi INSTALL-path RETIRED_HOOKS sweep is the one unpinned #977 mechanism (its mutant kept the kimi suite green on the install side) — deliberate per tests-977 and live-verified twice, but a future regression there is silent where opencode's twin is pinned by P8. **R2** → doc pass: `docs/opencode-edition.md:146-148` ("copy_tree removes exactly two things") is now FALSE, and `CHANGELOG [Unreleased]` is untouched. **R3** pre-existing, unchanged by this diff: retired support scripts still survive opencode/kimi `--uninstall`.

  **#978 REVIEW IN — PASS, 0 blocking.** `.cache/review-978.md`. Nothing taken on trust: suite green at 1014 in a hash-verified scratch mirror, and the reviewer's own four mutations (backslash condition alone, trailing-slash alone, legacy stage alone, `--ignored` widening) each red exactly their own pin set (8/8/4/4) and nothing else — every mechanism load-bearing, (f) and (g) still armed. **The highest-value check landed clean**: the LEGACY route (no `--sink`) driven end-to-end over a backslash fixture and an embedded-repo fixture on all four editions **REFUSES with the worktree intact** — the guard-level repair genuinely covers both entry points, which the pins structurally could not show. Root=codex byte-identical; gitlab/gitea ports semantically AND behaviourally identical. Every added comment claim re-measured true, including quotePath under both settings.
  Four non-blocking. **R1 (medium, demonstrated on all four editions) — I am ruling this IN, see the item below**: a single dangling/self-referential symlink or unreadable file inside `kaola-workflow/<project>/` makes the new legacy stage THROW, the `catch` swallows it, and the sink destroys the journal at exit 0 `"merged"` saying nothing — *the exact silent-destruction shape #978 exists to close*, surviving via a trigger the (l) fixture does not model. Outcome is byte-identical to baseline, so it is a residual and not candidate-caused; a constraint-safe repair exists (refuse or report only when the stage throws) and **no green arm would flip**. **R2 (low)**: an in-lane basename containing a backslash now refuses every sink where it previously sank — loud and fail-closed, but (f)-blind; accepted as a deliberate decision, the safe direction. **R3 (low)**: merge-fail after stage strands the only journal copy in an unnamed `kw-wtsync-*` tmpdir — inherited #619(4), disclosed by the implementer. **R4 (low)**: the main-root preflight still exempts backslash names — already on the follow-up list.

  **INCIDENT, disclosed by the reviewer and INDEPENDENTLY VERIFIED BY ME.** Its first live kimi check missed the `HOME`/`KIMI_CODE_HOME` overrides and wrote into the user's REAL `~/.kimi-code`. Its remediation checks out: all 17 shipped scripts in the real home byte-match main HEAD `51db5d2d` (0 differing), `kaola-workflow-sink-merge.js` hashes `0fd34c7b…` = main HEAD and NOT the worktree build `430650f8…`, and both retired hook names are correctly absent. Two of my own probes were wrong along the way and are recorded so nobody repeats them: (i) `find -newermt '-6 hours'` returned 0 **as a false negative** — its positive control failed to detect a file I had just touched, so the real mtimes (19:37–19:39) show the install did rewrite the tree; (ii) comparing the deployed install against the full `scripts/` tree reported 63 "missing" files, which is the wrong oracle — a deployed kimi install ships 17 runtime scripts, not the test/dev set. The installed hook differs from the root source by exactly 7 lines: a 2-line generated header naming `sync-kimi-edition.js` plus one payload adaptation (`p.agent_name` fallback for Kimi's field names) — the legitimate rendered variant, not damage. **The user's kimi installation is intact.**

- item: Dock the documentation the changes make user-visible — `CHANGELOG.md` under `[Unreleased]` **before** the receipt run, `README.md`/`docs/` where a public surface moved, and regenerate routing surfaces if any skeleton in `templates/routing/` was touched.
  status: done
  dispatched: doc-updater subagent on Fable, into the worktree, docs and CHANGELOG only — no production or test file; note at `kaola-workflow/bundle-976-977-978/.cache/docs.md` in main. Inventory I established myself rather than leaving it to be discovered: `[Unreleased]` EXISTS and is live (carries #968), so entries are appended not created; `docs/opencode-edition.md:146-148` states `copy_tree` "removes exactly two things" which is now FALSE (RETIRED_HOOKS is a third); and all three issues carry genuinely user-visible behaviour changes — opencode `--uninstall` now deletes retired commands and hooks, the kimi installer no longer aborts on GNU when a config exists, and the sink now refuses over backslash names and embedded repos where it previously destroyed them silently.
  result: `.cache/docs.md`. Five files, docs/CHANGELOG only. **It found FOUR false claims, not the one I briefed** — `opencode-edition.md` ("exactly two things" → three, naming RETIRED_HOOKS), `kimi-edition.md` (an uninstall sentence already stale at HEAD, predating this bundle), `conventions.md` (a false "three shapes this widening still cannot see"), and `api.md`'s `worktree_dirty` entry (same false residual text). It also verified `copy_skills`' own "exactly two things" claim is **still true** and left it alone — the harder half of that job. **Two same-release consistency repairs inside existing `[Unreleased]` entries, which I approved**: #975's "three shapes are still destroyed silently" now notes #978 closes them, and #973's "verified complete against that edition's full shipping history" gained the clause that #977's census shows it fell short for `install.sh`. Without those the section would have shipped self-contradictory on day one. Boundary I applied: amending a neighbour in the SAME unreleased section is repair; in an already-released section the answer would have been a new entry instead. Checked-clean, stated not implied: `architecture.md`, `workflow-state-contract.md`, `README.md`, `docs/README.md`, api.md's porcelain and sandbox-TMPDIR passages, and the released CHANGELOG sections. **ADR audit answered my question**: no ADR states the old behaviour in the present tense as a now-false live rule. `D-579-01:95` is present-tense about `isParkedLanePath` itself and stays TRUE, because #978 deliberately left the kernel classifier untouched and changed the *caller*; **ADR 0012:84-86** is present-tense normative ("install and uninstall remove previously-installed profiles of the retired role on every runtime") and is the very obligation axis A found `uninstall.sh` violating — the tree had drifted from the stated rule and #977 restored it, so the sentence is more true now than before. Verified: `generate-routing-surfaces --check` exit 0 (18 surfaces), `test-suite-registration.js` exit 0 (549) since api.md was edited, `test-forge-finalize-findings.js` exit 0. **R1 addendum still owed** — the slot is the #978 entry's third paragraph, and the word to qualify is "best-effort", since the landed fix makes a stage failure loud on BOTH routes rather than swallowed.

- item: Run the verification the run is judged by — `node scripts/simulate-workflow-walkthrough.js` at full scope (the fast gate only samples a 1/12 shard), then the chains with `--project bundle-976-977-978`, and hand off to `/kaola-workflow-finalize bundle-976-977-978`.
  status: done
  dispatched: self.
  **Walkthrough DONE at FULL scope**: exit 0, shard **1/1** (not the sampled 1/12), **210 scenarios ran, 210 passed, 0 failed**, spawn-census 2432. Log at `scratchpad/walkthrough-full.log`. Full scope was non-negotiable here — this bundle touched the kernel, six walkthrough entry points, four installers and four sink copies, so a sampled gate would have been close to meaningless.
  **Suite tally I ran myself across the run**: sink-merge 1058 · walkthrough 210/210 · relative-tmpdir-escape 48/0 · run-chains 283 · install-all 254 · kimi-edition 618 · opencode-edition 655 · validation-allowband 17 · validation-runner, install-upgrade-rewrite, install-adaptive-config, suite-registration (549), forge-finalize-findings all exit 0 · routing surfaces 18/18 byte-match · validate-script-sync exit 0.
  **First chain run: RED, one violation.** `spawn-classification: scripts/test-relative-tmpdir-escape.js: 3 unclassified spawn site(s) exceeds the ceiling of 0` at lines 118, 227, 561; `run-chains: 1 chain(s) failed: claude`. Cause is by design — `test-spawn-classification.js` is default-ON, so a file with no row has a ceiling of 0 precisely to guard a NEW suite rather than silently exempt it, and this run added one. **Note the trap that nearly hid this**: the backgrounded command's own exit was 0 while `RUN-CHAINS exit=1` — echoing the real exit code is what caught it, and gating on the wrapper's status would have reported a green receipt over a red chain. Routed to `tests976` (custody). Two constraints given: classify TRUTHFULLY from what each site does at the process boundary, never whichever of the five tokens turns the guard green — a wrong-but-green classification makes the annotation lie to the next reader, which is the guard's entire value; and do NOT add a ceiling row, since the guard states "Raising the ceiling is not a fix" and is forward-only by design. If a site fits none of the five classes it is to report rather than force one, with converting the spawn in-process offered as the alternative the guard itself names.
  **Resolved — 2 classified, 1 CONVERTED, and the conversion is the interesting one.** `:118` (spawning `sync-{opencode,kimi}-edition.js --write`) → `environment`, because it materialises the generated edition trees into the sandbox copy, which is the materialization half of that class word for word. `:227` (`bash -c mktemp`) → `environment`, because the shim-armed positive control is literally a PATH probe proving resolution reaches the shim before any location verdict is trusted. `:561` was **not classified at all** — it was converted to the shared `test-git-fixture` library (`G.init` / `G.commitPaths`, opts passed verbatim so the `childEnv` git-config isolation survives), on the library's own stated grounds that a git commit inside a fixture *"is not a property under test at all — it is ARRANGEMENT"* and annotating arrangement *"with a boundary class they do not have"* is what that library exists to prevent. Forcing a fifth-class token onto it would have been exactly the wrong-but-green annotation I warned against; taking the guard's own alternative route was correct. No ceiling row added, no guard edited. Verified: `test-spawn-classification` exit 0 (669 sites across 67 files, 237 classified), `test-relative-tmpdir-escape` still 48/0, `validate-script-sync` exit 0. **It also propagated the tracked-file correction unprompted** — the "modified two tracked files" overstatement was still baked into the suite header, scenario D's failure message and the shared six-file block comment; all now state the transcript's version, with block byte-identity re-verified across all six.
  **Chains still owed**, and must run LAST: `docs/api.md` is chain-consumed, so the receipt must postdate every doc edit or it lands stale. Run with `--project bundle-976-977-978` and `KAOLA_RUN_CHAINS_CONCURRENCY=serial`. The diff touches three plugin trees, so it is edition-touching and owes the **full four-chain** receipt, not the claude chain alone.
  **RECEIPT GREEN — all four chains.** `.cache/chain-receipt.json`, `completedAt 2026-08-14T13:04:02Z`. Read by field rather than by the wrapper's exit status: `claude` exitCode 0 (37 steps), `codex` 0 (2), `gitlab` 0 (3), `gitea` 0 (3). `scope.decision: "all-four"`, `reason: "edition_coupling"`, 20 touched edition paths — the diff-scoping failed closed to all four exactly as it should. `codeTreeHash 3248d6d4…`. `headSha` binds `51db5d2d`, the pre-implementation commit, which is normal for a pre-commit receipt in this repo and not a broken binding. **The receipt postdates every doc edit**, which was the sequencing constraint — `docs/api.md` is chain-consumed and an earlier receipt would have landed stale. **Freshness re-verified AFTER the roadmap sources were added**, rather than assumed: `computeCodeTreeHash` over the current tree returns `3248d6d4…`, byte-equal to the receipt's — the `.roadmap/*.md` additions are validation-invisible, so no re-run is owed. **Item closed.** **User rulings 2026-08-14**: file all five follow-ups; **NO release** — land the bundle and leave `[Unreleased]` accumulating, since a release is a separate deliberate act needing its own tag-time four-chain receipt bound by exact `headSha`.
  result: Walkthrough exit 0 at FULL scope, shard 1/1, **210/210 scenarios**, spawn-census 2432. Four-chain receipt GREEN — claude 0 (37 steps), codex 0 (2), gitlab 0 (3), gitea 0 (3), `scope.decision: "all-four"` / `reason: "edition_coupling"` over 20 touched edition paths. First receipt came back RED on 3 unclassified spawn sites in the new suite; resolved by classifying two as `environment` and CONVERTING the third to the shared git-fixture library, with no ceiling raised. Freshness re-verified after the roadmap sources landed. Finalize transaction: archived, `closure_invariants.ok: true`, `validation: chains_green`.
