# Finish #940–#944 — the five audit findings left over from #935 — adding no mechanism an observed failure does not demand

- item: Verify #940's premise against the current tree — that `--enforce-floor` / `isReasoningClass` have zero production consumers, and that the dispatch-log hook calls the resolver without the flag and is fail-open; claims sit at `scripts/kaola-workflow-resolve-agent-model.js:46,246,432` and `plugins/*/hooks/kaola-workflow-subagent-dispatch-log.sh`
  status: in-flight
  dispatched: read-only `investigator` subagent; report to `kaola-workflow/bundle-940-941-942-943-944/premise-940.md`
  result: `premise-940.md`. Consequence CONFIRMED and stronger; the three line numbers are +2 off at HEAD (54cbe8d3 added two comments). Hook copies are FOUR, not three — the `plugins/*/hooks/` glob misses root `hooks/`; all four tracked and all four installed are byte-identical, so what ships is what was authored. Live run: `synthesizer` lowered to sonnet logs `model_planned:"sonnet"` and exits 0; positive control with `--enforce-floor` exits 1 with `reasoning_floor_violation`. **Two things the issue does not say**: (a) the obvious one-line wiring fix is a measured NO-OP — splicing `--enforce-floor` into the hook still exits 0 because `2>/dev/null || printf ''` eats the resolver's exit 1, so biting requires changing the hook's fail-open construction, and it is SubagentStart (fires AT dispatch, not before); (b) the production consumer was DELETED, not never-built — `kaola-workflow-next-action.js` required and called it at `c0b48043^`, and `c0b48043` (the ADR-0017 mission-list rewrite) removed it with 9 DAG scripts. This is DAG-removal residue. Removal drops the floor cluster in 4 resolver copies (`validate-script-sync.js:159-164` forces them together), ~80 lines of `test-agent-model-resolver.js`, 5 validator `assertIncludes` pins, `test-agent-profile-parity.js:51-52`, and prose in `agents/synthesizer.md:14`, three `synthesizer.toml:18`, `docs/conventions.md:208`. `DEFAULT_AGENT_MODELS['synthesizer']='opus'` survives untouched — removing the floor lowers nobody's tier.

- item: Verify #941's premise — that `sync-opencode-edition.js:870` prints an unconditional `--write` footer on any `--check` failure while `:661-666` makes `--write` a no-op for an existing `opencode.json`, so only `--write-config` clears the stale-config class
  status: in-flight
  dispatched: read-only `investigator` subagent; report to `kaola-workflow/bundle-940-941-942-943-944/premise-941.md`
  result: `premise-941.md`. All three claims CONFIRMED (footer `:870`, `writeConfig` `:660-668`, reason `:865`), reproduced end-to-end. **Wider than filed**: of 14 mismatch classes `--write` is correct for 12; C14 needs `--write-config`, and C9 (unregistered plugin, `:838-841`) needs a source edit to `PLUGIN_SCRIPTS` `:88-90` — no flag clears it. `--write-config` is `runWrite(true)` (`:907`) and a strict superset, but is NOT a safe blanket footer: it destroys a user's documented `opencode.json` model pin. Nothing pins the footer wording, so no test would have caught it. The kimi twin's identical footer (`sync-kimi-edition.js:780`) is CORRECT — 10 classes, no user-owned config, no `--write-config` — do not change it in sympathy.

- item: Verify #942's premise — that `scripts/test-opencode-edition.js` materializes `.opencode-gitlab` / `.opencode-gitea` during the run so a first run drift-checks fewer trees than a second; reproduce by removing both trees and running twice
  status: in-flight
  dispatched: read-only `investigator` subagent; report to `kaola-workflow/bundle-940-941-942-943-944/premise-942.md`
  result: `premise-942.md`. Materialization CONFIRMED (FA3 `:1739`, again via FA9 `:1814`). **Headline REFUTED**: the banner does not read as coverage either way — run 1 prints `NO tree verified; 3 ABSENT, not checked (...)` plus three loud per-tree SKIPPED lines, versus run 2's `3 tree(s) in parity (...)`; the file's own comment at `:41-64` states that exact intent. Run 1 verifies **zero** trees, not one. Reordering the check after materialization was measured to make it **vacuous** — a tree drifting one command earlier passed post-write — and would destroy the only position from which drift is observable. Every consumer is single-run; no CI exists.

- item: Verify #943's premise — that `EXPECTED_ROLE_MODELS` in `scripts/test-install-model-rendering.js` (~`:3031`) holds 13 of the 14 `DEFAULT_AGENT_MODELS` roles and that `investigator` appears nowhere in the file
  status: in-flight
  dispatched: read-only `investigator` subagent; report to `kaola-workflow/bundle-940-941-942-943-944/premise-943.md`
  result: `premise-943.md`. All five claims CONFIRMED; `investigator` is the ONLY missing key, values for the other 13 all agree, and the table is a strict subset. **Completeness assertion is ABSENT, not weak**: the loop at `:3060` iterates `Object.entries(EXPECTED_ROLE_MODELS)` — the direction that structurally cannot notice a missing key — and the file never `require`s the resolver, so the comparison is impossible as written. Mutation-proven: leg C (a coherent `investigator` re-tier across 16 files) passes an unwaived **four-chain green**; leg D (the identical re-tier of the pinned `code-explorer`) reds. The sibling `test-agent-model-resolver.js:27-31` already carries exactly the missing shape as a `deepStrictEqual` over the Codex class table — the pattern to reuse, not invent. Out-of-scope find: `{INVESTIGATOR_MODEL}` is a dead placeholder registered at `install.sh:544,576` with zero consuming templates.

- item: Verify #944's premise — that no installed Codex surface carries role→tier membership; installed roles are `~/.codex/agents/kaola-workflow/*.toml` and the skill renders from `templates/routing/next.skeleton.md:6-13`; `synthesizer` is the working positive control and the only role→tier enumeration is the uninstalled `README.md:143-158`
  status: in-flight
  dispatched: read-only `investigator` subagent; report to `kaola-workflow/bundle-940-941-942-943-944/premise-944.md`
  result: `premise-944.md`. **Headline HOLDS**; claim 5 REFUTED and claim 2 qualified. The membership is NOT confined to an uninstalled README — `CODEX_PINNED_*` ships in THREE installed Codex cache scripts, is current (already post-#935), and `kaola-workflow-resolve-agent-model.js --raw` is a runnable role→tier oracle on this box. Nothing renders, prints or points a prompt at any of it. Constraint on the remedy space: the installed Codex validators **forbid** `model=` and `model_reasoning_effort=` in role TOMLs (`install-codex-agent-profiles.js:736-737`), so emitting a tier as those keys would be rejected by the installer's own check. The PIN also ships on the finalize skill, not just next. Codex-specific: no runtime's installed role definition carries a tier (Claude's `opus`/`sonnet` is stripped by `install.sh:261`, opencode's removed at `162135a8`, kimi never had one) — Codex is simply the only runtime whose prompt asks the question. Two resync facts, not defects: the Codex plugin cache is stale at v9.5.5/`660fec1d` so its `DEFAULT_AGENT_MODELS` still says sonnet for the two #935 roles; and a Codex spawn's `model_planned` resolves through the **opencode** tree's copy, so on a Codex-only box it is silently empty. Also flagged out of scope: `kaola-workflow-next/SKILL.md:251-253` cross-references a "Codex Profile Freshness Gate" section that does not exist.

- item: Fix #941 — the `--check` remediation footer must name the remedy that actually clears the classes it reported, across all 14 (12 → `--write`, C14 → `--write-config`, C9 → a `PLUGIN_SCRIPTS` source edit), never blanket-advising `--write-config` because it destroys a user's model pin
  status: in-flight
  dispatched: `tdd-guide` authors the test first (custody), proving it fails on baseline d2ab06c2; test lands in `scripts/test-opencode-edition.js`, notes to `kaola-workflow/bundle-940-941-942-943-944/test-941.md`. `implementer` follows once the test exists.
  status-note: DONE — both legs closed.
  result (impl leg): `impl-941.md`. `scripts/sync-opencode-edition.js` only, +58 −14. All 14 `mismatches.push` sites now carry a `remedy` (`WRITE` ×12, `WRITE_CONFIG` for C14, `SOURCE_EDIT` for C9) and `remediationLines()` derives the closing lines from the remedies actually present — nothing re-reads reason strings or path prefixes, so an unassigned remedy falls through to "no command offered", the safe direction. Suite: exit 1 (`7 failure(s), 550 passed`, all 7 A30's) → exit 0 (555). Kimi untouched, 516 both sides. Mixtures verified incl. C9+agent → `--write` (not `--write-config`). Orchestrator ruling on its flagged judgment call: **KEEP** the added line warning that `--write-config` discards model pins — a user reaches C14 by using the documented pin feature, so omitting it repeats the issue's own harm one level down.
  result (test leg): `test-941.md`. Band A30 at `scripts/test-opencode-edition.js:2536-2816`, mutation-proven, plus pin K12 in `test-kimi-edition.js` that reds if kimi's correct footer is changed in sympathy. The band is an OUTCOME check — it asserts the advised command actually clears what was reported — not a prose pin. Author found and repaired a crash in its own fixture (`fixed_fixture_missing_tree` threw ENOENT out of `cpSync`, taking the suite summary with it). Reference `FOOTER_FIXED` in the scratchpad is a satisfiability proof, NOT a design. Implementation leg now dispatched: `implementer` subagent in the worktree, `scripts/sync-opencode-edition.js` only, notes to `impl-941.md`.

- item: #942 needs no code change — the banner already names what it skipped and the reorder was measured vacuous, so the remaining act is closing the issue on the measurement; carry the refutation into the user decision batch and the CHANGELOG rather than building anything
  status: todo

- item: Fix #943 by pinning `investigator`'s rendered tier in `EXPECTED_ROLE_MODELS` — test custody means `tdd-guide` authors this, and the pin must be derived independently from `DEFAULT_AGENT_MODELS`, not copied from it; also close the class by adding the completeness assertion the file lacks, reusing the `deepStrictEqual` shape already at `test-agent-model-resolver.js:27-31`
  status: in-flight
  dispatched: `tdd-guide` subagent in the worktree, test-only change to `scripts/test-install-model-rendering.js`; notes to `kaola-workflow/bundle-940-941-942-943-944/test-943.md`
  result: `test-943.md`. One file, +23/−0. `investigator: 'sonnet'` pinned at `:3046`, derived from a fresh sandboxed install (that same sweep reproduced all 13 existing pins exactly) and corroborated by `agents/investigator.md:5` and `README.md:146` — not copied from the resolver map. Completeness assertion at `:3063-3067` reuses the `deepStrictEqual` shape from `test-agent-model-resolver.js:27-31` and compares **keys only**, so values remain independently pinned and the table's premise survives. Armed: leg C reds WITH the change (`fresh install must resolve investigator -> sonnet; got opus`) and was green WITHOUT it; a deleted key and a bogus key each red the completeness check. Coherence control: `test-agent-model-resolver.js` stays exit 0 under leg C, so the new pin is the only objector. Verified green both before AND after the concurrent floor removal landed in the resolver.

- item: Decide #940 with the user — the floor is enforced for no role, so the call is wire it into a shipping dispatch path or delete machinery no observed failure demands; deleting working capability is escalation-worthy, so put evidence and a recommendation in front of the user and wait
  status: done
  dispatched: self, in conversation, with `premise-940.md` as the evidence
  result: **User ruled: REMOVE the machinery.** Delete the floor cluster; tests fall out with the mechanism, never repaired ahead of it.

- item: Execute the #940 removal — drop `REASONING_FLOOR_ROLES` / `isReasoningClass` / `enforceReasoningFloor` / `--enforce-floor` from the 4 byte-identical resolver copies together (`validate-script-sync.js:159-164` forces that), the ~80 lines of `test-agent-model-resolver.js` that test only it, the 5 validator `assertIncludes` pins, `test-agent-profile-parity.js:51-52`, and the floor prose in `agents/synthesizer.md:14`, three `synthesizer.toml:18`, `docs/conventions.md:208`; `DEFAULT_AGENT_MODELS['synthesizer']='opus'` must survive untouched
  status: in-flight
  status: done
  dispatched: `implementer` subagent in the worktree; notes to `kaola-workflow/bundle-940-941-942-943-944/impl-940.md`
  result: `impl-940.md`. 19 files, **+48 / −494** (net −446). All four resolver copies byte-identical after (`46fcbd37…`), produced by `cp` so identity is by construction. Tests fell out with the mechanism (−81 resolver test, −2 parity pin), and each deleted floor block was first checked for a unique non-floor assertion — none had one, table in the notes. `synthesizer` → `opus` verified four ways plus a whole-map before/after proving **no role was re-tiered**. Retired flag now fails honestly: `--enforce-floor` → `unexpected argument`, exit 2. Walkthrough 209/209 at FULL scope (shard line quoted), not the 1/12 sample. Three things the premise missed: (1) `description` is a machine-bound THIRD copy — `codex-preflight.js:1685-1686` refuses when `synthesizer.toml:2` and `config/agents.toml:62` diverge, so editing one alone would have shipped a preflight failure; (2) `prose-census-baseline.json:401` carries `reasoning_floor_violation` and must NOT be updated — it is the frozen "before" of the refusal census, and rewriting it destroys the measurement; (3) five untracked generated edition files still carry the stale sentence, deliberately not regenerated mid-edit. Two non-green results each disproven by control: `test-route-reachability.js` (all 6 are test-944's unimplemented T19b — identical counts with impl-940's files reverted) and `plugins/kaola-workflow/scripts/validate-workflow-contracts.js` (pre-existing; a byte-copy with no `commands/` dir — HEAD's version fails identically).

- item: Decide #944 with the user — the tier→effort mapping ships to Codex but role→tier membership does not, so the call is emit a tier into each generated role TOML, name the roster in the skeleton prose, or rule the split was never meant to be machine-carried; same escalation, same wait
  status: done
  dispatched: self, in conversation, with `premise-944.md` as the evidence
  result: **User ruled: GENERATE the roster into the skeleton** from `CODEX_PINNED_STANDARD_ROLES` / `CODEX_PINNED_REASONING_ROLES` at render time — checked by `generate-routing-surfaces.js --check`, cannot drift, and gives a documented dead export its first shipping consumer. The TOML-emission option is foreclosed by the installer's own validator.

- item: Author the acceptance test for #944's generated roster — it must pin that the rendered Codex surface carries the full 14-role two-tier membership and that it equals `CODEX_PINNED_*`, so a role added to the registry cannot silently miss the roster
  status: in-flight
  dispatched: `tdd-guide` subagent in the worktree; notes to `kaola-workflow/bundle-940-941-942-943-944/test-944.md`. `implementer` follows once the test exists.
  result: `test-944.md`. Block T19b in `scripts/test-route-reachability.js`, +232/−0, exit 1 with `6 failure(s), 325 passed`; control on HEAD's suite exits 0, so all six reds are the new block and none depends on the concurrent floor removal. Runs in `test:kaola-workflow:claude` at FULL coverage (not the sampled shard) and in `claude:full`; not in codex/gitlab/gitea — measured. The surface universe is **derived** — every generated surface whose committed bytes carry the routing PIN owes the roster, asserted = 6 (both `next` and `finalize` skills), so a 7th surface inherits the obligation with no edit. Membership pinned both directions against the kernel `CODEX_PINNED_*`; format deliberately NOT pinned. Tier→effort bound to preflight's `CODEX_STANDARD_EFFORT`/`CODEX_REASONING_EFFORT` rather than literals. Honesty flag from the author: the effort assertions PASS on baseline (retention pins, proved by mutation only). M1 — adding a 15th role to the constants without regenerating reds all six — is the mutation that justifies generation over prose. Conflict check by orchestrator: this file was ALSO edited by `impl-940`; verified both landed (T19b present, floor cluster gone).

- item: Implement #944 — render the 14-role two-tier roster into the routing PIN block from `CODEX_PINNED_STANDARD_ROLES` / `CODEX_PINNED_REASONING_ROLES`, editing the skeletons and regenerating; hand-editing a rendered surface cannot satisfy T19b
  status: in-flight
  dispatched: `implementer` subagent in the worktree; notes to `kaola-workflow/bundle-940-941-942-943-944/impl-944.md`
  result: roster renders via a new `<!-- SLOT:codex-tier-roster -->` inside the existing PIN block — the idiomatic slot mechanism, no new machinery. T19b green: `test-route-reachability.js` exit 0 at 331 assertions (was exit 1 / 6 fails / 325). **Orchestrator verified the shipped bytes independently**: all 6 rendered surfaces carry the PIN, and `plugins/kaola-workflow/skills/kaola-workflow-next/SKILL.md` now names all 14 roles in two tier lists, with `build-error-resolver` and `adversarial-verifier` on the reasoning side per #935. Orchestrator ruling on the implementer's offer to reshape the roster as literal data in `slots.js`: **REFUSED** — that hand-maintains the 14-role duplicate the user's ruling rejected and would defeat T19b's M1 mutation. One consequence routed onward: the design adds a fourth render input, staling a hand-typed sandbox copy list (see the next item).

- item: Repair the stale sandbox copy list at `scripts/test-generate-routing-surfaces.js:622-626` — the roster design adds a fourth render input (`slots.js` now requires the kernel schema) and the hand-typed list omits it, so the sandboxed `--check` dies on a missing module and collapses 16 assertions; the one-line add is the whole fix, and deriving the copy set is deliberately declined because this failure is LOUD and names the missing module
  status: in-flight
  status: done
  dispatched: `test-944` (the test author, by custody) via message; edits `scripts/test-generate-routing-surfaces.js` only, appends to `test-944.md`
  result: one-line copy add as ruled, +14/−1 (the line, four explanatory comment lines, one message-only change). 432 assertions exit 0; `--check` 18/18 exit 0; T19b still 331 exit 0. P0 proves the line load-bearing. **The author refuted half my rationale and was right**: I claimed a stale list "names the missing module" — measured, `grep -c 'adaptive-schema'` over the failing output returns **0**, because the real error lived only in unprinted child stderr. It kept exactly one thing from its reverted derived version — carrying the spawned stderr into the baseline assertion's message — on the ground that my own argument assumes it. Orchestrator: **kept**; the conclusion (one line, not a require-graph walker) stands and now its premise is true. Also re-proved against the REAL implementation: a 15th role added to the constants without regenerating reds T19b on all six surfaces, and `--write` then puts it in the shipped bytes.

- item: Adversarially verify whatever actually got implemented, reading what ships rather than what was authored, and mutation-prove every guard touched — a green suite is not proof a guard is armed
  status: in-flight
  status: done
  dispatched: `adversarial-verifier` subagent over the full branch diff, fresh context (this session did the work, so a self-review would be no gate); report to `kaola-workflow/bundle-940-941-942-943-944/verify.md`
  result: `verify.md`. **All four claims UPHELD**; nothing refuted. It attacked the named lines and several unnamed ones: tried and failed to construct a mismatch set #941 advises wrongly (all 7 mixtures correct), proved #944's roster genuinely DERIVED rather than a literal that agrees, confirmed #943's completeness assertion armed both directions with no cycle or inherited offline switch, and confirmed no cross-agent collision (`test-route-reachability.js` is +232/−0 — only T19b landed, and #940 owed that file nothing; `docs/conventions.md` carries both agents' edits intact). It also ran `test-claim-hardening` (766, exit 0) precisely BECAUSE it is full-tier-only and never mandated, and #944 modified a skeleton it reads. Four findings, none blocking: **940-L1** acted on — added a supersession banner to `docs/decisions/D-687-01.md` point 6, matching the file's two existing retroactive banners; **941-L1** acted on — added an ADR 0017 watch-list row (a mismatch class pushed with no `remedy` falls out of both branches and prints no advice; probe-measured, no producer today, so recorded not built); **941-L2** and **944-R1** informational, no action.

- item: Finalization — acceptance, docs, summary, gap sweep, closure, transaction, sink
  status: in-flight
  dispatched: self, running the finalize skill; `doc-updater` subagent out to `.cache/doc-updater.md`; gap sweep recorded via `.cache/run-gaps-manual.md` and re-scanned (5 classes swept). **User ruled at the closure decision**: file four follow-ups, leave the watch-list item as an ADR row only. Filed **#945** (7 vacuous sandbox assertions), **#946** (dead `{INVESTIGATOR_MODEL}` placeholder), **#947** (dangling "Codex Profile Freshness Gate" cross-reference), **#948** (A30 `{WRITE + SOURCE_EDIT}` coverage). Remaining: doc-docking, `finalization-summary.md`, the implementation commit (the transaction will NOT author it), the finalize transaction, then the merge sink closing all five of #940–#944.

- item: Raise the vacuity weakness `test-944` measured but deliberately did not build against — with a dead sandbox, 7 assertions in `test-generate-routing-surfaces.js` PASS vacuously because a sandbox that cannot start exits 1 for every invocation, indistinguishable from detected drift (mutation P2: strip `cmdCheck`'s `process.exit(1)` while leaving DRIFT printing intact → exactly those 7 red); out of this bundle's scope, so record and ask the user before filing
  status: todo

- item: Decide what to do with `install-opencode.sh:156-160`, flagged by impl-941 as out of its lane — it implements the old advice in shell (`--check … || --write`, output discarded) so on a C14/C9 tree it composes exit 1 `||` exit 0 and proceeds from a tree it failed to repair; assess whether that is actually a defect before acting, since preserving a user-owned `opencode.json` and proceeding may be the deliberate design, and changing it would make installs fail where they now succeed
  status: done
  dispatched: self, inline read of `install-opencode.sh` and `sync-opencode-edition.js:76`
  result: **REFUTED — no change.** `OPENCODE_JSON` is `path.join(REPO, 'opencode.json')`, the repo ROOT file, not part of `SOURCE_TREE="$SCRIPT_DIR/.opencode$FORGE_SUFFIX"` that the installer copies from; `--write` does refresh that tree, so the compound achieves its stated goal ("ensure the in-repo generated tree is fresh before copying from it"). Preserve-and-proceed is deliberate and separately reported: `--adopt-config` opt-in (`:73,:90`), the CONFIG DRIFT banner (`:94`), a drift reporter (`:430`), a staleness reporter (`:449`), and the `Preserved …opencode.json` notice (`:396`). Making it fail-closed would break designed behaviour. Silence is the answer here.

- item: Regenerate every edition tree LAST, after `impl-944` lands, since the routing surfaces feed them — covers the stale `.opencode-gitlab` / `.opencode-gitea` trees (3 write-clearable mismatches each, gitignored artifacts, not a repo defect) and the five untracked generated files impl-940 flagged as still carrying the removed floor sentence (`.opencode-{gitea,gitlab}/agent/synthesizer.md:13`, `.kimi{,-gitea,-gitlab}/skills/kaola-role-synthesizer/SKILL.md:13`); `.opencode/agent/synthesizer.md:13` already carries the new wording, which is the evidence regeneration fixes them
  status: done
  dispatched: self, in the worktree, after `impl-944` landed
  result: regenerated all six trees — opencode github/gitlab/gitea (0/1/1 files updated) and kimi github/gitlab/gitea (1/1/1). Verified: zero tracked remnants of `non-lowerable` / `reasoning-tier floor` outside the immutable historical records, and all six generated `synthesizer` surfaces now report 0. `opencode.json` correctly preserved as user-owned on every run.

- item: Dock the docs (`CHANGELOG.md`, `README.md`, `docs/` where structure moved); write CHANGELOG before the receipt run
  status: done
  dispatched: self, in the worktree
  result: `CHANGELOG.md` `[Unreleased]` gains an `### Added` (#944 roster), an `### Fixed` (#941 footer, #943 pin) and a `### Removed` entry (#940 floor) — written BEFORE the receipt run, per the recorded staleness rule. `docs/conventions.md` gains two facts the run proved: the render-inputs sentence now names the kernel as a render input (the omission that staled the sandbox copy list), and the role-roster table gains a row for the six generated Codex SKILL surfaces. `#942` gets no entry — no user-visible change; its refutation goes in the issue comment at closure. Verified `README.md` and `docs/api.md` carry NO floor references, so nothing further is owed there.

- item: Run the chains and hand to `/kaola-workflow-finalize bundle-940-941-942-943-944` — chain selection is the producer's, and this diff touches editions so it fails closed to all four; `KAOLA_RUN_CHAINS_CONCURRENCY=serial` and always pass `--project`
  status: in-flight
  dispatched: self; receipt to `kaola-workflow/bundle-940-941-942-943-944/.cache/chain-receipt.json`, then the finalize skill
  result (chains): **all four GREEN, unwaived** — claude 376s/35 steps, codex 8s, gitlab 93s, gitea 93s, plus 11 preamble steps all zero. Scope `all-four` by `edition_coupling`, `changedFileCount: 37`, so the chains genuinely saw this diff. First invocation was discarded: I piped to `tail` and read `${PIPESTATUS[0]}`, which is a **bashism** — in zsh it silently fell through to `tail`'s status. Re-run without a pipe, exit read directly. Coverage note: the `claude` chain samples the walkthrough at a rotating 1/12 shard, but `impl-940` and `verify` each ran it independently at FULL scope (209/209), so the sampling gap is covered.
