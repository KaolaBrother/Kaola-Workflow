# Close #952–#955, the ponytail-review adoption set: subtraction audit (report-only), solution ladder to every code-role carrier, three ADR-0017 watch-list rows, one per-runtime capability table

Run facts: branch `workflow/bundle-952-953-954-955`; worktree
`/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-952-953-954-955` (all edits land there,
not in the main tree); active folder lives in the main tree. Reviewer subagents run on the **fable**
model at the user's direction. Every filed issue in this set demands a premise pass before any build —
the last four bundles had premises refuted or corrected in nearly every case.

- item: Premise pass #952 — re-measure the two figures the issue rests on against the current tree: the "24% byte-identical duplication" across scripts/ and the "84-file scripts/ count", both dating from before recent closes. Report the measured numbers, the method, and whether the audit's framing survives.
  status: done
  dispatched: self (orchestrator, inline). FIRST DISPATCH to an investigator subagent DIED at 14:58:51 on the same connection fault that killed the #954 and #955 passes, zero output written. Three of four subagents died inside 90s, so this premise — being pure bash measurement — is taken inline rather than re-dispatched into a failing transport. Measurements land inline in this file's result and in reports/premise-952.md.
  result: reports/premise-952.md. Count CORRECTED: scripts/ is 82 files (81 js + 1 json), not 84 — and the real surface is 167 js across scripts/ + plugins/{kaola-workflow,-gitlab,-gitea}/scripts/, so a scripts/-only audit sees 48% of it. The 24% figure SURVIVES (23.3% of bytes, 26.3% of files, over a capture proven complete at 167=167) but its FRAMING IS REFUTED: 23.2 of those 23.3 points are the deliberate four-edition port structure; non-port redundancy is 3,693 bytes = 0.04%, and that one group is itself a rename-normalized forge port. No duplicate file lives twice in one tree. So duplication is NOT a finding class, and "net deletable lines" needs a stated x3/x4 multiplier rule for ported files or the ranking is decided by portedness rather than by merit. docs/ = 198 files / 34,359 lines; docs/audits/ exists with one precedent file. Open item carried into the audit brief: which docs are test-consumed (api.md known; others unestablished). METHOD NOTE: the first capture died partway and would have reported 0.2% — plausible, clean, and wrong in the direction that kills the audit.

- item: Premise pass #953 — enumerate the real carrier surfaces for the implementer / code-architect / planner roles across all four runtimes (canonical `agents/*.md`, the codex `agents/*.toml` propagation surface, whatever the opencode/kimi syncs render), by grepping the role names, never assuming; record which carriers are byte-renders and which are transformed. Also quote implementer's current minimalism guidance (filed as zero), planner's one incidental hit, and code-architect's thin two-line wording.
  status: done
  dispatched: investigator subagent, read-only, working in the worktree; output to land at kaola-workflow/bundle-952-953-954-955/reports/premise-953.md (main tree)
  result: reports/premise-953.md (complete, 425 lines, mutation-proven on a scratch mirror). implementer "zero minimalism guidance" CONFIRMED for SOLUTION minimalism but the file does carry scope restraint twice — and carries an unopposed generality push ("correct for every valid input"). planner "one hit, incidental" REFUTED — two direct hits, one with its own H2 `## Sizing and Phasing`. code-architect "thin" CONFIRMED (2 lines) but "SECOND wording" undercounts: FIVE live wordings exist (CLAUDE.md:159, init.skeleton.md:127, code-architect:40-41, build-error-resolver, planner). THE FINDING THE ISSUE DOES NOT NAME: code-architect's two lines never reached the Codex carrier at all — 0 hits in all three `.toml` and both installed codex locations; that `.toml` is an independent rewrite that drops `## Process` entirely. So the load-bearing defect is a MIRRORING gap, not an authoring gap, and adding a wording without a pin reproduces it at 4x scale. CARRIERS: 45 live files, 15 per role. Authoring surface = canonical `agents/<role>.md`, then HAND-mirror to 3 codex `.toml` (no generator owns them); opencode+kimi follow automatically and byte-identically (the body transform is the identity function for these three roles — no anchor to miss). GUARD: test-agent-profile-parity.js is BLIND at this scope — threshold sweep flips red only at 8/11 roles, and #953's scope is 3; a rule under MIN_RULE_CHARS=48 is invisible at any distribution. Three ROLE_PINS entries arm it (measured: red without the prose, 9 toml failures with it).

- item: Premise pass #955 — verify the shipped state of the four motivating divergence facts before any cell is written; the issue itself flags that the #944 role→tier carrier gap has since been closed by the tier-roster work. Report, per row (dispatch carrier, command/skill surface, hooks, model & tier handling, install path), what is true today across the four runtimes and the authoritative file that holds each fact.
  status: done
  dispatched: investigator subagent, read-only, working in the worktree; output to land at kaola-workflow/bundle-952-953-954-955/reports/premise-955.md (main tree). FIRST DISPATCH DIED at 14:57:48 on a connection fault; re-dispatched, and the second also stopped mid-report. PARTIAL RESULT IN HAND (reports/premise-955.md, 85 lines): rows 1 and 4 settled, rows 2/3/5 outstanding. THE FLAGGED STALENESS IS CONFIRMED STALE — #944's "codex has no installed role→tier carrier" is CLOSED at 483a5e5e: all six codex SKILL surfaces open with a `codex-dispatch-model-routing` PIN carrying the full roster, rendered from adaptive-schema.js:46-63 via templates/routing/slots.js, guarded by test-route-reachability T19b. Codex agent TOMLs carry NO model/tier key, so a cell pointing at `plugins/*/agents/*.toml` for codex tier would be WRONG. Also settled: kimi CANNOT dispatch a named custom subagent at all (built-ins only; the 14 roles ship as Skills, dispatch cards rewritten to coder/explore) — an absence cell. TERMINOLOGY TRAP the table must respect: four RUNTIMES != four EDITIONS; routing surfaces render over SIX edition trees because the forge axis multiplies claude and codex but not opencode/kimi. Weakest pointer target found so far: claude's dispatch carrier has no prose file that states the fact — the `agents/` directory IS the fact. Re-dispatched narrowly for rows 2, 3 and 5.
  result: reports/premise-955.md, COMPLETE (rows 1-5 across all four runtimes, plus doc-placement recommendation and per-edition-doc findings). Every cell's shipped truth and authoritative pointer established; five cells flagged as having NO single authoritative file, which the implementation then had to solve honestly rather than paper over. Its placement recommendation (a subsection of the existing docs/architecture.md § Editions and runtimes, NOT a new doc, because a new doc would make per-runtime divergence live in two places) was adopted, along with the obligation it attached: repoint the § Model resolution restatement or ship a contradiction one screen from the table forbidding it.

- item: Premise pass #954 — read the ADR 0017 watch-list table and its exact three-column format, and check whether each of the three proposed rows has a real arming observation (row 3 must stand on its own observation, not on symmetry with the editions that have a scan). Confirm the external implementation (DietrichGebert/ponytail) pointers the rows are to carry inline.
  status: done
  dispatched: investigator subagent, read-only, working in the worktree; output to land at kaola-workflow/bundle-952-953-954-955/reports/premise-954.md (main tree). FIRST DISPATCH DIED at 14:57:48 on a connection fault with zero output written (reports/ was empty); re-dispatched identically — a transient fault, not a bad task. SECOND DISPATCH also stopped mid-report, but section 1 landed complete and that is the section that matters most. PARTIAL RESULT IN HAND (reports/premise-954.md, 168 lines): the watch-list table is at docs/decisions/0017-the-mission-list.md:121-181, columns `| failure class | observation that would arm it | mechanism already sized |`, separator `|---|---|---|` with NO padding, one physical line per row, cells hold PROSE with inline locators/SHAs/counts (not bare pointers), and rows have grown to 2160 chars so a long row is native. The ADR's intro imposes a format obligation: "Every row therefore carries its own recovery information inline" — so a bare external URL is weaker than what every existing row does. DECISIVE: ADR lines 149-156 already contain the symmetry refusal in the ADR's own prose, and EXISTING ROW 7 is the additive-edition transform row — which is very close to proposed row 3. Remaining: sections 2 (arming), 3 (ponytail pointer material), 4 (CHANGELOG — already settled inline: no [Unreleased] section exists, top is [9.6.0]). Re-dispatched narrowly for section 3; section 2 is a judgement the orchestrator keeps.
  result: reports/premise-954.md, COMPLETE (76KB across the two passes). Verdicts: row 1 ARMED but requiring rewording, row 2 ARMED only if #524 is named inline, row 3 ALREADY-OBSERVED-SO-NOT-A-WATCH-ROW. AND THE SIZING PREMISE ITSELF WAS REFUTED: NO local artifact describing the external implementation exists anywhere — not in docs/, not in the ~390 archived run folders, not in the git log — so the issue's "sizing carried inline as pointers to the reviewed external implementation" was not implementable as written. Rather than invent figures or fetch the network, both surviving rows are sized against internal evidence, which the ADR's own intro demands anyway ("Every row therefore carries its own recovery information inline") and which is strictly stronger than an external URL.

- item: #954 — add the three watch-list rows to `docs/decisions/0017-the-mission-list.md` in its existing format, sizing carried inline as pointers to the external implementation. Docs-only: the diff touches the ADR and CHANGELOG only, no mechanism, no guard.
  status: done
  dispatched: self (orchestrator, inline) — the rows are a judgement about what may enter the design of record, so authoring them is deciding, not production. Landed in the worktree at docs/decisions/0017-the-mission-list.md:148-149.
  result: TWO rows landed, not three, and the sizing is INTERNAL rather than external. (1) Row 1 (subagent rule-carrier gap) ARMED but REWORDED: the carrier already exists and already runs on every dispatch — hooks/hooks.json:18-31 registers the dispatch-log hook on a `*` SubagentStart matcher, fail-open at four exits — so what is absent is an emitted PAYLOAD, not a hook. Sized internally, which is strictly stronger than the external pointer the issue asked for. (2) Row 2 (prose rule not followed) ARMED only once #524 is named inline: CHANGELOG.md:2710 records a live instance, so a row claiming the class is unobserved is refutable on its face; the row now names it, notes it predates this design and sits on a retired role, and concedes the partial measurement at docs/conventions.md:846-856 which found the failures were the BRIEF being wrong, not the agent disobeying. (3) Row 3 REFUSED — verdict ALREADY-OBSERVED-SO-NOT-A-WATCH-ROW. It is not symmetry-only; it has a real arming observation, and that is exactly what disqualifies it: the failure SHIPPED (a canonical rename moved a heading out from under a plain `if`; nothing threw, the surface shipped without the paragraph) and a four-layer mechanism was built for it in #949 — anchor, near-miss `else`, throw, and suite anchors. The watch list is defined at ADR line 123 as failure classes "never observed in this methodology", so writing row 3 would place a false claim in the design of record. Structural check: both new rows carry exactly 4 unescaped pipes like all 12 existing rows, at 1142/1151 chars against a native max of 2156. BONUS FINDING routed to #952: the Codex-note strip at sync-opencode-edition.js:494 / sync-kimi-edition.js:470 is dead — matches nothing, zero references in either edition suite.

- item: #953 — author the solution ladder once (speculative brief parts are findings not builds; reuse-in-repo first; stdlib; already-installed dependency, new deps escalate; then minimum working code; no single-implementation abstractions; deliberate corner-cuts marked with ceiling + revisit trigger) and render it to every carrier the premise pass enumerated, harmonizing code-architect's existing wording into the shared one. Agent-facing text names no vendor and no origin. The ladder governs the solution, never the reading — comprehension and verification tiers untouched.
  status: done
  dispatched: ladder text AUTHORED INLINE by the orchestrator (one rule one wording is a project axiom, so the exact bytes are a decision, not production) and frozen at reports/solution-ladder-text.md — pin sentence "Reuse or extend an existing mechanism before writing a second one." (66 chars, over MIN_RULE_CHARS=48). TWO subagents read those identical bytes: (a) implementer — appends the section to the 3 canonical agents/*.md, replaces code-architect:40-41 with it, and hand-mirrors into the 9 codex .toml, then regenerates the opencode/kimi trees; (b) tdd-guide — holds test custody and adds the 3 ROLE_PINS to scripts/test-agent-profile-parity.js. Edits land in the worktree; reports at reports/impl-953.md and reports/pins-953.md.
  result: LANDED across 12 carriers + 6 regenerated additive-edition trees. Parity guard 784 -> 808 assertions, exit 0. Pin sentence present exactly once in all 12 files; the three codex trees remain byte-identical per role (sha256 verified, 3 hashes x 3 roles); code-architect's two old bullets are GONE (harmonized into the ladder, not duplicated by it). GUARD IS MUTATION-PROVEN, not merely green — leg 1 (tdd-guide, on a mirror): pins present, prose absent -> exit 1 with 3 presence-first failures naming each .md, and the 9 twin assertions correctly NOT reached, so the guard never claims a .toml obligation whose source it has just disproved. LEG 4 run inline by the orchestrator after tdd-guide went quiet mid-report, on a fresh mirror with a positive control (unmutated: 808 assertions, exit 0): deleting the sentence from exactly one carrier (plugins/kaola-workflow-gitea/agents/code-architect.toml) gives REAL_EXIT=1 and names that exact file — "is in agents/code-architect.md but MISSING from plugins/kaola-workflow-gitea/agents/code-architect.toml" — plus a second failure on cross-tree byte-identity. That is precisely the historical failure this issue exists to close, now reproduced and caught.

- item: #955 — place one pointer-only table in docs/ (short new doc or a section of architecture.md, decided by what the premise pass found): rows = dispatch carrier, command/skill surface, hooks, model & tier handling, install path; columns = the four runtimes; every cell a tier label plus a pointer that resolves — never a restated mechanism fact, except absent capabilities, which name the issue or ADR that established the absence. `docs/README.md` indexes it.
  status: done
  dispatched: implementer subagent, working in the worktree, reading reports/premise-955.md as its source of truth. PLACEMENT DECIDED from the premise pass: a new `### Runtime capability divergence` subsection inside the EXISTING `## Editions and runtimes` in docs/architecture.md — NOT a new doc, because a new doc would make that the second place per-runtime divergence lives, which is the failure #955 exists to end. Carries a mandatory obligation: docs/architecture.md:346-348 currently restates the codex tier mapping as prose (a THIRD copy of a fact whose source is CODEX_PINNED_* and whose carrier is the SKILL PIN), so it must be repointed, not left one screen from a table forbidding restatement. Report to land at reports/impl-955.md.
  result: LANDED in docs/architecture.md (new `### Runtime capability divergence` inside `## Editions and runtimes`, before `### Agent profiles`) + docs/README.md sub-pointer. 5x4 grid, 20 cells, one consistent label set defined once (full/partial/rendered/substituted/inherited). Pointer resolution done MECHANICALLY from the diff rather than from memory: 23 paths + a 42-file glob + 8 symbols + 13 heading anchors all resolve, and ZERO bare line numbers are cited (heading anchors were preferred precisely because line numbers drift). The mandatory repoint LANDED: the `gpt-5.6-sol`/`medium`/`xhigh` literals are gone from docs/architecture.md (grep exits 1) and now point at CODEX_PINNED_* plus conventions.md § Bundle Lane. generate-routing-surfaces --check: 18 surfaces byte-match, exit 0, before AND after. THE SUBAGENT CORRECTED MY BRIEF: my instruction to state "every claude and codex cell is really x3" is FALSE for at least two cells — claude's `agents/` is a single forge-independent tree — so shipping my wording would have put a prose absolute in the file that the premise pass's own row 5 contradicts two sections down. It kept the required statement but made the POINTER ITSELF the disclosure (`agents/` reads x1, `plugins/*/agents/*.toml` reads x3). Deliberately left, each recorded as a finding rather than silently fixed: docs/architecture.md:287 axis conflation (counts Codex among "four forge editions"), :295-296 half-true propagation-set claim, docs/README.md:17 stale opencode effort-mapping line. On :287 I ruled it OUT OF SCOPE and out of my hands — "four editions" is repo-wide vocabulary that CLAUDE.md itself uses for the four script trees, so rewording it is a user-owned terminology call, not a docs fix; the new subsection already tells the reader the axes differ.

- item: #952 — run the subtraction audit over scripts/ and docs/ as dispatched readers, every candidate measured before it is listed (a `delete:` shows its zero-consumer search, a `shrink:` shows the shorter form running), findings in the fixed grammar `delete:|stdlib:|native:|yagni:|shrink:` ranked by net deletable lines, report landing dated in `docs/audits/`. Report-only, no cut applied; the adaptive-schema byte-identical copies and ADR 0017 "built once, removed, recoverable" rows are not findings; test custody holds.
  status: done
  dispatched: TWO investigator subagents over a brief the orchestrator rewrote at reports/audit-952-brief.md after the premise pass struck duplication from the finding classes and added a ranking rule (rank by CANONICAL lines, state the x3/x4 multiplier separately, so portedness cannot decide the ranking). Reader A takes scripts/ (81 canonical .js; the 86 ported copies are audited once via their canonical), reader B takes docs/ (198 files) and owes the test-consumed doc set FIRST since no doc may be called deletable until that is established. Both write findings to reports/audit-952-scripts.md and reports/audit-952-docs.md; the orchestrator merges and ranks into the dated report in docs/audits/. One finding is pre-seeded from the #954 premise pass: the Codex-note strip at sync-opencode-edition.js:494 / sync-kimi-edition.js:470 matches nothing today and has zero references in either edition suite. THREE readers in the end: the docs half ran twice because I wrongly declared the first agent dead, and I kept the second deliberately once it self-reported the collision — that redundancy paid for itself (see the delete: disagreement below).
  result: docs/audits/2026-08-11-subtraction-audit.md. SCRIPTS: 6 findings, 643 canonical / 667 shipped lines, led by run-chain-pool.js (428 lines — a within-chain step pool with no chain, no CLI and no installer caller, whose only consumer is the test that exists to test it) and fixtures-orphan-legality.js (102 lines — both importers deleted, 8/8 exports unreferenced). The pre-seeded Codex-note strip was not merely confirmed but strengthened: all THREE dead strips removed together render byte-identical output. native:/stdlib: EMPTY with the reason measured — the repo declares no Node version floor, so "a built-in covers this" cannot be asserted without inventing a target; that is a finding about the missing floor. DOCS: 8 findings, all yagni:/shrink:, two of them inside test-consumed docs and marked unfree. THE TWO DOCS READERS DISAGREED ON THE BIGGEST CLAIM and the disagreement is the point: both found ~26 zero-inbound docs; reader B filed them as a 3,940-line delete:, reader A refused because docs/README.md indexes decisions/ and investigations/ AS DIRECTORIES and states a retention policy ("they remain accurate as history"). A is right — that would have been filing against a stated retention decision, and it would have been the report's headline. REJECTED. D6 was confirmed twice from opposite directions in the same run: reader B found the Codex model/effort pair restated in two docs unbound to its source, and the #955 adversarial reviewer found the same fact as a WRONG POINTER; one site was repaired during #955, the second escalates. Three measurement traps recorded, each having produced a clean plausible wrong number: truncated capture (0.2%), basename-not-stem (false zero-consumer), and an unquoted zsh pathspec that reported all 198 docs unreferenced. A FOURTH trap landed after the report was first written and changed a shipped file: a path NAMED in a test is not a doc the test READS — docs/architecture.md and docs/decisions/D-547-01.md appear in test-validation-allowband.js only as arguments to a path-SHAPE predicate whose bytes are never read, so those assertions survive the files' deletion; the test-consumed set was confirmed twice (declared constants, and the shipped isValidationInvisible predicate over all 198 docs -> 195 invisible) and both returned the same three files. ALSO RECORDED: a read-only auditor must never run a suite in a worktree other agents are editing — the chain opens with edition-sync.js --materialize-kernel, which WRITES; one reader started that in the shared worktree, killed it, and verified no damage. LATE SECOND PASS added D9 and D10 and cleared five retired-machinery hypotheses (consent valve, --profile, model badge, issue-scout, DAG/node-id) — credible clears because the same method over the same docs produced D1/D3/D7. D9 is third by VALUE despite being one line: architecture.md:295-297's "not wired into the routing-surface propagation set" is true of RENDER TARGETS and false of DERIVATION (both sync scripts derive their command list from that registry), proven operationally — append a comment line to commands/workflow-next.md and opencode --check flips exit 0 -> exit 1 "stale — regenerate". The cost is behavioural: a reader who believes it edits a routing skeleton and never regenerates. Its kimi leg is reported as carrying NO signal (.kimi untracked, red in both legs). D10 REFUTED MY OWN STATED REASON for leaving architecture.md:287 alone: I said rewording would ripple through repo-wide vocabulary; measured, "four forge edition" is 3 hits with exactly ONE live doc, and the load-bearing phrase is the DIFFERENT "four editions", untouched. The real defect is narrower than "four editions is wrong" — four trees call THREE forge CLIs (Codex calls gh 64, glab 0, tea 0), so the false clause is "against a different forge CLI". I still did NOT fix either: the audit is report-only by its own terms, I left all 14 other findings unfixed, and fixing two because they sit in the file I happened to edit would be inconsistent — they escalate with everything else, now with measured blast radius attached. D6 FINAL, after a third correction — AND IT CAUGHT A FALSE MECHANISM CLAIM I HAD ALREADY SHIPPED. I wrote in docs/architecture.md that the constants copy and the skeleton-prose copy "are not bound to each other". FALSE, verified at test-route-reachability.js:530-545: EXPECTED_EFFORTS is built from codexPreflight.CODEX_STANDARD_EFFORT/CODEX_REASONING_EFFORT and every shipped Codex SKILL is regex-asserted against it, over a computed obligated universe, with the code's own comment saying the binding exists "so the prose and the validator cannot drift apart" — and it runs in the fast gate. One precision the audit did not state and I added: that check pins the EFFORT and accepts any model string (`model: "[^"]+"`), so a model change is caught by the contract validator, not the prose check. This makes D6 SHARPER, not weaker: the repo binds this fact in every prose surface it ships and the two docs/ copies are the sole unbound exception — 22 carrier files in all. The mutation proof had already demonstrated it and was under-read: with both docs falsified, test-route-reachability still exits 0 because it reads constants and SKILL prose and never opens a doc. Re-verified after the fix: route-reachability exit 0, routing 18/18, parity 808, walkthrough 209/209 — third full-scope run of this session. EARLIER D6 ESCALATION NOTE (superseded in part): the pair has NO single source — four named constants in kaola-workflow-codex-preflight.js:89-92 AND typed literals in the routing skeletons' PIN (which is what ships), unbound to each other. My first TWO repairs of this both named a single wrong source; architecture.md now names both. docs/api.md and docs/conventions.md remain unrepaired and are what escalates.

- item: Adversarial review of every landed change — reviewers on the fable model, one per issue, each told to falsify the completion claim rather than confirm it. A green suite has certified an unimplemented headline fix in this repo before; read what ships, not what was authored.
  status: done
  dispatched: adversarial-verifier subagents on model=fable at the user's explicit direction, one per issue, each pointed at the SHIPPED bytes rather than the reports. Wave 1 (#953 and #954, both settled) out now; wave 2 (#955, #952) follows when those land. Reports to reports/review-95N.md. Self-caught before dispatch and already repaired: my own ADR row 1 cited `simulate-kaola-workflow-walkthrough.js:528` for an assertion that is in NO file — the script is `simulate-workflow-walkthrough.js` and `hookSpecificOutput` appears nowhere in it; the real record is a comment at plugins/kaola-workflow/scripts/kaola-workflow-codex-compact-resume.js:166-169 saying the envelope is optional and unused. Row 2's citations (CHANGELOG:2710, conventions.md:846-856, 14 profiles with no issue-scout) all verified good.
  # REVIEW RESULTS SO FAR:
  # #953 — NOT REFUTED, 0 blocking. Verified against shipped bytes: pin present exactly once in all 30 carriers (12 tracked + 18 generated, which ARE present in the worktree); codex tomls hash-identical per role; both old code-architect bullets gone from the ENTIRE tree incl. generated/docs/archive, so no sixth wording; all 42 tomls parse; zero vendor/model/provenance tokens. It ran mutations mine did not: deleting from a DIFFERENT tree+role, flipping meaning identically in all three tomls so byte-identity stays green (pin alone catches it), and deleting from a GENERATED carrier — which test-opencode-edition.js reds with a named D0 drift finding. So all 30 carriers fail loudly via two mechanisms, not one.
  # #954 — REFUTED, 2 blocking, BOTH MINE, both now repaired and re-verified:
  #   D1: "five plugin copies and the opencode adapter" matches no partition of the tree. Measured 10: four tracked (hooks/ + 3 plugins/*/hooks/, byte-identical per validate-script-sync.js) and six sync-rendered — and the three KIMI copies are ADAPTED, not copied (HOOK_ADAPTATIONS in sync-kimi-edition.js rewrites the payload-extraction expression keyed on its exact source text), which my sizing omitted entirely. Fixed to the measured 10 with the kimi transform named.
  #   D2: row 2 cited CHANGELOG.md:2710 for #524 — true at HEAD, FALSE in the shipped tree, because this bundle's own +49-line [Unreleased] insertion shifts it. Worse than a dead pointer: line 2710 in the shipped tree is the #545 entry, whose body says "Building on #524", so the stale citation reads plausibly correct. The citation self-invalidated at the very commit that ships it. Now cited by ISSUE, not line, in both the ADR and the CHANGELOG.
  # AND A CORRECTION TO MY OWN EARLIER CORRECTION: I had "repaired" row 1 by removing a walkthrough citation I called fabricated. It was not. `plugins/kaola-workflow/scripts/simulate-kaola-workflow-walkthrough.js:528-529` really does assert compact-resume stdout carries no hookSpecificOutput envelope — the CODEX edition's walkthrough, a different file from the canonical scripts/simulate-workflow-walkthrough.js I checked. My repo-wide grep WOULD have found it, but I piped it through `head -10` and the hits fell past the cut. I truncated my evidence and called a true claim fabricated. The citation is restored alongside the compact-resume comment.
  # #954 review also strengthened the row-3 refusal on evidence I did not have: archive/issue-949/.cache/impl-sync.md:32-42 shows the silent no-op firing on the REAL tree in-run (rename landed, 6 sync legs exit 0 silently, rendered surface grep = 0) — an actual firing, not a mirror probe, so the table's own probe-vs-observed distinction cannot rescue the row.
  # #955 — REFUTED, 4 blocking, ALL FOUR verified by me before repair, ALL FOUR now fixed:
  #   R1 (high): the repointed § Model resolution said the model/effort PAIR is declared by CODEX_PINNED_* in adaptive-schema.js. FALSE — measured, that file contains ZERO `gpt-5.6-sol`/`xhigh` literals; the constants are role ROSTERS and the pair is authored in templates/routing/{next,finalize}.skeleton.md. A reader sent to the schema for the pair finds nothing: the exact wrong-pointer defect #955 exists to prevent, sitting one screen from the table forbidding it. Now names both halves separately.
  #   R2: the codex hooks cell pointed at plugins/kaola-workflow/config/hooks.json — a single-tree path for a x3 artifact whose three copies have THREE DISTINCT md5s. Repointed to plugins/*/config/hooks.json with the per-forge divergence stated.
  #   R3: "No runtime registers a PreToolUse or PostToolUse hook" over-generalized. Both halves measured: the absence IS true across all six hooks.json (SessionStart + SubagentStart only), but opencode's plugin registers `tool.execute.before` at templates/opencode/plugins/kaola-workflow-hooks.js:148 — the very file the table's own opencode cell points at. Rescoped to the hooks.json family with the opencode event named.
  #   R4: the post-table paragraph called claude's dispatch carrier "the weakest pointer — no prose states that mechanism" while the cell's own second pointer is § Agent profiles, which IS prose. Reworded.
  #   What HELD under attack: all 23 paths, the 42-file glob, 8/8 symbols, 13/13 heading anchors, zero bare line numbers; no mechanism restatement in any of the 20 cells; every surviving claim of the original repointed paragraph carried; and the labels judged honest cell by cell (kimi `substituted` not `none`; codex model&tier genuinely ships in all six SKILL surfaces, so #944 is closed).

- item: Verification and docking — `generate-routing-surfaces.js --check` if any prompt skeleton moved, `test-agent-profile-parity.js`, edition sync `--check`, the walkthrough at FULL scope (the fast gate only samples a 1/12 shard), and CHANGELOG under `[Unreleased]` for all four issues.
  status: done
  dispatched: self (orchestrator, inline), run only after every subagent had finished — the suite is spawn-bound and produces false reds under concurrency.
  result: ALL GREEN at full scope, RE-RUN after the final late repair rather than reasoned around (the only changes since the first green run were docs, and the audit had established architecture.md is NOT test-consumed — but re-running cost two minutes and certainty outranks the saving). Second full run identical: 209/209, exit 0. Walkthrough `total:1` (NOT the 1/12 shard): 209/209 scenarios, 0 failed, exit 0, spawn-census 2400. test-agent-profile-parity: 808 assertions, exit 0. edition-sync --check: 8 forge aggregator ports in parity, committed kernel parity verified at HEAD, exit 0. generate-routing-surfaces --check: all 18 surfaces byte-match, exit 0. CHANGELOG [Unreleased] now carries all four issues. Final diff: 17 files changed, 323 insertions, 7 deletions, plus the untracked docs/audits/2026-08-11-subtraction-audit.md.
  # Notes gathered while the work ran, so they are not lost:
  # - The walkthrough must be re-run at FULL scope at the end. impl-953 already ran it green (209/209, exit 0) but that was BEFORE the #954 ADR rows, the #955 architecture.md table and the CHANGELOG landed.
  # - Do NOT run the suite while subagents are in flight: it is spawn-bound and produces false reds under concurrency.
  # - CHANGELOG [Unreleased] is written for #953 and #954 only; #955 and #952 still owe entries. There was NO [Unreleased] section at all (top was [9.6.0]) — it was created by this run.
  # - LIFECYCLE, from review-953: the installed copies under ~/.claude, ~/.codex, ~/.config/opencode and ~/.kimi-code are still PRE-BUNDLE and stay that way until a reinstall. A reinstall of all four runtimes belongs after the sink, not before.
  # - This run's diff touches agent profiles and the codex plugin trees, so it IS edition-touching: chain selection should fail closed to all four chains, not the claude chain alone.
