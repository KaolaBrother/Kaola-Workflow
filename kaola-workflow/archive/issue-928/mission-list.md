# Settle whether `dispatchEffort` + `normalizeTier` are dead in the ×4 anchor, and cut or record accordingly (#928)

- item: Measure every consumer of `dispatchEffort` and `normalizeTier` across ALL surface classes, not just JS — the deciding question is whether a PROSE surface consumes it (Codex plan-run SKILLs map a tier to `codex_reasoning_effort`, and prose never appears in a JS grep). Definitions live in `scripts/kaola-workflow-adaptive-schema.js` (`dispatchEffort` ~`:1616`); `normalizeTier` is claimed to have exactly two call sites, both inside that module, one of them `dispatchEffort` itself. This repo's `grep` is ugrep and SKIPS dot-directories — name `.claude` `.codex` `.opencode` `.kimi` explicitly. The schema is byte-identical ×4, so check every edition mirror, plus `templates/routing/` skeletons and installed surfaces. A grep is a hypothesis: report literal hit lines, never a summary count.
  status: done
  dispatched: self — inline all-surface sweep via `git grep` over tracked files (covers dot-dirs that ugrep skips); hit lines land inline in this file's `result`.
  result: THE SETTLING QUESTION IS ANSWERED **NO** — no prose surface consumes it. `codex_reasoning_effort` appears in exactly 5 tracked files: the 4 byte-identical schema copies and `scripts/test-agent-model-resolver.js`. Zero hits in `commands/`, `agents/`, `skills/`, `templates/routing/`, or any edition's prompt surfaces. There is no `mapTier`-class documentation entanglement to record.
    Measured call graph (live files only; CHANGELOG/`docs/decisions/`/`archive/`/`.origin/` excluded as history):
    - `dispatchEffort` — defined `scripts/kaola-workflow-adaptive-schema.js:102`, exported `:1616`. Only caller repo-wide: `scripts/test-agent-model-resolver.js:79`. That test is NOT mirrored into the plugins.
    - `normalizeTier` — defined `:63`, called at exactly ONE site: `:103`, inside `dispatchEffort`. Closed loop confirmed.
    - `NODE_MODEL_TIERS` (`:51`) and `TIER_ALIASES` (`:62`) — read ONLY inside `normalizeTier` (`:66`, `:67`). Neither is exported. They are orphaned by the cut.
    - Two live PROSE citations by name, both comments, neither a consumer: `scripts/kaola-workflow-resolve-agent-model.js:49` names `normalizeTier()` as the canonical alias map (while stating the resolver DELIBERATELY inlines it and must stay dependency-free, since the dispatch-log hook copies it standalone); `scripts/validate-workflow-contracts.js:962` cites `TIER_ALIASES` to explain why lowercase `opus`/`sonnet` escape the capitalized vendor-noun ban regex at `:965`.
    - A STALE COMMENT found in passing: `:49-50` claims the tier list is "Defined here … so the validator, the executor, and every edition share one list" — already false, nothing outside `normalizeTier` reads it. Same rot the file itself warns about at `:57-58`.
    - Substantive finding: `dispatchEffort` is a NO-OP. It computes `tier` but never uses the value — only its truthiness picks between `parent_session` and `role_default` labels; model and effort come wholly from `sessionProof`. The test calls it with no `sessionProof`, so both values are always null, which is exactly what it asserts.

- item: Confirm the issue's own stated premises independently — that `kaola-workflow-adaptive-node.js` and `buildDispatch` are genuinely gone (CHANGELOG #451 named them as the live consumer), and that the only measured `dispatchEffort(` caller is a test. A premise that collapses is the result, not a setback.
  status: done
  dispatched: self — inline existence check on `kaola-workflow-adaptive-node.js`, `buildDispatch`, and the CHANGELOG #451 record; findings land inline in this file's `result`.
  result: PREMISE HOLDS, with one count correction that STRENGTHENS it. `buildDispatch` has zero live definitions or references (CHANGELOG/archive only). `kaola-workflow-adaptive-node.js` is gone — all 6 `git ls-files` matches are archive artifacts under `archive/bundle-423-425-431/` and `archive/issue-725.*/`, no live script. `dispatchEffort`'s only measured caller is indeed a test.
    CORRECTION: the issue says `normalizeTier` has "exactly two call sites, both inside the schema module, one of them `dispatchEffort` itself." Measured, there is exactly ONE call site (`:103`), and it is inside `dispatchEffort`. The other two mentions (`:55`, `:100`) are comments. The loop is tighter than filed, not looser.

- item: Decide the cut from the measurement, then execute it — if genuinely dead, delete `dispatchEffort` with `normalizeTier` and their tests (a test is deleted WITH its mechanism, never repaired ahead of it); if a prose surface depends on either, record that entanglement explicitly the way #880 did for `mapTier` rather than leaving it implicit. Deleting working capability is user-owned: state the evidence and confirm before cutting.
  status: done
  dispatched: self — USER RULED (in conversation): cut the WHOLE closed loop, not just the two symbols #928 named. Delete `NODE_MODEL_TIERS`, `TIER_ALIASES`, `normalizeTier`, `dispatchEffort` and the `:1616` export from all 4 byte-identical schema copies; delete the 4 `dispatchEffort` assertions at `scripts/test-agent-model-resolver.js:79-83` (the surrounding CLASS_DIVERGENCE loop stays — different, live mechanism); reword the 2 comments that name the deleted symbols (`kaola-workflow-resolve-agent-model.js:49`, `validate-workflow-contracts.js:962`) to cite the live carrier instead. Edits land on branch `workflow/issue-928`.
  result: Cut applied in the worktree `.kw/worktrees/issue-928` on branch `workflow/issue-928`. Schema: deleted `NODE_MODEL_TIERS` + its comment block, `TIER_ALIASES` + its comment block, `normalizeTier`, `dispatchEffort` + its 11-line `#405` comment block, and the `dispatchEffort` export entry — then copied the canonical file over the 3 plugin mirrors, so all four remain byte-identical (1 distinct md5) and all four `node --check` clean. Test: deleted the 5 `dispatchEffort` lines from `scripts/test-agent-model-resolver.js` and the now-false "asserted below" parenthetical in the CLASS_DIVERGENCE header; the surrounding CLASS_DIVERGENCE loop and the INSTALL-INVARIANT TIER block are untouched. Comments rewritten and propagated: `kaola-workflow-resolve-agent-model.js` (×4, now states it IS the alias seam rather than a mirror, keeps the dependency-free rationale, points at `DEFAULT_AGENT_MODELS`) and `validate-workflow-contracts.js` (×2, cites the live carriers instead of the deleted `TIER_ALIASES`). The stale `:49-50` claim that the validator and executor share the tier list left with the const it described.

- item: Prove the cut is complete and nothing else was holding on — after deletion, re-run the same all-surface sweep as a positive control, and confirm the ×4 schema copies stayed byte-identical (`kaola-workflow-adaptive-schema.js` is the cross-edition drift anchor).
  status: done
  dispatched: self — inline repo-wide residual sweep over tracked live files for all four deleted symbols, plus a require-time smoke that the export is gone and the module still loads in every edition; findings land inline in this file's `result`.
  result: COMPLETE. Final sweep over live tracked files reports `(clean)` for all four symbols. Require-time smoke on all 4 editions: module loads, export count 52 (was 53), none of the four deleted names present, and the survivors `CODEX_PINNED_STANDARD_ROLES` / `CODEX_PINNED_REASONING_ROLES` / `PLAN_FILE` / `MISSION_LIST_FILE` all still exported. Mirror parity holds everywhere: schema 1 distinct hash across ×4, resolver 1 across ×4, validator 1 across ×2, `test-opencode-edition.js` single copy.
    THE SWEEP EARNED ITS KEEP — it caught two stale references the planned edit list did not name: (1) `scripts/test-opencode-edition.js:770` cited `NODE_MODEL_TIERS` as "the canonical … cross-edition internal token" while explaining the lowercase-token exemption, now reworded to name the `{opus,sonnet}` vocabulary directly; (2) my own first draft of the `validate-workflow-contracts.js` comment named `TIER_ALIASES` as deleted, which is itself the "record naming something that no longer exists" failure the schema warns about — rewritten to name only live carriers. The `resolve-agent-model.js` comment was likewise resimplified to state the result (it IS the seam) rather than the history.

- item: Verify — walkthrough at FULL scope (`node scripts/simulate-workflow-walkthrough.js`, exit 0; the fast gate only samples a 1/12 shard so it is not proof), then the four-chain receipt, which this diff owes because it touches the ×4 anchor. Pass `--project issue-928` to run-chains or the receipt lands where finalize never looks; use `KAOLA_RUN_CHAINS_CONCURRENCY=serial`.
  status: done
  dispatched: self — run from the worktree `.kw/worktrees/issue-928`: first `node scripts/simulate-workflow-walkthrough.js` at FULL scope (no shard), then the targeted suites this diff touches (`test-agent-model-resolver.js`, `test-opencode-edition.js`, `validate-workflow-contracts.js`), then the four-chain receipt with `--project issue-928` and `KAOLA_RUN_CHAINS_CONCURRENCY=serial`. Exit codes land inline in this file's `result`; the receipt lands at `kaola-workflow/issue-928/.cache/`.
  result: ALL GREEN. Walkthrough at FULL scope: `##KW-SHARD {"index":1,"total":1,"scenarios":202,"ran":202,"passed":202,"failed":0}`, 2145 spawns, exit 0 — shard 1/1, so this is the full suite and not the fast gate's rotating 1/12 sample. Touched suites, all chain-wired, all exit 0: `test-agent-model-resolver.js`, `test-opencode-edition.js`, `validate-workflow-contracts.js`. Four-chain receipt at `kaola-workflow/issue-928/.cache/chain-receipt.json` (MAIN copy — where finalize reads): claude/codex/gitlab/gitea all exit 0, no waivers, `scope.decision: all-four`, `scope.reason: edition_coupling`.
    POSITIVE CONTROL — the green is not vacuous. `headSha` is `9e308348`, the pre-change base, which is exactly the shape that would appear if the chains had tested MAIN's untouched copy instead of the worktree. Refuted directly: main's working tree is clean and its schema still contains `dispatchEffort` (2 hits), while the worktree carries exactly 13 modified files — matching the receipt's own `changedFileCount: 13`, whose `touchedEditionPaths` enumerate my modified files by name. The chains ran against the modified tree; the receipt merely landed in the main-root `.cache/` as finalize expects.

- item: Adversarial review of the finished diff — every prior bundle found real defects AFTER every suite was green, so a green suite closes nothing. Look specifically for a deletion that silently orphaned a caller, and for a test that was rewritten to keep passing rather than deleted with its mechanism.
  status: done
  dispatched: self — inline read of the full diff plus four targeted falsification attempts: (a) an UNTRACKED or dot-directory copy of the schema (`.opencode`/`.kimi`/`.claude`/`.codex`) that `git grep` never saw, since a guard reads what SHIPS not what was authored; (b) an installed surface under `~/.claude`/`~/.codex` still carrying the deleted symbol; (c) the surviving test loop reduced to a vacuous or weaker assertion by the deletion; (d) a generated routing surface gone stale. Findings land inline in this file's `result`.
  result: NO DEFECT FOUND IN THE DIFF. (a) Exactly four schema copies exist anywhere on disk, tracked or not — no dot-directory or untracked copy; `.agents`, `.opencode`, `.opencode-gitea`, `.opencode-gitlab` all sweep clean under an explicit named grep (ugrep would have skipped them). (c) The surviving test loop is NOT weakened: the five deleted lines asserted only `dispatchEffort`'s return shape, while the bidirectional CLASS_DIVERGENCE assertions and the INSTALL-INVARIANT TIER block are untouched and still fire — this was a deletion with its mechanism, not a pin rewritten to keep passing. (d) `generate-routing-surfaces.js --check`: all 18 surfaces byte-match the skeleton, exit 0.
    Two extra falsification attempts beyond the planned four, both negative: no `docs/api.md` or test pins the export count or an export list, so dropping one export breaks no snapshot; and there is NO dynamic property access (`schema[...]`) and no `Object.keys/entries` iteration over the module anywhere, which is what makes the static name-sweep a complete measurement of consumers rather than a hypothesis.
    Chain wiring verified so the passes are real evidence: `test-agent-model-resolver.js` and `validate-workflow-contracts.js` both run in the `claude` chain; `test-opencode-edition.js` runs in `test:kaola-workflow:editions`.
    (b) ONE STANDING FACT, NOT A DEFECT IN THIS DIFF: the installed copies still carry the deleted symbols — `~/.claude/kaola-workflow/scripts/`, `~/.config/opencode/kaola-workflow/scripts/`, and the Codex plugin cache at `.../kaola-workflow/7.5.0/scripts/`. They lag the repo by design until reinstall, so the routine `./install-all.sh --global --yes` after this merges is what drops the dead code from the shipped surfaces. Nothing in this branch can fix it and nothing in this branch broke it.

- item: Dock the docs — `CHANGELOG.md` under `[Unreleased]`, and `docs/api.md` only if it names either symbol (note: `docs/api.md` is test-consumed, so editing it stales the chain receipt — write it BEFORE the receipt run).
  status: done
  dispatched: self — taken AHEAD of the verify item on purpose, because editing test-consumed docs after a receipt run stales it. Measured: no `docs/` or `README.md` surface names any of the four symbols, so the only edit owed is a new `[Unreleased]` → `### Removed` entry in `CHANGELOG.md` (head is `## [9.5.0]`, no Unreleased section exists yet).
  result: `CHANGELOG.md` — new `## [Unreleased]` / `### Removed` entry naming all four symbols, why the cut went wider than #928 named, that no prose surface consumes `codex_reasoning_effort` (so the #880 `mapTier` precedent has no counterpart here), and that the `opus`/`sonnet` vocabulary survives in `DEFAULT_AGENT_MODELS` + agent frontmatter. No `docs/api.md` or `README.md` change owed — measured, neither names any of the four.
