# Ship #994 (typed follow-up body contract), #992 (filing existence/non-empty verification), #993 (backlog-delta stamp in the closure receipt)

- item: Read `templates/routing/finalize.skeleton.md` Step 7 end to end and map the surface both #994 and #992 must edit — the filed-follow-up sentence (`:238` "file a follow-up and record `filed: #N`"), the `independent slices` paragraph (`:250`, must stay byte-untouched), the correction-comment rule (`:246`), the `forge-is-the-backlog` pin boundaries, and which rendered surfaces/editions the skeleton reaches. One shared investigation serves both issues; #994 and #992 are NOT disjoint so their edits serialize.
  status: done
  dispatched: Explore agent (read-only) over the worktree `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-992-993-994`; output to land at `kaola-workflow/bundle-992-993-994/.cache/step7-surface-survey.md` in the MAIN root.
  result: `.cache/step7-surface-survey.md` (31KB, verified present). Decisive facts: (1) the `forge-is-the-backlog` PIN spans `finalize.skeleton.md:237-248` — filing paragraph + correction paragraph; the `independent slices` paragraph at `:250-253` is OUTSIDE it and is UNGUARDED by any test today. (2) `templates/routing/required-blocks.js` is a TEST ARTIFACT per ADR 0018 (`docs/decisions/0018-...md:235-262`): "the party placing the markers must not also author the thing that judges their presence" — so tdd-guide owns the manifest, not whoever writes the prose. (3) block `fn-forge-is-the-backlog` (`required-blocks.js:347-364`) pins FIVE exact token strings; matching is whitespace-normalized (`test-route-reachability.js:23`, `:915`) so rewrapping is safe but adding/removing a word INSIDE a token reds `missing-token` × 12 surfaces. (4) A NEW pin in Step 7 without its own manifest entry reds `orphan-surface` × 12 — so #994's contract goes INSIDE the existing pin (my call, least machinery, still guardable). (5) One Step 7 edit reaches 6 tracked surfaces + 6 in-memory edition trees = 12 finalize surfaces; `generate-routing-surfaces.js --check` byte-compares all 18 and is the default with no args. (6) `RENAMES` is EMPTY — a forge CLI name written into Step 7 prose is NOT renamed per forge; it must go through a forge-keyed SLOT/SPLICE, so both edits stay forge-neutral in prose. (7) No `searched:` line exists anywhere in the repo — new vocabulary. (8) A prose-only routing-skeleton edit is EDITION-TOUCHING: run-chains diff-scopes to all four chains.

- item: #994 — add the typed body contract to Step 7: `## Measured` (every figure stamped with commit + evidence source), `## Hypothesis` (static derivations land here by default), `## Proposed remedy (non-binding)` (optional), and one `searched:` line (mechanism/symbol-level duplicate probe, query + hit count). Adds no measurement obligation; forbids only an unstamped figure or unrun attribution presented as fact. Forge-neutral, names no forge CLI. Then regenerate routing surfaces — never hand-edit a rendered surface.
  status: in-flight
  dispatched: PROSE AUTHORED BY ME (a rule's wording is a decision, not production) and frozen verbatim at `.cache/step7-prose-authored.md` — paragraph A. Guarding it dispatched to tdd-guide under test custody: ADR 0018 makes `templates/routing/required-blocks.js` a test artifact ("the party placing the markers must not also author the thing that judges their presence"), so the party that authored the prose must not author its tokens. tdd-guide output → new `content_tokens` on block `fn-forge-is-the-backlog`, proven RED via `node scripts/test-route-reachability.js` BEFORE the prose lands; red transcript to `.cache/step7-tokens-red.md` in the MAIN root. — GUARD LANDED AND VERIFIED BY ME (not taken on report): `git diff` shows exactly one modified file, all SIX original tokens intact and unreordered (the 4 deletions are a rewritten comment, not tokens), 7 new tokens added one per obligation, plus an explicit note on what is deliberately NOT pinned. RED proven at baseline `c62e8a3f`: 84 `missing-token` findings = 7 tokens × 12 obligated surfaces, on a tree that passed 331 assertions clean beforehand. Mutation-proved ARMED in memory, one mutant at a time, each redding exactly its own token — so this is evidence, not a green suite. Agent raised one SUBSTANTIVE wording defect and I fixed it: paragraph A never named the artifact it structures, and Step 7's neighbourhood is full of other `##`-bearing artifacts (`## Run gaps`, finalization-summary.md), so `## Measured` could plausibly be read as a summary heading. Amended to "Structure the body of the issue you file ... In that body, `## Measured` ...". No token quotes that sentence, so NO re-derivation was needed — I verified all 7 tokens still match the amended prose (7/7 OK). Two cosmetic nits DECLINED: both sit inside pinned spans, so rewording would force token re-derivation for no change in what the rule requires. NOTE for any successor: `.cache/step7-tokens-mutation-proof.js` slices the prose file by HARDCODED line numbers and my amendment shifted them — re-running it unchanged would silently check the wrong text.
  dispatched-next: implementer — land the amended prose into `templates/routing/finalize.skeleton.md` and regenerate; output lands as a worktree diff on branch `workflow/bundle-992-993-994` plus `.cache/step7-prose-landed.md`.
  status: done
  result: **#994 AND #992-layer-1 BOTH SHIPPED.** `.cache/step7-prose-landed.md`. VERIFIED BY ME, not accepted on report: (a) `git diff --numstat` = `+16/-0` on all SEVEN files (skeleton + 6 tracked surfaces) and `git diff -U0 | grep -c '^-[^-]'` = **0 deleted lines anywhere** — so #994's acceptance that the `independent slices` paragraph and the correction-comment rule are byte-untouched is proven structurally, not asserted; (b) `generate-routing-surfaces.js --check` re-run by me: exit 0, "all 18 surfaces byte-match the skeleton"; (c) the rule reached what SHIPS, not just what was authored — all 5 markers present in both `commands/kaola-workflow-finalize.md` (claude) and the gitea SKILL surface; (d) ZERO forge-CLI tokens in the added lines, so the forge-neutrality constraint held. Implementer reconstructed the red baseline itself rather than inheriting mine: 330 passed/85 failed → 331 passed/0 failed on `test-route-reachability.js`. Wrap is 97 cols and keeps backtick spans atomic so `## Proposed remedy (non-binding)` never straddles a break. It read `required-blocks.js` but never wrote it — test custody held end to end.

- item: #992 layer 1 — add to Step 7 one sentence requiring the operator to verify, after filing each follow-up, that the issue exists and its body is non-empty, recording issue number + body length in the run's OWN record (mission-list result line, NOT the `## Run gaps` row whose grammar the parser owns). Declare per-forge divergence where a forge cannot express the check, per #985's convention. Serialize after the #994 edit — same file, same Step.
  status: in-flight
  dispatched: Same authored-prose freeze at `.cache/step7-prose-authored.md` — paragraph B — and the SAME tdd-guide dispatch as the item above (one file, one Step, so the two edits are one unit rather than two racing writers). MEASURED RESOLUTION of the issue's "declare per-forge divergence" clause: all three forges CAN express an existence + non-empty-body check, so there is no capability difference to declare and NO `<!-- REGION -->` is warranted — the renderer's own rule is that a region whose reason cannot name a runtime difference is drift, not divergence. The paragraph therefore names no forge CLI at all.
  status: done
  result: Shipped in the same additive edit as #994 — see the result on the item above; paragraph B is present on all 6 tracked surfaces and pinned by tokens 6 and 7. #992's remaining obligation is the durable record of the layer-2 floor re-affirmation, carried by the docs item.

- item: SECOND RUN-DISCOVERED GAP, found by RUNNING, not reading — file as a follow-up at Step 7. `node scripts/generate-routing-surfaces.js --write` executed inside a LINKED WORKTREE mutates the MAIN ROOT's six gitignored edition trees. Measured: after the implementer ran `--write` in `.kw/worktrees/bundle-992-993-994`, all six main-root trees (`.kimi`, `.kimi-gitea`, `.kimi-gitlab`, `.opencode`, `.opencode-gitea`, `.opencode-gitlab`) carry the new Step 7 prose while ZERO main-root TRACKED surfaces do (`grep -rl` over `commands/`, `plugins/`, `templates/` = 0); the worktree itself has no edition trees at all (gitignored, absent from a fresh checkout). Doubly silent: gitignored so `git status` in main root shows nothing, and `--check` deliberately never reads edition trees. Effect, stated without asserting a mechanism: the installed opencode/kimi runtimes on this machine now carry a rule that exists in no tracked file of the main root, and that divergence would persist if this branch were discarded. Self-corrects if the branch merges. Related to the known family "edition suites go vacuous in a fresh worktree" — same worktree-vs-gitignored-edition-tree seam, opposite direction.
  status: in-flight
  dispatched: self — BODY DRAFTED under #994's typed contract at `.cache/followup-worktree-edition-write.md`, ready for Step 7 of finalize to file verbatim. `searched:` records the real probe: `gh search issues "generate-routing-surfaces"` → 6 hits, nearest is **#969** "Edition sync --check runs in no chain, so four green chains can certify stale .opencode/.kimi trees" (CLOSED) — the SAME seam in the opposite direction (stale trees unnoticed vs trees written that no tracked file backs), so adjacent prior art, not a duplicate; `"worktree edition tree"` → 0 hits. Honest typing enforced on myself: the ROOT CAUSE ("presence probed against the git common dir rather than the working tree") went under `## Hypothesis` because I measured only the effect and never traced the resolution code — exactly the class #994 exists to stop me shipping as fact.
  status: done
  result: **FILED AS #996** (bug, area:scripts, P2). Verified per #992's rule: exists, state=OPEN, **body_len=3213**.

- item: #992 layer 2 is a VALUE CALL for the user, not mine: wire the forge adapter into `kaola-workflow-gap-sweep.js`'s online-probe branch so a `filed:` row fails closed on a nonexistent issue, versus re-affirming n1-design's floor (`gap-sweep.js:443-446`, "the syntactic check is the floor") and shipping layer 1 alone. Changing a gate's refusal direction is value-laden; the issue's own text permits declining. Ask in conversation, then either build it or record the re-affirmed decision.
  status: done
  dispatched: self — asked the user in conversation, offering ship-layer-1-alone / typed-finding / hard-refusal.
  result: USER RULED: ship layer 1 alone. n1-design's floor (`kaola-workflow-gap-sweep.js:443-446`, "the syntactic check is the floor") is RE-AFFIRMED — no forge probe is wired into `--check`, the script stays forge-neutral bookkeeping, and no new refusal is added. #992 closes on the prose layer. The re-affirmation must be recorded durably (issue comment on #992 + CHANGELOG entry) as the issue's body requires — carried by the docs item below.

- item: Map #993's write surface before touching it — the `## Closure` block writer at `scripts/kaola-workflow-claim.js:5188-5192` and its serializer at `:2419-2422`, where the sink's `issue_numbers`/`issue_action` are readable at stamp time, how the `## Run gaps` `filed:` rows are parsed today (`kaola-workflow-gap-sweep.js --check --json` emits `{mapped, filed, noise}`), what the archive carries forward, and which edition copies of `claim.js` must be re-normalized. Disjoint from the Step 7 survey.
  status: done
  dispatched: Explore agent (read-only) over the same worktree; output to land at `kaola-workflow/bundle-992-993-994/.cache/closure-surface-survey.md` in the MAIN root.
  result: `.cache/closure-surface-survey.md` (37KB, verified present). **#993's PREMISE IS PARTLY REFUTED — measured, not read.** (1) FATAL to the issue's acceptance as written: on the SHIPPED merge lane `cmdFinalize` closes ZERO issues — `mergeLaneDeferred` defaults true (`claim.js:4283-4288`, `sink:` defaults to `merge`) and the shipped finalize surface passes `--keep-worktree` (`commands/kaola-workflow-finalize.md:300-303`). Real closes happen later in `sink-merge.js:2917-2974`, and `appendClosureBlock` is HEADING-guarded (`claim.js:2428`) so the sink can NEVER revise the block. Pinned by `test-bundle-finalize.js:986-995` (asserts `closure.closed === []` and ZERO `gh issue close` calls). So the issue's acceptance "a run that filed 4 and closed 4 stamps `net_backlog_delta: 0`" would in fact stamp **+4** on every normal run. (2) "the parsed `## Run gaps` rows" is not readable as claimed: `run-gaps.json` (`kaola-workflow/<project>/.cache/run-gaps.json`, shape `{project, sweptClasses:[{reasonClass,sample,count}]}`) carries NO issue numbers; `filed: #N` lives only in `finalization-summary.md`'s `## Run gaps` prose, and its parser `parseGapSection` is NOT exported (`gap-sweep.js:587` is `module.exports = { main };`). Reuse needs an export — never a duplicated regex. (3) CONFIRMED as claimed: zero new forge calls IS achievable (both artifacts are on disk at stamp time; `claim.js:4765-4771` already computes archive-then-live `.cache` dirs, `probeSelectionEvidence` at `:2434-2443` is the precedent one line above the call). (4) No production reader parses `## Closure` — additive fields break nothing; `fields` bypasses `CLOSURE_RECEIPT_FIELDS` entirely. (5) House degradation pattern for an unmeasurable value is a NAMED TOKEN or a companion `*_probe` field (`finalize_commit: 'unknown'` `:5163`; `changed_paths_probe` `:3858`) — a numeric field cannot carry `unknown` and stay numeric. (6) claim.js has 4 copies: canonical↔codex byte-pinned by `COMMON_SCRIPTS` (`validate-script-sync.js:45-47`), gitlab/gitea HAND-PORTED with no automated guard. Edition-touching → all four chains.

- item: #993 — stamp `issues_closed`, `follow_ups_filed`, `follow_up_numbers`, `net_backlog_delta` into the `## Closure` block written at `scripts/kaola-workflow-claim.js:5188-5192` / `:2419-2422`, deriving all four from data already on disk (sink `issue_numbers` + `issue_action`, parsed `## Run gaps` rows) — ZERO new forge calls, offline and keep-open semantics untouched. Keep-open stamps `issues_closed: 0`. A missing/malformed gap artifact degrades to explicit `unknown`, never a silently wrong `0`. Update `docs/workflow-state-contract.md` with the four fields.
  status: in-flight
  note: DESIGN SETTLED after the premise refutation. USER RULED: `issues_closed` = the size of the set this run's closure decision is closing (what the sink then closes), NOT closes this process performed — so the issue's 4-closed/4-filed → 0 example actually fires. NO fifth companion field: the block ALREADY emits `issue_disposition` (`close-pending|closed|kept-open|unknown`) directly above, which is the disposition token — reuse before adding. Source is `closureReceipt.closure.attempted` / the existing `issueSet` expression at `claim.js:4743`; keep-open stamps 0. `follow_ups_filed`/`follow_up_numbers` come from `parseGapSection` over the archived `finalization-summary.md` — which requires EXPORTING `parseGapSection` from gap-sweep.js (`:587` currently `module.exports = { main };`); never duplicate the regex. Missing/malformed section → `follow_ups_filed: unknown` AND `net_backlog_delta: unknown`; zero filed → `follow_up_numbers: none`.
  dispatched: implementer, after I ruled on the four design items the test author surfaced and refused to guess on. MY RULINGS: (1) ALL-ROWS-MALFORMED IS DELIVERABLE after all, without touching the scanner's contract — `parseGapSection` returns `null` only when the file or heading is absent, and `[]` BOTH for genuinely-empty and for all-bullets-unreadable, which conflates them. Resolve locally in claim.js over the section text it already holds: `null` → `unknown`; `[]` WITH ≥1 line starting `- ` in the section → `unknown` (content was seen and could not be read); `[]` with no bullets → `0`/`none`. That counts bullets only, never re-implements the strict grammar, so the issue's "never a silently wrong `0`" is met IN FULL rather than half. (2) `follow_up_numbers` GAINS `unknown` as a permitted value, used exactly when `follow_ups_filed` is `unknown` — the three degrade together, and `none` stays reserved for measured-zero. The author was right to leave it unpinned rather than invent a token: `none` there would have re-created the very conflation these fields exist to prevent. (3) CONFIRMED: `noise:` rows are NOT filings; `follow_ups_filed` counts `filed:` rows only. (4) Ordering is DOCUMENT order — the order the run filed them — never sorted. Implementer must reuse the existing parser and never duplicate its regex, choosing the seam itself (export from gap-sweep vs hoist to the shared schema module) and making it work across all four editions including the two HAND-PORTED forge trees. Output: worktree diff on `workflow/bundle-992-993-994` + `.cache/closure-delta-impl.md`.
  status: done
  result: **#993 SHIPPED.** `.cache/closure-delta-impl.md`. All three suites green, each exit code read directly: test-bundle-finalize 12-failed→**all 192 passed**; test-finalize-door 40-failures→**579 assertions**; walkthrough at FULL scope **184/184 scenarios**. The implementer also ran `test-sink-merge.js` unprompted (**1063 assertions**, exit 0) because it is the THIRD `appendClosureBlock` call site — one I had not named in the brief. VERIFIED BY ME independently: canonical↔codex `diff -q` IDENTICAL for both `claim.js` and `gap-sweep.js`; `validate-script-sync.js` exit 0; `edition-sync.js --check` exit 0. All four claim.js copies +65/−2, all four gap-sweep copies +4/−1, docs +5/−1. **MY DEGRADATION RULING WAS WRONG and the implementer was right to refuse it.** I ruled `[]` + ≥1 `- ` bullet → `unknown`; T14's `freetext` leg writes `- none`, a located section returning `[]` WITH a `- ` line, and asserts `0`/`none`. Free-text bullets are ignored BY DESIGN (`gap-sweep.js:267-281`, "must never warn") so a bullet is NOT evidence of unreadable content — the test author had told me this in their design item 1 and I ruled against it anyway. Shipped behaviour is TWO states: `null` → `unknown`×3; any array → measured, counted by `kind === 'filed'`. The implementer reported instead of editing the test to fit my brief — test custody held under pressure, which is the harder half. USER RULED on the residue: accept two states, file all-rows-malformed as its own follow-up rather than reshaping the scanner's parser late in a P3.

- item: Tests for #993 under test custody — whoever implements does not author them. New behavioral logic in the closure writer, so the test artifact belongs to tdd-guide, proven red on the pre-fix baseline before any implementation is merged. Cover the four fields, the keep-open zero case, and the `unknown` degradation. #994/#992 are prose; their guard is `generate-routing-surfaces.js --check` plus the edition byte-identity checks.
  status: in-flight
  dispatched: tdd-guide, concurrent with the Step 7 token work (disjoint files: test suites vs `required-blocks.js`; each instructed to run ONLY its own suite in the shared worktree). Brief carries the settled design from the item above. Nearest fixtures named for it: `simulate-workflow-walkthrough.js:299-360` `testKeepOpenArchiveStamp` (#333, the closest existing `## Closure` fixture), `:4936-4944` (the `--keep-worktree` lane, where a new field must stay inside the same `chore: archive` commit or the clean-tree assert reds), `test-bundle-finalize.js:970-995` (the only suite that can assert a count against actual forge calls), `test-gap-sweep.js:175-186` (T3, the `filed: #123` shape). Output: failing tests + red transcript to `.cache/closure-delta-tests-red.md` in the MAIN root.
  status: done
  result: `.cache/closure-delta-tests-red.md`. VERIFIED BY ME: `git diff --numstat` = 374 insertions / **0 deletions** across exactly three test files, and ZERO production files modified — no existing test altered, test custody held. Clean baselines PASSED first (test-bundle-finalize 160, test-finalize-door 515 assertions, walkthrough 2 scenarios). RED now, every failure `got undefined` (field-absent), never a crash or fixture fault: test-bundle-finalize.js:1004-1249 (12 failed/180 passed, 4-member bundle merge lane, three legs), test-finalize-door.js:2686-2820 (40 failures = 10 × 4 editions, T14, incl. two explicit PAIR assertions that absent !== empty), walkthrough:349-358 (`/^issues_closed: 0$/m` unmatched on the keep-open lane). Pre-existing counts exactly preserved (180−20=160, 539−24=515), proving the concurrent Step 7 edit reaches none of these suites. The zero-forge-calls control was ARMED, not assumed: a temporary mutant issuing `gh issue view` per filed row fired 4 control assertions, then was fully reverted — so it is a live control, not a vacuous one. `parseGapSection` export NOT needed by the tests (all assertions go through the finalize CLI and read the archived block) and NOT added; `gap-sweep.js:587` untouched. **SECOND ROUND — the one unpinned value is now pinned.** `follow_up_numbers: unknown` on the section-absent lane rested on my brief alone; I sent the test author back for it (custody kept with the same holder). TWO assertions, not one, and the reasoning is why it is right: the pair form alone still passes if the field is DROPPED (`undefined !== 'none'`), and the direct form alone never says what it is distinguished from. 8 new assertions = 2 × 4 editions; 579 → 587, so nothing pre-existing moved. MUTATION-PROVED on ONE edition only (`claim.js:2467` `'unknown'` → `'none'`), leaving the other three claim copies as live controls — exactly 2 failures, both new, both on T14(root), 585+2=587. VERIFIED BY ME that the revert is byte-clean: `claim.js` numstat `65 2` identical to the implementer's, `followUpNumbers: 'unknown'` restored at the site, canonical↔codex `diff -q` IDENTICAL, `validate-script-sync` exit 0. Test file additive-only (`152 0`).

- item: Dock documentation for whatever actually ships — `CHANGELOG.md` under `[Unreleased]` (watch the existing 9.10.0 double `### Fixed` heading wart while in that file), plus README/`docs/api.md` only if the change is user-visible. Provenance stays OUT of agent-facing prompts: the rule ships in the skeleton, its origin in CHANGELOG/commit messages. MUST also durably record the #992 layer-2 decision: n1-design's syntactic floor re-affirmed by the user, layer 1 shipped alone — as a comment on #992 and in the CHANGELOG.
  status: in-flight
  dispatched: self — landing docs as each issue firms rather than batching (firm work consolidates, volatile work splits). DONE so far: `CHANGELOG.md` in the worktree gained a NEW `## [Unreleased]` section (none existed; the file opened straight at `## [9.10.0]`) carrying the #994 and #992 entries, including the durable record of the layer-2 floor re-affirmation and the no-divergence ruling, plus the additive-only and mutation-proof evidence. REMAINING: #993's entry once it lands, and the `docs/workflow-state-contract.md` four-field row, which belongs to the #993 implementer. #993's CHANGELOG entry NOW LANDED too, stating the `issues_closed` semantics and why measurement forced them, plus the explicitly-not-addressed malformed half. DELIBERATELY NOT DONE: the 9.10.0 double `### Fixed` heading (confirmed real — `### Fixed` appears at both relative line 29 and 179, with `Changed` and `Removed` between). It is a published release section, unrelated to all three claimed issues, and folding it into this bundle would make this diff less attributable. Flag it for a cleanup pass; do not fix it here.
  status: done
  result: `CHANGELOG.md` `## [Unreleased]` carries all three entries (#994, #992 incl. the durable layer-2 floor re-affirmation and the no-divergence ruling, #993 incl. the measurement that forced `issues_closed`'s semantics and the explicitly-not-addressed malformed half). `docs/workflow-state-contract.md` gained the four field names in emission order (+5/−1), landed by the #993 implementer, who also verified that `docs/api.md` and `docs/architecture.md` do NOT enumerate the block's fields — so no second prose copy now disagrees. README/`docs/api.md` untouched: nothing in this bundle changes the installed command surface or a documented API shape.

- item: RUN-DISCOVERED GAP, out of scope for all three claimed issues — file as a follow-up at Step 7, do not fix here. ADR 0018 §8 item 8 asserts a duty the shipped prose never carried: "an issue filed without a `P` label is tier 99 and invisible to the sorter ... So the tier is written in the same breath as `filed: #N`. **That is the whole of it.**" The step7 survey grepped `finalize.skeleton.md` for `P0`/`tier`/`label`/`priority` and found NO priority-tier mention anywhere in Step 7. Adjacent to #994 (both govern what a filing carries) but distinct: #994's acceptance never mentions tiers, and widening it silently would be scope drift. Note it also explains a live symptom — this bundle's own three issues arrived correctly tiered, so the duty is being honored by practice, not by prose.
  status: todo
  note: PREMISE VERIFIED BY ME, BY RUNNING, not by trusting the survey — and the ADR's own wording is WRONG, so the filing must not repeat it. Measured at `claim.js:267-274` + `:276-290`: `priorityTier` returns `{tier: 99}` for an issue carrying no `P\d+` label and no configured top-tier label, and `listOpenIssues` SORTS on that tier (`at - bt || number - number`) — it does not filter or truncate. So an untiered issue is NOT "invisible to the sorter" as ADR 0018 `:264-265` claims; it sorts LAST, beneath every tiered issue, so a P0-urgent defect filed untiered ranks below a P3. File the measured consequence, under `## Measured` with this stamp, and put the ADR's "invisible" phrasing nowhere — this is the exact failure class #994's contract exists to stop, and the filing must obey the contract it ships.
  status: in-flight
  dispatched: self — BODY DRAFTED under #994's own typed contract at `.cache/followup-tier-duty.md`, ready for Step 7 of finalize to file verbatim. Its `searched:` line records a probe I actually ran: `gh search issues "priorityTier"` → 2 hits both closed (#984 ADR build parent, #150 GitLab/Gitea `priority_top_tier_labels` port bug), `"tier 99"` → 1 closed hit (#99, unrelated). No open duplicate. The measured/hypothesis split is real, not decorative: the four code facts sit under `## Measured` stamped `@c62e8a3f`, while "runs tier only because they happen to know the ADR" is labelled hypothesis with its falsifier named (check whether any archived run's `filed:` issue landed unlabelled).
  status: done
  result: **FILED AS #995** (bug, area:workflow-router, P2). Verified per #992's newly-shipped rule: issue exists, state=OPEN, **body_len=2832**, labels `bug,area:workflow-router,P2`.

- item: THIRD RUN-DISCOVERED GAP, owner-deferred rather than found late — file at Step 7. A `## Run gaps` section whose rows ALL fail the strict grammar stamps `follow_ups_filed: 0` instead of `unknown`, because `parseGapSection` returns the same empty array for that as for a genuinely empty section. Surfaced while building #993; the user ruled two states ship and this lands as its own slice rather than reshaping a parser the scanner owns late in a P3.
  status: in-flight
  dispatched: self — BODY DRAFTED at `.cache/followup-malformed-gap-rows.md`. **I CAUGHT MYSELF VIOLATING #994's OWN CONTRACT WHILE WRITING IT**: I first wrote a `searched:` line claiming "parseGapSection → 0 hits" WITHOUT running the probe. Running it returned **#726** (closed) — "parseGapSection grammar rejects any `## Run gaps` sample containing `)` — a swept manual gap with a paren is silently unmappable" — and **#848** (closed) — "Four conditions say 'anchor absent' when the value was present and failed a shape check", which is THE SAME CONFLATION CLASS generalized and is the design precedent to read first. Both corrections landed in the draft, and #726 upgraded a `## Hypothesis` claim into `## Measured`: unmappable rows are documented fact, not speculation, so "no instance observed" was narrowed to the new stamp specifically. This is the contract catching its own author within an hour of shipping.
  status: done
  result: **FILED AS #997** (enhancement, area:scripts, P3). Verified per #992's rule: exists, state=OPEN, **body_len=4159**. Filing note worth keeping: the first `gh issue create` attempt FAILED for all three on `could not add label: ' area:scripts' not found` — `gh` does not trim spaces after commas in `--label`. It failed BEFORE creating anything (confirmed by listing open issues: still only 992/993/994), so there were no partial bodyless issues to clean up. A loud failure this time — but it is the same create-path that produced the silent empty-bodied filing #992 was filed about, which is precisely why the verification now runs per filing rather than per audit.

- item: Verify before claiming done — walkthrough at FULL scope (`node scripts/simulate-workflow-walkthrough.js` exit 0), then the chains via `kaola-workflow-run-chains.js --project` at finalize, NOT `npm test` (npm test writes no receipt). run-chains prints nothing without `--json`; read the receipt, not the wrapper. A prose+script diff touching editions fails closed to all four chains. Confirm any new guard is mutation-proven — a green suite is not proof a guard is armed.
  status: in-flight
  dispatched: self — SUITE-LEVEL VERIFICATION IS COMPLETE; what remains is the four-chain receipt, which belongs to finalize. Green as of now, each exit code read directly and never after a pipe: `test-bundle-finalize` 192 · `test-finalize-door` 587 · `simulate-workflow-walkthrough` **184/184 at FULL scope, not a shard** · `test-sink-merge` 1063 · `test-route-reachability` 331 · `generate-routing-surfaces --check` 18 surfaces byte-match · `validate-script-sync` 0 · `edition-sync --check` 0. The last four I ran myself. BOTH new guards are mutation-proven armed, not merely green: the 7 Step 7 tokens each redded exactly their own mutant, and `follow_up_numbers` redded on a single-edition mutant with the other three editions as live controls. This diff IS edition-touching (routing skeleton + 4 claim.js copies + 4 gap-sweep copies), so `run-chains.js` correctly fails closed to ALL FOUR chains at finalize — expect ~11 min, and read the RECEIPT, not the wrapper, since run-chains prints nothing without `--json`.
