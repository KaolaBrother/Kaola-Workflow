# Move role-profile receipt hashes out of agent-visible instructions (#1073)

Worktree: `/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/issue-1073`, branch `workflow/issue-1073`, base `2b1af448`.

Measured premises (this session): `.cursor/` and `.grok/` agent trees are gitignored renders of `renderRuntimeRole`; tracked profiles are `agents/*.md` (Claude) and `plugins/kaola-workflow*/agents/*.toml` (Codex x3). `agents/generated-agent-manifest.json` already carries `behavior_sha256` + `resolved_profile_sha256` per runtime/variant/role and is the sidecar. `./install-all.sh --check` never inspects installed agent profiles (per-runtime installers become `PLAN` rows); the only producer of `agent_resolved_profile_hash_mismatch` is the Codex kernel `agentProfileContract`, whose installed-profile detection is already duplicated by `profile_bytes_mismatch` and `manifest_file_hash_mismatch`. Claude's installer already records `sha256(dest)` in `.kaola-workflow-agent-manifest`. Cursor/Grok installers verify nothing; their in-body hash served only the G4 tests.

Design: drop the whole `<!-- runtime-adapter -->` appendix (hash lines and both bullets) from every render and the three hash lines from Claude frontmatter; add `adapter_capabilities_sha256` to the generated manifest so it holds the triple; `resolved_profile_sha256` becomes a plain digest of the profile bytes (no zeroed-self slot exists anymore). Claude installer verifies source bytes against the sidecar and installed bytes against the inherit-rewritten source; Codex kernel retires `agentProfileContract`, installer manifest drops `profile_contracts`, preflight drops the appendix checks, keeping `files` digests + bundled-source byte compare as the fail-closed path.

- item: Candidate implementation: generator stops rendering the appendix and frontmatter hashes, manifest sidecar carries the triple, Claude installer + Codex kernel/installer/preflight re-source verification to the sidecar/receipt, all test and validator pins re-pointed (no 64-hex line in any profile body; corruption still fails closed), mirrors regenerated, focused suites green.
  status: done
  dispatched: sidekick (native Devin route), 2026-09-12; output lands as an uncommitted diff in the worktree plus focused-suite command output in the handback.
  result: PASS — uncommitted diff in the worktree, 57 files (+325/−1090), no new files. Orchestrator read the generator, install.sh, kernel, preflight, installer, and every test/validator diff. Appendix and frontmatter hashes gone from all 42 renders; `agents/generated-agent-manifest.json` carries `behavior_sha256` + `adapter_capabilities_sha256` + `resolved_profile_sha256` per runtime/variant/role; Claude installer verifies source digest against the sidecar and installed bytes against the inherit rewrite; Codex kernel `agentProfileContract` and install-manifest `profile_contracts` retired, `profile_bytes_mismatch` + `manifest_file_hash_mismatch` kept. Focused suites green: generate --check, edition-sync --check, validate-script-sync, validate-vendored-agents, validate-kaola-workflow-contracts, bash -n install.sh, test-install-model-rendering, test-issue-1054-role-redesign, test-runtime-agent-architecture (A10 isolation compare widened to content+adapter digest — accepted), test-issue-1055 (baseline recaptured), test-cursor/grok/kimi/opencode-edition, codex walkthrough. Live one-byte mutation of an installed Codex profile → preflight `profiles_stale` with `profile_bytes_mismatch` + `manifest_file_hash_mismatch`. `grep -E '[0-9a-f]{64}'` over all rendered profiles returns nothing.

- item: Documentation docking (README, docs/api.md, CHANGELOG [Unreleased], docs/agents-source.md, docs/architecture.md, docs/runtime-capabilities.md, docs/conventions.md) and the required chains: `npm test`, `npm run test:kaola-workflow:editions`, `node scripts/simulate-workflow-walkthrough.js`.
  status: done
  dispatched: sidekick (native Devin route), 2026-09-12; orchestrator-authored doc text applied in the worktree; chain output in the handback.
  result: PASS — 8 doc files (+46/−26): CHANGELOG [Unreleased] bullet, agents-source/architecture/conventions/runtime-capabilities/api rewritten to the sidecar; ADR 0020 and D-696-01 get one superseded-in-part sentence; README needed no change. Chains: `npm test` PASS (4 chains), `npm run test:kaola-workflow:editions` PASS (9 suites), `node scripts/simulate-workflow-walkthrough.js` PASS (178/178), `node scripts/kaola-workflow-run-chains.js --output .cache/chain-receipt.json` PASS (unwaived; worktree `.cache/chain-receipt.json`, bound to the pre-commit tree hash — recompute at publication).

- item: Orchestrator review of the frozen diff (correctness, corruption-detection semantics, user-profile preservation, no new generic gate) and commit on `workflow/issue-1073`; stop at candidate readiness — no global install, merge, close, or release.
  status: done
  dispatched: self; commit lands on `workflow/issue-1073` in the worktree.
  result: PASS — verdict: diff accepted (subtraction only, no new files, corruption detection preserved via retained byte/digest paths, user-owned Claude profiles still detected by the TSV manifest digest, no new generic gate; one accepted test widening in A10). Committed as `5bd758d0` on `workflow/issue-1073` (65 files, +371/−1116), worktree clean. Candidate ready; stopped before install/merge/close/release per supervision instruction. Premise correction carried forward: `./install-all.sh --check` never inspected agent profiles (per-runtime installers are `PLAN` rows), so the acceptance's mismatch-from-`--check` bullet is satisfied by the runtime's actual detection path (Codex preflight `profile_bytes_mismatch`/`manifest_file_hash_mismatch`; Claude installer `agent_source_digest_mismatch`/`agent_installed_bytes_mismatch`).
