# Finalization - Summary: bundle-642-643-644

## Delivered
Bundle of three adaptive-machinery issues (all_or_nothing): #642 durable node-to-node information channel (## Node Briefs grammar frozen under plan_hash with brief_unknown_node/brief_duplicate_node walls; dispatch.goal_line + dispatch.upstream_evidence on all three openers; close-time consumed-proof upstream_read nonce echo with typed upstream_not_consumed refusal hard for implement-role consumers, universal n/a skip; record-evidence key re-injection; resume re-hydration from cached envelopes; relay prose ×6 surfaces via the generation seam). #643 per-role evidence-recording contract (8 ROLE_TOKEN_REGISTRY rows; registry-driven close shape checks; evidence-contract sections in 8 agent profiles ×4 editions + compact-plan posture with authoring syntax in workflow-planner ×4; manifest-derived role-kind enumeration ×6; future-agent wall + toml needle mirrors, fail-closed on a missing tools manifest). #644 scheduler gate-window residuals (live kind:'gate' counted at the co-open holds, the G4 merge fence, AND the fused advance — the third seam adversary-surfaced and fixed in-run; testConsumedExtra threaded into the R2b predicate).

## Files Changed
34 tracked files modified + 3 decision records created (git diff origin/main..HEAD on branch workflow/bundle-642-643-644, commit 2008a913): the two GENERATED_AGGREGATORs ×4 editions, 2 test surfaces + walkthrough, 6 validators/walls, 33 agent-profile files, routing skeleton + required-blocks + 6 generated surfaces, CHANGELOG + 4 docs + 3 D-records, plus workflow state/evidence.

## Test Coverage
No formal coverage gate in this repo. Assertion counts: test-adaptive-node 1610, test-adaptive-handoff 153, route-reachability 329, agent-profile-parity 33, walkthrough sentinel green.

## Final Validation Evidence
Four-chain receipt (self-host gate): .cache/chain-receipt.json — headSha 2008a913 (HEAD), claude/codex/gitlab/gitea all exit 0, serial, no waivers, no timeouts. Four adaptive barrier gates: --resume-check 0, --gate-verify 0, --barrier-check 0, --verdict-check 0. Validation reuse boundary: the receipt covers code/test impact through the final repair close (n8 re-review) and the n10 docs tree; the only post-receipt writes are workflow-state band (finalize bookkeeping), outside the code-relevant hash by construction.

## Documentation Docking
DOCKED — .cache/doc-docking.md

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none — all validation green at finalize) | | | | |

## Follow-Up Items
None. Operator mandate for this run: zero follow-ups — every gate/adversary finding (5 repairs: consumed-proof n/a carve-out; brief_duplicate_node wall; fused-advance gate_live hold; planner-facing briefs syntax ×4; security-reviewer enumeration example ×6; fail-closed manifest guard ×4 validators) was fixed and re-verified IN-RUN; both code-reviewer gates and the adversarial-verifier re-ran to verdict: pass / findings_blocking: 0 afterward.

## Run gaps
- in_run_repair (n2-validator): noise: gate-routed in-run repair (brief_duplicate_node wall), fixed and re-verified in-run by the n7 re-review — no residual defect, zero-follow-up mandate satisfied
- in_run_repair (n3-adaptive): noise: two gate/adversary-routed in-run repairs (n/a carve-out; fused-advance gate_live hold), fixed and re-verified in-run by n7+n9 re-runs — no residual defect
- in_run_repair (n4-agents): noise: gate-routed in-run repair (briefs authoring syntax sentence ×4), fixed and re-verified in-run by the n8 re-review — no residual defect
- in_run_repair (n5-prose): noise: gate-routed in-run repair (security-reviewer enumeration example via the generation seam), fixed and re-verified in-run by the n8 re-review — no residual defect
- in_run_repair (n6-enforcement): noise: gate-routed in-run repair (fail-closed manifest guard ×4 validators), fixed and re-verified in-run by the n8 re-review — no residual defect
- in_run_repair (n7-cr-engine): noise: gate re-opens are the repair choreography itself (verdict re-issued fresh after upstream repairs; final verdict pass/0) — not a product defect
- in_run_repair (n8-cr-surface): noise: gate re-opens are the repair choreography itself (verdict re-issued fresh after upstream repairs; final verdict pass/0) — not a product defect
- in_run_repair (n9-adversary): noise: adversary re-run after its own surfaced seam was fixed in-run (final verdict pass/0) — not a product defect
- in_run_repair (n11-finalize): noise: sink parks during the sanctioned mid-run repair window (reopen-complete-node card §6 full-reset; the advance auto-opened the sink between repairs) — bookkeeping, not a product defect

## Closure Decision
None needed — closure scan found no deferred items, no unresolved conflicts, no partial implementation, no open review follow-ups, no user-decision items. All three issues' acceptance criteria are covered by the pinned tests + gate evidence (V1–V6 per #642/#643; A1/A2 + third-seam fix per #644).

## Commit And Push
Pending final Git gate (worktree merge sink; final hash reported after push).

## GitHub Issue
Pending sink: close 642, 643, 644 together (all_or_nothing).

## Roadmap
Pending cmdFinalize closure (removes issue-642/643/644 sources, regenerates ROADMAP.md once).

## Archive
Pending cmdFinalize (kaola-workflow/archive/bundle-642-643-644/).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | subagent-invoked (n10-docs node) | .cache/n10-docs.md | |
| documentation docking | invoked | .cache/doc-docking.md | |
| final-validation fix executors | N/A | — | all validation green at finalize; the 5 in-run repairs ran inside the plan via reopen-node + same-agent nudges (gate evidence) |
| roadmap refresh | pending cmdFinalize | kaola-workflow/ROADMAP.md | |
| archive completed folder | pending | | |
| finalize (n11-finalize) | main-session-direct | .cache/n11-finalize.md | |
| final commit and push | ready | git status clean on branch; upstream push pending sink | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE
