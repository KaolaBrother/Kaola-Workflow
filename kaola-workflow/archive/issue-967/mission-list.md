# #967 — the CLAUDE.md line rule becomes a recommendation that notifies, never a build failure

Owner ruling, taken in conversation this run: **no hard failure at any size** — nothing about
CLAUDE.md's length may red a chain — and it reaches **both** surfaces, this repo's own guard and the
consumer guidance `workflow-init` injects into other people's repos. Above the recommended size the
agent tells the user and offers to trim; the build stays green. This is the same shape as the
project's own "nothing refuses": a gate justified by "the file might get long" argues against the
design's premise rather than for a guard.

- item: Convert the hard assert to a non-fatal warning in all THREE live validator copies — `scripts/validate-workflow-contracts.js:339` (claude chain), `plugins/kaola-workflow/scripts/validate-workflow-contracts.js:339` (byte-identical copy, enforced by `validate-script-sync.js`), and `scripts/validate-kaola-workflow-contracts.js:184` (the CODEX chain's own validator — a third copy, easy to miss because the two filenames differ by one word). While there, fix the count itself: `split(/\r?\n/)` counts the trailing empty element, so today's "200" is really 198 physical lines. Warn on physical lines, so the number in the message is the number `wc -l` prints.
  status: in-flight
  dispatched: self — edits land in `$WT/scripts/validate-workflow-contracts.js`, `$WT/plugins/kaola-workflow/scripts/validate-workflow-contracts.js`, `$WT/scripts/validate-kaola-workflow-contracts.js` (worktree `.kw/worktrees/issue-967`, branch `workflow/issue-967`). Measured first: **no test pins the throwing behaviour**, so nothing dies with this mechanism. The one hit that looked like a pin — `scripts/test-run-chains.js:1514` — is a `rootReadSurfaces` DIFF-SCOPING list, not a behaviour assertion; CLAUDE.md stays on it because the codex validator still reads the file, but its trailing comment says "line-count" and must stop implying an assertion.
  result: DONE — the assert is gone from all three (`git grep` for the old message returns ZERO outside archive/CHANGELOG); each now counts PHYSICAL lines (`replace(/\n$/,'')` before splitting) and writes a non-fatal `notice:` to stderr above 200. **Mutation-proven at three sizes, one validator at a time: 200 → no notice, exit 0; 201 → notice, exit 0; 260 → notice, STILL exit 0.** Honest scope of that proof: **2 of 3 proven by direct run.** `plugins/kaola-workflow/scripts/validate-workflow-contracts.js` exits 1 at EVERY size with no notice — measured against HEAD and it does the same there, failing on `commands/kaola-workflow-finalize.md is missing` because its `root` resolves to `plugins/kaola-workflow/`. It is not standalone-runnable in this layout and is not a package.json chain step; its correctness rides on byte-identity with the canonical, which `diff -q` confirms. Also retitled the stale `rootReadSurfaces` comment in `test-run-chains.js:1514`.

- item: One wording across every prose surface. Authoring surface is `templates/routing/init.skeleton.md` (3 sites: 68, 204, 534) — edit the skeleton and regenerate, NEVER a rendered surface; `generate-routing-surfaces.js --check` currently reports all 18 byte-matching. Site 68 already carries a two-tier rule ("Target size: under 200 lines. Hard limit: if the result would exceed 240 lines, stop and summarize") — under the ruling the 240 stop becomes notify-and-offer too, since the ruling is that nothing stops. Also this repo's own `CLAUDE.md:197` Maintenance line and `README.md:1180`, which states the workflow "enforces context discipline".
  status: done
  dispatched: self — skeleton `templates/routing/init.skeleton.md` (3 sites) + `CLAUDE.md` + `README.md` in `$WT`, then `generate-routing-surfaces.js --write`.
  result: DONE. The regeneration carried its own control: `--check` FAILED first with `6 surface(s) drifted from the skeleton`, then PASSED `all 18 surfaces byte-match` after `--write` — so the skeleton edit demonstrably propagated rather than being asserted to. Measured after: ZERO hits repo-wide for `hard limit`/`240 lines` (the stop is gone from every surface), ZERO for `enforces context discipline` and `200-line target`, and the new wording present in all 6 rendered surfaces plus the skeleton. README's "enforces" became "encourages", which was the word that made the docs disagree with the ruling.

- item: Prove the conversion, per validator and one at a time. A guard is evidence only once mutation-proven, and the mutation here is the inverse of the usual: plant a CLAUDE.md ABOVE the threshold and show each validator now WARNS and still exits 0, where it previously threw. Three separate proofs — an N-site mutant would prove one of them converted, never three. Also check whether any existing test pins the throwing behaviour: if one does it dies with the mechanism and is never rewritten to keep passing.
  status: done
  dispatched: self — plant-and-revert against `$WT/CLAUDE.md`, snapshot first to `scratchpad/CLAUDE.md.snapshot`.
  result: DONE — proof table at three sizes per validator is in `finalization-summary.md` § Test Coverage: 200 → silent/exit 0, 201 → notice/exit 0, 260 → notice/**still** exit 0. **2 of 3 by direct run**, the plugin copy by byte-identity for the reason recorded in item 1. **No test pinned the throwing behaviour**, so nothing died with the mechanism and nothing was rewritten to keep passing. `CLAUDE.md` byte-restored after planting (`git diff --quiet` clean). Live demonstration: the repo's own CLAUDE.md now sits at 199 lines — the length that threw at session start — and passes silently.

- item: Dock and verify — `CHANGELOG.md` under `[Unreleased]`, `docs/` wherever the rule is described, then the chains. This diff touches `plugins/` and the codex validator, so chain selection fails closed to all four; the walkthrough runs at full scope, not the 1/12 shard.
  status: in-flight
  dispatched: self — `CHANGELOG.md` `### Changed` (not `### Fixed`: this removes an enforcement, it does not repair one), `.cache/doc-docking.md` = DOCKED, gap scanner returned `sweptClasses: []` and the `--check` gate exits 0. Impl committed as `e4522be9`. Full-scope walkthrough already green (209/209). Four chains running in background; receipt lands at `kaola-workflow/issue-967/.cache/chain-receipt.json` and must bind `headSha e4522be9` with each chain's OWN exit code read, never the wrapper's.
