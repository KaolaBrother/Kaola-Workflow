# Finalization - Summary: issue-673

## Delivered
Heading-match parity fix (post-#670 sibling). `schema.locateSection` selected headings via a loose
`startsWith('## ' + heading)` prefix while the hash-covering classifier uses an anchored
`^##\s+<heading>\s*$` — diverging on forward-suffix decoys (`## Node Ledger Extra`) and reverse
whitespace variants (`##  Node Ledger`, `##\tNode Ledger`), a `plan_hash` wedge via the heading channel.
`locateSection`'s opener is now the classifier's anchored regex (identical escaping) and its terminator
is the classifier's `^##\s`; the `i > 0` line-0 guard is retained (structurally unreachable / inert on
frozen plans). `classifier.js` untouched.

## Files Changed
- scripts/kaola-workflow-adaptive-schema.js (+ plugins/kaola-workflow, gitlab, gitea copies — byte-identical ×4) — locateSection heading anchor + terminator
- scripts/test-adaptive-node.js — T6e heading-decoy regression (forward-suffix / two-space / tab / line-0 + end-to-end resume-check)
- CHANGELOG.md — `[Unreleased]` entry for #673

## Test Coverage
Not a coverage-gated repo. `node scripts/test-adaptive-node.js` → 1815 assertions (8 new T6e);
`node scripts/simulate-workflow-walkthrough.js` → "Workflow walkthrough simulation passed";
`node scripts/edition-sync.js --check` → clean (4 adaptive-schema copies byte-identical).

## Final Validation Evidence
Self-host four-chain receipt (`.cache/chain-receipt.json`) stamped by `run-chains.js --project issue-673`
after the feature commit. Per-node evidence: `.cache/n1-heading-parity.md` (RED 8-fail → GREEN),
`.cache/n2-review.md` (opus, verdict pass, 0 blocking; line-0 guard ruled sound),
`.cache/n3-adversary.md` (opus, NOT-REFUTED; 80,000+ fuzzed constructions, zero divergence). NOTE:
reviewer/adversary gates ran on OPUS (fable out of credits; operator chose opus).

## Documentation Docking
DOCKED — sole doc surface is the CHANGELOG `[Unreleased]` entry; no public interface/API/env/architecture
changed (classifier oracle untouched). Evidence: this summary + `.cache/n4-finalize.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
None. The adversary's A1 (pre-existing DOCUMENTED ambiguous-channel first-hit behavior, schema.js:1130-1135)
is out of the heading-match channel, inert on frozen plans, and not a new defect — not filed.

## Run gaps
(sweep empty — no in-run repair, no deferred/red chain, no seeded gap; section omitted per template.)

## Closure Decision
No deferred item, unresolved conflict, partial implementation, or value/standing decision. The fix is
complete and both gates passed (review + 80k-fuzz adversary). Close #673.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#673 — to be closed by the merge sink after the four-chain merge gate.

## Roadmap
Regenerated by cmdFinalize: removes .roadmap/issue-673.md, retains #677/#678/#679.

## Archive
pending (cmdFinalize Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| n1-heading-parity (tdd-guide) | invoked | .cache/n1-heading-parity.md | |
| n2-review (code-reviewer/opus) | invoked | .cache/n2-review.md | fable out of credits; operator chose opus |
| n3-adversary (adversarial-verifier/opus) | invoked | .cache/n3-adversary.md | fable out of credits; operator chose opus |
| doc-updater | skipped | CHANGELOG-only, no checklist doc impact | sole doc surface is CHANGELOG, updated inline in the finalize node |
| documentation docking | invoked | this summary + .cache/n4-finalize.md | |
| final-validation fix executors | N/A | — | no final-validation failure |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | cmdFinalize Step 8b |
| archive completed folder | pending | | cmdFinalize Step 8b |
| final commit and push | ready | git status/upstream check | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE

## Attestation
claim_planner_attested: attested
finalize_contractor_attested: attested
