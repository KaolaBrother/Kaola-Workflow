# Close #980 (sink stages the journal then red-stops, leaving it in an unnamed tmpdir) and #981 (retired support scripts survive an opencode/kimi --uninstall)

- item: Premise-check #980 against the current tree — does a red-stop after a successful stage and the forced worktree removal, but before landing, actually leave the staged copy unnamed in operator-visible output? Read scripts/kaola-workflow-sink-merge.js: both routes stage <wt>/kaola-workflow/<project>/ into kw-wtsync-*, #978 made a THROWING stage refuse, sinkLandStagedUnion's existsSync destination probe and the #707 h landing pins are load-bearing and must not move. Name the exact red-stop exits that sit between stage and land; if there are none, the issue is refuted.
  status: done
  dispatched: self — subagents declined this run; findings land inline in this file's result field
  result: CONFIRMED, and broader than filed — the corner exists on BOTH routes, not one. Route 1 (--sink merge step, stage at :2183, removal at :2188, land at :2300-2306) red-stops without naming wtStageDir at :2224 post_rebase_tests red, :2249 chains_red in the FF loop, :2267 non_fast_forward, and :2289 rebase_conflict (bare stderr, no envelope). Route 2 (sinkMergeMain, stage at :3271, removal at :3291, land at :3380-3386) has the identical four at :3321, :3340, :3353, :3372 (bare exit, no envelope at all). Both also pass an uncaught `git checkout` execFileSync (:2218, :3300) between removal and landing. In every one the worktree is already gone, so the kw-wtsync-* stage is the only surviving journal and no envelope carries its path.

- item: Premise-check #981 by independent census — which support scripts have opencode or kimi actually retired? Use git log --no-renames --diff-filter=D --name-only and git log -L over the generators; NEVER read the installer's own array, that is the flaw that produced #977. If the retired set is empty the issue is refuted as unobservable; if non-empty, that set is the blocklist's content.
  status: done
  dispatched: self — subagents declined this run; findings land inline in this file's result field
  result: CONFIRMED. Install path already converges (install-opencode.sh:505-519 enumerates the destination and intersects against what was just deployed) — that is why a reinstall heals it. Uninstall (install-opencode.sh:445-460) removes strictly by the CURRENT manifest, so a retired name survives. Census by two independent routes that agree — `git log -L` over SUPPORT_SCRIPTS in kaola-workflow-install-manifest.js, cross-checked against `git log --diff-filter=D` over scripts/ — yields 13 retired canonical bases: autopilot (retired 2026-06-26), parallel-batch (07-02), fast-advance/full-advance/phase4-advance (07-19), and repair-state/plan-validator/next-action/commit-node/adaptive-handoff/adaptive-node/replan/task-mirror (07-31). Edition bounds: opencode ships support scripts since 2026-06-19 (74da6a5b) so it carries all 13; kimi since 2026-07-17 (f6dbf40d) so it carries 11 — autopilot and parallel-batch were retired before kimi existed. All 13 had ports in BOTH gitlab and gitea, so the deployed basename is forge-dependent: canonical for github, kaola-<forge>-workflow-<X>.js otherwise (pure prefix rule; FORGE_RENAME_OVERRIDES touches only sink-pr, none of the 13).

- item: Fix #980 so a run that stages the journal and then stops before landing leaves the operator able to find the staged copy — naming the path in the failure output, landing into the archive band before stopping, or deferring removal until the landing commits are all admissible; specify the result, not the method. Must not disturb the #707 h pins, sinkLandStagedUnion's existsSync probe, or arms (e)-(n) of scripts/test-sink-merge.js.
  status: done
  dispatched: self — implemented in the worktree at .kw/worktrees/bundle-980-981 on branch workflow/bundle-980-981
  result: Chose "name the path in the failure output", structurally rather than per-stop. A module-scoped note (stagedJournalDir + armStagedJournalNote/disarmStagedJournalNote + a process.on('exit') hook writing to STDERR) is armed where the stage succeeds and disarmed where the landing completes, so it covers EVERY exit in the window — including the two uncaught `git checkout` throws that an enumeration of the eight typed stops would have missed. Nothing touches sinkLandStagedUnion, the existsSync probe or the #707 h pins; stdout stays byte-identical so no envelope consumer moves. Landed in all four sink copies: scripts/kaola-workflow-sink-merge.js, plugins/kaola-workflow/scripts/ (byte-identical mirror), and the hand-ported plugins/kaola-workflow-gitlab/ and plugins/kaola-workflow-gitea/ copies — each with arm=2/disarm=2, verified by count.

- item: Fix #981 so a support script the edition retired is gone from a deployed install after an uninstall, following the existing explicit-declaration blocklist model #977 established for commands/role skills/managed agents/hooks. It must stay a BLOCKLIST — a namespace sweep of the support-scripts directory reintroduces the defect #973 removed. Both editions (install-opencode.sh, install-kimi.sh).
  status: done
  dispatched: self — implemented in the worktree at .kw/worktrees/bundle-980-981 on branch workflow/bundle-980-981
  result: Added RETIRED_SUPPORT_SCRIPTS + remove_retired_support_scripts() to both installers, following the RETIRED_HOOKS declaration model #977 established. opencode carries all 13 censused names; kimi carries 11 (autopilot and parallel-batch were retired before the edition existed, 2026-06-26 and 07-02 vs kimi's 07-17). Each base is removed alongside its two forge-port spellings, since the deployed basename depends on the forge the home was installed under and that need not be the forge the uninstall is invoked with; every name still passes is_plain_basename before becoming a delete path. The call sits OUTSIDE the manifest guard on purpose — a retired script is absent from the manifest by definition, so its removal must not be conditional on the manifest being readable. Stays a blocklist; no namespace sweep.

- item: Author the tests for both fixes under test custody — whoever implements does not author its tests; prove each fails on the pre-fix baseline (mutation-prove via a scratch mirror, never git checkout --).
  status: done
  dispatched: self — CUSTODY DEVIATION, stated rather than hidden: subagents are declined this run, so the split between implementer and test author was not available. Intent preserved the other way — each oracle authored from the issue's SPECIFY-THE-RESULT clause before the implementation it judges, then mutation-proven.
  result: Four arms, every one mutation-proven via an explicit backup copy (never git checkout --). (#980 w) in scripts/test-sink-merge.js drives a red post-rebase gate on a worktree-postured run and pins DISCOVERABILITY as a disjunction — reachable from a named path OR already in the operator band — so it pins the result, not the method; mutant (both arms stripped) fails on the pin itself with premises intact. (#980 w2) sweeps all four sink copies counting arm/disarm against the canonical's own stage/landing site count; mutant (one gitlab arm stripped) caught. (#981 S1c) in test-opencode-edition.js and (P1c) in test-kimi-edition.js each plant two real retired names AFTER the seed install, with a positive control that the uninstall removed the current manifest set; mutant (removal call dropped) caught on both. Each also carries a BLOCKLIST scope clause pinned with a user-authored .js — the load-bearing half, since a non-.js file survives even install.sh's *.js sweep; mutant (namespace sweep) caught on both. Suites: sink 1082 assertions, opencode 663, kimi 627, all green.

- item: Adversarially review both landed fixes — every prose/comment claim measured against the shipped tree, guards mutation-proven, and the review looking for the fix stranding something else the way #973's own fix stranded kaola-role-issue-scout.
  status: done
  dispatched: self — every claim re-measured against the shipped tree rather than re-read
  result: ONE FALSE CLAIM FOUND IN MY OWN WORK, corrected. (w2)'s disarm assertion justified itself with "or a COMPLETED sink prints a warning about a staged copy it already landed and deleted" — measured FALSE: dropping both disarm calls leaves (#707 h) GREEN, because the landing deletes the stage and the handler's existsSync probe then finds nothing. The disarm IS load-bearing, but for the narrower case that probe cannot see (a cleanup rmSync that fails leaves the directory on disk after a successful landing). Both the (w2) message and the (#707 h) comment now state what was measured, including that (#707 h) does not arm the disarm. Claims verified, not assumed: the two post-removal checkouts at :2246 and :3330 really are uncaught (read, both outside any try); process.on('exit') really does fire on both the caught-terminal and genuinely-uncaught throw paths (run, both print); bash 3.2 really does perform the ${base/#...} prefix expansion and leaves non-matching names alone (run on /bin/bash 3.2.57); array counts really are 13 and 11 (sourced and counted, not eyeballed). Added a false-alarm control to (#707 h) — a completed worktree sink must not warn about a journal it landed. Nothing stranded: edition-sync --check 8/8 in parity, validate-script-sync OK across 27 byte-identical groups, generate-routing-surfaces 18/18.

- item: Run the verification the changes actually demand — the walkthrough at full scope for any workflow change, plus the edition suites for an edition-touching diff; then hand off to /kaola-workflow-finalize bundle-980-981.
  status: done
  dispatched: self — verification run inline, then the finalize hand-off
  result: Walkthrough at FULL scope: 210/210 scenarios, 0 failed (not the rotating 1/12 sample). test:kaola-workflow:claude: real exit 0, no failing suite. Sink suite 1083 assertions. Edition suites: opencode 663, kimi 627. edition-sync --check, validate-script-sync and generate-routing-surfaces --check all clean. Docs docked: CHANGELOG [Unreleased] Fixed entries for both issues; docs/opencode-edition.md and docs/kimi-edition.md updated for RETIRED_SUPPORT_SCRIPTS and the blocklist scope. docs/api.md deliberately NOT touched — no export changed and no doc describes the staging mechanism. Chain receipt at .cache/chain-receipt.json binds headSha aa7a4944 with scope decision all-four (reason edition_coupling, 12 changed files): claude/codex/gitlab/gitea all exitCode 0, accepted_red false, no timeouts, one attempt each. Finalize transaction ran green — mirror mirrored, ledger_compare pass, closure_invariants ok, validation chains_green — archiving to kaola-workflow/archive/bundle-980-981 and deferring the archive commit to the sink.
    THIS ITEM'S OWN RECORD WAS MALFORMED and the finalize transaction caught it: it carried a `result` while still `status: in-flight` (reported as mission_list.outcome_while_not_done: [33]), which is precisely the defect #970 exists to detect. The work was in fact complete; the record was not. Corrected here rather than argued away.
