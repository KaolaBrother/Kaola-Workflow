# #933 — a claim must not write run state into a reserved directory (`.roadmap`, `archive`)

- item: Re-verify the issue's premise against HEAD `406b5639` — reproduce R1 (`claim --project .roadmap --issue N`) and R2 (`startup` where the roadmap source carries `workflow_project: .roadmap`) in a throwaway repo with `KAOLA_WORKFLOW_OFFLINE=1`, and confirm both still land `workflow-state.md`/`.cache/` inside `kaola-workflow/.roadmap/` at exit 0. #932 shipped after the issue was filed, so the repro must be re-run, not trusted. Prior evidence: `kaola-workflow/archive/issue-932*/.cache/repro-932.md`. Also measure the exact call sites: `isSafeName` at `scripts/kaola-workflow-active-folders.js:14-18`, `isReservedWorkflowDirName` at `scripts/kaola-workflow-claim.js:2460` (issue claims exactly ONE call site, `archiveProjectDir` `:2487` — count it), and `projectNameForIssue` at `scripts/kaola-workflow-claim.js:293-300`.
  status: done
  dispatched: self, inline. Repro script + throwaway repo under `kaola-workflow/issue-933/.cache/`; measurement written to `kaola-workflow/issue-933/.cache/repro-933.md`.
  result: `kaola-workflow/issue-933/.cache/repro-933.md` — REPRODUCED at `406b5639`. R1/R2/R5 all `acquired` at exit 0 writing into the reserved dir; R2 needs no operator flag (name travels in `workflow_project:`). Call sites of `isReservedWorkflowDirName` = exactly ONE (`archiveProjectDir`), confirmed in all four editions. Issue's line numbers shifted post-#932: predicate now `claim.js:2536`, its call `:2563`; `projectNameForIssue:293-300` and `isSafeName` `active-folders.js:14-18` unmoved.

- item: Put the value call to the user — refuse a reserved name at the claim site, or resolve around it and proceed. The refusal count outside the destruction class is deliberately zero and nothing is destroyed here, so this is the owner's decision, not a checkable fact. Ask in conversation; do not choose it unilaterally.
  status: done
  dispatched: self — asked the owner in conversation, three framed options (resolve-and-report / refuse-at-claim-site / split-by-who-typed-it). Ruling to land inline here.
  result: **OWNER RULED: resolve around it, report the swap.** The claim succeeds in a legitimate folder (`issue-<N>`) and the envelope names the reserved name it declined to use. The refusal count outside the destruction class STAYS ZERO — this is routing, not a stop. Refusal at the claim site is DECLINED; do not re-file it. The report is what keeps the substitution from being silent: delete the verdict, keep the measurement.

- item: Survey the four editions for the claim-site surface that needs the fix — `scripts/` canonical plus the gitlab/gitea/codex ports — and record per-edition whether `isReservedWorkflowDirName` exists, where the claim-site name resolution lives, and whether the file is GENERATED, COMMON-byte, or a DIVERGENT hand-port. Cross-edition mechanics: `docs/conventions.md`, and `kaola-workflow-adaptive-schema.js` is byte-identical across all four.
  status: done
  dispatched: self, inline; folded into the repro pass. Output landed in the "Edition survey" section of `kaola-workflow/issue-933/.cache/repro-933.md`.
  result: All four editions already ship `isReservedWorkflowDirName` with one call site — nothing to PORT, only to WIRE. `kaola-workflow-claim.js` is a `COMMON_SCRIPTS` entry (`validate-script-sync.js:46`), so claude `scripts/` and codex `plugins/kaola-workflow/scripts/` must stay BYTE-IDENTICAL; gitlab/gitea are divergent hand-ports, deliberately not generated (`edition-sync.js:30-33`).

- item: Author the failing test for the demanded result — after `startup` for an issue whose `workflow_project:` is `.roadmap` (and separately `archive`), `kaola-workflow/.roadmap/` contains no `workflow-state.md` and no `.cache/`, and the run does not report the folder acquired. Test custody: whoever implements does not author this. Cover R1 (`--project` flag) and R2 (roadmap-data path) as distinct entry points — guarding `init-issue` alone does not close R1.
  status: in-flight
  dispatched: `tdd-guide` subagent (custody separation — I implement, so I must not author this). Brief carries the owner's resolve-and-report ruling, the acceptance surface, and `kaola-workflow/issue-933/.cache/repro-933.md` as evidence. Output to land as committed edits in the suite it picks (`scripts/simulate-workflow-walkthrough.js` and/or `scripts/test-claim-hardening.js`) plus a baseline-failure report at `kaola-workflow/issue-933/.cache/test-baseline-933.md`.
  result: `scripts/simulate-workflow-walkthrough.js` — new scenario `testClaimNeverAdoptsReservedDir933` (registered in `buildRegistry()`), 4 cases: R1 `.roadmap`, R2 roadmap-data-no-flag, R5 `archive`, plus `Archive` case-fold aliasing (skipped with a printed reason on a case-sensitive fs). RED at baseline `406b5639`, exit 1 read off the process not through a pipe; per-case failure text (7/9/7/7 assertions) at `kaola-workflow/issue-933/.cache/test-baseline-933.md`. **Envelope contract it pins: `reserved_project_note` (non-empty prose naming the declined dir) AND `reserved_project` (declined dir verbatim, discrete field).** Also pins `project`/`selected_project` === `issue-<N>`, `workflow-state.md` recording `name: issue-<N>`, and the reserved dir gaining NOTHING as a set.

- item: Implement the chosen behaviour at the claim site across every edition the survey names, then mutation-prove the guard per site AND per edition — a mutant that reds nothing is more likely broken than redundant, and an N-site mutant proves >=1, never N. Mutate one call site at a time.
  status: done
  dispatched: self, inline. Edits to `scripts/kaola-workflow-claim.js`, `plugins/kaola-workflow/scripts/kaola-workflow-claim.js` (byte-copy), and the two forge hand-ports; mutation log to land inline here.
  result: **All four editions fixed at one structurally equivalent site.** New `unreservedProjectName(requested, issueNumber)` beside `projectNameForIssue`; wired at `claimProject` where BOTH doors converge (startup passes its resolved name in as `args.project`), above the existing `isSafeName` assert; envelope gains `reserved_project_note` + `reserved_project`. Forge ports use `issueIid` and their own condensed comment style; `validate-script-sync.js` green (15 common scripts, 27 byte-identical groups). Driven per edition x per door: R1/R2/R5 all now `project=issue-<N>`, reserved dir untouched, in claude/codex/gitlab/gitea. **Mutation-proven at canonical, one at a time, with a green positive control and a shasum-verified restore:** (A) bypass wiring -> red `got project=".roadmap"`; (B) resolver made identity -> red same; (C) drop ONLY the envelope report -> red on missing `reserved_project`, proving the report is pinned independently of the filesystem assertions. Forge-edition arming is NOT proven by this suite — see the forge-sibling item below.

  **Correction, measured late: the resolver shipped with TWO arms and now has ONE.** I added a `project-<name>` arm for the reserved-name-plus-no-issue-number case, justified in-comment by "`claimProject` is exported and driven directly, so a hole here is a hole in the guarantee." **That justification was false.** The refusal is INSIDE `claimProject`, not upstream in `cmdClaim` as both I and the test author had it: `claimProject:830` -> `buildClaimAnchors:780` -> `buildClaimIdentity` -> `normalizeIssueNumbers` (`kaola-workflow-adaptive-schema.js:141`) throws `claim_issue_numbers_invalid` on anything not a positive safe integer. Driven directly — `claimProject(root, {project:'.roadmap', issue:null})` throws there and the reserved directory afterwards holds exactly what it held before. So the arm could never execute observably. **Removed with its two pins rather than pinned** (the test author had added direct-call legs F/G at my request; I asked for them back out — a test outlives its mechanism only as a trap). The comment now records the measurement instead of the false mechanism claim.

- item: Close the edition gap the test author measured — the walkthrough covers claude+codex only, while gitlab/gitea are divergent hand-ports that `validate-script-sync.js` compares to nothing, and all four reproduced #933 at baseline. A new `scripts/test-*.js` is red-on-arrival until wired into all four `package.json` chains, and `package.json` is not a test path, so authorship and wiring are split.
  status: in-flight
  dispatched: `tdd-guide` (same agent, `tdd933`) authors the sibling ONLY, modelled on `scripts/test-forge-claim-rollback-scoping.js`; it owns that one new file and must not touch the claim scripts or `package.json`. I own the chain wiring. It is to report the exact filename and expected npm-script entry, plus whether the forge ports resolve the project name at a structurally equivalent site (they do — measured `:892` gitlab / `:896` gitea, same `args.project || projectNameForIssue` shape).
  result: `scripts/test-forge-claim-reserved-project.js` — 20/20 legs (4 editions x 5), 364 assertions, green against the fix. Wired by me into ALL FIVE `package.json` entries (claude, codex, gitlab, gitea, claude:full) immediately after `test-forge-claim-rollback-scoping.js`; `test-suite-registration.js` green (45 test files, 42 registered, 3 exempt). **Per-edition arming PROVEN, one edition mutated at a time with a green positive control either side: each mutant reds on EXACTLY its own edition's leg (`#933[claude/canonical/`, `#933[codex/`, `#933[gitlab/`, `#933[gitea/`) and no other.** This is what the walkthrough could not do — it reaches claude+codex only. claude<->codex byte-identity re-confirmed (1 distinct hash).

- item: Land it — CHANGELOG under `[Unreleased]`, any doc surface the change reaches, then the walkthrough at full scope (`node scripts/simulate-workflow-walkthrough.js`) and `npm test` before the sink. The fast gate samples a 1/12 shard and is not proof.
  status: done
  dispatched: self. Docs landed; work migrated main-root -> worktree and committed as `8de1ac9a` on `workflow/issue-933`; full `npm test` run against the frozen worktree, log at `scratchpad/npmtest-final.log`.
  result: `CHANGELOG.md` `[Unreleased]`, `README.md` claim section, `docs/api.md` claim-envelope surface. **Four-chain `npm test` exit 0** on the shipped worktree — all four chains reached their own terminal step, no signals; the forge sibling ran in ALL FOUR (`364 passed, 0 failed` x4). **The four-chain green did NOT cover the walkthrough scenario** — `:claude` samples `--shard auto/12` and shard 3/12 came up, running 17 of 206 scenarios, and `testClaimNeverAdoptsReservedDir933` was not among them; the earlier 206/206 predated both the dead-arm removal and the migration, so it spoke for no shipped tree. **Re-run at full scope IN THE WORKTREE: `ran:206 passed:206 failed:0`, scenario `PASSED (4/4 doors)`, exit 0** — log `scratchpad/wt-full-walkthrough.log`. The 25 hits from a failure-grep over the chain log were all benign (test NAMES containing "FAIL", and `0 failed` summaries) — checked rather than trusted, since an exit code and a log grep can disagree and the grep is the one that lies. **PROCESS ERROR, corrected: the whole run was done in the MAIN ROOT on `main` — the shared tree — instead of the claimed worktree.** Recovered by patch (629 lines) + untracked-file copy into `.kw/worktrees/issue-933`, with all TEN changed files verified byte-identical between the two trees BEFORE the main root was reverted, so what shipped is what was measured. Main root now clean at `406b5639`, only the untracked run folder left for the sink. **Second process error: an earlier `npm test` was started and then edited under, invalidating it; stopped by task ID (never `pkill -f`, which would kill the concurrent agent's runs) and re-run once on a frozen tree. No result from the invalidated run was used.**
