# Close #918–#923 — six divergences, five of them on the one unpoliced hand-ported forge seam

Worktree: `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-918-919-920-921-922-923`
(referred to below as `$WT`; subagents get absolute paths).

The seam: `scripts/edition-sync.js:30-34` keeps the data-layer forge ports (claim / sink-merge /
sink-pr / active-folders / classifier / roadmap) hand-ported — absent from `COMMON_SCRIPTS` and
`RENAME_NORMALIZED_FAMILIES` — so neither `validate-script-sync.js` nor `edition-sync --check` can
witness a function, a wording or a whole feature present on one side and missing on the other.
#918, #919, #921, #922, #923 all live there. Only behavioural-per-forge coverage can see them.
Prior art to reuse, not reinvent: `scripts/test-forge-finalize-findings.js` (added by #916, wired
into all five chain definitions). Do not re-file forge-adapter extraction — owner DECLINED it.

**Owner rulings this run (2026-08-02, in conversation):**
1. **#923 — remove branchless everywhere**, do not port it. Measured first; see that item.
2. **Test custody holds**: `tdd-guide` subagents author every test artifact; the implementer (me,
   inline) writes production code and never its own tests.

- item: Map the seam before touching it — confirm the six defects' exact sites at HEAD, inventory what behavioural-per-forge test harnesses already exist and how they are wired into the chains, and report which of the six can share one harness; findings are stale-by-a-bundle and must be re-measured, not trusted.
  status: done
  dispatched: self, inline
  result: All six premises HOLD at HEAD, and two were refined. **#919** canonical 0 / gitlab 5 / gitea 5, and NOTHING pins the dead pair — only canonical's own note at `sink-merge.js:1808` recording the retirement, so deletion is clean. **#921** `archive_unstaged` is 1/0/0/1 and `residue_unstaged` 1/1/1/1, exactly as filed. **#920 is NOT canonical-only** as the issue claims — the identical false sentence is in `residue_stage_failed` too, in all four editions (canonical/codex `:4699 :4703 :4849 :4859`, gitlab `:4579 :4589`, gitea `:4574 :4584`), and no test anywhere pins that wording. **#922** gitlab `:4424` / gitea `:4419` vs canonical's `rm -r --cached` `:4655` + candidate list `:4672`. **#918** RULES_BLOCK canonical `:37` vs forge `:42`. **#923** see its own item. Harness prior art: `test-forge-finalize-findings.js` (390 lines, `mkdtempSync` fixtures, no host-repo git mutation → concurrent suites are safe); chains are defined in `package.json`.

- item: #922 — scope the GitLab/Gitea archive-staging pathspec so a foreign project's live folder and archive band cannot enter a `chore: archive <project>` commit; the ports use one unscoped `git add -A 'kaola-workflow/'` where canonical stages a computed candidate list after `git rm -r --cached`; whatever lands must still force the live folder out of the branch (canonical's `git rm --cached` does, the forge `-A` re-adds it — the M6 observation).
  status: done
  dispatched: tdd-guide (T2) → authors `$WT/scripts/test-forge-archive-scoping.js`, behavioural per forge, proven RED on the unscoped pathspec at HEAD. Test artifact only; I hold the production fix.
  result: **DONE, MUTATION-PROVEN.** Both forge ports now mirror canonical's scoped shape — a computed candidate list (`.roadmap`, `ROADMAP.md`, plus the archive dest, or the `source-missing` fallback) filtered by existence, staged with an explicit `git add -A -- …paths`, preceded by `git rm -r --cached --ignore-unmatch` on the project's own live folder. Both calls share ONE try/catch, so the forge keeps its single `archive_stage_failed` finding and #914's "the forges are not owed a sixth type" ruling still holds. The `rm --cached` is NOT optional alongside the scoping — the unscoped `-A` used to re-add the live folder from disk, so narrowing the add alone would have left it on the branch (the M6 observation). T2: **62 passed/10 failed → 72 passed/0 failed, exit 0**, and all four editions now carry exactly 4 paths, which is the convergence signal. **Mutation-proven ONE SITE AT A TIME, never all at once**: unscoping gitlab alone → 5 gitlab FAILs / 0 gitea; unscoping gitea alone → 5 gitea FAILs / 0 gitlab; deleting gitlab's `git rm --cached` → exactly 1 FAIL, the live-folder regression guard. Neither port stayed green without its own fix and neither reacted to the other's mutant, so the coverage is non-vacuous AND genuinely per-port. Baseline restored to 72/0 after each.
  note: **This change falsifies a rationale in `docs/api.md:359-365`** — that paragraph explains the forges' missing `archive_unstaged` by saying they "make one unscoped `git add -A`" and so have "no candidate list". After this fix they DO have one. The #921 docs edit must not repeat that now-stale reasoning; its COUNTS stay correct and untouched, per that issue's AC-2.

- item: #919 — canonical carries neither `persistExpansionRollupToSummary` nor `parseExpansionRecords`; both forge claim ports do, reading a `workflow-plan.md` nothing authors, so the early `return false` always fires; delete the dead pair with whatever tests pin it, or record the divergence as deliberate.
  status: done
  dispatched: self, inline — nothing pins them (measured in item 1), so this is a straight deletion from the two forge claim ports; no test artifact is owed and none is authored.
  result: **DONE.** Four sites removed per port — the `adaptiveSchema` destructure at line 30 (now `const { parseGoal } = adaptiveSchema;`, matching canonical `:29`), the commented function block, the call site with its comment, and the `module.exports` entry with its comment. **2,926 bytes from each port, byte-identical counts**, which independently confirms the two ports were identical in that region. `node --check` passes on both and residue grep is 0/0. Cleared four ways before deleting: (a) the function is provably inert, (b) NO consumer — the export comment claims "reuse by sink-merge's SOLE-archiver finalize path" but grep for both names in both forge `sink-merge.js` returns EMPTY, so the comment describes a reuse canonical had already removed, (c) no test pins it, (d) the superset guard is unaffected. **The inertness proof was then confirmed empirically, not just argued**: T2's suite is unchanged at exactly 62 passed / 10 failed across the deletion, and `validate-script-sync.js` exits 0 with all 6 forge export-superset families in sync.
  note: **The issue UNDERSTATES this — it is a latent crash, not merely dead code, and the deletion is provable rather than merely safe.** Proven by RUNNING, not grepping: `scripts/kaola-workflow-adaptive-schema.js` (the byte-identical cross-edition anchor, 56 exports) no longer exports `parseExpansionRecords` OR `expansionRecordEfficiency` — both are `undefined`. The forge ports destructure them at line 30 anyway, so `:2013 parseExpansionRecords(planContent)` throws `TypeError: not a function` on its first real statement. The whole body is wrapped in `try { … } catch (_) { return false; }`, so the TypeError is SWALLOWED — the [[feedback_broad_catch_disarms_the_guard]] shape exactly. Consequence: every path through the function returns `false` and the closing `writeFile` is UNREACHABLE, so the function cannot ever do its job on either forge. The issue's premise ("inert because nothing authors `workflow-plan.md`") is true but weaker than the fact: it would be inert even if something did. Canonical predicted this at `sink-merge.js:1808-1810` — "a call to a retired export is not a stale comment — it throws". Deleting it therefore changes nothing observable, which is a proof and not a hope. Superset guard at `validate-script-sync.js:485` is unaffected: canonical does not export the name, so dropping the forge's extra export preserves the superset.

- item: #920 — canonical's `archive_stage_failed` finding tells the operator `git add` is all-or-nothing over its pathspec list so nothing staged, which is measurably false in the ignored-path case (exit 1 with the good path staged); state what was measured and stay silent on the staged set if it is not cheaply available, and make coverage red on the current wording bound to the specific claim, not the whole string.
  status: done
  dispatched: tdd-guide (T1) → extends `$WT/scripts/test-forge-finalize-findings.js`, proven RED on the current wording, bound to the specific claim. Carries #921 too (same file, same surface). Test artifact only; I hold the production fix. Scope widened past the issue: the same false sentence is fixed in `residue_stage_failed` and in all four editions, because leaving it two lines away in the sibling finding is the same defect.
  result: **DONE, MUTATION-PROVEN. T1: 21 failed → 133 passed / 0 failed, exit 0.** The false sentence is gone from all 12 message sites across four editions, in BOTH findings. **T1's end-to-end arm found more than the issue filed**: `archive_unstaged` was assigned from the candidate list (`existingPaths.slice(0,50)`), so on a partial stage it named `ROADMAP.md` as unstaged while that file was in the index AND in the resulting commit — the field was factually wrong, not merely under-documented. Fixed by MEASURING: a new local `pathsNotStaged(root, paths)` beside `gitFaultDetail` reads the actual index (`git diff --cached --name-only -- …`), handles directory candidates by prefix, and returns **null** when the probe itself fails, in which case the field is omitted and the message says nothing about the staged set rather than guessing — AC-2's explicit branch. Also narrowed the ANCESTOR: the `#907` comment that generalized the exit-128 case into a property of `git add` was the origin the messages were copied from, so it now names the one case it measured. **Mutation-proven, one site at a time**: all-or-nothing restored in canonical → canonical 2 FAILs, every other edition 0; the same restored in gitlab's residue message ONLY → gitlab 1, gitea 0, proving per-port and not a shared assertion; `archive_unstaged` reverted to the attempted list → canonical 1; docs scope falsified → static 1. Baseline 133/0 after each. **One mutation initially failed to apply and I caught it because `grep -c` returned 0** — a mutation that does not mutate proves nothing, and it was re-run properly.

- item: (#921, carried with #920 above)
  status: done
  dispatched: tdd-guide (T1, bundled) — see the item above.
  result: **DONE.** `docs/api.md` `archive_unstaged` is scoped to canonical + Codex and its meaning now says the set is read back from the index, absent when unreadable. `residue_unstaged` is explicitly marked all-four-editions and was NOT caught in a blanket edit, per AC-3. **AC-2 needed care in the opposite direction from how it reads**: the counts at the edition-caveat paragraphs are correct and are untouched at six-vs-seven, but my own #922 fix FALSIFIED their stated reasoning — they explained the delta by "the forges make one unscoped `git add -A`, no `git rm -r --cached`, no candidate-path list", which stopped being true the moment #922 landed. Rewritten so the count is explained by what is now actually true (both forge calls share one try/catch, so a fault in either raises the single `archive_stage_failed`), and the third paragraph — which listed the unscoped shape's three silent successes as open, "tracked separately in issue #922" — now records them as closed BY this bundle. Leaving either would have shipped documentation describing machinery that no longer exists.

- item: #918 — the two forge roadmap generators emit a 4-bullet rules block that overlaps canonical's 5-bullet `RULES_BLOCK` in only 2 bullets, and `templates/routing/slots.js` slot `in-shared-004` renders those forge bullets into the workflow-init surfaces, so surface prose and generator output disagree on those forges; converge the shared wording or declare each difference with its reasoning, and note the mirror is regenerated BEFORE the merge lands so a generator change never reaches `ROADMAP.md` in its own run.
  status: done
  dispatched: tdd-guide (T3) → authors `$WT/scripts/test-forge-roadmap-rules.js`, behavioural per forge, proven RED on the current divergence. Test artifact only; I hold the generator + skeleton edits.
  result: **DONE, MUTATION-PROVEN. T3: 74 passed/16 failed → 90 passed/0 failed, exit 0.** Only the GENERATORS needed changing — the rendered surfaces were already correct and already converged for all three forges (`in-shared-002` renders the source-of-truth header line, `in-shared-004`/`005` the rules), so no skeleton edit and no surface regeneration was owed. The forge generators gained the three missing shared bullets, KEPT their genuinely forge-specific `iid`/issue-number bullet, and the source-of-truth sentence moved from a rule into the HEADER where canonical states it (check C: two positions, one had to win; canonical's is what every surface already renders). All four generators gained the sixth bullet the surfaces promise. **T3 found the ANCESTOR the issue did not know about**: the surface-vs-generator disagreement was NOT forge-specific — canonical had it too, so fixing only the forges would have made the generators agree with each other while all four still disagreed with the surfaces, the exact [[feedback_find_the_ancestor_sentence_first]] trap. The last difference was one bullet: the generator said "After each workflow cycle" where the surface says "After each `<NEXT>` cycle". Resolved on the GENERATOR side and toward runtime-neutrality — bullet 2 already writes plain `` `workflow-next` `` and normalizes to `<NEXT>` cleanly, so bullet 3 now matches its sibling's style. A generator cannot render a runtime-specific route: one script writes a file every runtime reads. **Mutation-proven per port**: dropping a shared bullet from gitlab alone → check A 1 FAIL + gitlab D3 2, gitea and canonical 0; moving source-of-truth back into a gitlab rule → check C 1 + gitlab D3 2, gitea 0; reverting bullet 3 in canonical alone → check A 4 + canonical D3 2, both forges 0. One mutation initially missed its anchor on shell backtick escaping and was redone on a backtick-free bullet rather than counted.

- item: #923 — RULED: remove branchless everywhere rather than port it. Measured before deciding — both producers named in its own comment (`sink-merge.js:2933-2938`) are gone: `TBD` count in all three `claim.js` ports is 0 and the claim always writes a real branch (`claim.js:840`), and the adaptive planner that "preserved TBD" was retired by ADR 0017. Never exercised: `branch_mode` is in ZERO archived receipts across ~373 runs and `^branch: TBD` in ZERO archived `workflow-state.md`; `branch_mode` has NO consumer at all (written `:1848`, read only by its own test `test-sink-merge.js:919`, absent from `docs/api.md`). Only producers tree-wide are the three test suites and the `--help` line at `:2912`. Delete the ~11 guards in canonical and codex plus the forge's one #912 site, `receipt.branch_mode`, the `branch_tbd_requires_sink` refusal, the usage line, and every TBD test case WITH the mechanism.
  status: done
  dispatched: tdd-guide (T4) → authors the post-removal loud-failure assertion and REPORTS the exact test line ranges that must die with the mechanism, into `.cache/t4-branchless-testplan.md` (17KB, consolidated into the main-root active folder). It does NOT delete them — a test dies WITH its mechanism, never ahead of it, so I removed the production sites first and only then released T4 to execute its own deletion plan.
  result: **DONE. The measurement is the headline and it is recorded in full at `.cache/t4-branchless-testplan.md`.** Post-removal `--branch TBD --sink` **FAILS LOUDLY**: exit 1, typed refusal `sink_incomplete` at `push_upstream` online, the merge checkout offline — both strictly before `finalize`/`archive_commit`/`push_main`/`closure`, so local and remote heads were unchanged, the live folder survived and no issue closed. Two CONTROLS make it conclusive rather than suggestive: the undoctored build on the same fixture still exits 0 with `branch_mode:"branchless"` (the fixture genuinely reproduces today's behaviour), and the doctored build still sinks a REAL branch at exit 0 (the removal is surgical, not a general break). Method avoided a hand-cut patch's adjacent-line risk by pinning the three predicates to `false`. #923's one unacceptable outcome — silent acceptance — does not occur, so the removal needed no compensating refusal. Production: ~11 guards removed from canonical and its byte-identical Codex mirror plus the one site each forge carried, `receipt.branch_mode`, the `branch_tbd_requires_sink` refusal and the `--help` line; the `if/else` and `if (!branchless)` blocks were UNWRAPPED with bodies dedented, and `main()` now runs the `--branch is invalid` assert unconditionally, which is exactly what makes `TBD` an ordinary name routed to the loud failure. Tests died with it: `test-sink-merge.js` **648 assertions exit 0** with its label now `…/#893/#923` and `#711` dropped; both forge sink suites exit 0; `branchless` residue is 0 in all three suites. **The (C) survivors were protected**: #912 arms (b)(c)(d) pin the worktree-clean data-loss guard and stayed, and the forge `runDirectMerge` `--branch is invalid or TBD` assertions are the PRE-#711 legacy refusal, deliberately not harmonised here. Where an assertion survived but its message named gone machinery, only the prose changed — that is not repairing a pin ahead of its mechanism, since the assertion passes on its own merits.

- item: Verify the whole bundle — walkthrough at FULL scope (the fast gate samples a rotating 1/12 shard), then the four chains from inside `$WT` (the receipt hash follows the invoking tree), plus mutation-proof each new guard both ways since a green suite is not proof a guard is armed.
  status: done
  dispatched: self, inline
  result: **ALL GREEN.** Walkthrough at **FULL scope: 202/202 scenarios, exit 0** — run separately and deliberately, because the fast gate sampled it at shard 1/12 (17 of 202) as designed, and that slice is not the suite. Four chains run from inside `$WT`, **each chain's OWN exit code echoed separately** rather than trusting the compound status, since a SIGTERMed chain reads as wrapper-green: claude 0, codex 0, gitlab 0, gitea 0, with zero 143/137 in any log. Confirmed the new suites actually RAN in all four chains rather than being silently absent. `test-sink-merge.js` 648 assertions exit 0, both forge sink suites exit 0. **Both new suites wired into all five chain definitions** — `test-suite-registration.js` caught them as unregistered first (40 registered / 3 exempt now), which is exactly the guard doing its job; `package.json` diff is 5 lines, not a reformat. Every guard mutation-proven one site at a time, per issue, recorded in each item above — and **two mutations that silently failed to apply were caught and redone**, because a mutation that does not mutate proves nothing.

- item: Dock the documentation — `CHANGELOG.md` under `[Unreleased]`, `docs/api.md`, `docs/conventions.md` if an observation earned a rule, and regenerate routing surfaces if any skeleton changed; a docs commit stales the receipt on self-host, so re-run the chains after.
  status: done
  dispatched: self, inline
  result: **DONE.** `CHANGELOG.md` `[Unreleased]` gained Removed/Fixed/Changed entries for all six, each stating the measured fact rather than the intent. `docs/api.md` carries the #921 scoping plus the rewritten edition caveat #922 falsified. **No routing surface was regenerated and none was owed** — the #918 fix moved the GENERATORS to match surfaces that were already correct, so no skeleton changed; `generate-routing-surfaces.js --check` runs in every chain and stayed green throughout. `kaola-workflow/ROADMAP.md` regenerated with the script (never hand-edited): `validate` went stale → `generate` → `ok`. **Ordering was handled deliberately rather than discovered**: docs were docked BEFORE the commit and before `run-chains`, so the receipt is bound to a tree that already contains them — a docs commit landing after the receipt would have staled it on self-host. Receipt `headSha 3973af23` equals the worktree HEAD exactly, scope `all-four`/`edition_coupling` over 22 changed files, chains claude=0 codex=0 gitlab=0 gitea=0.
  note: **`kaola-workflow-roadmap.js` changed in this run, so the #917 trap applies**: the finalize transaction regenerates the mirror while the branch is still unmerged, so a generator change does not reach `main`'s mirror in its own run. Re-run `generate` and `validate` on `main` AFTER the sink, and push if it moves.
