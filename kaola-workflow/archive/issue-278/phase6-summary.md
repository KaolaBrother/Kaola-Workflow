# Phase 6 - Summary: issue-278

## Delivered
Forge-parity bug fix: the GitLab and Gitea claim ports now delete the `<!-- kw:claim project=<slug> -->`
marker note/comment on `discard`/`release`/`finalize`, mirroring the GitHub edition fix from #275.
This unblocks same-issue re-claim on GitLab and Gitea (previously the orphaned marker kept the
classifier's remote-claim detector blocking re-claim for up to 24h).

## Files Changed
- plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-forge.js (new `deleteIssueNote` + export)
- plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js (`clearAdvisoryClaim` marker delete + slug)
- plugins/kaola-workflow-gitlab/scripts/test-gitlab-forge-helpers.js (5 new tests)
- plugins/kaola-workflow-gitea/scripts/kaola-gitea-forge.js (new `deleteIssueComment` + export)
- plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js (`clearAdvisoryClaim` marker delete + slug)
- plugins/kaola-workflow-gitea/scripts/test-gitea-forge-helpers.js (5 new tests)
- CHANGELOG.md ([Unreleased] Fixed entry)

## Test Coverage
10 new RED→GREEN assertions (5 per edition), run in-chain via the GitLab/Gitea walkthroughs.
No coverage % tool in this repo (hand-rolled assert suites); the new behavior is fully asserted
(delete-call shape, scoped-vs-generic match, no-false-positive, OFFLINE skip).

## Final Validation Evidence
- `npm test` (full, all four editions: claude/codex/gitlab/gitea) → exit 0.
  validate-script-sync OK (15 common scripts + 6 byte-identical groups), vendored agents validated,
  all walkthroughs + new forge-helper tests pass. Run from the worktree against the final candidate
  state (CHANGELOG included). Also independently green in the n3 review gate.
- Adaptive barriers: `--resume-check`=0 `--gate-verify`=0 `--barrier-check`=0 `--verdict-check`=0.

## Documentation Docking
DOCKED — .cache/doc-docking.md. CHANGELOG is the only impacted doc class (matches n4's frozen
write-set); README/api/architecture/.env.example have explicit no-impact reasons (internal forge
wrappers, bug-fix parity, no public-contract change).

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
None. The code-reviewer (n3) recorded 2 LOW non-blocking notes, both accepted (not deferred):
- pre_existing: the classifier detector matches `(project|sess)=` but no edition ever WRITES a
  `sess=` marker; `project=`-only deletion is faithful to #275. Out of scope.
- in_scope (accepted #275 limitation, NOT a new follow-up): the scoped branch uses exact slug
  string-equality with no generic re-fallback on a mismatched slug. This is byte-identical to the
  shipped, accepted #275 contract (#275 itself logged the null-`project` fallback as "currently
  unreachable — every call site passes a truthy project"). Write-slug == clear-slug (same folder
  context), so the mismatch case cannot occur. The frozen plan mandates mirroring #275 exactly, so
  "fixing" this would violate the parity contract. No follow-up issue.

## Closure Decision
Closure scan: no deferred items, no unresolved conflicts, no partial implementation, no
user-decision items. The single LOW in_scope note is an accepted #275-parity limitation (above),
not a deferral. Issue may close. No advisor-closure escalation required (no closure-gate items).

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
278 — to be closed by the merge sink after acceptance criteria pass.

## Roadmap
To be regenerated by the contractor (Step 7): no active work after closure.

## Archive
Pending — cmdFinalize archives kaola-workflow/issue-278/ → kaola-workflow/archive/issue-278/.

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | skipped | .cache/doc-docking.md (CHANGELOG-only; n4 write-set is CHANGELOG.md) | only CHANGELOG impacted; other docs no-impact; out-of-lane edits barred by union barrier |
| documentation docking | invoked | .cache/doc-docking.md | |
| closure advisor gate | N/A | closure scan (no deferred/decision items) | no closure-gate items |
| final-validation fix executors | N/A | — | final validation passed first run |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | contractor Step 7 |
| archive completed folder | pending | | contractor Step 8b |
| final commit and push | ready | git status/upstream check | final gate after this file is committed |

## Status
READY FOR FINAL GIT GATE
