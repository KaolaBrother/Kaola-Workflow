# #936 — an issue the sink leaves OPEN keeps its kw:claim marker and cannot be re-claimed; single-source the release

- item: Verify the reported asymmetry against the sources at main — clearAdvisoryClaim (claim.js:957, marker delete :967-979) removes label AND marker; sink-merge.js removes only the label (:961, :971, :996, :1006, :2829, :2840) and contains zero "kw:claim"; classifier short-circuits on closed (:360) and computes `blocked` as label OR marker (:371). Report what is true, not what the issue says.
  status: done
  dispatched: investigator "verify-asymmetry" reading main-root sources; output lands at kaola-workflow/issue-936/.cache/asymmetry.md
  result: kaola-workflow/issue-936/.cache/asymmetry.md — asymmetry CONFIRMED by four measurements. sink-merge.js has zero `kw:claim` AND zero `'api'` verbs (it cannot delete a comment at all), and `clearAdvisoryClaim` is NOT exported from claim.js (only `postAdvisoryClaim` is), so the sink could not delegate either. Six label-only sites, but they split across TWO mutually exclusive terminal paths: postMergeCleanup (:961,:971,:996,:1006) and runSinkTransaction (:2829,:2840). Two facts the issue MISSES: (1) the marker EXPIRES after 24h (classifier.js:216-217) so a stranded marker self-heals — the label never does; (2) runSinkTransaction's closure step is wrapped in `if (!keepIssueOpen)` at :2809 with NO else arm, so `--sink --keep-issue-open` releases NOTHING — no close, no comment, no label, no marker. Classifier confirmed: closed short-circuits before any claim check (leg B), `blocked` is label-first-OR-marker and the label short-circuits the comments probe (leg G). Detector regex is strictly WIDER than the deleter's exact substring (accepts `sess=` and loose whitespace; no `sess=` producer exists). cmdFinalize clears the marker unconditionally ABOVE its own keepIssueOpen branch (claim.js:4601-4612), so the exposure is a sink running without a preceding cmdFinalize.

- item: Establish REACHABILITY of the live symptom — the reporter could not capture which branch produced it. cmdFinalize does call clearAdvisoryClaim per member (claim.js:4598-4612). Determine by running/tracing: does a keep-open member reach cmdFinalize's clear loop at all, does `args.project` match the marker's `project=`, and does the sink path run without the cmdFinalize leg? A defect found by reading has not established reachability — name the exact door.
  status: done
  dispatched: investigator "reachability" running a real finalize+sink keep-open reproduction in a scratch repo; output lands at kaola-workflow/issue-936/.cache/reachability.md
  result: kaola-workflow/issue-936/.cache/reachability.md — REPRODUCES, three doors, all driven against the real scripts via KAOLA_GH_MOCK_SCRIPT fixtures. (a) CLOSED as stated: a keep-open bundle finalize visits EVERY member and deletes both markers (E1/E2a), the archived state retains `issue_numbers` so a resume clears again (E6), and no code path rewrites `issue_numbers` after the claim (writeState has exactly 2 call sites). (b) OPEN, REPRODUCED (E7e): `--project Bundle-936` on this case-insensitive APFS box resolves every PATH operation but `activeByProject`'s strict `===` returns null, the null-folder fallback recovers issue_numbers while `args.project` is NEVER reconciled against the durable record, so the deleter's exact `includes()` misses every member — finalize emits `result: closed`, exit 0, no warning. (c) OPEN and broader (E5-SUB, with E5-CTL as the positive control that makes the zero meaningful): `--sink --keep-issue-open` makes exactly 2 gh calls, both `issue view` probes — zero close, zero remove-label, zero comment, zero DELETE — while reporting `status: sinked`, `closure: done`, exit 0. FOURTH DOORS: D1 an OFFLINE finalize returns `skipped_offline` before touching the forge yet still emits `status: closed` (the one door that reproduces BOTH retained artifacts from a single cause); D2 six early returns in cmdFinalize (claim.js:4248/4277/4328/4343/4379/4414) exit 1 before the clear loop at :4605; D3 the keep-open teardown that WAS written lives only in postMergeCleanup, unreachable in --sink mode; D4 stale markers accumulate — a wrong `--project` under-deletes while a MISSING one falls back to the generic regex and over-deletes ALL markers. Stub mechanism for tests: KAOLA_GH_MOCK_SCRIPT at 5 sites, prior art `scripts/test-bundle-finalize.js:237-353`, and KAOLA_WORKFLOW_OFFLINE must be '0'.

- item: Decide the fix shape and get it right, not wide — sink calls the same complete release for every issue it leaves OPEN, vs hoisting marker deletion into a shared helper both scripts use. Derive additively: add only what the observed failure demands, no fourth call site.
  status: done
  dispatched: self
  result: DECIDED — single-source the release; the sink calls claim.js's `clearAdvisoryClaim` for every issue it leaves OPEN. Three findings fixed the shape. (1) REACHABILITY IS SETTLED FROM THE SHIPPED SURFACE, not from a reproduction: `templates/routing/slots.js:124` (`fz-sink-merge-run`, all three forges) invokes the sink as `--sink $SINK_KEEP_OPEN_FLAG`, so `runSinkTransaction` IS the production path and its closure step — wrapped in `if (!keepIssueOpen)` at sink-merge.js:2809 with no else arm — releases NOTHING under keep-open. That, not the marker asymmetry, is what strands the never-expiring LABEL, and the label is checked FIRST in the classifier, so the label is what actually blocked the reporter. The issue's headline diagnosis is a real but SECONDARY defect on the other (postMergeCleanup) path. (2) `--sink` does `process.chdir(os.tmpdir())` at sink-merge.js:3021, which is why every sink forge call carries `forgeOpts = {cwd: mainRoot}` — but `clearAdvisoryClaim` calls `ghExec` with NO opts, so calling it as-is from the sink would run `gh` outside any repo, throw into its four swallowed catches, and be SILENTLY DEAD. It must take a cwd/opts pass-through; that is the pin that matters most. (3) Exporting exactly the name `clearAdvisoryClaim` costs zero port-claim.js edits — gitlab/gitea already export it and the superset guard checks names. NOT in scope, recorded not built: the `sess=` detector-wider-than-deleter gap (no producer exists), the dead `project`-falsy fallback, `cmdRepairLabels`/`closeIssueIdempotent` leaving markers on CLOSED issues (harmless — the classifier short-circuits on closed), and the undiscriminating "has a remote workflow claim" message that made the reporter unable to tell label from marker.

- item: Author the regression test (test custody: whoever implements does NOT author) — finalize/sink a keep-open issue, then assert `startup --target-issue N` returns owned/acquired, not user_target_blocked. Prove it fails on the recorded baseline.
  status: done
  dispatched: tdd-guide "author-tests" in the worktree $WT=.kw/worktrees/issue-936; writes scripts/test-sink-merge.js + a simulate-workflow-walkthrough.js leg + the two port sink suites, proves each red at baseline; baseline transcript lands at kaola-workflow/issue-936/.cache/test-baseline.md
  result: kaola-workflow/issue-936/.cache/test-baseline.md — 4 suites modified in the worktree, all RED at baseline (test-sink-merge.js 8 failed/772 passed with `calls=[]` as the measurement; one walkthrough leg `testSinkKeepOpenReleasesClaimMarker`; one leg each in the two port sink suites). DISCRIMINATION IS MUTATION-PROVEN, which was the point: MUTANT A (the plausible naive fix — export clearAdvisoryClaim, call it from the sink, no cwd) is CAUGHT — its label clauses go green while the marker clauses still red, with `REJECTED-wrong-cwd:/private/var/folders/.../T` in the call log, exactly the silently-dead shape. MUTANT B (forgeOpts carried at every call) is fully green incl. a 207/207 full walkthrough. The gh mock is cwd-honest (walks up for `.git`, else rejects like real `gh`), with a positive control leg so the cwd clause is not vacuous; port mocks are cwd-honest for `repo view` only because glab/tea address by fully-qualified project ref. MEMORY CORRECTION: the standing `init.defaultBranch` caution is STALE at this commit — both port sink suites pin `-b main` themselves (test-gitlab-sinks.js:87, test-sink-merge.js:161) and are GREEN at baseline on this box, so every red below is attributable to the new legs alone.

- item: Implement the fix against that test.
  status: done
  dispatched: implementer "implement-fix" in $WT=.kw/worktrees/issue-936; edits the 4 sink copies + canonical/codex claim.js export, never the 4 test files; lands as commits on branch workflow/issue-936 and a report at kaola-workflow/issue-936/.cache/implementation.md
  result: commit 40d4a5c9 on workflow/issue-936 (13 files, +937/-32), report at kaola-workflow/issue-936/.cache/implementation.md. Three release sites per sink copy, all terminals that leave an issue OPEN: a new keep-open arm on runSinkTransaction's closure step (it had none), postMergeCleanup's primary arm under keep-open only, and the #403.6 keep-open bundle arm. Close paths (:996/:1006/:2829/:2840 + mirrors) deliberately untouched. Single-sourced through `clearAdvisoryClaim`, which gains an optional `opts` threaded into ALL FOUR of its ghExec calls and is now exported from canonical claim.js — both forge claim ports already exported it, so the export-superset guard needed no port claim.js edit. Receipt shape untouched (the return feeds `claimLabelRemoved`, same enum). ONE THING THE BRIEF DID NOT ANTICIPATE, found and removed by the implementer: gitea's `clearAdvisoryClaim` gates LABEL removal on `projectInfo.full_name`, which `readProjectInfo` can return empty (its fallback `forge.discoverProject()` resolves from cwd, and the process is in tmpdir) — so routing that site through it alone would have STOPPED removing the label, strictly worse than shipped. `releaseClaimArtifacts` falls back to the bare label call on unresolved identity only, A/B-proven. I VERIFIED THE GUARD MYSELF against what ships rather than taking the receipt: dropping `forgeOpts` from the single runSinkTransaction call site reds test-sink-merge.js 8/772 with `REJECTED-wrong-cwd:/private/var/folders/.../T` in the log; `git checkout --` restores it to 780 assertions, exit 0.

- item: Cross-edition propagation — claim.js and sink-merge.js are ported per forge (github/gitlab/gitea) and across runtime editions. Measure which ports carry the same release code before costing a port; check the canonical producer first. install.sh must reach consumers.
  status: done
  dispatched: investigator "edition-census" measuring per-forge/per-runtime copies of the release code; output lands at kaola-workflow/issue-936/.cache/edition-census.md
  result: kaola-workflow/issue-936/.cache/edition-census.md — 8 copies on the FORGE axis only; the runtime axis (.opencode*/.kimi*) carries ZERO copies, and codex consumes plugins/kaola-workflow/ rather than a fourth tree. canonical+codex are COMMON-byte identical (guard `validate-script-sync.js`, MUTATION-PROVEN armed); gitlab/gitea are DIVERGENT hand-ports (residual diff after aggressive rename-normalization EXCEEDS the port's own line count) — hand-port twice, in each idiom. Comment list+delete primitives ALREADY EXIST and are already exported and already `require`d at line 8 of both port sinks (gitlab `deleteIssueNote` is issue-scoped, gitea `deleteIssueComment` is repo-level) — the fix's shape is NOT constrained by a missing primitive. Export-superset guard MUTATION-PROVEN armed: a new canonical export forces the same name into both port claim.js files — but gitlab/gitea claim.js ALREADY export `clearAdvisoryClaim` (:6220/:6212) while canonical does not, so exporting exactly that name costs zero port edits. install.sh --forge=github ships CANONICAL scripts/. COVERAGE HOLE: `test-sink-merge.js` (150KB, the dedicated sink suite) runs in NO chain — only the never-mandated `claude:full` — and no sink suite on any forge asserts anything about the marker today; run it by hand.

- item: Docs and changelog — CHANGELOG.md under [Unreleased], plus any doc that describes claim release or the keep-open terminal mode.
  status: done
  dispatched: self (CHANGELOG written by the implementer inside 40d4a5c9; I checked the rest of the doc surface myself)
  result: CHANGELOG.md gained a `## [Unreleased]` → `### Fixed` entry in 40d4a5c9 (there was no [Unreleased] section — 9.5.4 was the head). I added the contract clause myself at docs/workflow-state-contract.md:271-277: the keep-open terminal now documents that it releases the claim on every issue left open, and that release means BOTH artifacts. Checked and deliberately NOT changed: docs/api.md:1280 ("clearAdvisoryClaim returning the status enum and finalize/watch emitting claim_label_removed is shared across all three forges") is still true — the return enum and receipt shape are untouched, only an optional trailing param was added. README.md describes install and overview, unaffected. docs/architecture.md:265 mentions keep-open only re: the PR-body Closes-#N hazard, unaffected.

- item: Close the acceptance gap found at finalize Step 3 — the issue names its own regression test ("finalize a keep-open issue, then assert `startup --target-issue N` returns owned/acquired rather than user_target_blocked") and nothing pins it. The shipped legs assert the END STATE via a test-local marker spelling, so they cannot see a producer/detector/deleter divergence — and the asymmetry census proved the detector is strictly WIDER than the deleter, which is exactly that seam. Compose the real sink with the real classifier.
  status: done
  dispatched: tdd-guide "author-tests" (same test-custody holder) in $WT=.kw/worktrees/issue-936; one walkthrough leg driving sink keep-open then a real startup/classify; commits on workflow/issue-936. Chains must be RE-RUN after — the green receipt is bound to headSha f1d13b50.
  result: commit 2bb344bb, +170 lines, one walkthrough leg. It SPELLS THE MARKER NOWHERE — seeds through the real producer `postAdvisoryClaim`, releases through the real sink, reads the verdict off the real classifier via `startup --target-issue N` — so a producer/deleter/detector divergence now fails the re-claim instead of hiding behind a shared test spelling. Carries a POSITIVE CONTROL: a freshly claimed issue must first come back `user_target_blocked`, else the post-release `acquired` proves nothing. Verified by me: walkthrough 208/208 exit 0 at full scope, spawn guard green (203 classified, ceiling table untouched), chains re-run and the receipt now binds headSha 2bb344bb / workTreeHash clean / all-four by edition_coupling / zero non-zero steps / accepted_red false.

- item: Report the adjacent reproduced defects to the user rather than silently widening this fix — door (b) (`args.project` never reconciled against the durable record; wrong case under-deletes, missing `--project` over-deletes ALL markers), D1 (an OFFLINE finalize emits `status: closed` having made zero forge calls), D2 (six cmdFinalize early returns before the clear loop). Each is reproduced with a recipe in .cache/reachability.md. Filing them is the user's call, not mine.
  status: done
  dispatched: self
  result: Reported to the user in conversation at the end of the run, with the reproduction recipe for each held in .cache/reachability.md §"Minimal reproduction recipes". NOT filed — filing is the user's call. The three: (1) door (b) — `args.project` is the only input to the marker deleter's predicate that finalize never reconciles against the durable record, so a mis-cased `--project` on a case-insensitive volume under-deletes while a MISSING `--project` falls back to the generic regex and over-deletes every marker, both at exit 0 with no warning; (2) D1 — an OFFLINE finalize returns `skipped_offline` before touching the forge yet still emits `status: closed`, the one door that strands BOTH artifacts from a single cause and the best fit for what the reporter actually saw; (3) D2 — six cmdFinalize early returns (claim.js:4248/4277/4328/4343/4379/4414) exit 1 before the clear loop at :4605. Also recorded, not built: the classifier's "has a remote workflow claim" message does not say WHICH artifact blocked, which is why the reporter could not diagnose their own symptom.

- item: Verify — full walkthrough at full scope plus npm test (the fast gate samples a 1/12 shard; green there is not a verified suite).
  status: done
  dispatched: self, in the worktree
  result: GREEN at f1d13b50. `npm test` exit 0 — it is an `&&` chain of the four chain scripts, so a 0 cannot hide a skipped or SIGTERMed chain; all four ran. Full-scope walkthrough exit 0, 207/207 (baseline was 206/206, the +1 is the new leg). test-sink-merge.js 780 assertions exit 0 — this suite is in NO chain, so it must be run by hand and was. Both port sink suites exit 0. FIRST npm test attempt exited 1 and it was NOT the fix: `spawn-classification` (the forward-only guard capping unclassified spawnSync/execFileSync sites per file) went 1-2 over its ceilings on the three suites the new legs touched, and it fires in the preamble so no chain started. Sent back to the test author (test custody) with "raising the ceiling is not a fix" — it annotated its own new sites and CONVERTED two raw execFileSync('git',...) calls through the existing G.exec helper, +27/-2 across 4 test files, ceiling table untouched, no production code, commit 11366528. Guard now passes (631 sites/65 files, 199 classified, 126 slots of slack).
