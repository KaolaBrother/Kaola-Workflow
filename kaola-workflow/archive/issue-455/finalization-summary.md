# Finalization - Summary: issue-455

## Delivered
Fixed `scripts/kaola-workflow-release.js --cut` so it honors the deliberate dual-version scheme
(root `5.x` ↔ Codex `3.x`, #193) instead of forcing the three `.codex-plugin` manifests equal to the
root version. `--cut` now resolves a Codex version in its own series (derive-by-bump-kind default +
`--codex-version` override, fail-closed `codex_version_underivable` / `non_monotonic_codex_version`
guards in the pre-mutation block, crash-resume idempotent via a `codex_resolution` receipt), bumps the
2 `.claude-plugin` manifests to root, and corrects the README dual-axis lines. JSON envelope gains
`codex_version` + `codex_version_source`. Propagated to all four edition release scripts.

## Files Changed
- scripts/kaola-workflow-release.js
- plugins/kaola-workflow/scripts/kaola-workflow-release.js (codex byte copy)
- plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-release.js (forge port)
- plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-release.js (forge port)
- scripts/test-release.js (RED tests + fixture + crash-resume regression; 94 assertions)
- CHANGELOG.md (### Fixed entry)
- docs/conventions.md (release-cut section)
- docs/decisions/D-442-01.md (## Amendment (#455))

## Test Coverage
`node scripts/test-release.js` → all 94 assertions pass (up from 78). Hand-rolled assert suite (no
framework); coverage measured by assertion count, not %. New cases: derive-minor (T5 rewrite),
derive-major, explicit-override, non_monotonic_codex_version, codex_version_underivable (+ no-mutation),
and both crash-resume faces (T12 derived, T13 explicit).

## Final Validation Evidence
- `node scripts/test-release.js` → all 94 assertions passed (real exit 0).
- All four cross-edition chains run SEPARATELY at the impl tree, real exit codes captured:
  TESTRELEASE_EXIT=0, CLAUDE_EXIT=0, CODEX_EXIT=0, GITLAB_EXIT=0, GITEA_EXIT=0 (each with its
  walkthrough success sentinel). Evidence: .cache/tdd-guide.md, .cache/code-reviewer.md.
- Reuse boundary: those chain runs covered the code/test tree at commit 09bc8da2; the subsequent
  finalize-node edits (CHANGELOG + docs/conventions.md + docs/decisions/D-442-01.md, folded into the
  amended impl commit 9ea35ea0) are docs-only and outside the code/test rerun trigger. The sink's
  `npm test` (sink-merge --sink step 4) re-runs all four chains on the final merged tree as the
  binding pre-merge gate.
- code-reviewer (opus): Round 1 BLOCK (1 CRITICAL crash-resume regression, 2 faces) → fixed → Round 2
  PASS (0/0/0/0), mutation-tested (neutering the fix reproduces both bug signatures).

## Documentation Docking
DOCKED — .cache/doc-docking.md

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- PRE-EXISTING (out of #455 scope): `runCut`'s full-completion idempotent short-circuit (fires when the
  `git_tag` step is already done) returns an envelope that omits `codex_version`/`codex_version_source`.
  Minor observability inconsistency, not a correctness issue (the release is already complete). Surfaced
  by tdd-guide + code-reviewer. Recommend filing a follow-up issue — pending owner decision (see Run gaps).

## Run gaps
- in_run_repair (codex crash-resume idempotency): noise — found by the code-reviewer and FULLY fixed +
  regression-tested (T12/T13) + re-reviewed to PASS within this run; not an outstanding defect.
- manual:short-circuit-envelope-codex-version (pre-existing): deferred — short-circuit envelope omits
  codex_version/codex_version_source; minor, pre-existing, out of scope. Follow-up offered to owner at
  closure (file vs skip).

## Closure Decision
One deferred item (the pre-existing short-circuit envelope gap) surfaced to the owner; it does NOT block
#455 closure (out of scope, pre-existing, non-correctness). #455's own acceptance is fully met. No
roadmap/issue reorganization performed without permission.

## Commit And Push
Implementation committed on branch workflow/issue-455 (9ea35ea0, 8 files). Mechanical finalize
(archive + roadmap closure + bookkeeping commit) delegated to the contractor; sink via
kaola-workflow-sink-merge.js --sink (push, rebase, npm test, FF-merge, push main, close #455, archive).

## GitHub Issue
#455 — to be closed by the sink after archive/roadmap land (probe-before-close).

## Roadmap
To be regenerated by cmdFinalize at archive (removes .roadmap/issue-455.md, regenerates ROADMAP.md).

## Archive
pending (cmdFinalize Step 8b)

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked | .cache/doc-updater.md (report) / docs diffs | |
| documentation docking | invoked | .cache/doc-docking.md | |
| final-validation fix executors | N/A | — | no final-validation failures; in-run review fix done in execute via tdd-guide |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | done by cmdFinalize at archive |
| archive completed folder | pending | | done by cmdFinalize Step 8b |
| final commit and push | ready | git status / branch 9ea35ea0 / sink-merge --sink | final gate runs after this file |

## Status
ARCHIVED AFTER FINAL GIT GATE
