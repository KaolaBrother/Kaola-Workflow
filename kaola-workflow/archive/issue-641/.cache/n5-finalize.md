evidence-binding: n5-finalize 38a5a128a814
compliance: main-session-direct

## n5-finalize — terminal sink (non-delegable, run main-session-direct)

Added the #641 entry to `CHANGELOG.md` under `## [Unreleased]` → `### Added` (prepended,
newest-first): the R1 leg-contained writer co-open behind live reads (4-precondition fail-closed
matrix + typed `serialDegradeReason`, G4 merge fence retained — the #622 mirror), the R2
`observes: scratch` consent-tier legless co-open (freeze grammar + `isValidationInvisible` band cap
+ the barrier-invisible ≠ observation-invisible correction), AC5 serial byte-identity, the two fenced
residuals (A1 gate-blindness, A2 testConsumedExtra) confirmed by both gates and filed #644, decision
record D-641-01, cross-edition #307 with all four chains green (serial, unwaived).

Sink write set: CHANGELOG.md (the unique docs/state sink). The four n4 docs + CHANGELOG are
barrier-invisible allowband writes. The authoritative unwaived serial four-chain receipt is produced
by the orchestrator after the feature commit (finalize step).
