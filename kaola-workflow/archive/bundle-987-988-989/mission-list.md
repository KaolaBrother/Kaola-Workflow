# Close the #984 retirement's tail: dead ROADMAP.md pathspecs (#988), T11's unreachable gate (#989), and a pin that cannot fail (#987)

Worktree: `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-987-988-989`
Branch: `workflow/bundle-987-988-989`

Standing facts for a successor:
- Subagents were **declined** for this run (session instruction: no AgentTool unless requested). The
  design permits declining — the run still finishes. Test custody is not violated by that here: #988
  is a pure removal that owes no new pin, and #989/#987 are test-artifact work, not production.
- `core.fsyncMethod=writeout-only` is set repo-local; without it `git commit` hangs forever on this
  volume.
- Order is not free: #988 removes `kaola-workflow/ROADMAP.md` from `claim.js`'s `archive_stage`
  candidate list, and #989's verdict is about the gate that reads that same list. Do #988 first.

- item: #988 — remove the dead `kaola-workflow/ROADMAP.md` pathspec from `sink-merge.js:2520-2522` (`roadmapPathspecs`) and from `claim.js:4825` (`archive_stage` `candidatePaths`), KEEPING the `kaola-workflow/.roadmap` sibling which still holds `_rules.md`; the mirror was deleted by #984 so both entries are dead weight that reads as a live claim.
  status: done
  dispatched: self, inline in the worktree — edits land in `$WT/scripts/kaola-workflow-sink-merge.js` and `$WT/scripts/kaola-workflow-claim.js`, then `node scripts/test-sink-merge.js` and `node scripts/test-finalize-door.js` green in that worktree.
  result: EDITS APPLIED, one blocking value call open. Landed: canonical sink-merge (mirror pathspec + two stale prose sites), canonical claim.js (`candidatePaths` + the now-unreachable `ROADMAP.md` disjunct at the `roadmap_staged` gate), and the SAME removal hand-ported to gitea + gitlab `claim.js` (`archivePaths`, `roadmap_staged`) and `sink-merge.js` (pathspec + refusal `detail`). The codex copies under `plugins/kaola-workflow/scripts/` are byte-identical mirrors and are NOT hand-edited — `edition-sync.js --write` carries them.
    **THE ISSUE'S "Nothing breaks" IS WRONG, measured.** `node scripts/test-forge-finalize-findings.js` → 252 passed, **1 failed**: `behavioural-C[claude/canonical]` "WITNESS MISSING". That leg pins honest partial-stage reporting (a `git add` that exits non-zero over a gitignored candidate must still report the paths that DID reach the index) and it needs TWO fixed candidates with different ignore status — `kaola-workflow/.roadmap` (gitignored) and `kaola-workflow/ROADMAP.md` (addable). Removing the second leaves ONE fixed candidate on a linked run (`destRel` is excluded there, #832), so the witness is structurally unavailable, not merely relocated.
    Probed git directly rather than reasoning: a DIRECTORY pathspec holding one ignored and one addable member exits **0** and silently skips the ignored one, so the witness cannot be rebuilt inside `.roadmap`. `behavioural-C[codex]` passed only because the codex copy is still unsynced; it will fail identically once synced. The gitea/gitlab ports have no behavioural-C leg at all (they stage with a single unscoped `git add`), so the forge edits break nothing.
    **FORK PUT TO THE USER, and RESOLVED: option B — accept the loss, written down.** Full record and the corrected costing: `.cache/988-witness-fork.md`.
    The deciding argument was the project's own additive-derivation rule. #988 does not just take the test's second candidate away, it takes away the PRODUCTION STATE the test witnessed: with one candidate a linked run cannot be partly staged at all. Rebuilding the witness would mean adding a second run posture (in-place) to a shared fixture builder — machinery for a failure class never observed on that shape, and on the NON-default posture at that, since the claim provisions a worktree by default. Recorded, not built.
    Applied: deleted the five staged-path assertions in `behavioural-C` (the `ls-files` witness, `archive_unstaged` non-inclusion, the two `NONE_STAGED_CLAIMS` scans, the "Paths not staged:" bullet check) plus the `ALL_OR_NOTHING_CLAIM` scan whose justification was "on the very run that disproves it" — the run no longer disproves it, and a control that stays green while its own message is false is the exact defect this file exists to catch. Deleted WITH the state they described, never rewritten to keep passing. Also removed the now-unused `NONE_STAGED_CLAIMS` and `ADDABLE_ARCHIVE_PATH` constants and both fixture plants of `ROADMAP.md`.
    `behavioural-C` KEEPS: exit 0 (report, not refuse), `archive_stage === 'failed'`, the `archive_stage_failed` finding raised, the gitignored candidate stayed out of the index, `archive_stage_detail` carries git's own message, and the finding written durably. It is now the total-failure case and is named as one.
    A marker was added at `pathsNotStaged` (claim.js:4185) saying the logic is LIVE and merely unwitnessed, reachable on in-place and `source-missing` shapes — the #989 lesson is that unwitnessed code later reads as dead and gets deleted.
    **`node scripts/test-forge-finalize-findings.js` → 233 passed, 0 failed, exit 0.** `edition-sync --write` then carried the 4 codex byte-copies.

- item: #989 — decide T11's `roadmap_staged === false` gap in `test-finalize-door.js`: the assertion is not vacuous (hardcoding `roadmap_staged = true` is caught) but removing the `archiveAddOk &&` gate is NOT caught, because `buildMainResidentRun`/`initSelfHostRepo` never populate `.roadmap`, making `existingPaths` structurally empty. Ruled out already: planting roadmap-shaped fiction in the fixture. Either reach the gate through something that genuinely exists post-retirement (`.roadmap/` holds `_rules.md` in a real repo), or write the coverage statement into the test — accepting the gap is legitimate if it is WRITTEN.
  status: done
  dispatched: self, inline — the repair lands in `$WT/scripts/test-finalize-door.js` (`buildMainResidentRun` gains a tracked `kaola-workflow/.roadmap/_rules.md`, committed BEFORE the worktree add so the linked worktree carries it). `_rules.md` is tracked in THIS repo (`git ls-files kaola-workflow/.roadmap/` → `.gitkeep`, `_rules.md`) and CLAUDE.md's Durable State Contract names it as the one local file that survives — so this reproduces reality, it is not the ruled-out "plant roadmap-shaped fiction". Verdict must be MUTATION-PROVEN: deleting `archiveAddOk &&` from claim.js:4876 must turn T11 RED.
  result: DONE — the gate is reached, not documented-around. `buildMainResidentRun` now seeds and commits a tracked `kaola-workflow/.roadmap/_rules.md` before the worktree add, so `existingPaths` is genuinely non-empty in the linked worktree. Three assertions added to T11: a PREMISE (the worktree carries `.roadmap`, so a future fixture that stops providing it reds here and says why), the existing statusfail `roadmap_staged === false` (message rewritten — it was describing a distinction the fixture could not make), and a new CONTROL-leg `roadmap_staged === true`.
    **Mutation-proven both ways, canonical only, one site at a time** (`scripts/test-finalize-door.js`, run from the worktree):
    - baseline 487 → 491 assertions, exit 0.
    - MUTANT A, drop `archiveAddOk &&` at claim.js:4880 → `FAIL: T11(root statusfail) ... got true`, exit 1. This is the gate #989 reported as unreachable; it is now reachable.
    - MUTANT B, drop `.roadmap` from `candidatePaths` at claim.js:4829 → `FAIL: T11(root control) ... got false`, exit 1. This pins #988's "keep `.roadmap`" decision.
    - Only the `root` (canonical) leg redded under a canonical-only mutant, so the four per-edition legs are independent witnesses rather than one mutant reading as four.
    claim.js byte-restored from `scratchpad/claim.bak.js` after each mutation and verified IDENTICAL.

- item: #987 — diagnose `testClosureAuditTimeoutEnvOverCapFallsBack` (walkthrough:8790, registered 11669): determine by measurement whether it is unreachable, tautological, or merely mutation-insensitive on this Node version — its sibling `InvalidFallsBack` DOES red under the closure-audit timeout-clamp mutation and this one does not. Then give it teeth or delete it with a stated reason. Explicitly forbidden: raising a threshold or relaxing the mutation to make the question go away. Note ADR 0018 §5 already re-pointed its discriminator off the deleted `stale_roadmap_sources`/`closed_remote` reason, so the current form may differ from the archived evidence at `kaola-workflow/archive/bundle-984-985/.cache/m984-fixture-rebuild.md:63-78,301`.
  status: done
  dispatched: self, inline — diagnosis by direct measurement of `execFileSync`, then mutation runs via `node scripts/simulate-workflow-walkthrough.js --only testClosureAuditTimeoutEnvInvalidFallsBack testClosureAuditTimeoutEnvOverCapFallsBack` in the worktree; the verdict and its evidence land in `.cache/987-diagnosis.md` and the edit (if any) in `$WT/scripts/simulate-workflow-walkthrough.js`. Only ONE copy survives — the forge editions' copies went with #984.
  result: DONE — verdict **merely mutation-insensitive**, deleted with the reason. Full record: `.cache/987-diagnosis.md`.
    Root cause is a runtime fact: on Node v24.18.0 `execFileSync` accepts EVERY finite positive `timeout` (2**31, 2**53, 1e21, 1e300, MAX_VALUE — all measured, none throw). Only `Infinity` throws, and `Infinity` fails the `Number.isInteger` guard and cannot come out of `parseInt` of a digit string. So no input reaches the clamp and crashes, and the test's stated premise ("causes execFileSync to throw ERR_OUT_OF_RANGE") is false here.
    **TRAP, cost a full cycle:** there are TWO identically-named `REMOTE_TIMEOUT_MS` constants with identical bodies. `active-folders.js:9` feeds `probeIssueState` and IS the one both tests discriminate on; `closure-audit.js:44` feeds `detectStaleLabels` and moves NEITHER test. The first two mutants hit closure-audit.js and produced green runs that looked like evidence and were not.
    Re-run on `active-folders.js`: MUTANT E (delete `Math.min(n, 600000)`, the exact mechanism the test is named after) → BOTH tests still PASS. MUTANT F (delete the `Number.isInteger` guard) → `InvalidFallsBack` REDS with `unresolved_closed_state: [941]`, proving the axis is armed and only the over-cap half is dead.
    Action: deleted the function + its `add(...)` registration, left a tombstone carrying the derivation, and left a one-line marker at BOTH clamp sites — **the clamp is live and must not be deleted for lacking a test**; it still bounds the hang at 600000 ms, which costs ten minutes to witness, so teeth were not available at a testable cost. Post-delete: `--only testClosureAuditTimeoutEnv` → sibling PASSED, exit 0; scenario registry lists 186.

- item: Verify the bundle — run the walkthrough at FULL scope (the claude fast gate samples it at a rotating 1/12 shard, so a green there does not verify these changes) plus `npm test` for the four chains, since `claim.js`/`sink-merge.js` are byte-identical across editions and this diff touches them.
  status: done
  dispatched: self — FULL walkthrough already green (184/184 scenarios, exit 0, run from the worktree). `npm test` (four chains) launched in the BACKGROUND from the worktree cwd; log lands at `scratchpad/npmtest.log`, task id `bzih4qh1c`. **cwd was verified as the worktree before launching** — an earlier run in this session silently executed in the MAIN root and produced a meaningless green, so the check is not optional. Read EACH chain's own exit code from the log; a wrapper-green can hide a SIGTERMed chain (143/137 = killed).
  result: DONE, both tiers green.
    FULL walkthrough (not the 1/12 shard the fast gate samples): **184/184 scenarios, 0 failed, exit 0**.
    `npm test` four chains: **exit 0 in 12m06s** (13:47:20 → 13:59:26). Verified beyond the wrapper — all four chain headers present (`claude`, `codex`, `gitlab`, `gitea`), zero `killed`/`SIGTERM`/`137`/`143`/`npm ERR` hits in the log, and the run ends on `generate-routing-surfaces --check: all 18 surfaces byte-match the skeleton`. The chains are `&&`-chained, so a non-zero or signalled chain could not have reached the wrapper's 0.
    Per-suite highlights: `test-forge-finalize-findings.js` 233/0 (was 252/1 mid-run), `test-finalize-door.js` 491 assertions, edition-sync byte groups + committed Oracle Kernel parity green, 18 routing surfaces byte-match.

- item: Dock the docs — `CHANGELOG.md` under `[Unreleased]`, and any doc row that describes the removed pathspecs or the T11/OverCapFallsBack pins. Check whether `docs/api.md` or `docs/conventions.md` cite them.
  status: done
  dispatched: self, inline — edits to `$WT/docs/api.md` and `$WT/CHANGELOG.md`, made BEFORE the four-chain run so the chains gate the final tree rather than a pre-docs one.
  result: DONE. `docs/api.md` — the surviving-after-retirement paragraph claimed finalize "still stages `kaola-workflow/.roadmap/` and `kaola-workflow/ROADMAP.md`"; corrected to `.roadmap/` alone plus a new paragraph saying the mirror pathspec went in #988 and that migrating a consumer's own `ROADMAP.md` stays a separate deliberate act (ADR 0018 §8 step 6). `CHANGELOG.md` `[Unreleased]`: #988 and #987 under `### Removed`, #989 under `### Fixed`.
    Surveyed every other `ROADMAP.md` citation in `docs/` and `README.md` and left them ALONE deliberately: `docs/investigations/*` and `docs/decisions/*` are historical records of what was true when written, and `docs/conventions.md:805,809` describes the reserved-name/parked-lane classification, which is STILL LIVE — `claim.js:3535` and the residue walker must keep classifying an unmigrated consumer's `ROADMAP.md` correctly.
