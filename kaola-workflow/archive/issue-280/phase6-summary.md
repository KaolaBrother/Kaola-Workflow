# Phase 6 - Summary: issue-280

## Delivered
Fixed the #277 closure attestations false-failing (`failed`/`missing`) on a clean, fully-delegated adaptive run. Two root causes:
- M1: the workflow-planner spawn was structurally unloggable (dispatched first; its own claim creates the project `.cache/`). Fixed via planner self-attest — `agents/workflow-planner.md` startup passes `--attest-planner-spawn`; `claim.js` back-fills the planner dispatch-log entry (fail-open), gated strictly on the flag so a #276 inline bypass still reports missing/failed.
- M2: `sink-merge` built closure_receipt without calling `checkDispatchAttestations`, so emptyReceipt's `failed` default survived. Fixed by running the same archive-first `checkDispatchAttestations` (reusing the exported helper) before emitting the receipt.

## Files Changed (6 = fix node write-set + 1 finalize CHANGELOG)
- scripts/kaola-workflow-claim.js (+ plugins/kaola-workflow/scripts/ byte-mirror)
- scripts/kaola-workflow-sink-merge.js (+ plugins/kaola-workflow/scripts/ byte-mirror)
- agents/workflow-planner.md (startup invocation + inline note)
- scripts/simulate-workflow-walkthrough.js (3 regression tests)
- CHANGELOG.md ([Unreleased] ### Fixed entry)

## Test Coverage
3 new regression tests (AC1 fully-delegated ⇒ both attestations attested in both receipts; AC2 zero-spawn inline ⇒ both stay missing/failed; non-circular contract guard that workflow-planner.md passes the flag). Hand-rolled assert suite (no coverage %).

## Final Validation Evidence
- `node scripts/simulate-workflow-walkthrough.js` → "Workflow walkthrough simulation passed".
- Full `npm test` → green across all four editions (Claude, Codex, GitLab, Gitea); validate-script-sync OK (17 common + 7 byte groups); validate-vendored-agents OK (13 agents). Evidence: .cache/fix.md, .cache/review.md.
- Adaptive barrier gates (whole-plan): resume=0 gate=0 barrier=0 verdict=0 (barrier-check: no sensitive hits, no out-of-allowlist).

## Documentation Docking
DOCKED. CHANGELOG [Unreleased] ### Fixed entry covers both mechanisms + the new internal flag. docs/api.md attestation-invariant descriptions (lines 1060-1063) remain accurate (they describe WHAT is checked, not HOW the spawn is logged; "recorded before the plan was frozen" stays true — the back-fill runs during the claim). The `--attest-planner-spawn` flag is internal (passed only by the planner procedure, never a user), so no api.md edit required; api.md is also out of the finalize node's write-set. No README/architecture/.env impact (behavior-only internal bug fix). doc-updater skipped: no public API/schema/setup/env change.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | all green first pass |

## Follow-Up Items
- Codex `.toml` planner-profile attestation parity (the 3 Codex/forge planner ports do not pass the flag) — deliberately out of scope; tracked by #266/#286 (Codex attestation story). Not a new user-decision item.

## Closure Decision
None needed. Closure scan of phase artifacts (.cache/fix.md, .cache/review.md, workflow-plan.md Design Notes): no deferred items, conflicts, partial work, or user-decision items beyond the pre-tracked #266/#286 follow-up. Reviewer verdict: pass, zero findings.

## Commit And Push
[pending final Git gate; contractor runs cmdFinalize + chore: finalize commit; final hash reported after sink push]

## GitHub Issue
280 — to be closed by the sink after merge (acceptance criteria pass).

## Roadmap
[regenerated by contractor Step 7]

## Archive
[archive path set by cmdFinalize Step 8b]

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | skipped | docs-impact check (this summary, Documentation Docking) | behavior-only internal bug fix; no public API/schema/setup/env change; CHANGELOG covers it |
| documentation docking | invoked | this summary (Documentation Docking) | |
| closure advisor gate | N/A | closure scan (this summary, Closure Decision) | no deferred/user-decision items |
| final-validation fix executors | N/A | all green first pass | no failures to route |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | contractor Step 7 |
| archive completed folder | pending | | contractor Step 8b |
| final commit and push | ready | git status/diff | final gate after this file is committed |

## Status
READY FOR FINAL GIT GATE
