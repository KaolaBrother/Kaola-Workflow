# Finalization - Summary: issue-670

## Delivered
Fence-detection PARITY fix (post-#665 residual). `schema.locateSection`'s fence regex is now
`/^\s{0,3}(`{3,}|~{3,})(.*)$/` matched against the RAW line at both scan sites (was
`ln.trim().match(/^(`{3,}|~{3,})(.*)$/)` — ANY indentation), byte-identical to the hash-covering
classifier `markdownFenceTransition`. An indented (4-space) backtick decoy can no longer make a runtime
section reader select a fenced heading the classifier keeps inside the fence — closing the `plan_hash`
mismatch wedge on resume/barrier. The run-length/family/empty-suffix closer semantics and first-hit
heading selection are unchanged (surgical — only the indent anchor moved).

## Files Changed
- scripts/kaola-workflow-adaptive-schema.js (+ plugins/kaola-workflow, gitlab, gitea copies — byte-identical ×4) — locateSection fence anchor
- scripts/test-adaptive-node.js — T6d indented-decoy end-to-end regression (locateSection ↔ classifier.sectionBody parity + clean --resume-check)
- CHANGELOG.md — `[Unreleased]` entry for #670
- kaola-workflow/.roadmap/issue-673.md — new follow-up source (run gap R1)

## Test Coverage
Not a coverage-gated repo. `node scripts/test-adaptive-node.js` → 1780 assertions pass (1777 baseline +
3 T6d); `node scripts/simulate-workflow-walkthrough.js` → "Workflow walkthrough simulation passed";
`node scripts/edition-sync.js --check` → clean (4 adaptive-schema copies byte-identical).

## Final Validation Evidence
Self-host four-chain receipt (`.cache/chain-receipt.json`) stamped by `run-chains.js --project issue-670`
after the feature commit. Per-node evidence: `.cache/n1-fence-parity.md` (RED→GREEN), `.cache/n2-review.md`
(verdict pass, 0 blocking), `.cache/n3-adversary.md` (NOT-REFUTED, verdict pass; ~250 parity checks +
12,000 fuzzed docs, zero fence divergences).

## Documentation Docking
DOCKED — sole doc surface is the CHANGELOG `[Unreleased]` entry; no public interface/API/env/architecture
changed (classifier oracle untouched). Evidence: this summary + `.cache/n4-finalize.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- #673 — `locateSection` heading-match divergence (`startsWith` prefix vs classifier's anchored `^##\s+H\s*$`), a `plan_hash`-wedge-class residual in a DIFFERENT channel than #670's fence anchor; adversary finding R1, plan-pre-authorized as out-of-scope.
- Adversary R2 (CRLF) is NOT a defect — both parsers are identically CRLF-blind and #670 removed a pre-fix `trim()` CRLF divergence; recorded here for completeness, not filed.

## Run gaps
- manual:locatesection-heading-match-divergence (locateSection selects headings via startsWith prefix while the classifier uses an anchored heading regex so a runtime reader can pick a heading the hash-covering classifier does not): filed: #673

## Closure Decision
One deferred residual (R1), pre-authorized by the plan as out-of-scope for the fence-anchor AC and filed
as #673 (see Run gaps). No unresolved conflict, partial implementation, or value/standing decision for
#670 itself — the fence-anchor parity fix is complete and both gates passed (code-review + adversary).
Close #670.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#670 — to be closed by the merge sink after the four-chain merge gate.

## Roadmap
Regenerated by cmdFinalize: removes .roadmap/issue-670.md, adds .roadmap/issue-673.md, retains #671/#672.

## Archive
pending (cmdFinalize Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| n1-fence-parity (tdd-guide) | invoked | .cache/n1-fence-parity.md | |
| n2-review (code-reviewer/fable) | invoked | .cache/n2-review.md | |
| n3-adversary (adversarial-verifier/fable) | invoked | .cache/n3-adversary.md | |
| doc-updater | skipped | CHANGELOG-only, no checklist doc impact | sole doc surface is CHANGELOG, updated inline in the finalize node |
| documentation docking | invoked | this summary + .cache/n4-finalize.md | |
| final-validation fix executors | N/A | — | no final-validation failure |
| roadmap refresh | pending | kaola-workflow/ROADMAP.md | cmdFinalize Step 8b |
| archive completed folder | pending | | cmdFinalize Step 8b |
| final commit and push | ready | git status/upstream check | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE

## Attestation
claim_planner_attested: attested
finalize_contractor_attested: attested
