# Run-gap manual seeds — issue-670

Deferred adversarial-verifier residual R1: a SEPARATE parity divergence family (heading match, not the
fence anchor #670 fixed), pre-existing and out-of-scope for #670's AC, filed as #673.
R2 (CRLF) is NOT a defect (parity holds; #670 removed a pre-fix trim CRLF divergence) — not seeded.

gap: locatesection-heading-match-divergence — locateSection selects headings via startsWith prefix while the classifier uses an anchored heading regex so a runtime reader can pick a heading the hash-covering classifier does not
