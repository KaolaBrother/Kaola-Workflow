# Measure whether Adaptive subagents switch models by task difficulty

| item | status | dispatched | result |
|---|---|---|---|
| Build probe fixture: fresh Git repo `/tmp/kw-adaptive-subagent-probe` with one custom profile `model: adaptive`, a driver script that dispatches subagents with different task prompts/roles, and a DB query script to extract `generation_model` per subagent session | done | self → created fixture files under `/tmp/kw-adaptive-subagent-probe`; probe notes mirrored in `kaola-workflow/bundle-1061/.cache/probes/` | Fixture created at `/tmp/kw-adaptive-subagent-probe` with profile `kwp-adaptive.md` (model: adaptive), `prompt.txt`, `run.sh`, `db-query.sh`. |
| Run Adaptive parent session and dispatch subagents across task-difficulty gradient (trivial / medium / complex) and Kaola role descriptions, capturing raw outputs and session ids | done | self → run `/tmp/kw-adaptive-subagent-probe/run.sh`; raw outputs in `/tmp/kw-adaptive-subagent-probe/out/run/`; summary to `kaola-workflow/bundle-1061/.cache/run.md` | Parent session `thankful-dragon`, `sessions.model=adaptive`, parent `generation_model=gpt-5-6-sol-low` for all 20 steps. Subagents A/B/C (TRIVIAL/MEDIUM/COMPLEX) dispatched with profile `kwp-adaptive` (model: adaptive), all replied `POWERED=You are powered by Adaptive.`, `MODEL_ID=UNKNOWN`. Subagent D (architect role) was rejected 3 times (`Subagent error: Tool was rejected`). |
| Query the session DB for each subagent's `sessions.model` and `message_nodes.generation_model` distribution | done | self → query `sessions` and `message_nodes` for `thankful-dragon`; raw output `/tmp/kw-adaptive-subagent-probe/out/db/subagent-models.txt`; summary in `kaola-workflow/bundle-1061/.cache/db.md` | Parent session `thankful-dragon` has `sessions.model=adaptive`. Subagent nodes appear as distinct contexts within the parent's `message_nodes` (unique `message_id` per subagent `powered by` node). All subagent system prompts report `You are powered by Adaptive.` Subagent assistant reply nodes have empty `generation_model` and `model_name` in metadata; no local field exposes the concrete model used for subagent inference. |
| Summarize whether Adaptive subagent routing varies by task difficulty, delete probe fixture, confirm `devin doctor` baseline, and post the evidence comment on #1061 | done | self → delete `/tmp/kw-adaptive-subagent-probe`, run `devin doctor --json`, post comment via `gh issue comment 1061` | Comment posted: https://github.com/KaolaBrother/Kaola-Workflow/issues/1061#issuecomment-5643884536. `/tmp/kw-adaptive-subagent-probe` removed. `devin doctor --json` returns `ok: true` with 14 Kaola profiles loaded. |

Constraints: measurement only. No edits to `templates/`, `scripts/`, `docs/`, generated mirrors, or installed `~/.config/devin/` carrier/profiles/config. Artifacts land under `kaola-workflow/bundle-1061/.cache/`. No price inference. No `git push`, no PR, no sink inside missions.
