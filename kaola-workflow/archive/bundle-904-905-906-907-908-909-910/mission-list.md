# Close #904–#910: the #900–#903 review fallout plus the live vrpai-cli consumer-run defects

Run posture: worktree. Work tree is
`/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-904-905-906-907-908-909-910`.
Main root `/Users/ylpromax5/Workspace/Kaola-Workflow` holds this file and the durable run folder.
Both were at `2018521f` at claim time.

Port topology measured at claim: `validation-runner.js` and `adaptive-schema.js` exist in four
byte-identical copies (canonical, `plugins/kaola-workflow`, `-gitlab`, `-gitea`). `claim.js`,
`sink-merge.js`, `run-chains.js` are canonical + Codex byte-identical, with forge ports named
`kaola-{gitlab,gitea}-workflow-*.js`.

Standing rule for this run: every premise gets measured before anything is built on it. Five of the
last six bundles shipped work on a premise that turned out false.

- item: Measure #904 — does the runner's sandbox root really push a tsx IPC pipe past macOS's 104-byte `sun_path` limit, and what is the shortest root that fixes it without weakening the sandbox? Sandbox is `$TMPDIR/kaola-workflow-validation/<64-hex-policy-seed>/{home,tmp}`; the seed length is the whole problem. Also check the sibling cargo/`.rustup` finding.
  status: done
  dispatched: investigator (read-only, measures on this box's real macOS TMPDIR) → report at `kaola-workflow/bundle-904-905-906-907-908-909-910/.cache/premise-904.md` in the MAIN root
  result: `.cache/premise-904.md` — HOLDS with two corrections. (a) Truncating the seed alone does NOT fix it: dirname `kaola-workflow-validation` + an 8-hex seed still binds at 106 > the measured 104/105 threshold. The DIRECTORY LITERAL must shrink too; `kwv/<16-hex>` = 92 chars, 12 spare. (b) Real lengths are 143/162, not the issue's ~146/~165. Seed WIDTH is not load-bearing, its DETERMINISM is (same policy → same `command_id`); path reaches the receipt only as `sha256(HOME)`/`sha256(TMPDIR)`. Two default sites, not one: runner :727-730 (seeded) and :177-178 (unseeded twin). Reproduced through the real CLI with a one-axis control leg: long → `outcome: fail`, shortened → `outcome: pass`. NOTHING pins the shape — `defaultSandboxPaths` unexported and NO suite invokes the `run` subcommand at all, so this code has zero coverage today. Four copies byte-identical `992fad71…`, enforced `validate-script-sync.js:138-143`. Sibling cargo/`.rustup` finding reproduces exactly; `--env-allowlist` is the only remedy and allowlisting `HOME`/`TMPDIR` is SILENTLY IGNORED. Likely not macOS-only (Linux `sun_path` 108 vs 119 current shape) — inferred, not measured.

- item: Measure #905 — what does the runner have in hand at hash time and throw away, and what is the smallest change that makes a red receipt diagnosable? The normalized failure-signature text is already computed to be hashed; find out whether stdout/stderr are still live at that point or already streamed away.
  status: done
  dispatched: investigator (read-only) → report at `.cache/premise-905.md` in the MAIN root
  result: `.cache/premise-905.md` — HOLDS in full, not solved by any flag, and it is a code gap not a doc gap. Captured a real red receipt: `outcome: fail`, `reduction_reasons: []`, zero bytes of child output anywhere; runner terminal stdout/stderr both 0 bytes; no raw log on disk. Proved the preimage was ALIVE at hash time — `normalizeFailureSignature` returns `{normalized, digest}` at `:633-641`, caller at `:807-809` binds both and reads only `.digest`; re-derived all three digests out-of-band and they match exactly. The whole diagnosis fitted in 165 bytes. ONE CORRECTION: there is NO truncation cap to reuse — `MAX_OUTPUT_BYTES` is `spawnSync`'s `maxBuffer` and exceeding it SIGTERMs the child into `inconclusive`; a cap would be new code. NOTHING pins the receipt's field set — every assertion recomputes rather than compares literals, so an added field breaks no pin. Porting is one command: `npm run sync:editions`. **BLOCKER — value call:** all three fix directions reverse `docs/decisions/D-697-01.md:57` ("raw child output are not persisted"), which nothing machine-enforces. `normalizeOutputText` redacts absolute paths ONLY, no secret redaction, and `kaola-workflow/**/.cache/` is TRACKED (6593 files committed today) — so retained output lands in permanent git history. Direction 1 (`--keep-output`) is the only one with zero effect on receipt hash properties; `audit` is the only determinism-safe home for a path field; direction 3 is one line but re-keys every `vector_id` once and is unbounded to 16.78 MB.

- item: Measure #906 — reproduce both destructive routes on real fixtures: the `#395.4` backstop at `claim.js:4090-4092` deleting main's live folder uncompared, and the three-row symlink table (`.cache/link-evidence.md` refused vs `extra-link.txt`/`notes-link.md` destroyed at exit 0). Report what `verifyArchiveComplete` returns for each and what its return shape is pinned to across editions.
  status: done
  dispatched: investigator (read-only, builds throwaway fixtures under the scratchpad) → report at `.cache/premise-906.md` in the MAIN root
  result: `.cache/premise-906.md` — BOTH ROUTES HOLD, both reproduced end-to-end. Route 2's three-row table reproduces EXACTLY, row for row, plus TWO MORE destructive rows the issue did not report. Route 1 reproduced: exit 0, `main_live_cleaned_on_resume: true`, a main-only file destroyed and never in the archive. Positive control armed: leg C1 refuses under the identical `KAOLA_WORKFLOW_OFFLINE=1` env, so the comparison under test is demonstrably not silenced. TWO CORRECTIONS: the delete is at `claim.js:4107` (block 4101-4111), not `:4090-4092` (that is the terminal-state repair); and "terminal-closed" is enforced upstream by `resolveFinalizeAuthority:3583`, not by the backstop's own condition — consequence, `archiveStateStamped = 'repaired'` at `:4093` is UNREACHABLE from `cmdFinalize`. **NEW LIVE DEFECT IN THE VERY SET #906 TURNS ON:** GitLab `:4964` and Gitea `:4958` list THREE fixed names where canonical/Codex list FOUR — `mission-list.md` is ABSENT. Verified by RUNNING all four editions' exported `verifyArchiveComplete` against one fixture: on GitLab/Gitea a main-only symlink named `mission-list.md`, the ADR 0017 run record itself, is destroyed at exit 0 in its ORDINARY non-dangling form. The issue's "shared across four editions and pinned" is true of the shape and body, FALSE of the name set. **ROUTE 1's TRADE-OFF IS LARGELY DISSOLVED:** the backstop's own comment at `:4098-4099` states the goal as stopping `readActiveFolders` from claiming, NOT removing the folder — and two lossless dispositions achieve that exactly (move-aside to a non-`.archived-` suffix, or stamp main's state terminal in place), both measured to give `active` count 0 with the evidence file SURVIVING, versus today's delete which gives 0 and DESTROYED. Refusing instead costs a permanent phantom active claim (`status` count 1 from main, re-claim answers `owned` — a correction to #395's wording, which said `user_target_blocked`; `owned` is worse, it silently resumes a finished run). Measured constraint: a `.archived-` suffix breaks the next resume (`archive_authority_ambiguous`), a `.orphan-` suffix does not.

- item: Measure #907 — confirm the six `split('\n')` readers at `claim.js:2577,3349,3361,3392,5195` and `sink-merge.js:458`, whether each can ever see a path, that `git add -f -- <dir>/.git` on a `.git`-named FILE exits 0 and indexes nothing, and the three copies of the journal regexp. A grep is a hypothesis: read each site.
  status: done
  dispatched: investigator (read-only) → report at `.cache/premise-907.md` in the MAIN root
  result: `.cache/premise-907.md` — TWO OF THE ISSUE'S CLAIMS ARE FALSE, and three defects it never names are LIVE. (1) "A `.git`-named FILE is a permanent unclearable block" is FALSE and never was true: `requiredArchiveFiles` (`sink-merge.js:1346`) skips it from birth in 7350ba9c, so `missingBlobs` is empty and the refusal at `:2379` never fires. But a real permanent block sits next door that the skip does NOT cover — a `.git` DIRECTORY or valid gitfile collapses the tree into a `160000` gitlink, `ls-tree -r -z` returns only the gitlink while `requiredArchiveFiles` still demands the siblings beneath, `forcePaths` is empty, and a hand `git add -f` exits 128 `fatal: … is in submodule`. Fix must skip the whole SUBTREE, not the entry. AC3 is right; its rationale needs rewriting. (2) "Six readers" UNDERCOUNTS: 14 readers split on '\n' across five files, 5 are path-bearing, THREE are live defects. **THE BIG ONE IS A FALSE GREEN:** `parsePorcelainPaths` (`adaptive-schema.js:438` — the byte-identical ×4 anchor) strips quotes WITHOUT unescaping then `.trim()`s; reproduced with #900's own filename `notes.md␠`, cmdFinalize (`claim.js:4487-4506`) feeds the mangled path to `git add -A --`, which exits 128 and stages NOTHING — not even the good files beside it — `catch (_) {}` swallows it, `git diff --cached --quiet` returns 0, and finalize reports `finalize_commit: 'nothing_to_commit'` AS SUCCESS. Also live: `sink-merge.js:1449` is a SECOND divergent porcelain parser with no quote strip, so a quoted path defeats the #893 own-archive-mirror exemption → permanent `sink_blocked` over the run's own evidence; and `run-chains.js:628/630` `isEditionCouplingPath` reads `indexOf('plugins/')===0 === false` for a C-quoted path, scoping an edition-touching diff to ONE chain — that is the "B1 fail-open hole" whose comment claims it is "fail-closed by construction", measured false. Stale line numbers: 3392→3377, 5195→5133, sink 458→451. The two `for-each-ref` sites are measurably immune (git rejects refnames with LF/TAB/space/backslash) so they want documenting, not converting. "None feeds durable evidence" is WRONG — `sink-merge.js:451` writes `workflow_only_files` onto a recorded sink finding. Journal regexp confirmed exactly three copies: `claim.js` 2533/2715/4490, no fourth. PORT TRAP: `validate-script-sync` enforces canonical↔Codex byte-identity and four-way for adaptive-schema, but the GitLab and Gitea ports of claim/sink-merge are policed by NOTHING (absent from COMMON_SCRIPTS and RENAME_NORMALIZED_FAMILIES; `edition-sync.js:30-34` says hand-ported) — so AC5 coverage must be BEHAVIOURAL tests, not parity. RISK: converting `parsePorcelainPaths` to `-z` is not a one-line change — `-z` porcelain has no rename arrow (rename records are two NUL-separated fields), so the parser BODY changes, on the ×4 drift anchor, across 8 call sites. **FALSE-GREEN FOLLOW-UP (challenged, then settled):** the first report said "reproduced" but was INFERRED-FROM-LINKS; on challenge the agent ran it and it is now MEASURED-END-TO-END, appendix in the same file. One-axis A/B, two fixtures differing only by a `notes.md␠` file, identical command both legs. Control: exit 0, `finalize_commit: "committed"`, commit authored. Hazard: exit 0, stderr `fatal: pathspec 'notes.md' did not match any files`, `closure_invariants.ok: true`, `finalize_commit: "nothing_to_commit"`, NO finalize commit in `git log`, and `src/pending-good.js` — the GOOD file — left uncommitted. Positive control armed: `KAOLA_WORKFLOW_OFFLINE=1` set on BOTH legs, so it cannot be the suppressor. Does NOT converge: a second finalize is byte-identical. REACHABILITY: gated by `claim.js:4402 if (args.keepWorktree)` AND `:4407` linked-worktree-only — so the blast radius is `--keep-worktree` linked runs, **which is this project's own documented finishing sequence and is exactly what THIS run will do**. Two further facts: `finalize --check` already emits the mangled name in `dirty_paths` at exit 0 `ok:true`, reporting a path that does not exist; and the wrong name is NOT durable — nothing in the archived run folder records it, so a reader of the archive has ZERO trace anything was dropped. TWO INDEPENDENT FAULTS COMPOUND: the parse, and the `catch (_) {}` at `claim.js:4506` that converts a failed `git add` into `nothing_to_commit` — fixing only the parse leaves every other staging failure equally silent.

- item: Measure #910 — from the live worktree, run `run-chains --project bundle-904-905-906-907-908-909-910` and then `finalize --check`, and record where the receipt actually lands versus where the gate reads. Then find whether `resolveRecordFolder`'s split is genuinely reusable from run-chains, and whether `codeTreeHash` really would change if run from main.
  status: done
  dispatched: investigator (read-only apart from the receipt it writes) → report at `.cache/premise-910.md` in the MAIN root
  result: `.cache/premise-910.md` — CONFIRMED, reproduced live on this run's own worktree first try and again in an independent fixture. Every factual claim held: receipt lands in the worktree, gate reads main's `source_dir`, four green chains classify `chains_unverified`, Step 8a mirrors main→worktree (`claim.js:3233-3234`, wrong direction), running from main instead gives `chains_stale`, and the manual byte-identical copy does flip it to `chains_green`. **THE ISSUE'S FIX IS NOT AVAILABLE:** `resolveRecordFolder` is NOT exported from anything — it is private at `validation-runner.js:1105-1123`, absent from that module's 29 exports and from `adaptive-schema.js`. "Just reuse it" is off the table. Three honestly-sized options: (1) export it from validation-runner and add a new run-chains→validation-runner edge, ×4 copies; (2) move it into `adaptive-schema.js`, the cross-edition drift anchor, ×4 byte-identical; (3) re-derive the ~12-line split from `schema.resolveMainRoot`, which IS exported and ALREADY imported at `run-chains.js:143` — no new dependency but a second implementation of one rule. SECOND FINDING NOT IN THE ISSUE: nothing tests this in ANY edition — `test-run-chains.js` has zero `git worktree add` and the gitlab/gitea suites never mention "worktree". That is why it shipped. Cheap-receipt hook for testing: `--chains claude --mock-chain claude:<script>` (`run-chains.js:898`) — ~1s instead of ~25min, and `evaluateChainReceipt` cannot distinguish a mocked receipt. Precedence today: `--output` > `--plan` > `--project` > cwd (`:748-762`). Agent cleaned up both reproduction receipts; verified restored.

- item: Verify each of #908's five recorded gaps and its five "also worth recording" notes still describe the code that shipped — several name exact line numbers and an exemption list at `validate-script-sync.js:485`. Distinguish gaps that are now closed, still open, or were never real.
  status: done
  dispatched: investigator (read-only) → report at `.cache/premise-908.md` in the MAIN root
  result: `.cache/premise-908.md` — **THE DATING FACT GOVERNS THREE VERDICTS: #908 was filed 11 minutes BEFORE the repair commit `7350ba9c` landed**, so gaps 1 and 3 describe a mid-run snapshot, not what shipped. G1 ALREADY CLOSED — `test-claim-hardening.js:4745-4795` drives `claim.js release` end-to-end and asserts `refuse`/`archive_incomplete`/`missing[]` naming the file, mutation-proven armed (MUT-1 → exactly +5 failures); the proposed extraction is already in the tree at `claim.js:5220` and NO EXPORT WAS NEEDED. G2 STILL OPEN, reasoning holds, plus a new adjacent unpinned case G2-b. G3 STILL OPEN but NARROWED — the self-host worktree-lane full finalize IS pinned (`:4383-4407`); the un-driven case is the CONSUMER-repo worktree lane (`test-finalize-door.js:1435` stops at `--check`). G4 STILL OPEN confirmed — codex walkthrough and codex contracts validator contain ZERO closure-audit references; a mutated codex copy passes the whole codex chain and is caught only by `validate-script-sync` byte-identity, which IS armed. G5 STILL OPEN but **CHEAPLY CLOSABLE WITH NO EXPORT** — mutation-proven that deleting `.filter(e => e.isDirectory())` at `closure-audit.js:220` leaves both scenarios GREEN, and a fifth CLI leg (a regular FILE in the band matching the name shape) discriminates it; working fixture already built. Na CONFIRMED AND WORSE — no test asserts either hint's text and a THIRD unbound spelling sits in `validation-runner.js:1411`'s usage(). Nb/Nc/Nd CONFIRMED by measurement with controls; Nd's unwritable-dest arm still reports `sync_required` `ok:true` `reasons:[]` while the real transaction exits 1 with a typed `archive_exception`, destroying nothing. Ne SETTLED — the stated rationale is WRONG but the exemption is NOT too wide: a legitimate byte difference exists on the `mainLive ↔ dest` pair, not `src ↔ dest`, and byte tolerance there is a CALLER property (`claim.js:2531` reads `.missing` only) covering every file, exempt or not.

## Owner decisions (2026-08-02, in conversation)

- **#907 swallowed failure** → REPORT, don't refuse. Fix the parse AND make a failed `git add` emit a typed finding on the envelope and in `finalization-summary.md`; exit stays 0.
- **#905 retention** → opt-in `--keep-output <path>`. Default posture unchanged; amend `D-697-01` to record the carve-out. Not the always-on inline text.
- **#906 route 1** → move aside to a `.orphan-<ts>` suffix. NOT `.archived-` (measured: breaks the next resume with `archive_authority_ambiguous`).
- **#909** → remove #796's label; rehome the residue's unique `sink-receipt.json` into the sibling archive then delete the residue; record the 4 citation findings as decisions. (User also ticked "delete outright", which contradicts rehoming — taking the lossless reading; the delta is one `git rm` if they meant otherwise.)
- **#910 mechanism** → orchestrator's call on facts: EXPORT `resolveRecordFolder` from validation-runner and have run-chains inject the `adaptiveSchema` it already imports. NOT a move into `adaptive-schema.js` — validation-runner requires only Node builtins today and the resolver already takes `schema` as a parameter, so a move would break the one module with no sibling edges.

- item: Verify #909's two drift findings against this repo as it stands — #796's `workflow:in-progress` label, and `archive/bundle-429-434/` holding exactly one tracked file and no `workflow-state.md`. Also run an unscoped `closure-audit` and report the full finding set, so the "zero findings" criterion has a baseline.
  status: done
  dispatched: investigator (read-only, must not pass `--execute`) → report at `.cache/premise-909.md` in the MAIN root
  result: `.cache/premise-909.md` — both findings HOLD; the issue's SCOPE is short by four findings. Finding 1: #796 really carries the label (two independent reads agree), and the widened sweep makes it EXACTLY one — no second closed issue, no unbacked open issue (the seven `workflow:in-progress` open ones are this run's own claim). Finding 2: holds in every particular — one tracked file, no untracked entry, no `workflow-state.md`, valid sibling archive, report-only class, sole instance; line citation in the issue is stale. NEW FACT the issue omits: the residue's `sink-receipt.json` is UNIQUE — the sibling archive has no receipt of any kind — so deleting loses the working tree's only copy, though it stays recoverable at `4d857a23`. **AC3 "zero findings" IS UNREACHABLE:** an unscoped audit reports SIX findings across THREE classes, and the third class `archive_summary_citation_missing` (4 findings) is absent from #909 entirely. Of those four, 3 are REAL LOSSES (bundle-440-441, bundle-513-514, issue-455 — cited bytes are gone, unrepairable by any means) and 1 is a known false positive (issue-891, already adjudicated in `closure-audit.js:338-340`'s own comment — and it is the fossil of #910, the same missing-`--project` receipt). AC3 is satisfiable ONLY via its second branch: every finding a decision on record. Mutation proof: 8310-file shasum manifest byte-identical before and after; `git status` unchanged.

- item: Establish the pre-change baseline — the walkthrough at FULL scope must exit 0 before anything is edited, or a later red cannot be attributed. Run it AFTER the premise checks land, not during: the suite is spawn-bound and concurrent agents produce false reds.
  status: todo

- item: Fix #904 + #905 + export `resolveRecordFolder` for #910 — all three live in validation-runner.js, so one owner. Sandbox root to `kwv/<16-hex>` at BOTH sites (:727-730 and the unseeded twin :177-178); opt-in `--keep-output <path>` leaving receipt bytes untouched; export the resolver without changing it or adding a require.
  status: done
  dispatched: implementer `impl-runner`, owns validation-runner.js ×4 exclusively, forbidden to write tests → report at `.cache/impl-runner.md` in the MAIN root; code lands in the WORKTREE
  result: `.cache/impl-runner.md` — done, 110+/5-, four copies byte-identical at `892a19d7…`. Both premises re-measured independently before editing and both held. #904: both sites changed; bind succeeds through the real CLI; determinism control green; mutation-proved BOTH halves load-bearing (long dir literal alone → red, 64-hex seed alone → red). #905: `--keep-output <dir>` as a DIRECTORY with `run-<index>.stdout/.stderr` keyed to the receipt's `runs[].index` (proved: `sha256(run-1.stdout) === receipt stdout_sha256`); refuses UP FRONT on an existing target rather than overwrite/append, because a stale file read as this run's output is a FALSE diagnosis, worse than #905's no-diagnosis state. Found and designed around a hazard NO report flagged: a write landing in the repo mid-loop makes the runner report its OWN log as `candidate_mutation` — retention is buffered and written after the last digest (mutation proof M3: moving the write back inside the loop yields `outcome: inconclusive, reduction_reasons: ["candidate_mutation"]`). Receipt bytes proved unchanged by the flag. #910's export landed. **A reversal worth keeping:** it initially also exported `defaultSandboxPaths` so the tests would have a seam; the test author had already pinned the RESULT end-to-end instead, so the export was removed — `specify the result, never the method`. It mutation-proved the test author's #904 pin rather than trusting it: positive control green, both negative controls red. **THREE HANDBACKS:** (1) #905 has ZERO test coverage — every behaviour above is the implementer's own measurement, not a suite, and will silently regress; (2) `docs/api.md:486-488` and CHANGELOG owe entries, both TEST-CONSUMED so prose goes BEFORE the receipt run; (3) `validate-script-sync` is RED at hand-off and NOT because of this file — a sibling's un-propagated `run-chains.js`. It deliberately did not clear it, because `sync:editions` is not group-scoped and would publish another agent's in-flight work. **A final `sync:editions` after the LAST edit is now a required step of this run.** Also still open: the `.rustup`/`--env-allowlist` sibling defect (`buildScrubbedEnvironment` silently ignores an allowlisted `HOME`), a different mechanism, not in scope. Non-darwin is inference: Linux `sun_path` is 108 and the new shape is 92, so at least as good, unmeasured. FOLLOW-UP ROUTED AND CLOSED: `impl-parse` found `test-kernel-conformance.js` red on two unledgered `writeFileSync` sites introduced by `--keep-output`; sent back to `impl-runner` (its own diff) with instructions not to weaken the check and to stop rather than edit `adaptive-schema.js` if the ledger lived there. Verified closed by me: zero "unledgered" findings, `test-validation-runner` exit 0. SECOND FOLLOW-UP, ruled and closed: the dead Buffer coercion was REMOVED (additive derivation — a defensive branch with no observed failure behind it), and the archive-band refusal was RULED TO STAY and is now pinned by the test author rather than resting on the implementer's flag. Empty-stream behaviour preserved and re-proved through the real CLI (`run-1.stderr` exists at 0 bytes), receipt bytes still equal, band refusal still exits 2. `test-kernel-conformance` 254 assertions exit 0; ledger stays closed. **THE LESSON THE IMPLEMENTER RECORDED AGAINST ITSELF, worth carrying forward:** it had "mutation-proved" the coercion, but only against a hand-injected adapter returning a string — the very path that does not ship. *A mutation control constructed from the same assumption as the code it defends will confirm that assumption rather than test it.* Its M5 was armed and still proved nothing about production. FINAL canonical hash `5938e4ee…`, 128+/5-; the three plugin copies are deliberately stale at `892a19d7…` awaiting the single end-of-run `sync:editions`.

- item: Harden `parsePorcelainPaths` on the ×4 drift anchor so a hazard-named path survives git→parser→`git add` byte-exactly. Not a one-line `-z` swap: `-z` porcelain has no rename arrow, and `diff --name-only` vs `status --porcelain` quote differently.
  status: done
  dispatched: implementer `impl-parse`, owns adaptive-schema.js ×4 exclusively, forbidden to write tests, must publish `.cache/parse-contract.md` EARLY for the call-site agents to code against → report at `.cache/impl-parse.md` in the MAIN root; code lands in the WORKTREE
  result: `.cache/impl-parse.md` + `.cache/parse-contract.md` (contract unchanged since publication). `parsePorcelainPaths` keeps its name and signature and AUTO-DETECTS format (NUL → `-z` porcelain, rename = `XY <dest>\0<source>\0` dest-first no arrow; no NUL → LF porcelain with the `->` arrow), decoding git's C-quoting instead of stripping quote marks — so existing call sites keep working and three LF-fixture tests it may not edit stay green. Two new exports: `splitNulPaths` for plain path streams (`diff --name-only -z` has ONE field per record and no rename source — feeding it to `parsePorcelainPaths` would eat 3 chars off every path) and `unquoteCStyle` for streams that cannot take `-z`. Four copies byte-identical at `439d6bad…`. **THE DECISIVE EVIDENCE:** an end-to-end A/B with the SAME pristine `claim.js` on both legs and the hazard file in both, only the kernel differing — HEAD → `finalize_commit: nothing_to_commit`, no commit, `src/pending-good.js` uncommitted; patched → `finalize_commit: committed`, commit in the log, tree clean. That isolates the kernel as the sole cause of the false green. Acceptance harness 96 checks 0 fail; negative control on the HEAD kernel 70 failures including `git add -- 'notes.md ' → fatal: pathspec did not match [exit 128]` with an ordinary file left unstaged — #907 in one assertion. Full-scope walkthrough 198/198 exit 0. Five mutations all caught, via scratch mirrors. **PREMISE CORRECTION it routed:** premise-907's "run-chains.js — canonical only, no plugin copy exists" is FALSE at HEAD — the Codex copy exists and is policed by `COMMON_SCRIPTS`, and gitlab/gitea have renamed ports policed by edition-sync, so a run-chains fix owes 3 more copies. Flagged as owed prose: CHANGELOG, `docs/api.md`, and `docs/decisions/D-579-01.md:99` which documents the OLD behaviour.

- item: Fix #906's two destructive routes (move-aside at `claim.js:4107`, symlink comparison without a third reader), converge the GitLab/Gitea three-name set with canonical's four, and #907's claim.js half — the swallowed `git add` failure at `:4506` becomes a typed finding, the mangled `dirty_paths`, the five readers, the three journal regexps.
  status: done
  dispatched: implementer `impl-claim`, owns claim.js canonical+Codex+both hand-ported forge ports exclusively, forbidden to write tests → report at `.cache/impl-claim.md` in the MAIN root; code lands in the WORKTREE
  result: `.cache/impl-claim.md` — all five goals, all four editions, every guard mutation-proven; forge ports applied by an exact-match script asserting occurrence count 1 per hunk and verified by RUNNING each edition, never by diffing. **THE FLAGGED SINK RISK WAS REAL AND DECIDED THE DESIGN:** measured on three identical fixtures through the real sink — the premise's proposed SIBLING placement `archive/<project>.orphan-<ts>` gives exit 1 `sink_blocked` with the rescued evidence named as `foreign_dirt`; NESTING it at `archive/<project>/.orphan-main-live-<ts>/` gives exit 0 `sinked` AND the sink's `archive_commit` step COMMITS the orphan, so rescued evidence reaches git history rather than only disk. No `sink-merge.js` change needed or made. Nesting also makes authority ambiguity structurally impossible. **A FURTHER DEFECT FOUND BY RUNNING IT AND FIXED:** `cmdRelease`, `watch-pr` and the abandon sweep reported `missing` only, so an uncomparable entry refused with an EMPTY list — the premise's route-2 fix as specified would have refused while naming nothing. Fixed at canonical `:4749`/`:5706`/`:5801` and both ports. **HONEST CAVEAT IT VOLUNTEERED:** after route 2 lands, the GitLab/Gitea name-set convergence is NO LONGER independently load-bearing — the end-to-end leg passes even on the reverted-names mutant, because the entry reaches `uncomparable[]` regardless; the residual the name set alone decides is an empty DIRECTORY named `mission-list.md`, which carries no evidence. Still correct and still worth having as a one-rule-one-wording parity fix, but not a second destruction fix, and the discriminating oracle is the four-edition probe, not the release door. Dead `archiveStateStamped = 'repaired'` KEPT with the reachability finding recorded as a source comment — "the ordinary path cannot produce this state" is the weakest argument for removing a crash-repair backstop, and no observed failure demands the subtraction; offered to me as a one-line value call. THREE PREMISE-906 ERRORS CALLED OUT: it under-measured the move-aside (the sink, not the suffix collision, is the binding constraint); its inference that the fix must also close `missingArchiveSidecars`' `isFile()` filter is WRONG (the sidecar-skip is itself `isFile()`-gated, so an exempt-named symlink reaches `invalid[]` anyway — `missingArchiveSidecars` untouched); and it missed the naming-nothing defect. premise-907 accurate everywhere it worked, line numbers matched HEAD.

- item: Fix #910 (receipt follows the run folder, hash follows the invoking tree, via the resolver `impl-runner` is exporting) plus #907's sink/run-chains half — `isEditionCouplingPath` fail-open, the `.git` SUBTREE gitlink block, the second divergent porcelain parser at `sink-merge.js:1449`, and the durable-evidence reader at `:451`.
  status: done
  dispatched: implementer `impl-sink-rc`, owns sink-merge.js + run-chains.js across all four editions exclusively, forbidden to write tests → report at `.cache/impl-sink-rc.md` in the MAIN root; code lands in the WORKTREE
  result: `.cache/impl-sink-rc.md` — all four goals on all four editions, mutation-proven. **THREE CORRECTIONS TO MY BRIEF, all measured.** (1) **My `.git` wording would have WEAKENED the gate**: "skip any directory containing a `.git`" over-skips, because a junk `.git` FILE does not collapse the tree — measured, git commits both siblings and `git add -f` exits 0. Only a nested `.git` DIRECTORY or a RESOLVING gitfile makes a gitlink. It used the measured discriminator (`rev-parse --show-toplevel` inside the dir == the dir itself), leaving the benign case byte-for-byte in the blob gate. (2) The gitlab/gitea run-chains ports are GENERATED, not hand-ported (`validate-script-sync.js:67-69` COMMON_SCRIPTS + `edition-sync.js:52-65` GENERATED_AGGREGATORS) — a missed run-chains forge port REDS, it is not silent. Only `sink-merge` is genuinely unpoliced, and it treated only that one as such. (3) `resolveRecordFolder` had already landed in all four trees; it edited validation-runner in none. **FALSE RED CONFIRMED, matching the known spawn-bound trap:** `test-sink-merge` red on 2 forge legs when run concurrently with a background walkthrough; serially exit 0 / 423 assertions, and in isolated mirrors the new z1 pin fails on all FOUR editions at HEAD and passes on all four with the fix. Do not run these suites in parallel. Green serially: walkthrough 198/198 full scope, run-chains 258, finalize-door 301, sink-merge 423, release 247, claim-hardening 557, both forge walkthroughs, both forge run-chains suites, `edition-sync --check`, `routing-surfaces --check`. **THE OUTSTANDING RUNNER SYNC IS MINE, NOT impl-runner's** — I instructed it not to run `sync:editions` because the command is not group-scoped; it is the end-of-run item already on this list. TWO THINGS FLAGGED, NOT DECIDED, both to be FILED rather than fixed here: `--plan` with a RELATIVE path carries the identical #910 defect (left alone because it is an explicit caller-supplied path and the AC pins `--plan` precedence unchanged), and the forge `sinkPreflight` ports call `assertWorktreeClean` UNCONDITIONALLY — no #711 branchless clause, no `[project]` arg — a pre-existing divergence from canonical.

- item: Settle #908's ten items. G1 needs NO work — already closed by `7350ba9c`, filed 11 minutes early; amend the issue to point at `test-claim-hardening.js:4745-4795`. G5 closes with a ~15-line fifth CLI leg and NO export (fixture already built at `<scratchpad>/probe-gap5.js`). G3 closes by extending `T8m` past `--check`. G2/G2-b and G4 get their reasoning recorded. Na/Nb/Nc/Nd/Ne get recorded, with Ne's rationale CORRECTED (right conclusion, wrong pair — the legitimate byte difference is on `mainLive ↔ dest`, not `src ↔ dest`).
  status: todo

- item: Settle #908's gaps — each one pinned, closed with its reasoning recorded, or shown unreachable. No production seam added purely to make a test possible.
  status: todo

- item: Settle #909 — both findings need a user decision (issue state, and a tracked path).
  status: in-flight
  dispatched: self, inline. Label removal done and verified: `gh issue edit 796 --remove-label workflow:in-progress` exit 0, #796 now carries only `bug`/`area:workflow-phases`/`area:workflow-router`, state CLOSED. Residue rehomed rather than deleted: `git mv` of the unique `sink-receipt.json` into `bundle-429-434.archived-2026-06-13T08-52-23-135Z/.cache/`, staged as a RENAME (`R`), then both empty dirs `rmdir`ed — residue path gone, zero information lost. Confirmed independently before moving: exactly one tracked file, sibling `.cache/` had no receipt of any kind, and the receipt's `updated_at` 08:52:37 sits 14s after the sibling's `08-52-23` stamp, so it demonstrably belongs there. REMAINING: record the 4 `archive_summary_citation_missing` findings as decisions on record (3 real losses — bundle-440-441, bundle-513-514, issue-455; 1 known false positive — issue-891, already argued in `closure-audit.js:338-340`). Goes with the docs item. Note AC3's "zero findings" stays unreachable by construction and the issue must be closed saying so.

- item: Pin the four fully-measured surfaces — #904's socket bind, #907's finalize false green (linked worktree + `--keep-worktree` or it passes vacuously), `isEditionCouplingPath` fail-open, and the `.git` DIRECTORY block. Each RED on an unmodified baseline first, each with a positive control.
  status: done
  dispatched: tdd-guide `tdd-paths`, holds test custody, writes only `scripts/test-*.js` and walkthrough scenarios, forbidden to touch production files → report at `.cache/tdd-paths.md` in the MAIN root; tests land in the WORKTREE
  result: `.cache/tdd-paths.md` — all four pins written, each RED against unmodified `2018521f` via a scratch mirror (worktree never stashed). No new suite file; everything went into already-registered suites. RED baselines: PIN 1 `test-validation-runner.js` `1 !== 0` (first coverage the `run` subcommand has EVER had); PIN 2 `test-finalize-door.js` T9, 23 failures — `T9b(a trailing space): the healthy deliverable beside the hazard MUST be committed` with `finalize_commit:"nothing_to_commit"`; PIN 3 `test-run-chains.js` T-907a, 9 failures — `got "claude"` where four chains are owed; PIN 4 `test-sink-merge.js` z1, 4 failures, one per edition. **PIN 4's FIRST VERSION PASSED ON THE BASELINE AND WAS REPLACED** — it asked whether the refusal "names the embedded directory" and the broken sink answered yes by accident, because the journal's `archived_paths` lists the gitlink at exactly that path; the oracle is now mechanical (every itemized path must be one `git add -f` can take). **MY BRIEF'S HAZARD TABLE WAS HALF WRONG, measured:** only trailing space, non-ASCII and embedded newline are red — `qu"ote.md` and `back\slash.md` are ALREADY GREEN on the baseline because the escaped form git prints happens to match the real file as a pathspec. Kept as regression pins with a comment, so nobody reads five green legs as five caught defects. #907's `.git` mechanism error recorded in the test's own header so it is not re-derived. Damaged `testCollisionSuffixedArchiveCommittedAndDisposed` mid-task with an over-broad `String.replace`, caught it on the next run and restored it — verified `git diff | grep "^-"` returns only the diff header. **FIXTURE TRAP worth carrying forward:** the gitlab/gitea sink legs first red at `step: "closure"`, not the archive gate, because the offline mock speaks gh's argv rather than glab's or tea's — a red indistinguishable from a real one; fixed with `--keep-issue-open` per edition plus an explicit premise assertion that the run reached `step: archive_commit`. Per-edition coverage is BEHAVIOURAL on both hand-ported families (all four claim.js copies incl. the unstageable-reporting arm, which does not ride the ×4 kernel; all four sink-merge.js copies). `test-spawn-classification.js` and `test-suite-registration.js` pass — the new git spawn sites route through `G.git` rather than raising a ceiling.

- item: Pin `--keep-output`, which shipped with ZERO coverage — receipt-bytes equality (the property that justified the whole direction), after-the-loop placement proven on a validation-VISIBLE path, up-front refusal on an existing target, and the `run-<index>` ↔ `runs[].index` digest identity.
  status: done
  dispatched: tdd-guide `tdd-keepoutput`, test custody, appends to `scripts/test-validation-runner.js` without disturbing `tdd-paths`' cases, forbidden to touch production or add a seam → report at `.cache/tdd-keepoutput.md` in the MAIN root
  result: `.cache/tdd-keepoutput.md` — all four behaviours pinned plus a fifth the file grew mid-task, at `test-validation-runner.js:674-1027`, PURELY additive (496 insertions, ZERO deletions; 680 + 355 = 1035 lines exactly, so `tdd-paths`' cases were not renumbered or reorganised). Suite exit 0. TEN mutants, each reding on its OWN named assertion: flag-ignored, destination-as-`runs[]`-field (moves `vector_id`), the same field inside `audit` (moves NEITHER `vector_id` nor the top-level key set — that mutant exists purely to arm the audit-key-set assertion a `vector_id`-only test cannot see), write-moved-inside-the-loop (`candidate_mutation`), overwrite-guard-removed, guard-moved-after-the-run, index-keying-reversed, empty-stream-skipped. **ACCIDENTAL STRONG EVIDENCE:** the runner changed under the author mid-task (the write moved to `writeFileAtomicReplace`) and EVERY pin stayed green with no edit — result-not-method, demonstrated rather than claimed. Environment arming handled honestly: the runner reads NO `KAOLA_*` variable at all, so nothing can switch the mechanism off defensively; instead each leg proves the child actually ran and produced the bytes being digested. **ONE NEGATIVE RESULT RECORDED:** `mut-E` (removing the Buffer coercion) PASSES — the coercion is unreachable from the CLI because `defaultExecute` already returns Buffers. Not pinned, correctly: pinning a guard that defends a test-only path would freeze machinery no shipping caller reaches. TWO JUDGEMENT CALLS ROUTED AND RULED: (1) the dead coercion → sent back to `impl-runner` to REMOVE, per additive derivation; (2) the `kaola-workflow/archive/**` band refusal → RULED IT STAYS (decision (c) was delegated to the implementer, and keeping logs out of tracked history is exactly why the owner chose opt-in over always-on) — now owed a pin. Deliberately unpinned and correct: a destination inside tracked `.cache/` (a values call, would freeze the owner's answer) and non-darwin.

- item: Pin #908's two closable gaps — G5's count rule via a CLI leg with NO export (a regular FILE in the archive band matching the `<project>.archived-<ts>` shape; measured that deleting `.filter(e => e.isDirectory())` leaves both existing scenarios green, so today's four legs arm nothing), and G3's consumer-repo worktree lane by driving `T8m` past `--check`.
  status: done
  dispatched: tdd-guide `tdd-paths` resumed with its own context (it already owns `test-finalize-door.js` and the walkthrough suites), told to STOP rather than add an export if the CLI cannot reach it → appended to `.cache/tdd-paths.md` in the MAIN root
  result: both pinned, NO production edit and NO export added. PIN A (G5): a fourth leg inside `testClosureAuditScopedArchiveAmbiguousMatch903` — the solo negative control's own fixture plus ONE regular file named `proj-file.archived-<ts>`, one axis. No new scenario, so registry and shard ordinals untouched. `archiveNameIsAmbiguous` stays module-private: **the CLI scope envelope already carried the answer, so the declined export was never what stood in the way.** Mutant (delete `.filter(e => e.isDirectory())` at `closure-audit.js:220`) → EXIT=1 naming the bogus flag; shipped EXIT=0. Pins BOTH halves the miscount moves — the flag and the downgrade of the real archive from `name_match` to `ambiguous_name_match`. PIN B (G3): `T8m(W6)` appended after W5, reusing T8m's fixture/gh-mock/recorded validation — assertions, not scaffolding. Pins `chains_green` AFTER the transaction rather than only at `--check`, dest under MAIN's band, artifacts on disk where the envelope says, main's live folder gone. **ARMING AND COVERAGE PROVEN SEPARATELY:** M1 (read validation after the archive) → 15 failures across many legs = armed; M2 (pre-#832 destination, `isLinkedRun → false`) → exactly 2 failures, both W6's, 308 passed = W6 is the ONLY witness for the consumer × worktree × full-transaction cell, since in-place legs cannot see it (in-place IS the invoking root). **SELF-CAUGHT NEAR-MISS worth carrying:** its first PIN B mutant was a NO-OP nearly banked as proof — `finalizeTx.mirror` runs at `claim.js:3956` BEFORE `resolveFinalizeAuthority` at `:3970`, so both paths were already the same directory and the suite passed either way; *"the mutant passed" and "the pin is decorative" look identical in a transcript*. Third over-broad-replace incident this run (`.filter(e => e.isDirectory())` occurs 5× and the first mutation landed in a different function); every mutation now matches a full unique line and asserts `hits.length === 1`. Not pinned, correctly: `archiveNameIsAmbiguous`'s `catch` arm for an unreadable archive dir — not CLI-reachable without a permissions axis, and a chmod fixture the suite must then clean up is its own hazard. VERIFIED: `test-finalize-door.js` 310 assertions exit 0 (T9 intact, was 301); walkthrough FULL scope 198/198 exit 0; `test-suite-registration.js` and `test-spawn-classification.js` exit 0. All five touched test files purely additive — `git diff | grep "^-"` returns only the diff header.

- item: Tests for #906's two routes. **I FORGOT TO DISPATCH THIS AND #906 SHIPPED UNPINNED** — caught by adversarial review as C5, not by me: `main_live_orphan`/`.orphan-main-live-` appear only in the four implementations and `docs/api.md:300`, and `uncomparable` appears in NO suite at all. Also settle the reviewer's undriven suspect: whether a main-only EMPTY directory is still deleted uncompared (zero bytes, the one entry kind `uncomparable[]` cannot name).
  status: done
  dispatched: tdd-guide `tdd-paths` resumed, given the reviewer's "attacks that failed" list as a ready-made set of true-today behaviours to freeze → appended to `.cache/tdd-paths.md` in the MAIN root
  result: both routes pinned in `test-claim-hardening.js` after the existing `#901(D1)` block (same `release`-from-linked-worktree vehicle, one of the three no-mirror routes), 557 → 766 assertions, `FULL_ONLY` so the fast gate is unchanged, driven behaviourally on all four editions. PIN 1 (route 1): three legs asserting exit 0/closed, the claim cleared **by running `status --json` from MAIN** rather than inferring it, the main-only file still readable under the archive authority, and `finalize --check` still ok. Mutant (rename → rmSync) → 3 failures, all the "still readable" assertion, while the claim-clearing assertions STAY GREEN — which is the point: deleting also clears the claim, so an oracle checking only the claim passes on the destructive shape. PIN 2 (route 2): 8 legs × 4 editions including `C0_clean` as an anti-blanket-refusal control and `C1` as a positive control that the guard is armed under the identical `KAOLA_WORKFLOW_OFFLINE=1`; mutant A (`uncomparableFromMain = []`) → 20 failures including "must not claim it archived; got true", the pre-#906 defect reproduced verbatim; mutant B (drop `mismatched` from cmdRelease) → exactly 4 failures, only the NAMES-the-entry assertion. **PIN 3 SETTLED — the empty directory IS silently deleted, pinned as KNOWN AND ACCEPTED, and the reasoning is better than my brief's:** not "zero bytes" (weak on its own) but that **git cannot store an empty directory at all** — the archive band is committed, so a preserved empty directory would vanish at the next commit and be absent from every clone; refusing would block a `release` to protect something the durable record can never hold, i.e. exactly the permanent unclearable refusal its own `z1` pin forbids. Bounded by `P2_dir_with_content`: one byte and the refusal fires and names the file. Acceptance scoped to zero bytes, never to directories, with the bound written at the assertion. **A wrong assertion it wrote, caught and fixed:** L3's walker used `withFileTypes`, stopped at the orphan symlink and reported a successful rescue as a loss — recorded because it is "a red I could have 'fixed' by deleting the leg". DECLINED TO PIN, correctly: the sink-journal finding (C1), because the fix is in another agent's file and **a test written against current behaviour would FREEZE the defect**; and the pre-existing TOCTOU, unreachable without a seam custody forbids. Verified serially: claim-hardening 766 exit 0, walkthrough FULL 198/198 exit 0, finalize-door 310 exit 0 (second pass intact), spawn-classification and suite-registration exit 0, diff purely additive.

- item: Run `npm run sync:editions` AFTER the last edit to this worktree lands, then confirm `validate-script-sync.js` exits 0. Required because the command is not group-scoped: whoever runs it publishes every in-flight byte-identical group, so it must run once at the end, not per agent.
  status: done
  dispatched: self, inline, after all four implementers reported done
  result: RUN TWICE. The second and final run, after every production edit and after `impl-sink-rc`'s R1 comment fix, again updated exactly the three validation-runner mirrors. **All four contract validators then green: `validate-workflow-contracts` 0, `validate-kaola-workflow-contracts` (Codex) 0, gitlab-contracts 0, gitea-contracts 0** — the two-cause red the edition reviewer isolated is fully cleared, and its prediction that fixing only one cause would leave the other hidden behind it held exactly. First run's result follows: `edition-sync: write complete (3 file(s) updated)` — exactly the three validation-runner mirrors that were owed, nothing else in flight. `validate-script-sync.js` exit 0: "15 common scripts, 27 byte-identical groups, 1 rename-normalized families, 2 hooks.json families, 6 forge export-superset families in sync; committed kernel parity: 4 Oracle Kernel copies identical at HEAD."

- item: Port every change to the editions it reaches and reconcile `validate-script-sync.js` byte-identity.
  status: todo

- item: Act on the adversarial review. `.cache/review-destruction.md`: 4 CONFIRMED + 1 coverage — C1 the orphan commits sink JOURNALS into history (one level deeper than four EXACT `:(exclude)` pathspecs at `sink-merge.js:2241-2244`), C2 `--keep-output`'s no-overwrite rule is pre-flight only while the write is unconditional (a concurrent run silently destroys the earlier's output — the exact false-diagnosis the refusal was built for), C3 the `.git` discriminator misses `core.bare=true` and `core.worktree` (both of which git DOES collapse, so the unclearable `sink_incomplete` #907 exists to remove still happens), C4 `usage()` claims "absolute paths redacted and NOTHING ELSE" while retaining raw bytes, C5 #906 shipped with NO test. Both #906 headline claims SURVIVED direct attack. `.cache/review-verdict.md`: 3 CONFIRMED, 0 suspected — **R1 BLOCKING AND CANDIDATE-CAUSED: the #910 fix makes the ordinary retry lane WORSE than HEAD.** Step 8a's mirror copies main's `.cache/` forward with no staleness test, so a mirrored-then-refused finalize makes local-first resolution land the next receipt in the worktree, and the following finalize overwrites that fresh receipt with main's older one → `chains_stale` over a tree the chains just ran green on, and the archive carries a receipt bound to a tree that no longer exists. Proven a regression by one-axis A/B swapping ONLY `run-chains.js` between main and the branch, with a green control. R2: the #907 false green SURVIVES via the `git status --porcelain` probe at `claim.js:4614` that FEEDS the converted call, plus bare catches at `:4531`/`:4549` — driven, same envelope, archived summary carries no `## Finalize Findings`. R3: `isEditionCouplingPath` still scopes to one chain when a plugins path is the SOURCE of a rename, because `diff --name-only -z` reports only the destination; discriminated from the fixed hole by four fixtures.
  status: in-flight
  dispatched: C2+C4 → `impl-runner`; C1+C3+R3+S1 → `impl-sink-rc`; R1+R2 → `impl-claim` (told to STOP rather than edit run-chains or resolveRecordFolder if R1's fix belongs there); C5 + the empty-directory suspect → `tdd-paths`; the four SUSPECTED destruction findings → `rev-destruction` to drive to CONFIRMED or REFUTED; S2's prose → `docs`. All appended to the reporters' own report files.
  C2+C4 CLOSED by `impl-runner`: the no-overwrite guarantee now holds at write time as well as pre-flight (both, not either — the up-front refusal is still right, because one arriving after a 27-minute suite throws away the run it was meant to explain), and `usage()` no longer claims "absolute paths redacted and NOTHING ELSE". Full regression pass over everything previously proved still holds (R1–R6: six files at 3 repetitions, recursive parent creation, empty stream present at 0 bytes, band refusal still exit 2, visible keep path with NO `candidate_mutation`, receipt bytes and both digests equal, no `.tmp` residue). Canonical now `41c0fd483ca91865…`. Harness artefact recorded so nobody re-derives it as a defect: `--output /dev/null` exits 2 with or without `--keep-output`, because the atomic replace cannot rename over it — pre-existing and unrelated. THREE NEW BEHAVIOURS ROUTED TO `tdd-keepoutput`: the write-time refusal (the half a pre-flight structurally cannot reach), concurrent runs (exactly one proceeds, loser exits 2 destroying nothing), and `--keep-output` at an EXISTING directory now being a usage error where an empty one was previously adopted. Told it explicitly that its original pin was not wrong but incomplete in a way its own mutant set could not reveal — every mutant attacked the pre-flight check, none attacked the gap between check and write.
  C1+C3 CLOSED by `impl-sink-rc`, all four editions. **C3's root cause was deeper than the review's framing:** the round-1 probe ran `rev-parse --show-toplevel` INSIDE the candidate, asking the INNER repo where its work tree is — and inner config can answer anything (`core.bare` errors, `core.worktree` points elsewhere). The question is about the OUTER repo. Fixed with `git -C <mainRoot> rev-parse --resolve-git-dir <dir>/.git` — git's own resolver, the same predicate `git add` uses, asked from outside so no inner config can misdirect it. Validated against ground truth (what the outer repo stages into a HEAD-seeded scratch index) on TWELVE `.git` shapes, agreeing in BOTH directions. End-to-end: pre-#907 four permanent blocks → round-1 two (the review's finding, reproduced) → round-2 zero. False-positive direction proven LOAD-BEARING, not incidental: mutating always-true strips benign siblings out of `required[]`. **C1's leak was ONE FILE WIDER than reported** — a journal at DEPTH 0 leaks too, three not two. Fixed in sink-merge only, `claim.js` untouched and the orphan path unchanged: `claim.js`'s `SINK_JOURNAL_RE` has always stated the rule BY BASENAME AT ANY DEPTH, so the four fixed-depth pathspecs were the divergent wording — converged to `:(exclude,glob)<prefix>**/sink-{receipt,fallback}.json` rather than inventing a rule. No-over-exclusion control: exact `.cache/` leaks 3, non-glob `*/` leaks 1, glob `**/` leaks none, and ALL THREE stage an identical 6-file evidence set. Suites all exit 0 run strictly serially. HONEST CAVEAT it volunteered: the twelve shapes are measured; that an unenumerated shape also follows `git add` is an INFERENCE from `--resolve-git-dir` being git's own `resolve_gitdir()`, not a measurement. R3 and S1 still in flight with the same agent.
  R1+R2 CLOSED by `impl-claim`, all four editions; all round-1 #906 drives re-run against the finished tree and still passing (route 2 seven legs × 4, route 1 two legs × 4, four-edition convergence, and the sink-orphan trio C/A_nested/B_sibling). New envelope surface from the fix: `finalize_transaction.archive_stage`, `archive_commit_probe`, `finalize_commit_probe`, `findings`, a new `finalize_commit: 'unknown'` value, and a changed `roadmap_staged` derivation — the code no longer conflates "nothing was staged" with "git failed", which is the root mechanism of the original false green. **A VACUITY WARNING IT MEASURED AND HANDED FORWARD:** R1's outcome-log leg PASSES ON THE UNFIXED CODE when both trees' logs are empty, so the pin must seed both trees with distinguishable content and must read the ARCHIVED copy, not the live worktree one — routed to `tdd-paths` with that warning stated up front. Also routed: pin R2 across all four editions (the forge ports have a different archive-staging shape, so a canonical-only pin cannot witness them) and pin that the two `git diff --cached --quiet` probes distinguish exit 1 from exit 128. Prose REOPENED for the new fields — `docs` recalled for a final pass, told explicitly NOT to changelog R1 since it never shipped, only to stop the #910 entry overclaiming about receipt durability.
  R3+S1 CLOSED by `impl-sink-rc` (round 3), all four editions. R3: fixed with `--no-renames`, which decomposes a rename into delete-of-source + add-of-destination — chosen over `--name-status -z` because it keeps the reader on `splitNulPaths` and **can only WIDEN the set, so it cannot fail open**; r2/r6/r7 byte-identical with and without the flag. Reproduced against POST-#907 code, confirming it is genuinely a second mechanism rather than a remnant of the C-quoting hole. S1: new `symlinkTargetsOutsideArchive` REPORTS and never refuses — `missingBlobs` untouched, `archive_commit` still done, exit 0 still 0; what changes is `receipt.archive_unbacked_symlinks` plus a stderr remedy. Discrimination measured in both directions: absolute-outside, dangling and relative-escape links NAMED; two links pointing INSIDE the archive NOT named (they travel with the archive and resolve in any clone). Resolution is lexical with a realpath second chance, because realpath alone cannot answer for an already-dangling link — one of the cases that must be named. Mutation honesty: disabling the `120000` mode filter was SILENT on the natural fixture (`readlinkSync` already excludes regular files), so it BUILT the shape that separates them — a path committed as `100644` that is a symlink on disk now. **FOUR BEHAVIOURS HANDED BACK AS LOAD-BEARING WITH ZERO COVERAGE, fixtures pre-built** → dispatched to a new `tdd-guide` `tdd-sink` owning `test-sink-merge.js` + `test-run-chains.js` only (the other two test authors are in different files) → `.cache/tdd-sink.md`. **The sharpest of the four: the existing `z1` pin plants only a plain nested repo, so it PASSES on the broken discriminator** — a pin that cannot see the defect it names. And C1's regression is structurally invisible to the blob gate, because `SINK_STAGE_SKIP` keeps journals out of `required[]`, so its pin must inspect what was COMMITTED rather than what the gate reports.
  A NINTH FINDING, found by `tdd-keepoutput` while pinning the C2 fix and routed rather than fixed by it: the write-time refusal could leave a PARTIAL retention and then misdescribe it — when the collision lands on the STDERR carrier, this run's genuine `run-1.stdout` is already on disk while the message says "no retained output was written for this run". Same false-diagnosis class, reached from the other side; the directory reads as a COMPLETE retention while one file is the command's artefact. It pinned only what is invariant across both candidate fixes so its pins survive either, and `impl-runner` closed it with check-all-before-writing-any. FINAL #905 COVERAGE: 14 harnesses, control green, 13 mutants red, each on its own assertion, every anchor asserted to match exactly once so no mutation silently no-ops; bound to runner `37b7dcb4252bd01c`. The archive-band refusal is now PINNED (`mut-H1`) following my ruling — which had reached the implementer but not the test author, my coordination error, corrected. STILL UNRULED AND DELIBERATELY UNPINNED: decision (c), whether a `--keep-output` destination inside tracked `kaola-workflow/**/.cache/` is permitted at all — a values call the owner has not been asked; the test author sited its control to avoid freezing it by accident.
  EDITION-PARITY REVIEW (`.cache/review-editions.md`, re-run against the REPAIRED tree, pin `795dfa0d`): FAIL — 1 blocking, 3 notes. **R1 BLOCKING, and it would have been MASKED BY MY OWN DEFERRAL NOTE:** the red on the forge contract validators is TWO-cause and `sync:editions` clears only one. `run-chains.js:650`'s new `--no-renames` comment contains the literal `git mv plugins/kaola-workflow/scripts/x.js`; the forge ports are GENERATED and the rename map does not rewrite an inline path in a comment, so it lands byte-identical and trips `validate-kaola-workflow-gitlab-contracts.js:363`, which tests the FULL text where its sibling `gh` rule one line above strips comments first. Absent from `main` — introduced here. Mutation-proven one axis at a time: mirrors alone → still red on the comment; comment alone → red on the runner mirrors; both → EXIT 0 on gitlab and gitea. A known-red hiding an unknown-red, which is this bundle's own subject. Routed to `impl-sink-rc` with instructions to rewrite the comment WITHOUT restructuring the validator or adding an exemption. **R3:** `kaola-gitlab-workflow-claim.js:4494`, new this bundle, says `watch-pr` where GitLab's subcommand is `watch-mr` (`watch-pr` prints `unknown subcommand`); correct on Gitea, where that IS the name — the exact hand-port hazard the brief warned about, invisible to parity. Routed to `impl-claim`, with the pre-existing duplicate at `:2291` to fix if it is the same mistake. **The four-vs-five divergence CONFIRMED in substance but OFF BY ONE:** canonical 6, codex 6, gitlab 5, gitea 5 — so five-vs-six; `docs/api.md:361` corrected, and it is to be FILED as pre-existing (both shapes present at `main`). The delta is precisely `archive_unstage_failed`, which can only exist where there is a `git rm -r --cached` to fail: canonical makes two archive-staging calls against the forge ports' one unscoped `git add -A`. Same root explains `archive_stage` showing 7/7/5/5 sites — NOT a port miss. CLEAN elsewhere, driven not diffed: eight probes × four editions on identical fixtures, each with an armed negative control against `main`; the new `.git` discriminator agrees with git on all 12 shapes in both directions on all four editions (the old `--show-toplevel` version disagreed on 3, so the rewrite was necessary AND correctly ported to both unpoliced copies); `claim.js` hand ports show an EMPTY `comm -23` of added code lines canonical-vs-forge. **Two false alarms it caught itself on** — a Gitea signature and a walkthrough `ReferenceError` both looked like port misses and were files caught MID-WRITE: "a port miss and a half-written file look identical in one read." **And its own invalidated first pass, recorded: cwd had silently reset to the MAIN root, so relative-path gates reported green against a tree containing none of the bundle** — the same trap that has bitten this repo before and bit me once this session.
  R1+R2 PINNED by `tdd-paths` (fourth pass) as `T10`/`T10b`/`T11` in `test-finalize-door.js` — deliberately the FAST-GATE suite, since both are false-VERDICT defects; 310 → 394 assertions; new `buildMainResidentRun` builder because T9's posture cannot reach either finding. R1's vacuity trap handled exactly as warned: both trees seeded with distinguishable content, the seeding itself asserted as a premise, and the assertion reads the ARCHIVED copy (the live worktree log is legitimately gone after a successful finalize, so reading it would measure the archive step instead of the mirror). Mutant (Step 8a down-mirror) → exactly T10's three step-D assertions; `T10b` with step B omitted stays green, proving the axis is the refused finalize and not the fixture. R2 pinned three legs × four editions, with `nothing_to_stage` added as a third leg because `statusfail` pins a non-1 exit as a FAULT while `nothing_to_stage` pins the exit-1 arm as an ANSWER — neither alone separates them; the flush mutant reds T9c and T11's DURABLE assertion while every envelope-half assertion stays green, so the two halves are armed separately. **THE FINDING THAT MATTERS BEYOND ITS OWN FILE — a mutant caught a REAL VACUITY in the test author's own work:** its per-edition loop called a helper that always shelled the MODULE-LEVEL canonical script, so four legs ran against ONE file under four labels. Caught only because a CANONICAL-only mutation reddened ALL FOUR legs, which is impossible if the legs are independent. Fixed and proven in both directions. **`A green per-edition loop proves nothing until a single-edition mutant reddens a single edition.`** Dispatched a bundle-wide audit of EVERY per-edition loop added on this branch — ours and everyone's — because multiple agents claimed four-edition coverage this run, and a four-edition claim that is silently a one-edition claim is precisely the false-verdict failure this bundle exists to fix, committed by us in the tests meant to prevent it. `tdd-sink` warned mid-task, since `sink-merge.js`'s forge ports have NO other check; also asked whether the pre-existing `z1` loop has the same defect, which would explain how the broken discriminator survived. LEFT UNPINNED, correctly: `archive_commit: "deferred_to_sink"` — the reviewer calls it a false statement and the implementer disagrees ON THE RECORD; a disagreement about what a token MEANS is not a fact a test should freeze, so it pinned `archive_stage`, which both agree on. Open for me to settle if it needs settling.
  R1 CLOSED by `impl-sink-rc` (round 4): the path literal is gone and the reasoning is kept — the example is now "a `git mv` that carries a file OUT of an edition tree and into `src/`", plus a parenthetical recording WHY there is no example path so the next reader does not helpfully add one back. No validator restructured, no exemption added, regenerated through `edition-sync`'s `renderForgePort` rather than by hand. It swept ALL FOUR forge-tree files for the pattern, not just the one that fired: hits=0. **Both forge contract validators now stop at MY deferred sync**, proven rather than asserted with a scratch mirror of HEAD + every working-tree change + ONLY the runner canonical→3-mirror copy → gitlab-contracts, gitea-contracts and validate-script-sync all EXIT=0. R3's behaviour re-confirmed after the rewrite, so the comment change did not move code. **A GAP IN ITS OWN VERIFICATION IT PUT ON THE RECORD RATHER THAN BURYING:** in rounds 2 and 3 it ran the PLAIN forge walkthroughs and treated them as forge coverage, but those do not invoke the contract validators — the CODEX forge walkthroughs do, and it had last run those in round 1, before the comment existed. *"A green forge walkthrough is not evidence the forge contract validator is green, and I used it as if it were. That is how my own change reached you as a blocking find."* All five of its items done: C1, C3, R3, S1, R1.
  PER-EDITION VACUITY AUDIT COMPLETE (`tdd-paths`, fifth pass): **six loops on the branch, exactly ONE broken — its own `T11`, already fixed before the audit began.** Every other loop PROVEN per-edition by single-edition mutants reding single editions in both directions, against a mirror control at 631 assertions so every red is attributable: `#906(R2)` 8×4 (gitlab mutant → 20 fails all gitlab; gitea → 20 all gitea), `#906(R1)` (gitlab mutant → exactly 1, `R1 gitlab L1_plain`), `T9d`, `z1`, and `tdd-sink`'s `z2/z2b/z3/z4` (boundary mutant per forge → 3 each, all that forge; journal-glob revert → `z4 (gitea)` only; `symlinkTargetsOutsideArchive→[]` → `z3 (gitlab)` only). `z2b` stated honestly as inspection rather than folded into the measured rows. Two files correctly have NO edition loop: `test-validation-runner.js` (byte-identical ×4, policed by `BYTE_IDENTICAL_GROUPS`) and `test-run-chains.js` (`T-907a`/`T-907b` loop hazard names and rename scenarios, not editions). **FLAGGED AS A COVERAGE QUESTION, NOT A VACUITY:** `T-907a`/`T-907b` are canonical-only while four run-chains copies exist with their own pre-existing suites this branch does not touch — lower risk because those forge ports are GENERATED from canonical, so a code divergence reds, but recorded. **A THIRD NO-OP MUTANT, and it nearly produced a FALSE REPORT:** the first loop-E mutation changed `--resolve-git-dir` to `--resolve-git-dir-NOPE`, and `git rev-parse` ECHOES an unrecognised option and EXITS 0 — so the mutation changed nothing, and presented as "a single-edition mutation reddens nothing", which is one of the two signatures of a broken loop. It was one step from filing a SOUND loop as vacuous; the tell was that mutating canonical produced the same nothing. Effective mutation is a non-existent gitdir (exits 128, measured). Three no-op mutants this run — wrong `String.replace` target, already-identical directory, swallowed git flag — **all three looked like evidence.** Rule for the receipt notes: *before concluding a loop is vacuous, confirm the mutation bites on an edition you already know is driven.*
  FOUR SINK PINS LANDED by `tdd-sink` (`.cache/tdd-sink.md`). C3 (`z2`): plants SEVEN `.git` shapes in one archive — plain nested repo, `core.bare=true`, `core.worktree` elsewhere, junk `.git` FILE, broken gitfile, `.git` symlink to a live gitdir, and no `.git` — with **git itself as the oracle**, measured per run in a HEAD-seeded scratch index: `receipt.archive_embedded_repos` must EQUAL the set the outer git stages as `160000`. **It settled WHY the broken discriminator survived, and corrected my hypothesis:** restoring the round-1 predicate verbatim as a mutant reds `z2` on all four editions (missing c2/c3, sink refuses over `c2/inner.md`) while **`z1` passes straight through it with ZERO failures**. So the cause is a FIXTURE blind spot, not a vacuous loop — `z1` plants only a plain nested repo, a shape both the broken and the fixed predicate classify correctly. A vacuous loop and a blind fixture produce identical green; only the mutant separates them. S1 (`z3`): pinned as an EXACT SET rather than containment — five committed `120000` links in ONE archive so both directions are decided by the same code on the same run; absolute-outside, dangling and relative-escape named, two inside-targets NOT; mutant with the containment test disabled over-reports L2/L5, mutant returning `[]` reds with `got []`; posture pinned too (exit 0, `sinked`, `archive_missing_paths` empty, `archive_commit: 'done'`). R3 (`T-907b`): five cases including r7 (rename out with a NON-ASCII source, needing BOTH halves of #907) and **r8, an over-capture control I did not ask for** — a rename with no edition path either side must stay claude-only; removing `--no-renames` reds r1 and r7 only, making `isEditionCouplingPath` return true reds r8 only. `test-run-chains` 258 → 283, exit 0. R3 correctly makes NO per-edition claim: the forge run-chains ports are generated and policed by `edition-sync --check`. Mutant hygiene: every mutant built by a script that EXITS 2 if the `String.replace` changed nothing and prints which files differ — the direct answer to this run's three no-op mutants. Mutant E was silent and it verified the text DID change, recording the `readlinkSync` reason as an unpinnable-without-a-seam gap WITH the measurement rather than banking it as evidence.
  SUSPECTS DRIVEN TO VERDICTS: **S1 CONFIRMED [high]** — a symlinked orphan commits as a `120000` entry, the sink says `status: sinked` exit 0 with `archive_missing_paths` unset, and a fresh clone gets a dangling link that `git status` calls complete; the gate is blind because `scanArchiveTree` admits symlinks into `required[]` (`sink-merge.js:1413`) and `blobPathsUnder` uses `ls-tree -r -z --name-only` (`:1456`), which cannot tell `120000` from `100644`. Introduced by route 1 specifically — the only path that puts a symlink in the band. NOT data loss (bytes survive at the original path) but a GREEN VERDICT OVER MISSING CONTENT, the same class as #907's false green, relocated. Routed to `impl-sink-rc` with a report-don't-refuse posture. **S2 CONFIRMED [low], recorded not fixed** — retention buffers and writes after the last repetition, so an interrupted run retains NOTHING (measured: SIGTERM after two completed repetitions → empty directory). The placement is CORRECT and must not change: a write inside the loop moves the candidate digest and makes the runner report its own log as `candidate_mutation`, a false RED on the verdict, strictly worse. Resolution is one sentence of prose → `docs`. **S3 REFUTED as widened, confirmed pre-existing and unchanged** — main already has both verifies before both `rmSync`s; width measured with `hrtime.bigint()` over 7 runs per side, base median 0.3197 ms vs branch 0.2616 ms, indistinguishable, and the only added statement early-returns on an empty array. It also CORRECTED MY FRAMING: the move-aside is not at that site at all (`claim.js:4200-4220`) and has NO verify→delete window because it performs no comparison whatsoever — its safety is entirely "it is a rename, not a delete", which is exactly the property S1 exploits. **S4 skipped**, left to the test author. Findings pinned to a `2026-08-01T23:02:35Z` snapshot and re-checked against live copies afterwards — all three shapes still present, neither finding stale.

- item: FILED (done). #911 `--plan` with a relative path carries the identical #910 defect. #912 the forge `sinkPreflight` ports call `assertWorktreeClean` unconditionally, with no `#711` branchless clause and no `[project]` arg — on a surface policed by nothing, so its acceptance demands BEHAVIOURAL per-forge coverage. #913 `buildScrubbedEnvironment` silently discards an allowlisted `HOME`/`TMPDIR` (`deterministic.has(key) → continue`, no error) — and those two keys are exactly what a caller reaches for first, which is how the `.rustup`/cargo failure was reached. #914 canonical emits six finalize finding types where the forge ports emit five, the delta being `archive_unstage_failed`, structural rather than a port miss (two archive-staging calls vs one unscoped `git add -A`), same root as `archive_stage`'s 7/7/5/5 sites; both shapes present at `main`. NOT filed, because they are recorded as decisions instead: #908's G2/G2-b and G4 (in `D-908-01`), #909's four citation findings (in `D-909-01`), and S2's retention-on-kill behaviour (documented in `docs/api.md`).
  status: done
  dispatched: self, inline

- item: superseded — see the FILED item above — `--plan` with a relative path carrying the identical #910 defect; the forge `sinkPreflight` ports calling `assertWorktreeClean` unconditionally (pre-existing divergence from canonical); and `buildScrubbedEnvironment` silently ignoring an allowlisted `HOME`/`TMPDIR` rather than rejecting it (premise-904 §6, the `.rustup` sibling). Missing is a routing problem, never a stop.
  status: todo

- item: Adversarially review the whole diff. Eleven green suites and every implementer's own mutation proof missed nine real defects last bundle; the reader found them, not the suite. Give the reviewers the premise reports so they can check the work against what was MEASURED, not against the issues — three issue claims were already proven false this run.
  PER-EDITION MATRIX PROVEN by `tdd-sink`, one mutated FILE per mirror, five mirrors, whole suite each: boundary→false on gitlab only → 3 failures, all gitlab; same on gitea → 3, all gitea; boundary→true on gitea → 2, all gitea; `symlinkTargetsOutsideArchive→[]` on codex → 1, codex only; journal-glob revert on root → 1, root only. Both directions per pin family, on three different editions. **An independent arithmetic check I would not have thought of:** the whole-tree mutants run earlier produced EXACTLY 4× these counts (12=4×3, 8=4×2, 4=4×1, 4=4×1) — if any leg were shelling the module-level canonical script that ratio could not hold. **My `z1` hypothesis was half wrong in the half that matters:** z1's loop is NOT vacuous — patching only the GitLab sink reds `z1 (gitlab)` and nothing else; the forge sink copies were always genuinely driven. The broken discriminator survived because of a FIXTURE blind spot, so the remedy is the new shapes, not a loop repair, and `z1` needs no edit. Also confirmed post-sync: `edition-sync --check` exit 0 (8 forge aggregator ports in parity, committed kernel parity verified) and `validate-script-sync` exit 0 — which upgrades "all four run-chains ports carry `--no-renames`" from a grep to a policed fact. Nothing outstanding.
  status: done
  dispatched: THREE reviewers with deliberately different lenses, all read-only on the branch, all told to reproduce rather than reason and to report cleanly if they find nothing: `rev-destruction` (does it still destroy or lose anything — attacks the move-aside collision/symlink/missing-authority cases, the entry kinds beyond symlinks, the gitlink discriminator, and the band-check evasion) → `.cache/review-destruction.md`; `rev-verdict` (can it still report an untrue verdict — attacks other swallowed-failure routes, whether the typed finding survives INTO the archive, receipt topology and staleness, parser detection edge cases, remaining `isEditionCouplingPath` shapes) → `.cache/review-verdict.md`; `rev-editions-2` (did every change reach every edition and only those — starts at `sink-merge.js`'s forge ports, the one surface policed by nothing, and drives behaviour per edition rather than diffing) → `.cache/review-editions.md`. All three given the premise reports and told to check against MEASUREMENTS, not issue text.

- item: Docs, CHANGELOG under `[Unreleased]`, and the ADR text the changes reach — including the D-697-01 amendment recording the owner's opt-in carve-out, D-579-01's stale parser paragraph, a durable decision record for #909's three unrepairable citation losses plus the one known false positive, and #908's four open-with-reasoning dispositions. Written BEFORE the chain-receipt run because several targets are test-consumed.
  status: done
  dispatched: doc-updater `docs`, PROSE ONLY — forbidden to touch `scripts/**`, told to stop and report rather than edit code if prose cannot be made true → report at `.cache/docs.md` in the MAIN root; prose lands in the WORKTREE
  result: `.cache/docs.md` — one pass, before the receipt, nothing under `scripts/`. CHANGELOG: 7 entries under `[Unreleased]`; #904 leads with the migration consequence (an inherited `{command_id, required_pass_vector_id}` obligation is now unsatisfiable), #907 leads with "finalize reported success while committing nothing, including healthy files, on `--keep-worktree` linked runs". `docs/api.md`: 8 edits — the `--project` row CORRECTED (it was factually wrong post-#910), a record-follows-folder/hash-follows-tree paragraph, `--keep-output`, a new "Kernel path-stream decoders" section covering the new contract plus the four readers deliberately NOT converted, the `mismatched` correction with `uncomparable[]` as a strict subset, `archive_embedded_repos`, and two new finalize-envelope field tables. `docs/workflow-state-contract.md`: three-key return + the crash-resume backstop now moving rather than deleting. `D-697-01` amended (dated, original `:57` sentence kept VERBATIM with an inline note — no history rewriting). `D-579-01:99` corrected with a dated block. TWO NEW RECORDS: `D-909-01` (the four unrepairable citation findings, incl. issue-891's false positive recorded as the fossil of #910) and `D-908-01` (dispositions for all ten items). **It caught a factual error in its OWN draft before verifying:** the four fixed archive filenames are NOT required unconditionally — `listSourceEvidenceFiles` (`claim.js:5398-5410`) includes each only when the source holds it, and only `workflow-state.md` is unconditional at `:5486`; fixed in both CHANGELOG and the contract doc. `api.md:1309` verified to need NO change against the real diff — no `module.exports` line moved in claim/sink-merge/run-chains; only adaptive-schema and validation-runner gained exports. Skipped with reasons checked: README and architecture describe nothing this bundle changed; `agents-source.md` unreached; no routing skeleton needed changing. No CI/CD mention. VERIFIED serially: `generate-routing-surfaces --check` exit 0, all 18 surfaces byte-match; both contract validators exit 0. **The candidate hash has now moved — any receipt taken before this point is stale, and the receipt run goes after.**

- item: Verify the ASSEMBLED tree, then commit, then take the chain receipt. Everything before this was verified by individual agents against a tree that kept moving underneath them.
  status: done
  dispatched: self, inline
  result: Full-scope walkthrough exit 0 (`Workflow walkthrough simulation passed`, 2085 spawns) — not a shard. Six touched suites serially, all exit 0: claim-hardening 766, finalize-door 394, sink-merge 631, run-chains 283, validation-runner PASSED, kernel-conformance 254. All four contract validators exit 0. Implementation committed at `8cbb8797`, tree clean afterwards. Chain receipt then launched FROM THE WORKTREE with `--project` and `KAOLA_RUN_CHAINS_CONCURRENCY=serial` — the exact topology #910 fixed, so the run dogfoods its own fix: pre-#910 this receipt would have landed in the worktree and the gate would have called four green chains `chains_unverified`. **FIRST RECEIPT ATTEMPT RED, correctly:** the `claude` chain failed on `spawn-classification: scripts/test-run-chains.js: 6 unclassified spawn site(s) exceeds the ceiling of 5` (lines 33, 47, 1244, 1568, 1688, 1704) — the new `T-907b` pins added spawn sites. Routed to `tdd-sink` to classify or convert in-process, explicitly NOT to raise the ceiling ("Raising the ceiling is not a fix" is the guard's own wording, and a threshold raised to admit new work stops being a threshold), and told to stop and argue rather than change it if it thinks the ceiling is wrong. Note `test-spawn-classification.js` passed STANDALONE earlier in the run — this is the chain's own ceiling check over the same file, a different gate, so the standalone green was not evidence here. **And `${PIPESTATUS[0]}` came back EMPTY** (the known zsh quirk in this repo) — the failure was visible only because I read the chain's actual output rather than gating on an exit code; trusting the empty value would have walked a red chain into finalize.

- item: Finalize — `/kaola-workflow-finalize bundle-904-905-906-907-908-909-910`. Verify the finalize COMMIT by reading `git log`, never by trusting the envelope: the false green this bundle fixed reported `finalize_commit: "nothing_to_commit"` at exit 0 while committing nothing, and it was reachable on exactly this posture (`--keep-worktree`, linked worktree).
  status: todo
