evidence-binding: n6b-residual-prose 106492ea9b03
<!-- non_tdd_reason: paste non_tdd_reason here -->
non_tdd_reason: mid-run plan-repair (config/prose migration + glue): prose migration of 8 under-scoped surfaces from the retired "Switch-ON contract (#515)" model to the #538 adaptive-default contract + ADR 0007 supersession header + adding the --with-fast/--with-full opt-in flags to the 3 install.sh invocations in test-install-model-rendering.js that read now-opt-in phase[1-5]/fast files; behavior-preserving, no new behavioral logic with a natural unit test (the route-reachability T11 contract was already RED and is the existing test that machine-enforces the prose) — verified by the existing route-reachability contract + the install-rendering test + the full claude chain, not a new test-first unit assertion.
<!-- regression-green|build-green|smoke-integration -->
regression-green: route-reachability GREEN (170 assertions, T11 now passes on all 12 surfaces incl. my 8); test-install-model-rendering GREEN (phase[1-5]/fast files install + render under --with-fast --with-full); ALL FOUR chains GREEN (cross-edition diff: 6 forge files touched) — claude (exit 0, "Workflow walkthrough simulation passed"), codex ("Kaola-Workflow walkthrough simulation passed"), gitlab ("GitLab Codex workflow walkthrough simulation passed"), gitea ("Gitea Codex workflow walkthrough simulation passed"); retired-vocab grep on the 8 prose surfaces = ZERO hits (no path_requires_explicit_opt_in / Switch-ON contract / "adaptive switch is ON|OFF" / "switch is OFF"); all 8 contain path_not_installed.
