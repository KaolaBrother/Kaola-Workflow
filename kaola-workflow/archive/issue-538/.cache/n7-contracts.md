evidence-binding: n7-contracts 450297f3d6e0
<!-- RED: paste RED here -->
RED: all 4 contract validators + test-route-reachability FAILED on the old pins before re-pin (fail-fast throw shows first failure per validator):
  - validate-workflow-contracts.js: `Error: commands/workflow-next.md must include: mechanical` (L248 #198 rubric token — deleted from workflow-next with Branch-A; behind it the E4 renames L749/773/825/826/830 were unreached);
  - validate-kaola-workflow-contracts.js: `Error: plugins/kaola-workflow/skills/kaola-workflow-next/SKILL.md must include: Workflow path: {fast|full` (L123 retired Branch-A menu framing);
  - test-route-reachability.js: `4 failure(s)` — `T11: <fast surface> must contain "path_requires_explicit_opt_in" literal` on the 4 n5-migrated fast surfaces (literal retired by #538/B2).
  Retired pins confirmed absent in the new world: resolveEnableAdaptive, workflow_path_refused, --enable-adaptive(live flag), fast|full|adaptive menu, path_requires_explicit_opt_in (on migrated surfaces).
<!-- GREEN: paste GREEN here -->
GREEN: re-pinned all 6 files to the new vocabulary (resolveInstalledPaths / path_not_installed / bundle_requires_adaptive; --with-fast/--with-full + `--enable-adaptive is retired (#538)` warn-ignore; KAOLA_PATH/adaptive/default/path_not_installed concept; `Workflow path: {adaptive by default`; #198 rubric safe-dropped from next-router, kept on fast surface; KEEP assertNotIncludes enable_adaptive/KAOLA_ENABLE_ADAPTIVE on repair-state + plan-validator). Results:
  - validate-workflow-contracts.js: PASS (exit 0) "Workflow contract validation passed";
  - plugins/kaola-workflow/scripts/validate-workflow-contracts.js: BYTE-IDENTICAL to canonical (cp; diff clean);
  - validate-kaola-workflow-contracts.js: PASS (exit 0) "Kaola-Workflow Codex contract validation passed";
  - validate-kaola-workflow-gitlab-contracts.js: PASS (exit 0); validate-kaola-workflow-gitea-contracts.js: PASS (exit 0);
  - validate-script-sync.js: PASS (exit 0) "26 common scripts, 25 byte-identical groups… in sync" (proves #1<->#2 byte-pair identical);
  - all 6 files `node -c` parse clean.
  BLOCKER (outside n7 write set — needs upstream prose migration): test-route-reachability T11 is correctly RED on 8 un-migrated surfaces that still carry the retired `path_requires_explicit_opt_in` + "Switch-ON contract (#515)" framing (a switch that no longer exists post-#538). Re-pointing T11 to `path_not_installed` (the correct target token) is the faithful read — NOT dropping the pin (that would ship the retired vocab unguarded). The 8: commands/kaola-workflow-phase1.md; plugins/kaola-workflow/skills/kaola-workflow-research/SKILL.md; plugins/kaola-workflow-gitlab/commands/{kaola-workflow-fast.md,kaola-workflow-phase1.md}; plugins/kaola-workflow-gitlab/skills/kaola-workflow-research/SKILL.md; plugins/kaola-workflow-gitea/commands/{kaola-workflow-fast.md,kaola-workflow-phase1.md}; plugins/kaola-workflow-gitea/skills/kaola-workflow-research/SKILL.md. They need the same `path_not_installed` / "(#515, #538)" treatment the fast SKILLs got (kill the "Switch-ON contract (#515)" / switch-ON-OFF framing, rename the refusal literal, keep the PIN comment).

BLOCKER DIAGNOSIS (PLAN GAP, not a closed-node-incomplete): the frozen workflow-plan.md names ONLY the fast-set + adapt/next surfaces in n6-router-prose's write-set (plan L26: workflow-next/adapt/fast commands + next/adapt/fast SKILLs ×3 editions). NONE of the 8 un-migrated surfaces — the full-entry `kaola-workflow-phase1.md` ×3 + `kaola-workflow-research/SKILL.md` ×3, and the 2 forge `kaola-workflow-fast.md` *commands* — appear in ANY node's write-set. So no closed node owns them; this is the planner (n1) under-scoping the #400 12-surface propagation (it scoped the 6 fast surfaces but not the 6 full-entry surfaces nor the 2 forge fast commands). ORCHESTRATOR ACTION: replan / add a node whose write-set is those 8 surfaces (NOT reopen n6 — they were never in n6's declared write-set; editing them under n6 would be a write-set overflow). Template for the migration: github fast.md is the byte-template for the 2 forge fast commands; the 6 full-entry surfaces have NO migrated peer and need fresh "(#515, #538)" prose. The run CANNOT finalize (claude chain runs test-route-reachability → RED) until these 8 are migrated.

n7 WRITE-SET DELIVERABLE: COMPLETE — all 4 contract validators green, byte-pair #1<->#2 in sync, 6-file scope clean (git diff shows exactly the 6 declared files), evidence durable. The RUN: BLOCKED at finalization on the upstream plan gap above. This GREEN line is NOT "run is shippable".
