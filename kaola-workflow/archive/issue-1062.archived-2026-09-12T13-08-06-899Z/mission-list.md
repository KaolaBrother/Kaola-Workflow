# Land #1062: lean orchestrator — 7 roles, one subagent binding per adapter, native_only for OpenCode/Kimi/ZCode/Devin

Branch `workflow/issue-1062`, worktree `.kw/worktrees/issue-1062`. Issue §11.1 is the settled design; §7 is acceptance; §12 is the subtraction checklist (grep in §7.9–10 is the final judge, not the checklist). Owner authorized changing test acceptance meaning for this issue. Release is major (11.1.1 → 12.0.0). No mechanism additions: no counts, caps, thresholds, auto-upgrade, review gates, cost ledger.

- item: Authority sources — `templates/agents/behavior-contracts.json` down to 7 roles with no `intent_class`, `implementer`/`code-reviewer` bodies revised per §11.1 #7–8 with version bumps; `templates/agents/runtime-capabilities.json` gives Claude/Codex×3/Grok/Cursor one `subagent_default` each (§5 table, `model_carrier: profile_model_effort` for Codex+Grok, carrier prose per §12 H) and OpenCode/Kimi/ZCode/Devin `role_dispatch: "native_only"` with only `native_routes`/`availability`; `templates/agents/provenance.json` down to 7.
  status: done
  dispatched: sidekick handoff 1 (shared with the generator item) → edits land in worktree `.kw/worktrees/issue-1062` on `workflow/issue-1062`, committed as "feat(#1062): authority sources and generator — 7 roles, one subagent binding"; handback = commit sha + `--check` outputs
  result: commit `80d324a0` on `workflow/issue-1062`. `behavior-contracts.json` = 7 roles, no `intent_class`, implementer v2 (dependency/architecture + check-narrowing constraints), code-reviewer v4 (focus modes, ADR 0019 §5 clamp). `runtime-capabilities.json`: 6 binding adapters carry `subagent_default {model, effort?, summary}` (Claude sonnet; Codex×3 gpt-5.6-luna/max, `model_carrier: profile_model_effort`; Grok grok-4.6/medium, `profile_model_effort`; Cursor grok-4.6/medium, `exact_binding`); OpenCode/Kimi/ZCode/Devin `role_dispatch: native_only`, `named_roles/deterministic_profiles: false`, guidance = `native_routes` + `availability` only. `provenance.json` = 7. Reviewed diff by orchestrator: matches §11.1 / §12 H–I.

- item: Generator, schema, and sync scripts — `generate-agent-profiles.js` (7 `ROLES`, `subagent_default`/`role_dispatch` validation, per-runtime frontmatter: Claude `model: sonnet`, Grok `model: grok-4.6`+`effort: medium`, Cursor unchanged, Codex TOML pins `model`+`model_reasoning_effort`; drop opencode/kimi/zcode/devin renders; delegation block renders `**Subagent default:**`+`**Roles:**` or the native_only design sentence; 42 renders), `kaola-workflow-resolve-agent-model.js`, `kaola-workflow-adaptive-schema.js` (`CODEX_PINNED_ROLES`, `validateProfileText` flipped to "present and equal", `RETIRED_PROFILE_FILES` +7), Codex preflight + plugin copies, six `sync-*-edition.js` (no agent render or model scaffold for the four native_only editions; REGION cards per §7.14); regenerate every mirror and reach zero drift on all `--check` commands.
  status: done
  dispatched: sidekick handoff 1 (shared with the authority item) → same worktree/branch/commit; handback = commit sha, list of regenerated mirrors, `--check` outputs, and the list of test suites now red (expected; tests are the later mission)
  result: same commit `80d324a0`. Generator: 7 `ROLES`, `BINDING_RUNTIMES`/`isBindingAdapter`, `named_profile` vs `native_only` validation, Claude/Grok/Cursor frontmatter from `subagent_default`, Codex TOML pins `model`+`model_reasoning_effort`, delegation block = `**Subagent default:**`/`**Roles:**` or the native_only design sentence, manifest lists `runtimes` + `native_only_runtimes`; 7 retired `agents/*.md` and 3×7 TOMLs removed; `DEFAULT_AGENT_MODELS` = 7×sonnet. Kernel: `CODEX_PINNED_ROLES` + `CODEX_PINNED_MODEL/EFFORT`, `validateProfileText` requires present-and-equal pins, `RETIRED_PROFILE_FILES` +7, preflight treats pre-#1062 `inherit` posture as migration input. Sync: devin/opencode/kimi/zcode render no agents, `opencode.json` scaffold gone, Devin/ZCode cards say "use a native route or work inline", Cursor card → `implementer`, Grok maps retired `subagent_type` → `implementer` (transitional until finalize skeleton is fixed). `.devin/` added to `.gitignore`. `generate-agent-profiles --check` (7 roles, 42 renders), `generate-routing-surfaces --check` (24 surfaces), `edition-sync --check`, `validate-script-sync`, and every `sync-*-edition --check` exit 0. Expected red (next missions): 15 chain scripts + `validate-workflow-contracts.js`, all 9 edition suites; `test-kernel-conformance.js` hangs >180s (investigate in tests mission). Residue for routing mission: `custody-bearing` still in the Cursor card prose; remove the Grok retired-role regex and dead `runtimeRestrictions` once the skeleton names `implementer`.

- item: Routing prose — `templates/routing/dispatch-contract.md` (§4.6 paragraph, "lets a clean context check what your own cannot", "the subagent default binding", native_only sentence, no `custody-bearing`), `templates/axioms.md` (same first-paragraph copy, "stronger or more expensive models"), `next.skeleton.md` (orchestrator holds design/acceptance meaning/final verdict on a read candidate; conditional `tdd-guide`/`implementer` guard), `finalize.skeleton.md` (suggested routes, `implementer` REGION example without `model=`, "Dispatch `doc-updater` when useful"); `required-blocks.js` tokens updated in the same edit; regenerate routing surfaces.
  status: done
  dispatched: sidekick handoff 2 with orchestrator-authored prose (verbatim in the brief) → worktree `.kw/worktrees/issue-1062`, commit "feat(#1062): routing prose — orchestrator holds judgment, one subagent binding"; also removes the transitional Grok retired-role regex and `custody-bearing` from the Cursor card; handback = commit sha + `generate-routing-surfaces --check` / `edition-sync --check` / every `sync-*-edition --check` output
  result: commit `72c4c60d`. `dispatch-contract.md` carries the §4.6 paragraph (executor not judge / handback is evidence / cost sits in the main context / fan out by cut not count / no escalation ladder), "lets a clean context check what your own cannot", "the subagent default binding", "claim a named role's identity", and the native_only design sentence; `axioms.md` first-paragraph copy synced and "stronger or more expensive models"; `next.skeleton.md` Run-it paragraph states the orchestrator holds selection, design, acceptance meaning, verdict on a read candidate, and finalization, with the conditional `tdd-guide`/`implementer` guard and self-authored paths legitimate; `finalize.skeleton.md` suggested routes, `subagent_type="implementer"` card without `model=`, "Dispatch `doc-updater` when useful". Required-block tokens intact (24 surfaces byte-match). Grok transitional regex, Cursor `custody-bearing`, dead `runtimeRestrictions` removed with zero render change. Grep gate over templates/commands/agents/plugins/hooks/sync scripts: zero hits for tier words, retired role names, `custody-bearing`, `model="opus"`. Diff reviewed by orchestrator: verbatim to the authored text.

- item: Validators and installers — `validate-workflow-contracts.js` (+plugin copy: routed-fix → `implementer`, `VENDOR_MODEL_NOUN_BAN` floor recomputed from the real surface count, tier wording gone), `validate-kaola-workflow-contracts.js` (+gitlab/gitea: single `CODEX_PINNED_ROLES` policy, new binding regex, TOML two-key assertion); `install.sh`/`uninstall.sh` 7 `REQUIRED_AGENTS` + 7 retired names + `BUILD_ERROR_RESOLVER_MODEL` removed from both placeholder lists; `install-grok.sh`/`install-cursor.sh` retire 7; `install-opencode.sh`/`install-kimi.sh`/`install-zcode.sh`/`install-devin.sh` drop agent deployment, retire all 14 (Kimi `kaola-role-*`, Devin doctor assertion removed); root `opencode.json` scaffold removed; `./install-all.sh --check` passes.
  status: done
  dispatched: sidekick handoff 3 → worktree `.kw/worktrees/issue-1062`, commit "feat(#1062): validators and installers follow the 7-role single-binding catalog"; the live `./install-all.sh --yes` + `--check` on this machine is deferred to the acceptance sweep (final tree), so this item verifies installers through `bash -n`, the install/uninstall test suites, and a sandboxed `--check`; handback = commit sha, validator outputs, install-suite outputs. Landed as `b153482b`; orchestrator review found one defect (ZCode `remove_retired_agents` deletes by bare name without the managed marker) → fix folded into handoff 4, item stays in-flight until it lands
  result: commits `b153482b` (validators + installers), `64d6b57e` (ZCode sweep gated on the managed marker), `e5153c16` (Kimi `kaola-role-*` sweep restored to the exact-bytes ownership gate — same defect class, found by the tests). Validators: routed-fix card asserts `implementer` and no `model=`; noun-ban floor measured at 46 surfaces; `CODEX_PINNED_ROLES` parity + `gpt-5.6-luna`/`max` present in every plugin TOML; binding regex renamed; forge validator copies are hand-maintained siblings (not sync products) and were edited directly. Installers: `install.sh`/`uninstall.sh` 7 required + 7 retired; Grok retires 7; Cursor via receipt; Kimi/OpenCode/ZCode/Devin deploy no agents and sweep previously deployed profiles only when Kaola ownership is proven (managed marker; manifest hash where one exists); Devin doctor 14-role assertion gone; `opencode.json` never written. All four contract validators, `bash -n` on nine installers, and the install/uninstall suites exit 0; sandboxed `--check` under a temp HOME reports DRIFT (not installed) without crashing. Live `./install-all.sh --yes && --check` on this machine deferred to the acceptance sweep.

- item: Tests rewritten to the new acceptance meaning (Owner-authorized) — §12.6 list: model-rendering, resolver, routing-surfaces rosters, role-redesign (7 roles, no intent), render-subtraction oracle (Grok/Cursor agents + 5 command runtimes, re-captured baseline), runtime-agent-architecture, six edition suites, install/uninstall suites, retired-name fixtures in claim/finalize/run-chains/sink-merge tests, both walkthroughs and forge plugin walkthroughs/tests; `npm test`, `npm run test:kaola-workflow:editions`, `npm run test:kaola-workflow:claude:full`, `node scripts/simulate-workflow-walkthrough.js` green.
  status: done
  dispatched: sidekick handoff 4 (acceptance meaning set by the orchestrator in the brief: replace tier/roster assertions with their single-binding / native_only analogues, never delete a check to pass, re-capture the render-oracle baseline in the same commit) → worktree `.kw/worktrees/issue-1062`, commit "test(#1062): acceptance follows the 7-role single-binding catalog"; handback = commit sha, per-suite pass list, the four chain commands' exit codes, and the `test-kernel-conformance.js` diagnosis
  result: commit `8ec4c234` (23 files). `npm test`, `npm run test:kaola-workflow:editions` (9 suites), `npm run test:kaola-workflow:claude:full`, `node scripts/simulate-workflow-walkthrough.js` all exit 0 on the worktree; generator/routing/edition-sync/script-sync `--check` 0. Render oracle: `EXPECTED_TOTAL` 300→132 with the baseline re-captured in the same commit; role-redesign 7 roles / `intent_class` absent; resolver + model-rendering single binding; routing-surfaces and runtime-architecture assert the `**Roles:**` line on binding surfaces and the native_only sentence elsewhere; Grok/Cursor 7 pins; Kimi/OpenCode/ZCode/Devin suites assert zero profiles while commands/skills still deploy, with legacy-cleanup ownership proofs against planted previous-release state; retired fixture names → surviving custody; Cursor prompt-bundle ceiling raised to 10.5 KB (measured 9.5 KB after the §4.6 paragraph; Grok stays 8.5 KB). `test-kernel-conformance.js` was never deadlocked — it spawns the vehicle suites and finishes in ~4 min once they are green. Orchestrator reviewed the diff: deleted bands all pinned carriers that no longer exist; no check was removed to pass.

- item: Documentation — new `docs/decisions/0025-lean-orchestrator-single-subagent-binding.md` from issue §4 (two observed facts + Owner value decisions, official facts with fetch date 2026-09-12, Codex luna/max rationale); status notes on ADR 0019/0021 and D-687-01; `README.md`, `docs/api.md`, `docs/architecture.md`, `docs/agents-source.md`, `docs/runtime-capabilities.md`, `docs/conventions.md`, `docs/README.md`, six edition docs (four say "no Kaola role profiles, vendor-native harness"); `CHANGELOG.md` `[Unreleased]` with migration map (old role → surviving role brief mode) and reinstall / Cursor Cloud rebuild note; major version lockstep (package.json 12.0.0 + Codex plugin versions) via the release script's rules.
  status: done
  dispatched: sidekick handoff 5; ADR 0025 authored by the orchestrator at `/tmp/adr-0025-lean-orchestrator-single-subagent-binding.md` and copied in verbatim; version numbers are NOT bumped in-run (`kaola-workflow-release.js --prepare --version 12.0.0` owns that at release; CHANGELOG `[Unreleased]` states the next release is major) → worktree `.kw/worktrees/issue-1062`, commit "docs(#1062): ADR 0025 and live documentation for the lean orchestrator"; handback = commit sha, list of docs touched, doc-grep results
  result: commit `7b52e20c` (18 files). ADR 0025 landed byte-identical to the orchestrator's draft (one enumeration to tighten in the sweep: Cursor's `model_carrier` was already `profile_model_effort`); ADR 0019 / 0021 / D-687-01 annotated as superseded; `docs/README.md` catalog entry; README, api, architecture, agents-source, runtime-capabilities, conventions, and six edition docs state 7 roles / 42 renders / per-runtime binding / native_only; CHANGELOG `[Unreleased]` carries the major upgrade note, migration map, binding table, native_only, routing prose, ADR notes, and the two sweep-ownership fixes. Focused chain `npm run test:kaola-workflow:claude` exit 0. Residue for the sweep: same-sentence "retired" annotations still spell `rolesByIntent` / `intent_class` / `CODEX_PINNED_*_ROLES` in `docs/api.md`, `KAOLA_OPENCODE_*_MODEL` in `docs/opencode-edition.md`, and the tier words in `docs/README.md` — §7.9 wants live docs at zero. One pre-existing dead CHANGELOG history link left alone.

- item: Acceptance sweep — run the §7.9 zero-hit and §7.10 must-hit greps over the stated scope, fix any residue found (record checklist misses in ADR 0025), re-run the four chains and both `--check` families on the final candidate, and read the full diff for the orchestrator's own verdict before finalization.
  status: done
  dispatched: sidekick handoff 6 running orchestrator-authored grep gates (§7.9 zero-hit, §7.10 must-hit), fixing residue, tightening the ADR 0025 carrier sentence, running the live `./install-all.sh --yes` then `./install-all.sh --check` on this machine from the worktree (issue §11.2/§7.5), and re-running the four chains → commit "chore(#1062): acceptance sweep — grep gates, live reinstall, final chains"; orchestrator then reads the full branch diff for the verdict
  result: commit `04f66095`. §7.9 zero-hit gate over the agent-facing scope: only allowed hits remain (installer/uninstaller retired-name lists, kernel `RETIRED_PROFILE_FILES`, the generator's two vocabulary-rejection lines); `{opus|sonnet}` in `validate-kaola-workflow-contracts.js` comments and negative probes in `test-*.js` are outside the §7.9 scope and stay. §7.10 must-hit gate: `subagent_default` ×6, `native_only` ×4, dispatch-contract / next / finalize sentences present, `model=` count 0, `**Subagent default:**` + `**Roles:**` (exactly 7) on all 14 binding surfaces, design sentence and no Roles line on fresh Devin/OpenCode/Kimi/ZCode trees, Grok/Cursor renders free of `model: inherit`. Live docs residue rewritten without spelling retired identifiers; ADR 0025 §7 carrier sentence tightened. Final candidate: `npm test`, editions (9), `claude:full`, walkthrough (178/178), and all four `--check`/sync validators exit 0. Live reinstall on this machine: `./install-all.sh --yes` PASS on 8 runtimes (7 retired profiles removed from Claude/Grok/Cursor/Codex incl. `config.toml` tables; all 14 removed from Devin/OpenCode/ZCode; Kimi role skills gone), `./install-all.sh --check` exit 0 `CURRENT`; `~/.config/devin/AGENTS.md` and `~/.cursor/rules/kaola-workflow-global.mdc` carry the new blocks; `devin doctor` reports no agent profiles configured. Orchestrator read the branch diff (8 commits, `origin/main..HEAD`) and reviewed README/CHANGELOG/ADR surfaces: verdict PASS, ready for finalization.
