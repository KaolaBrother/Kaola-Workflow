# Measure Devin Fusion parent mode and the sidekick tool (F15–F28), evidence only, no decisions

| item | status | dispatched | result |
|---|---|---|---|
| Environment record and probe harness: `devin --version`, live `agent.model`, `devin --help` / `devin doctor --json` / on-disk docs grep for `fusion`/`sidekick`, the exact CLI flags that select a model per child session, the #1058 `env -i` auth workaround re-verified; author the probe fixture repo under `/tmp/kw-fusion-probe` (probe profiles, marker prompts, tmux driver notes) so later missions run a fixed recipe | done | self → `kaola-workflow/issue-1060/.cache/env.md` and `kaola-workflow/issue-1060/.cache/probes/` (fixture generator + driver notes) | `.cache/env.md`. `devin 3000.10.21`; live `agent.model=fusion-claude-fable-5-1-high-sidekick-swe-2-medium`; `devin models list` shows a 210-variant `fusion-<main>-sidekick-<sidekick>` family priced at the main model, SWE-2 listed Free, no Adaptive-main variant (`.cache/devin-models-list.txt`); docs and `--help` have no fusion/sidekick string; `--model` is per-invocation and `-r … --model` switches a resumed conversation; `doctor` loads probe profiles and does not validate `model:`; export carries `tool_definitions`, `powered by`, `generation_model`. Harness `.cache/probes/` validated by a `swe-2-medium` smoke run: no `sidekick` tool on that parent, hooks log works. |
| F15–F17 parent-model tool catalog: fresh child sessions per model (`fusion`, `adaptive`, `fable`, `astra`, one other) listing tools and `powered by` line with exports; mid-session `/model` switch Fusion↔Adaptive in a tmux TUI recording tool list and profile catalog before/after | done | sidekick handoff 1 (shared with mission 4) → executes `.cache/probes/NOTES.md` §F15/F16 and §F17 verbatim; raw outputs in `/tmp/kw-fusion-probe/out/f15-*`, `f17-*`; summary to `kaola-workflow/issue-1060/.cache/f15-f17.md` | `.cache/f15-f17.md` (+ orchestrator DB verification). Sessions `short-frigate`, `malachite-ironclad`, `unique-turner`, `observant-molybdenum`, `western-bongo`, `gaudy-turnip`, `cuddly-grin`, `steel-text`. Only Fusion parents expose `sidekick` (26 tool defs) and their catalog lacks `subagent_general`/`subagent_explore`; every other parent (adaptive, fable, astra, swe-2, glm) has 25 tools, no sidekick, 28-name catalog. `fusion` alias → full id in `sessions.model`. F17: `/model adaptive` mid-session changed `generation_model` (fable → `kimi-k2-7`; resume `-r --model adaptive` → `swe-1-7-medium`) while the `powered by` line and self-reported tool list stayed Fusion; sidekick callability after switch unmeasured. |
| F18–F22 sidekick facts from a Fusion child session: system-prefix model carrier (session DB `message_nodes`), sidekick tool surface and `PROFILES:` answer, self-nesting, instruction loading (`KW-COMPACT-RECOVERY-V2` count, UserPromptSubmit hook stdin), persistence across two handoffs / `/compact` / exit+resume, write boundary foreground vs `block:false` | done | sidekick handoff 2 → executes `.cache/probes/NOTES.md` §F18–F22 (steps 1–10 incl. 8b) in tmux `kw1060-sk`; raw captures `/tmp/kw-fusion-probe/out/f18-22-*.txt`, `f17b-*.txt`, `f18-db.txt`, `hooks.log`; summary to `kaola-workflow/issue-1060/.cache/f18-f22.md` | `.cache/f18-f22.md`. Session `frost-amusement`. F18: sidekick chain root is the system prompt `You are the Sidekick subagent of Devin…`, `powered by SWE-2 Medium`; generation nodes `claude-fable-5-1-high`×109, `swe-2-medium`×29, `gpt-5-6-sol-low`×5 (post-`/model adaptive`), `compactor`×1. F19: sidekick tools = parent set minus `ask_user_question`, `run_subagent`, `read_subagent`, `sidekick`; `PROFILES=NONE`, no self-nesting. F20: sidekick saw project `AGENTS.md` marker ×1, project UPS marker ×1, `KW-COMPACT-RECOVERY-V2` ×1; UPS hook fires once per user prompt with `session_id` only (no subagent id); sidekick tool calls surface as `PreToolUse` `tool_name` (`sidekick`×8, `exec`×5, `write`×2, `read_subagent`, `request_scope`). F21: same shell id across turns, `/compact`, and exit+`-r` resume, but cwd/env reset each call; detached process survived turns and compact. F22: foreground write → TUI approval prompt, Esc cancels the whole turn (`Canceled due to user interrupt`); `block:false` write → `denied because this agent is running in the background`; no file created either way. F17b: after `/model adaptive` the parent reports `The sidekick tool is unavailable in the current tool catalog`; after switching back the same sidekick (same shell) answers `SK_AFTER_SWITCH_OK`. |
| F23–F26 and F28 subagent routing under a Fusion parent: unpinned Kaola profile `powered by` line and `generation_model`, `subagent_general` inheritance, custom profile `model:` acceptance for `fable` / `swe` / `fusion` / `astra` / `gpt-6-astra` / full ids and `devin doctor` validation, `install-devin.sh --check` and `./install-all.sh --check` under Fusion, every locally observable billing/model-uid field | done | sidekick handoff 1 (shared with mission 2) → executes `.cache/probes/NOTES.md` §F23–F25, §F26, §F28 verbatim; raw outputs in `/tmp/kw-fusion-probe/out/f23-*`, `f25-*`, `f26-*`; summary to `kaola-workflow/issue-1060/.cache/f23-f28.md` | `.cache/f23-f28.md` (+ orchestrator notes). Sessions `stream-giraffe` (Fusion parent), `magical-reaper` (Adaptive control). Profile→child carrier identical under both parents: unpinned → `Subagent Default`; `fable` → Fable 5 XHigh; `swe` → SWE-2 High; `astra` → Astra XHigh; `gpt` → Astra Medium; `fusion` → Fusion (Fable 5.1 Medium + SWE-2 Medium); full ids → themselves; Fusion children report `SIDEKICK=NO`. `subagent_general` → `Unknown profile` under Fusion, dispatches under Adaptive. `doctor` lists 26, validates nothing. F26: `install-all.sh --check` exit 0; `install-devin.sh --check` exit 1 caused solely by extra user skill dir `devin-kaola-project-runner` (pre-existing, not Fusion). F28: exports carry no cost field; `sessions.cogs_json` is prompt assembly, not billing. |
| F27 cross-session model switch on this run: snapshot this Mission List while the parent is Fusion, exit, resume in a new Adaptive session via `/workflow-next`, record whether the frontier reconciles with no intake or claim and what the resumed parent reports as its model | done | self → snapshot `kaola-workflow/issue-1060/.cache/f27-before.md` taken in Fusion session `hyper-reply`; successor session running `/workflow-next` writes `.cache/f27-after.md` and closes this row | `.cache/f27-after.md`. Frontier reconciled without intake or claim. Resumed session is not an Adaptive Devin parent; env marks it as Codex Desktop (`CODEX_SESSION_ID=01a045f8-8ff4-7682-b804-994f2841d6b7`, `CODEX_SHELL=1`, `CODEX_INTERNAL_ORIGINATOR_OVERRIDE=Codex Desktop`). Latest Devin DB row `recondite-newsprint` has empty `model`; tool catalog lacks `sidekick`, consistent with non-Fusion. |
| Publish the F15–F28 evidence table as a comment on #1060 in the #1058 row style (artifact path or verbatim quote per row, unmeasurable rows say why), delete every probe fixture and probe profile, confirm `devin doctor` matches the pre-probe baseline | done | self → post comment via `gh issue comment 1060`; probe fixture `/tmp/kw-fusion-probe` deleted; `devin doctor --json` confirmed at 14 profiles | Comment posted: https://github.com/KaolaBrother/Kaola-Workflow/issues/1060#issuecomment-5643669933. `/tmp/kw-fusion-probe` removed. `devin doctor --json` returns `ok: true` with 14 Kaola profiles loaded, matching pre-probe baseline. |

Constraints: measurement only. Do not edit `templates/`, `scripts/`, `docs/`, generated mirrors, or the installed `~/.config/devin/` carrier, profiles, or `config.json` `agent.model` (use per-invocation `--model`; the live orchestrator session must not be the F17 subject). Do not open follow-up issues or write recommendations inside this run; recommendations are a separate comment after the table, at the user's request. Artifacts land under `kaola-workflow/issue-1060/.cache/`. No price inference in F28. No `git push`, no PR, no sink inside missions.
