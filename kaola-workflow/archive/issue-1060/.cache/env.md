# #1060 environment record (2026-09-12)

Measured from the orchestrator shell of Devin session `hyper-reply` (this run's Fusion parent).

| fact | value | source |
|---|---|---|
| CLI | `devin 3000.10.21 (611c1cba)` at `~/.local/bin/devin`; only version under `~/.local/share/devin/cli/_versions/` | `devin --version`, `ls` |
| Live parent model | `agent.model = "fusion-claude-fable-5-1-high-sidekick-swe-2-medium"`; `preferred_family_models = {fusion: <same>, Adaptive: adaptive}` | `~/.config/devin/config.json` (read only) |
| Fusion in `devin models list` | Family `Fusion (fusion)`, **210 variants**, id shape `fusion-<main>-sidekick-<sidekick>`. Mains: Claude Fable 5.1 {low,medium,high,xhigh,max}, Claude Opus 5 {…,+fast}, GPT-5.6 Sol {…,+fast}, GPT-6 Astra {…,+fast}. Sidekicks: `swe-2-medium` ×35, `swe-2-high` ×35, `swe-1-7-medium` ×35, `glm-5-2` ×35, `gpt-5-6-luna-high` ×20, `gpt-5-6-luna-high-priority` ×20, `gpt-5-6-sol-high` ×15, `gpt-5-6-sol-high-priority` ×15. No Adaptive-main Fusion variant. | `.cache/devin-models-list.txt` lines 203–412 |
| Listed price of a Fusion id | Equals the main model's listed price, e.g. `fusion-claude-fable-5-1-high-sidekick-swe-2-medium` = `$10 / 1M Input · $0.25 / 1M Cached · $50 / 1M Output`, identical to `claude-fable-5-1-high`. `swe-2-*` and `glm-5-2` (High) are listed `Free`; `adaptive` is `$0.5 / $0.1 / $2`. Whether the sidekick's tokens are billed at all is **not** stated by the list; no arithmetic done. | same file lines 17–22, 106–113, 203–205 |
| Aliases | `swe` → SWE-2 family; `gpt` → GPT-6 Astra; `opus` → Claude Opus 5; `sonnet`/`claude` → Sonnet 5; `gemini` → Gemini 3.8 Flash; `haiku` → Haiku 4.5. **No alias line** for `fable`, `astra`, `fusion`, `adaptive` — those are family ids, not aliases. | same file, `aliases:` lines |
| Shipped docs | `share/devin/docs/{models,subagents,changelog/stable}.mdx` contain no `fusion`, `sidekick`, `swe-2`, or `fable` string | `grep -iE` over the docs dir |
| `devin --help` | No `fusion`/`sidekick` string. `--model <MODEL>` per invocation; with `-c`/`-r` it *switches the resumed conversation to this model*. `-p/--print`, `--export [PATH]`, `--permission-mode auto|accept-edits|smart|dangerous`, `--respect-workspace-trust false` for non-interactive untrusted dirs. | `devin --help` |
| `devin doctor --json` (repo root, before fixture) | `ok: true`, one check `custom subagent profiles`: `14 profile(s) loaded: <the 14 Kaola names>` — pre-probe baseline | this shell |
| `devin doctor --json` (fixture dir) | `26 profile(s) loaded` = 14 Kaola + 12 `kwp-*`. Accepted `model: fusion`, `fable`, `astra`, `gpt`, `swe`, `adaptive`, and full ids without complaint → **doctor does not validate `model:` values** (F25, partial; consistent with #1058 F14) | `make-fixture.sh` run |
| Auth | `devin auth status` → `Logged in (via Devin).` from this shell; no `WINDSURF_*`/`ACP_*`/`DEVIN_*` env vars present, so the #1058 `env -i` workaround was not needed | this shell |
| Session DB | `~/.local/share/devin/cli/sessions.db` (685 MB). `sessions(id, model, agent_mode, working_directory, cogs_json, metadata, …)`, `message_nodes(session_id, node_id, parent_node_id, chat_message, metadata)`, `subagent_heads(session_id, agent_id, chain_node_id)`, `tool_call_state`. `sessions.model` stores the parent model id per session. | `sqlite3 .tables/.schema` |
| Export shape (`-p --export`) | keys `schema_version, session_id, agent, steps, final_metrics`; `agent.model_name` (display name), `agent.tool_definitions[]` (**authoritative tool list**), a `source: system` step `You are powered by <Model>.`, per-step `extra.generation_model` (resolved id), `final_metrics.total_*_tokens`. No `cogs`/billing field in the export. | `out/smoke-swe2.export.json` |
| Hooks | User `config.json` has exactly one `UserPromptSubmit` command hook (Kaola). Project `.devin/hooks.v1.json` is the documented per-project carrier; the fixture installs `UserPromptSubmit` (logs stdin, injects `KW-PROBE-UPS-MARKER-1060`) and `PreToolUse` (logs `tool_name`) writing `out/hooks.log`. Smoke run confirms the project UPS hook fired and its marker reached the child (`UPS_MARKER=1`). | fixture + smoke |
| Smoke result (`swe-2-medium` parent, non-interactive) | `powered by SWE-2 Medium`; 25 tools; **no `sidekick` tool**; `run_subagent`/`read_subagent` present; catalog = 14 Kaola + 12 kwp + `subagent_explore`, `subagent_general`; `AGENTS_MARKER=1`, `RECOVERY_MARKER=3`; `sessions.model=swe-2-medium`; 17 204 prompt tokens. | `out/smoke-swe2.*` |

Side effect noted: running a command whose cwd was the fixture loaded `/tmp/kw-fusion-probe/AGENTS.md` into **this** orchestrator's rules. The orchestrator is not a probe subject, so no evidence row is affected; child sessions start fresh.

Harness: `.cache/probes/{make-fixture.sh, run-parent.sh, db-queries.sh, export-summary.js, NOTES.md}`. Fixture at `/tmp/kw-fusion-probe` (12 profiles, project hooks, `out/`). Mission-4 note: `install-devin.sh --check` and `install-all.sh --check` run from the repo root with the 12 probe profiles present under the *fixture* only, so the user-scope catalog those checks read stays at 14.
