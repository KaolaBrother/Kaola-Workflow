# Finalization - Summary: bundle-506-507

## Delivered
Transient-fault fail-open hardening bundle (#506 + #507) — the "opaque/transient fault → swallowed to a determinate outcome" anti-pattern (audit H1 family), across all four editions.

- **#506** — `sink-merge.js` `assertWorktreeClean` OUTER `git worktree list --porcelain` probe is now fail-closed (bounded retry → typed refusal) instead of `catch (_) { return; }`, mirroring the #496 inner-probe fix. Distinguishes "no worktree for this branch" (legitimate skip) from "probe failed" (cannot prove → fail closed).
- **#507** — classifier boundary-2 internal CLI-fetch catch now classifies the error (`spawn_fault`/`killed`/`clean_nonzero`), bounded-retries transient classes (N≤2), and emits `indeterminate` (→ `result: escalate` via the existing #495 envelope + forge forward-compat handlers) on persistent transient. `clean_nonzero` stays determinate (`target_unavailable`, not retried).

## Files Changed
14 code/test files + CHANGELOG.md (15 total):
- sink-merge.js ×4 editions (root, codex twin, gitlab, gitea) + simulate-workflow-walkthrough.js + test-gitlab-sinks.js + test-gitea-sinks.js
- classifier.js ×4 editions + test-claim-hardening.js + test-gitlab-workflow-scripts.js + test-gitea-workflow-scripts.js
- CHANGELOG.md ([Unreleased]/Fixed — #506 + #507 entries)

Root↔codex byte-identical for both sink-merge.js and classifier.js.

## Test Coverage
RED→GREEN per node (real-git fixtures). Cross-edition gate: all four chains green (exit 0) — claude / codex / gitlab / gitea.

## Final Validation Evidence
- `npm run test:kaola-workflow:claude` — exit 0 ("Workflow walkthrough simulation passed")
- `npm run test:kaola-workflow:codex` — exit 0 ("Kaola-Workflow walkthrough simulation passed")
- `npm run test:kaola-workflow:gitlab` — exit 0 ("GitLab Codex workflow walkthrough simulation passed")
- `npm run test:kaola-workflow:gitea` — exit 0 ("Gitea Codex workflow walkthrough simulation passed")
- Adaptive barrier gates: `--resume-check`=0, `--gate-verify`=0, `--barrier-check`=0, `--verdict-check`=0
- Evidence: `.cache/n1-sink-merge-fix.md`, `.cache/n2-classifier-fix.md`, `.cache/n3-review.md`
- Validation reuse covers code/test impact through node n5; the finalize-node CHANGELOG edit (n4) is docs-only and outside the rerun trigger.

## Documentation Docking
DOCKED — CHANGELOG.md is the only doc impacted (two user-visible Fixed entries). No README/API/architecture/.env impact (internal fail-closed hardening; no public interface change). Code comments updated inline at the fix sites.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- #510 (R1) — forge `parseJson` malformed-JSON #307 parity divergence (root/codex escalate vs forge determinate-refuse). Non-blocking; fix lives in forge.js, outside #507's frozen write-set.
- #511 (R4) — forge end-to-end claim-flow determinate-refuse coverage not re-pinned after #507. Low severity; behavior unchanged.

## Run gaps
- manual:forge-parsejson-parity (n3 review finding R1 — classifier boundary-2 malformed-JSON cross-edition divergence): filed: #510
- manual:forge-claimflow-coverage (n3 review finding R4 — forge claim-flow determinate-refuse coverage gap): filed: #511

## Closure Decision
Closure scan: no partial implementation, no unresolved conflicts. Two review follow-ups (R1/R4) filed as #510/#511 (non-blocking, tracked). Both #506 and #507 acceptance criteria pass → close both on sink.

## Commit And Push
[pending final Git gate; final hash reported after push]

## GitHub Issue
#506 + #507 — to be closed on merge sink (bundle, all-or-nothing).

## Roadmap
updated yes — `.roadmap/issue-506.md` + `.roadmap/issue-507.md` removed; ROADMAP.md regenerated by cmdFinalize.

## Archive
pending — `kaola-workflow/archive/bundle-506-507/` (cmdFinalize Step 8b).

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | invoked | .cache/n4-docs.md | |
| documentation docking | invoked | this summary (Documentation Docking) | |
| final-validation fix executors | N/A | — | no validation failures |
| roadmap refresh | invoked | kaola-workflow/ROADMAP.md | |
| archive completed folder | pending | | |
| final commit and push | ready | git status/diff/upstream | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE
