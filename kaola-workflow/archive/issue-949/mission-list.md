# Retire the model badge repo-wide: delete what exists only to make a badge render, keep what actually routes a dispatch — the owner's rule is that cosmetic redundancy goes and functioning mechanism stays

- item: THE DECIDING MEASUREMENT, and everything else waits on it — is the rendered `model="opus"` / `model="sonnet"` literal in `commands/kaola-workflow-finalize.md` (`:87` tdd-guide, `:96` build-error-resolver, `:155` doc-updater) FUNCTIONAL or COSMETIC? Known: `install.sh` `install_managed_agent` (~:390-395) rewrites every installed agent's frontmatter `model:` to `inherit`, and `scripts/kaola-workflow-resolve-agent-model.js:15`'s own header claims "THIS MAP IS THE EFFECTIVE TIER OF EVERY INSTALLED AGENT … resolution always lands here". If the literal genuinely pins build-error-resolver to opus at dispatch, it is functional and stays; if the resolver governs regardless and the literal changes no behaviour, then by the owner's own rule the literal is cosmetic too and goes with the badge. Do not assume — the resolver's comment describes ITS OWN resolution path, not the Claude Code runtime's handling of an explicit `model=` dispatch parameter, and `test-install-model-rendering.js` shells out to the resolver rather than exercising a real dispatch, so no existing test settles this.
  status: done
  dispatched: investigator (read-only, must RUN something — sandbox installs and a real resolution, not a grep) → report at kaola-workflow/issue-949/.cache/premise-model-literal.md
  result: .cache/premise-model-literal.md — **FUNCTIONAL, observed not inferred. The literals STAY.** Evidence: 283 subagent transcripts across 50 sessions of this project. Of dispatches carrying `model=` where the requested family differed from the parent's (n=71), **69 followed the request**; the 2 exceptions are `fable` aliases outside the runtime's availableModels allowlist, neither of them sonnet or opus. Of dispatches carrying NO `model=` where the role's map tier differed from the parent (n=22), **22/22 ran on the PARENT model and zero on the map tier**. Same-role A/B is direct: in session bbc1c516 the finalize surface was expanded into context, and 1m54s later doc-updater was dispatched `model="sonnet"` → its transcript records 80 assistant messages all `claude-sonnet-5` while the parent ran `claude-opus-5`; every other subagent in that same session was dispatched without `model=` and every one ran opus. Corroborated by the shipped binary's own schema strings ("Use `inherit` to match the spawning conversation"). **`resolve-agent-model.js` does NOT govern a Claude Code dispatch at all** — it governs the other runtimes and the hook's advisory field. `assertEveryDispatchHasModel` is load-bearing, mutation-proven on all three literals (each deletion → validator exit 1 naming the block). CONSEQUENCE IF DELETED: tdd-guide and doc-updater move sonnet→opus on every finalize, and build-error-resolver silently un-pins from opus in any non-opus session. TWO SIDE FINDINGS: (1) the prose "it is what shows the model badge" frames the badge as the PURPOSE when it is a side effect of the override — the removal proposal, mine included, was reasoning from that incomplete premise; (2) `DEFAULT_AGENT_MODELS`' header comment "THIS MAP IS THE EFFECTIVE TIER OF EVERY INSTALLED AGENT" is FALSE for Claude Code, refuted 22/22 — it has now misled this project at least twice.

- item: Map every `badge` occurrence into cosmetic vs functional, exhaustively, across the 21 tracked files (~118 hits). Live surfaces measured so far: `README.md` (10, incl. `:204-218` explanation + session-model blockquote, `:260`, `:1264-1268`), `docs/architecture.md:341-343`, `templates/routing/finalize.skeleton.md:44-49` (the `REGION:command`-wrapped `## Agent Model Badge` section), the 3 GENERATED `kaola-workflow-finalize.md` surfaces, `install.sh:786`. Out of scope by this repo's own precedent: `docs/decisions/`, `docs/investigations/`, `docs/audits/` (5 files) and `CHANGELOG.md` are dated historical records — the same precedent that left the retired `--profile` axis named in them.
  status: done
  dispatched: investigator (read-only census, classifies every hit cosmetic vs functional with evidence) → report at kaola-workflow/issue-949/.cache/badge-census.md
  result: .cache/badge-census.md — real numbers are **358 total hits / 120 files; 99 LIVE across 22 files** (not the ~118 I sketched); 218 are archive run records, 41 sanctioned historical docs + CHANGELOG. Case-insensitivity was load-bearing AGAIN: 4 files match `Badge` but not `badge` — the validators, carrying the hardest guard. **CORRECTION that refutes #949's filed premise: `docs/architecture.md`'s "every dispatch" is TRUE.** There are exactly 3 `Agent(` dispatch cards across ALL Claude Code command surfaces and all 3 carry a placeholder; `workflow-init`/`workflow-next`/every SKILL have ZERO cards. Only the trailing badge clause is cosmetic. BLAST RADIUS of deleting the section, measured on a mirror: 3 of 4 chains red (`must include: ## Agent Model Badge` at validate-workflow-contracts:179, gitlab:204, gitea:203, plus `You MUST pass model=` at :180) and test-opencode S2 red ("found 0 of 3"); **kimi stays SILENT at exit 0 / 516** — a real coverage asymmetry. The opencode/kimi anchored rewrite does **NOT throw** — it no-ops silently (plain `if` with no `else`) and the edition marker vanishes, so the sentence telling those runtimes their task tool has no model parameter would be dropped with nothing reporting it. Nothing asserts on README badge prose, but README IS in `SELF_HOST_TEST_CONSUMED`, so its edit must land BEFORE the receipt run.

- item: OWNER'S RULINGS, taken in conversation with the census in hand — (1) RENAME `## Agent Model Badge` → `## Agent Model Dispatch`, updating all 6 anchor sites in lockstep, because the WORD is cosmetic but the heading STRING is functional; (2) KEEP the 12 "why" comments in the sync/test transforms, rewording only where they state the badge as the PURPOSE rather than a side effect; (3) REWORD `install.sh:786` rather than drop it — the operator still needs to learn the settings key was popped; (4) CLOSE kimi's silent gap by adding the twin of opencode's S2 anchor assertion. The three `model=` literals, the `REGION:command` directive and `assertEveryDispatchHasModel` all STAY — measured functional.
  status: done
  dispatched: self — asked the user in conversation
  result: recorded above; these four rulings plus the resolver-comment fix are the whole write plan.

- item: Settle the edition-sync layer, which I initially mis-scoped as pure deletion — `scripts/sync-opencode-edition.js` (7) and `scripts/sync-kimi-edition.js` (5) carry an ANCHORED rewrite that restates the canonical badge section for runtimes whose task tool has no model parameter (`OPENCODE_BADGE_GUIDANCE`), plus a residue guard that THROWS `model-badge residue in <surface>` on any surviving `model=` prose (`sync-opencode-edition.js:364`, `MODEL_MENTION = /model=/`). That guard is FUNCTIONAL — it stops a Claude-only instruction shipping to a runtime that cannot honour it — so the machinery is re-anchored to whatever the canonical surface becomes, not deleted. `test-opencode-edition.js` (8) and `test-kimi-edition.js` (2) pin the restatement and must move with it, never be repaired ahead of it.
  status: todo

- item: Fix the false scope claim in `DEFAULT_AGENT_MODELS`' header comment (`scripts/kaola-workflow-resolve-agent-model.js` ~:10-15) — "THIS MAP IS THE EFFECTIVE TIER OF EVERY INSTALLED AGENT" is refuted 22/22 for Claude Code, where the map is consulted by nobody and an absent `model=` yields the parent model. The comment's second half ("resolution always lands here") is correct — it is the first sentence's SCOPE that overreaches, so narrow it to the runtimes that actually consume the script rather than deleting it. Owner approved this in conversation. NOTE: this file has three plugin byte-mirrors at `plugins/kaola-workflow{,-gitlab,-gitea}/scripts/kaola-workflow-resolve-agent-model.js` and `validate-script-sync.js` asserts 4-copy parity at HEAD, so the edit must be byte-identical across all four or that guard reds.
  status: done
  dispatched: implementer (F) → .cache/impl-resolver-comment.md; scope EXTENDED mid-flight on the agent's own finding, with my approval: a second sentence inside DEFAULT_AGENT_MODELS ("A change here reaches every runtime, not Claude alone") is literally true but invites the exact dispatch-time-authority misreading this issue kills, so it is being qualified as TRANSITIVE rather than deleted
  partial-result: first pass DONE and it CORRECTED MY BRIEF TWICE. (1) My parity claim was wrong: the 4 resolver copies are a WORKING-TREE byte group (validate-script-sync.js:158-166, checkByteIdenticalGroup :377-395, reads bytes off disk), NOT the "committed kernel parity at HEAD" family — that is KERNEL_COPIES/adaptive-schema.js at :93-99. So the green is earned on uncommitted bytes and no caveat is owed; the agent mutation-proved it in a scratch non-git mirror by perturbing one word inside the new comment. (2) My three-consumer list was wrong: only the dispatch-log hook (hooks/...dispatch-log.sh:34, :77) reads this map directly. Codex per-spawn effort comes from CODEX_PINNED_*_ROLES in the kernel (adaptive-schema.js:46,55) via templates/routing/slots.js, held in lockstep by test-agent-model-resolver.js:43-50; opencode derives its reasoning list from SOURCE FRONTMATTER (sync-opencode-edition.js:144-146). Both are TRANSITIVE via the pins — writing my phrasing unqualified would have planted a SECOND false scope claim in the comment fixing the first. Hashes now byte-identical x4; node --check x4, validate-script-sync and test-agent-model-resolver all exit 0. FLAGGED, must be re-run after wave 2: the three contract validators abort at :179 on the renamed heading and so never reach their resolver assertions at :328/:331/:961 — the agent hand-checked those; I confirm them mechanically instead.
  result: both passes shipped. Final bytes a50edf6b… on all four copies (progression 46fcbd37 HEAD → d549c190 wave 1 → a50edf6b shipped), which I verified independently. Comment-only across both passes: 80 insertions / 36 deletions, the non-comment filter on a -U0 diff returns nothing, and no `'sonnet'`/`'opus'` line was added or removed, so map values are untouched. Finding (b) qualified rather than deleted, and it encodes the TIMING the old sentence flattened: the tier reaches other runtimes transitively through the pins — Codex classes held in lockstep, opencode's reasoning list derived from the frontmatter this map is held equal to, both following at the next sync — while the dispatch-log hook, the one direct runtime reader, follows immediately, and a Claude Code dispatch follows none of it. Mutation proof re-run against SHIPPED bytes with a vacuity interlock (the script asserts the new sentence is present before mutating) and drifting a different copy than wave 1 (gitea, not gitlab) so the proof does not reuse one lucky path: control clean, mutant reports drift, guard armed.

- item: Apply the removal — delete the cosmetic wording, and either restate the `model=` rule with its real reason or retire it, per what the first item measured. Test custody applies: `implementer` writes prose and `install.sh`, `tdd-guide` owns every test-path change. Edit the SKELETON and regenerate; never hand-edit a rendered surface.
  status: in-flight
  dispatched: WAVE 1, four concurrent agents in the worktree /Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/issue-949 on branch workflow/issue-949, disjoint write sets, all agreeing on the exact new heading string `## Agent Model Dispatch`; reports to main's kaola-workflow/issue-949/.cache/. (A) implementer → templates/routing/finalize.skeleton.md + regenerate the 3 rendered command surfaces → .cache/impl-skeleton.md. (C) implementer → scripts/sync-opencode-edition.js + scripts/sync-kimi-edition.js → .cache/impl-sync.md. (D) implementer → README.md + docs/architecture.md + install.sh → .cache/impl-prose.md. (F) implementer → the 4 byte-identical kaola-workflow-resolve-agent-model.js copies → .cache/impl-resolver-comment.md. WAVE 2 after all four land, so it verifies against the final tree and holds test custody last: (B) tdd-guide → the 3 contract validators + test-opencode-edition.js + test-kimi-edition.js, anchor updates plus the new kimi S2 twin → .cache/impl-guards.md. Suite is spawn-bound; every agent told to re-run an unexpected failure serially, and I re-verify everything serially at the end.
  partial-result: WAVE 1 COMPLETE, all four disjoint. (A) skeleton: heading renamed to `## Agent Model Dispatch`, badge clause cut, rule kept and now carrying its real reason (`inherit` → an omitted `model=` runs the role on this session's model), REGION wrapper and all three placeholders untouched; regenerated, `--check` exit 0 at 18 surfaces; the 4 expected reds captured verbatim. (D) prose: README/architecture/install.sh badge hits 14 → 0, `bash -n` exit 0; agent extended the brief on its own judgement by adding the *why* clause the deletion would otherwise have taken with it, kept "not by a status-line override" because dropping it would orphan a nearby bullet, and confirmed none of its three files cross-references the renamed heading. (C) sync: REPRODUCED the briefed hazard first (6 × exit 0, no throw, `carries edition marker=false`), then re-anchored both transforms and added `assertModelDispatchAnchorMatched` + a deliberately looser `MODEL_DISPATCH_HEADING_NEAR_MISS` that throws when the anchor goes stale — scanning the CANONICAL body, never the output, because opencode's own emitted `## Model and effort are inherited` heading would otherwise self-trigger; zero "badge" left in either file; all six generator legs exit 0. (F) resolver: 4 copies byte-identical (verified independently by me — `--numstat` +20/−9 with identical diff-content hashes on all four; my earlier `--stat` read of "29 vs 19" was a scaling artifact, not drift), `validate-script-sync` exit 0, and it corrected my brief twice as recorded above.
  result: WAVE 2 (B, tdd-guide) DONE — .cache/impl-guards.md. Started from the RED baseline verbatim (3 validators exit 1 on the old string; opencode 2 failures / 561 passed; **kimi exit 0 / 516 — silent**), re-anchored every site, and added `K2-anchor` at test-kimi-edition.js:335-392 as the twin of opencode's S2. Mutation-proven on a scratch mirror with the section removed and regenerated: mutated kimi exit 1 with K2-anchor red (517 passed), mutated opencode exit 1 (560 passed) — and the control that makes it mean anything, **the PRE-CHANGE kimi suite passed at 516 against that same mutated tree**, so the gap was real and is now closed. Also mutation-proved the two validator assertion families that had been unreachable since the rename (`:334`/`:337` resolver presence → exit 1 when the resolver is moved aside; `:965` VENDOR_MODEL_NOUN_BAN → exit 1 on an injected vendor noun), both restored to 0. Final counts: opencode 563 (unchanged, correct for a pure re-anchor), kimi 521 (+5, accounted). It added a NEGATIVE PIN — `assertNotIncludes(file, 'Agent Model Badge')` in all four validators — so the retired heading cannot come back; that pin plus its comment are the only surviving live occurrences of the word, and they are functional.
  self-verified: I re-ran everything SERIALLY in the worktree with absolute paths and real exit codes. All four validators exit 0; generate-routing-surfaces --check 18 surfaces exit 0; test-generate-routing-surfaces 434; test-route-reachability 331; test-agent-model-resolver 0; validate-script-sync 0; test-opencode-edition 563; test-kimi-edition 521; test-install-model-rendering 0. Live badge sweep (case-insensitive, excluding CHANGELOG/archive/.origin/decisions/investigations/audits) returns ONLY the four negative pins and their comments. One false red of my own making along the way: `node $WT/$s` with a flag inside `$s` — zsh does not word-split an unquoted variable, so `--check` was glued into the filename and the script "crashed"; re-run quoted, it is exit 0.

- item: (wave 2 closed the impl item) Verify — `generate-routing-surfaces.js --check` green at 18 surfaces, the opencode and kimi edition suites, `validate-workflow-contracts.js` and its 2 plugin mirrors, `test-install-model-rendering.js`, and the walkthrough at FULL scope (the fast gate samples a 1/12 shard, so green there is not verification). Diff is edition-touching, so it owes all four chains.
  status: todo

- item: Dock docs and finalize — CHANGELOG under `[Unreleased]` BEFORE the receipt run, then `/kaola-workflow-finalize issue-949`. Note `README.md` is test-consumed, so it is in this run's write set and its edit must land before the chains.
  status: todo
