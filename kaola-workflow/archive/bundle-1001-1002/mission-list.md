# Close #1001 (finalize surface splices the gap-sweep gate but never the scan that produces its artifact) and #1002 (`finalize --check` reports `chains_stale` as a bare token, dropping the culprit paths #648 shipped)

Worktree: `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1001-1002`
Branch: `workflow/bundle-1001-1002`. Both issues were filed by READING at `83b997e0`; both bodies
name their own hypothesis as unmeasured. Establish reachability by RUNNING before writing any code —
a premise that fails is a values fork for the user, not a licence to build.

- item: #1001 census — do archived runs actually carry `.cache/run-gaps.json` beside their summary? The issue's own body says this comes first: if every archived run has one, the surface omission is costing nothing today and the priority drops. ~408 folders under `kaola-workflow/archive/`. Measure presence, size, and whether the summary's `## Run gaps` section is non-trivial; report the count both ways.
  status: done
  dispatched: investigator "census-1001" (read-only) → writes `kaola-workflow/bundle-1001-1002/findings/census-run-gaps-artifact.md` in the MAIN root
  result: `findings/census-run-gaps-artifact.md`. **"Costing nothing today" is TRUE of outcomes, FALSE of mechanism.** Recent-40: artifact present 40/40, harm 0/0. BUT all 27 populated recent artifacts carry a `manual:*` class — machine proof an operator hand-wrote the seed AND hand-invoked the producer. **The producer has ZERO automated callers**; all 140 archived artifacts were hand-typed. The whole-archive aggregate is worthless: gap-sweep was born at `issue-435` (2026-06-13) and the 228 runs before it stamp nothing. One genuine escape post-dating the reverse check: `bundle-888-…-895` (2026-08-01) wrote 6 manual seed lines + 6 summary rows + 3 real filings with `sweptClasses: []` — scanner never re-run. **`install.sh` ships NO `docs/`**, so a consumer install cannot reach the `conventions.md:507` MUST at all; its only route to the producer is the `artifact_missing` refusal payload. Consumer exposure > self-host exposure.

- item: #1001 reachability — confirm the shipped surface really omits the producing invocation. `templates/routing/slots.js:148` defines `fz-gapsweep-run`; `commands/kaola-workflow-finalize.md:221` is one of 12 rendered finalize surfaces. Check ALL 12 rendered surfaces (claude/codex/opencode/kimi x github/gitlab/gitea as applicable) for any `--json` scan invocation, and check whether some other slot or step already emits it. A defect that is already covered elsewhere on the surface is not a defect.
  status: done
  dispatched: investigator "surfaces-1001" (read-only) → writes `kaola-workflow/bundle-1001-1002/findings/surface-audit-gapsweep-slot.md` in the MAIN root
  result: `findings/surface-audit-gapsweep-slot.md`. **Premise HOLDS on every particular.** `slots.js:148` is the ONLY gap-sweep slot; all 12 finalize surfaces (6 generated+tracked, 6 gitignored additive) carry the gate and ZERO carry a scan. Refutation attempt FAILED by measured A/B: `--check` never scans (`main()` at gap-sweep.js:613-617 is an exclusive if/else; `runCheck` never calls `runScan`) and on a missing artifact REFUSES with `{"result":"refuse","reason":"artifact_missing"}` + exit 1. No other slot/step/script produces `run-gaps.json`. `docs/conventions.md:505-535` states a three-step MUST; the surface splices step 3 only, and NO finalize surface points its reader at that doc. **NO existing test pins the surface** — `test-gap-sweep.js` (65 matches) never reads one; the walkthrough has zero gap-sweep mentions. Implementer exposure is regeneration breadth, not a stale pin.

- item: #1002 reachability — stale a chain receipt deliberately in a scratch clone and read all three consumers of the `chains_stale` finding: the `finalize --check --json` envelope, the finalize transaction's own envelope, and `## Validation` in `finalization-summary.md`. The issue only observed `--check`. Find where `attachChainsStaleDiagnostics` (`scripts/kaola-workflow-adaptive-schema.js:1300`) output is dropped, and say which consumers keep it. Do NOT edit tracked files.
  status: done
  dispatched: investigator "stale-1002" (read-only, fabricated stale receipt — NOT a real chain run) → writes `kaola-workflow/bundle-1001-1002/findings/chains-stale-consumer-probe.md` in the MAIN root
  result: `findings/chains-stale-consumer-probe.md`. Reproduced on two stale conditions (code + prose-only) via the real producer over `--mock-chain`, under a minute, no chain run. **TWO drop sites, not one** — the #991 lesson again. Site 1 `claim.js:4141` (`--check`) flattens to `classification` and loses EVERYTHING. Site 2 `persistValidationToSummary` `claim.js:3959-3966` renders a FIXED field list — keeps both hashes (they ride inside `detail[0]`) but drops `stale_paths`/`stale_kind`; this is the DURABLE archived copy, so its loss outlives the run. **The finalize transaction envelope is FINE** (`claim.js:5267` assigns the whole object). Corrections to the issue: "one call site" wrong; "no stamped hash and no current hash" true of `--check` ONLY. `--check` is an UNOBSERVED consumer, not a regression — `evaluateFinalizePreconditions` (`8e42a864`) postdates #648 by 19 days and was BORN flattening; #648's verifying surface (`plan-validator --finalize-check`) was retired with the plan grammar. Drop is GENERIC (`chains_red` loses `redChains[]` too) but only stale reads blind, since `chains_red` names its culprit inside `detail`. **Nothing pins `stale_paths` on any finalize consumer today**; the 2 live pins are on `run-chains --release-check`. Both sites are in `claim.js`, NOT the anchor → propagation set is the 4 rename-normalized `*claim.js` copies at differing line numbers (see findings table). Docs to update: `docs/api.md:233-247`, `:592`.

- item: #1001 remedy — splice the scan ahead of the gate so the surface carries the whole procedure, in the skeleton not the rendered surface, then regenerate. Shape depends on the census: if runs already produce the artifact some other way, the right change may be smaller or nothing. The refusal shape (`--check` refusing on an absent artifact) is against the design's zero-refusal count — do not take it without asking the user.
  status: done
  dispatched: DECIDED SHAPE — new slot `fz-gapsweep-scan` (`--json`, three forges) spliced at the HEAD of `## Step 6`, before the `## Run gaps` grammar, because the scan's `sweptClasses` is what tells the author which rows to write and the Step 7 gate reconciles afterwards. Refusal shape NOT taken (`--check` already refuses loudly; adding refusals is against the design). implementer "impl-1001" in `$WT`, edits `templates/routing/slots.js` + `templates/routing/finalize.skeleton.md` + regenerates the 6 tracked surfaces. `scripts/test-route-reachability.js` T6c is READ-ONLY to it. Baseline VERIFIED RED BY ME: 6 failures / 344 passed, one per tracked surface.
  result: LANDED, verified by me end-to-end. `slots.js` gained `fz-gapsweep-scan` (`--project {project} --json`, three forge basenames) beside `fz-gapsweep-run`; skeleton `## Step 6` gained a 3-line lead-in + a `fz-scripts-resolver` + `fz-gapsweep-scan` block ahead of the summary template. Rendered check: `commands/kaola-workflow-finalize.md` Step 6 carries resolver+scan; gitea SKILL scans at :173 with the GITEA basename and gates at :217 — order correct, forge correct. `generate-routing-surfaces.js --check` → "all 18 surfaces byte-match the skeleton", exit 0. `test-route-reachability.js` → PASSED, 368 assertions (was 6 FAIL / 344 pass). Step 7's previously dangling "re-run the scanner" referent now resolves.

- item: #1002 remedy — carry the already-computed culprit detail into the `chains_stale` report on whatever consumers were measured to drop it. Plumbing, not a new measurement. `adaptive-schema.js` is byte-identical across all four editions, so any edit there is an edition-touching diff and must be propagated + four-chain stamped.
  status: done
  dispatched: DECIDED SHAPE — ADDITIVE sibling fields; `checks.validation` STAYS the bare string token (reshaping it to an object would break the documented `docs/api.md:233-247` envelope contract — out of scope, and a user-owned contract I will not alter unilaterally). Anchor `adaptive-schema.js` is NOT touched: both drop sites are in `claim.js`, so the propagation set is the 4 rename-normalized `*claim.js` copies. implementer "impl-1002" in `$WT`; `scripts/test-finalize-door.js` T15 is READ-ONLY to it. Baseline VERIFIED RED BY ME: 13 failures / 716 passed.
  result: LANDED, verified by me. Site 1 `evaluateFinalizePreconditions`: three guarded `if (diag.X) checks.X = diag.X` assignments beside `changed_paths`; `checks.validation` UNCHANGED as a bare token. Site 2 `persistValidationToSummary`: `stale_kind:`, `stale_paths_truncated: true`, and a `stale_paths:` label with `- <path>` bullets matching `## Changed Paths` rendering. Absent-stays-absent honoured at both (no `[]`, no `null`). The in-file envelope doc comment block was updated too. Propagation VERIFIED BY MY OWN GREP — all four copies report identical counts (stale_kind=3, stale_paths=8, stale_paths_truncated=3). Gates: `test-finalize-door.js` PASSED 729 assertions (was 13 FAIL/716); `test-edition-sync.js` PASSED 30; `test-route-reachability.js` still PASSED 368 (#1001 undisturbed).

- item: Tests for both remedies, authored by someone who did not implement them. Each must be mutation-proven: plant the defect back, watch it red. An assertion count is not a witness.
  status: done
  dispatched: TWO tdd-guide agents, concurrent, disjoint files, both in the worktree `$WT=/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1001-1002`. "tests-1001" → surface pin over TRACKED rendered surfaces; "tests-1002" → `--check` envelope + `## Validation` summary pins. Each writes ONLY test files, commits nothing, and reports its mutation proof inline in its final message.
  result: BOTH armed, and I verified each transition MYSELF rather than trusting a report. `scripts/test-route-reachability.js` T6c (+87 lines): universe derived from `GENERATED_SURFACES` so it cannot go vacuous on the gitignored editions, asserts that universe is 6 BEFORE asserting over it, per-surface (no roll-up), forge-correct basenames via `edition-sync.forgeRel`, skips commented-out lines, and pins ORDER (scan before gate AND before the row grammar) — a bare "contains --json" check would have passed a uselessly-placed scan. 6 FAIL → 368 pass. `scripts/test-finalize-door.js` T15a/b/c/d/e (+210 lines): T15b is the load-bearing one — the code-stale and prose-only envelopes were BYTE-IDENTICAL before and demand opposite actions. T15c pins `stale_paths_truncated` (a capped list that does not say so reads as complete). T15d/e pin the degrade paths. Also guards `checks.validation` is STILL a bare token. 13 FAIL → 729 pass. Test custody honoured: neither implementer authored the pins it was judged by.

- item: Adversarial verification of both closures, then docs docking — `CHANGELOG.md` under `[Unreleased]`, `docs/` where structure or contract changed. Write CHANGELOG BEFORE the four-chain stamp: on this self-host repo `codeTreeHash` includes docs, so a docs commit after the stamp stales an ~11-min receipt.
  status: done
  dispatched: TWO strands in parallel. (a) adversarial-verifier "verify-both" (model=fable) in `$WT`, tasked to REFUTE both closures — output lands inline in its final message AND at `kaola-workflow/bundle-1001-1002/findings/adversarial-verdict.md` in the MAIN root. (b) docs docking done by me inline (`dispatched: self`) — `docs/api.md` envelope + gap-sweep sections, `CHANGELOG.md` under `[Unreleased]`. Docs are written BEFORE the four-chain stamp because `codeTreeHash` includes them on self-host.
  result: `findings/adversarial-verdict.md`, verdict `fail`, `findings_blocking: 1`. **Claim 1 (#1001) SURVIVES** — five one-site mutations (missing scan / scan-after-gate / scan-after-grammar / missing `--project` / wrong-forge basename in the gitea surface) each reddened for the right reason naming the right surface; vacuity attacks defeated (universe pinned at 6, all six git-tracked so fresh-worktree immune, reads rendered bytes); resolver proven to define `$KAOLA_SCRIPTS` in the same fence (`RESOLVED=./scripts`); and the rendered instruction verified END-TO-END on a scratch project — gate alone refuses `artifact_missing` exit 1, then scan writes the artifact and reports `sweptClasses`, rows reconcile, `--check` passes. **Claim 2 (#1002) behaviour SURVIVES** (both sites independently pinned, absent-stays-absent proven at producer AND both consumers, four-copy hunks byte-identical with zero forge-token leakage, codex parity-policed). **Its GUARD CLAUSE is FALSIFIED → new item appended below (R1).** Docs docked by me: `docs/api.md` gained the three `checks` fields with an explicit "`stale_paths` is NOT `changed_paths`" warning, and the gap-sweep section now states the modes are exclusive and the gate consumes without producing; `CHANGELOG.md` carries #1001 under **Added** (no code repaired) and #1002 under **Fixed**, both written from the MEASUREMENTS rather than the issue text.

- item: R1 from adversarial verification — the #1002 fix is present and byte-identical in all four `*claim.js` copies, but NO guard reds when it is removed from the gitlab or gitea copy. Measured: a mutant gitlab copy passes `test-edition-sync.js` (30 green), `validate-script-sync.js`, and `test-gitlab-workflow-scripts.js`. `claim.js` is a declared HAND-PORT (exempt from byte checks), T15 spawns canonical only, and byte-parity policing covers the codex copy alone. The repo's own idiom for this class is `testArchiveIntegrityPortedToAllEditions832` in `simulate-workflow-walkthrough.js` — "the cheap cross-edition pin that catches a hand port which silently skipped a remedy on one forge". Extend it; do not invent a new mechanism.
  status: done
  dispatched: tdd-guide "pin-fourcopy" (test custody — production code is already correct, so this is a test-only change) in `$WT`. Extends the existing #832 four-copy pin with #1002 tokens. Mutation proof required INDEPENDENTLY on the gitlab copy and the gitea copy — one site at a time. Reports inline in its final message; no separate findings file.
  result: `testStaleDiagnosticsPortedToAllEditions1002` in `scripts/simulate-workflow-walkthrough.js` (+88 lines), sited directly beneath the #832 guard it copies. Pins per COPY and per SITE (4 x 2), asserting the GUARDED WRITE (`if (diag.X) checks.X = diag.X`) rather than a bare token — the same field names appear in the `checks.*` doc comment, so a substring match would be satisfied by prose. It scopes to the function BODY and then STRIPS COMMENTS, because the agent measured a body-scoped regex passing against `// if (diag.stale_paths) checks.stale_paths = ...` — the "your check can silently not check" trap, found and closed before it shipped. **Arming re-verified INDEPENDENTLY BY ME**, not taken from the report: deleted the `stale_kind` push from the GITEA copy alone (snapshot first, never `git checkout`) → walkthrough exit 1 naming `plugins/kaola-workflow-gitea/.../kaola-gitea-workflow-claim.js persistValidationToSummary()`; restored from snapshot, `cmp` byte-identical, `numstat` back to 26/0 matching its siblings. FULL-SCOPE walkthrough then green: **185/185 scenarios, exit 0**. Targeted suites on the final tree: route-reachability 368, finalize-door 729, edition-sync 30, `generate-routing-surfaces --check` all 18 byte-match.

- item: #1003 (filed by this run, then adopted into it at the user's direction) — enrich the `chains_stale` operator hint with `stale_kind`, completing #648 item B.3. MECHANISM, traced by me: `operator_hint` is rendered inside `finding()` at `adaptive-schema.js:1267`, BEFORE `attachChainsStaleDiagnostics` runs, so the hint cannot see the diagnostics at construction time. There are TWO `chains_stale` construction sites (codeTreeHash mismatch ~:1298, headSha mismatch ~:1312), both wrapped by the same attach call — so re-rendering the hint inside `attachChainsStaleDiagnostics` fixes both from one place. CAUTION: `prose-only` does NOT mean "skip the re-run" — test-consumed prose is inside the hash by definition, so the receipt is genuinely stale; the value is telling the operator WHAT moved, which is #648's own stated motive.
  status: done
  dispatched: tdd-guide "tests-1003" authors the pin FIRST (test custody; must red on the current tree) in `$WT` → then I implement inline (`dispatched: self`) across the 4 byte-identical `adaptive-schema.js` copies. The chain run started earlier was KILLED deliberately (exit 144 = signal, not a chain failure) because this code change invalidates it; one four-chain stamp at the end covers everything.
  redispatch: tests-1003 died mid-response (API connection lost) having written NOTHING — `git status` clean, no test file. Re-dispatched identically as "tests-1003b". Nothing to salvage, so no merge risk.
  result: LANDED. Pin `T16` in `scripts/test-finalize-door.js` (tests-1003b, +~260 lines) — reads the hint off the SHIPPED finding (transaction envelope + durable `## Validation`), never the template, because the defect IS the ordering seam. Contract: `code` says code moved; `prose-only` says ONLY test-consumed prose moved; `mixed` names both; ALL THREE still command the regenerate; diagnostics absent → today's sentence byte-for-byte. Two refinements beyond my brief: distinctness is asserted AFTER scrubbing path-shaped tokens (closing the differ-check trap where interpolating the culprit list fakes difference), and the forbidden-excuse register is NEGATION-AWARE so stating the constraint does not trip it. Implementation by me: `chains_stale` became a `(ctx)` template; the re-render lives in `attachChainsStaleDiagnostics`, so BOTH construction sites are covered from one place and the degrade path returns before it. Propagated byte-identically to all 4 anchor copies (single md5 `11d05658…`). Gates: finalize-door 10 FAIL/781 → PASSED 791; walkthrough 185/185; edition-sync 30; validate-script-sync OK incl. "4 Oracle Kernel copies identical at HEAD"; route-reachability 368; all 18 surfaces byte-match. **Excuse guard verified BY ME**: planting "it is safe to proceed without a re-run" reds EXACTLY ONE assertion (the excuse guard on T16b) and nothing else; anchor restored from snapshot, `safe to proceed` count 0, all 4 copies re-identical.

- item: (discovered at finalize, after Step 7's sweep) the finalize transaction reported all three findings on its envelope, and the archived `## Validation` / `## Changed Paths` / `## Mission List` sections were EMPTY. Establish whether the surface's own Step 6 template is what silences them, and recover this run's record.
  status: done
  dispatched: self — inline, because it was blocking the integrity of this run's own durable record.
  result: **Real defect, filed #1004 (P1, verified OPEN, 4416 bytes).** `appendSummarySection` (`claim.js:3940-3946`) returns FALSE WITHOUT WRITING when the heading already exists and `replace` is falsy; all three producers (`:3976`, `:3988`, `:4057`) call it without `replace`. Step 6's template instructs creating exactly those headings, and the prose beneath calls them "where the finalize transaction's own findings land" — so following the surface literally is what stops them landing. **Measured A/B, one axis:** headings present → sections empty; same three headings deleted and the SAME `finalize --keep-worktree` re-run → script writes `classification: chains_green` / `green: true` / `mode: chain-receipt`, the 19 paths, and `items: 9`. **Corpus: 16 of 49** archived summaries with a `## Validation` heading are empty. Same class as #1001, one layer down. This run's record is recovered; the sections sit after `## Status:` because that is where the transaction appended them and its output is kept verbatim. NOT added to `## Run gaps`: the scanner refuses an archived project (`project_archived`) by design, and a hand-typed row would be the exact failure the surface warns about — recorded in `## Follow-Up Items` instead, with the seed line removed again.
