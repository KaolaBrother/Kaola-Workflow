# Let a merge sink proceed beside verified co-active worktree runs (#1075)

- item: Add a classification-only sinkPreflight exemption for an untracked sibling live claim folder that is verified as a real co-active run of this main root (status active, main_root realpath equal, worktree_path registered in this repo on the claim's own branch, not this sink's branch, no symlinks, segment boundary), in canonical scripts/kaola-workflow-sink-merge.js and the Codex/GitLab/Gitea ports, with positive and negative regressions in test-sink-merge.js; #715/#893/#1067 protections unchanged.
  status: done
  dispatched: sidekick implements the lead-designed arm and tests in .kw/worktrees/issue-1075 (uncommitted on workflow/issue-1075); handback is the diff plus test-sink-merge/#1067/spawn-classification outputs; lead reviews and commits.
  result: c1496281 on workflow/issue-1075. coActiveSiblingProjects verifies status active, main_root realpath, registered worktree on the claim's own branch, not main checkout, not this sink's branch, no symlinks, one folder per worktree; arm is segment-bounded and classification-only. Canonical, Codex mirror (byte-equal), GitLab and Gitea ports. Also restored the #893 carried-byte-equal read (archiveKey removed in 3973af23). Tests #1075 a/b (8 look-alikes) and #893 w11 with RED proof; test-sink-merge 1097 assertions, spawn-classification, edition-sync, script-sync and three contract validators pass. CHANGELOG and docs/api.md docked. Residual noted for the summary: a branch-carried byte-equal untracked archive copy still aborts later at git checkout (not the #1075 shape).

- item: Validate the frozen candidate with the producer-selected chains and the integration walkthrough, then establish readiness for finalization.
  status: done
  dispatched: sidekick runs run-chains --project issue-1075 and simulate-workflow-walkthrough.js from .kw/worktrees/issue-1075 at c1496281; receipt lands at kaola-workflow/issue-1075/.cache/chain-receipt.json.
  result: Receipt headSha c1496281, workTreeHash clean, scope all-four, claude/codex/gitlab/gitea exit 0, no failed step; simulate-workflow-walkthrough 178/178 passed. Ready for finalization; the base is still 2b1af448, so if bundle-1065-1066-1067 lands first the candidate is rebased and re-validated before its own finalize.

- item: Extend the same verification to a sibling run's finalized-but-unsunk archive folder kaola-workflow/archive/<sibling>/ (measured: the first #1075 sink at 250c1fd3 refused sink_blocked over issue-1072's untracked archive, whose worktree is still registered on workflow/issue-1072), with regressions, then re-validate the candidate; #893 w3 bare-sibling-archive refusal unchanged.
  status: done
  dispatched: sidekick implements the lead-designed extension and tests in .kw/worktrees/issue-1075 (uncommitted); lead reviews, commits, then chains/walkthrough re-run with the receipt landing in this archive folder's .cache.
  result: Withdrawn on owner direction before any commit: a sibling's finalized-but-unsunk archive blocking this sink is a scheduling-order matter, not a product gap, so no archive-phase mechanism is added. The partial uncommitted edits were discarded (git checkout -- .); the candidate stays 250c1fd3 with its bound receipt. Resolution recorded in finalization-summary Sink Findings: issue-1072's archive is preserved outside the repository with a sha256 manifest while this sink runs, then restored byte-identically before its own sink.
