# Issue #1080 — Retire root CLAUDE.md: AGENTS.md is the only repository-level instruction surface

Goal: this repository carries no root `CLAUDE.md` (nor `.claude/CLAUDE.md` / `CLAUDE.local.md`);
`AGENTS.md` is the only repository-level instruction surface for every supported runtime, Claude Code
(≥ 2.1.277) included. `workflow-init` reports shadowing `CLAUDE.md` files and never creates one.
Measured-capability records, docs, ADR, and CHANGELOG describe the new design. No finalize, merge, or
issue-state change from this agent; report candidate SHA, diff summary, and verification receipts.

## Missions

- item: M1 — Remove the root `CLAUDE.md`, replace the bridge pins (validator A2/`validate-workflow-contracts`/walkthrough) with one negative pin over `CLAUDE.md`, `.claude/CLAUDE.md`, `CLAUDE.local.md`, drop `CLAUDE.md` from `ROOT_EDITION_READ_FILES` and the run-chains T38 root-read list, and update the walkthrough named-surface count by hand.
  status: done
  dispatched: self — production edits land in `.kw/worktrees/issue-1080` on branch `workflow/issue-1080`; run records stay in `kaola-workflow/issue-1080/`
  result: `git rm CLAUDE.md`; `scripts/validate-workflow-contracts.js` drops CLAUDE.md from the retired sweep and gains the filesystem-absence pin over the three shadowing names; `scripts/test-runtime-agent-architecture.js` replaces A2 with the same negative pin; `ROOT_EDITION_READ_FILES` drops CLAUDE.md in canonical + regenerated codex/gitlab/gitea copies; `test-run-chains.js` T38 and the two forge ports drop the CLAUDE.md root-read row; walkthrough named surfaces 3→2 and expected `+3`→`+2`. Mutant proof (worktree): planted root `CLAUDE.md` → `validate-workflow-contracts` exit 1 `Error: AGENTS.md is the only repository-level instruction surface; remove CLAUDE.md (any repository CLAUDE.md shadows AGENTS.md for Claude Code)` and `FAIL: A2: no root CLAUDE.md — AGENTS.md is the only repository-level instruction surface`; file removed → `Workflow contract validation passed`, worktree clean.
- item: M2 — Change `workflow-init` to read `AGENTS.md` and report shadowing `CLAUDE.md` / `.claude/CLAUDE.md` / `CLAUDE.local.md` by name as the single project authority, never creating one; update the slot description to "AGENTS.md guidance"; regenerate the 18 canonical/forge command+skill surfaces and the codex/forge script mirrors from source.
  status: done
  dispatched: self — edit `templates/routing/{init.skeleton.md,slots.js,required-blocks.js}` then `generate-routing-surfaces.js --write` and `edition-sync.js --write`
  result: inspection loop reads `AGENTS.md` then `for file in CLAUDE.md .claude/CLAUDE.md CLAUDE.local.md` with `SHADOWING INSTRUCTION FILE (hides AGENTS.md from Claude Code)`; Maintain section makes `AGENTS.md` the single authority, proposes deleting an import-only file or moving owner facts then deleting, requires owner authorization, and never creates one; `in-frontmatter` description reads "with AGENTS.md guidance" on all three forges; six tracked init command/skill surfaces regenerated; codex validator + four run-chains copies synced; `test-kimi-edition.js` K4 allows `.claude/CLAUDE.md` as a shadowing-file reference. `generate-routing-surfaces --check` all 24; `edition-sync --check` 6 forge ports in parity.
- item: M3 — Update measured-capability records: `claude_instructions` claim/date/version floor plus the 2026-09-19 two-token runtime probe in `runtime-capabilities.json`, and `claude-local.compatibility_reads: ["project AGENTS.md"]` in `runtime-contract-adapters.json`.
  status: done
  dispatched: self
  result: `claude_instructions` observed 2026-09-19, `version_floor: "2.1.277"`, claim records direct `AGENTS.md` reading and the shadowing rule; new `runtime_probe` `claude_agents_md_native_20260919` records tokens `KAOLA-AGENTS-ONLY-7731` / `KAOLA-CLAUDE-OVERLAY-4402`; claude evidence array extended; `claude-local.compatibility_reads` is `["project AGENTS.md"]`. `test-issue-1044-runtime-adapters` 65, `test-issue-1046-global-contract` 171, `test-issue-1051-global-contract` 50; `generate-agent-profiles --check` unchanged (42 renders).
- item: M4 — Documentation and records: README runtime table, `docs/architecture.md`, `docs/runtime-capabilities.md`, `docs/conventions.md`, `docs/workflow-state-contract.md`, `docs/opencode-edition.md`, `docs/zcode-edition.md`, `docs/api.md`, new ADR 0026 (number 0024/0025 already taken in the live tree) with supersession notes in ADR 0020/0023, `docs/README.md` index, and CHANGELOG `[Unreleased]`.
  status: done
  dispatched: self
  result: README Claude row is "Commands and root `AGENTS.md` (direct from v2.1.277)"; architecture, runtime-capabilities (evidence + 2026-09-19 probe), conventions (provenance table + axiom count), workflow-state-contract, opencode/zcode editions, and `docs/api.md` updated; ADR `0026-agents-md-single-instruction-surface.md` created; supersession notes added to ADR 0020 (status + clause) and ADR 0023 decision 5; `docs/README.md` index lists 0026; CHANGELOG gains `[Unreleased] ### Removed` and `### Changed`. **Deviation:** the issue says "New ADR 0024", but the live tree already has ADR 0024 and 0025, so the record is 0026 to avoid a collision.
- item: M5 — Focused validation + integration chain + mutant proof: run the affected suites, `node scripts/simulate-workflow-walkthrough.js`, the four-chain receipt for the exact candidate, plant a root `CLAUDE.md` and watch the fast-chain validator go red, then remove it, and record the live Claude Code probe.
  status: done
  dispatched: self — focused suites first, then the required chains; receipts bound to the candidate commit
  result: walkthrough 178/178 (`Workflow walkthrough simulation passed`); `test:kaola-workflow:editions` 11/11 with `DSH_HOME` unset (`edition test lane passed`); validators (`validate-workflow-contracts`, codex/gitlab/gitea contracts), `validate-script-sync` (14 common + 18 groups), `test-validate-script-sync`, `test-runtime-agent-architecture` 730, `test-generate-routing-surfaces` 480, `test-route-reachability` 148, `test-run-chains`, `test-kimi-edition` 434 all green. Four-chain receipt `kaola-workflow/issue-1080/.cache/chain-receipt.json` bound to `headSha 151aeba2…`, scope all-four/edition_coupling: codex, gitlab, gitea green; claude red on ONE step, `scripts/test-issue-1052-cursor-cli-startup-prep.js`, because the workspace sandbox denies Darwin process introspection (`/bin/ps: Operation not permitted`, 14 failures, 236 passed). That test is byte-unchanged by this candidate and fails only from the sandbox; widening access required approval and no approval channel is available, so a green claude leg is not achievable in this environment. Live probe: Claude Code `2.1.277`, `claude -p --model haiku --max-turns 1` at the worktree root, quoted `node scripts/simulate-workflow-walkthrough.js` — a fact carried only by `AGENTS.md`. `install.sh` and `hooks/hooks.json` are byte-identical to base (`git diff` empty); `~/.claude/rules/kaola-workflow-global.md` sha256 `7b66221b…`, mtime Sep 18, untouched.
- item: M6 — Repair the acceptance regression: the #1080 `workflow-init` rewrite changed the command render, so recapture the #1055 render baseline and rerun the four chains bound to the new candidate.
  status: done
  dispatched: baseline recaptured by the worker in `.kw/worktrees/issue-1080` via the oracle's declared `--write-baseline`; the commit `4914d09a` and the four-chain rerun were executed by the orchestrator because the worker's sandbox (workspace-write, no approval channel) cannot write the git index or the main run folder
  result: acceptance claude chain went red on `test-issue-1055-render-subtraction-oracle` — 30 render drifts against the `d8a59cdc` baseline; recaptured with `--write-baseline`, changing exactly 30 `workflow-init` command records (5 runtimes x 3 forges x LF/CRLF), other 102 records byte-identical, oracle green (5 assertions, 132 comparisons); committed as `4914d09a` (candidate `151aeba2` + baseline recapture); four-chain receipt bound to `4914d09a` recorded under M5's receipt path. Environment deviations kept as evidence: the worker sandbox's `/bin/ps` denial and one claude-leg `test-install-model-rendering` timeout under concurrent M7 live-leg load (that file passes standalone in <4 min; three stale `install.sh` children were reaped).
