# Finalization - Summary: issue-669

## Delivered
ENOBUFS hardening of the `git status --porcelain` family (post-#666 sibling tier). Applied the existing
64 MB `GIT_MAX_BUFFER` cap to every `status --porcelain` CONTENT probe (plan-validator dirty-fence,
adaptive-node leg-dirty + two path-scoped barrier probes, claim's three probes, sink-merge's three
probes, and the divergent gitlab/gitea forge ports of claim + sink-merge). The safety-critical crux:
flipped the plan-validator `--parent-clean-check` dirty-fence from fail-OPEN (`catch → porcelain=''` →
zero dirty paths → pass on a huge tree) to fail-CLOSED — an unconditional typed `cannot_prove_clean`
refuse with non-zero exit on any probe fault.

## Files Changed
- scripts/kaola-workflow-plan-validator.js (+ 3 edition copies) — cap + fail-closed dirty-fence flip + `cannot_prove_clean` hint
- scripts/kaola-workflow-adaptive-node.js (+ 3 edition copies) — cap on leg-dirty + path-scoped porcelain probes
- scripts/kaola-workflow-claim.js (+ 3 edition copies, gitlab/gitea hand-ported) — cap on 3 porcelain probes
- scripts/kaola-workflow-sink-merge.js (+ 3 edition copies, gitlab/gitea hand-ported) — cap on porcelain probes
- scripts/test-adaptive-node.js — PARENT-CLEAN-CHECK-ENOBUFS (>1 MB) + PARENT-CLEAN-CHECK-CANNOT-PROVE-CLEAN (corrupt .git/index) regressions; runVal harness maxBuffer raised
- CHANGELOG.md — `[Unreleased]` entry for #669
- kaola-workflow/.roadmap/issue-672.md — new follow-up source (run gaps)

## Test Coverage
Not a coverage-gated repo. `node scripts/test-adaptive-node.js` → 1770 assertions pass;
`node scripts/simulate-workflow-walkthrough.js` → "Workflow walkthrough simulation passed", exit 0;
`node scripts/edition-sync.js --check` → clean parity.

## Final Validation Evidence
Self-host four-chain receipt (`.cache/chain-receipt.json`) stamped by `kaola-workflow-run-chains.js
--project issue-669` after the feature commit — the single full-suite pass before Finalization.
Per-node evidence: `.cache/n1-porcelain-cap.md` (RED→GREEN), `.cache/n2-review.md` (verdict pass, 0
blocking), `.cache/n3-adversarial.md` (NOT-REFUTED, verdict pass).

## Documentation Docking
DOCKED — the sole doc surface is the CHANGELOG `[Unreleased]` entry; no public interface / API / env /
architecture changed, so no README/docs/api/.env.example/ADR impact. The `cannot_prove_clean` reason is
an internal validator emit. Evidence: this summary + `.cache/n4-finalize.md`.

## Final Validation Failure Ledger
| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none) | — | — | — | — |

## Follow-Up Items
- #672 — residual porcelain-probe fail-opens deferred from #669 (adaptive-node:4522 leg-dirty probe, medium; claim:483 worktreeDirtyState, low; + closure-audit/release cap-completeness note). Both verified pre-existing and byte-identical pre-diff.

## Run gaps
- manual:porcelain-leg-dirty-failopen (adaptive-node synthesizeLevel leg-dirty-commit probe fails open on porcelain overflow so real leg content is silently omitted from the merge live since #542): filed: #672
- manual:porcelain-worktree-dirtystate-failopen (claim worktreeDirtyState conflates unprobeable with missing feeding destructive cmdSweepLegacyWorktrees): filed: #672

## Closure Decision
The two deferred residuals are both pre-existing, out-of-mandate porcelain fail-opens filed as #672
(see Run gaps). No unresolved conflict, partial implementation, or value/standing decision remains for
#669 itself — the single mandated crux (dirty-fence fail-closed) plus the full cap sweep are complete
and both gates passed. Close #669.

## Commit And Push
Feature commit `5a2f284b` (17-file cross-edition #669 fix + CHANGELOG + new `.roadmap/issue-672.md`).
Archive commit `479745fb` (`chore: archive issue-669`, cmdFinalize-authored — archive rename + roadmap
closure). Push and the merge sink itself remain owned by the orchestrator (`sink-merge --sink` from
main root); not run by this contractor.

## GitHub Issue
#669 — to be closed by the merge sink after the four-chain merge gate.

## Roadmap
Regenerated by cmdFinalize (commit `479745fb`): removed `.roadmap/issue-669.md`, added the new
`.roadmap/issue-672.md`, retained #670/#671. `kaola-workflow/ROADMAP.md` now lists #670, #671, #672
only.

## Archive
Archived by cmdFinalize (commit `479745fb`) to `kaola-workflow/archive/issue-669/`, `--keep-worktree`.

## Required Agent Compliance
| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| n1-porcelain-cap (tdd-guide) | invoked | .cache/n1-porcelain-cap.md | |
| n2-review (code-reviewer/fable) | invoked | .cache/n2-review.md | |
| n3-adversarial (adversarial-verifier/fable) | invoked | .cache/n3-adversarial.md | |
| doc-updater | skipped | CHANGELOG-only, no checklist doc impact | sole doc surface is CHANGELOG, updated inline in the finalize node |
| documentation docking | invoked | this summary + .cache/n4-finalize.md | |
| final-validation fix executors | N/A | — | no final-validation failure |
| roadmap refresh | done | kaola-workflow/ROADMAP.md (commit 479745fb) | |
| archive completed folder | done | kaola-workflow/archive/issue-669/ (commit 479745fb) | |
| final commit and push | ready | commit 479745fb on workflow/issue-669; push/sink owned by orchestrator | |

## Status
ARCHIVED AFTER FINAL GIT GATE

## Attestation
claim_planner_attested: attested
finalize_contractor_attested: attested
