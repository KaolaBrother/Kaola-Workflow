# Make the opencode and kimi installers prune retired support scripts, so a script removed from the tree stops living forever in the runtime home (#965)

- item: Author the failing pin — a fresh install over a home holding a `.js` absent from the manifest must leave that home holding exactly the manifest set, proven separately for opencode and for kimi. The shape being ported is `install.sh:188` ("Remove stale support scripts not present in source"); the edition suites are `scripts/test-opencode-edition.js` and the kimi equivalent. Must be shown RED on the baseline before any production edit.
  status: done
  dispatched: tdd-guide, working in the worktree `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/issue-965`. Test artifact lands in `scripts/test-opencode-edition.js` (beside the existing S1 case, ~:1334) and `scripts/test-kimi-edition.js` (beside P1, ~:942) — both already install the real installer into a temp home, which is the seam. RED proof to be reported inline in its return.
  result: `S1b` at `scripts/test-opencode-edition.js:1356` and `P1b` in `scripts/test-kimi-edition.js` (+139 lines, no production file touched). RED verified by me, not taken on report — `node scripts/test-opencode-edition.js` → 3 failures / 567 passed, `node scripts/test-kimi-edition.js` → 3 failures / 525 passed, both failing exactly on the planted retired script surviving the install. Each pin plants a retired name + a user `.js` + a non-`.js`, runs the REAL installer, and reads the manifest through the installer's own CLI; a positive control asserts neither planted `.js` is a manifest name so it cannot pass for the wrong reason. The non-`.js` survival assertion is green on baseline by construction — it is the overreach guard, meaningful only after the fix. JUDGEMENT CALL pinned: an unlisted `.js` in the installer-owned dir IS swept, matching `install.sh`'s `*.js` enumeration rather than the narrower manifest+hash shape opencode uses for agents.

- item: Port that prune into `install_support_scripts()` in `install-opencode.sh` (:404) and `install-kimi.sh` (:199), both of which are currently pure manifest-read-then-`cp` with no destination enumeration. Use the safe shape `sweep_retired_agents()` (`install-opencode.sh:237`) already models — enumerate the destination, intersect against the manifest allowlist, and never construct a delete path from a manifest-supplied name. Anything the installer did not write is user-authored and must survive.
  status: done
  dispatched: implementer, in the worktree `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/issue-965`. Edits `install-opencode.sh` and `install-kimi.sh` ONLY; the two suites are `S1b`/`P1b` and it may run but never edit them. Green is `node scripts/test-opencode-edition.js` and `node scripts/test-kimi-edition.js` both exit 0 with no pre-existing case lost.
  result: +27 lines `install-opencode.sh:427`, +26 lines `install-kimi.sh:223`; both test files untouched, so custody held. It improved on `install.sh`'s shape — it intersects against a `deployed` array of what was ACTUALLY copied rather than re-reading the manifest, and guards the sweep behind `${#deployed[@]} -gt 0` so a failed manifest read sweeps nothing instead of emptying the dir. Verified by me: opencode 570 pass (was 567+3), kimi 528 pass (was 525+3), `test-install-manifest-single-source.js` and `test-install-all.js` (131) still green. I suspected that empty-array guard was broken under `set -euo pipefail` on macOS `/bin/bash` 3.2.57 — REFUTED empirically, 3.2 returns `${#arr[@]}` on an empty local array gracefully.

- item: Mutation-prove each pin is armed, one install site at a time — revert one edition's prune, confirm that edition's suite reds, restore, repeat for the other. An N-site mutant proves >=1, never N, so the two sites are mutated separately.
  status: done
  dispatched: self. Snapshot both installers to the scratchpad first (`git checkout` would nuke the uncommitted fix), then flip the sweep guard `-gt 0` -> `-lt 0` in ONE file at a time.
  result: BOTH sites armed and edition-independent. Mutant 1 (opencode only): opencode RED 3 failures / 567, kimi stayed GREEN 528. Mutant 2 (kimi only): kimi RED 3 failures / 525, opencode back GREEN 570. Both files restored and byte-diffed against the proven-good snapshots — identical.

- item: Mutation-prove each pin is armed, one install site at a time — revert one edition's prune, confirm that edition's suite reds, restore, repeat for the other. An N-site mutant proves >=1, never N, so the two sites are mutated separately.
  status: todo

- item: Decide chain scope from the actual diff and run what it owes. An edition-only diff (the two additive installers plus their suites) owes its own suites, not the four chains; a diff reaching shared surfaces changes that. Run the walkthrough at full scope before calling anything verified — the fast gate samples it at a rotating 1/12 shard.
  status: done
  dispatched: self. Walkthrough first, then hand chain selection to `run-chains.js` at finalize rather than choosing it by hand — it diff-scopes, and the diff is the two additive installers + their two suites + CHANGELOG.md.
  result: walkthrough GREEN at FULL scope — 209/209 scenarios, `total:1` in the shard record, so not the 1/12 sample. Chains PASS, receipt at `kaola-workflow/issue-965/.cache/chain-receipt.json` (written to the MAIN root, not the worktree). Selection was `claude-only`, reason `non_edition_diff`, base `d5165f7c`, 5 files, `touchedEditionPaths: []` — the additive editions are not among the four chain editions, which is why their own suites were run separately. `headSha c3f4ecfc` equals the worktree HEAD. Worth noting the chain's own walkthrough step ran `--shard auto/12`, so the full-scope run above is the one that counts.

- item: Dock the docs — a `CHANGELOG.md` `[Unreleased]` entry under `### Fixed`, and confirm the two parity comments ("mirrors install.sh" in `install-opencode.sh`, "same as install-opencode.sh" in `install-kimi.sh`) are true once the code lands, since they were the sentences that made the gap invisible.
  status: done
  dispatched: self — prose judgement, and doc-updater fabricates schema when it is not handed exact text.
  result: `[Unreleased] ### Fixed` entry added at `CHANGELOG.md:3` (the section did not exist; v9.7.0 was at the top). The comment half of this item is CLOSED WITH NO EDIT, and the issue was wrong to ask for it: `install-opencode.sh:419` and `install-kimi.sh:215` say "mirrors install.sh" about the Oracle Kernel source fallback, and `install-kimi.sh:197` says "same as install-opencode.sh" about the manifest list — all three are accurate and none ever claimed prune parity. I had read a narrow comment as a broad one when filing. Both corrections are recorded in the CHANGELOG entry rather than left in the issue.
