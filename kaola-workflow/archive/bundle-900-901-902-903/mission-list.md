# Close #900–#903: the finalize/sink/closure gaps observed in one live consumer run (financial-agent #330, Codex 7.1.1 / upstream v9.1.1)

Run posture: worktree. main_root `/Users/ylpromax5/Workspace/Kaola-Workflow`,
worktree `/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-900-901-902-903`,
branch `workflow/bundle-900-901-902-903`. Baseline commit `9b68b096`.

Standing hazard for this run: in the last four bundles, **five issue premises turned out to be
false**. Every premise here is measured before anything is built on it. A collapsed premise is the
result, not a setback — the answer may be "wording, not a guard", or "close as already-correct".

- item: Verify #901's premise — reproduce, against a consumer fixture whose root `.gitignore` carries `.cache/`, whether the sink really emits `archive_commit: done` + exit 0 while the archive's `.cache` evidence never lands as a blob in the published commit. Cause is claimed at `git check-ignore` over `kaola-workflow/archive/<p>/.cache/final-validation.md`; the sink is `scripts/kaola-workflow-sink-merge.js`. Highest severity of the four — durable evidence loss. Report whether the loss reproduces, and whether the source copy is deleted after.
  status: done
  dispatched: investigator subagent, read-only, output to `kaola-workflow/bundle-900-901-902-903/.cache/premise-901.md`
  result: **PREMISE HELD.** Report at `.cache/premise-901.md`; transcripts in scratchpad `p901/run-both.log`, `run-sole.log`. Reproduced against the shipped sink on both archiver postures with a single-axis negative control (`.gitignore` body the ONLY difference): ignored leg → exit 0, `steps.archive_commit:"done"`, 3 files in the commit, `/.cache/` blobs `[]`; control (`node_modules/`) → all 8 land. Four seams, all in `step === 'archive_commit'` of `kaola-workflow-sink-merge.js`: (1) `:2111` the #832 ignore probe asks `check-ignore` about the archive **directory**, which a basename `.cache/` rule does not match (exit 1) though files under it are ignored (exit 0) — so `archiveIgnored=false` and the honest-skip arm at `:2170-2176` never fires; (2) `:2121-2122` and `:2135` `git add` exits 1 while still writing non-ignored entries, and `catch (_) {}` discards the only signal — hence the doubled warning; (3) `:2128`→`stagedPathsUnder(:155)` derives `archived_paths` from the index, so it reports survivors as the whole, then `persistArchivedPathsToSummary(:2133)` makes that the durable record; (4) `:2162-2166` the #700 completeness check is `cat-file -t HEAD:<archiveRel> === 'tree'`, which a partial archive still satisfies — **nothing verifies the paths became blobs.** Precisions: the token is `steps.archive_commit`, NOT `receipt.archive_commit` (the latter is `undefined`, only ever `'skipped_gitignored'`/`'failed'`) — do not guard the wrong field. "Unrecoverable" only PARTIALLY held: files survive untracked on disk but are absent from every commit, the pushed remote and a fresh clone, and `git status --porcelain` reports clean. #520 is NOT the cause (its exclusion names four `sink-receipt`/`sink-fallback` paths; the control leg proves it). Same blind spot one layer up: `classifyArchiveDisposition` (`claim.js:2592-2606`) probes the dir identically and returns `deferred_to_sink`. Live-copy disposal is real (`claim.js:2498`/`:2503`/`:2515`) gated on `verifyArchiveComplete`, a **disk-to-disk** check that knows nothing about git — and 4 of the 5 lost files are in `ARCHIVE_CACHE_SIDECAR_MD` (`claim.js:4920-4926`), skipped by that verifier at `:4988`. Closure audit does NOT catch it (`archive_content_incomplete: []` on the fixture; `archiveRequiredContent` at `closure-audit.js:131-136` requires only `workflow-state.md`, from disk, never git, never a citation). No test covers this shape — every `.gitignore` fixture writes either the whole `kaola-workflow/archive/` band (so the honest-skip arm DOES fire), an **anchored** `/.cache/` (matching only the repo root), or `kaola-workflow/` wholesale; and independently, no test asserts archived `.cache` files became git blobs (all such assertions are on-disk `existsSync`). Not established: `gap-sweep.js`/`active-folders.js`/`closure-contract.js` as detectors, `!`-negation re-inclusion, the R3 rescue path, the three forge ports.

- item: Verify #902's premise — does `finalize --check` classify `archive_authority_missing` on a normal linked-worktree topology (main-resident ledger, candidate in claimed worktree, no archive yet) that the immediate `finalize` transaction then self-heals to `mirror: mirrored`? This run's own topology is that exact shape, so it can be measured live as well as by fixture. Report both paths' classifications and where the two reason over different state.
  status: done
  dispatched: investigator subagent, read-only, output to `kaola-workflow/bundle-900-901-902-903/.cache/premise-902.md`
  result: **PREMISE HELD.** Report at `.cache/premise-902.md`; fixtures/logs in scratchpad `p902/`. Measured live on THIS run's topology and end-to-end in fixture. **The cwd is the only variable**: `--check` from the main root → exit 0, `{ok:true, mirror:"not_needed", workflow_state:"ok"}`; from the linked worktree → exit **1**, `{ok:false, mirror:"ready", workflow_state:"archive_authority_missing", reasons:["archive_authority_missing"]}`. Fixture leg A then ran the REAL finalize from the same cwd with **no operator repair between the two calls** → exit 0, `mirror:"mirrored"`, `archived:true`, `closure_invariants.ok:true`. Self-heal proven. Root resolution does NOT diverge (both share one `getRoot()` at `:3579`); the seam is **ordering plus one topology-blind probe**: execute runs `mirrorFinalizationArtifacts` (`:3615`, MUTATES — `mergeCopyDir` at `:3169`/`:3025` creates `<worktree>/kaola-workflow/<project>/workflow-state.md`) and only then resolves the authority at `:3645`, so `livePresent:true`; `--check` resolves the authority at `:3538` over the **pre-mirror** tree. The blind probe is `resolveFinalizeAuthority:3354` — `liveDir` considers **only `root`**, while `findArchiveAuthorities` (`:2986-2993`) explicitly widens to `{root, mainRoot}`. Leg C isolates it: a main-resident **archive** IS found from the worktree (`ok:true`), a main-resident **live folder** is not — so "the check has no main-root knowledge" is refuted. Leg B control (seed the worktree folder, single axis) → `ok:true`; presence of `<worktree>/kaola-workflow/<project>/` is the sole cause. **The codebase already states the rule this violates**: `:3336-3338` and `:3502-3503` say a machinery-repairable pending sync "is REPORTED as state, never as an operator-owed precondition" — `sync_required` is deliberately kept out of `reasons`, yet `archive_authority_missing`, equally repairable by the same Step-8a mirror, is pushed into `reasons` at `:3541` and drives exit 1. **Second defect from the same seam**: `--check` reported `validation:"not_checked"` while the transaction reported `chains_green` over the identical tree — the check loses the validation measurement because it is gated on having an authority (`:3566-3573`). Implementation hazard: `'ready'` is returned from three distinct situations (`:3329` dest-record absent, `:3335` compare safe, `:3347` compare threw — `compareLedgers` fails open on a null dest, `ledger-compare.js:51-53`), so `"ready"` alone cannot be read as "the mirror will CREATE the destination"; a distinguishable dest-absent signal is what the observation forces. Also `--check` carries **no `operator_hint`** at all (`:3592` vs the ladder emit at `:3650-3657`); `finalizeAuthorityHint` (`:3398-3409`) is reachable only from the transaction's refusal. `--check` confirmed read-only by three probes (porcelain, 383-entry stat listing, HEAD) all byte-identical; one stated caveat — `computeCodeTreeHash`→`snapshotWorktree` lands inert blobs in `.git/objects` via a tmpdir `GIT_INDEX_FILE` (`adaptive-schema.js:1058-1075`), never touching the repo index. Coverage: `archive_authority_missing` has **no test anywhere** (only the emit at `:3372` + forge copies); every `--check` fixture (`mk837` `test-claim-hardening.js:3180`, `mk816`, `mk941`) seeds the project folder into **both** roots so `livePresent` is always true and `:3371` is unreachable; no test invokes finalize with `cwd` = main root on a worktree topology (`runFinalize816`/`runFinalize837` hard-code `cwd: fx.wtRoot` at `:1352`/`:3274`); T2a/T2b do compare check-vs-execute but only on a **seeded** destination, pinning the agreement exactly where it holds. Not established: non-worktree reachability, `archive_authority_ambiguous` under this topology, the three forge editions (construct confirmed present at `plugins/.../claim.js:3372`, gitlab `:3181`, gitea `:3178`, unmeasured), and the blast radius of a nonzero `--check` exit across routing surfaces.

- item: Verify #900's premise — does the chain-receipt gate require a column-zero `validated_candidate_hash: <64-hex>` (claimed at `scripts/kaola-workflow-adaptive-schema.js` ~L1373-1405), and do the shipped finalize command/skill surfaces across all runtimes omit it? Establish whether any *invocable* producer exists today, or whether the only path is requiring the internal module for `computeCodeTreeHash`. Enumerate every shipped surface that states the consumer recipe.
  status: done
  dispatched: investigator subagent, read-only, output to `kaola-workflow/bundle-900-901-902-903/.cache/premise-900.md`
  result: **PREMISE HELD** — every factual claim reproduces; the issue's cited ranges are exact. Report at `.cache/premise-900.md`; scratch in `p900/`. Gate requirement (`adaptive-schema.js`, md5 `3f221dea…` identical across all four editions): file non-blank (`:1373-1380`), column-0 `verdict: pass` (`:1381-1385`), column-0 `validated_candidate_hash:` present AND well-formed (`:1390-1394`, parser `:418-425` — `^`-anchored so fence-blind, exactly 64 hex, last-match-wins, malformed fails closed identically to absent), and the recorded hash must **equal** a fresh `computeCodeTreeHash` (`:1395-1402`, else `final_validation_stale`). Reproduced: shipped recipe verbatim → `final_validation_unbound`, `green:false`; + correct hash → `chains_green`, `green:true`. Four positive controls prove the parser is armed, not vacuous: indented hash → unbound; 63 hex → unbound; 64 zeros → `final_validation_stale` with both hashes in the payload (comparison is live); and the runner's own digest ≠ the gate's. **NO invocable producer exists** — search by `git grep` (index-based, so it does not skip `.opencode`/`.kimi` the way this box's ugrep does) plus a programmatic scan of every `package.json` script. `scripts/kaola-workflow-validation-runner.js` **does exist** (1142 lines) but ships only `run` and `qualify-local`; anything else is `unknown subcommand`, exit 2, and neither verb writes `final-validation.md`. **Trap for the fix**: the runner's `computeLandableTreeDigest` (`:509-546`) is a DIFFERENT algorithm (NUL-joined, Buffer-sorted vs the schema's `\n`-join) — measured `dabe6e7b…` vs gate `38cbfebf…`, so a consumer copying the runner's `candidate_digest` earns `final_validation_stale`. `run-chains.js:1086` is the only non-test production caller of `computeCodeTreeHash`, writes it to `chain-receipt.json` never `final-validation.md`, and **refuses in a consumer repo** (`chains_config_missing`, real exit 1). The retired `plan-validator --candidate-hash` is gone. Every other caller is a test fixture reaching it by `require()` — **the suite is itself a standing user of #900's workaround**, which is exactly why it stays green while the shipped recipe is unusable. **Surfaces: 10 state the recipe, 0 mention the field** — `templates/routing/finalize.skeleton.md:121-124` is the single AUTHORING point; surfaces 2-7 are generated from it (`generate-routing-surfaces.js --check` → all 18 byte-match, exit 0); 8-9 are the opencode/kimi edition copies (outside the 18, synced by `sync-opencode-edition.js`/`sync-kimi-edition.js`); 10 is `README.md:957-960`. So the fix is **one skeleton edit + regenerate + two edition syncs + README**. 90 further agent/role surfaces swept (`agents/*.md`, all three plugins' `agents/*.toml` + `config/agents.toml` — the known-missed surface, `.opencode/agent/*.md`, `.kimi/skills/*`): 0 hits, i.e. silent on the whole mechanism rather than stating an incomplete recipe. Only `docs/api.md:447-452`, `docs/architecture.md:133-135` and `docs/conventions.md:412-427` name the field — and **the consumer never reads `docs/`**. Two framing corrections: (1) `docs/conventions.md:424-427` **already admits this hole in prose** ("no shipped command that prints the value it is asked to record") — #900 is a documented gap nobody routed around, not a discovery; (2) the classification does **not** block finalize — `claim.js:3743-3751` records and proceeds to archive, so the cost is a durable `green:false` + an unactionable hint, consistent with *nothing refuses*. **Sharpest constraint on any fix**: the binding follows the **cwd**, not the run folder — `resolveFinalizeCheckRoot` (`adaptive-schema.js:1022-1046`) is `process.cwd()`-driven and the gate re-resolves to that tree's top level (`:1309-1310`). Measured divergence: main and worktree hashes agree only while the branch has nothing main lacks (leg 1 identical; leg 2, one branch commit un-merged, `45f93f4e…` vs `bd5edd13…` differ; leg 3 after merge identical) — i.e. they differ across **exactly the pre-merge window in which finalize runs**. Live pair identical today (`f96f0ac4…` both) only by timing coincidence. The agent self-corrected a first fixture that omitted `.gitignore`, letting the nested worktree enter main's snapshot as a gitlink. Latent hazard: the issue's 2-arg workaround equals the 3-arg gate call **only because `VALIDATION_TEST_CONSUMES === []` today** (pinned at `test-finalize-door.js:870`). Coverage: **no contract test asserts any shipped surface mentions the field**; `validate-workflow-contracts.js:512-513` pins exactly `final-validation.md` + `verdict: pass` and is silent on the third requirement — **green while the recipe is wrong**, the "a threshold cannot see a rule beneath its bar" corollary; `test-finalize-door.js:859-916` proves producer==gate for the **self-host** arm only, because there is no consumer producer to test. Not established: end-to-end `cmdFinalize` on a consumer repo (gate reproduced in-process via the identical `claim.js:3444` call; persistence read, not measured), and whether any real consumer besides #900 hit this.

- item: Verify #903's premise — does `kaola-workflow-closure-audit.js` `parseArgs` recognize only `--execute`, silently ignoring `--help` and unknown flags, with no `--project`/`--issue` scoping and no per-project verdict? Confirm the drift classes it computes and which are project-attributable, and confirm the finalize surfaces invoke it unscoped as the terminal check.
  status: done
  dispatched: investigator subagent, read-only, output to `kaola-workflow/bundle-900-901-902-903/.cache/premise-903.md`
  result: **PREMISE HELD.** Report at `.cache/premise-903.md`; captures in `p903/`. `parseArgs` (`closure-audit.js:56-62`) is a 5-line loop whose only token is `--execute`, with **no `else`** — measured: bare vs `--help` vs `--bogus-flag-xyz` vs `--project X --issue 903` all exit **0** with **byte-identical stdout** (`diff` clean) and empty stderr. So an operator who believes they scoped the audit gets an unscoped answer that looks authoritative. **There is no verdict channel at all**: exit is 0 whether drift is zero or six classes (`:370-372` sets exit 1 only on a thrown exception). Bare invocation confirmed **read-only** first (every read path traced; `regenerateRoadmap` reachable only from `executeRepairs:306`; both imported modules guarded by `require.main === module`) — the known bare-roadmap-mutation gotcha does not apply. Live run: exit 0, two findings, **neither belonging to this bundle** — `stale_in_progress_labels` #796 (closed+archived 2026-07-25) and `archive_content_incomplete` `bundle-429-434`; establishing that took two manual greps, exactly the manual step the issue describes. Contamination survives from the worktree (byte-identical) and under `KAOLA_WORKFLOW_OFFLINE=1`. **Seven drift classes; none is repository-global in its emitted shape** — five carry a `project` string or issue number, so `--project` scoping is coherent for every class, with two qualifications: (c) `stale_in_progress_labels` detection is one repo-wide forge query that cannot be narrowed remotely, but the scoped question is a set-membership test on the returned array needing no extra calls; (g) `archive_content_incomplete` is attributable **by folder name only** — the sole missing artifact it can report IS `workflow-state.md`, the identity anchor, so **no issue number is recoverable for exactly the class that contaminated the run**, and archive names come in three shapes here (358 bare, 8 `.archived-<ts>`, 2 `.discarded-<ts>`) so a scope match must cover `P` and `P.archived-*`. The live finding is a worked ambiguity: `archive/bundle-429-434/` is residue holding one **tracked** file (`.cache/sink-receipt.json`) while the real archive is its `.archived-*` sibling — so this repo will carry ≥1 irrelevant finding forever, and it is unrepairable by construction (`:340-342`). A scoped `--execute --project P` needs **no change to any repair's semantics** (two of three repairs are per-item loops; `regenerateRoadmap:306` stays a whole-mirror idempotent rebuild, inherent to the mirror being one file). **Two findings beyond what #903 claims, both measured with positive controls: (1)** the audit already receives `issue_numbers` on every folder from `readActiveFolders` (`active-folders.js:267-268`) and **discards it** — `:257` and `archiveClosedIssues:109` read only the scalar `issue_number`, so a bundle's non-primary members are invisible to two drift classes (leg 4: sources planted for 500 AND 501, only 500 flagged, with 500 as the positive control; legs 5A/5B differ in exactly one fixture line). For THIS run only #900 is ever probed. **(2)** `unresolved_closed_state` is absent from the `--execute` output entirely (`:354-359`). Framing correction: **no document asserts repository-wide scope as a *designed* property of #165** — `D-497-01.md:18-19` calls it "not scoped to any one failure scenario", which is about failure scenarios, not projects; the absence of `--project` is recorded as a CLI fact (`D-497-01.md:113`, `docs/api.md:856-859`), never argued for. Counts differ from the issue (1 `archive_content_incomplete` here, not 6; `phase-37-8-bear-scale-dampening` does not exist in this tree) — the mechanism reproduces, the specific examples are the consumer's. **`--help` is an existing repo convention, not an invention**: `kaola-workflow-classifier.js` implements it and the walkthrough pins it at `:5803-5814`. Coverage: 23 test functions, all in the walkthrough; **no test passes an unknown flag or `--help`, and none asserts an exact key set** on `drift`/`counts` — so adding rejection, a usage string, or a key reds nothing. The guards that WILL bite a fix are the prose ones: `test-route-reachability.js:163-184` (T6) and `test-opencode-edition.js:804-810` (A16) require the `<!-- PIN: closure-audit -->` marker on all finalize surfaces, `required-blocks.js:262-274` requires two interior sentences verbatim, and `validate-script-sync.js:49,489` holds the two forge ports byte-identical with an exports-superset check. Not established: where the issue's six findings came from (git history unsearched), whether the audit is *mandatory* (prose-only obligation, exit always 0), the forge ports' detector bodies line-by-line, and whether any external consumer parses its stdout.

- item: Reconcile the two reports' disagreement on the propagation surface count — #900 enumerated 9 recipe surfaces (`.opencode`/`.kimi` only) while #903 enumerated 12 invocation sites across SIX dot-dir editions. Settle it by measurement, because "one rule, one wording" fails silently if a runtime is missed.
  status: done
  dispatched: self
  result: **#900 UNDERCOUNTED by 4; #903's sweep was right about the edition set.** Six dot-dir editions exist: `.opencode`, `.opencode-gitea`, `.opencode-gitlab`, `.kimi`, `.kimi-gitea`, `.kimi-gitlab`. Measured by grepping the recipe's own wording (`You own verification`) and `validated_candidate_hash` across every candidate: **13 surfaces state the recipe, all 13 mention the field zero times** — 1 authoring skeleton (`templates/routing/finalize.skeleton.md`), 6 tracked rendered (`commands/` + 3 plugins' `skills/`), and **6 dot-dir edition surfaces** (`.opencode*/command/kaola-workflow-finalize.md` ×3, `.kimi*/skills/kaola-workflow-finalize/SKILL.md` ×3). `README.md:958` is a 14th surface stating it in different wording ("consumer repos record their own `verdict: pass`"), which is why a `You own verification` grep misses it — a needle keyed to one phrasing undercounts. **Both reports' propagation claims are true and complementary**: `generate-routing-surfaces.js --check` covers **18 tracked** surfaces (measured: "all 18 surfaces byte-match the skeleton", exit 0) and the 6 dot-dir surfaces render from the *same* registry via `commandSurfacesForForge` (`:143-155`) but are WRITTEN by `sync-opencode-edition.js` / `sync-kimi-edition.js`, outside the 18. **So the propagation sequence is: edit the authoring point → `node scripts/generate-routing-surfaces.js` (18) → `node scripts/sync-opencode-edition.js` + `node scripts/sync-kimi-edition.js` (6) → `README.md` by hand.** Note the two authoring points differ per issue: #900's recipe lives in `finalize.skeleton.md:121-124`, #903's audit invocation in `templates/routing/slots.js:126` (slot `fz-closure-audit-run`) — so those two edits do NOT collide, but they share one regeneration.

- item: Decide the shape of each fix from the four premise reports — for every premise that HELD, what the minimal additive change is; for every premise that COLLAPSED, whether the answer is wording, a doc fix, or closing the issue as already-correct. Record the ruling per issue before any production edit. **Known write-set collision, found while closing #901: its acceptance item "closure audit detects a summary citation to a missing required `.cache` artifact" edits `scripts/kaola-workflow-closure-audit.js`, the same file #903 rescopes — so #901 and #903 cannot go to independent implementers. Also `#900` and `#903` both touch finalize surfaces rendered from `templates/routing/`.** On #901's fix shape: the issue's own Expected behavior authorizes force-adding the allowlisted evidence under the current project's archive (option 1) with the truthful-skip report as the fallback (option 5) — that is the owner's written ruling, so it is not re-opened here; the blob-verified `archive_commit` verdict is pure correctness and ships regardless.
  status: todo

- item: **RULING (all four premises HELD; nothing to close as already-correct).** Decompose by **file group, not by issue** — the four issues are one coordinated change over shared files. Group A = `sink-merge.js` + `claim.js` archive/disposal (#901). Group A2 = `claim.js` finalize-check (#902) — runs AFTER A, same file, no concurrent owner. Group B = `closure-audit.js` + its two forge ports (#903 + #901's citation detector). Group C = `validation-runner.js` (#900's recorder). Group D = prose/propagation, AFTER B+C+A2 settle their CLI shapes. Per-issue shape: **#900** — add a `record` subcommand to the EXISTING `validation-runner.js` (reuse, don't add a script); it must resolve the candidate root cwd-driven exactly as the gate does, hash via the canonical `computeCodeTreeHash` and **never** the runner's own `computeLandableTreeDigest` (measured different value → `final_validation_stale`), let the gate's own default band apply so the latent `VALIDATION_TEST_CONSUMES` hazard cannot bite, write verdict+command+hash atomically at column zero, print root/project/hash machine-readably, and report ambiguity rather than bind the wrong checkout; then extend `validate-workflow-contracts.js:512-513` to require the field (today it pins exactly the two things the incomplete recipe states — green while the recipe is wrong) and make the `final_validation_unbound` hint name the command. **#901** — the issue's Expected behavior authorizes force-add, so: fix the `:2111` probe to ask about required FILES not the directory; `git add -f` the allowlisted evidence under the current project's archive only, keeping #520's four-path journal exclusion; stop discarding the add failure (`catch (_) {}` at `:2121`/`:2135`); gate `steps.archive_commit: "done"` on **blob presence in the published commit** rather than `cat-file -t HEAD:<dir> === 'tree'`; on decline/failure emit the typed skipped/incomplete result and RETAIN the source; and fix the same dir-probe blind spot in `classifyArchiveDisposition` (`claim.js:2592-2606`) plus `verifyArchiveComplete`'s git-blindness (it skips 4 of the 5 lost files via `ARCHIVE_CACHE_SIDECAR_MD`). **#902** — apply the rule the code ALREADY states at `:3336-3338`/`:3502-3503`: make `probeFinalizeMirror` distinguish the dest-absent case (today `'ready'` conflates three situations), feed that prediction into `evaluateFinalizePreconditions` so a will-be-created authority is reported as **state** not pushed into `reasons` at `:3541`, keep `archive_authority_missing` for what execution cannot repair, and expose main/linked root + authority source + intended destination. **Do NOT widen the live probe to main** — that would make `--check` name main as the authority while execute uses the worktree, breaking the "check predicts what execute reaches" criterion. **#903** — add `--project`/`--issue` scoping, `--help`, and rejection of unknown flags; add the scoped verdict (`scope`, `current_project_clean`, `current_project_drift`, `repository_drift_outside_scope`) while leaving unscoped output byte-unchanged; resolve bundle members from `issue_numbers`; and fix the measured latent bug that `:257`/`archiveClosedIssues:109` read only the scalar `issue_number` so bundle members are invisible to two drift classes. No new rule is invented anywhere: `--help` follows the classifier's existing convention, and #902 applies an existing documented rule to a rung that was missed.
  status: done
  dispatched: self
  result: this item IS the ruling; the four groups below carry it out.

- item: Group A — #901: fix the sink's archive-evidence loss in `scripts/kaola-workflow-sink-merge.js` and the two matching blind spots in `scripts/kaola-workflow-claim.js`, per the ruling above. Implementer owns both files exclusively; A2 must not start until this closes.
  status: done
  dispatched: implementer subagent `impl-901`, working IN the worktree `/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-900-901-902-903` on branch `workflow/bundle-900-901-902-903`; owns `scripts/kaola-workflow-sink-merge.js` + `scripts/kaola-workflow-claim.js` exclusively; notes to land at `kaola-workflow/bundle-900-901-902-903/.cache/impl-901.md` in the MAIN root
  result: **DONE for the canonical + codex copies; the two FORGE PORTS still carry the defect (separate item below).** Notes at `.cache/impl-901.md`. 4 files, 448+/24−: `scripts/kaola-workflow-sink-merge.js`, `scripts/kaola-workflow-claim.js` + both `plugins/kaola-workflow/scripts/` copies (`cmp` exit 0 — both files ARE under byte-lockstep per `validate-script-sync.js:46,51`). New claim.js helpers deliberately **unexported**: those files are also in `FORGE_EXPORT_SUPERSET_FAMILY:485-486`, so a new canonical export would fail that check while the forge ports were outside the write set. **Proof by driving the identical fixture through the shipped sink and the fixed one** (single axis = the script path): IGNORED leg (`.gitignore = .cache/`) went from 5 `.cache` files missing as blobs / missing from a fresh clone / missing from `archived_paths` → **0/0/0**, and the archive commit from `3 files changed` to `8`; same on the sole-archiver posture with the #700 guard live; `node_modules/` control unchanged (0 before and after); and the #832 whole-band leg still decides `skipped_gitignored` at exit 0 with the archive on disk — identical decision, now itemizing the uncommitted files in `archive_missing_paths`. **Both new gates mutation-proven, because a green happy path proves nothing here**: (a) blob gate — same fixture with one required file `chmod 000`: shipped → exit 0 `"ok"`/`"sinked"`/`archive_commit:"done"`; fixed → exit 1 with `refuse`/`sink_incomplete`, all 5 paths named, and `archive_add_errors` carrying the exact `fatal: adding files failed` that `catch (_) {}` used to eat, branch retained (`rev-parse --verify` exit 0); (b) disposal gate — two scratch mirrors with an identically doctored `copyDir` and **no repo file mutated**: shipped deletes the live copy and the dropped sidecar exists nowhere, fixed returns `archive_incomplete` naming it and retains the source, with an inert-mutation negative control showing no false refusal. Suites, real exit codes, before and after: sink-merge **0** (298 assertions both), finalize-door **0**, claim-hardening **0**, bundle-finalize **0**, plus **full-scope walkthrough 0 (184/184, not a shard)**. Caveat it stated itself: the "after" runs include siblings' uncommitted edits, so the isolation evidence is the before/after driver work rather than the suite. `validate-script-sync.js` exits 1 solely on a **sibling's** `closure-audit.js` drift; neither of its files appears. **Two of my briefed criteria were wrong and it refused them with better evidence — both refusals ACCEPTED. (1)** It did not change `classifyArchiveDisposition`'s token: after the sink fix the sink genuinely *can* commit that subtree, so `deferred_to_sink` became **correct**, and changing it would put finalize and the sink into disagreement about the same `.gitignore`; a third token would also have broken a value pinned at `simulate-workflow-walkthrough.js:4818-4819` and documented in `docs/workflow-state-contract.md:135`. The real defect there was **silence**, which it fixed instead (`finalizeTx.archive_ignored_evidence` + a NOTE naming the covered files). **(2)** My criterion 6b was structurally unsatisfiable as worded: at the disposal decision **there is no commit yet, by design** (`claim.js:2480-2482`), so a git-presence gate there would refuse *every* finalize. It built the nearest load-bearing version — the delete is blocked when the destination is missing a file the source holds, **including the four sidecars `verifyArchiveComplete` exempts**, which was the actual gap — and the committed-archive half is answered by the sink's new blob gate, which runs before the sink disposes the worktree. Also measured: git's "paths are ignored" advice **still prints twice** because it complains about the ignored *directory* entry, which persists even once its files are in the index; not suppressed, because `-c advice.addIgnoredFile=false` also flips the exit to 0 on this git and the fix must not depend on that coupling. Five tests spec'd with working fixtures, none authored (custody honored) — the load-bearing one being **assert on `git ls-tree`, not `fs.existsSync`**, which no test in the corpus does and which is why #901 survived its entire life.

- item: The gitlab and gitea forge ports of the SINK still carry the #901 defect — they are hand-ports held only by export-superset, not byte-lockstep, so the canonical fix does not reach them, and #901's acceptance explicitly demands coverage across canonical/Codex, GitLab and Gitea. `check-ignore` is forge-neutral so identical behaviour is expected but was NOT measured. Port the sink fix to `plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-sink-merge.js` and the gitea equivalent, respecting each port's naming and the documented forge divergences.
  status: done
  dispatched: implementer subagent `impl-901-ports`, in the worktree; owns `plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-sink-merge.js` + `plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-sink-merge.js` ONLY (deliberately excludes every `*-claim.js` port, which a later item owns); notes to `kaola-workflow/bundle-900-901-902-903/.cache/impl-901-ports.md`
  result: **BOTH PORTS HAD THE DEFECT — measured, not assumed — and both are now fixed with the blob gate mutation-proven armed.** 151 insertions each, uncommitted, no export added. BEFORE in both ports with `.gitignore=".cache/"`: exit 0, `status:"sinked"`, `steps.archive_commit:"done"`, `archived_paths` = 3 of 8, and **5 `.cache` files missing as blobs at HEAD, from the pushed remote, AND from a fresh clone** — on both archiver postures. AFTER: **0 missing**, archive commit 3 files → 8, `archive_forced_paths` naming the five plus a stderr NOTE. `node_modules/` control unchanged. The #832 whole-band leg is preserved **byte-for-byte** in its decision (`skipped_gitignored`, exit 0, no force-add) with only its receipt now itemizing the 8 uncommitted files. Armed proof per port (one required file `chmod 000`, axis verified by `cat` exit 1): shipped port exits **0** reporting `sinked`/`done`; fixed port exits **1** with `refuse`/`sink_incomplete`/`step:archive_commit`, all 5 paths named, `steps.archive_commit` NOT done, `archive_add_errors` carrying both previously-swallowed statuses, and **branch + on-disk archive retained** (`rev-parse` exit 0, vs 128 on torn-down happy legs). #520 held — no journal tracked after the forced run. **Zero new cross-port divergence introduced**: the two ports' `archive_commit` regions were byte-identical to each other before the edit and still are, and the port-to-port normalized diff is 193 lines both before and after. No function the canonical fix modified was missing from either port; adaptations were naming only (`ps` for `projectPathspec`, `exRcpt`/`exFb`/`exLiveRcpt`/`exLiveFb` for the #520 excludes, `args` not `rawArgs`). The three new fields are forge-neutral, so the MR/PR substitutions at `docs/api.md:886-890` do not apply. Suites before→after: `test-gitlab-sinks` 0→0, `test-gitea-sinks` 0→0, `simulate-gitlab-workflow-walkthrough` 0→0, `simulate-gitea-workflow-walkthrough` 0→0; after only: both codex forge walkthroughs 0, both contracts validators 0, `edition-sync --check` 0, `test-spawn-classification` 0, `validate-script-sync` 0, and the canonical **full-scope walkthrough 0 (184/184, 1958 spawns)**. It stated its own isolation caveat: the worktree carries siblings' uncommitted edits so the "after" column is green including theirs; the isolation evidence is the before/after drives where the sink script path is the only axis. **Two structural gotchas for whoever writes the tests, both load-bearing:** the forge suites need the **stateful** CLI mock or the post-close probe mis-buckets the close, and **`KAOLA_WORKFLOW_OFFLINE` must be set to `'0'` EXPLICITLY or the durability half of the test is silently disabled** — the exact trap that nearly shipped a false verdict in a prior run. Working fixtures for all four legs × both postures × both ports are in scratch `impl901ports/`. Five pins are needed **per forge edition**, since each edition's suite defends its own copy and a canonical pin does not cover the ports.

- item: Port the #901 claim.js changes AND #902's fix to the gitlab and gitea `*-workflow-claim.js` ports together — they are the same two files, so one owner does both to avoid a collision. Must run AFTER Group A2 lands the canonical #902 fix. #902's construct is present at gitlab `:3181` and gitea `:3178`, unmeasured. `impl-901-ports` confirms the claim-side half of #901 (the dir-probe NOTE and the disposal gate) is also still unported and unmeasured there.
  status: done
  dispatched: implementer subagent `impl-claim-ports`, in the worktree; owns `plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js` + `plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js` ONLY; carries BOTH #901's claim-side half and #902's fix in one pass; notes to `kaola-workflow/bundle-900-901-902-903/.cache/impl-claim-ports.md`
  result: **DONE — all six defects (2 ports × 3 fixes) MEASURED, not assumed, all fixed, all guards armed.** 163 insertions / 25 deletions per file, matching the canonical numstat exactly; uncommitted; no new exports; `validate-script-sync.js` 0 before and after. Per port × fix: **#902** — check exit 1 `archive_authority_missing` while execute exited 0 `archived:true`; now leg A is exit 0 `pending_mirror` and agrees with execute, with M1 flipping leg C to exit 0 and M2 regressing legs A/E to exit 1. **#901 NOTE** — was `deferred_to_sink` with 8 ignored evidence files, `archive_ignored_evidence` undefined and no NOTE; now 6 files named (journals subtracted, armed by count: raw `ls-files -o -i` = 8, reported = 6), with a `node_modules/` control reporting nothing and the #832 BAND leg still `skipped_gitignored` with the NOTE correctly not firing. **#901 disposal** — was `archived:true` with the live source DELETED and the dropped sidecar existing nowhere; now `archive_incomplete:true`, `missing:[".cache/final-validation.md"]`, live copy retained. **Zero new cross-port divergence, proven two ways**: whole-file normalized diff is **761 lines before and 761 after**, and the divergence *content* (all +/− bodies) is **byte-identical before vs after** (`diff` exit 0 over 286 lines); stronger still, the **added lines of all three diffs — canonical, gitlab, gitea — are byte-identical** after normalizing only the claim-script name in the stderr NOTE prefix. What remains is pre-existing forge vocabulary only (notes-vs-comments API, `project_id`/`path_with_namespace` vs `full_name`, `mr_*` vs `pr_*`, `glab` vs `tea`). Suites, real exit codes, serial: `test-gitlab-sinks` 0→0, `test-gitea-sinks` 0→0, both forge walkthroughs 0→0, **canonical full-scope walkthrough 0→0 (184/184)**, `validate-script-sync` 0→0, plus seven it added on its own judgement because both fixes change an edition's emitted envelope (both forge contract validators, both forge **codex** walkthroughs, both `test-*-workflow-scripts.js`, `test-active-folders-field-parity.js`) — all 0. **Three findings worth carrying: (1) Leg C is load-bearing per edition** — M1 leaves leg A at exit 0 and only the fail-closed negative detects a blanket suppression, on both ports; any #902 test arm omitting it passes against a suppression. **(2)** It measured the #901 finalize NOTE **end-to-end**, which the canonical implementer had recorded as *inferred, not measured* — it works. **(3) It caught its own FAKE negative control**: `KW901_DROP_SIDECAR=` (empty) hit its driver's `|| 'final-validation.md'` default, so the "control" was silently the positive leg and appeared to refuse; fixed with an explicit `KW901_INERT=1` that deletes the var from the child env, and the reported table is from the corrected run. Its own lesson, worth keeping: **a control that agrees with the positive leg is the signal to check the control.** Untouched as instructed: canonical + github-plugin `claim.js`, both sink-merge ports, both ports' dead retired-Node-Ledger `archiveRequiredContent` code, and every sibling's file. Test custody respected; 8 per-edition arms listed for the tdd agents.

- item: Settle the design call two implementers deferred — whether to add a `KAOLA_WORKFLOW_FORCE_*` env seam to production code so an in-repo suite can make `copyDir` lossy and thereby exercise the #901 disposal gate.
  status: done
  dispatched: self — my call to make
  result: **RULED: NO seam in production code.** Nothing observed demands it — the seam would exist solely so a test can inject a fault, which is test machinery leaking into shipped code, against both "add only what an observed failure demands" and the standing "there is already too much in this project." The disposal gate compares the **source tree against the destination tree**, so a test can construct that asymmetry **directly on disk** — build a destination archive already missing a file the source holds — without needing `copyDir` to fail at all. Both implementers proved the gate this way already, using a doctored `copyDir` in a scratch mirror; the in-repo pin needs only the end state, not the failure path that produced it. Communicated to `tdd-finalize`, with instructions to report back rather than add a seam if it concludes the on-disk construction is genuinely impossible.

- item: Group A2 — #902: converge `finalize --check` with the execute path in `scripts/kaola-workflow-claim.js`, per the ruling. Starts only after Group A closes (same file, single owner at a time).
  status: done
  dispatched: implementer subagent `impl-902`, in the worktree on `workflow/bundle-900-901-902-903`; owns `scripts/kaola-workflow-claim.js` + `plugins/kaola-workflow/scripts/kaola-workflow-claim.js` (byte-lockstep pair) exclusively, now released by `impl-901`; notes to `kaola-workflow/bundle-900-901-902-903/.cache/impl-902.md`
  result: **DONE**, uncommitted, byte-identical pair (`cmp` 0), diff confined to `:3315-3717`, #901's work untouched, no new export so `FORGE_EXPORT_SUPERSET_FAMILY` is unaffected. Four changes: (1) `probeFinalizeMirror:3372` gains an internal `destAbsent` bit on every return (`!existsSync(destDir)` hoisted to `:3380`), branch order unchanged, still zero-write — **`'ready' + destAbsent` is the only signal meaning "the mirror will CREATE the destination"**; (2) new unexported `predictFinalizeAuthority:3485`, fires **only** when ALL of: inner reason is exactly `archive_authority_missing`, linked worktree, `mirror.state==='ready'`, `destAbsent`, and the main source is live — and **the authority is not relocated to main**, `dest_dir` stays the tree execution reads; (3) `evaluateFinalizePreconditions:3653` sets `checks.workflow_state='pending_mirror'` and pushes **nothing** into `reasons`; (4) `cmdFinalize:3712` emits a new `authority` block — `main_root`, `linked_root`, `source` (`live|archive|pending_mirror|none`), `source_dir`, `dest_dir`. `checks.mirror`'s vocabulary is unchanged; no new severity field and no `operator_hint` — the existing "state in `checks`, never a token in `reasons`" mechanism carries it. **Live, cwd the only variable**: worktree exit **1** `archive_authority_missing`/`validation:not_checked` → exit **0** `pending_mirror`/`validation:chains_unverified`; main root unchanged `ok:true`; **the two cwds now agree on validation too**. Read-only re-proven (8286-entry stat listing + both HEADs byte-identical). With `--keep-worktree` the live check exits 1 on `implementation_commit_missing`, which is **correct** — `git diff main...HEAD` shows 0 committed non-`kaola-workflow/` paths because this bundle's work is genuinely still uncommitted; `archive_authority_missing` is gone. **Five fixture legs, each `--check` then the real execute from the same cwd**: A (#902 topology) check exit 0 `pending_mirror`/`chains_green` → execute `archived:true`/`chains_green`/`closure_invariants.ok` — **agree**; B (seeded control) `workflow_state:"ok"`, `source:live`, unchanged; **C (mandatory negative — nothing anywhere) check exit 1 `archive_authority_missing` → execute exit 1 same token, fails closed**; D (two archives) `archive_authority_ambiguous` both sides; E (main source lacking `workflow-state.md`) check predicts `state_missing` → execute `state_missing` — a **bonus fix**, since before this the check said `archive_authority_missing` while execute said `state_missing`. **Two mutation proofs in a scratch mirror of `scripts/` (never revert-in-place): M1** — a blanket suppression flips leg C to exit 0 / `reasons:[]` while leg A is unaffected, so **leg C is the only leg that distinguishes this fix from a suppression; leg A alone cannot**; **M2** — forcing `destAbsent=false` regresses legs A and E back to exit 1 + `archive_authority_missing` + `validation:not_checked` while leg C is unchanged, so the bit is load-bearing and the guard is armed. `checks.validation` recovered from `not_checked` to a real measurement matching the transaction over the identical tree, and `checks.changed_paths` recovered with it (`[]` → `["impl.txt"]`), with no new plumbing. Suites before→after, real exit codes, serial: claim-hardening 0→0, finalize-door 0→0, bundle-finalize 0→0, **full-scope walkthrough 0→0 (184/184)**, `validate-script-sync` 0→0. **Correction to my brief: `validate-script-sync.js` exits 0, not 1** — the sibling `closure-audit.js` drift had already converged before its baseline. Docs debt it identified for Group D: `docs/api.md:219-222` lists the `--check` envelope keys and is now **stale** (no test pins it so nothing is red, but it is test-consumed so editing it re-stales a receipt), and `templates/routing/finalize.skeleton.md:349` says "clear everything it lists" which should read "everything in `reasons`". Unverified and stated: the three other editions were not measured; a symlinked `workflow-state.md` in the main source could yield `state_invalid_type` where execute yields `state_missing` (both fail closed at exit 1, and the pre-existing behaviour agreed with neither); blast radius beyond `--check`'s prose consumers.

- item: Group B — #903 + #901's citation detector: scope `scripts/kaola-workflow-closure-audit.js` and keep its two forge ports byte-identical (`validate-script-sync.js:49,489` enforces lockstep + exports superset), per the ruling.
  status: done
  dispatched: implementer subagent `impl-903`, working IN the worktree on branch `workflow/bundle-900-901-902-903`; owns `scripts/kaola-workflow-closure-audit.js` + `plugins/kaola-workflow{,-gitlab,-gitea}/scripts/*closure-audit.js` exclusively; notes to land at `kaola-workflow/bundle-900-901-902-903/.cache/impl-903.md` in the MAIN root
  result: **DONE in all four copies**, uncommitted. Notes at `.cache/impl-903.md`. **CLI contract (authority for Group D):** bare and `--execute` are UNCHANGED repo-wide; `--project <name>` (single value, not repeatable), `--issue <N>` (repeatable, positive int that round-trips), `--help`/`-h`. Any scoping flag switches to the scoped envelope. **Exit 0 for every successful run including one that found drift** (no verdict in the exit code — deliberate) and for `--help`; **exit 1 only for operator-input error** (unknown flag, missing/malformed value, or a `--project` resolving to no `workflow-state.md` with no `--issue`), and on exit 1 **stdout is empty**. Scoped dry-run keys: `dry_run`, `offline`, `scope{project, issue_numbers, state_file, archive_name_ambiguous?}`, `current_project_clean`, `current_project_drift`, `current_project_counts`, `repository_drift_outside_scope`, `repository_counts_outside_scope`; scoped `--execute` swaps the two `current_project_*` for `repaired`/`reported_not_repaired`. **Four caller facts:** (1) `current_project_clean` is **fail-closed** — `true` only if every scoped class actually evaluated, so an offline scoped run is never `true`; do not read `false` as "drift found" without the counts. (2) A skipped class appears verbatim in **both** halves. (3) Scoped archive findings carry `attribution: "name_match"|"ambiguous_name_match"`; unscoped ones are not annotated. (4) Scoped `--execute` still rebuilds `ROADMAP.md` whole — inherent. Verification: `validate-script-sync.js` **0** with the codex copy byte-identical; **full-scope walkthrough 0 on an ISOLATED tree** (scratch clone at `9b68b096` + only its 4 files — chosen because siblings were concurrently editing `claim.js`/`sink-merge.js`/`validation-runner.js`, so a red here would not have been attributable), 184/184 with all 23 closure-audit tests passing, and 0 again on the integrated worktree; `test-route-reachability.js` 0, `test-opencode-edition.js` 0, `generate-routing-surfaces.js --check` 0. **Byte-identical unscoped output proven** across all four modes (dry-run/`--execute` × offline/online) on a tree with no bundle-member or citation drift; on the live repo the only delta is the new omit-when-empty class. Live scoping proof: `--project bundle-900-901-902-903` → `current_project_clean: true` with all four members resolved from `issue_numbers`, while #796, `bundle-429-434` and the 4 citation findings all land in `repository_drift_outside_scope`. Member fix proven in **both** halves with positive controls plus a negative control (`closure_policy: partial` → member not flagged); the cross-copy parity harness is **mutation-proven armed** (breaking gitea's name matcher reds gitea only). **Escalated two calls (see the decision item below).** Also recorded, not fixed: both forge ports' `archiveRequiredContent` still carries the **retired Node-Ledger mechanism** — `plan_hash` probing, a `workflow-plan.md` demand, and a lazy require of `listRecordedNodeEvidence` which no longer exists anywhere, silently inert. The canonical copy deleted all of it. It is dead code rather than a live divergence, and it is **not** among the deliberate divergences `docs/api.md:886-890` records. Left alone as a subtraction outside the brief — it wants its own issue. No tests authored (custody honored); §8 of the notes ranks 11 needed tests with mutation controls each, and warns that both existing runners (`runClosureAudit:1746`, `runClosureAuditOffline:1768`) assert `status === 0` unconditionally and `JSON.parse` stdout, so the exit-1 and `--help` cases need a direct `spawnSync` — nine functions were newly exported for exactly that. Unverified: neither forge port's scoped `main()` ran end-to-end (no `glab`/`tea` on this box), and `--execute` was never run against the live repo.

- item: Group C — #900: add the `record` subcommand to `scripts/kaola-workflow-validation-runner.js` and name it in the `final_validation_unbound` hint, per the ruling. Note `kaola-workflow-adaptive-schema.js` is byte-identical across all four editions — any edit there must land in all four copies identically.
  status: done
  dispatched: implementer subagent `impl-900`, working IN the worktree on branch `workflow/bundle-900-901-902-903`; owns `scripts/kaola-workflow-validation-runner.js` (+ the four `kaola-workflow-adaptive-schema.js` copies IF the hint text must move) exclusively; notes to land at `kaola-workflow/bundle-900-901-902-903/.cache/impl-900.md` in the MAIN root
  result: **DONE.** Notes at `.cache/impl-900.md`. 8 files changed, uncommitted: `scripts/kaola-workflow-validation-runner.js` (+181) and `scripts/kaola-workflow-adaptive-schema.js` (1 line, `:1206`), each byte-propagated to its three plugin copies (runner md5 `96bee64e…`, kernel md5 `54e0791e…`, all four identical). **CLI contract (render this in Group D):** `record --project <run-folder> --verdict pass|fail --command "<exact cmd>" [--output <path>]`; `--project` one path segment (rejects `/ \ NUL . ..`), `--command` non-empty single-line NUL-free. Output is canonical sorted one-line JSON: `kind:"final_validation_record"`, `schema_version:1`, `outcome:"recorded"|"inconclusive"`, `project`, `verdict`, `validation_command`, `candidate_root`, `validated_candidate_hash`, `record_path`, `other_candidate_roots[]`, `operator_hint`, plus `reasons[]` only when inconclusive (`candidate_root_unresolved`/`project_folder_missing`/`candidate_hash_unresolved`). **Exit 0 = the record was WRITTEN, not that tests passed — read `verdict`**; 1 = inconclusive, nothing bound; 2 = usage error. Writes three column-0 lines (`od -c` proven) to `kaola-workflow/<project>/.cache/final-validation.md`; no `findings_blocking` since the gate never reads it. **Existing-file policy is MERGE, never clobber**: it owns those three field names at column 0 anywhere in the file, strips them wherever they sat and appends one fresh block — byte-idempotent (`cmp` exit 0) on a bare record and over pre-existing prose, and a stale `verdict: fail` + 64-zero hash was replaced rather than left below the new block where last-match-wins would have picked it. **Acceptance proven** via the identical `claim.js:3438-3444` gate call on a no-`package.json` consumer fixture: recipe verbatim → `final_validation_unbound`; after `record` → `chains_green`, `green:true`, hash byte-equal to what `record` printed; all four shipped copies produce the identical binding. **Criterion 3 (linked worktree) proven decisively**: `.kw/` gitignored, real `git worktree add`, one un-merged commit → main `d4451c69…` ≠ worktree `2710b69d…`; `record` from the worktree binds `2710b69d…` and the gate reads green there, while **the same file read with the gate standing in main returns `final_validation_stale` with `current_candidate_hash: d4451c69…`** — so the binding is provably the worktree's, not main's. Recording from main while the folder lives only in the worktree returns `project_folder_missing`, exit 1 — it refuses to bind the wrong checkout rather than hashing main. Folder in both trees still binds the worktree and reports `other_candidate_roots`. **Mutation-proved its own acceptance test in a scratch mirror** (`git archive HEAD` + only its 8 files; the live worktree never mutated — the correct technique, since reverting in place would destroy sibling work): substituting the runner's `computeLandableTreeDigest` → `final_validation_stale`; indenting the field lines → `final_validation_failed`. Both flip to a refusal, so the proof is not vacuous. Suites, real exit codes: walkthrough **full scope** 0 before and after (184/184 both), `test-finalize-door.js` 0 (156 assertions), `validate-workflow-contracts.js` 0, `test-validation-runner.js` 0, `test-validate-script-sync.js` 0, `test-install-manifest-single-source.js` 0, `test-spawn-classification.js` 0. **Two reds are NOT from this diff** and were proven so three ways (drift accumulation named only `sink-merge.js`; scoped `checkByteIdenticalGroup` + `checkCommittedKernelParity` → `drift: []`; and both validators exit 0 in a HEAD+own-files-only mirror): `validate-script-sync.js` and `validate-kaola-workflow-contracts.js` red on the **siblings' uncommitted files not yet mirrored into `plugins/kaola-workflow/scripts/`** — that is Group A/B's mirroring step, correctly left alone. **This diff touches the cross-edition kernel, so a four-chain receipt is owed at finalize.** Watch items recorded not built: `record` in a self-host repo writes a file the gate never reads (`mode: "chain-receipt"`), left silent because the recipe is conditioned on the absence of those scripts and the gate tells the truth at the door; and a pre-existing `parseCli` limit rejects a `--command` value whose first token starts with `--` (same as existing `run --command`, unfixed since it would change `run`). Seven test gaps ranked for `tdd-guide`, each with the negative control that keeps it from passing vacuously, plus a warning not to build fixtures from the suite's existing seeders — they inherit an already-correct hash or `KAOLA_WORKFLOW_OFFLINE`.

- item: One-line follow-through inside #900's own goal — `final_validation_stale`'s operator hint (`adaptive-schema.js:1207`) still names no producer command, the same hole criterion 6 fixed for `unbound`. A consumer who re-records from the wrong checkout lands on `stale` and is left equally stranded, so leaving it silent defeats the criterion's purpose. Must land byte-identically in all four kernel copies.
  status: done
  dispatched: `impl-900` resumed via message (it already owns those four files and has the hint wording in context); edit lands in the worktree, note appended to `.cache/impl-900.md`
  result: **DONE by `impl-900`.** `adaptive-schema.js:1207` now reads: "A relevant source/test/test-consumed file changed after validation — or the record was written from a different checkout than this one. Re-run the validation command, then re-record with \`kaola-workflow-validation-runner.js record --project <project> --verdict pass --command \"<the validation command you ran>\"\`, invoked from the working tree you validated (the gate hashes the tree its own shell is in, and a linked worktree and main differ until the branch merges); never hand-patch the hash." Names the command, states the wrong-checkout cause, keeps "never hand-patch the hash", and matches the `unbound` hint's register. `git diff --stat` confirms 4 files × 4 lines; all four copies byte-identical at md5 `57e83365115aa84fc3bf82ffad2cff8a`. I verified independently that **no test pins the old text** (grepped `test-*.js`, the walkthrough and `validate-*.js` for both distinctive phrases — zero hits), so this reds nothing. Process note for a successor: I read the locator while the agent was still writing and briefly concluded the edit had not landed — the md5 moved between my check and my own attempted patch. The idle notification I acted on predated the agent processing my message. My fallback patch script was written to fail closed on an unexpected occurrence count, so it made no change when it found the text already replaced; had it blind-replaced, it would have double-applied. **Check the locator, but a stale idle signal is not proof a dispatch is finished.**

- item: Two value-laden calls `impl-903` escalated, put to the user in conversation (First Principle 4 — machines decide facts, humans decide values). Both were genuine forks, not checkable facts.
  status: done
  dispatched: self — asked the user directly
  result: **USER RULED, both as recommended. (1) Ship the #901 citation detector as-is**, accepting the measured ~25% noise floor (4 flagged / 3 real losses / 1 false positive at `issue-891`, whose prose says the receipt landed at the repository root). Rationale carried: #901's acceptance requires the detector, it found 3 genuine historical evidence losses, it is report-only and omitted when empty, and under `--project` every finding lands out-of-scope so it cannot contaminate a scoped terminal verdict. The agent's refusal to build a section allowlist stands — it would have overfit 3 data points using retired-DAG-era headings. **(2) Keep the widened `--execute` repair**: the bundle-member fix makes unscoged `--execute` remove EVERY stale member `.roadmap/issue-N.md`, not just the primary (measured: baseline removed `issue-700.md` and left `issue-701.md`; the fix removes both). It is the correct repair, #903's acceptance asks for it, a closed `all_or_nothing` bundle genuinely makes every member source stale, and leaving one behind is silent residue nothing detects. Bounded by the pre-existing `comment_keep_open` arm plus a new `closure_policy` check; census: all 54 bundle archives here are `all_or_nothing`. **Do not re-litigate either.**

- item: Group D — propagation, AFTER B+C+A2: edit the two authoring points (`templates/routing/finalize.skeleton.md:121-124` for #900's recipe, `templates/routing/slots.js:126` for #903's scoped invocation), then `node scripts/generate-routing-surfaces.js` (18 tracked) → `node scripts/sync-opencode-edition.js` + `node scripts/sync-kimi-edition.js` (6 dot-dir editions) → `README.md:958` by hand → extend `scripts/validate-workflow-contracts.js:512-513`. **14 surfaces, 2 authoring points.** Never edit a rendered surface. Guards that will bite: `test-route-reachability.js:163-184` (T6), `test-opencode-edition.js:804-810` (A16), `required-blocks.js:262-274` (two interior sentences verbatim). **Facts from `impl-900` to carry in**: its final diff is 8 files `+732 −8` (`validation-runner.js` +181, `adaptive-schema.js` 2 lines at `:1206`+`:1207`, each byte-propagated to three plugin copies via `edition-sync.js --materialize-kernel`, all four md5 `57e83365115aa84fc3bf82ffad2cff8a`); and the recorder's command name now appears in the kernel **twice** (`:1206` and `:1207`) with **no guard** keeping the two spellings in step across the four copies. Record that where the surface contract is written — do NOT build a guard for it (no observed failure yet; watch-list discipline). **Additional debt from `impl-902`**: `docs/api.md:219-222` lists the finalize `--check` envelope keys and is now stale (no test pins it, so nothing is red — but it is test-consumed, so editing it re-stales a chain receipt: write it BEFORE the receipt run), and `templates/routing/finalize.skeleton.md:349` says "clear everything it lists" where it should say "everything in `reasons`", now that a `pending_mirror` state is deliberately NOT in `reasons`. **From `impl-901-ports`**: `docs/api.md` needs the three new sink fields (`archive_forced_paths`, `archive_missing_paths`, `archive_add_errors`) documented.
  status: done
  dispatched: implementer subagent `impl-prose`, in the worktree; owns `templates/routing/finalize.skeleton.md`, `templates/routing/slots.js`, all 18 regenerated surfaces + the 6 dot-dir edition surfaces (via the generator and the two edition-sync scripts — never hand-edited), `README.md`, `docs/api.md`, `CHANGELOG.md`, and `scripts/validate-workflow-contracts.js`; notes to `kaola-workflow/bundle-900-901-902-903/.cache/impl-prose.md`
  result: **DONE**, 13 files, uncommitted. Notes at `.cache/impl-prose.md`. **I verified the load-bearing claims myself rather than accepting them**: all **14/14** surfaces carry both `validated_candidate_hash` and the recorder command (my first grep read 0 on 13 of them — my needle `validation-runner.js record` missed the closing quote in `...validation-runner.js" record`; **the same one-phrasing undercount I had warned this agent about, committed by me**); `slots.js:126` now passes `--project {project}` for all three forges with a commented `--execute` twin; `generate-routing-surfaces.js --check` → "all 18 surfaces byte-match the skeleton", exit 0; `validate-workflow-contracts.js` exit 0. **I independently mutation-proved the new pin is armed**: baseline exit 0 → stripping `validated_candidate_hash` from a rendered surface → exit **1** → restored → exit 0, restoring from my own byte copy rather than `git checkout --` so no sibling work was destroyed. **CHANGELOG citation rule satisfied**: `[Unreleased]` (166 lines, `### Added` for #900/#903, `### Fixed` for #901/#902) hashes **only** `#900 #901 #902 #903` — every background reference is hashless, and it verified this with the release verifier's own parser rather than by eye. It added one file to its write set with justification I accept: `plugins/kaola-workflow/scripts/validate-workflow-contracts.js`, the **byte-lockstep mirror of its own assigned file** (in `COMMON_SCRIPTS`), because editing the canonical alone red `validate-kaola-workflow-contracts.js` and `validate-script-sync.js`; that is the same file's own contract, not a sibling's script. **Judgement calls it made and I accept**: it did **not** add the five-field `authority` block to the agent-facing finalize surfaces, only to `docs/api.md` — the observed failure was a false obligation in `reasons` and the wording fix addresses exactly that, while naming a diagnostic block in a prompt surface is not something an observed failure demanded (correct additive derivation); and it left `README.md:1016`'s table unscoped deliberately. It documented `archive_summary_citation_missing` **with its false-positive mode stated outright**, framing it as a prompt to adjudicate rather than a verdict — the honest register. **The one claim in its output contingent on another agent**: it first measured while the **gitea** `claim.js` port carried neither #901's claim half nor #902 (`pending_mirror=0`), so it deliberately wrote the entry with **no edition claim**, re-measured after the port agent landed, and only then added "lands in all four editions" — flagging that if any of those regresses before the tag, those two sentences must be corrected. **I verified that claim directly: all four `claim.js` copies read `pending_mirror=5`, `predictFinalizeAuthority=2`, `ignoredArchiveEvidence=3`, and all four `sink-merge.js` copies carry `archive_forced_paths`/`archive_missing_paths`/`archive_add_errors` — uniform.** Also stated: the dot-dir surfaces are gitignored generated trees so nothing in a commit records that the sync scripts ran — the two edition suites are what re-establish it; and the four chains were not run (owed at finalize, belongs to the producer). **One later edit by me, after this item closed**: `CHANGELOG.md:86`'s #901 headline read "no longer discards a consumer's archived evidence and report success" — a grammar slip in release-facing prose; changed to "and reports success anyway". No other CHANGELOG content altered, and the citation set is untouched.

- item: Author the regression tests for whatever ships, from the acceptance surfaces of the issues that survive. Custody: `tdd-guide` owns the artifact; prove each test fails on the `9b68b096` baseline AND that its guard is armed — a positive control is mandatory, because reusing this suite's fixture builders inherits `KAOLA_WORKFLOW_OFFLINE` and can silently disable the very guard under test.
  status: done
  dispatched: self — decomposition only
  result: **Split by TEST FILE, not by issue** — the five implementers collectively spec'd 35+ pins, and two agents writing the 11k-line walkthrough would collide. Three `tdd-guide` agents on disjoint files: `tdd-walkthrough` owns `simulate-workflow-walkthrough.js` + the two forge walkthroughs; `tdd-sinks` owns `test-sink-merge.js` + the two forge sink suites; `tdd-finalize` owns `test-claim-hardening.js` + `test-finalize-door.js` + `test-validation-runner.js` + `test-bundle-finalize.js`.

- item: Tests for #903's scoped closure audit (11 pins ranked in `.cache/impl-903.md` §8) plus any walkthrough-resident pin for the other three issues. The two existing runners `runClosureAudit:1746` and `runClosureAuditOffline:1768` assert `status === 0` **unconditionally** and `JSON.parse` stdout, so the exit-1 and `--help` cases need a direct `spawnSync`; nine functions were newly exported for exactly this.
  status: done
  dispatched: `tdd-guide` subagent `tdd-walkthrough`, in the worktree; owns `scripts/simulate-workflow-walkthrough.js`, `scripts/simulate-gitlab-workflow-walkthrough.js`, `scripts/simulate-gitea-workflow-walkthrough.js` ONLY; notes to `.cache/tdd-walkthrough.md`
  result: **DONE — 13 scenarios covering all 11 items of `impl-903` §8**, ordinals 133–145, `717 insertions(+), 0 deletions(-)` in `simulate-workflow-walkthrough.js` only. **No existing assertion edited, relaxed or deleted.** Notes at `.cache/tdd-walkthrough.md`. **11 of 13 proved RED on baseline `9b68b096`** via `git archive` + overlay-only-the-test-file (nothing reverted in the worktree); the headline red signature IS the regression: `the scoped dry-run envelope must carry the scope, the verdict, both halves and both count objects, got: ["dry_run","offline","drift","counts"]` — i.e. `--project` produced the unscoped envelope. Also red: unknown-flag-exits-1 (`got 0`), mistyped `--project`, fail-closed `clean`, both bundle-member halves, the scoped `--execute` swap, the #901 citation class, the unit helpers, and both attribution pins. **Two pins CANNOT red on baseline and that is structural, not weak authoring**: the `closure_policy` negative and citation-omitted-when-empty both assert an **absence**, and the baseline has nothing to be absent (no member expansion, no citation class exists) — they guard a future deletion inside the new machinery, so their arming rests entirely on mutation (M6, M7/M7c), which it ran FIRST for that reason; both RED. **24 mutations, all RED but one, and it correctly diagnosed the exception as NOT a vacuous pin**: removing the `if (report.drift.archive_summary_citation_missing)` guard at `closure-audit.js:630` assigns `undefined`, and **`JSON.stringify` omits undefined-valued keys**, so the mutation is unobservable through stdout and no CLI-level test can red it; M7c (`… || []`) reds the pin instantly, proving it armed. Implication recorded: that `if` documents intent, not mechanism — `JSON.stringify` already does the omission. No change needed. It avoided the named trap: S4's `clean:true` positive control goes through `runClosureAudit` with `KAOLA_WORKFLOW_OFFLINE: '0'` set **explicitly**, since inheriting `'1'` would make the control agree with the fail-closed leg for the wrong reason and leave an always-false verdict indistinguishable from a working one. Beyond §8 it pinned exact top-level key sets for all four envelopes plus the `drift` key list/order and `counts` mirroring it — the only form a single-version suite can express "unscoped is byte-identical", and it covers `driftCounts` for free. On the #901 detector it pinned the **measured** behaviour only, with no pin asserting the accepted ~25% FP mode away. Suites: **full-scope walkthrough exit 0 in the worktree including all sibling edits — 197/197 scenarios (184 + 13), 0 failed, 2052 spawns**; `--only testClosureAudit` (23 existing + 13 new) 0 on baseline+shipped-audit; `validate-script-sync` 0; `test-kernel-conformance` 0. All 23 pre-existing closure-audit pins pass unchanged. It declined to pin the dead retired-Node-Ledger residue in the forge ports, correctly — pinning dead code would pin it in place, and a test is deleted with its mechanism, never repaired ahead of it.

- item: **MY BRIEFING ERROR, caught by `tdd-walkthrough`**: I assigned `scripts/simulate-git{lab,ea}-workflow-walkthrough.js`, which are not files. The real forge walkthroughs are `plugins/kaola-workflow-git{lab,ea}/scripts/simulate-git{lab,ea}-workflow-walkthrough.js`, and I made the same mistake for the sink suites (`scripts/test-git{lab,ea}-sinks.js` → really `plugins/kaola-workflow-git{lab,ea}/scripts/`). `tdd-sinks` found the correct files on its own. **Verified: the real forge walkthroughs have ZERO closure-audit coverage** — no `runClosureAudit`, no audit script reference — and forge audit coverage lives in `test-git{lab,ea}-workflow-scripts.js`, which no agent owned. So 13 pins protected the canonical scoping surface and **zero** protected either port's, while `impl-903` §9 records that neither port's scoped `main()` had ever been run end-to-end. Largest remaining coverage hole in the bundle.
  status: done
  dispatched: `tdd-guide` subagent `tdd-forge-audit`, in the worktree; owns `plugins/kaola-workflow-gitlab/scripts/test-gitlab-workflow-scripts.js` + `plugins/kaola-workflow-gitea/scripts/test-gitea-workflow-scripts.js` ONLY, told to verify that is the right home and propose rather than expand if not; briefed to mirror the canonical 13-pin set so the three editions stay comparable by reading; notes to `.cache/tdd-forge-audit.md`
  result: **DONE — and the headline is that the ports' scoped `main()` WORKS on its first-ever execution.** Notes at `.cache/tdd-forge-audit.md`. Home confirmed rather than corrected (those two files are the only place forge closure-audit coverage exists; the forge walkthroughs and the sink/run-chains/forge-helpers suites have none). **13 pins per edition, +726/−0 each**, same names both editions, mirroring the canonical S1–S13 item-for-item across all 11 of `impl-903` §8's items. **11 of 13 red on baseline `9b68b096`** per edition, same 11 and same signatures — the headline red being the regression itself (`the scoped dry-run envelope must carry exactly these keys, got ["dry_run","offline","drift","counts"]`), plus `unknown flag must exit 1, got 0` and `ca.parseArgs is not a function`. The 2 that cannot red are structural absence-assertions (P6 `closure_policy: partial`, P9 citation-omitted-when-empty), and it ran their mutations FIRST — M6/M7/M7c all red on both editions. **22 mutations, every one RED on both editions**, on a scratch mirror restored by **exact inverse write** (never `git checkout --`; the worktree untouched), with a harness that **rejects a missing or non-unique anchor rather than silently no-op-ing** and re-verifies a byte-identical restore. **M21 caught a defect in its own pin**: following the canonical map, M21 (`driftCounts` drops non-array keys) ran **GREEN** against P1 because P1's fixture is online so every class is an array and the mutation is unobservable there — a passing mutation means the test is wrong, so it strengthened P4's **offline** leg with an explicit counts-key-set assertion and M21 now reds P4 on both. **The canonical notes' M21→S1 mapping does not transfer between editions** — worth remembering, since mirroring a pin set does not mirror which fixture can observe a given mutation. The offline trap is closed **in both directions**: P4's `clean:true` control asserts `offline === false` *before* asserting `clean === true`, so it cannot silently become the fail-closed leg's twin, and `runClosureAuditRaw` sets `KAOLA_WORKFLOW_OFFLINE` explicitly rather than inheriting. Suites, real exit codes, serial: gitlab suite 0 (13 PASSED), gitea suite 0 (13), both forge walkthroughs 0, `validate-script-sync` 0, `test-spawn-classification` **0** naming nothing of its own — its files' unclassified counts (61/60) are **identical to the pristine baseline's**, with the one new spawn site per file annotated `// spawn-class: cli-contract` and no ceiling raised. Reported not fixed: **F1** (see the next item — it overturns two earlier agents' characterisation), **F2** (`--project ../../outside` resolves a scope from outside the repository at exit 0; `resolveProjectIssues` never validates the name though `active-folders.js` carries an unused `isSafeName`; identical on canonical so shared, not a port defect), **F3** cosmetic (the `#903` ambiguity comment in `resolveProjectIssues` sits above the `resolved:false` return, describing nothing, in all editions), **F4** (`--project <name>.archived-<ts>` passed directly is still an unmeasured invocation form).

- item: **F1 — two earlier agents were WRONG and `tdd-forge-audit` measured it.** `impl-903` §7 and `tdd-walkthrough` both called the forge ports' `archiveRequiredContent` residue *dead code*. The `listRecordedNodeEvidence` arm is indeed inert, **but the `plan_hash` → `workflow-plan.md` demand above it is LIVE**: on one fixture archive with `plan_hash` set and no plan file, canonical reports `archive_content_incomplete: []` while **both ports report `[{"project":"issue-777","missing":["workflow-plan.md"]}]`**. Three editions, two different verdicts for the same archive; it **flips the new scoped `clean` verdict**; and the divergence is **not** among the deliberate ones `docs/api.md:886-890` records — while #903's last acceptance criterion is edition parity. I verified it independently: canonical's closure audit has **zero** `plan_hash`/`workflow-plan.md` references, both ports have 1 and 3. `tdd-forge-audit` wrote no pin against it, correctly — a test is deleted with its mechanism, never repaired ahead of it.
  status: done
  dispatched: **USER RULED: delete from both ports to match canonical** (asked in conversation, since it is a production deletion beyond the four issues' literal scope). Also ruled in the same exchange: **F2 — validate the project name, reusing the existing unused `isSafeName`**, conforming to the contract's published exit-1-for-operator-input-error rather than adding a new gate. Both go to implementer subagent `impl-parity`, which owns all four `*closure-audit.js` copies; notes to `.cache/impl-parity.md`
  result: **BOTH DONE, and the divergence was reproduced on the real files before the fix.** F1: on the fixture (archive `issue-777` carrying `plan_hash`, no `workflow-plan.md`), gitlab and gitea went from `[{"project":"issue-777","missing":["workflow-plan.md"]}]` to **`[]`**, canonical and codex unchanged at `[]` — **all three editions now agree.** It used the diff-and-converge method as briefed: the two port `archiveRequiredContent` regions are now **byte-identical to canonical's** (`diff` clean), forge surroundings untouched (gitlab keeps `unarchived_mr_folders`/`mr_url`/`mr_state`), the inert `listRecordedNodeEvidence` arm went with it, and residual `plan_hash`/`workflow-plan.md`/`listRecordedNodeEvidence` references across all four closure-audits are **0**. **One honest qualifier it volunteered**: scoped `--project issue-777` shows the in-scope class term flipping to `[]`, but `current_project_clean` reads `false` in **both** states offline because two remote classes return `skipped_offline` and fail-closed forbids clean — so it measured the **class term**, not the boolean, and said so rather than letting the table read as more than it is. F2: `--project ../../outside` went from exit **0** with 1294 stdout bytes and a scope resolved outside the repo, to exit **1** with **0 stdout bytes** and a stderr message; the positive control `--project issue-555` is **byte-identical** at exit 0, and an unscoped run is unchanged at 539 bytes — same in all four editions, both rows, confirming the defect was shared rather than a port defect. **`isSafeName` needed no export at all** — it was already exported from all four `*-active-folders.js` with character-identical bodies, so it only added it to the existing destructuring `require`; the `FORGE_EXPORT_SUPERSET_FAMILY`/`canonicalOnly` concern never arose, no `*-active-folders.js` was touched, and no new export key exists. Assert placement is in `parseArgs` beside the existing `--issue` validation and before `getRoot()`, matching the convention at `sink-pr.js:148`/`sink-merge.js:2698`, with the existing `--project requires a project name` message unchanged so pins on it still hold. All "before" legs are real measurements from a **scratch mirror with its two edits mechanically reverse-applied** (each replacement asserted to match exactly once, restored region verified byte-identical to `git show HEAD:<port>`) — no `git checkout --`, nothing in the worktree reverted. **Eight suites exit 0 before AND after**, with the full-scope walkthrough at 197/197 / 2052 spawns and the forge suites' spawn counts (547/548/112/127) **identical before and after — proving nothing was silently skipped**, which is a check I had not asked for and should have.

- item: **Pin the two changes `impl-parity` just made — they are UNPINNED in every edition, which is how the divergence survived.** A `tdd-guide` had already deleted the plan/ledger assertions in `simulate-workflow-walkthrough.js:7647` (its own `// DELETED:` note at `:7708`), and **no port fixture writes `plan_hash` anywhere** (0 grep hits across both port suites and both forge walkthroughs) — so the F1 fixture is precisely the case no suite builds, in any edition. Needed: a pin asserting `archive_content_incomplete === []` for a `plan_hash`-bearing, plan-less archive (would have caught the divergence and will catch its return), and for F2 `['--project','../../outside']` in the exported-`parseArgs` throw list (`simulate-workflow-walkthrough.js:8825`, `test-gitlab-workflow-scripts.js:3354`) plus a CLI exit-1/empty-stdout case in the `:8370` loop. Also a stale prose leftover to remove: `simulate-workflow-walkthrough.js:7644` still names `listRecordedNodeEvidence`, a mechanism now gone from all four closure-audits — it asserts nothing.
  status: in-flight
  dispatched: `tdd-guide` subagent `tdd-parity`, in the worktree; owns `scripts/simulate-workflow-walkthrough.js` + `plugins/kaola-workflow-gitlab/scripts/test-gitlab-workflow-scripts.js` + `plugins/kaola-workflow-gitea/scripts/test-gitea-workflow-scripts.js` (all three now released by the agents that authored in them); notes to `.cache/tdd-parity.md`

- item: Route `impl-parity`'s `docs/api.md` findings. `:960`'s exit-1 row already publishes "a missing or **malformed** flag value", so F2 conforms to the contract as written and needs no edit — optional sharpening only. `:971`'s `archive_content_incomplete` row ("missing a required artifact") is not falsified but is now vague, since the required set is exactly one file. And `:990`'s "GitLab ships … with the same contract and JSON shape" **was FALSE before this change and is true now** — the sentence F1 restores.
  status: done
  dispatched: `impl-prose` resumed via message — it owns `docs/api.md` and `CHANGELOG.md`; asked for the `:971` sharpening and a CHANGELOG note that the parity sentence is now true, nothing more; notes appended to `.cache/impl-prose.md`
  result: **DONE, verified by me** (the agent went idle without a summary, so I checked the locator rather than assume either way — the third time that has happened this run, and the second time the work was in fact complete). `docs/api.md:971` now reads "An archived run whose folder is missing its `workflow-state.md` identity anchor — the only unconditionally required artifact. **Report-only in both modes**, and identical offline" — the suggested sharpening, adopted verbatim. Final prose diff across the run: `CHANGELOG.md` +192, `docs/api.md` +116, `README.md` +8 (308 insertions / 8 deletions). **Citation rule re-verified by me: hashed references in `[Unreleased]` are still exactly `#900 #901 #902 #903`**, with every background and cross-forge reference hashless.

- item: `test-spawn-classification.js` is RED in the worktree with 3 violations, all from `tdd-sinks`' new spawn sites — `plugins/kaola-workflow-gitea/scripts/test-gitea-sinks.js` 49 vs ceiling 47, `plugins/kaola-workflow-gitlab/scripts/test-gitlab-sinks.js` 54 vs 52, `scripts/test-sink-merge.js` 4 vs 3 (new site at `:2111`). Attribution is exact: the same checker passes on pristine `9b68b096` overlaid with only the walkthrough file. Fix is `// spawn-class: <cli-contract|concurrency|crash|durable-handoff|environment>` annotations per site; **the checker states outright that raising the ceiling is not a fix.**
  status: in-flight
  dispatched: `tdd-sinks` resumed via message with the measured violation list and the corrected file paths; it owns those three files already

- item: Tests for #901's sink evidence loss — canonical plus **both forge editions**, since each edition's suite defends its own copy and a canonical pin does not cover a port. The load-bearing pin: **assert on `git ls-tree` (blob presence), never `fs.existsSync`** — no test in the corpus does, which is exactly why #901 survived its entire life.
  status: done
  dispatched: `tdd-guide` subagent `tdd-sinks`, in the worktree; owns `scripts/test-sink-merge.js`, `scripts/test-gitlab-sinks.js`, `scripts/test-gitea-sinks.js` ONLY; notes to `.cache/tdd-sinks.md`
  result: **DONE across all three editions.** Notes at `.cache/tdd-sinks.md`. It corrected my wrong paths itself (the forge suites are under `plugins/kaola-workflow-git{lab,ea}/scripts/`). Pins: canonical `test-sink-merge.js` (y1)–(y6) with 68 new assertions; gitlab and gitea `(a)`–`(e)`, 5 pins each. **Baseline-red: canonical exit 0 → exit 1 with 19 failed / 347 passed; gitlab 20 red clauses; gitea 20 — and ZERO non-#901 failures in any baseline run.** Method was `git archive 9b68b096` + overlay only the one test file, nothing reverted in the worktree. Because the forge suites use node's throwing `assert`, a single baseline run would stop at the first red and never measure later legs, so it built a scratch-only recording harness — **and put a positive control on the harness itself: run over the FIXED port it records 0 reds**, so a recorded red is the baseline sink and not the shim. That is the right control on a measuring instrument. **Every durability clause asserts BLOB presence via `git ls-tree`, never disk existence** (verified independently: 3 `ls-tree` additions per file) — the distinction that is the entire reason #901 survived its life. **`test-spawn-classification.js` is now GREEN (exit 0, verified by me)** — it annotated each new site with `// spawn-class:` rather than raising a ceiling, which the checker states is not a fix. **Three observations worth carrying, none a defect: (1)** a **refusal envelope carries no `receipt` key at all** — its first draft asserted `out.receipt.archive_commit === 'failed'` and failed against the *fixed* sink, and worse, the neighbouring `out.receipt.steps.archive_commit !== 'done'` **would have passed vacuously** for the same reason; both now read the surviving on-disk journal, where the failed status, the not-done step and `archive_missing_paths` actually live. This is the `receipt.` vs `steps.` trap in a **second** location, the refusal path — anyone writing further #901 pins must assert the refusal's durable half from the journal. **(2)** In the sole-archiver posture all three editions produced a collision-suffixed dest (`archive/<p>.archived-<ts>`) even with **no** pre-existing archive directory, so the pins read `receipt.archive_dest` rather than assuming the plain path. **(3)** `git status --porcelain` reports the tree **CLEAN** while the evidence is only on disk, so (y5)'s "main must be clean after `status:sinked`" clause **passes on the baseline** and cannot detect #901 — it is offered for the tracked-live-path deletion only, and is the sharpest single statement of why the incident was silent. Not written and recorded: the finalize-side `archive_ignored_evidence` NOTE end-to-end (needs walkthrough-internal helpers in a file it must not touch — since measured by `impl-claim-ports`), the opencode/kimi editions (outside its write set), and #901's item-5 disposal-gate pin (see the reopened ruling below).

- item: **REOPEN my own no-seam ruling — `tdd-sinks` refuted its premise.** I ruled that the #901 disposal-gate pin needed no production change because a test could build the source/destination asymmetry directly on disk. `tdd-sinks` reports that is **not writable in-repo today**: under the shipped `copyDir` the sidecar exemption is unreachable because **the copy is fully recursive**, so a pre-built incomplete destination is simply filled in before the gate looks. Two independent agents (`impl-901`, `tdd-sinks`) have now declined to invent a production seam on their own authority, correctly. **My alternative was wrong on the mechanism, so the ruling has to be re-decided rather than restated.**
  status: in-flight
  dispatched: `tdd-finalize` resumed via message with a preference-ordered ruling and a measurement to settle it: (1) first test empirically whether an **unreadable source sidecar** (`chmod 000`, the same trick `impl-901` used to arm the sink's blob gate) yields a lossy copy with **zero** production change — if so, write the pin that way; (2) if `copyDir` throws instead of skipping, I authorize **exporting the disposal-gate helper** in canonical + both forge ports, which `validate-script-sync.js:493-495` permits since it requires every canonical export to exist in each port (`canonicalOnly` is the exemption list) — an export widens no runtime behaviour and creates no test-only code path, unlike an env seam; (3) an env seam remains refused, and leaving the pin unwritten-and-recorded is preferred over one. Result to `.cache/tdd-finalize.md`

- item: Tests for #902's `finalize --check` convergence and #900's recorder. For #902 the mandatory pins are the **unseeded-worktree topology**, the **fail-closed negative** (M1 proved the topology leg ALONE passes against a blanket suppression), and **varying the cwd axis** — `runFinalize816`/`runFinalize837` hard-code `cwd: fx.wtRoot`, which is why nothing caught this.
  status: done
  dispatched: `tdd-guide` subagent `tdd-finalize`, in the worktree; owns `scripts/test-claim-hardening.js`, `scripts/test-finalize-door.js`, `scripts/test-validation-runner.js`, `scripts/test-bundle-finalize.js` ONLY; notes to `.cache/tdd-finalize.md`
  result: **DONE.** Notes at `.cache/tdd-finalize.md`. No seam added, no production file touched. **#900/#902 pins landed** — including **T8** ("the consumer arm's `record` producer and the finalize gate agree on one candidate hash") and **T8l** ("`record` binds the working tree it was invoked from, provably not the other one"), both independently green against `impl-900`'s implementation. **BOTH RUNGS OF MY PREFERENCE ORDER FAILED, and it measured that rather than reasoning it** — three legs against the real CLI with a real `git worktree add`: **Leg 1** (pre-build `archive/<project>` as a faithful copy minus `.cache/final-validation.md`) → **exit 0, `status: closed`, no refusal**, the pre-built dir untouched and the real destination becoming a `.archived-<ts>` sibling that `copyDir` filled faithfully — because `claim.js:2485-2489` **manufactures `dest` from `src` one statement earlier at a path proven not to exist**, so pre-existence only *renames* the destination. So my "build the end state on disk" idea was structurally impossible, not merely awkward. **Leg 2 (control)**, nothing pre-built → exit 0, one archive, disagreeing with leg 1 on the archive-name set, so leg 1 was not a fake control. **Leg 3** (source sidecar at mode 000, the only on-disk way to stop `copyFileSync`) → **exit 1 with `reason: 'archive_exception'` and no `missing`** — `copyDir` **throws**, so the typed `archive_incomplete` + `missing: [...]` the gate exists to emit never appears. The end-to-end gate pin is therefore **not written, reported not bought**. **What it pinned instead is the substance**: `test-finalize-door.js` T6g/T6h/T6i, +14 assertions, driving the already-**exported** `verifyArchiveComplete` as a two-directory property, which IS on-disk constructible — T6g asserts per sidecar name **one at a time**, because the exemption matches by exact basename so a set-membership bug on a single name would be invisible to a combined arm; T6i drops an exempt sidecar *and* a non-exempt file together and asserts `missing[]` names the non-exempt loss while staying **silent about the exempt one even while already refusing**; T6h is the discriminating control (non-exempt `.cache/*.md` dropped → `ok:false`, path named); plus a source-text pin extracting the shipped set so a **name added without an arm reds**. These arms are **green on the baseline by design** — `missingSidecars` does not exist at `9b68b096` and the exemption set is byte-identical there, so they measure a **pre-existing** gap and their arming is mutation, **in both directions**: exemption **removed** from the walk → exit 1 with **11 reds** (T6g on all five names + T6i, T6h correctly staying green); exemption **widened** to all `.cache/*.md` → exit 1 with **5 reds** (T6h + T6i + pre-existing T6b/T6c). The control provably disagrees with the positive leg under one mutant and agrees with the implementation under the other — a **bidirectional** mutation proof, stronger than the one-directional kind, and it excludes the fake-control failure mode by measurement. **CORRECTION TO ME: the sidecar set has FIVE names, not four** — `final-validation.md`, `run-gaps-manual.md`, `selection-evidence.md`, `doc-docking.md`, `doc-updater.md` (verified at `claim.js:5058`). `impl-901`'s own comment is accurate ("four of the five evidence files the incident lost live in that set"); I compressed it wrongly into "four sidecars" and repeated that in three briefs. All five are driven. Suites, real exit codes, serial: `test-claim-hardening` 0 (514), `test-finalize-door` 0 (**208**, was 194), `test-validation-runner` 0, `test-bundle-finalize` 0 (149, untouched), full-scope walkthrough **0 at 197/197**. No new spawn site, so `test-finalize-door.js` holds 0 unclassified against a ceiling of 0. One stale claim in its report, checked and refuted: it reported `test-spawn-classification.js` still red on `scripts/test-sink-merge.js` — **it is GREEN, exit 0** (measured by me); it had measured before `tdd-sinks` landed the annotations. Reported and deliberately NOT pinned: the exemption is silent about **byte differences** as well as absence (the walk `continue`s before `sourceFiles.set`), and `impl-901` justifies that as "a normalized sidecar may legitimately differ from its source" — but `archiveProjectDir` normalizes `src/.cache/final-validation.md` at `:2455-2463` **before** the `:2489` copy, so on the readable path they should agree and the rationale could not be confirmed. Its reasoning for leaving it alone is right: pinning the hole would vote to keep it on a justification it cannot verify, and asserting against it could break a normalization it cannot see.

- item: Decide `tdd-finalize`'s escalated proposal — extract the sidecar loop into an exported pure `missingArchiveSidecars(srcDir, destDir)` so the disposal gate itself becomes on-disk testable, permanently removing the doctored-`copyDir` dependency. Cost: `FORGE_EXPORT_SUPERSET_FAMILY` requires both forge ports to export it too, so it is a production change across three files.
  status: done
  dispatched: self — my call
  result: **DECLINED for this bundle, and RECORDED as a watch item.** I am declining something I had authorized at rung 2, and the reason is that the measurement changed the picture in a way I could not have known when I wrote the preference order: **T6g/T6h/T6i turned out to cover the substance** — the exemption gap is the actual defect class, it is now pinned per-name with a bidirectional mutation proof, and a name added without an arm reds. What remains unpinned is one layer above: the gate's typed `archive_incomplete` + `missing[]` emit end-to-end. That guard has already been **mutation-proven twice by independent agents** (`impl-901` and `impl-claim-ports`, the latter having caught its own fake control), so what is missing is durable regression protection for an emit, not evidence the guard works. Against that: the extraction is a production refactor plus three export additions in `claim.js` and both forge ports — a file three agents have already edited — at the tail of a bundle where every suite is green and every mutation proof is complete, so it would cost a full re-verification for **anticipatory** coverage that no observed failure demands. Watch-list discipline applies directly: recorded, not built. The recovery path is in this entry and in `.cache/tdd-finalize.md`, including that the `FORGE_EXPORT_SUPERSET_FAMILY` check permits it as long as the export lands in canonical **and** both ports. **REINFORCED on a second report**: it is an **extraction**, not an export — `claim.js:2500-2510` is an inline `for` loop inside `archiveProjectDir` with no function to export — so the cost is a production refactor plus four export sites, more than I authorized at rung 2, not less. Signature recorded for whoever picks it up: `missingArchiveSidecars(srcDir, destDir) → ['.cache/<name>', …]`, a pure two-directory comparison in the shape of `verifyArchiveComplete`; and my `validate-script-sync.js` reading is **confirmed** (`:485` carries `canonicalOnly: ['ghExec']` for the claim family, and `forgeClassifierExportDrift` treats canonical keys absent from any port as fail-closed drift). **Rung 1 is now refuted with a four-leg probe, not an argument** — and with the uid printed up front, since a root run would have made the whole chmod axis inert: (A) the mechanism — `readFileSync`/`cat`/`copyFileSync` all EACCES; (B) end-to-end with an exempt sidecar at mode 000 → exit 1, `reason:'archive_exception'`, EACCES detail naming the exact copyfile, **no `missing` key at all**, archive left **partial**, live source *and* sidecar surviving; (C) control with the sidecar readable → exit 0 `status: closed`, all three files archived, live source gone — disagreeing with B on exit code, reason, archive contents AND source survival; (D) an unreadable `.cache` **directory** → same `archive_exception` via `scandir` EACCES. **The structural reason no permission trick can ever reach the gate**: the throw is *inside* `copyDir`, which runs **before** `verifyArchiveComplete` and the `missingSidecars` loop, and `archiveProjectDirSafely` (`:2660-2666`) catches it into a different envelope with no `missing[]` — the copy aborts **mid-way** rather than completing lossily, so the "lossy copy, then gate" sequence never exists at all.

- item: Take `tdd-finalize`'s offered end-to-end **destruction-safety** arm — probe legs B/D turned into a pin: an unreadable exempt sidecar yields exit 1, `archive_exception`, and **both live copies survive**. It pins "a mid-copy failure never destroys the only copy", which is the harm class #901 is actually about, and would red if the archive ever deleted before verifying or swallowed the exception.
  status: done
  dispatched: `tdd-finalize` resumed via message — ACCEPTED (zero production change, already-measured legs, ~10 assertions), with the instruction to carry the probe's **uid guard** so a root run skips loudly rather than passing vacuously; extraction confirmed declined in the same message; result to `.cache/tdd-finalize.md`
  result: **DONE — arm in, mutation-proven in both halves.** Placed as case (3) of the `#941` block in `test-claim-hardening.js` (9 assertions, 515 → **524**) rather than the `#902` block, because `#941` already owns "nothing DESTROYED" and this is the same property one door over. Vehicle is `mk941` with an **exempt** sidecar (`final-validation.md`) at `chmod 000` — exempt deliberately, since a non-exempt file would be caught by the completeness comparison and the arm would merely re-measure case (2). It carries the **uid guard skipping LOUDLY** (`process.getuid() === 0` prints `SKIP: … running as uid 0, where chmod 000 is inert and this arm cannot fail` and runs nothing), and it **proves the axis INSIDE the arm before the run** — `readFileSync` must give EACCES, so a chmod that did not take reds there rather than downstream. Asserts it fails at the **right door**: exit 1, `result:'refuse'` **and** `reason:'archive_exception'` — a *different* door from `archive_incomplete` because the completeness gate is never reached — with `detail` bound to the **basename**, not to Node's errno wording. Then four distinct survival assertions: the worktree run folder, **the unreadable sidecar itself (the one file with no second copy anywhere)**, the sibling evidence the aborted walk never reached, and the **main** live copy. **Two mutants that separate the halves, and MD1's result is good news about the design: MD1** (`copyDir`'s throw swallowed) → only **2 reds**, the typing and the locator, while **all four survival assertions stay green — because destruction is defended by a second independent gate downstream**; **MD2** (throw swallowed **and** the incompleteness return bypassed) → **15 reds including every survival assertion**, with the transaction reporting **exit 0, `status: closed`, `archived: true` over an aborted copy** and deleting the live folders. MD2's reach into pre-existing `#941` case (2) is correct rather than collateral, since MD2 removes the refusal case (2) exists to pin. **Final tally for this agent across both assignments: 1047 insertions, three test files, no deletions, no production file touched at any point, nothing in the worktree ever reverted.** `test-claim-hardening` 461 → **524**; `test-finalize-door` 156 → **208** (T8/T8l for #900, T6g/T6h/T6i for #901's exemption gap); `test-validation-runner` **+31**; `test-bundle-finalize` untouched. Baseline-red on `9b68b096`: **20** (claim-hardening, including the #902 cwd-axis signature) + **22** (finalize-door, all #900) + **30 of 31** (recorder). Everything that greens on baseline is a fail-closed anchor or a gap measurement, each armed by a **named** mutant — #902 arm C by M-suppress, arms A/D/E by M-disable, T6g/T6i by MX1, T6h by MX2, `#901(disposal)` by MD1/MD2, and #900 by MU1–MU4 — where **MU2 and MU4 are caught by only the door suite**, which is precisely why that arm is not redundant with the unit one. Suites, real exit codes, serial, with `pwd` printed alongside the walkthrough to prove cwd: claim-hardening 0 (524), finalize-door 0 (208), validation-runner 0, bundle-finalize 0 (149), **full-scope walkthrough 0 at 197/197, 2052 spawns**. Spawn ceilings clean (claim-hardening 40/64, validation-runner 1/1, finalize-door 0/0). **One claim of its own I checked and refuted for the second time**: it reported `test-spawn-classification.js` still red on `scripts/test-sink-merge.js` — it is **GREEN, exit 0**, "601 spawn sites across 60 files, 176 classified", with 3 `spawn-class:` markers in that file. `tdd-sinks` fixed it after this agent's measurement, and this is the **second** stale cross-agent claim from it. Lesson for the run record: **a cross-agent red is only as fresh as the moment it was measured — re-measure before acting on someone else's failure report.**

- item: `tdd-finalize` found a **fail-open in its own #902 arm A** using the collision-suffixed-dest trap surfaced earlier by `tdd-sinks` — a hardcoded `archive/<project>/workflow-state.md` existence check that passes when the real dest is suffixed. Fixed by asserting **both** halves: the envelope names a dest under main's archive band for the project (plain **or** suffixed) **and** the folder is on disk at that dest. It then checked its other archive assertions for the same shape (clean) and confirmed it wrote no negative-existence assertion anywhere.
  status: done
  dispatched: self — recording a finding, no work needed
  result: **A THIRD location of the same trap.** The finalize **success** envelope carries a **top-level `dest`** while `closure_receipt.dest` is `undefined`; the `archive_incomplete` refusal also carries top-level `dest` (`claim.js:3910`). Combined with `tdd-sinks`' discovery that a **refusal envelope carries no `receipt` key at all** (so `out.receipt.steps.archive_commit !== 'done'` passes **vacuously**), that is now **three distinct places in this codebase where the nested key looks right and is empty** — and in every case the wrong reading produces a green, vacuous assertion rather than a visible error. Worth carrying into any future work on these envelopes: **read the top-level key and verify it is defined before asserting on it.**

- item: New evidence arrived mid-run — the user pointed at fresh comments on #900 and #902, both from a **second independent reproduction** in the **Kimi** edition (the originals were Codex): consumer run of `vrpai-cli` #1042, 2026-08-01. Assess whether either changes the shipped fix.
  status: done
  dispatched: self
  result: **Both premises are now doubly confirmed across two runtime editions, and each comment adds a NEW named harm.** #900: the consumer followed the recipe verbatim, got `final_validation_unbound`, and used the same operator-only workaround (requiring `kaola-workflow-adaptive-schema.js` directly, stamping `validated_candidate_hash: ddf468a0…` from `computeCodeTreeHash(worktree, "issue-1042")`). **The new harm: the binding was stamped AFTER the finalize transaction had already archived the project folder, so the archived record was edited post-hoc** — the comment's own conclusion is that "a recorder exposed before the transaction would have avoided that too." #902: from the linked worktree `--check` reported `archive_authority_missing` with no other unmet precondition, and **reading the check at face value the operator hand-seeded the project folder main→worktree (`cp -R kaola-workflow/issue-1042`) before re-running — unnecessary, since the transaction self-heals.** The named harm: the disagreeing preflight **trains operators to hand-copy project folders, exactly the class of manual mirror repair the transaction is supposed to own.** **Rulings: #902 needs NO code change** — the shipped fix returns `ok:true`, exit 0, `workflow_state: pending_mirror` with `reasons` empty, so nothing prompts a hand-copy, and the `authority` block names `source_dir`→`dest_dir` so the operator can see the mirror will do it. It also retroactively vindicates the decision NOT to widen the live probe to main, since that would have named main as the authority rather than predicting the mirror. **#900 DOES demand one change**: `validation-runner.js:1156`'s `project_folder_missing` message names the linked-worktree case only and is silent on the archived case — the exact situation observed. It must name it, and must NOT make writing into an archived folder easier: the comment frames post-hoc editing of archived evidence as **the problem**, not a capability to add. Minimal fix is a more specific message, no new mechanism.

- item: Extend `validation-runner.js`'s `project_folder_missing` message (`:1156`) to name the archived case — detect that the live run folder is absent while `kaola-workflow/archive/<project>/` (or a `.archived-*` sibling) exists, and say that the record belongs BEFORE finalize and that an archived record must not be edited retroactively. No new mechanism, no writing into an archive. Must land byte-identically in all four `validation-runner.js` copies.
  status: done
  dispatched: `impl-900` resumed via message — it owns that file and already knows the ×4 byte-identical propagation via `edition-sync.js --materialize-kernel`; note appended to `.cache/impl-900.md`
  result: **DONE** (FOLLOW-UP 2 in `.cache/impl-900.md`), all four `validation-runner.js` copies at md5 `8a781aeda1ad244125f8073964b1ca82`; kernel copies untouched, still `57e83365…` ×4. New message at `:1192-1195` names the archived case **with its resolved path**, says the binding belongs in the record **before** finalize as part of the validation step, and states the archived record is closed evidence that must not be edited retroactively. Its closing clause deliberately names **no procedure**, because it searched and found there is no `reopen` or `unarchive` verb anywhere in `claim.js` — telling the operator to re-open would have invented a capability. Detection (`:1109-1141`, new read-only `archivedProjectPaths`) mirrors the finalize authority resolver's own search: `archive/<project>` **or** a `<project>.archived-*` sibling, in this working tree **and** in main via the already-exported `schema.resolveMainRoot`, because the transaction lands the archive in main first. No new dependency, no write path near the archive; the wrong-checkout message is unchanged and still fires when no archive is found. **Six legs, two of them the controls that matter**: A1 exact archive dir → archived message + path, exit 1, **archived record byte-unchanged**, no live folder created; A2 `.archived-<ts>` sibling → suffixed path; A3 archive in main with the caller in the linked worktree → main's path; **A4 no archive anywhere → the ORIGINAL wrong-checkout message with `archived_project_paths: []`**; **A5 a DIFFERENT project archived → original message, `[]`** — so the detection is project-specific, not a blanket rewrite; A6 live folder alongside an archive → `recorded`, exit 0, `record_path` = the **live** folder, nothing written into the archive. Its own framing of why A4/A5 matter is right: a detection that always fired would emit the archived message on every wrong-checkout run and still look green. **CORRECTION TO MY INSTRUCTION**: `--materialize-kernel` does **not** propagate the runner — `MATERIALIZED_SHARED` (`edition-sync.js:84-86`) holds `kaola-workflow-adaptive-schema.js` and nothing else; the runner travels in `validate-script-sync.js`'s `BYTE_IDENTICAL_GROUPS`, copied by `--write` step (c). Measured: `--materialize-kernel` after its copy reported "0 file(s) already present". **It used `cp` and deliberately NOT `--write`, because `--write` also syncs `COMMON_SCRIPTS` and every other byte group and would have dragged three siblings' in-flight files into the plugin trees** — judgement I did not anticipate and would have got wrong. Verified clean by the sync module's own helper (`validation-runner module copies → drift: [] missing: []`). Suites, real exit codes: `test-validation-runner.js` 0, `test-finalize-door.js` 0 at **208 assertions (was 156)** — and the test author's new **T8** ("the consumer arm's `record` producer and the finalize gate agree on one candidate hash") and **T8l** ("`record` binds the working tree it was invoked from, provably not the other one") are **green against this implementation**, i.e. two agents' work cross-verifies; full-scope walkthrough 0 at **197/197**; `test-claim-hardening.js` 0 (514 assertions); `test-bundle-finalize.js` 0 (149 tests); `validate-script-sync.js` **0** with "committed kernel parity: 4 Oracle Kernel copies identical at HEAD" — the earlier sibling reds are gone. Attribution control: fresh `HEAD` mirror + only its 8 files → all clean. Three further test gaps appended for the test author, including turning the observed failure into a pin: **`record` must never write inside `kaola-workflow/archive/**`**.

- item: Contract call `impl-900` escalated — whether to mint a fourth `reasons` token `project_archived` (so a script can branch without string-matching the hint) or keep the archived case inside `project_folder_missing` with the new optional `archived_project_paths` key.
  status: done
  dispatched: self — my call
  result: **RULED: keep it as implemented. No fourth token.** Nothing observed demands machine-branching on the archived case — the observed failure was an operator *editing archived evidence*, which the message solves; "add only what an observed failure demands, silence is an answer" applies directly. And the stated need is already met without a token: `archived_project_paths` is present **only** on a `project_folder_missing` result (absent, not `[]`, elsewhere — deliberately, since a `[]` nobody looked for would read as "no archive exists"), so a script can branch on key presence rather than parsing prose. Minting an enum member now would also invalidate the contract 13 already-rendered surfaces were written against, for no measured gain. `reasons` keeps exactly its three published tokens. No further change requested, so `impl-900` needs no action.

- item: Strengthen the `[Unreleased]` CHANGELOG entries for #900 and #902 with the second-reproduction evidence and each comment's named harm — both incidents now reproduce independently in two runtime editions, and the concrete harms (post-hoc editing of an archived record; a preflight that trains operators to hand-copy project folders) are exactly the "what was measured" register the existing 9.1.1/9.1.0 entries use. **The `vrpai-cli` and `financial-agent` issues are OTHER forges — their numbers must carry NO `#`**, or the release verifier refuses `changelog_unknown_reference`.
  status: done
  dispatched: `impl-prose` resumed via message — it owns `CHANGELOG.md` and verified the citation set with the release verifier's own parser; note appended to `.cache/impl-prose.md`
  result: **DONE.** `[Unreleased]` grew from 166 to 184 lines. **I verified the citation rule myself, since it is a release gate**: hashed references in `[Unreleased]` are still exactly `#900 #901 #902 #903`, and the cross-forge citation reads "**vrpai-cli issue 1042**" — hashless, as required, since the release verifier's known set is `--issues-closed` plus every `#\d+` in commit messages since the last tag and a hashed cross-forge reference refuses as `changelog_unknown_reference`.

- item: Adversarially verify the shipped diff — hunt for a guard that cannot fail, a vacuous assertion, and a test that passes under mutation. Prior runs shipped exactly this class of defect past four green chains twice. Production surface is **6 unique files** (`claim.js` +188, `closure-audit.js` +422, `sink-merge.js` +158, `validation-runner.js` +221, `adaptive-schema.js` ±4, `validate-workflow-contracts.js` +11); the other 25 changed files are edition copies, whose convergence was separately measured byte-for-byte. Total 8130 insertions / 333 deletions across 31 files.
  status: in-flight
  result-so-far: **`adv-guards` REFUTED 4 of 5 guard claims. The bundle is NOT committable as it stood.** Report at `.cache/adv-guards.md`. Ranked: **(1) C2 destruction gate — SEVERE, destroys evidence at exit 0.** `archiveProjectDir` **deletes TWO live copies but verifies ONE**: `verifyArchiveComplete(src, dest)` and #901's new `missingSidecars` re-check both read `src` = the **worktree** copy, while `mainLive` (`claim.js:2517-2523`) is `rmSync`'d with **no comparison against `dest` at all**. Measured: `release --project issue-91001` from a linked worktree → `exit=0`, `{"released":true,"archived":true,"discard_archive_committed":true}`, and `LOST FROM EVERYWHERE: [".cache/MAIN-ONLY-evidence.md", ".cache/final-validation.md", "finalization-summary.md"]` — no `missing[]`, no warning, and the thin archive then committed to main. Control proves the guard IS armed on the `src` side (unreadable `src/.cache` → `archive_exception`, exit 1, deletes nothing), so it is a **scope hole, not a dead guard**. Three routes reach the delete with no Step-8a mirror: `release`/`discard` (`:4433`), `watch-pr` on MERGED (`:5350`), the abandon backstop (`:5442`). This is squarely #901's own acceptance ("does not delete the only recoverable copy") and our gate guards the wrong copy. **(2) C3 `pending_mirror` — SEVERE, the dangerous direction I asked them to hunt.** `probeFinalizeMirror`'s writability probe exists only in the `sync_required` arm and probes the **source**; the `'ready'` arm our new predicate reads to *promise construction* probes nothing. With the worktree's `kaola-workflow/` at `chmod 555` (axis printed, uid 501): `--check` → exit 0, `ok=true`, `reasons=[]`, `pending_mirror` → transaction → **exit 1 with NO JSON envelope**, raw `EACCES: permission denied, mkdir '<wt>/kaola-workflow/issue-92002'`. Positive control (same topology, writable) passes both, so mode is the only axis. Second defect in the same shot: `mergeCopyDir` at `claim.js:3221` is **unguarded**, so the failure is untyped where `mirror_sync_failed` already exists for the sibling direction. **(3) C5 recorder — REFUTED on the archive-write half** (cwd-binding half survived every topology: subdirectory, linked worktree, detached HEAD, zero-commit, bare repo, and merge byte-idempotent). `--project archive` is a valid single segment → writes `kaola-workflow/archive/.cache/final-validation.md` at `outcome:"recorded"`, exit 0 — **and closure-audit then reports a permanent, unrepairable `archive_content_incomplete` for a phantom project `.cache`**, i.e. it manufactures exactly the contamination #903 exists to remove. Second route: `--output` has **no band check**, so `--output kaola-workflow/archive/issue-9/.cache/injected.json` writes into a real closed archive. Directly violates the "must never write inside `kaola-workflow/archive/**`" requirement I set. **(4) C1(a) blob gate TOO AGGRESSIVE and permanently bricking.** `.trim()` on `-z` output (`sink-merge.js:1357`, `:1368`, and `ignoredArchiveEvidence` in `claim.js`) destroys leading/trailing whitespace that `requiredArchiveFiles` preserves from `readdirSync`. Under the exact `.cache/` shape an archive file `notes.md␠` is excluded from `forcePaths` (so the repair never runs) and then refused over: `exit=1 sink_incomplete archive_missing_paths:[".../.cache/notes.md "]` — and **three consecutive re-runs give the identical refusal, so the run can never be sinked.** Verified `git ls-files -z` emits the space raw (`od -c`). Non-ASCII, embedded newlines, nested dirs and 0-byte files all survive. **(5) C1(b) blob gate FALSE PASS.** The comment justifying the symlink exclusion ("neither becomes a blob") is **measurably wrong** — `git add -f` stages a symlink as `120000 <sha>`. A gitignored symlink in the archive yields `steps.archive_commit:"done"`, exit 0, `status:sinked`, `archive_missing_paths` undefined, and `fresh clone holds the symlink? false`. **C4 scoped verdict SURVIVED** (offline `clean=false` with an online positive control giving `clean=true`; prefix collision, `.archived-<ts>` as `--project`, both unreadable-dir axes all behave) — but with two adjacent defects and one fragility: **(a)** a mistyped `--project` **plus any `--issue`** answers `current_project_clean:true` with `state_file:null`, contradicting `resolveScope`'s own comment; **(b)** `archive_name_ambiguous` misses two timestamped siblings when no bare `P` exists (fail-closed direction); and the offline defence is **indirect and single-point** — `probeIssueState` returns `open` offline so three remote classes read empty-for-the-wrong-reason, and `false` is held only by the two classes that token `skipped_offline`. Pre-existing and not ours, recorded: a space-named archive file blocks the sink at **preflight** (`sink_blocked`, exit 1) where a plain-name control sinks cleanly, because `git status --porcelain` C-quotes such paths and the prefix match fails (the #429/#715 family). Stated attack gaps: C4 online-with-a-lying-`gh` and scoped `--execute`; C5 under `GIT_DIR`/`GIT_WORK_TREE`.
  dispatched: two reviewers concurrently, both read-only on tracked files, dispatched now because **all production code is final** (`impl-parity` was the last production change) while tests and docs are still in flight — `code-reviewer` subagent `adv-review` for correctness/regression/scope over the canonical production diff → `.cache/adv-review.md`; `adversarial-verifier` subagent `adv-guards` to attack the five specific guard claims (sink blob gate, disposal/destruction gate, `pending_mirror` prediction, scoped fail-closed `current_project_clean`, recorder cwd-binding) → `.cache/adv-guards.md`

- item: Propagate prose to every generated surface and document the change — `templates/routing/` skeletons then regenerate (never a rendered surface), plus `README.md`/`docs/api.md`/`CHANGELOG.md` under `[Unreleased]`. Note: `docs/api.md` is test-consumed, so editing it stales the receipt — write docs BEFORE the receipt run.
  status: todo

- item: Repair the `adv-guards` refutations. Grouped by file so no two owners share one. **Every fix must land in all four editions.** Ranked by severity above; C2, C3 and C5 are the blocking ones.
  status: in-flight
  attribution: **I measured whether C2's data loss is ours: it is NOT.** The `mainLive` delete exists at baseline `9b68b096:2499-2503` (`fs.rmSync(mainLive, …)` guarded only by `realpathSync(mainLive) !== dest`), and this bundle's nearest hunk is `:2511` — so #901 added its sidecar re-check **beside** a pre-existing unguarded delete and scoped the new check to `src` only. It is a pre-existing data-loss defect that our new gate sits next to and gives **false assurance** about. Three consequences carried to `fix-claim-sink` by message: the CHANGELOG must not claim #901 fixed it nor blame this bundle for it; the blast radius is wider than #901 because `release`/`discard`, `watch-pr` on MERGED and the abandon backstop have been deleting main's copy silently for a long time, so a fix must not start refusing ordinary runs; and if an existing test **pins** the current delete behaviour, that pin and this fix genuinely conflict and the decision is mine, not the implementer's. It is still fixed here — a defect found while working gets fixed even when the fix adds code, and destruction is the one place refusal is legitimate in this design. Shape guidance given: extend the **existing** comparison to cover both live copies rather than add a third comparison, since two comparisons drifting apart is how the original hole formed.
  fix-audit: **DONE, both defects, all four copies, published contract UNCHANGED.** Notes `.cache/fix-audit.md`. **D1 reproduced ONLINE only** — offline the same argv reads `clean=false` for the unrelated `skipped_offline` reason, so an offline repro would have shown nothing. It took the second option I allowed rather than exit 1, correctly: `docs/api.md:960` publishes exit 1 only for an unresolvable `--project` **with no `--issue`**, and three suites pin the accepted form. So `resolveScope` now carries `project_unresolved` (`:194`, `:204`), `driftIsClean(drift, scope)` returns false on it (`:595`, scope kept optional so the 4 single-arg unit pins retain their meaning), and `scopeOut` gains it **omit-when-false** (`:699` — load-bearing, because both forge suites exact-key-pin the 3-key resolvable scope). Result: `clean=false`, `project_unresolved:true`, exit 0. Controls, all online: `--issue` alone → `clean:true`; **resolvable** `--project` + `--issue` over zero drift → `clean:true` (rules out an always-false verdict); typo without `--issue` → exit 1, empty stdout; scoped `--execute` with typo + `--issue 701` → still repairs `[701]`. **D2 was two broken halves, not one**: `archiveNameIsAmbiguous` now counts folders matching by any of the three name shapes (`:222`), and `annotateAttribution` (`:582`) no longer keys on `finding.project === scope.project` — **keyed on the bare name it could only ever stamp the bare-`P` half, so a timestamped sibling read clean even when the flag fired.** Negative controls: single archive, and `proj-a` beside `proj-a-extra` + `proj-a.something` → not flagged. **Byte-identical unscoped proven across 4 modes × 3 sources, 12 digests identical**, with "before" taken from a pre-fix scratch mirror whose `reverse.js` **fails unless each of 11 edits hits exactly once** — nothing reverted in the worktree. Suites: walkthrough full scope **198/198** exit 0 with all 14 `…903` scenarios passing, both forge script suites 0, both forge walkthroughs 0, codex plugin walkthrough 0, `validate-script-sync` 0, `test-spawn-classification` 0. **Offline defence remains INCIDENTAL, not direct** — `project_unresolved` conditions the scope, not the remote classes, so `clean=false` offline is still held only by the two tokening classes. Recorded, not built, per my earlier ruling. **Trap it passed on, and the reason I routed an online pin rather than an offline one:** its first online mock keyed on `gh`'s `issue view`, but **gitea's CLI says `issues view`**, so gitea's online axis was silently dead and its positive control read `false` where it had to read `true` — *the control disagreeing with itself is what caught it.*
  fix-audit-followups: pins routed to `tdd-parity` (resumed) with the vacuity trap stated explicitly — **the existing `runClosureAuditRaw` hardcodes `KAOLA_WORKFLOW_OFFLINE=1`, under which the D1 assertion passes against the defect**, so pin 1 must be built on an online path with a resolvable-project `clean:true` leg beside it, and the mock must be verified live in **both** forge editions. Prose routed to `impl-prose` (resumed): `docs/api.md:957`, `templates/routing/finalize.skeleton.md:481` (authoring surface → regenerate 18 → sync 6 editions) and `CHANGELOG.md:64-67` all now understate the rule; no shipped surface passes `--project` with `--issue`, so no rendered flow changes behaviour.
  fix-runner-V1: **DONE — the run's blocking defect is closed.** md5 ×4 `9fef450853c44ce7e5d731a29ee50c99`; 175 added / 22 removed across all four defects, 1363 → 1516 lines, no test file touched. **It verified the premise from source before touching anything** rather than accepting my report: `probeFinalizeValidationGate` (`claim.js:3609-3614`) reads the record from `<authorityDir>/.cache` and hashes `resolveFinalizeCheckRoot(root)` = the invoking tree, while `predictFinalizeAuthority` (`:3548-3561`) makes the authority **main's** run folder in the `pending_mirror` topology — its own comment says so. Premise held. **Fix: the hash follows the invoking tree, the record follows the run folder, resolved separately because the gate reads them separately.** New `resolveRecordFolder` (`:1105-1124`) checks this tree then main via the already-used `schema.resolveMainRoot` (main is not derived a second way); `resolveCandidateRoot` untouched so the cwd-binding property survives; and the fallback is deliberately **one-directional** — a folder living only in a linked worktree is never written from main. **Acceptance measured twice, including through the real CLI**: `record` from the worktree then `finalize --check --keep-worktree` from that worktree, `authority.source: "pending_mirror"` → `checks.validation: "chains_green"`, `ok: true`, `reasons: []`; and it reproduced both pre-fix outcomes end-to-end on the same fixture (record-from-worktree → refused; record-from-main → `final_validation_stale`). **The controls that make it non-vacuous**: the gate standing in **main** over the same bytes reads `final_validation_stale`, proving the bound value is the worktree's and not one that reads green anywhere; after the **real** Step 8a the mirrored worktree copy also reads green; **no run folder is created in the worktree**, so the hand-create workaround I told it not to design around is verifiably not taken; T8l's arm unchanged; plain repo unchanged; and `project_folder_missing` now names **both** searched paths, so the looping hint is gone. **Three mutation controls** in a scratch mirror: MV1 (no main fallback) → back to exit 1, so the fallback IS the fix; **MV2 (hash the destination instead of the invoking tree) → `final_validation_stale`**, demonstrating the "just hash main" wrong answer was not taken; MV3 (no band check on the resolved destination) → writes into main's band. **It found and closed a defect its own fix created**: the fallback makes main's band reachable where it wasn't, so `--project Archive` from a worktree with no local archive while main has one is now refused at exit 2 by a band check on the **resolved** destination (leg W9, proven armed by MV3). **V5 fixed as the comment, not the logic**, and I accept its reasoning: the call is kept because it is the same call the gate makes over the same value, so a divergence cannot be introduced there without both sides moving; the comment now states plainly that it resolves nothing today and does not cover the gate's own redirect case. Removing it would decouple producer from gate for no behavioural gain. **Contract unchanged** — no new token, no new key, no exit-code change; the one observable change is the fix itself, plus `operator_hint` naming which tree was hashed, where the record landed, and *do not create the run folder here by hand*. Suites after V1/V5, serial: `test-validation-runner` 0, `test-finalize-door` 0 (208), `validate-script-sync` 0, walkthrough **full scope 0 — 198/198**, both contracts validators 0. Recorded not built: a full `cmdFinalize` (not `--check`) in the worktree lane, and **two linked worktrees carrying the same project** — `resolveRecordFolder` looks at this tree and main only. Follow-ups routed: five V1 pins to `tdd-finalize` (with pin 2, the stale-from-main control, flagged as the one without which the set is decorative), and one recipe sentence to `impl-prose` (the record lands in the main-resident run folder — an addition, since nothing in the recipe is wrong now).
  fix-runner-C5: **DONE, contract unchanged.** Notes `.cache/fix-runner.md`; md5 ×4 `b958042d88ca9b9a58cc1796796ad2d2`. It found **three routes I had not given it**: `--project Archive`/`ARCHIVE` reach the same real directory on this case-insensitive filesystem, and a fifth `--output` route — `--output <main>/kaola-workflow/archive/…` **invoked from the linked worktree** — which killed its first cut, because that cut checked against the record's candidate root while the durable archive lands in **main** first. The check now resolves against the working tree that **owns the output path**, which is the same asymmetry that produced V1. Band predicate (`:1109-1181`) is expressed as a **location**, not a name blacklist: `archiveBandRoot()`, literal containment over a `..`-normalized/symlink-followed path, then filesystem **identity** (dev+ino) of the target or any ancestor — identity rather than a case-insensitive compare because it **measured** that `fs.realpathSync` on darwin does not canonicalize case, and a case-insensitive compare would over-reject on a case-sensitive filesystem. All rejections happen **before** anything is written; stdout measured 0 bytes. **Register: it declined to mint a fourth `reasons` token**, reusing the published exit-2 usage register, with the argument that settles it — `project_folder_missing`'s remedy is *positional* ("record from the worktree") while `--project archive` is *categorical*, so reusing it would ship a report whose remedy is unreachable (the same unactionable-hint defect this bundle removed from two other hints); and `--output` cannot use the exit-1/`inconclusive` register at all, since refusing before the write means there is no measurement to report. Confirmed by me. Controls held including the one that matters — `--project issue-archive-cleanup` is **not** over-rejected — plus 4 mutation controls each regressing exactly one route. Suites: `test-validation-runner` 0, `test-finalize-door` 0 (208), `validate-script-sync` 0, walkthrough **198/198** (the +1 over my stated 197 is `tdd-parity`'s new scenario — attributed by me, not a red), both contracts validators 0. **Two decisions it escalated, both answered "do not build":** (1) a band-shaped directory belonging to **no** git repository still receives the `--output` JSON — not closed, because closing it requires the name-matching-with-nothing-to-anchor-it blacklist shape I warned against, and with no repository there is no durable archive to corrupt, so none of the actual consequence can occur; the dangerous half (a non-git cwd pointing into a real repo's band) IS refused. (2) No export of `isArchiveBandPath` — pins drive the shipped CLI, which is the faithful level since the `--output` check lives in `main()`. **V1 remains outstanding and is the run's blocking defect**; confirmed it has the brief.
  dispatched: three implementers concurrently — `fix-claim-sink` owns `claim.js` + `sink-merge.js` ×4 (C2 mainLive scope hole, C3 writability probe + unguarded `mergeCopyDir`, C1a `.trim()` deadlock, C1b symlink false pass) → `.cache/fix-claim-sink.md`; `fix-runner` owns `validation-runner.js` ×4 (C5 archive-band violations on `--project` and `--output`) → `.cache/fix-runner.md`; `fix-audit` owns `closure-audit.js` ×4 (C4a false `clean:true` on a mistyped `--project` with `--issue`, C4b `archive_name_ambiguous` blind to timestamped-only siblings) → `.cache/fix-audit.md`

- item: **Owner-directed, OUTSIDE the four issues' scope**: write the run's design conclusion into the durable philosophy. The user proposed the formulation — *give the agent the goal or the form of the result we want, check whether it did it correctly, and do not care how, because when it gets it wrong it adapts* — and asked for it recorded.
  status: done
  dispatched: self
  result: **Added as `## Specify the result; the method is the agent's (#900–#903)` in `docs/conventions.md` (+44 lines)** — the house home for a rule plus the observations that forced it. It records the measured asymmetry (result statements held without exception; **mechanism claims failed four times out of four** — the impossible on-disk construction, `--materialize-kernel` not carrying the file, four-vs-five sidecars, two non-existent test paths — each corrected by the receiving agent precisely because nothing obliged it to comply); the limit of self-correction (**adaptation covers visible error, and the two defects that survived every suite were invisible from inside the task** — an unreachable guard condition, and a fixture with the topology inverted); and the two conditions that make "don't care how" affordable — **the result must be falsifiable**, and **the check must not come from the doer**, which is the test-custody rule one level up. Plus the two corollaries live in this run: mutation-proving a guard proves it is armed, never that its condition is reachable; and a control that agrees with its positive leg is a signal to check the control. **`CLAUDE.md` was NOT changed, and that is a decision awaiting the user.** I added a two-line pointer, and it tripped `validate-workflow-contracts.js` — the assert is `read('CLAUDE.md').split(/\r?\n/).length < 200`, which with the trailing newline means **`wc -l` ≤ 198**, and the file was already at exactly 198. So there is **zero headroom**: putting this rule in the orchestrator's primary surface requires **evicting an existing rule**, which is a value call I will not take silently. I reverted my edit precisely (`git diff -- CLAUDE.md` empty, back to 198) rather than deleting someone else's rule to insert mine, and `validate-workflow-contracts.js` returned to exit 0. **RESOLVED — the user authorized compressing `CLAUDE.md` by removing or redirecting lesser content, and the rule is now in it at 197 lines (one line of headroom, down from 198).** Three surgical changes, none deleting a rule: (1) merged the two `### Nothing refuses` paragraphs, −1 line, no content lost; (2) condensed `## Key Scripts` from six two-line bullets to three combined bullets, −2 lines — signatures and subcommands are fully in `docs/api.md`, which the Documentation Map already points at; (3) **folded the new rule into the EXISTING `docs/conventions.md` pointer bullet rather than adding a second pointer**, so the Validation Policy now reads "a guard reads what ships … ; a threshold cannot see a rule beneath its bar; and **specify the result, never the method** — a mechanism claim in a brief rots and makes the agent wrong, where the same fact as evidence only makes it check; affordable exactly when the result is falsifiable and the check is not the doer's." Both `validate-workflow-contracts.js` and `validate-kaola-workflow-contracts.js` exit **0**. Guard-pinned content untouched (the Durable State Contract's five literals and `active folders` were never edited — a scare in my own verification was my grep, which was case-sensitive and treated `issue-*.md` as a regex; the file says "Do not purge" capitalized). Also still owed: a `CHANGELOG.md` line for this docs addition, since it is user-visible and outside the four issues — to be routed once the CLAUDE.md question is settled.

- item: `impl-prose`'s accumulated four-item prose pass — `docs/api.md:211` (the transaction owns the project-folder sync in **both** directions, typed `mirror_sync_failed` either way), the `project_unresolved` understatement across `docs/api.md` + `finalize.skeleton.md` + `CHANGELOG.md`, a `[Unreleased]` bullet for the new `docs/conventions.md` section, and one recipe sentence saying where the record lands.
  status: done
  dispatched: `impl-prose` resumed via message ×3 across the run; it went idle without the enumeration I asked for, so I verified every item directly
  result: **ALL FOUR LANDED, verified by me.** `docs/api.md:211` now reads that the transaction "owns the project-folder sync itself, in **both** directions — worktree→main and main→worktree", with either direction typed `mirror_sync_failed` and failing closed "so a sync the script cannot perform is a refusal the operator can read rather than an untyped crash". `project_unresolved` present in all three surfaces. `[Unreleased]:204` carries the conventions-section bullet, kept separate from the four issue entries as instructed. The recipe now states: *"The record itself lands in the run folder the gate reads it from — on a worktree run that is the main checkout's rather than this tree's, because the gate takes the record from the authority folder and hashes the tree its own shell is in — so `record_path` is where to look for the file."* Verified: `generate-routing-surfaces --check` → all 18 surfaces byte-match, exit 0; `[Unreleased]` hashes **exactly** `#900 #901 #902 #903`; and **all 12 rendered surfaces (6 tracked + 6 dot-dir editions) carry the new landing sentence**. **Lesson for the run record, and it is mine: I mis-measured this three times in one check.** My greps reported item 1 absent (the text says "in **both** directions" — markdown bold inside my needle), item 4 absent (it says "the main checkout's", not my guessed "main-resident"), and earlier two guard-pinned `CLAUDE.md` literals absent (case-sensitive needle plus `*` read as a regex). Every one was a **false negative from a weak measurement stated confidently** — the same root as the four mechanism claims that the new `docs/conventions.md` section was written about, committed by me twice more within the hour of writing it. **Read the text; a grep is a hypothesis, not a measurement.**

- item: `tdd-parity` round 2 — pins for the two `fix-audit` repairs (the false `clean:true` on a mistyped `--project` with `--issue`, and `archive_name_ambiguous` blind to timestamped-only siblings), across all three editions.
  status: done
  dispatched: `tdd-parity` resumed via message; notes appended as § ROUND 2 of `.cache/tdd-parity.md`
  result: **DONE — 16 pins across three editions, 13 baseline-red, 3 mutation-armed.** The 3 mutation-armed are the **mock-liveness** assertions (one per edition): no state of the shipped code can red them, only a broken mock can, so they are armed by breaking one. **The vacuity trap I warned about is measured, and it is worse than "passes":** same argv, same fixture — PRE-FIX **online** gives `clean=true` with `project_unresolved` **absent** in all three editions, while PRE-FIX **offline** gives `clean=false`. So a pin written through the `OFFLINE=1` runner would have asserted the correct value **against the broken code**. Every verdict leg now goes through `runClosureAudit` with `OFFLINE=0` explicit and a mock on each forge's own hook. **It proved the online axis LIVE in both forge editions by BREAKING it rather than by a green** — rewriting only its own shim (anchored on its following line, since the suites reuse that shim text elsewhere) to the wrong verb per edition, and getting reds whose signatures name the failure (`stale_in_progress_labels must be an EVALUATED array … got: {}`; `an extra class here means the mock never answered`). gitea's shim uses `issues view`/`issues list` per that suite's convention, with the trap written into the comment. **Three legs were masked and it isolated each rather than assuming transfer:** the verdict leg sits behind the scope-key assertion which reds first pre-fix (isolated by a one-line partial revert of the call site → RED with `got: true` in all three); the `.discarded-*` suffix shape never runs because the loop reds on `archived` first (measured directly: pre-fix flag absent + `name_match`, shipped `true` + `ambiguous_name_match`); and the stamp half of D2 sits behind the flag assertion (one-line revert → RED on the timestamped sibling in all three **with the flag still true**, i.e. the two halves of one report disagreeing). Positive controls per pin, including a resolvable `--project` + same `--issue` over zero drift reading `clean=true` with a **three**-key scope — the four-key variant is on the unresolved case only, so existing exact-key pins are untouched — plus a D2 negative that rules out an always-ambiguous stamp. **A METHOD HAZARD IT CAUGHT THAT COULD HAVE CORRUPTED MY OWN NUMBERS: its bash cwd is not stable across calls, and the main root holds a PRE-BUNDLE copy of all three files** (no `runClosureAuditRaw` at all). Two of its measurements had silently read main — a spawn-site grep appearing to show a deleted site, and a `test-spawn-classification` run reporting 591/169. Corrected with absolute paths: its three files are 115/63/62 spawn sites, **identical** to before its edits, and the guard is **603 sites / 176 classified**, exit 0. My own commands each begin with an explicit `cd` to the worktree and my file edits used absolute worktree paths, and this agent independently confirms it: it observed the dirty count move 42 → 44 for `CLAUDE.md` and `docs/conventions.md` — my edits, landed in the worktree, verified by a third party. Suites, serial, absolute-path re-runs: walkthrough **0** (198/198, 2079 spawns, +20 accounted), gitlab **0** (15× `903: PASSED`), gitea **0** (15×), both forge walkthroughs **0** at 112/127 unchanged, `validate-script-sync` 0, `test-spawn-classification` 0 (no new sites, no ceiling touched), `validate-workflow-contracts` 0, `test-shard-lib` 0. No implementation defect. It declined to export `archiveNameIsAmbiguous` for a unit pin — "I would not widen a module surface for a test's convenience" — consistent with my two earlier rulings; the four CLI legs cover the count rule.

- item: `tdd-finalize` round 2 — pins for the D1 destruction gate and the D2 writability probe.
  status: done
  dispatched: `tdd-finalize` resumed via message; notes appended to `.cache/tdd-finalize.md`
  result: **DONE — `test-claim-hardening.js` 524 → 557 assertions; cumulative 1314 insertions across three test files, no deletions.** True baseline `9b68b096`: **39 reds** (the original 20 plus 19 new). **The D2 writable-dest control (G1) is in and is the whole arm** — same `mk902` fixture with `wt/kaola-workflow/` present and writable, one bit different from G2: `--check` exit 0, `ok:true`, `mirror:'ready'`, `workflow_state:'pending_mirror'`, `reasons` empty with `archive_authority_missing` absent, transaction exit 0. Its own message states the consequence: *if this arm ever reds, the writability probe has become a blanket refusal and the #902 conversion is undone.* Green under both D2 mutants. **`pd2a` and `pd2b` red disjoint assertion sets — the two D2 halves are separately load-bearing and neither masks the other, measured rather than asserted**; and `pd1` leaves L1–L3 untouched, so the three must-not-refuse legs are not coupled to the new term. G2 carries the uid guard plus an in-arm `accessSync`-must-give-EACCES assertion before anything downstream is trusted, and its load-bearing transaction assertion is that **the envelope parses** — a reason-only assertion passes on `null`. `archive_authority_missing` reappearing on G2 is deliberately NOT asserted absent, with a comment saying why, so nobody "fixes" it later. Every envelope value was measured in its own fixture first rather than transcribed: the D1 refusal carries `missing` **top-level**, no `mismatched`, no `archived`. **NEAR-MISS IT CAUGHT IN A ~40-SECOND WINDOW: `repoWideIgnoredNames` was CALLED at `claim.js:2534` and DEFINED NOWHERE in any edition** — a `ReferenceError` that `archiveProjectDirSafely` converted into `archive_exception`, so every linked-run archive with a disposable main copy failed, `release`/`discard`/`watch-pr` were fully broken, and the D1 gate was unreachable behind the crash. It failed *safe*, but **had it been committed the bundle would have shipped a dead `release`.** Since resolved; **I verified independently (explicit `cd`, `pwd` printed): defined once and referenced 3× in all four editions, all four `node --check` clean** — with the caveat that a parse check cannot catch a `ReferenceError` inside a function body, so the definitive evidence is the runtime exercise in `test-claim-hardening` (its D1 legs drive `archiveProjectDir`) and the final verification below. **V2 landed mid-pass and its arms needed no repair** — `destAbsent` became `destAuthorityAbsent` and the prediction widened to `state_missing`, but its arms assert **behaviour** (`pending_mirror`, empty `reasons`, real `validation`) and never the internal bit's name, so they survived a rename of the very bit a mutant had proved load-bearing. Nothing was rewritten to keep passing — the custody rule working as intended. Suites, serial, explicit `cd` with `pwd` printed: claim-hardening 0 (557), finalize-door 0 (208), validation-runner 0, bundle-finalize 0 (149), spawn-classification 0, walkthrough **full scope 0, 198/198**.

- item: **RUN-WIDE MEASUREMENT HAZARD — two independent agents hit it, and it changes how final verification must be done.** The Bash cwd **resets between calls**, and the **main root holds the pre-bundle copy of every file at `9b68b096`**. So a command like `node scripts/test-…` without an explicit `cd` silently exercises **main's** copy, not the worktree's. `tdd-finalize` caught it because two runs reported `461 assertions / 668 spawns` — the *exact* baseline numbers — while the worktree file provably carried its new arms; `tdd-parity` caught its own via a spawn-site grep that appeared to show a deleted site and a guard count of 591/169 instead of 603/176. **Consequence: any suite result reported anywhere in this run that did not `cd` may have measured main.** Both agents re-took every number with absolute paths.
  status: done
  dispatched: self — recording a hazard, and setting the rule for the item below
  result: **Rule for final verification, and for any future run in a worktree: every verification command begins with an explicit `cd` to the worktree and prints `pwd`, and the tell for this failure is a count that exactly matches the recorded baseline.** My own commands have each begun with an explicit `cd` and my file edits used absolute worktree paths — independently corroborated by `tdd-parity`, which observed the dirty count move 42 → 44 for `CLAUDE.md` and `docs/conventions.md`, i.e. my edits landed in the worktree and were seen there by a third party rather than asserted by me. **I will re-run the full gate myself after all agents stop rather than aggregate their reported greens**, because a green measured against `9b68b096` is indistinguishable from a green measured against the bundle except by the count.

- item: `tdd-sinks` round 2 — pins for the `.trim()`-on-`-z` non-convergence (D3) and the symlink false pass (D4), across the canonical sink suite and both forge sink suites.
  status: done
  dispatched: `tdd-sinks` resumed via message; it went idle without a summary, so I verified the locator directly (explicit `cd`, `pwd` printed). Notes at `.cache/tdd-sinks.md`, now 26 KB
  result: **DONE, and the D4 pin is the right shape — it names why the obvious assertions are useless.** Its own words: `fs.existsSync` and `steps.archive_commit === 'done'` were **both satisfied by the defect**, so neither is used. (y8)/(g) instead assert, in order: the symlink is an entry in the **published commit at `HEAD` with mode `120000`**, read through `ls-tree -r -z` with NUL-split only; `archive_forced_paths` names it; **a `git clone` of the pushed bare remote materializes it** — `lstatSync(...).isSymbolicLink()` true **and** `readlinkSync(...) === 'plain.md'`; and the **clone's own HEAD** carries it as a blob. Clause 3 is the one the defect failed, and it is disk — but the disk of a *fresh clone built from the commit*, with the run's own working tree never consulted, which is precisely the distinction that makes it not a repeat of the vacuous check. Verified present by me in all three suites (`'clone'` 1×, `lstat` 2×, `ls-tree` 8× canonical / 5× each port). Baseline for the reds is worktree HEAD + one expression (`mut-D3`/`mut-D4`) against release baseline `9b68b096`; under `mut-D4` all 6 clauses report as assertions — a gap it measured and then fixed rather than leaving. (y7)/(f) is explicitly framed as the **over-refusal** direction, where the naive fix is "stop requiring awkward names", so its five controls guard that. The `.git`-named-file case is left unpinned per my instruction — pre-existing, and the type-agnostic skip only avoided adding a second instance of the D3 non-convergence shape. *(Method note for the record: my first check used `git clone` as a literal needle and read 0 in all three files; the call passes `'clone'` as an array argument. Sixth false negative of the run from a grep-as-measurement — the rule now in `docs/conventions.md` keeps earning itself.)*

- item: **RETRACTED FINDING — `test-claim-hardening.js` does NOT make real network calls.** `fix-claim-sink` reported that the suite "makes real `api.github.com` calls and hits rate limits while passing", and I relayed it to the owner as a finding without measuring it. The owner corrected it; I then proved it false.
  status: done
  dispatched: self — measured with a PATH shim, the technique this repo's own rule prescribes
  result: **The suite is hermetic. Proven, not argued.** I placed a poisoned `gh` on `PATH` that exits 97 and appends to a breach log on any invocation, then ran the suite from the worktree (explicit `cd`, `pwd` printed): **`SUITE_EXIT=0` and the poisoned `gh` was never invoked** — zero breach entries. Supporting: the suite carries **26** `KAOLA_WORKFLOW_OFFLINE` references and **33** `binDir`/`PATH` shim setups, i.e. it is deliberately hermetic by construction. The three `api.github.com` occurrences are **in-suite fixture strings** (mock response bodies), which is the likely source of the misread — output containing an API URL and a rate-limit-shaped message looks identical to a real call in a log. **Scope of my proof, stated honestly: it poisons `gh` only, so it disproves the specific claim (a `gh`/API call producing a rate limit) rather than proving the suite touches no network by any route.** Two lessons, both mine: this repo's recorded rule is **prove suite hermeticity with a PATH shim, never diagnose network access from log text** — the agent broke it, and I compounded it by **relaying an agent's unverified claim to the owner as an established finding**. A subagent's report is evidence to check, not a measurement to forward.

- item: `tdd-finalize` round 3 — the five V1 pins (worktree-lane end-to-end, the stale-from-main control, the one-directional property, W9 through the fallback, and the plain-repo regression guard).
  status: done
  dispatched: `tdd-finalize` resumed via message; notes appended to `.cache/tdd-finalize.md`
  result: **DONE — `test-finalize-door.js` 208 → 233 assertions; cumulative 1529 insertions across three test files, no deletions.** **Pin 18 is in as `T8m(W3 control)` and is proven non-decorative**: the same bytes read with the gate standing in **main** → `final_validation_stale`, `recorded_candidate_hash === wtHash`, `current_candidate_hash === mainHash`; and under **MV2** (hash the destination instead of the invoking tree) it **inverts to green**, so it is the assertion that *identifies* the wrong answer. Its own framing is the point: without it MV2 would still trip the other legs, but nothing would say **why**. **True baseline `9b68b096`: 40 reds in this suite, and nothing in the pass is baseline-green-by-design** — the verb does not exist at baseline, so every V1 assertion depends on it. Three one-line mutants: MV1 (fallback removed) → **12 reds**, the fallback is the fix; MV2 → 5 reds including W3 inverted; MV3 (destination band check removed) → 3 reds all on W9, including "nothing written into main's band", i.e. the mutant writes into it. MV1's reach into W9 is correct rather than collateral — W9's exit-2 exists *because* the fallback made main's band reachable, so removing the fallback removes the route and the token together. Pin 17 asserts the bound hash `=== wtHash` **and `!== mainHash`**, no run folder created in the worktree, `operator_hint` naming both the folder written and the tree hashed, then the real CLI end-to-end (`finalize --check --keep-worktree` → exit 0, `ok:true`, `reasons:[]`, `authority.source:"pending_mirror"`, `checks.validation:"chains_green"`). T8l is **unchanged** with only its comment hardened to say why it must never become a success case — a symmetric fallback would reintroduce the wrong-tree binding in mirror image. W9 also enumerates main's band **byte-unchanged** before/after. **A METHOD CATCH WORTH KEEPING: `T8m(W5)`'s record read was unguarded, so MV1 CRASHED the suite there and hid T8n entirely — 8 visible reds instead of 12, and the first MV1 run *looked like a complete result*.** A mutation run that aborts early under-reports while appearing to have finished; it guarded the read and re-ran. Same trap it hit on T8/T8l in its first pass, so twice in one run. Two gaps recorded not pinned, both with reasoning I accept: a full `cmdFinalize` (archive + closure) in the worktree lane, where the extra property over the real `--check` CLI is the archive/closure half that `#902` arm A already pins on the self-host fixture; and two linked worktrees carrying one project, where a pin would assert the **absence of a route nobody asked for** — a pin against a mechanism that does not exist, so recorded per additive derivation. Suites, serial, explicit `cd` with `pwd`: claim-hardening 0 (557), finalize-door 0 (**233**), validation-runner 0, bundle-finalize 0 (149), spawn-classification 0, walkthrough **full scope 0, 198/198**.

- item: `fix-claim-sink`'s two outstanding asks — the `stagedPathsUnder` `-z` fix (which I overrode my own brief scope to require, because a space-trimmed path was being written **durably** into the archive) and the D1 refusal wording (per-live-copy after the fix, but the text was singular and named no tree).
  status: done
  dispatched: `fix-claim-sink` resumed via message with both restated and their reasons; verified by me directly rather than from its summary, explicit `cd` + `pwd`
  final-report: **15 suites exit 0 on a third full pass**, walkthrough full scope 198/198 / 2079 spawns, nine mutants across three passes each regressing only its own leg, byte-lockstep pairs `cmp` 0, tree clean with no `.rej`. **IT CORRECTED ITS OWN EARLIER OVERCLAIM, which is the most valuable thing in the report: a main-only SYMLINK is NOT covered, and it had previously said it "would now refuse rather than destroy".** Measured on a clean single axis (target present in both copies, so the symlink is the only main-only entry): `.cache/link-evidence.md` → exit 1, named, main's copy retained; but **`extra-link.txt` → exit 0, archived, destroyed** and **`notes-link.md` → exit 0, archived, destroyed**. Cause: `verifyArchiveComplete` routes every symlink to `mismatched[]`, which the main leg ignores on purpose, so a symlink is caught only when its name also lands in `listSourceEvidenceFiles`' set (a `.cache/*.md` name or one of four fixed top-level names). **Not fixed, and I accept the reasoning**: separating "cannot be compared" from "bytes differ" requires changing `verifyArchiveComplete`'s return shape, which is shared and pinned, and a symlink probe would be the third reader I steered it away from — two comparisons drifting apart is how the original hole formed. Scope is genuinely narrow (`copyDir` **follows** symlinks, so archives do not normally contain them, and a main-only symlink whose target is inside the run folder loses the link, not the bytes), and it remains **strictly better than baseline, which destroyed every main-only entry silently.** Recorded for its own issue. Porting precision worth keeping: **one hunk of thirteen could not be `patch`ed to the forge editions** — their `stagedPathsUnder` keeps the `execFileSync` call on one line with an abridged comment — and `patch` **rejected it loudly rather than fuzzing it in**, so it hand-applied against each port's own anchors, **preserving both pre-existing shapes rather than converging them**. The parity claim is therefore stated exactly: added/removed sets byte-identical **port-to-port** (`diff` 0 both sides) and identical in count across all six files (217/52 claim, 73/11 sink), with canonical-vs-port differing by **exactly that one wrapped line**. Also recorded, not changed: `claim.js:2577`/`:3349`/`:3361`/`:3392`/`:5195` and `sink-merge.js:458` still split on `\n` with a trim — **none is a `-z` stream and none feeds durable evidence** (ref names and `startsWith('kaola-workflow/')` prefix classifications), but a C-quoted path would break such a prefix test, so it is a **latent instance of the same class**; pre-existing and outside the ruling.
  result: **BOTH LANDED, and the wording is better than what I specified.** `stagedPathsUnder` (`sink-merge.js:155`) now runs `['diff','--cached','--name-only','-z', …]` and returns `out.split('\0').filter(Boolean)` — so all four `-z` sites in the two files agree and the archive no longer records a whitespace-named file under a trimmed path. The D1 emit now reads "the archive copy dropped evidence **a** live project folder still held" and "**every** live project folder was left in place", delegating the remedy to a new computed `archiveIncompleteRemedy(root, project)`: *"Every live copy is compared against the archive — the run folder in the tree this command was invoked from (`<path>`) and, on a linked run, the one in the main checkout — so a named file may be held by either. Put each named file in `<path>`, then re-run: the archive is built by copying THAT folder, so a file left only in the main checkout is not carried in."* That names the tree with its resolved path, states that both copies are compared, gives the action, **and** explains the mechanism that makes the action correct — an actionable remedy rather than a restated symptom, which is the standard this bundle applied to two other hints. Awaiting its final suite report before I start my own gate: the suites are **spawn-bound**, so running mine concurrently with its verification would produce false reds in both.

- item: Two false claims in `fix-claim-sink`'s final report, both corrected — one about my verification, one a finding whose premise I had already disproved.
  status: done
  dispatched: self, then `fix-claim-sink` by message (report text only — no suites, since the four-chain receipt was running and the walkthrough is spawn-bound)
  result: **BOTH CORRECTED, and the agent's own analysis of its errors is sharper than my correction was.** (1) It had claimed my verification read the main root. My command printed `pwd` as the worktree in the same shell invocation immediately before the `sed`, so that was impossible; and the artifact that settles it was **its own prior report, which listed `stagedPathsUnder` as the fourth, still-open site twice.** D3's first pass fixed three `-z` sites (`ignoredUntrackedUnder`, `blobPathsUnder`, `ignoredArchiveEvidence`); seeing four afterwards it read the set as "landed previously" — true of three, false of the one I had just asked for. **Its reusable lesson, which is the better formulation: `:155` plus the old form was consistent with TWO explanations — wrong root, or right root before the edit — so it was never a signature of either. A tell that matches two hypotheses is not a tell.** It also retitled its suites-table probe as a **content** probe rather than a path check, so `present=0` now flags both confounds instead of only the one it had in mind. WHERE TO VERIFY kept, with the false attribution removed and the table reframed as current state. (2) The rate-limit finding is **WITHDRAWN**, refuted decisively from inside the suite it was reading: **`test-claim-hardening.js:692` writes the string itself** — `'process.stderr.write("API rate limit exceeded for user\n"); process.exit(1);'` — one of four mock `gh` scripts (`:576`, `:636`, `:653`, `:692`) the suite synthesizes on disk to exercise TLS-failure and rate-limit handling. All three `api.github.com` occurrences are inside those mocks. So **the conclusion inverts**: that log line beside a green is evidence the rate-limit arm **ran**, not that an assertion never evaluated, and the 557 assertions are not devalued. Supporting measurements: a poisoned `gh` on `PATH` was never invoked while the suite exited 0, and there are no absolute-path or non-`PATH` `gh` invocations in the suite or in `claim.js`. Its own account of the error: *"I invoked the rule against diagnosing network access from log text, then diagnosed network access from log text"* — and when asked to sharpen it, built a larger conclusion on the unmeasured premise instead of spending the one `grep` that would have refuted it. **"A sharper conclusion drawn from an unchecked premise is not sharper, it is further from the ground."** **My own share, recorded as mine: I forwarded an unverified subagent claim to the owner as an established finding, which converted an unmeasured observation into a stated fact for someone with no way to check it. That was the worst link in the chain — worse than making the claim.** The honest scope of my proof also stands: poisoning `gh` disproves *a `gh` call producing a rate limit*, not "no network by any route".

- item: **The first four-chain receipt run was DISCARDED as untrustworthy, by my own reading of its artifacts.** It exited **0** but wrote a **0-byte log** and produced **no `chain-receipt.json` anywhere in either root within the previous two hours** — and a real four-chain run cannot be silent. An exit code with no artifact behind it is precisely the class of green this bundle spent the day learning not to accept, so it was not counted.
  status: done
  dispatched: self — re-run in the background with output going directly to the task file so nothing can be swallowed by a redirect
  result: Re-run confirmed live (`START 16:25:59`, progressing), which retroactively confirms the first invocation was anomalous rather than fast. **Rule for the record: a chain receipt is verified by its artifact — the receipt file's chain list, `green`, `headSha` and code-tree hash — never by the runner's exit code.** `run-chains --help` confirms the receipt path is `<plan-dir>/.cache/chain-receipt.json` resolved at the git top level, with precedence `--output > --plan > --project > cwd default`, so `--project bundle-900-901-902-903` is required or the receipt lands where finalize never looks.

- item: **GATE GREEN — every check re-run by me rather than aggregated from agents' reports**, because two agents were caught measuring the main root's pre-bundle copy and a green measured against `9b68b096` is indistinguishable from a real one except by the count. Every command began with an explicit `cd` and printed `pwd`.
  status: done
  dispatched: self
  result: **Five validators exit 0** (`generate-routing-surfaces --check` = 18 surfaces byte-matching, `validate-script-sync`, `validate-workflow-contracts`, `validate-kaola-workflow-contracts`, `test-spawn-classification`). **Full-scope walkthrough exit 0 at 198/198 scenarios, 2079 spawns** — and critically ≠ the baseline's 184/184 / 1958, which is positive proof it exercised the bundle rather than main. **Four-chain receipt: all four chains (`claude`, `codex`, `gitlab`, `gitea`) `exitCode 0`, `accepted_red false`, `timed_out false`**; `headSha 9b68b096` (nothing committed yet, expected), `codeTreeHash 5e4b8f67…`. **The gate's own verdict: `ok=true`, `validation=chains_green`, `workflow_state=pending_mirror`, `reasons=[]`** — the #902 conversion working live on this run's own topology. **A REAL DEFECT FOUND AT THE GATE, recorded not filed** (outside the four issues the owner approved): `run-chains --project P` invoked from a linked worktree writes the receipt to the **worktree's** run folder, while the gate reads it from `authority.source_dir` = **main's** run folder — so the first verdict was `chains_unverified`. **That is exactly V1's producer/gate root divergence, fixed for the recorder in this bundle and still live for the chain runner.** Resolved by placing the receipt in the authority folder; defensible and self-proving: the receipt's content is path-independent, `kaola-workflow/` is validation-invisible (`adaptive-schema.js:1007`) so its location cannot alter the code-tree hash, the copy is byte-identical (`cmp` 0), and **the gate accepting it is the proof** — a wrong-tree or stale hash would have returned `final_validation_stale`. Two of my own method errors during this gate, both caught by reading rather than asserting: my **first** chain run exited 0 while writing a 0-byte log and producing no receipt anywhere, and I discarded it rather than count it (a real four-chain run cannot be silent — it takes ~7 minutes); and I guessed the receipt's field names (`status`, `green`) and read `undefined`, then read the actual keys (`headSha`, `workTreeHash`, `codeTreeHash`, `chains[].exitCode`, `chains[].accepted_red`).

- item: Follow-up issues for the ~19 findings recorded but deliberately not built — the owner chose four grouped issues over one-per-finding, on the reasoning that ~19 single-line issues would swamp the backlog, which is the contamination #903 exists to remove.
  status: done
  dispatched: self, after asking the owner in conversation (their tracker, their call)
  result: **Filed four: #906** destruction-safety in the archive path (the #395.4 backstop's uncompared `mainLive` delete, and main-only symlinks escaping via `mismatched[]` — both exit 0 while losing evidence); **#907** latent path-quoting readers (six non-`-z` readers where a C-quoted path breaks a `startsWith('kaola-workflow/')` test, plus a `.git`-named *file* being a permanent unclearable `sink_incomplete`, and the journal regexp's three copies); **#908** five guards unpinned by construction, each with the declined mechanism and its signature recorded so it is recoverable rather than re-derived; **#909** this repo's own pre-existing drift (#796's stale `workflow:in-progress` label, and `archive/bundle-429-434/` residue whose single **tracked** file makes it unrepairable by construction and therefore permanent in every clone).

- item: **Owner authorized finalization explicitly, in conversation.** I stated what it does before asking — closes #900/#901/#902/#903, removes their `.roadmap` sources, regenerates the mirror, archives the run folder, commits the 44-file diff and sinks to `main` (a push) — because that is issue state plus a write to the default branch, which First Principle 4 puts with the human. Consent given: "yes".
  status: done
  dispatched: self
  result: proceeding to `/kaola-workflow-finalize bundle-900-901-902-903`.

- item: Full verification — `node scripts/simulate-workflow-walkthrough.js` at full scope (the fast gate only samples a 1/12 shard), `generate-routing-surfaces.js --check`, and the four-chain receipt with `--project bundle-900-901-902-903` and `KAOLA_RUN_CHAINS_CONCURRENCY=serial`.
  status: todo

- item: Hand off to `/kaola-workflow-finalize bundle-900-901-902-903`.
  status: todo
