# Run gaps — manually swept

Five run-discovered defects, hand-recorded because the scanner keys on explicit markers and this
run's findings live in prose reports under `.cache/`. Each is a real defect and each is filed.

gap: destruction — two routes delete a live copy uncompared: the #395.4 crash-resume backstop (claim.js:4090-4092, rmSync with no comparison and no realpath check) and main-only symlinks routed to mismatched[] which the main leg ignores; both exit 0 while losing evidence
gap: path-quoting — six non-`-z` readers remain (claim.js:2577/3349/3361/3392/5195, sink-merge.js:458) where a C-quoted path breaks a startsWith('kaola-workflow/') classification, plus a `.git`-named file being a permanent unclearable sink_incomplete, plus the journal regexp having three copies
gap: coverage — five guards unpinned by construction, each with the declined mechanism recorded: the archive-incomplete emit needing missingArchiveSidecars extracted, two-linked-worktree topology, full cmdFinalize in the worktree lane, the codex closure-audit copy having no suite, archiveNameIsAmbiguous unexported
gap: drift — this repository carries two pre-existing closure-audit findings that belong to no active work: issue #796's stale workflow:in-progress label, and archive/bundle-429-434/ residue whose single tracked file makes it unrepairable and permanent in every clone
gap: producer-gate — run-chains --project P from a linked worktree writes the receipt to the worktree's run folder while the finalize gate reads the authority (main's) folder, so a correct four-chain-green receipt classified as chains_unverified; same producer/gate root divergence as #900, unsplit for the chain runner
