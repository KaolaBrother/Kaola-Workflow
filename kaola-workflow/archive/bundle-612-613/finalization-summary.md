# Finalization - Summary: bundle-612-613

## Delivered

Two independent test-hygiene/hardening fixes from the 2026-07-04 post-completion audit of #585-#611:

- **#612** — the run-progress mirror (`writeRunProgressMirror` in `kaola-workflow-adaptive-node.js`,
  mirrored across all 4 editions) now fails CLOSED instead of fail-open: it only writes when
  `mainRoot` is trustworthy (affirmatively resolved from `workflow-state.md`'s `main_root:` field,
  or a fallback root that demonstrably already owns the project via a present `workflow-plan.md`).
  Originally scoped as a test-fixture-only fix; the fixture escape did not reproduce, and after
  exhaustive investigation the scope was widened (user-approved) to a genuine production hardening.
  During the widened investigation, a real reproduction of the escape class was found and pinned
  (a linked worktree whose project exists only there; `mainRoot` falls back to a foreign checkout).
- **#613** — two closure-audit walkthrough cases raced a real 300ms OS timeout against a hung
  subprocess shim, flaking under CPU contention. Fixed by making the shims self-terminate via
  `SIGTERM` deterministically; the label-removal case's must-succeed call also got a
  contention-proof budget. Extended to 4 sibling timeout-probe cases sharing the same root cause
  (transparently reported, reviewed, no findings).

## Files Changed

- `scripts/kaola-workflow-adaptive-node.js` + 3 edition mirrors (`plugins/kaola-workflow/`,
  `plugins/kaola-workflow-gitlab/`, `plugins/kaola-workflow-gitea/`) — #612 fail-closed guard
- `scripts/test-adaptive-node.js` — #612 regression fixtures (T612-escape, T612-legit)
- `scripts/simulate-workflow-walkthrough.js` — #613 SIGTERM-shim fix
- `plugins/kaola-workflow-gitea/scripts/test-gitea-workflow-scripts.js` — #613 mirror
- `plugins/kaola-workflow-gitlab/scripts/test-gitlab-workflow-scripts.js` — #613 mirror
- `CHANGELOG.md` — entries for both fixes

`kaola-workflow-adaptive-schema.js` (byte-identical-×4 anchor) confirmed untouched across all 4
copies.

## Test Coverage

No formal coverage tooling in this repo (Node scripts, no coverage instrumentation — see CLAUDE.md
Commands). Correctness evidence instead: `scripts/test-adaptive-node.js` grew to 1399 assertions
(2 new fixtures); `scripts/simulate-workflow-walkthrough.js` + both edition test suites green,
including the two originally-flaky #613 cases passing reliably under a 72-CPU-burner stress test
(480/480 correct vs. pre-fix 160/160 fail under the same load for label-removal).

## Final Validation Evidence

- Adaptive barrier gates (`--resume-check`, `--gate-verify`, `--barrier-check`, `--verdict-check`):
  all exit 0, evidence above.
- Chain receipt: `kaola-workflow/bundle-612-613/.cache/chain-receipt.json`, headSha
  `077341c264a195564489824ee80ba02f1fb83f6f` (matches current HEAD), all four chains green
  (claude/codex/gitlab/gitea, exitCode 0 each, no timeouts, no accepted-red waivers).
- n4-review (code-reviewer gate): `verdict: pass`, `findings_blocking: 0` —
  `.cache/n4-review.md`.
- Run-gap sweep: `--check` passes clean (0 swept classes; the mid-run write-set-overflow
  refutation was resolved in-run via user-approved plan repair, not deferred).

## Documentation Docking

DOCKED — `.cache/doc-docking.md`. CHANGELOG.md updated by n3-docs; no other doc class impacted
(no public API/architecture/env-var surface changed).

## Final Validation Failure Ledger

| Failing Command | Classification | Routed To | Evidence | Status |
|-----------------|----------------|-----------|----------|--------|
| (none — all gates passed on first evaluation after the mid-run scope widening) | — | — | — | — |

## Follow-Up Items

None filed — see Run gaps below.

## Run gaps

(sweep returned 0 swept classes; nothing to file)

## Closure Decision

None needed. The one mid-run judgment call (widening #612's scope from a test-fixture fix to a
production hardening, after the original fixture-escape hypothesis was exhaustively refuted) was
already routed to and approved by the user during the run, not deferred to closure.

## Commit And Push

Pending final Git gate; final hash is reported after push.

## GitHub Issue

Pending — #612 and #613 both close via the bundle's all-or-nothing sink at Step 9.

## Roadmap

Pending — regenerated by `cmdFinalize`/`archiveProjectDir` at Step 8b.

## Archive

Pending — `kaola-workflow/archive/bundle-612-613/` at Step 8b.

## Required Agent Compliance

| Requirement | Status | Evidence | Skip Reason |
|-------------|--------|----------|-------------|
| doc-updater | cited (n3-docs) | .cache/doc-updater.md | plan-node docs work already covers full scope |
| documentation docking | invoked | .cache/doc-docking.md | |
| final-validation fix executors | N/A | | no final-validation failures occurred |
| roadmap refresh | pending | | runs at archive (Step 8b) |
| archive completed folder | pending | | |
| final commit and push | ready | git status clean at HEAD 077341c2 | final gate runs after this file is committed |

## Status
ARCHIVED AFTER FINAL GIT GATE
