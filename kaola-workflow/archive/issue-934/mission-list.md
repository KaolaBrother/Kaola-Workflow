# #934 — settle what, if anything, the unguarded gitlab/gitea claim ports demand

Issue #934 is an observation with a measurement, proposing no mechanism, and explicitly closeable
with no code if the risk is knowingly accepted. The governing rule is `.roadmap/_rules.md`: **derive
additively — add only what an observed failure demands; silence is an answer.** So the run's job is
to measure whether a failure has been observed, not to pick a mechanism and justify it.

- item: verify the issue's premise at HEAD ecd8eb9a — its measurement was taken at 406b5639, before #933 landed; confirm the coverage table (codex byte-identical via validate-script-sync.js COMMON_SCRIPTS; gitlab/gitea checked only by the module.exports superset at :485) still describes what ships, and that simulate-workflow-walkthrough.js still reaches claude+codex only
  status: done
  dispatched: self, inline — reading validate-script-sync.js and the walkthrough in the main repo root (read-only); findings land inline in this file's result
  result: PARTIALLY FALSIFIED at HEAD ecd8eb9a. The static half holds — claim.js IS in COMMON_SCRIPTS (validate-script-sync.js:45-46) so codex is byte-identical (verified with cmp), and the gitlab/gitea ports are covered only by `forge claim module.exports superset` (:485, canonicalOnly ['ghExec']), a surface check. The BEHAVIOURAL half is wrong as generally stated. The issue says the forge claim CLIs are reached by nothing; in fact FOUR suites spawn them as child processes — test-claim-hardening.js (the #906 block at :4823-4830 drives all four editions and its own comment says "PER EDITION, BEHAVIOURALLY ... These run it"), test-forge-claim-rollback-scoping.js:169, test-forge-claim-reserved-project.js:174, and test-forge-bundle-lane.js:187. The issue's narrow sentence about simulate-workflow-walkthrough.js is TRUE (its only forge-claim references, :5530-5564, are source-text pins for #563/#832, not CLI drives), but the title's "the behavioural suite reaches claude+codex only" is false — #906 predates #932/#933 and already built per-edition legs.

- item: measure whether canonical and the two forge claim ports are behaviourally identical TODAY on the claim path — this is the crux; the issue asserts the four ports are "one shape" and "drift or stay correct together", and if that holds at HEAD then no divergence has ever been observed and additive derivation answers "close with no code", while any real divergence found IS the observed failure that would demand a mechanism
  status: done
  dispatched: self, inline — normalise forge vocabulary across the three claim ports and diff/hash them; scratch scripts under /private/tmp/claude-501/-Volumes-WorkspaceA-ylminiserver-workspace-kaola-workflow/532203a4-c9d2-4f9f-a701-f0738ce62b6c/scratchpad, findings land inline in this file's result
  result: The "one shape" claim does NOT hold at file scope, but no defect was found. Line counts 6608 / 6608 / 6263 / 6255 (canonical / codex / gitlab / gitea); canonical==codex byte-identical. Raw differing lines canonical↔gitlab 3253, canonical↔gitea 3149, gitlab↔gitea 320. After normalising forge vocabulary (normalize-forge.js in the scratchpad — glab/tea, issueIid, MR/PR, path renames) those become 3133 / 3113 / 200: normalisation moves ~4%, so the divergence is STRUCTURAL, not vocabulary. #934's "the four ports are behaviourally one shape" was established for ONE defect class (#933's resolution site hashed identical) and does not generalise to the file. I inspected the three most suspicious residues and ALL THREE are benign: (a) gitea's closeIssueIdempotent omits the inline claim-label removal that canonical and gitlab carry, but all three editions have exactly 9 structurally-aligned clearAdvisoryClaim call sites and gitea's finalize path removes the label there — redundancy difference, not a gap; (b) gitea's state writer emits `issue_number:` in both its forge section and its Sink section where gitlab emits `issue_iid:` then `issue_number:`, but both gitea lines are the identical expression `(data.issue_iid || '')` so the duplicate is value-identical; (c) gitea keeps `issue_iid` as its INTERNAL field name (set at :1034) so `issue_number: data.issue_iid` is self-consistent, not a stale gitlab-ism. Net: structural divergence is real and larger than the issue implies, and reading it found nothing broken.

- item: inventory which claim-path behaviours have per-edition test legs today versus canonical-only — #932 and #933 each hand-built a bespoke per-edition sibling suite (test-forge-claim-rollback-scoping.js, test-forge-claim-reserved-project.js) and nothing enumerates the rest; this is the "inventory" option the issue names and it is also the evidence the values call needs
  status: done
  dispatched: self, inline — enumerating test/ for suites that drive a forge claim CLI vs canonical-only; findings land inline in this file's result
  result: 5 of 20 claim subcommand handlers have per-edition behavioural legs; 15 do not. The claim CLI registers 20 handlers over 22 accepted tokens (scripts/kaola-workflow-claim.js:6520-6540; `release|discard` and `bootstrap|startup` each share a handler). Driven against ALL FOUR editions: `claim` (#932 rollback-scoping :227-228, #933 reserved-project :328), `startup` incl. --target-issue and --target-issues (#932 :275-359, #933 :327, bundle-lane :213/:271), and `release` / `status` / `finalize` incl. --check (#906 block in test-claim-hardening.js, two loops over EDITIONS_906 at :4942 and :5089, drives at :4957/:5099/:5111/:5130). NOT driven per-edition: authoring-allowed, patch-branch, watch-pr, pick-next, resume, worktree-status, stale-worktree-check, stale-worktree-cleanup, legacy-worktree-cleanup, worktree-finalize, sink-fallback, verify-sink, audit-labels, repair-labels, barrier-ref-sweep. Confirmed every forge-claim drive uses a LITERAL path, so the static enumeration is complete — no constructed-path drive evades it. METHOD NOTE: I first tried to measure this by instrumenting all four claim CLIs and running the full suite; the shim (a byte-identical, CLI-only appendFileSync keyed on __filename, positive-control-proven on canonical and gitea) was caught by an armed guard — "every non-atomic write API in a production script is accounted for in the exempt ledger" — so npm test exited 1 having produced no inventory. Instrumentation reverted, worktree verified clean, no KW934_PROBE residue. Incidental evidence that the repo's write-API guard is genuinely armed.

- item: put the fork to the user with the measurements attached — accept the risk and close with no code, build one of the named mechanisms, or file the watch-list row; this is a values call under First Principles axiom 4 and is not mine to take
  status: done
  dispatched: self, in conversation — the three options presented with the measurements, hash-parity flagged as refuted, and "close with no code + correct the premise" recommended
  result: OWNER RULED — close with no code, correcting the premise. Risk knowingly accepted: the 15 claim subcommands without per-edition legs stay unguarded, and if a forge port diverges there it ships silently. Do not re-file this as a coverage gap; a NEW observation (a forge port found broken where canonical is correct) is what would reopen the question.

- item: execute the ruling — post the measurements as a closing comment on #934 and close it, verifying CLOSED rather than trusting exit 0
  status: done
  dispatched: self, inline — comment body written to the scratchpad and posted with --body-file (heredoc command-substitutes backticks); state read back with `gh issue view 934 --json state`
  result: DONE. Comment posted — https://github.com/KaolaBrother/Kaola-Workflow/issues/934#issuecomment-5175248076 — carrying the premise correction, the 5-of-20 inventory, the normalisation table, the three benign residues, the refutation of the hash-parity option, and the named reopening trigger. Issue closed as NOT_PLANNED and VERIFIED by reading state back: `#934 state=CLOSED reason=NOT_PLANNED`. No `.roadmap/issue-934.md` ever existed, so closure removes no roadmap source and the mirror already reads "No active work".

- item: finalize the run — no production diff, so the chains have nothing to grade; archive the folder and close out
  status: in-flight
  dispatched: self, inline — /kaola-workflow-finalize issue-934 from the main repo root
