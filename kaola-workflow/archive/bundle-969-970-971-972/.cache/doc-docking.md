# Documentation docking — bundle-969-970-971-972

**Verdict: DOCKED**

## Changed files reviewed

37 tracked files. Production: `install-all.sh`, `install-opencode.sh`, `install-kimi.sh`,
`scripts/kaola-workflow-claim.js` (+3 ported copies), `scripts/kaola-workflow-gap-sweep.js` (+3
ported copies), `scripts/sync-opencode-edition.js`, `scripts/sync-kimi-edition.js`,
`scripts/generate-routing-surfaces.js`, `scripts/validate-workflow-contracts.js` (+1 vendored copy),
`templates/routing/finalize.skeleton.md`, `templates/routing/required-blocks.js`, and 6 regenerated
routing surfaces. Tests: `test-gap-sweep.js`, `test-install-all.js`, `test-opencode-edition.js`,
`test-kimi-edition.js`, `test-bash-block-guards.js`, `simulate-workflow-walkthrough.js`.

## Documents checked, and what changed

| document | outcome |
|---|---|
| `CHANGELOG.md` | Docked centrally, one `### Fixed` entry per issue plus the installer repair. Written BEFORE the receipt run so the receipt does not go stale against it. The #970 count was corrected mid-run from the premise check's 27-across-9 to the shipped predicate's measured **34 items across 11 of 36 records**. |
| `docs/api.md` | Third finalize report added to the two-report section, its table row, and the envelope sample; sync CLI row gained `--refresh-present` and `--print-tree-root`. **Test-consumed** — enters the code-tree hash, which is why the receipt was produced after the last write. |
| `docs/architecture.md` | Two-measurement statements corrected to three. `:299` was **FALSE, not incomplete** — it still claimed a routing-surface change leaves the edition trees stale until regenerated, which #969 removed. |
| `docs/workflow-state-contract.md` | "the finalize transaction's own **two** measurements" corrected. Test-consumed. |
| `docs/conventions.md` | The mandated-regenerate paragraph now says the step also refreshes present edition trees. |
| `docs/opencode-edition.md`, `docs/kimi-edition.md` | `--refresh-present` added to the Develop/regenerate blocks; the additive-edition boundary sentence qualified to the routing-surface **`--check`** contract, because the unqualified wording now reads as the opposite of what shipped. |
| `README.md` | Mission-list report added to the finalize-reports list; finalize row amended. Five other suspected locations checked and verified NOT stale, each with a stated reason. |
| `templates/routing/finalize.skeleton.md` + `required-blocks.js` | `## Mission List` added to the summary template and to the do-not-delete sentence; a new `fn-mission-list-report` block obligates it on all six rendered surfaces. Regenerated, never hand-edited. |
| Issue comments | The four issues are closed by the sink with the merge commit. |
| `.env.example` | No impact — no environment variable added or changed. |
| `kaola-workflow/ROADMAP.md` | Regenerated by the finalize transaction from the remaining `.roadmap/` sources. |

## Gaps found and fixed

- `docs/architecture.md:299` — a **false** claim, not merely an incomplete one. Fixed.
- `README.md` — the finalize-reports enumeration omitted the third report. Fixed.
- Both edition docs omitted `--refresh-present`, the only place in the repo the sync CLI is
  enumerated as a command list. Fixed.
- The additive-edition boundary sentence was ambiguous in the one direction that matters. Qualified.

## Explicit no-impact reasons

- `CLAUDE.md:181` and its four restatements (`docs/api.md`, `docs/architecture.md`,
  `docs/audits/opencode-edition-audit.md`, `docs/decisions/D-530-02.md`) — the user's ruling KEEPS
  that rule, and it was verified intact: `package.json` and all four chain scripts are byte-unchanged,
  and planted edition drift survives `--check` and the no-arg default.
- `README.md:225` (`install-all.sh`) — PARTIAL is not new; only the conditions producing it for Codex
  are. The paragraph describes the orchestrator role, which is unchanged.
- `README.md:233`, `:235` — describe *runtime* script resolution, a different question from the
  *generation* source tree #969 moved.
- `docs/README.md` — no relevant mentions; not in the diff.
- No verbatim `--help` output is quoted in README, so the rewritten help text stales nothing.

## Anti-fabrication

`--print-tree-root`'s documented behaviour was verified by RUNNING it from the linked worktree before
being written down: both scripts print the main checkout path, exit 0, nothing else on stdout, and
`--forge=gitea` returns the same answer.

## Verification

`generate-routing-surfaces --check` exit 0 (18 surfaces) · `validate-script-sync` exit 0 ·
`test-generate-routing-surfaces` 434 · `test-route-reachability` 331 · `test-bash-block-guards` 49 ·
`validate-workflow-contracts` exit 0 · full walkthrough 210/210 exit 0.
