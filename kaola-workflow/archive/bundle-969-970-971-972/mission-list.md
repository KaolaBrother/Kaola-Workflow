# Close #969, #970, #971, #972 — the whole open backlog; reviewers run on the Fable model this run

- item: Premise-check #969 — verify that sync-opencode-edition.js --check and sync-kimi-edition.js --check genuinely run in none of the four chains and only under test:kaola-workflow:editions, and that generate-routing-surfaces --check can report exit 0 with edition trees stale; read package.json chain wiring and scripts/generate-routing-surfaces.js, and say which of the issue's stated facts survive.
  status: done
  dispatched: investigator subagent, read-only, running in the MAIN checkout (editions do not exist in a fresh worktree); report to land at kaola-workflow/bundle-969-970-971-972/premise-969.md
  result: kaola-workflow/bundle-969-970-971-972/premise-969.md — all six claims SURVIVE (title refuted on a literal reading: edition-sync.js --check does run in gitlab/gitea chains, but that is a different script). Reproduced live: generate-routing-surfaces --check green at 18/18 while 12 edition files are stale in main RIGHT NOW, and the stale prose is not cosmetic — it tells readers to run --target-issue where canonical says --target-issues. Finding X, not in the issue: the #968 run synced correctly IN ITS WORKTREE, which was then deleted, so main was never written — a fix relying on "the run regenerates" inherits that. Candidate C (diff-scope) measured and does NOT reach the result; B (fold into generate-routing-surfaces) creates a circular require; A (wire --checks into chains) reaches it but violates CLAUDE.md:181 as written and reverses the recorded ruling at CHANGELOG.md:1714-1718; D (regenerate-present-trees at source) has no rule tension but produces no red. The false-red objection is answered by presence-gating; the RULE objection is a user value call, not a measurement.

- item: Premise-check #970 — verify that finalize has no report for an item carrying an outcome while status is not done, and locate where finalize already measures-and-reports onto the envelope and finalization-summary.md (chain receipt, changed paths); confirm the correct condition is outcome-present-and-status-not-done, and find whether any mission-list parser already exists to reuse.
  status: done
  dispatched: investigator subagent, read-only, MAIN checkout; report to land at kaola-workflow/bundle-969-970-971-972/premise-970.md
  result: kaola-workflow/bundle-969-970-971-972/premise-970.md — claims 1, 2, 3 SURVIVE exactly (7 of 17, and precisely the three non-spec names); claim 4 QUALIFIED — there are TWO report shapes and the chain receipt uses Shape A (appendSummarySection + envelope), not Shape B (recordFinalizeFinding), which costs 4 hand-ported claim.js copies plus 2 docs/api.md pins; claim 5 SURVIVES but is corrected upward — this is not a #968 one-off, 9 of 36 archived runs (27%, 27 items) carry the condition, so the report will fire often and must state counts and line numbers without implying fault. No mission-list item parser exists anywhere; ledger-compare.js:32-39 is a line counter and the precedent for owning a local parse. Parser must survive: column-0 item:, DUPLICATE status: lines inside one item (11 in the archive; first-vs-last moves the count 27%), and free-text result prose that mimics fields.

- item: Premise-check #971 — verify that kaola-workflow-gap-sweep.js resolves the run folder against cwd rather than the repo/authority root, reproduce the "artifact not found at .kw/worktrees/..." message from the worktree, and confirm templates/routing/finalize.skeleton.md Step 7 carries no cwd guidance while Step 10 wraps in ACTIVE_WORKTREE_PATH.
  status: done
  dispatched: investigator subagent, read-only, MAIN checkout, may reproduce from the provisioned worktree; report to land at kaola-workflow/bundle-969-970-971-972/premise-971.md
  result: kaola-workflow/bundle-969-970-971-972/premise-971.md — claim 1 SURVIVES (gap-sweep.js:476-479 resolves root from process.cwd(), everything derives from it), claim 3 SURVIVES, claim 4 QUALIFIED (count exact, attribution wrong — Step 1 not Step 4 establishes the tree, and already documents this same split in prose), claim 6 QUALIFIED — propagation is 6 tracked + 2 edition = 8, not 6+6. Claim 2 REFUTED AND THE TRUTH IS WORSE: re-running the scanner from the worktree does not merely fail to help, it converts a loud red into a SILENT GREEN — the worktree scan reads an empty .cache, sweeps zero classes, creates a stray untracked run folder, and the gate takes the vacuous-pass branch at :370 and exits 0 while a real gap sits in main. Severity is therefore not "a confused minute" but evidence loss. The helper to reuse EXISTS: resolveMainRoot (adaptive-schema.js:520, already required by gap-sweep at :213, zero new dependency) or resolveRecordFolder (validation-runner.js:1286), with the settled idiom --show-toplevel = this tree, resolveMainRoot = main. KAOLA_GAP_ROOT must keep precedence or all 127 test-gap-sweep assertions red; runCheck does not currently receive root; both modes must move together. Neighbouring instance found and NOT in the issue: Step 9's SINK_STATE_FILE is cwd-relative too, and from the worktree SINK_BRANCH comes back EMPTY and is consumed by the sink.

- item: Premise-check #972 — verify install-all.sh's codex currency check is pure version-string equality at the cited lines with no content comparison and no force path, read the header comment that states the intent, and establish what the Codex plugin cache layout actually permits (can a version directory be re-added in place, or does add refuse when the version already exists).
  status: done
  dispatched: investigator subagent, read-only, MAIN checkout, inspects the live ~/.codex plugin cache without mutating it; report to land at kaola-workflow/bundle-969-970-971-972/premise-972.md
  result: kaola-workflow/bundle-969-970-971-972/premise-972.md — defect REPRODUCED LIVE at HEAD: the cached 7.8.0 SKILL carries the retired rule at a two-day-old mtime while claude/opencode/kimi all took the new prose; 3 of 47 files differ, and origin/main == HEAD so it is not an unpushed-work artifact. Claims 2, 5, 6 SURVIVE. Claim 1 QUALIFIED — line is 425-430 not 426-430, and "no force path anywhere in the file" is REFUTED: a working remove+add refresh exists at :439-469 and converged 7.7.0 to 7.8.0 on this box today, so only the TRIGGER is unreachable. Claim 3 QUALIFIED — two live install roots, not one (the plugin cache plus ~/.codex/kaola-workflow), though only the plugin half is version-keyed. Claim 4 QUALIFIED — the ownership sentence is accurate but the failure the comments NAME (a tree bump) is the one the code handles; content-moving-without-a-bump is named nowhere. Decisive feasibility fact the issue did not have: the marketplace is LOCAL, so a refresh is a sub-second local copy with no network. Hazard the issue did not name: a git-sourced marketplace would refresh-loop forever on local edits, so any content trigger needs a marketplaceSource.sourceType == "local" gate.

- item: Put the #972 value call to the user in conversation — refresh a version-keyed cache in place versus report the staleness and require a version bump — framed with the premise-check facts, since the issue names this as genuinely open and it is the user's call, not mine.
  status: done
  dispatched: self, in conversation; three questions put to the user together — #969's rule tension, #972's cache call, and the out-of-scope Step 9 neighbour found while measuring #971
  result: ALL THREE RULED BY THE USER. (1) #969 — "Close at source, keep the rule": sync-*-edition resolves its tree root against MAIN via resolveMainRoot, and the mandated regenerate refreshes any edition tree that is PRESENT. CLAUDE.md:181 stands; no chain gains an edition step; no red is produced, deliberately. (2) #972 — "Refresh in place on content difference": change the trigger at install-all.sh:425 from version-inequality to version-OR-content, reusing the existing :439-469 remove+add path unchanged, gated on marketplaceSource.sourceType == "local". (3) #971 — "Fix it in this run": the Step 9 SINK_STATE_FILE cwd-relative defect is IN SCOPE for this bundle.

- item: Repair the SECOND pre-existing red that tests-969 measured and that is not #969's — opencode S2 (#927) reds on the mechanism word "variant" in the generated finalize prose, introduced by fd00ef63 in the #968 bundle at templates/routing/finalize.skeleton.md:267. It reds no chain and is invisible in main only because D0 halts the suite first, which is why it shipped. In scope by necessity rather than ambition: the editions suite cannot go green for #969 while it stands, so I cannot verify my own work without it. Must be sequenced AFTER impl-971 lands, since that agent is editing the same skeleton right now, and the surface is generated so the edit goes in the skeleton and is regenerated.
  status: done
  dispatched: self, in the worktree, now that impl-971 has released the skeleton; the reworded skeleton plus its regenerated surfaces land in the worktree diff and the outcome is recorded inline on this item. HELD — MY OWN MEASUREMENT REFUTES THE PREMISE, and I am not rewording a consumer-facing surface on an unverified one. S2's locator is BLOCK_HEADING "Model and effort are inherited" (test-opencode-edition.js:792) and blockSection (:802-815) runs from that heading to the NEXT heading of any level, which in the rendered opencode finalize is lines 28-36; the only "variant" in that file is at line 240, under "### Keep-open terminal mode", far outside it. /\bvariants?\b/i against the extracted block is FALSE across all nine opencode command surfaces in all three trees, and the assertion at :872 sits inside if (sec !== null) so it cannot fire on the two files that carry no block at all. Routed back to tests-969 for the literal S2[<file>] failure text and the cwd it ran from — its trees were materialized from a mid-flight worktree skeleton and may have produced a transient red rather than a defect.
  result: MY REFUTATION WAS WRONG AND THE TEST AUTHOR WAS RIGHT; both measurements were sound and examined DIFFERENT assertions. I checked only the block-scoped assertion at test-opencode-edition.js:872, which sits inside if (sec !== null) and is bounded by blockSection to lines 28-36 of the rendered file. The red comes from a SECOND, body-wide mechanism-word sweep at :914-937, prefixed "S2 (#927):" with a file:line, which iterates every generated agent and command and whose own comment says it exists precisely because "a scoped check is exactly what an anchor miss disarms". I reproduced it independently in main: sweep hits 1, at .opencode/command/kaola-workflow-finalize.md:240, and git grep confirms canonical carries exactly one occurrence, falsifying the sweep's stated premise of zero. FIXED by rewording the skeleton, not by narrowing the sweep — narrowing a mutation-proven guard to accommodate one sentence is the worse trade by this project's own rules, and "form" loses no meaning. Skeleton line 267 now reads "no per-issue form of it"; regenerated with --write, 18 surfaces, --check exit 0 (real exit code, not a piped one — I first read tail's status by mistake here).

- item: Adapt the two editions suites' D1 and tree-read root once #969's resolution semantics are chosen — tests-969 measured that moving the tree root to main makes BOTH suites unrunnable from a linked worktree (D1 fails, then a node:fs crash), because they read the tree through path.join(sync.REPO, rel). It deliberately did NOT close this, because the correct adaptation depends on semantics the implementer picks and a guessed one would have killed the red evidence. This is TEST CUSTODY and routes back to tests-969, never to the implementer. The implementer HIT it and did not silence it, reproducing the exact predicted crash in a disposable worktree fixture: D1 fails, then A2 dies at node:fs:440 on an ENOENT for the worktree's absent tree.
  status: done
  dispatched: tests-969, resumed with impl-969's now-fixed semantics (canonical from the invoking checkout, tree from main, resolveMainRoot failing open beside the script when no main checkout resolves); adapts D1 and the path.join(sync.REPO, rel) tree-read root in both editions suites; output appends to kaola-workflow/bundle-969-970-971-972/tests-969.md
  result: Delivered by tests-969 and verified by me at the end of the run. Both suites are now runnable from BOTH postures against the real trees: opencode 631 assertions exit 0 and kimi 589 exit 0 from the worktree, all six trees reported in parity, and D0 now prints the tree root it actually used — "[tree root: /Users/ylpromax5/Workspace/Kaola-Workflow, not this checkout]" — which without that line would have said "in parity" about a tree in another checkout, the same dishonesty D0 exists to prevent. The suites compute the root themselves rather than importing it from the writer, deliberately: a probe derived from the writer agrees with it by construction including when both are wrong, which is exactly the failure D1 exists to catch. Nothing was weakened to get there — A31/A32/K13/K14 untouched, D0 skip-when-absent intact, no assertion deleted or relaxed.

- item: Dock the prose that both implementations make incomplete — for #970 the finalize now has THREE durable reports where seven sites say two (docs/api.md:268,270,275-278,292-307, docs/architecture.md:192,196-197, docs/workflow-state-contract.md:113-115) plus a PROMPT surface (finalize.skeleton.md:215,222 listing only ## Validation and ## Changed Paths, pinned by templates/routing/required-blocks.js:219 and mirrored to 6 rendered surfaces); for #969 the regenerate step now also refreshes present edition trees, which docs/conventions.md:141 does not say, and both sync scripts gained --refresh-present. Every one of these is INCOMPLETE rather than false, and none is machine-pinned to the count, which is why every suite is green as-is — so this is docking, not a bug fix.
  status: done
  dispatched: implementer subagent in the worktree; owns docs/api.md, docs/architecture.md, docs/workflow-state-contract.md, docs/conventions.md, templates/routing/finalize.skeleton.md + required-blocks.js and the regenerate; explicitly NOT CHANGELOG, which stays mine; report to land at kaola-workflow/bundle-969-970-971-972/impl-docs.md. Second pass dispatched to the same agent for docs/opencode-edition.md:333-335 and docs/kimi-edition.md:307-308, the only place in the repo the sync CLI is enumerated as a command list and the one place --refresh-present is missing.
  result: kaola-workflow/bundle-969-970-971-972/impl-docs.md — 6 assigned files plus 6 rendered surfaces REGENERATED, full walkthrough 210/210 exit 0 run three times, and every routing guard green. THREE CORRECTIONS TO ME, all measured: my premise that required-blocks.js:219 pinned the block list and would have to move was WRONG — :219 is a whitespace-normalized content token matched anywhere on the surface, so adding a heading could not break it, which is why nothing went red; docs/architecture.md:299 was FALSE rather than incomplete, still claiming a routing-surface change leaves the edition trees stale until regenerated, which #969 removed; and docs/api.md enumerates the sync scripts in exactly one row with no flag list, so there was no flag enumeration to correct. THE MOST VALUABLE FINDING IS A FAILED MUTATION: its first pin used the bare "## Mission List" heading, and deleting that heading from a shipped surface left the suite GREEN, because the token was still satisfied by a second occurrence inside the neighbouring sentence — a guard that could not fail. Re-pinned on the sentence, the mutation now reds correctly across 180 obligated file-checks. Two calls of its own I accepted rather than overruled: no Step-2 paragraph restating the third measurement, and a new fn-mission-list-report block rather than a token bolted onto the changed-paths one. SEQUENCING FACT IT SURFACED AND I AM ACTING ON: docs/api.md and docs/workflow-state-contract.md are in SELF_HOST_TEST_CONSUMED, so they enter the code-tree hash and STALE THE CHAIN RECEIPT — the chains must be produced after the last write in this bundle, which is why they have not been run yet.

- item: Leave MAIN's six edition trees self-consistent with MAIN's own canonical before this run ends, and regenerate once more after the merge. State as of now: running generate-routing-surfaces --write from the WORKTREE refreshed MAIN's trees — which is impl-969's fix working exactly as designed and is Finding X inverted — but it also realized the hazard I flagged, so main's trees now carry UNMERGED branch prose (main's .opencode finalize says "per-issue form of it" while main's canonical still says "variant"), and sync --check from main now reds all six where github was green. Re-running the sync FROM MAIN renders from main's HEAD canonical and yields the strictly best interim state: the original 12-file staleness fixed AND no phantom prose. RE-SEQUENCED once impl-969 reported: restoring now would be pointless, because impl-docs is about to run --write from this worktree and would immediately rewrite main's trees again. The honest resolution is the merge itself — once the branch lands, main's canonical IS the branch's canonical, so the post-merge reinstall of all four runtimes regenerates correct trees as a side effect. Do it there, and verify with a --check per forge from main. Whether writing main's trees from a worktree is the right rule at all is the review question for the adversarial pass, not a thing to settle by fiat here.
  status: todo

- item: Decide the installer consequence impl-969 measured and declined to fix — install-opencode.sh:152 and install-kimi.sh:122 compute SOURCE_TREE from their own script dir, so an edition installer run from a linked worktree now refreshes MAIN's tree and then exits 1 with "no agent sources found in <worktree>/.opencode". DECIDED BY ME, and stated for the user to overrule: ACCEPT it. It is strictly more honest than what it replaced — the previous behaviour silently deployed a throwaway tree that died with the worktree — no run surface, chain, command, skill or script invokes either installer, the only callers are a human and the human-run install-all.sh, and the documented reinstall posture is main. Fixing it properly means new CLI surface (a --print-tree-root mode on both sync scripts plus a line in each installer) that no test pins, and installer behaviour deserves a test author before an implementer. Recorded in CHANGELOG rather than built.
  status: done
  dispatched: self, in conversation and in this record
  result: ACCEPTANCE REVERSED ON NEW EVIDENCE, and the reversal is the right outcome. I accepted this on the rationale that it "fails loudly, strictly more honest than silently deploying a throwaway tree". tests-969 then measured both installers hermetically, with no test code involved, and that rationale holds for opencode ONLY. install-kimi.sh run from a worktree exits 0 AND DEPLOYS 0 SKILLS — a silent empty install — because --check now passes against main's current tree, so the --check-or-write line never regenerates and the copy from a missing source directory is swallowed. Running install-all.sh from a worktree therefore gives a loud failure on one edition and a silent no-op on the other. A silent empty install is not a more honest failure, it is the worst outcome available, and the watch-list argument I used does not apply to a defect that has now been observed and measured. Superseded by the item below; the CHANGELOG entry is corrected with it.

- item: SUPERSEDED by the item above, which absorbed it — repair the 12 edition files that were stale in MAIN — .opencode-gitlab, .opencode-gitea, .kimi-gitlab, .kimi-gitea, three files each — since they ship a wrong instruction today, telling readers to run --target-issue where canonical says --target-issues. This is remediation of observed state, not part of the #969 mechanism, and must be done in main where the trees live.
  status: done
  dispatched: self; folded into the item above rather than run separately, once impl-969's fix changed what "repair" means here
  result: Not run as its own task, and deliberately so. The 12 files were repaired as a side effect of exercising impl-969's fix — generate-routing-surfaces --write from the worktree rewrote all six of main's trees at 00:53 — so the staleness this item named is gone and the retired --target-issue instruction is confirmed absent from .opencode-gitlab/command/workflow-next.md. What replaced it is a different and smaller problem, now owned by the item above: those trees carry unmerged branch prose rather than main's own. Kept in the record rather than deleted, because an item that silently vanishes is worse evidence for a successor than one that says what happened to it.

- item: Implement #969 by the user's ruling — close the class at source: sync-opencode-edition.js and sync-kimi-edition.js resolve the tree root against MAIN (resolveMainRoot, adaptive-schema.js:520) instead of cwd, so a sync run from a worktree writes main's trees rather than throwaway ones, and the mandated regenerate refreshes any edition tree that is PRESENT. CLAUDE.md:181 stands untouched, no chain gains an edition step, --check semantics unchanged; watch the circular require (sync-*-edition requires runtime-edition-forge which requires generate-routing-surfaces) — a lazy require inside the write path avoids it. THE DESIGN QUESTION THE IMPLEMENTER MUST SETTLE AND STATE, not assume: the sync reads canonical sources AND writes an edition tree, and those two roots must not both move to main. Canonical must come from the INVOKING tree or a worktree's edits never render at all; the tree root is the half in question. Writing main's trees from a worktree makes them reflect prose that has not merged yet, and two concurrent worktrees would race to last-writer-wins — both bounded, since the trees are gitignored derived artifacts that regenerate, but neither is free. The addendum records the fact that makes this tractable: --check treats a MISSING tree as a mismatch and never a skip, so once the tree root resolves to main, running --check from a worktree reads main's real trees instead of reporting 19 phantom missing files. The circular require is CONFIRMED REAL, not theoretical: tests-969 tried a lazy require of the sync module inside the generator and it crashed with TypeError: ed.FORGES is not iterable, cleared by spawning child processes instead.
  status: done
  dispatched: implementer subagent in the worktree, against tests-969's landed RED bands (test-opencode-edition.js A31/A32, test-kimi-edition.js K13/K14, +42 assertions each, 6 genuine failures per suite); satisfiability already proven on a /tmp mirror at opencode 612 passed and kimi 570 passed / 0 failed; told NOT to touch either editions suite, since the D1 adaptation those bands will need is test custody and routes back to tests-969; output lands as the worktree diff plus a report at kaola-workflow/bundle-969-970-971-972/impl-969.md
  result: kaola-workflow/bundle-969-970-971-972/impl-969.md — 3 production files, no test file, no package.json, no chain step. THE RULE CHOSEN AND STATED: canonical sources resolve to the INVOKING checkout, the generated tree to MAIN, and where no main checkout resolves the tree belongs beside the script rather than at cwd — resolveMainRoot fails open, which is what keeps the non-git installer posture working. Circular require avoided by SPAWNING the sync scripts as children rather than requiring them; the cycle is real and measured (runtime-edition-forge reads routing.FORGES at module top level while the generator assigns module.exports at the bottom, so the generator-as-entry-point captures a permanently empty object). Both editions suites fully green on a mirror after my S2 fix: opencode 612, kimi 570. Observed failure closed end-to-end in a real git worktree fixture — marker present in all six of MAIN's trees, zero throwaway trees left behind — and the fresh-clone posture creates none. TWO CONSEQUENCES IT HIT AND DELIBERATELY DID NOT SILENCE: the D1/worktree crash the test author predicted, routed back to test custody; and one nobody predicted — install-opencode.sh:152 and install-kimi.sh:122 compute SOURCE_TREE from their own script dir, so an installer run from a linked worktree now refreshes main's tree and then finds nothing to deploy, exiting 1 with the path named where it previously deployed a throwaway tree silently.

- item: Implement #970 so finalize reports an item carrying an outcome with a non-done status onto the envelope and durably in finalization-summary.md — a typed finding, no refusal, no new required field, and NOT conditioned on status == in-flight. The test author settled the duplicate-status question by measurement: LAST match wins, all 11 duplicate-status items in the archive have the later line as done and there are zero counter-examples, so first-wins would report ten items whose author wrote the correction directly beneath the stale line. One hard constraint: a Shape-B-only implementation does NOT satisfy the issue, because flushFinalizeFindings de-duplicates findings down to type names and the line numbers would exist only in the summary.
  status: done
  dispatched: implementer subagent in the worktree, against tests-970's landed RED scenario testFinalizeReportsMissionListOutcomeWithoutDone in simulate-workflow-walkthrough.js (isolate with --shard 94/999999; three legs, leg 3 a negative control green on baseline, every deeper assertion mutation-proven on a scratch mirror that was deleted); output lands as the worktree diff plus a report at kaola-workflow/bundle-969-970-971-972/impl-970.md
  result: kaola-workflow/bundle-969-970-971-972/impl-970.md — SHAPE A, across the 4 hand-ported claim.js copies, proved to carry the SAME change rather than a similar one by hashing each copy's -U0 diff body to one digest; the forge ports were applied by asserted string replacement after a fuzz-3 patch dry-run was measured placing a hunk ~300 lines from its real site. Envelope key mission_list = {items, outcome_while_not_done:[line numbers]}, durable "## Mission List" via the existing appendSummarySection, written before archiveProjectDirSafely so it lands in the kept copy. Full walkthrough 210/210 at FULL scope, exit 0; validate-script-sync, test-forge-finalize-findings, test-bundle-finalize, test-finalize-door, and the four suites that slice finalization-summary.md by heading all exit 0. All four editions smoke-proved LIVE on one fixture whose five items each flip under exactly one wrong reading, and all four agreed on all five. Its three judgement calls, stated: an outcome is a field name STARTING with result (covers the decorated result (test leg): forms, excludes earlier result:, which changes no verdict anywhere); an item with an outcome and NO status is not reported, since nothing there contradicts anything and calling it defective would be judging sufficiency; and a coherent record still reports zero, because a key that appears only on a contradictory run is indistinguishable from a report that never ran.

- item: Implement #971 so the gap sweep resolves the run folder against main the way the rest of finalize does — reuse resolveMainRoot (adaptive-schema.js:520, already required by gap-sweep at :213, zero new dependency); KAOLA_GAP_ROOT must keep precedence or all 127 test-gap-sweep assertions red, runCheck does not currently receive root so resolve before the checkMode branch, and BOTH modes must move together or the scanner keeps writing stray artifacts into the worktree. Four tracked copies move together. Also in scope by the user's ruling: the same cwd-relative defect at finalize.skeleton.md Step 9's SINK_STATE_FILE, whose empty SINK_BRANCH is consumed by the sink — that half is a generated-surface fix, so edit the skeleton and regenerate.
  status: done
  dispatched: implementer subagent in the worktree, against tests-971's landed RED suite (test-gap-sweep.js T25a-f, 8 failing; test-bash-block-guards.js Test F, 18 failing); told explicitly NOT to touch scripts/validate-workflow-contracts.js:521, which pins the defective Step 9 literal verbatim and is being converted to a result assertion by tests-971 under test custody; output lands as the worktree diff plus a report at kaola-workflow/bundle-969-970-971-972/impl-971.md
  result: kaola-workflow/bundle-969-970-971-972/impl-971.md, and the only issue in this bundle whose adversarial review came back with ZERO findings. Resolution rule: the root is the tree that HAS kaola-workflow/<project>/ — this one when it does, main otherwise, cwd when neither does — with KAOLA_GAP_ROOT overriding the whole search; "always main" was rejected because it fails the post-mirror topology, and resolution moved before the checkMode branch so the scanner and the gate cannot disagree, that disagreement being the false-green mechanism itself. Step 9 uses the same resolver idiom already sitting in Step 11 of the same file and does not cd. Verified by the reviewer against the SHIPPED bytes in fresh per-copy fixtures: the false green is dead in all four copies, every ambiguous posture measured, all six rendered Step 9 blocks executed under bash from three cwds, and the removed contract pin independently re-mutation-proved to red both test-bash-block-guards and generate-routing-surfaces --check. test-gap-sweep 151 assertions, test-bash-block-guards 49, both exit 0.

- item: Implement #972 by the user's ruling — refresh in place on content difference: change the trigger at install-all.sh:425 from version-inequality to version-OR-content-difference, reusing the existing remove+add refresh at :439-469 unchanged, gated on marketplaceSource.sourceType == "local" so a git-sourced marketplace cannot enter a refresh loop it can never converge out of. The re-read proof at :452-466 compares versions, which are equal by construction in this case, so it must gain a content re-read or it becomes vacuous exactly on the new path. ONE SUB-QUESTION SETTLED BY ME, not the user, because it is a fact and not a value: for a GIT-sourced marketplace the cache installs from the remote snapshot rather than the working tree, so tree-differs-from-cache is not evidence of staleness there and reporting it would be factually wrong rather than merely noisy — behaviour on that path is therefore UNCHANGED from today, version equality reports current, and no standing PARTIAL is introduced.
  status: done
  dispatched: implementer subagent in the worktree, against tests-972's landed RED suite (scripts/test-install-all.js, 9 failing across S2 and S5, 161 passing, every control mutation-proven armed on a scratch mirror); output lands as the worktree diff plus a report at kaola-workflow/bundle-969-970-971-972/impl-972.md. FOLLOW-ON, dispatched to tests-972 then back to impl-972: the implementer flagged rather than took a judgement call, correctly — it compared the cache against the INVOKING tree as I briefed, but codex plugin add installs from the row's marketplaceSource.source, which is MAIN, so a non-dry run from a worktree whose plugins/ differs would remove+add, get main's content back, still differ, and report codex FAIL every run with no convergence reachable. That state is live today, this worktree already differing from main in 9 files. I RULED the oracle must be the same path the refresh installs from, on correctness rather than taste: a trigger whose refresh provably cannot converge is broken, and a permanently-red row is the noise codex_not_applicable was added to remove. The authored tests cannot discriminate the two oracles, so a discriminating case is being authored FIRST, red against the current implementation, including a repeat-invocation assertion pinning that no reachable state demands a refresh that cannot converge.
  result: kaola-workflow/bundle-969-970-971-972/impl-972.md — install-all.sh only, 1 file. test-install-all.js exits 0 at 196 assertions, exactly the count the test author's satisfiability shadow predicted, and the oracle now reads the marketplace source (8 references in the file). The live --check codex row went from "marketplace plugin already at 7.8.0" to "plugin refresh pending: served content differs at 7.8.0", so the defect reports where it used to conceal. Host ~/.codex verified unmutated by a digest snapshot of all 1310 files under the plugin cache before and after, with the witness SKILL.md still at its 08-10 23:22 mtime and premise-972's recorded sha256. The non-convergent loop the oracle swap fixes was OBSERVED, not argued: W3 ran the wrapper twice after a successful refresh and got remove+add reissued with codex FAIL both times. Two gaps the test author named and did not close: nothing pins what --check SAYS under the worktree shape, and W1-W3 all run the github edition, so a forge edition combined with a differing marketplace source is uncovered. The oracle field was chosen with a reason better than my brief supplied: source.path, the row's own statement of where the plugin comes from, rather than marketplaceSource.source, which is the marketplace ROOT and would need plugins/<forge-selected-name> appended — hardcoding an assumption about a marketplace's internal layout that happens to hold here, and whose failure mode is a wrong directory that reads as unanswerable and silently disables the check. Read the path that is stated, never reconstruct one that is implied. The local gate still reads marketplaceSource.sourceType, since the two fields answer different questions. Accepted cost, stated in the file: run from a linked worktree the wrapper now reports on the marketplace's checkout, so worktree-only prose reads as current — the same stance already settled for git marketplaces, that a difference this wrapper cannot arbitrate is not reported as staleness.

- item: Author the tests for whatever lands, under test custody — tdd-guide writes them, the implementer never does — and mutation-prove each guard, since a green suite is not proof a guard is armed.
  status: done
  dispatched: four tdd-guide subagents in parallel, one per issue, all working in the worktree at .kw/worktrees/bundle-969-970-971-972, each required to land its test RED on the current baseline and record the literal failure; tests land in their existing suites — #970 in simulate-workflow-walkthrough.js, #971 in test-gap-sweep.js, #972 in test-install-all.js, #969 in the editions suites (test-opencode-edition.js / test-kimi-edition.js) so that npm test gains no edition coverage and the user's ruling to keep CLAUDE.md:181 intact is not quietly reversed by the TEST
  result: All four landed RED and are reported at tests-969.md / tests-970.md / tests-971.md / tests-972.md. #969 — test-opencode-edition.js A31/A32 and test-kimi-edition.js K13/K14, +42 assertions each, 6 genuine failures per suite, and nothing placed in a chain-resident suite so the user's ruling is not reversed by the test. #970 — testFinalizeReportsMissionListOutcomeWithoutDone, three legs with leg 3 a negative control green on baseline, duplicate-status settled as LAST-WINS on 11 archive items with zero counter-examples, and eight simulated defects each mutation-proven to red the assertion they should. #971 — test-gap-sweep.js T25a-f pinning the SILENT GREEN as primary rather than the error string, plus test-bash-block-guards.js Test F executing Step 9 from all 6 rendered surfaces; the verbatim contract pin at validate-workflow-contracts.js:521 was REMOVED rather than rewritten, because the two valid fix shapes share no text and every static candidate red on baseline is also red on a valid fix. #972 — test-install-all.js grew a content dimension on the stub, 7 cases pinned including 3 I did not ask for, every negative control mutation-proven armed. Three things the authors found that were in no brief: a second pre-existing red the #968 run shipped (opencode S2 on the word "variant"), the D1/worktree consequence of moving the tree root, and the confirmed-real circular require. NOTE FOR MY OWN CONDUCT: I set a constraint here that was wrong — requiring the converted contract pin to be RED on baseline — and the test author refuted it with a measurement rather than complying. #971 — test-gap-sweep.js +T25a-f (real git worktree fixture, a runner that does NOT set KAOLA_GAP_ROOT, and T25d arming env-var precedence against a THIRD tree), 127 exit 0 to 142 passed/8 FAILED; test-bash-block-guards.js +Test F executing Step 9's block from all 6 RENDERED surfaces, 7 exit 0 to 31 passed/18 FAILED. #972 — test-install-all.js grew a content dimension on the stub, 21 cases/131 assertions exit 0 to 29 cases/161 passed+9 FAILED, with every negative control mutation-proven armed on a scratch mirror. ONE OF MY CONSTRAINTS WAS WRONG AND THE TEST AUTHOR REFUTED IT WITH A MEASUREMENT: I required the converted validate-workflow-contracts.js:521 pin to be RED on baseline, but a deletion cannot be, and every static candidate that IS red on baseline is also red on a valid fix — the cd-only fix leaves the pinned string byte-identical while the rooted fix deletes it, so the two valid shapes share no text and any substring check must reject one of them. The pin is therefore REMOVED, not rewritten, and its coverage was mutation-proven rather than asserted: with the fix applied and Test F green, deleting the Step 9 capture turned Test F RED on 4 assertions including the from-main leg the old pin covered. Trap recorded: validate-workflow-contracts.js has ONE vendored copy under validate-script-sync byte-identity, and editing only the root reds it.

- item: Dock CHANGELOG.md centrally rather than letting four implementers append to one [Unreleased] section, which is a guaranteed conflict surface, and write it BEFORE the chain-receipt run so the receipt does not go stale against it.
  status: done
  dispatched: self, in the worktree; all four implementers were told explicitly not to touch the file and each confirmed it clean in git status
  result: A "### Fixed" section added under [Unreleased] carrying one entry per issue, each stating the OBSERVED failure rather than the mechanism — #969 with the twelve stale files and the measurement that every chain-resident guard verifies canonical-to-render and none verifies render-to-disk; #970 with 27 items across 9 of 36 archived runs and the reason severity comes from the resume rule; #971 with the silent green rather than the filed error message, plus the Step 9 twin and the removed contract pin; #972 with the live reproduction and the stated-not-reconstructed source path. Both accepted consequences are recorded rather than repaired: main's trees can carry unmerged prose, and the edition installers now fail loudly from a worktree. Note for whoever cuts the next release: the release --verify refuses on unknown changelog references, so run it FIRST and de-hash any background citation rather than injecting it.

- item: Fix both edition installers so they deploy from the tree the sync actually writes — install-opencode.sh:152 and install-kimi.sh:122 compute SOURCE_TREE from their own script dir, which is empty in a worktree now that the tree resolves to main. opencode fails loudly; kimi exits 0 having installed nothing, which is the one this must not ship with. The oracle already exists and is already red, so no new test is owed: 77 installer-band failures across the two editions suites when run from a worktree (opencode 56 — P1 20, G1 20, U1 5, S1b 4, S1 3, R1 2, I1 2; kimi 21 — all P1), each naming the cause in its own text, and a grep of those failures for D1/A2/FA*/A31/A32/K13/K14 returns none, so nothing else is hiding among them.
  status: done
  dispatched: implementer subagent in the worktree; owns install-opencode.sh, install-kimi.sh and any CLI surface the sync scripts need to answer "where is the tree"; target state is the test author's measured controls — both suites exit 0 from main at opencode 612 and kimi 570, and the 77 installer-band failures clear when run from a linked worktree; report to land at kaola-workflow/bundle-969-970-971-972/impl-installers.md
  result: kaola-workflow/bundle-969-970-971-972/impl-installers.md — 4 production files. Chose the --print-tree-root shape impl-969 sized and declined, on the reasoning that recomputing the main root in bash would write resolveMainRoot's rule a second time in a second language in the one place where getting it wrong is silent; the installer asks the owner of the rule instead. All 77 installer-band failures clear and the worktree totals now EQUAL the main-posture totals exactly (opencode 612, kimi 570, both postures, zero failures), which is stronger evidence than the band grep I asked for — and both baseline worktree runs had crashed partway, so bands past the crash had never run at all. Hermetic installer runs report COUNTS rather than exit codes, because exit 0 had stopped being evidence: 14 agents / 3 commands and 17 skills from both postures after, against opencode 0 agents and kimi 0 skills before. The silent empty install is mutation-proven dead: the new guard plus the OLD defective root line gives "no skill sources found", exit 1, where it previously exited 0 with 0 skills. Consumer posture verified preserved on an unpacked copy with no .git. IT ALSO REPAIRED A CLAUDE-CHAIN STEP: test-install-adaptive-config.js is a literal step in test:kaola-workflow:claude and runs the REAL install-opencode.sh, so the fast gate itself was red from any worktree between impl-969 landing and this change — my chain run would have failed and I would have been hunting the wrong cause. TWO THINGS ROUTED ON: --print-tree-root is undocumented at docs/api.md:1498, sent to impl-docs; and a RESIDUAL PRE-EXISTING defect it made loud rather than introduced — copy_skills prunes the deployed skill dirs BEFORE copying, so an install that can deploy nothing still removes what was there. Out of scope, not filed by me, surfaced to the user.

- item: Adversarially review the whole diff on the Fable model as the user directed, hunting for the recurring defect classes in this repo: prose absolutes true only of the root edition, guards blind below their threshold or by capitalization, claims about a mechanism rather than a result, and tests repaired ahead of their mechanism.
  status: done
    #971 CAME BACK CLEAN — 0 blocking findings, everything verified by running the shipped bytes rather than the suites: the false green dead in all four copies against fresh git-init-plus-worktree fixtures, every ambiguous resolution case measured (both-trees, neither-tree tracked and untracked, subdirectories of both, detached worktree, bare repo, no-git, and nonexistent/relative/empty overrides), the override winning in every mode including when it points at an empty tree while cwd holds the folder, all six rendered Step 9 blocks executed under bash from three cwds, and the removed pin's replacement independently re-mutation-proved to red BOTH test-bash-block-guards and generate-routing-surfaces --check. THREE THINGS RECORDED, NONE REMEDIATED, each a deliberate call: (1) residue, pre-existing and byte-identical pre/post fix — a tree ALREADY polluted by the old bug's stray run folder still false-greens, because the resolver stops at the stray; the fix prevents new pollution and ships no detection or cleanup of old, this repo's worktree is clean, and the test author had already recorded the case as deliberately unpinned, so it is accepted rather than built on the additive-derivation rule; (2) impl-971's report claims a worktree scan misses project_archived, which the review measured WRONG for tracked archives — the refusal does fire from the worktree — so that sentence in the archived report is false and is corrected here rather than in the file it sits in; (3) .kimi's INSTALLED finalize surface still ships the old Step 9 until the post-merge reinstall, while .opencode already carries the new one, which is expected and is what the queued reinstall is for.
  dispatched: three code-reviewer subagents on the FABLE model, one each for #970, #971 and #972, launched now because those three parts of the diff are settled and only the installers are still being written; each reads the worktree diff for its own files plus the premise, tests and impl reports, and reports to kaola-workflow/bundle-969-970-971-972/review-97N.md. A fourth Fable review covering #969 plus the installer fix follows once impl-installers lands, since that agent may add CLI surface to the sync scripts and would invalidate a #969 review run now.

- item: Remediate the Fable reviews' admitted findings, in custody order — test author adds the case RED, implementer then fixes. #970 R1 (medium, REAL DATA): MISSION_RESULT_LINE at claim.js:4130 in all four copies counts an EMPTY "result:" line as an outcome, and two genuine archived records trigger it (archive/issue-878/mission-list.md:65 with status todo and an empty result: at :73, and issue-899:45), so the shipped report would durably state that an outcome landed where nothing did. #970 R2 (low, comment-only): the comment at claim.js:4109 says 9 of 36 archived lists carry the condition while the predicate beneath it measures 13 of 36, 11 after R1 — a false claim in a comment is the rot class this project names explicitly. #970 R3 (accepted, not fixed): column-0 prose at the front of a line can fake an outcome or suppress a genuine offender under last-wins; constructed only, ZERO instances across all 36 archives, and the front-of-line trade is already documented — recorded so that accepting it is a decision rather than an accident.
  status: done
  dispatched: tests-970 resumed first to add a scaffolded-empty-fields item to the negative control and prove it RED against the shipped bytes; impl-970 follows to fix R1 across all four claim.js copies and correct the R2 comment to the measured count; outcomes append to tests-970.md and impl-970.md. ALSO #972, same custody order via tests-972 then impl-972 — F1 (medium): every unanswerable state prints a bare "codex PASS" BYTE-IDENTICAL to a verified row (empty/nonexistent/unreadable source.path, missing cache dir, an EACCES walk, wrong CODEX_HOME, an @ in the marketplace name), demonstrated on three doors, contradicting the file's own contract at :124-125 which defines PARTIAL as exactly that case — the original #972 defect through a new door. F2 (medium): MIXED ORACLES produce the very non-convergent loop the oracle swap existed to kill — the trigger compares against source.path but the post-refresh proof at :559 judges the resulting version against $ROOT's manifest, so a runtime that converges BYTE-EXACTLY to its source is declared NOT converged, codex FAIL, permanently red and churning remove+add every run, and under --strict it aborts before kimi installs. W1-W3 cannot see it because that fixture holds both manifest versions equal by construction. ALSO #969 R1 (low, and a regression THIS bundle introduced, so it is fixed rather than accepted): bare-repo-with-worktree and submodule postures resolve the tree root INTO a git metadata directory — bare.git/.opencode and super/.git/modules/sub/.opencode — contradicting the rule the shipped comment states, that with no main checkout the tree belongs beside the script. Installs still deploy full counts and nothing is destroyed, which is why it is low, but a generated tree inside git's own metadata is somewhere git may clobber and nobody expects. Routed to tests-969 for the pin, then impl-969. #969 R2 recorded and NOT fixed: the kimi prune-before-copy is confirmed pre-existing by a mutation repro taking a target from 17 skills to 0 on a failed install, the loss is the kaola-namespaced set alone and fully regenerable by re-running, and with the fixed root the guard is near-unreachable because heal-or-abort now precedes the copy — this bundle made an existing SILENT loss loud rather than creating it. THE REST OF #969 HELD under measured fixture attack across 7 postures, and the user's ruling was independently confirmed intact: package.json and all four chain scripts byte-unchanged, and planted edition drift SURVIVES --check and the no-arg default while being repaired only by --write.
    #972 IS CLOSED THROUGH REMEDIATION, and both findings turned out to be one principle in two places: everything the runtime is measured against comes from the directory it is installed from, and a measurement that did not happen is never reported as one that succeeded. F2 was fixed at the SOURCE of the divergence rather than at the two comparison sites — the target version is re-read from the install-from directory's own manifest, so the trigger and the post-refresh proof read one oracle because they read one variable, which is why it is about six lines. F1's scoping is STRUCTURAL rather than conditional: the comparison state can only be assigned inside the local-marketplace branch, so git-sourced and no-provenance rows cannot reach it — nothing attempted, nothing incomplete. Mutation-proven rather than asserted, exactly because I warned that X1-X4 passing is not evidence the scoping is right: degrading when nothing was attempted reds ONLY Z1, both rows, while X1-X4 stay green. Its comment audit found TWO comments made FALSE by the fix — the file header and the --help text both still said the version is compared against the tree's manifest — and corrected both. AN ANOMALY IT FLAGGED RATHER THAN PAPERED OVER, now RESOLVED: a copy of install-all.sh it took mid-round held a different implementation of these same findings and failed Z1 and S6; it could not reproduce it and refused to invent a mechanism, recommending I verify the digest before the chain run. I did: the worktree file matches its recorded sha256 e724cd1e… exactly and carries zero occurrences of the marker. The benign explanation is that the divergent copy carried a source_version local, which is precisely how the TEST AUTHOR described its own satisfiability shadow — a scratch artifact was picked up, not a second implementation of the shipped file.
    #970 IS CLOSED THROUGH REMEDIATION. R1 fixed across all four copies (-U0 digest identical in all four, forge ports again by asserted string replacement rather than fuzzy patch), the cross-edition smoke regrown to seven discriminating items including both empty forms, and R2's comment corrected to a number the implementer measured itself rather than copying: 34 items across 11 of the 36 records, out of 445 read — which also corrected my CHANGELOG, which had carried the premise check's 27-across-9. THE THIRD FINDING WAS MINE TO CHASE AND IT PAID: I asked for the inherited "near-orthogonal" clause to be measured or dropped, and it was measured FALSE — 5 of the 15 affected runs carry both conditions, MORE co-occurrence than independence predicts, not less. The justification for the condition survives on a strictly better footing than the claim it replaces: the two conditions are structurally disjoint at the ITEM level, since one requires an outcome and the other requires its absence, which is true by construction on every record that will ever exist and cannot rot. The false clause was arguing the right conclusion from the wrong premise.

- item: Run the walkthrough at full scope and the chains, remembering the fast gate only samples a 1/12 shard and that an edition-touching diff fails closed to all four chains.
  status: done
  result: ALL GREEN, every exit code read unpiped. Full walkthrough at FULL scope 210/210 exit 0. Four chains exit 0 with accepted_red false, no timeouts and no signals — claude 366s, codex 6s, gitlab 86s, gitea 85s — scope resolved all-four on edition_coupling over 38 changed files. Editions suites, which no chain runs, green from the worktree at opencode 631 and kimi 589 with all six trees in parity and the banner correctly reporting the tree root as MAIN rather than this checkout. ONE REAL FINDING ALONG THE WAY, and it is the user ruling's known cost observed live: the kimi finalize SKILL was stale by three separate changes (the Mission List section, the variant-to-form fix, and the Step 9 capture) while generate-routing-surfaces --check reported all 18 tracked surfaces byte-matching at exit 0. That is exactly the #969 phenomenon, and under the chosen design it is BY DESIGN — --check gains no edition coverage, so nothing reds; the editions suite is what catches it, and it did. Regenerating converged it, and the receipt still binds: the code-tree hash after the regenerate is byte-identical to the one in the receipt, so no validation-visible tracked file moved. I also misread this receipt once, reporting all four chains RED from a wrong field name before checking the actual structure.
  dispatched: self, in the worktree — full walkthrough already run at FULL scope with a real (unpiped) exit code, 210/210 exit 0; chains next via kaola-workflow-run-chains.js --project bundle-969-970-971-972 with KAOLA_RUN_CHAINS_CONCURRENCY=serial, since concurrent chain runs have SIGKILLed on this box; the receipt lands in the run folder in main and is being produced AFTER the last write because docs/api.md is test-consumed and moves the code-tree hash

- item: Finalize the bundle — /kaola-workflow-finalize bundle-969-970-971-972 — closing all four issues all-or-nothing.
  status: todo
