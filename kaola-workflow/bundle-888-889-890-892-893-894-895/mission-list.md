# Close the deferred residue of #891 and the v9.0.0 release cut — #888, #889, #890, #892, #893, #894, #895

Owner rulings taken in conversation before the claim, do not re-litigate:
#894 → delete BOTH orphaned layers. #888 → DELETE the unreachable carry-over and correct the claim.
#893 → widen bucket-2 to this sink's own archive subtree. #890 → convention only, option 1.

Known shared conflict surface: `kaola-workflow-adaptive-schema.js` (the ×4 byte-identical anchor) is
reached by #892 (its test-consumed band at :1016), #894 site 2 (the curated-root vocabulary), and
possibly #889 (if the version constant lands there). Whoever touches it must not race another writer.

MEASURED at claim time: all four anchor copies are byte-identical (md5 `9b77e989a978f2ca3226966dd52a9be1`)
and `node scripts/edition-sync.js --write` propagates byte groups, so only the canonical
`scripts/kaola-workflow-adaptive-schema.js` is ever hand-edited. The three wanted regions are disjoint
by line — #894s2 at 435-472 + the export at 1778, #892 at 1016 (`SELF_HOST_TEST_CONSUMED`, from which
`'docs/mission-list.md'` is simply removed once the file is deleted), #888 at ~1520-1710 — but the
anchor gets ONE owner regardless, because concurrent writers to one file race whatever the line
numbers say. Installer copies `plugins/*/scripts/install-codex-agent-profiles.js` ×3 are wanted by
both #894 site 1 and #889 sites 6-8; same rule, one owner.

ALL WORK HAPPENS IN THE WORKTREE: `/Users/ylpromax5/Workspace/Kaola-Workflow/.kw/worktrees/bundle-888-889-890-892-893-894-895`

- item: #895 — restore the canonical edition's lost assertion that `readActiveFolders` excludes a folder whose issue is already closed; #891 deleted `testClassifierClosedIssueResidueIgnored` (it went unfalsifiable with the overlap axis, correctly deleted rather than rewritten) and every surviving canonical call passes `excludeClosedIssues: false` (`test-active-folders-field-parity.js:145,148`, `test-claim-hardening.js:2089`); mirror the forge shape at `plugins/kaola-workflow-gitea/scripts/test-gitea-workflow-scripts.js:677` and the gitlab twin at :693 — plant one open-issue folder and one closed-issue folder, drive `readActiveFolders(root)` on DEFAULT options against a gh mock, assert only the open project survives — then mutation-prove it by flipping the exclusion off in `scripts/kaola-workflow-active-folders.js` and confirming it reds; the behaviour itself is unchanged by #891, so this is test authoring under custody, not a fix; also check whether the `released`-status exclusion (previously `testClassifierReleasedFolderExcluded`, deleted for the same reason) has a surviving canonical assertion or is a second instance of the same gap
  status: in-flight
  dispatched: tdd-guide subagent "w1-895", working in the worktree; sole write-set `scripts/simulate-workflow-walkthrough.js`; output lands as edits to that file plus a written mutation-proof transcript at `kaola-workflow/bundle-888-889-890-892-893-894-895/.cache/895-mutation-proof.md` in the MAIN checkout
  result: DONE — `.cache/895-mutation-proof.md` (transcript) + 101 inserted lines in `scripts/simulate-workflow-walkthrough.js`. Added `testActiveFoldersExcludesClosedIssue895` with a subprocess driver (needed because `OFFLINE` is frozen at module load, so an in-process call would have read green whatever the filter did — a vacuity trap avoided). TWO sub-cases, batched-prefetch and per-issue-fallback, each with a non-vacuity CONTROL assertion proving both folders are visible with the filter off. FIVE mutations recorded red-then-restored, md5-verified against a scratch pristine copy each time (no `git checkout --`): M1 exclusion no-op, M2 prefetch stops memoizing, M3 per-issue probe returns nothing, M4 exclusion ignores the option (arms the control). The two sub-cases are proven INDEPENDENTLY armed — under M3 the batched arm still passed while the per-issue arm reddened. SECOND-INSTANCE QUESTION ANSWERED AND REFUTED: the `released`-status exclusion is NOT a second gap — it survives in `scripts/test-bundle-claim.js:1341-1346` (twin-rule row, in the FAST gate, with a driven-coverage non-vacuity assertion), proven by mutation M5 which reddened it. Nothing was added for `released`. Three near-misses checked by reading and correctly rejected as non-coverage. RESIDUE HANDED BACK: `simulate-workflow-walkthrough.js:845-849` still advertises a section heading naming two scenarios #891 deleted.
  status: done

- item: #894 — delete both support layers orphaned by #891, per the owner's ruling; site 1 is the compare-and-swap half of the atomic writer in `plugins/*/scripts/install-codex-agent-profiles.js` ×3 — `captureAtomicTargetVersion` (:1084, zero callers tree-wide), `atomicWriteSameDirectory`'s `else` branch (:1154-1157), the `if (expectedVersion !== undefined && current)` block in `assertAtomicTarget` (:1107-1113), and `readAtomicTargetVersion` which only those reach; note `assertAtomicTarget` itself is LIVE (called unconditionally at :1200 with `expectedVersion` undefined) and `atomic_stage_conflict` is still thrown from six other sites, so do not over-cut — the sole caller that passed an `expectedVersion` was `seedKaolaConfig`, and its four tests already went with it; site 2 is `extractCuratedRootPaths` / `CURATED_ROOT_PATHS` / `CURATED_ROOT_LC` in `kaola-workflow-adaptive-schema.js`, whose only consumers were the four classifier copies' curated-root-overlap `yellow` that #891 deleted, and which now appear only in the four copies of the anchor itself — that file must stay byte-identical across all four editions, so the edit is one change applied identically four times, and it collides with #892's anchor edit
  status: in-flight
  dispatched: SITE 1 ONLY to implementer subagent "w1-894s1", working in the worktree; sole write-set the three `plugins/*/scripts/install-codex-agent-profiles.js` copies; explicitly NOT the version constant (that is #889's, same three files, later wave) and NOT the anchor; output lands as edits to those three files plus a reachability note at `kaola-workflow/bundle-888-889-890-892-893-894-895/.cache/894-site1.md` in the MAIN checkout. SITE 2 (the anchor vocabulary) is now also dispatched, to implementer subagent "w2-anchor" together with #888 — one owner for the whole anchor, output at `.cache/888-894s2.md`.
  result: SITE 1 DONE — `.cache/894-site1.md`; −213 lines across the three byte-identical installer copies (2998→2927 each), verified byte-identical before (`b4f27cd6…`) and after (`e77dc3e2…`). Deleted `captureAtomicTargetVersion`, `readAtomicTargetVersion`, `atomicWriteSameDirectory`'s `else` arm and `assertAtomicTarget`'s CAS block — each proven unreachable by READING every call site, and by the fact that neither function is exported, so no test could reach the CAS parameter either. Consequentially the now-tautological `expectedVersion` parameter was dropped from both functions; it was the LAST parameter of each, so no positional shift (checked, plus no `arguments`/`Function.length` use). KEPT and justified: `assertAtomicTarget` itself (live on the 2-arg path), `sameFileVersion` (third caller `treeIdentityIsCurrent` is live), and the `atomic_stage_conflict` code (still thrown from the hook-backup path). ARMING PROVEN, not assumed: a scratch inode-swap probe made the surviving guard fire with `atomic_stage_unsafe` identically against both the edited and the pristine `fa5157b3` copy — simultaneously proof the guard is reachable and an observational-equivalence check that the deletion changed no live behaviour. No test written, edited or removed (the four CAS-only tests had already gone with `seedKaolaConfig`). CORRECTION TO THE ISSUE: its "six other sites still throw `atomic_stage_conflict`" double-counted the CAS half it was warning about — after deletion the code throws from ONE site per file, which is the correct outcome. RESIDUE HANDED BACK: the comment at `scripts/test-install-model-rendering.js:157` still names a "shared-config" replacement stage that no longer exists. SITE 2 remains with w2-anchor; this item closes when that lands. || SITE 2 NOW DONE (`.cache/888-894s2.md`): `CURATED_ROOT_PATHS` (:435-443), `CURATED_ROOT_LC` (:451), `extractCuratedRootPaths` (:463-476), their three comment blocks and the export at :1778 — 51 lines + 1 export, deleted from the canonical anchor. Orphanhood proven by READING, not grep: it wrote an `fs.readdirSync` walker (because the local ugrep skips dot-directories) that swept 8697 files across 892 directories; after deletion the three symbols survive only in the three un-propagated anchor copies and in CHANGELOG release history at :2259-2260 (historical prose about v3.21.0, correctly left alone). Zero consumers. Corroborated by the #891 run's own record, which states the symbol "lost its last caller". BOTH SITES COMPLETE — this item is closed.
  status: done

- item: #893 — stop the sink refusing finalize's own freshly-mirrored archive as `foreign_dirt`, per the owner's ruling: widen the bucket-2 exemption in `sinkPreflight` (`scripts/kaola-workflow-sink-merge.js:1274`) to `kaola-workflow/archive/<project>/**` for THIS project only, classification-only and byte-superset-verified against the branch exactly as bucket 2 already does, so a sibling's archive tree stays bucket-3 and never-touch-another-project holds; observed twice running (bundle-886-887, issue-891) on the documented sequence `cmdFinalize --keep-worktree` then `sink-merge --sink`, where finalize's `finalize_transaction.mirror` step writes the archive tree untracked into the main root and the sink then refuses it with zero mutation; the existing `SINK_RECEIPT_EXEMPT` regex already understands the archive path and its comment carries the argument verbatim ("not-refusing is not mutation"), it is merely keyed to one filename; while here, the hard-coded `projStateFiles` list predates ADR 0017 and never names `mission-list.md` — the run record every run now writes — and `mission-list` appears nowhere in the sink at all
  status: done
  dispatched: TEST FIRST to tdd-guide subagent "w1-893t", working in the worktree; sole write-set `scripts/test-sink-merge.js`; it authors a failing assertion for the widened exemption against the CURRENT (refusing) sink and proves it reds, output landing as edits to that file plus a baseline transcript at `kaola-workflow/bundle-888-889-890-892-893-894-895/.cache/893-test-baseline.md` in the MAIN checkout. The implementation half — `scripts/kaola-workflow-sink-merge.js` and its three twins — is NOT dispatched yet and follows once the test exists. || TEST LANDED, and it corrected my brief on a load-bearing point: `archiveProjectDir` (`kaola-workflow-claim.js:2474-2486`) shows the branch NEVER carries the archive path on a `--keep-worktree` finish (finalize cannot stage outside its own worktree, so it defers the commit to the sink) — so a bucket-2-style "does the branch carry this" check can never fire on the observed shape, and my "byte-identical to what the branch carries" framing was wrong. It also caught that bucket 2's ACTION is `fs.unlinkSync` (`:1426-1431`), so treating the mirror as bucket 2 would DESTROY main's only copy of the finalization summary and mission list. SPEC APPROVED BY ME: absent on branch → exempt; present and byte-equal → exempt; present and DIVERGENT → stays foreign dirt. Classification-only (`continue`), never unlink. 16 assertions red at baseline reproducing the issue-891 envelope verbatim; oracle proven SATISFIABLE by a scratch candidate patch (208 pass, exactly the 16 flip); fences proven ARMED by M1 (prefix without segment boundary reds w3/w4) and M2 (blanket over the whole archive band reds 19 including the pre-existing #715 over-exemption guard). Implementation now dispatched to implementer subagent "w2-893i", write-set `scripts/kaola-workflow-sink-merge.js` + `plugins/kaola-workflow/scripts/kaola-workflow-sink-merge.js` + `plugins/kaola-workflow-{gitea,gitlab}/scripts/kaola-{gitea,gitlab}-workflow-sink-merge.js`, output at `.cache/893-impl.md`.
  result: DONE — `.cache/893-impl.md`. Suite goes 16 failed/192 passed → **208 assertions, 0 FAIL**; exactly the 16 flipped. The exemption sits immediately after `SINK_RECEIPT_EXEMPT` and only ever `continue`s or falls through — it never pushes to `foreignDirt`, never `break`s, and is never appended to `projDuplicates` (whose action is `fs.unlinkSync`), so #715 still wins for the receipt path where the two path sets intersect. Uses `git show` rather than bucket 2's `cat-file -e` deliberately, since an existence probe is false in the observed shape; compares raw Buffers so it is binary-safe. Scoped by a trailing-slash segment boundary using a plain string test (not a regex, so a project name with metacharacters cannot widen it), and gated on `xy === '??'` so a tracked modification under the archive path keeps refusing — that narrows the widening rather than expanding it. Forge ports treated correctly: canonical + `plugins/kaola-workflow/` were byte-identical and stayed so; the gitea/gitlab ports are GENUINE ports (different basenames, line counts, idiom, and no `#711` branchless handling) and each got a real per-forge edit in its own idiom. MUTATION-PROVEN ARMED: M1 (segment boundary removed) reds exactly the prefix look-alike and nothing else; M2 (widened to the whole archive band) reds all of w3 plus the pre-existing `#715 (l)` over-exemption guard — the never-touch-another-project invariant is defended by two independent suites. `projStateFiles` FINDING, and it inverts the issue's suggestion: adding `mission-list.md` there would CREATE a data-loss route, because bucket 2's superset test is `cat-file -e` (existence, not content) and its action is unlink — so a stale branch copy would license deleting main's newer run record, which is the exact failure this fix exists to avoid. It also cannot bite today: `archiveProjectDir` deletes or relocates main's live folder on both postures, and in the only shape that reaches it 6 of 8 files are off-list anyway, so adding one name turns a refusal listing six paths into one listing five. LEFT ALONE, per additive derivation. It surfaced a real separate gap worth a follow-up: `--sink` never runs the `run_not_finalized` measurement the legacy path runs.
  status: done

- item: #888 — delete the release-prep carry-over, per the owner's ruling, and correct the claim it makes; #881 shipped it so `--tag` would not force a redundant four-chain run after `--prepare`, but `evaluateReleaseReceipt` (`kaola-workflow-adaptive-schema.js`, step 2) requires every path changed between receipt commit and candidate to be a member of `RELEASE_FILES`, and the sink's `chore: archive <project>` commit — whose entire content is `kaola-workflow/archive/<project>/**` — always interposes, so the precondition is unreachable through the only sequence the workflow has; measured refusing at the v9.0.0 cut with `carryOver.failed: "off_surface"`; the four-chain demand at the release commit is unchanged and stays mandatory, tests fall out with the mechanism rather than being repaired ahead of it, and CHANGELOG plus `docs/` must stop describing a saving that never happens
  status: in-flight
  dispatched: to implementer subagent "w2-anchor", working in the worktree, which owns the ×4 anchor for BOTH #888 and #894 site 2. MEASURED blast radius: `scripts/kaola-workflow-adaptive-schema.js` (13 hits) + 3 plugin copies via byte group, `scripts/kaola-workflow-run-chains.js` (5) + `plugins/kaola-workflow/scripts/` twin + the two differently-named forge ports `kaola-{gitea,gitlab}-workflow-run-chains.js` (5 each), `scripts/kaola-workflow-release.js` (2) + 3 ports, `scripts/test-release.js` (6), `scripts/test-finalize-door.js` (1), `docs/conventions.md` (:598-599 and :612-613 describe the carry-over binding — I finished my own #890 edit at :566 in that file first, so it is free). CARVED OUT: `scripts/simulate-workflow-walkthrough.js` carries 1 carry-over reference and is held by w1-895; I will make that single edit myself once w1-895 lands. Also carved out: `edition-sync --write` — I run the ×4 propagation myself at the end so two agents never race it. Output lands as edits to those files plus a note at `kaola-workflow/bundle-888-889-890-892-893-894-895/.cache/888-894s2.md` in the MAIN checkout.
  result: DONE — `.cache/888-894s2.md`. Anchor −160 lines, run-chains −10, release −15, test-release −93, test-finalize-door −72. Deleted `evaluateReleasePrepCarryOver`, `firstJsonDifference`, `RELEASE_VERSIONED_JSON_FILES`, the binding `else` arm, and the `binding`/`carryOver` envelope keys; `--release-check` now binds by exact `headSha` equality alone, and the four-chain/unwaived/clean-stamp demand is untouched. THREE CORRECTIONS TO MY BRIEF, all of which would have cost real damage: (1) the forge run-chains/release ports are NOT hand-maintained — `edition-sync.js GENERATED_AGGREGATORS:52-65` renders them, so hand-editing would have had to reproduce the render byte-for-byte; it ran the repo's own `renderForgePort` instead and `--check` is green. (2) `release.js`'s `receiptBindsTo` could not be left alone: with `carryOver` gone its body `verdict.ok || !(verdict.carryOver && …)` evaluates to ALWAYS TRUE, which would have silently made the surrounding `chains_stale` arm unreachable and disarmed `--verify`'s staleness warning — it deleted the helper and verified the result restores the pre-#881 line BYTE-FOR-BYTE against `git show 523f1241^`. (3) the walkthrough handback was not one reference but ELEVEN lines and FIVE scenarios, one of which (`#877 (14)`) was already RED. PUBLIC ENVELOPE CHANGE, flagged: `--release-check --json` no longer emits `binding`; `docs/api.md:411` had already documented the envelope WITHOUT it, so removal makes code and doc agree — #881 added the key and never documented it. Coverage checked before dropping: `T5k`'s `stale_paths` diagnostic is independently pinned by the `#651 (4)` scenario, and `T5b` survives. PROCESS ERROR self-reported and INDEPENDENTLY VERIFIED BY ME: it line-spliced the MAIN checkout's `scripts/test-release.js` (−92 lines) before noticing its cwd was wrong, and restored via `git show HEAD:… >` — I confirmed main has ZERO tracked diffs and `test-release.js` is md5-identical to HEAD (`f6b78f10…`). HANDBACK APPLIED BY ME: deleted walkthrough lines 1248-1335 (the `fixturePkg651`/`commitPrepOnly651` helpers and cases 14-18) and rewrote the two stale headers; `node --check` clean and `testReleaseCheckPreTagGate: PASSED`. Propagation debt deferred to my central `edition-sync --write`: `validate-script-sync.js` reports 5 expected items (run-chains, release, and the three anchor copies).
  status: done

- item: #890 — settle upstream forge references in the changelog by convention alone, per the owner's ruling (option 1, which the issue itself calls probably right): `--prepare` extracts every `#\d+` from `[Unreleased]` and cannot tell this repo's issue numbers from another forge's, so cutting v9.0.0 forced `#19792` and `#33447` — an openai/codex PR and issue — into `--issues-closed`, putting two false statements in the release receipt; the repo already writes the same PR correctly in an older entry as "upstream Codex PR 19792" with no `#`, so the two offending lines are the deviation not the rule; document the convention wherever changelog style is documented, do NOT teach the extractor (machinery for a failure whose only cost is one metadata inaccuracy and one loud non-destructive refusal), and do NOT edit the two lines now sitting under `## [9.0.0]` — released history is not edited casually
  status: in-flight
  dispatched: self, inline, in the worktree; output lands as an edit to wherever changelog style is actually documented (to be located — candidates are `docs/conventions.md`, `CONTRIBUTING`, or the CHANGELOG's own header) plus a `[Unreleased]` note; no other agent holds those files, and I will finish before #892's editing half opens `docs/conventions.md` in wave 2
  result: DONE. `docs/conventions.md` §Release, new bullet at :566 (14 lines, in the worktree) — upstream forge refs are written without the hash (`openai/codex PR 19792`), a bare `#886` keeps meaning an issue in this repo, and the extractor is deliberately NOT taught `owner/repo` slugs because `--prepare`'s refusal is loud, non-destructive and zero-mutation. Measured while writing it: `#19792` appears with a hash TWICE (CHANGELOG.md:320 and :323, both under `## [9.0.0]`) and `#33447` once (:320) — the issue counted unique numbers, not lines, and both lines are released history left untouched as ruled. The correct no-hash form already exists at :493 under `## [7.0.0]`. One-wording check: `docs/conventions.md` is the ONLY surface stating changelog style — there is no `CONTRIBUTING.md`, and the `:674` table row is a provenance-location entry, not a competing statement of the rule. NOTE: my first application of this edit went to the MAIN checkout by mistake; it was copied into the worktree and main was restored path-scoped (`git checkout -- docs/conventions.md`, verified `git status` clean but for the untracked run folder). The CHANGELOG `[Unreleased]` entry for this belongs to the docking item, not here.
  status: done

- item: #889 — make a `behavior_contract_version` bump a single edit, or make an incomplete one fail on the FIRST site rather than the fifth, and leave no fixture regex pinned to a version literal; the #886 bump from 2 to 3 took eleven sites and `edition-sync --write` propagated only three — the issue tabulates all eleven, including `plugins/*/scripts/install-codex-agent-profiles.js` ×3 which no byte group carries (plugin-only, no canonical in `scripts/`), `validate-vendored-agents.js` with two assertions, `validate-kaola-workflow-contracts.js` plus its gitlab/gitea ports which are not a byte group because the basenames differ, and `install.sh` inside a heredoc that cannot import anything; the defect half is two fixture regexes pinned to the literal `2` (`/^behavior_contract_version: 2\n/m` and `/^behavior_contract_version: 2$/m`) that silently matched nothing once the tree said 3 — their mutation cases ran, were counted, and tested nothing; a fix must say what it does about the heredoc and about `kaola-workflow-adaptive-schema.js` being the natural home yet also the byte-identical cross-edition anchor; also fold in the one-rule-two-wordings instance where `validate-vendored-agents.js` carried the count in its failure MESSAGE as well as its condition
  dispatched: split by test custody into two concurrent subagents, both in the worktree, after w1-894s1 freed the installer copies. MEASURED FIRST, and it changes the picture: the two vacuous fixtures are no longer pinned to `2` — they were bumped and are now pinned to `3` at `scripts/test-install-model-rendering.js:2645` and `:2650`, so the defect MOVED with the version rather than being fixed, which is exactly the shape #889 predicts will recur. (1) implementer "w2-889p" owns the PRODUCTION sites: `scripts/generate-reviewer-profiles.js:361`, `scripts/kaola-workflow-codex-preflight.js:95` + its gitea/gitlab/plugin twins, `plugins/*/scripts/install-codex-agent-profiles.js:98` ×3, `scripts/validate-vendored-agents.js:120` (message) and `:123` (condition), `scripts/validate-kaola-workflow-contracts.js:622` + the gitlab/gitea ports, and `install.sh:248,249,269` — output at `.cache/889-production.md`. (2) tdd-guide "w2-889t" owns `scripts/test-install-model-rendering.js` ALONE — un-pin the two fixtures so they derive the version instead of naming it, mutation-prove both are armed, and fix the stale "shared-config" comment at :157 that w1-894s1 handed back — output at `.cache/889-fixtures.md`. Neither may touch `scripts/kaola-workflow-adaptive-schema.js` (w2-anchor holds it), so if the constant's only correct home is the anchor they must stop and report rather than race. || FIXTURE HALF DONE (`.cache/889-fixtures.md`): both fixtures now match the version as `\d+` and route through a new `replaceOnce(source, pattern, replacement)` helper carrying TWO loud-failure arms — `matches.length === 1` (fires when a pattern stops matching or starts matching twice) and `mutated !== source` (fires when the substitution is a no-op, which is what makes the hardcoded `1` safe). Deliberately NOT derived from the production constant: a fixture sharing the guard's constant would go blind to a profile carrying the wrong version, so it derives from the artifact under test instead. Mutation-proven four ways on a tar-copied scratch mirror — both fixtures red when their production guard is disabled, both survive a simulated bump to version 4, both loud arms fire when deliberately broken, and the OLD fixtures red on that same bumped tree. IT REFINED THE ISSUE'S CLAIM: on a fully consistent bumped tree the old fixture does not silently go green — it reds with `got []`, a message that misdirects by accusing the production guard of having stopped reporting a code. The defect is that it tests nothing AND misdirects; they survived the real 2→3 bump only because the tree was mid-flight and the suite died elsewhere first. TWO SITES FOUND THAT THE ISSUE'S TABLE OF ELEVEN MISSES, forwarded to w2-889p: `templates/reviewers/behavior-contracts.json` (×3 roles — arguably the actual source of truth) and `scripts/test-install-model-rendering.js:2993` (`columns[2] === '3'`, invisible to a `behavior_contract_version: 3` grep). The :2993 pin is NOT the silent defect — it reds loudly — and it was deliberately left underived, because deriving it from the source profile would make the manifest agree by construction and weaken what the column pins; I agree and endorsed that call. Also fixed the stale `:157` comment w1-894s1 handed back, verified against the four `runStageRegression` cases it heads — and found the old wording was wrong in TWO ways, not one: it named a shared-config stage that is gone, and it never described case 4 at all. PRODUCTION HALF ALSO DONE (`.cache/889-production.md`), and it REFUTED the issue's constraint (a) outright: `install.sh`'s heredoc is NOT a require-less island — it already does `require(process.argv[2])` at `:238`, where argv[2] is the generator path passed by the wrapper. The module was in hand the whole time; only the version was not read from it. That removes `install.sh` from the bump surface entirely, which the issue assumed impossible. Design: `scripts/generate-reviewer-profiles.js` exports `REVIEWER_BEHAVIOR_CONTRACT_VERSION` as ONE literal — deliberately NOT read back out of `behavior-contracts.json`, because the constant's job is to state which version this code UNDERSTANDS, and deriving it from the file it validates would make the check unfalsifiable (the same defect class as the vacuous fixtures). The anchor was NOT needed and is untouched — it is the wrong home on the merits, since the constant serves one subsystem whose canonical module already sits in `scripts/`. Acceptance branch A is unreachable (the seven preflight/installer copies run from installed plugin trees where the generator does not exist), so branch B was delivered: `checkContractVersionPins` sweeps all seven and reports EVERY disagreement in one message, wired into two call sites that between them cover all four chains. IT FOUND THREE MORE STALE-MESSAGE INSTANCES than the brief named, two of which were ALREADY WRONG in the tree (condition `=== 3`, message "version 2"). Its executed before/after experiment found the problem is worse than the issue states: on HEAD a partial bump took NINE rounds, and after round 6 all five reviewer validators reported GREEN while five sites were still wrong — a whole chain can pass over a half-done bump. After the fix: one edit, then one message naming all seven remaining sites. Sweep mutation-proven armed on three branches with a `[]` control. THE TWELFTH SITE, settled by me: my two #889 agents disagreed about `test-install-model-rendering.js:2993`; I ruled derive it from the exported CONSTANT (not from the source profile, which would have been tautological and which both agents were right to reject), and w2-889t proved it both directions — still reds when the installer writes a wrong version, no longer reds on a correct bump. The bump procedure is now three steps, not four. FOLLOW-ON: that ruling left `generate-reviewer-profiles.js` documenting the OPPOSITE of what ships (its step 4 says "do not fix it by deriving"), so w2-889p is correcting its own prose — the one-rule-one-wording defect appearing inside the fix for a one-rule-one-wording defect. PROSE CORRECTION LANDED: the bump procedure now reads three steps and closes "there is no fourth step: every other consumer, in the repo and in the suites, reads this constant"; the `CONTRACT_VERSION_PIN_SITES` non-membership note was re-justified on the durable MECHANICAL reason (membership is files that DECLARE the constant, since that is what the sweep pattern matches — a file that merely READS it cannot be a pin site) rather than the now-false "independent expectation" reason. It also corrected §5a and §6 of its OWN report in place, on the grounds that a report asserting a false fact about the shipped tree is the same defect in a different file — and flagged that its CHANGELOG draft had gone stale as a result, which I forwarded to w4-dock. Re-verified after: `generate-reviewer-profiles --check`, `validate-vendored-agents`, `validate-kaola-workflow-contracts` all exit 0. #889 COMPLETE.
  status: done

- item: #892 — give the mission-list format exactly one wording and remove the dead pointer: the `next` surfaces tell the reader to read `docs/mission-list.md`, a bare repo-relative path that resolves only in this repo, so a freshly-initialized consumer repo reports it missing; measured, the `init` surfaces never name it and the only mission-list path init injects (`kaola-workflow/{project}/mission-list.md`, `workflow-init.md:134`) is correct and must stay; nothing ships or creates the doc (`kaola-workflow-install-manifest.js` emits scripts and hooks only, `plugins/kaola-workflow/` has no `docs/`, init Step 4 scaffolds five other things); the guard at `test-route-reachability.js:805` is green because `REPO` is the AUTHORING site, not the reading site; the fix is to inline the ~18 lines of file shape from `docs/mission-list.md:21-46` into `templates/routing/next.skeleton.md` (the pointer is at :206, pinned as block `nx-mission-list` in `templates/routing/required-blocks.js:84`), DELETE `docs/mission-list.md` since an un-pointed-at copy is the same defect with the symptom removed and its rationale already lives in `docs/decisions/0017-the-mission-list.md`, and collapse the other two statements — `init.skeleton.md` at 154-157, 176-177 and 317-318, and root `CLAUDE.md:16-34` — onto the surviving wording or a DECLARED named region stating the capability difference, never a third incidental paraphrase; the trap is that init's output is a generated consumer `CLAUDE.md`, so its copy must be collapsed by restatement or a declared region and NEVER by a path, and init's Step 4 scaffold list must not grow a `docs/mission-list.md` line; no wording may drop the `dispatched` locator, which `init.skeleton.md:155` already does and the #877 resume drill established as the load-bearing half; follow the known call sites — `CLAUDE.md` ×2, `docs/conventions.md:5`, `docs/README.md`, `test-route-reachability.js:805` and its `LEGACY_PAIRS` token at :842, `required-blocks.js:84`, `scripts/validate-workflow-contracts.js:245` and the plugin twin, `scripts/test-generate-routing-surfaces.js:300`, and the test-consumed band at `kaola-workflow-adaptive-schema.js:1016` which propagates to all four editions and collides with #894 site 2; settle the weaker second instance of the same class in the same change — `commands/workflow-init.md:178` lists `docs/workflow-state-contract.md` in the Documentation Map it writes into a consumer's `CLAUDE.md`, and that file is neither scaffolded nor shipped; regenerate and expect `generate-routing-surfaces.js --check` clean at 18 surfaces; edition-touching, so this owes the full four-chain run
  status: done
  dispatched: RECON FIRST to a read-only investigator subagent "w1-892r", working in the worktree; it edits NO tracked file and lands a precise, verified edit plan at `kaola-workflow/bundle-888-889-890-892-893-894-895/.cache/892-surface-map.md` in the MAIN checkout — the exact surface inventory, the four wordings diffed against each other, every live call site confirmed by reading rather than grepping, and a named recommendation for init's copy (restatement vs. declared region). The editing half is NOT dispatched yet. || RECON LANDED at `.cache/892-surface-map.md` (36KB, an executable edit plan) and it undercounts the issue on THREE axes: (1) the dead pointer reaches **12 installed surfaces across 4 runtimes**, not 6 — the opencode and kimi trees regenerate from the same skeleton via `commandSurfacesForForge()`; (2) there is a SECOND red-on-fix assertion the issue never named, `test-route-reachability.js:156`, which positively REQUIRES the dead pointer on each Codex next SKILL; (3) there are **seven** wordings of the format, not four — `README.md:894-942`, `docs/architecture.md:30-55` and ADR 0017 also carry it. It also REFUTED the issue's locator attribution: `docs/mission-list.md:41` does NOT carry the locator in its table — root `CLAUDE.md:23` is the only site in the repo that binds the locator to the field definition. And it found the substantive problem is worse than filed: init's KW-CLAUDE-TEMPLATE region carries the locator NOWHERE on the write side while using it on the read side at :177, so the template tells a successor to check something it never told anyone to write. Two facts die with the file unless inlined: `M` (items identified by order, no stable ID — load-bearing, a real decision was made citing it) and `P` (absent fields are simply absent, UNIQUE to that file). MY SCOPE CALL on its open question 4.4: take the recommended reading — *one rule, one wording* governs prompt surfaces and generated templates, so the two skeletons collapse and root `CLAUDE.md` keeps its field table, while `README.md` / `docs/architecture.md` / `docs/api.md` / `docs/workflow-state-contract.md` get only their dead references fixed, and ADR 0017 is untouched because an ADR is a historical record that must not be rewritten to match a later state. Its init recommendation (restatement declared as a new `in-mission-list` block in `required-blocks.js`, never a pointer, never a REGION) is ACCEPTED — it is forced anyway by an existing armed guard at `validate-workflow-contracts.js:470-471` that pins the region to a token set no pointer sentence can satisfy. Editing half now dispatched to implementer subagent "w3-892i", output at `.cache/892-impl.md`; `docs/conventions.md` and the ×4 anchor are CARVED OUT because w2-anchor still holds them — I apply those two one-line edits myself afterwards. A tdd-guide pass to author and mutation-prove the NEW pinning tokens follows once the skeleton text is final.
  result: DONE — `.cache/892-impl.md`, plus the two carve-outs applied by me. The shape is inlined into `next.skeleton.md` carrying both facts that would otherwise have died with the file (`M` stable-ID, `P` absent-fields), both `dispatched` placeholders carry the locator, and the table's `dispatched` row uses root `CLAUDE.md:23`'s locator wording rather than the deleted doc's weaker one. `docs/mission-list.md` `git rm`'d. init collapsed by RESTATEMENT with the divergence declared as a new `in-mission-list` block in `required-blocks.js` (never a pointer, never a REGION); the skill-only duplicate restatement deleted, dropping the copy count 7→6; Step 4's scaffold list byte-unchanged. `docs/workflow-state-contract.md` dropped from the consumer template rather than scaffolded. 18 surfaces regenerated, 12 changed. THE RED-ON-FIX ASSERT THE ISSUE MISSED (`test-route-reachability.js:156`) was re-pointed at the inlined token rather than deleted, so the Codex next SKILLs are still positively required to carry the format. Three new pins MUTATION-PROVEN armed. It overruled the plan on item 22 and I agree: the `validate-kaola-workflow-contracts.js:304` parenthetical is a LOCATOR, not a historical claim, so the dead path went while the historical sentence stayed. ADDITIVE EDITIONS PROVEN, not assumed — opencode ×3 and kimi ×3 regenerated and their suites pass (490 + 505 assertions), all six generated trees grep clean, and each of the six installed next surfaces verified to carry both new facts. It correctly did NOT regenerate the MAIN checkout's six trees: those were generated from `main` and regenerating them now would sync them TO main, proving nothing and disturbing the owner's installed state. MY CARVE-OUTS, now applied: `docs/conventions.md:5` re-pointed at the ADR, and `'docs/mission-list.md'` deleted from `SELF_HOST_TEST_CONSUMED` (the root copy had MOVED to :964 under w2-anchor's demolition while the plugin copies were still at :1016 — a line-number trap the report caught). Central `edition-sync --write` run once by me afterwards: **anchor byte-identical ×4, one hash `3f221dea…`**, `validate-script-sync` exit 0 ("4 Oracle Kernel copies identical at HEAD"), `edition-sync --check` exit 0, and the tree-wide `git grep docs/mission-list` sweep now returns NOTHING. One gap it flagged for hardening, now CLOSED by tdd-guide "w4-892h" (`.cache/892-hardening.md`): the four field-table ROWS are pinned as whole rows in `required-blocks.js`'s `nx-mission-list` block, one file changed. IT AVOIDED THE OBVIOUS TRAP: `**where the output was to land**` appears TWICE on every next surface — the table row AND write moment 2 — so pinning the bare CLAUSE would have stayed green while the locator was stripped from the table, which is the exact "green for a reason unrelated to the property it names" shape #892 was filed against. The row is the unit because a row is one field's complete definition, including the `written` column that exists nowhere else on a shipped surface. Declared in the manifest ONLY, not duplicated into `validate-workflow-contracts.js` or `test-generate-routing-surfaces.js`, because those read a strict SUBSET (6 tracked surfaces) while `checkManifest` derives the obligated set from topic+tags and so reaches all TWELVE — one declaration, twelve surfaces, no way to obligate 10-of-12 by omission. All four rows individually mutation-proven armed. A2 is the proof that matters: it made the regression the project's OWN rule produces — edit the skeleton, regenerate — and `generate-routing-surfaces --check` stayed EXIT 0 and blind, with only the manifest pin reddening, naming all twelve surfaces. THE NEGATIVE CONTROL: with that regression still in place it reverted the manifest to its pre-pin state and every single guard went green — `--check`, route-reachability, both contract validators, test-generate-routing-surfaces. So the locator could have been deleted from the field table on every shipped surface with a fully green tree; these four tokens are the only thing that closes it. `validate-script-sync.js` now exit 0 (the five earlier entries were other agents' in-flight files, since landed). It also disclosed a self-inflicted false red — an early `--check EXIT=1` was its own zsh quoting error, not a failure — rather than letting it stand in the record.

- item: adversarially verify the landed batch before it is called done — the recorded lesson from #881–#885 is that four green chains certified a bundle whose headline fix was not implemented, and the defects were found afterwards by a reader, not by a test; check in particular that each new or widened guard is MUTATION-PROVEN armed rather than merely green (#895's new assertion, #893's widened exemption, #889's un-pinned fixtures), that the #894 deletions removed nothing still reachable from a call site actually read rather than grepped, that #892 left no surface naming a repo-relative path that fails to resolve in a consumer repo and no fifth wording of the format, and that #888's deletion left no test repaired ahead of its mechanism
  status: done
  dispatched: three adversarial-verifier subagents in parallel, all READ-ONLY, all against the final worktree, each with a distinct lens so redundancy does not substitute for coverage — "v-guards" (is every new/widened guard genuinely armed, and does any assertion pass for a reason unrelated to its name), "v-deletions" (did the four deletions remove anything still reachable, and was any test repaired ahead of its mechanism), "v-surfaces" (does any shipped surface still name a path or route that no longer exists, across all 12 next surfaces and 4 runtimes). Outputs land at `.cache/verify-guards.md`, `.cache/verify-deletions.md`, `.cache/verify-surfaces.md` in the MAIN checkout. Running the fast gate concurrently for early breakage signal — NOT as the binding receipt, since any fix these three force would invalidate it. || v-guards LANDED (`.cache/verify-guards.md`) and FOUND A BLOCKING DEFECT in #893, at exactly the target I named. `sink-merge.js:1411-1419` (+ plugin twin, gitea :1383-1388, gitlab :1390-1395) reads `branchBytes === null` as "absent on the branch", but the `catch (_) {}` above also swallows every READ FAULT — so the three-way rule silently loses its third arm and a DIVERGENT branch copy gets exempted whenever `git show` fails for any reason other than absence. Three executed triggers on w4's own fixture: an unreadable loose blob; a **65 MiB divergent copy with NO tampering at all**, which trips `ENOBUFS` past `GIT_MAX_BUFFER`; and end-to-end with no foreign file the sink stops emitting `sink_blocked` entirely — it gets past preflight, reaches `push_upstream`, and dies on `git checkout` with **exit 1 and no JSON envelope**, an untyped crash where w4 demands a typed refusal. It measured WHY bucket 2 lacks the defect rather than reasoning it: under the identical fault `git cat-file -e` exits 0 while `git show` exits 128, and `-e` emits nothing so it cannot ENOBUFS. REPAIR (dispatched): probe `cat-file -e` FIRST — non-zero means genuinely absent, which is the observed shape and leaves w1 untouched; zero means the branch carries it, so a failed `git show` is UNVERIFIABLE and belongs in foreignDirt. Test half to tdd-guide "w5-893t" (`scripts/test-sink-merge.js`), then the fix to implementer "w5-893i" (the four sink copies); outputs at `.cache/893-readfault-test.md` and `.cache/893-readfault-fix.md`. Its other verdicts: #892 CONFIRMED (it reproduced my negative control independently, and additionally destroyed the table on all 18 surfaces while re-emitting the rows inside an inert fence — everything stayed green, which is the boundary the hardening record already declares); #889-fixtures CONFIRMED not tautological (hardcoding a wrong version at `install.sh:272` reds `:3000`); #889-production WEAKER-THAN-CLAIMED (**nothing references `checkContractVersionPins` outside the generator** — replacing both call sites with `[]` leaves all five relevant guards green, so the sweep itself is unguarded; bounded impact, measured: with it disarmed AND all seven pins stale, two suites still red, so what is lost is the one-message diagnosis and letting the gitlab/gitea chains go green over a half-done bump); #895 WEAKER-THAN-CLAIMED (env isolation verified real under a hostile parent env, but both sub-cases use 10-open/11-closed so a parity-keyed filter would pass, and three plausible `issueIsClosed` regressions survive — including removal of the OFFLINE short-circuit, which is the very thing the subprocess driver exists for). || v-deletions LANDED: **all four deletions CONFIRMED SAFE**, 0 blocking. It independently re-verified the `receiptBindsTo` byte-for-byte restoration against `git show 523f1241^`, BUILT the deleted carry-over fixture from scratch rather than importing the test file, asserted its preconditions held before testing the verdict, confirmed `--verify`'s staleness arm still FIRES, and swept every changed conditional in the bundle for the lost-conjunct class — exactly two survivors, both correct, nothing constant, no unreachable error path. It corrected TWO premises in the agents' own reports while confirming their conclusions: `.opencode*`/`.kimi*` DO exist (12 generated gitignored trees across both checkouts — the earlier "do not exist at all" was inferred from `git ls-files` and would have misled the next sweep), and 892-impl's two carve-outs have since landed. It also recorded ONE non-blocking coverage fact per derive-additively: re-introducing the exact mechanism #888 deleted is invisible to every authored suite, and a BROAD relaxation is caught by exactly one test (`#651 (4)`) which the fast gate samples at 1/12. || v-surfaces LANDED: **4 defects**, plus the consumer-repo simulation RAN and came back CLEAN (sandboxed HOME, real `install.sh --forge=github`, fresh git repo, every backticked path token in the installed consumer CLAUDE.md resolved against its own Step 4 scaffold). R1 LEAD: `README.md:919` still carries the OLD weaker `dispatched` wording while :906/:911/:922-923 around it were changed by this bundle — a consumer-facing artifact contradicting itself on the exact field the row pin protects. R2: the seven "for the file format" references were repointed at ADR 0017, whose own field table carries the weaker wording and never says "where the output was to land". R3: a FOURTH #888 residue I missed — the comment above `runCut` ×4 still describes the chain run as conditional and tells a future editor to keep the qualifier. R4: the `in-mission-list` declaration is FALSE — 0 of 9 sentences verbatim, and init carries "The agent writes it; no script owns it." which appears nowhere in the next wording, so a subset cannot carry it. All four dispatched to implementer "w6-surfaces" (`.cache/surface-fixes.md`) and ALL FOUR NOW FIXED. R1: `README.md:919` canonical. R2: all seven references reworded to name the ADR as the DESIGN RECORD / derivation rather than the file format, ADR untouched, and none repointed at `next.skeleton.md` (which is not openable from a consumer repo). R3: the `runCut` comment rewritten to "Step 3 is UNCONDITIONAL … Keep it a plain imperative step with no qualifier", operator string and `sequence` untouched so `--cut` emits exactly as before; twins propagated by my central `edition-sync --write` afterwards — `validate-script-sync` and `edition-sync --check` both exit 0, anchor still one md5 across four copies. R4 MADE TRUE BY MAKING THE TEXT A REAL SUBSET, not by softening the declaration: added "No script owns this file; you write it." to the next surface, rewrote init's four format bullets from whole next sentences only, and reordered them into next's own order. **Subset proof 70/70 verbatim (14 sentences × 5 init/next pairs), order preserved, span derived from `required-blocks.js`'s own tokens rather than a hand-typed line list** — and mutation-proven three ways: restoring HEAD's init text gives 8/8 ABSENT (independently reproducing v-surfaces' finding), rephrasing one word gives 1 ABSENT, and an ORDER-ONLY swap with no word changed is caught. One forced next-side change it surfaced honestly: `**The three write moments.**` → `**Three write moments.**`, because `validate-workflow-contracts.js:473` pins that needle case-sensitively inside init's region, so init could satisfy the pin or be verbatim but not both. TWO CALLS IT ESCALATED, both accepted by me: (1) the two RESUME bullets at `init.skeleton.md:176-177` stay rephrasings because their lead-ins are init-specific trigger conditions with no next counterpart — making them verbatim would mean inventing next-surface text to serve init, so the declaration is now honestly SCOPED to the four format bullets it pins, which is a true declaration rather than a softened one; (2) it fixed one extra site beyond its brief, `docs/architecture.md:38`, which carried the identical stale `dispatched` row as R1 ten lines below the R2 line it was sent to — correct, same defect class. The only surviving instance of the weak wording is now ADR 0017:65, which I ruled untouched. It also disclosed unprompted that it `rm -rf`'d another agent's scratch mirror while building its own — regenerable scratch outside the repo, no repo impact, but it reported destroying something it did not own rather than staying quiet. Its O1, RECORDED NOT BUILT per #892's own ruling: the pins defend the format's PRESENCE, nothing forbids the dead pointer's RETURN — mutation-proven, a full fast gate stays green with the pointer re-inserted on all 12 surfaces. #892 explicitly said a third instance is what would force that guard. || **MY OWN ERROR, CAUGHT BY A SUBAGENT — record this, a successor must not resurrect it.** For #893's Defect 2 I proposed a design in which `cmdFinalize` would `git add` the archive into MAIN's index, making git's index the provenance manifest, and I sent it to the test author with only a vague feasibility caveat. The design is FALSE on a checkable fact and I should have verified it before dispatching. `scripts/kaola-workflow-claim.js:2480` states "The archive is untracked on main until the sink's archive_commit step lands it … cmdFinalize cannot stage a path outside its own worktree, so it defers the commit to the sink", and `:4150-4157` shows the staging block explicitly SKIPPING the main-resident dest because `path.relative(root, dest)` escapes the worktree. Issue #893's own observation says the files were "untracked in main". The test author acted on it and rewrote the fixtures to plant the mirror STAGED and gate on porcelain `A `, which would have pinned a shape production never produces — and its new `w9` asserted that an all-untracked archive tree must NOT sink, i.e. it pinned #893's exact observed bug as required behaviour. Implementer "w5-893i" caught it BEFORE writing the repair, refused to implement against the oracle, and gave six independent confirmations; I verified two of them myself before acting. RESOLUTION: fixtures revert to untracked planting, `w5`/`w6`/`w7` stand as the read-fault oracle, `w8`/`w9` are re-authored against the SETTLED ruling (report, do not refuse), the `??` gate stays, and NO producer change is made — `kaola-workflow-claim.js` is untouched. The general lesson, which is the same one this bundle keeps producing: a green suite would have certified the staged design, because the suite had been rewritten to agree with it. || READ-FAULT DEFECT NOW FIXED AND INDEPENDENTLY PROVEN ARMED. The repair (`.cache/893-readfault-fix.md`) probes existence with `cat-file -e` separately from the `git show` content read, giving a FOUR-way rule — not-carried → exempt, byte-equal → exempt, divergent → foreign dirt, **unreadable/truncated → unverifiable → foreign dirt** — in all four copies, `??` gate kept, classification-only, ports keeping their own idiom and non-branchless keying. I DID NOT TAKE THE AGENT'S WORD: I rebuilt a mirror myself, reintroduced `if (branchBytes === null) continue;`, and measured **7 failed / 228 passed** against the repo suite, versus 0 failures with the fix. The agent's own two mutations agree — (a) reverting reds exactly w5 ×1, w6 ×1, w7 ×5 including "the envelope must be a typed refusal; got null", with w1-w4 staying green, so those assertions were genuinely RED at baseline and not vacuously green; (b) ignoring the existence probe reds w1 on all 8 assertions, proving the absent-arm is still load-bearing and the fix did not simply make everything foreign dirt. Two independently-built oracles (the repo suite and its own 44-assertion production-shape probe driving the real CLI) agree on both mutations. At the w1-w7-only revision the suite read 235 passed / 0 FAIL. Reporting arm (w8/w9/w10, 12 red) dispatched separately; a message crossing meant the implementer first re-verified instead of implementing, and the instruction was re-sent explicitly superseding the older one. || REPORTING ARM NOW DONE: sink suite **257 assertions, 0 failed**, whole file, both waves. The sink emits `receipt.archived_paths` (present-and-empty `[]` when nothing is committed, so a consumer can always route on it) AND writes the same list into the committed `finalization-summary.md` under `## Sink Findings`. THE KEY DESIGN CHOICE, and it is the right one: the set is read from git's INDEX via `git diff --cached --name-only` between the `git add` and the commit — not from a list of what the sink believed it planted — so it can neither under-claim a file that rode in unnoticed nor over-claim one it never touched. The summary writer NEVER creates the file, reasoning that a report inventing a file in the archive would add the very kind of unaccounted path it exists to disclose; and because it only appends to a file the `add` already swept, it provably cannot change the set it just reported. Per-forge care: the ports declare `GIT_MAX_BUFFER` LATER than canonical, so a paste of canonical text would have referenced it before declaration — the helpers were placed after it in each port. Its two mutations show the two report homes are INDEPENDENTLY armed: omitting the stray via a basename allowlist reds BOTH homes (2 failures), while disabling only the durable half reds ONLY the at-HEAD assertion (1 failure). I VERIFIED INDEPENDENTLY: neutering `stagedPathsUnder` to return `[]` reds 7 assertions on the repo suite. Final state clean — 257 passed, `edition-sync --check` and `validate-script-sync` both exit 0, anchor still one md5 across four copies. CHANGELOG + `docs/api.md` line for the new envelope field routed to w4-dock; it must land BEFORE the binding receipt run or the receipt goes `chains_stale`.

- item: dock the batch into the documentation surfaces before finalization — `CHANGELOG.md` under `[Unreleased]` with a line per user-visible change (#888's removal is user-visible and must say the four-chain run at the release commit is mandatory; #890's changelog-style convention belongs wherever style is documented; #892 changes what every consumer `CLAUDE.md` says), plus `README.md`, `docs/` and architecture docs where structure moved, and inline comments where a public interface changed; write CHANGELOG BEFORE the final receipt run or the receipt goes `chains_stale`
  status: in-flight
  dispatched: implementer subagent "w4-dock", working in the worktree; write-set `CHANGELOG.md`, `README.md`, `docs/architecture.md`, `docs/api.md` — output lands as edits there plus a note at `.cache/dock.md` in the MAIN checkout. Three of the seven agents drafted suggested CHANGELOG text in their own notes (`.cache/888-894s2.md`, `.cache/889-production.md`, `.cache/893-impl.md`); those are drafts to check against the real diff, not text to paste. Running concurrently with the #892 pin-hardening pass, whose write-set is disjoint.
  result: DONE — `.cache/dock.md`. Seven `[Unreleased]` entries across four sections; `CHANGELOG.md` diff is 134 insertions and ZERO deletions, so nothing in a released section moved. `docs/architecture.md:345` (carry-over binding) and a new `docs/api.md` Sink bullet for #893. `README.md` deliberately untouched — swept and nothing stale. IT CORRECTED FIVE DRAFTED CLAIMS against the diff rather than pasting them: #889's "eleven hand edits" is not derivable (the executed replay measured NINE rounds and was still unfinished; distinct files needing a hand edit at old HEAD is fifteen), so the entry asserts only what was executed; "fails on the fifth" was replaced by the stronger verified fact that THREE OF FOUR CHAINS could pass over a half-done bump, since the only suite that caught it has no edition twin; the four-step framing was rewritten to three-step after my `:2993` ruling; #888 now says outright "**Nothing gets faster**", because the adjacent released `[9.0.0]` entry opens by promising the opposite and a reader would carry that framing down the file; and #894's "×4" was RE-MEASURED — the site-2 record said the plugin anchors were un-propagated, which stopped being true when I ran the central sync, so the entry now matches the tree. THE `#N` SET WAS EXTRACTED WITH THE RELEASE SCRIPT'S OWN `unreleasedSection` PARSER, not by grep: `[888,889,890,891,892,893,894,895]` — exactly the seven plus #891's pre-existing contribution, no upstream forge numbers. It deliberately did NOT cite #881/#877/#715/#651 despite each being tempting and real, because `issuesOkay` computes unknown refs as `[Unreleased]` minus `--issues-closed` ∪ commit-message refs, so any of them would refuse `changelog_unknown_reference` at `--prepare` or force a false number into `--issues-closed` — #890's own failure, avoided by applying #890's own lesson. It also applied #890's convention to its own prose (upstream refs written hashless). TWO RESIDUES FOUND OUTSIDE ITS WRITE-SET, both fixed by me: (1) `runCut` still advertised the deleted carry-over in a SHIPPED OPERATOR STRING in all four editions — the text a human reads at release time, telling them they may skip the run #888 just made mandatory; (2) `CLAUDE.md:178` still stated the carry-over binding. It correctly declined to "fix" `docs/api.md:1007`, which was a faithful MIRROR of the shipped string — repairing the doc would have made it truthful about the mechanism while lying about the CLI's actual output. Right call: I fixed the source, then the mirror.
  status: done

- item: run the validation the batch owes and finalize — the diff is edition-touching (#892 and #894 both reach the ×4 anchor), so it owes the full unwaived four-chain run, not the sampling fast gate; `KAOLA_RUN_CHAINS_CONCURRENCY=serial` on this box because concurrency SIGKILLs the octopus merge, and pass `--project bundle-888-889-890-892-893-894-895` so the receipt lands where finalize will actually look for it; VERIFY THE ×4 ANCHOR BY md5 DIRECTLY, NEVER BY `edition-sync --check` — w1-894s1 measured that its "committed kernel parity verified at HEAD" line reads the COMMITTED tree, so it returned exit 0 with identical output while `scripts/kaola-workflow-adaptive-schema.js` was visibly modified in the worktree; that green attests HEAD and says nothing about an in-flight four-copy edit. Run `edition-sync --write` centrally (one writer, after every editing agent is finished) and then confirm one distinct md5 across the four copies by hand; run `simulate-workflow-walkthrough.js` at FULL scope (the fast gate samples it at a rotating 1/12 shard, so green there is not verification) and `generate-routing-surfaces.js --check`; then `/kaola-workflow-finalize bundle-888-889-890-892-893-894-895`, and note #893's fix is expected to make the sink accept finalize's own mirror without the six-step manual recipe — this run is its first live test
  status: done
  dispatched: self, inline. PROGRESS SO FAR, before the binding receipt: (1) fast gate `test:kaola-workflow:claude` EXIT 0 — but it sampled the walkthrough at shard **12/12, 15 of 184 scenarios**, so that green says almost nothing about a bundle that deleted five release-gate scenarios and added one active-folders scenario; recorded as early signal, not verification. Everything else in it ran at full coverage (18 surfaces byte-matching, 432 routing assertions, 119 active-folders parity, 131 install-all contract). (2) FULL-SCOPE walkthrough EXIT 0 — `{"scenarios":184,"ran":184,"passed":184,"failed":0}`, with both of this bundle's scenarios explicitly present: `testActiveFoldersExcludesClosedIssue895: PASSED` (log line 9) and `testReleaseCheckPreTagGate: PASSED` (line 111). The single `FAIL`-matching line is the scenario NAME `testClassifierFailClosedOnRemoteError: PASSED`, not a failure. Log at `scratchpad/walkthrough-full.log`. (3) `test-release` 247 and `test-finalize-door` 153 assertions pass after my three carry-over residue fixes. STILL OWED: the binding four-chain receipt, which must run AFTER the three adversarial verifiers land and after any fix they force — running it earlier would bind a tree that is about to change. || PRE-RECEIPT STATE NOW CLEAN, all verifier-forced fixes landed: sink suite **257 assertions 0 failed**, `test-spawn-classification` exit 0 (the new `w6` ENOBUFS site was classified rather than the ceiling raised — the tool says outright that raising it is not a fix), `edition-sync --check` and `validate-script-sync` both exit 0, anchor one md5 across four copies, and `CHANGELOG.md`/`docs/api.md` carry the new `receipt.archived_paths` field. The `#N` set re-extracted with the release script's OWN `unreleasedSection` parser is exactly `[888,889,890,891,892,893,894,895]`. NOW RUNNING, in order: (1) full-scope walkthrough again — the earlier 184/184 run is STALE because the tree changed substantially after it, and a failure here would waste the chain run; then (2) the binding four-chain receipt with `KAOLA_RUN_CHAINS_CONCURRENCY=serial` (concurrency SIGKILLs the octopus merge on this box) and `--project bundle-888-889-890-892-893-894-895` (the bare form writes a receipt finalize never looks at). The diff is edition-touching, so all four chains are owed, not the sampling fast gate. || BOTH DONE. (1) Full-scope walkthrough on the FINAL tree: `{"scenarios":184,"ran":184,"passed":184,"failed":0}`, exit 0, zero FAIL lines, with `testActiveFoldersExcludesClosedIssue895` and `testReleaseCheckPreTagGate` both PASSED. This re-run was necessary — the earlier 184/184 was measured before four surface fixes, both #893 defects, the reporting arm and the spawn classification landed. (2) BINDING FOUR-CHAIN RECEIPT GREEN at `kaola-workflow/bundle-888-889-890-892-893-894-895/.cache/chain-receipt.json` — judged by receipt CONTENT, not exit code: `claude exitCode 0 (219.5s) | codex 0 (5.9s) | gitlab 0 (57.7s) | gitea 0 (56.2s)`, every one `accepted_red:false`, `timed_out:false`, `signal:null`, `attempts:1`. No waiver anywhere. Tree confirmed SETTLED before the receipt bound it — identical `git diff` hash across three samples 4s apart, 65 changed paths, `docs/mission-list.md` still staged for deletion. NOTE ON MY OWN FIRST READ: I initially printed `any red/waived: true` because I guessed the field names — the receipt uses `exitCode`/`accepted_red`, not `status`/`waived`, so my probe read `undefined` and reported a false alarm. Corrected by dumping the real structure. The receipt's `headSha` is `fa5157b3` because nothing is committed yet; finalize binds by the `codeTreeHash` content-address relaxation rather than exact headSha (that exactness is the RELEASE gate's demand, not finalize's), so committing without changing file content should keep it valid — and if finalize reports `chains_stale` anyway, the answer is to re-run the chains at the committed HEAD, not to waive. || COMMITTED AND RE-BOUND. `6fdbf714` carries the implementation (66 paths); I then re-ran the four chains rather than relying on the content-address relaxation, and the receipt now binds `headSha: 6fdbf714` exactly — `claude 0 (220.9s) | codex 0 (5.9s) | gitlab 0 (58.1s) | gitea 0 (56.2s)`, all `accepted_red:false`, `timed_out:false`, `signal:null`, `attempts:1`, **0 not-green**. Scope recorded as `all-four` for `edition_coupling` over 66 changed files, so no diff-scoping shortcut was taken. A second commit `28d6bbd6` records the receipt itself — bookkeeping only, so `codeTreeHash` is unchanged and the receipt still binds at finalize; this is the same shape as #888's problem in miniature (committing a receipt moves HEAD past the sha it names) and is precisely what the codeTreeHash relaxation exists for at finalize, versus the RELEASE gate which demands exact equality. Worktree clean, 0 modified paths. Handing off to `/kaola-workflow-finalize bundle-888-889-890-892-893-894-895`. NOTE FOR THE SINK: this run is the first live test of #893's own fix — the archive mirror should no longer be refused as `foreign_dirt`, and the sink should now report `receipt.archived_paths` naming everything it commits.
