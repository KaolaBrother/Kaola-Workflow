# Mission — Issue #1055: converge production-script ownership boundaries and remove proven redundancy

Implementation baseline: `5cb85515` (main = origin/main, v11.0.0 + bundle-1054 archive evidence). Drift from the audit candidate `db6af1be`: only `scripts/test-issue-1054-*` (+5 test lines); zero production-script drift; the 33-file / 30,734-line audit scope reproduces exactly.

| item | status | dispatched | result |
|---|---|---|---|
| Consumer inventory on the implementation baseline for every candidate the issue names — §1 sync-edition residue (`transformCommandBody` loop, `ZERO_HASH` ×5, `lowerSet` ×3, `cursorModelPin`/`CURSOR_MODEL_CLASS_PINS`), the four static candidates (`getRoot`/`requiredArchiveFiles` in sink-merge, `computeLandableBlobEntries` in validation-runner, `scopeIsFresh` in codex-preflight), §2 the 13 symbols sink-merge takes from claim and sink-pr's `defaultBranch`, §3 the Cursor host-adapter code in claim and the mechanically duplicated frontmatter/YAML/artifact logic across generators, and the conditional legacy items (non-`--sink` flow, old flags, old state blocks, old worktree layout, barrier references). Real consumers across public CLI, exports, dynamic `require`, spawned CLIs, shell, templates, install paths, generated mirrors, and tests; each item gets a disposition basis. Written to `kaola-workflow/bundle-1055/audit.md` | done | two `investigator` subagents (model sonnet) in parallel: A writes `kaola-workflow/bundle-1055/audit-generators.md` (§1 sync-edition residue, four static candidates, §3 generator duplication); B writes `kaola-workflow/bundle-1055/audit-lifecycle.md` (§2 claim→sink-merge/sink-pr symbols, §3 Cursor host adapter in claim, conditional legacy items); orchestrator merges both into `audit.md` | `kaola-workflow/bundle-1055/audit.md` (dispositions table + two HUMAN_DECISION_REQUIRED items); details in `audit-generators.md` and `audit-lifecycle.md`; the four pure re-exports and the 260 barrier refs re-measured by the orchestrator |
| Acceptance custody: migrate the G0-fable pin in `scripts/test-cursor-edition.js` off the source regex on the dead `CURSOR_MODEL_CLASS_PINS` table onto the real adapter/generated output; ensure each edition's `transformCommandBody` has behavioural CRLF/LF-normalisation acceptance; author the subtraction oracle (5 runtimes × 3 forges × 14 roles + 3 commands LF/CRLF, rendered bytes compared against baseline renders) as a runnable script; mutant-proof every new or migrated pin | done | `tdd-guide` (model sonnet) → edits `scripts/test-cursor-edition.js`, any of `scripts/test-{grok,kimi,cursor,opencode,zcode}-edition.js` needing CRLF/LF acceptance, and a new `scripts/test-issue-1055-render-subtraction-oracle.js` under `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1055/scripts/`; report file `kaola-workflow/bundle-1055/acceptance.md` | `kaola-workflow/bundle-1055/acceptance.md`: G0-fable pin migrated to `templates/agents/runtime-capabilities.json` + `renderRuntimeRole('cursor')` output (838→840 assertions, mutant on `intent_mapping.heavy` reddens 8); CRLF/LF acceptance added to all five edition suites (none existed), each armed by a per-script `split('\n')` mutant; `scripts/test-issue-1055-render-subtraction-oracle.js` + `scripts/fixtures/issue-1055-render-baseline.json` (captured on 5cb85515, sha256 bced34ce…), 300 comparisons asserted, armed by a grok `renderCommand` mutant (18 drifts), registered on `test:kaola-workflow:claude` and `:claude:full`; orchestrator re-ran: no mutant residue, oracle exit 0, cursor suite exit 0 |
| §1 subtraction in production: simplify the five `transformCommandBody` loops (CRLF/LF normalisation preserved), remove the unreferenced `ZERO_HASH`, `lowerSet`, and the `cursorModelPin`/`CURSOR_MODEL_CLASS_PINS` chain, and retire whichever static candidates the inventory proves consumer-free without touching the `ignoredUntrackedUnder` protection comment; regenerate every mirror through the existing mechanisms; subtraction oracle byte-identical; edition suites green | done | `implementer` (model sonnet, one owner for missions 3 and 5) → edits `scripts/sync-{grok,kimi,cursor,opencode,zcode}-edition.js`, `scripts/kaola-workflow-sink-merge.js`, `scripts/kaola-workflow-validation-runner.js`, `scripts/kaola-workflow-codex-preflight.js`, then `npm run sync:editions`; report `kaola-workflow/bundle-1055/impl-section1.md` | `kaola-workflow/bundle-1055/impl-section1.md`: five `transformCommandBody` loops collapsed to `split(/\r?\n/).join('\n')` (zcode `block`, kimi `strippedModelDispatch` remnants removed with them), `ZERO_HASH` ×5, `lowerSet` ×3, cursor `CURSOR_MODEL_CLASS_PINS`/`cursorModelPin`, sink-merge `getRoot`/`requiredArchiveFiles` (#901 comment intact), validation-runner `computeLandableBlobEntries` (+ its now-orphaned helper, −68), codex-preflight `scopeIsFresh` all deleted with zero-reference grep proofs; oracle 300/300 after every edit; orchestrator re-ran oracle, five edition suites at 3/3 trees (707/826/840/877/856), and the zero-reference greps |
| §2 dependency direction: sink-merge consumes `getCoordRoot`/`mainRootFromCoord`/`resolveMainRoot` from adaptive-schema and `readActiveFolders` from active-folders instead of the claim forwarders; decide sink-pr's `defaultBranch` and the shared Git/worktree + closure/archive service boundary with reasons (install and cross-forge sync cost counted; no new claim helpers); propagate to the gitlab/gitea mirrors; sink and forge suites green | done | `implementer` (model sonnet) → edits `scripts/kaola-workflow-adaptive-schema.js`, `scripts/kaola-workflow-claim.js`, `scripts/kaola-workflow-sink-merge.js`, `scripts/kaola-workflow-sink-pr.js` in the worktree, then `npm run sync:editions`; report `kaola-workflow/bundle-1055/impl-section2.md` | `kaola-workflow/bundle-1055/impl-section2.md`: sink-merge imports the 3 root helpers + `defaultBranch` from adaptive-schema and `readActiveFolders` from active-folders (top-level and both lazy sites); `defaultBranch` moved verbatim into adaptive-schema with its own `OFFLINE`/`REMOTE_TIMEOUT_MS` copies (the schema had none — the mission brief's premise was wrong, corrected by the implementer), claim re-exports the same object (`===` re-proven by the orchestrator), sink-pr no longer loads claim (re-proven via `require.cache`); closure/archive helpers stay in claim (reason in `audit.md`); `npm run sync:editions` regenerated 6 mirrors, `validate-script-sync.js` exit 0; focused suites exit 0: test-sink-merge (1063), test-claim-hardening (838), test-active-folders-field-parity (163), test-validate-script-sync (56), validate-workflow-contracts |
| §3 host adaptation and generator duplication: per-item implement-or-keep decision with reasons for the Cursor process/argv/parent detection and preparation logic in claim and for the verbatim-duplicated frontmatter/YAML-scalar/artifact-update logic across the five sync-edition generators; implement only what the reasons justify, preserving install paths, process boundaries, failure envelopes, startup/resume, and per-runtime format/permission/upgrade handling | done | same `implementer` as mission 3 → Cursor host adapter: KEEP per `audit.md` (no code); generator helpers: shared through `scripts/runtime-edition-forge.js` exports consumed by the five sync scripts; report `kaola-workflow/bundle-1055/impl-section3.md` | `kaola-workflow/bundle-1055/impl-section3.md`: Cursor host adapter KEPT (single-sited, one consumer; reason in `audit.md`); `parseFrontmatter`, `parseTools`, `yamlScalar`, `listCanonAgents` moved verbatim into `scripts/runtime-edition-forge.js`; `listCanonCommands`, `canonCommandPath`, `commandRel` shared with the per-script constant (`DEFAULT_FORGE` / `treeLabel`) passed as an explicit argument behind a 3-line per-script wrapper; `treeLabel`/`runCheck`/`runWrite`/hook/permission logic untouched; export key sets identical before/after; no circular require; +74 relocated lines in the forge helper |
| Conditional legacy interfaces: per-item conclusion (keep / retire with migration plan / pending support policy) for the non-`--sink` sink-merge flow, legacy flags, legacy state blocks, legacy worktree layout, and barrier references; any support-policy fork is surfaced as HUMAN_DECISION_REQUIRED with evidence, options, and a recommendation; no external state, other repositories, or unknown worktrees are touched | done | self | `kaola-workflow/bundle-1055/audit.md`: legacy non-`--sink` pipeline KEEP (documented public interface, 11 legacy invocations across 4 editions in `test-sink-merge.js`); three accept-and-warn flags KEEP; legacy state strips / tolerant reads KEEP; sibling worktree layout KEEP (migration command only); barrier code KEEP, 260 stale refs on this machine untouched. Two HUMAN_DECISION_REQUIRED items (legacy pipeline support policy; owner-run `barrier-ref-sweep`) carried to the final report |
| Independent review of the frozen candidate: code review for correctness, regressions, scope, and net-deletion vs pure-relocation accounting, plus adversarial verification of the subtraction and dependency-direction claims | todo | | |
| Validation on the frozen candidate: affected focused suites, all five runtime edition suites at three forge trees, the complete candidate-bound chain via run-chains for `bundle-1055`, the integration walkthrough, and `install-all.sh --check` if any install boundary moved; unexecuted host checks recorded as unknown | todo | | |
| Documentation to final behaviour: README, `docs/api.md`, CHANGELOG `[Unreleased]`, architecture/ADR where the design moved, public-interface comments; final report separating proven deletions, ownership relocations, kept items, and unmeasured benefits | in-flight | `doc-updater` (model sonnet) → edits `README.md`, `docs/api.md`, `CHANGELOG.md` `[Unreleased]`, `docs/architecture*.md` or the module-ownership doc if one exists, in the worktree; facts dictated from `audit.md`, `impl-section1.md`, `impl-section2.md`, `impl-section3.md`, `acceptance.md`; report `kaola-workflow/bundle-1055/docs.md` | |
