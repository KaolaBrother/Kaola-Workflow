# Mission — Issue #1020: add ZCode as an additive runtime (docs, generator, installer, suite, claim help, install-all)

| item | status | dispatched | result |
|---|---|---|---|
| Measure ZCode 3.9.1 capabilities (skills/plugins/agents layout; same-model-different-effort) | done | inline | ZCode 3.9.1 (AppImage log). Commands/skills: `.zcode/{commands,skills}` workspace + `~/.zcode/...` user. Subagents: `~/.zcode/agents/*.md`, frontmatter camelCase `name/description/model/thoughtLevel` (docs: key is NOT `reasoningEffort`; workspace scope unsupported in beta). Hooks: config.json `hooks` + `hooks.enabled:true`, 7 events, no SubagentStart. Plugins: `.zcode-plugin/plugin.json`. Same model at different effort: YES — GLM-5.3 variants low|high|max measured in ~/.zcode/v2/config.json. Tiers: standard→high, reasoning→max, heavy/fable→max. |
| Survey existing grok/cursor edition layout (generator, installer, suite, docs) | done | Explore subagent (agent_8c1ae4a4) → findings relayed to orchestrator | installers are install-grok.sh/install-cursor.sh copying from generated trees by sync-*-edition.js; suites test-{grok,cursor}-edition.js with B0/D0/D1/G0–G8 blocks; full surface list with line numbers obtained (see run context). |
| docs/zcode-edition.md (incl. reasoning-tier measurement: GLM-5.3 high/max/max) | done | implementer subagent (agent_9102743c) → docs/zcode-edition.md + all surface edits | Tier table + full 3.9.1 measurement recorded. |
| Generator support (generate-routing-surfaces.js or edition equivalent) | done | implementer subagent → scripts/sync-zcode-edition.js + refreshPresentEditionTrees line | Full CLI incl. --merge-hooks/--strip-hooks; 3 forge trees in parity. |
| install-zcode.sh + install-all.sh RUNTIMES + test-install-all.js | done | implementer subagent → install-zcode.sh + additive rows | install-all 269 assertions green. |
| test-zcode-edition.js suite | done | tdd-guide subagent (agent_ad62faa0) → scripts/test-zcode-edition.js | 1131 lines; suite green 687 assertions after tdd-guide fixed its own D2 bracketed-heading regex defect (implementer's shared-layer release.js/CHANGELOG-heading workaround reverted — shared surface untouched). |
| claim USAGE `--runtime` help adds zcode | done | implementer subagent → strictly additive `\|zcode` in root claim + 3 plugin mirrors | verified in diff. |
| Surface propagation: docs/api.md, README, docs/architecture.md, docs/README.md, CLAUDE.md additive sentence, workflow-state-contract, .env.example, CHANGELOG | done | implementer subagent → additive mentions only | workflow-state-contract + .env.example enumerate no runtimes — left untouched (measured, not skipped). ADR 0019 gained zcode column. |
| Validation (own suite + walkthrough sanity) + PR; no merge, no auto-next | todo | | |

Constraints: ADDITIVE ONLY; no changes to claude/codex/opencode/kimi/grok/cursor behavior or wording; zcode stays out of npm test, edition-sync.js, install.sh; tdd-guide writes tests only, implementer writes production only; no self-pass.
