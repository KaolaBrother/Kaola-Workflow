# Mission — Issue #1056: `defaultBranch` honours the environment at call time, and the #1055 records are corrected

Baseline `e72407b8` (main = origin/main = v11.0.0 + #1055 merged, unreleased). Owner authorisation (2026-09-10): fix the pre-release defect that #1055 introduced — after `require('adaptive-schema')`, setting `KAOLA_WORKFLOW_OFFLINE=1`, then `require('claim')`, `defaultBranch` still performs the two network probes (`OFFLINE` and `REMOTE_TIMEOUT_MS` are captured when adaptive-schema loads); test author first, then a minimal fix with an explicit contract; keep probe order, timeout bound, error fallback, and per-forge behaviour; no comprehensive forge hand-port rewrite; correct the four inaccuracies in the #1055 records without rewriting completed Mission results; keep the legacy non-`--sink` pipeline and the 260 barrier refs untouched; re-review; full candidate-bound validation; then PAUSE before finalize/sink for the owner's independent acceptance — no release, no runtime upgrade.

| item | status | dispatched | result |
|---|---|---|---|
| Acceptance custody: failable regressions for `defaultBranch` — (a) load-order: require adaptive-schema, then set `KAOLA_WORKFLOW_OFFLINE=1`, then require claim (and sink-pr, sink-merge), call `defaultBranch` with `child_process.execFileSync` mocked: zero network probes expected, RED on `e72407b8` (two probes); (b) the same for `KAOLA_GH_REMOTE_TIMEOUT_MS` set after load: the `timeout` passed to stages 2–3 must equal the configured bound; (c) probe order and fallback pinned: stage 1 local symbolic-ref wins without network, unset stage 1 + offline → `'main'` with zero network calls, unset + online → `git remote show origin` before `ls-remote --symref`, all failures → `'main'`; (d) the gitlab/gitea claim ports' `defaultBranch` measured the same way, RED today on their own load-time capture; every pin proven by mutant | done | `tdd-guide` (model sonnet) → new `scripts/test-issue-1056-default-branch-env-contract.js` under `/Volumes/WorkspaceA/ylminiserver/workspace/kaola-workflow/.kw/worktrees/bundle-1056/scripts/`, registered on `test:kaola-workflow:claude`, `:claude:full` and the gitlab/gitea chains as the repo's convention requires; report `kaola-workflow/bundle-1056/acceptance.md` | `kaola-workflow/bundle-1056/acceptance.md`: `scripts/test-issue-1056-default-branch-env-contract.js`, 31 assertions / 6 scenarios in fresh child processes with an in-process `execFileSync` mock; baseline `e72407b8`: 23 green / 8 RED (offline load-order ×2, timeout after load, clamp after load, gitlab+gitea port-before-env ×2 each); correction: for the hand-ports the RED order is 'require the port, then set env' because their local copy captures at the port's own load; 5 GREEN pins mutant-proven one at a time in adaptive-schema and restored byte-identically; registered on claude, claude:full, codex, gitlab, gitea chains; spawn-classification green; orchestrator re-ran: exit 1 on baseline, spawn-classification exit 0 |
| Minimal fix: `defaultBranch` in adaptive-schema reads offline and remote-timeout from the environment at call time (documented contract: env is consulted per call; module-level constants elsewhere untouched), probe order / timeout bound / fallback unchanged; the gitlab/gitea claim hand-ports consume and re-export the kernel `defaultBranch` instead of their local copy (import swap only, their sinks unchanged); mirrors regenerated; focused suites green | done | `implementer` (model sonnet) → edits `scripts/kaola-workflow-adaptive-schema.js`, `plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js`, `plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js`, then `npm run sync:editions`; report `kaola-workflow/bundle-1056/impl.md` | `kaola-workflow/bundle-1056/impl.md`: adaptive-schema `defaultBranch` reads `KAOLA_WORKFLOW_OFFLINE` / `KAOLA_GH_REMOTE_TIMEOUT_MS` per call via `offlineNow()` / `remoteTimeoutMsNow()` (the load-time constants had no other consumer and were deleted; probe order, clamp 1..600000/30000, fallbacks, `timeout:` placement unchanged); gitlab/gitea claim ports delegate `const defaultBranch = adaptiveSchema.defaultBranch` (their own `OFFLINE` stays for ~20 other sites); `npm run sync:editions` + `validate-script-sync.js` exit 0; suite 31/31; claim-hardening, sink-merge, forge scoping ×2, gitlab/gitea sinks + workflow-scripts, oracle, contracts, spawn-classification exit 0; identity `===` true for kernel, gitlab, gitea (re-proven by the orchestrator) |
| #1055 record corrections, appended, not rewritten: `kaola-workflow/archive/bundle-1055/corrections.md` + a comment on #1055 stating (1) the export key sets of sync-zcode/opencode DID change in the second candidate (four dead dispatch constants retired), (2) the 103 sink envelopes were mock-`gh`, test-gate-skipped runs, not real forge verification, (3) `audit.md`'s claim that adaptive-schema already had `OFFLINE` was wrong (the implementer added copies), (4) the load-order `OFFLINE` divergence is a public-export behaviour change fixed here, not a mere follow-up; replace the raw NUL byte in the archived `adversarial.md` and `acceptance.md` with an escape (content otherwise byte-identical); CHANGELOG `[Unreleased]` and `docs/api.md` state the call-time contract | done | self → `kaola-workflow/archive/bundle-1055/corrections.md`, NUL fixes in the two archived files, comment on #1055, `CHANGELOG.md`, `docs/api.md` | `kaola-workflow/archive/bundle-1055/corrections.md` (five-row table); #1055 correction comment posted; NUL bytes in the two archived files replaced by escape text (1 line each); CHANGELOG `[Unreleased]` › `### Fixed` and `docs/api.md` state the call-time contract for `KAOLA_WORKFLOW_OFFLINE` / `KAOLA_GH_REMOTE_TIMEOUT_MS` and the hand-port re-export; verified by `git diff --stat` (4 files, +23/−3) |
| Independent review of the frozen candidate (`code-reviewer`), including the hand-port import swap and the record corrections | done | frozen candidate `f210ef76`; `code-reviewer` (model opus) → `kaola-workflow/bundle-1056/review.md`, no tracked edits | `kaola-workflow/bundle-1056/review.md`: ready with notes, no blocking defect; fix, ports, test custody (mock interception, 8 RED reproduced against extracted baseline files), records and docs all verified. Notes and dispositions: N1 ports now honour `KAOLA_GH_REMOTE_TIMEOUT_MS` (defaults unchanged) → pinned by 8 new assertions (test author) and stated in CHANGELOG + docs/api; N2 assertion-count floor → added (40, mutant-proven); N3 'same kernel function' wording → docs/api now says the ports' own byte-identical kernel copy; N4 Timeouts entry → widened to the git probes; N5 informational (forge CLI timeouts still load-captured, by ruling); N6 cosmetic |
| Validation on the frozen candidate: `run-chains --project bundle-1056` (four chains), the render oracle, the five edition suites, `install-all.sh --check`; unknowns recorded | in-flight | self → `node scripts/kaola-workflow-run-chains.js --project bundle-1056 --json` from the worktree on `f210ef76` (receipt at the main root `kaola-workflow/bundle-1056/.cache/`), then oracle, five edition suites, `install-all.sh --check`; log `kaola-workflow/bundle-1056/validation.md` | |
