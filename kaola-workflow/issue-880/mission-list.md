# Delete the zero-consumer exports left in the Oracle Kernel after the ADR 0017 build (issue #880)

- item: Audit all 95 exports of kaola-workflow-adaptive-schema.js for consumers across canonical, the four editions, tests, templates, hooks and installers. Three buckets, not two — DELETE (no consumer, no internal use), UNEXPORT (internal use only), KEEP (named consumer at file:line); deleting a definition the kernel itself calls breaks the file. Two traps carried from the last campaign: ripgrep is NOT installed here so every `rg` sweep returns silently empty (grep only), and the kernel is BASE-NAMED in all four trees so forge ports require it under the unrenamed name.
  status: done
  dispatched: subagent `audit-dead-exports` (investigator), read-only, dispatched before the claim while the sink was merging #877. Output lands at kaola-workflow/.origin/dead-exports-audit.md — a table of all 95 symbols with per-symbol evidence, the duplicate-key finding, the orphaned-comment line ranges, and the contract-validator pin inventory.
  result: kaola-workflow/.origin/dead-exports-audit.md (462 lines). The surface is 93 exports, not 95 — canonicalJson and sha256Hex are each listed twice (runtime-verified via Object.keys). Split A=21 delete / B=18 unexport / C=54 keep, proven executably: a scratch kernel with all 21 plus 3 orphaned internal decls removed passes node --check, loads at exit 0, and leaves all 72 survivors source-identical. THE SEQUENCING TRAP DOES NOT FIRE — no A or B symbol is read by any validator or test in executable position (every occurrence is a comment or an assertion message), so machinery and pins do NOT need to move together here; that expectation, carried from the attestation repair, was wrong for this file. The real constraint is validate-script-sync.js checkCommittedKernelParity (:414-429), which reads `git ls-tree HEAD`, so all four copies must change in ONE commit. Three findings worth keeping: the kernel's shared envelope vocabulary is vestigial (emit and answer have zero callers anywhere; refuse has exactly one, a test); dispatchEffort's single test consumer is all that keeps four more symbols out of bucket A; and 20 of the 54 KEEP symbols have no production consumer at all — tests and validators are their only readers. Out-of-scope defect recorded for the guard audit: validate-kaola-workflow-contracts.js:566-589 loops over an `emittedSkillTargets` array that is declared empty and never appended to, so its #400 route-reachability assertion can never execute.

- item: Delete what the audit proves dead and unexport what it proves internal-only, plus the two duplicate export keys (canonicalJson, sha256Hex, each listed twice) and the orphaned comment blocks narrating exports that are already gone. The four kernel copies must end byte-identical — edit canonical, propagate, and prove it with one digest across all four rather than by inspection.
  status: in-flight
  dispatched: `impl-kernel-trim` (implementer) owns the four kernel copies only. Scope: bucket A minus mapTier (20 symbols) plus the 3 orphaned internal decls, bucket B's 18 unexports, the duplicate keys, and the orphaned comment blocks. ORCHESTRATOR DECISION carried in the brief — mapTier is HELD, not deleted: it has zero callers but README:370, docs/opencode-edition.md:97/:119, docs/kimi-edition.md:83/:324 and the generated opencode agent prose (sync-opencode-edition.js:247/:537) all name it as the LIVE tier-mapping mechanism, so deleting it silently would make shipped consumer documentation lie. That entanglement is itself a documentation defect and is routed to the `docs-adaptation` audit for a verdict before any deletion. Any other A symbol found to be named in live consumer-facing docs gets the same treatment — stop and report rather than create doc drift.

- item: Move the contract-validator pins WITH the deletion, not after it. The validators read the kernel's export surface directly and go red the moment it changes; that sequencing is what the audit's pin inventory is for. Test custody applies — whoever deletes the machinery does not author the pins that police it, and a pin is deleted with its mechanism, never rewritten to keep passing against machinery that is gone.
  status: done
  dispatched: nobody — the premise was refuted before dispatch.
  result: NO PINS NEEDED MOVING. The audit proved no deleted or unexported symbol is read by any validator or test in executable position; every occurrence is a comment or an assertion-message string. The expectation that machinery and pins must move together was carried over from the attestation repair and is simply false for this file, so dispatching a test author would have produced nothing. The one real coupling is validate-script-sync.js checkCommittedKernelParity, which reads `git ls-tree HEAD` — satisfied by landing all four copies in ONE commit (a9cf4756), not by editing a pin. Kept as a closed item rather than deleted, because "we expected a dependency and measurement removed it" is worth more to a successor than silence.

- item: Verify from outside the agents' reports — four chains green, walkthrough green at FULL scope (the fast gate samples, so a green shard is not verification), and the four-copy digest identity re-proved on the final tree.
  status: done
  dispatched: self
  result: VERIFIED INDEPENDENTLY, not from the implementer's report. Walkthrough green at full scope (203/203, 2,092 spawns) and all four chains green, both exit 0 over the trimmed tree; routing surfaces still byte-match at 18/18. Export count exactly 55 (93 − 20 deleted − 18 unexported) and one sha256 across all four copies. Added a check the brief had lacked: called every surviving function export — 32 of them — and confirmed ZERO ReferenceErrors. That check exists because the implementer caught what the specified checks could not: holding mapTier revived TIER_RANK as a live reader, and deleting it would have left a ReferenceError inside a function body, which both `node --check` and `require()` pass straight over. A parse check and a load check do not prove a module works; only calling it does.

- item: Finalize — receipt, CHANGELOG under [Unreleased], roadmap, archive, close #880.
  status: todo
