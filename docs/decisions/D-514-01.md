# D-514-01. Two cosmetic comment nits from the #500 build review

Date: 2026-06-16
Status: **Accepted** — shipped in the #513/#514 build run (bundle-513-514).
Issue: #514 (non-blocking follow-up from the #500 build, n5-review verdict:pass)
Related: #500 (`D-500-02` — the build that surfaced these), #463 (Slice 3 / AC18 — the shipped fact that
         made R1's comment stale), `scripts/test-route-reachability.js` (T9 marker).

## Context

The clean #500 build (n5-review, verdict:pass) surfaced two cosmetic, zero-behavior comment defects and
deferred them to a follow-up rather than re-opening the green build. This record documents the deliberate
fixes; there is no design choice to adjudicate.

## Decision

- **R1 — stale temporal fragment.** `scripts/kaola-workflow-adaptive-node.js` (+ the 3 forge ports): the
  reworded L2 comment described `working_dir` as staying parent-side "until Slice 3", framing Slice 3 as
  pending. Slice 3 shipped (#463 AC18 PASS); "working_dir stays parent-side" is permanently true (routing
  is via the surfaced `legPath` manifest + dispatch discipline, not by overwriting the field). Reworded to
  "routing into legs landed in Slice 3 (#463 AC18)". Propagated to all four `adaptive-node.js` editions via
  `edition-sync.js` (the comment carries no forge-specific nouns, so the ports take it verbatim).
- **R2 — block-header comment typo.** `scripts/test-route-reachability.js`: the T9 block-header comment
  read `<!-- PIN: speculative-open -->` while the assertion correctly checks `<!-- CARD: speculative-open -->`.
  Corrected the descriptive comment to `CARD:`. The assertion was already correct and was **not** changed.

## Consequence

Comment-only; no executable line changed. `test-route-reachability.js` (146 assertions) and
`test-edition-sync.js` (4-edition byte parity, 29 assertions) stay green. No API/schema/runtime impact.
