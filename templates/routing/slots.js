'use strict';

// slots.js — slot + splice data for the routing-surface render engine.
//
// SLOTS are the larger structural pieces a skeleton fills per render context:
// the frontmatter (2-shape: command description/argument-hint vs skill
// name/description), the H1, the intro paragraph, the setup-resolver runtime
// bash block, and (for next) the routing table. SPLICES are the smaller
// mid-paragraph divergences where a command and skill (or a github vs forge
// command) differ by only a clause or a route noun.
//
// Every value is either a string or an object keyed by surface_type and/or
// forge; the engine's resolveKeyed() descends surface_type first, then forge.
// Values are byte-exact reverse-engineered from the current committed surfaces,
// so the generator is a no-op on a clean tree. The forge-noun renames that the
// engine applies afterward (rename-table.js) never touch these values (the
// forge branches already carry their forge basenames; the github branch is the
// canonical namespace).

const REPLAN_SCRIPTS = {
  github: 'kaola-workflow-replan.js',
  gitlab: 'kaola-gitlab-workflow-replan.js',
  gitea: 'kaola-gitea-workflow-replan.js',
};

const REPLAN_LOCAL_DIRS = {
  github: './plugins/kaola-workflow/scripts',
  gitlab: './plugins/kaola-workflow-gitlab/scripts',
  gitea: './plugins/kaola-workflow-gitea/scripts',
};

const REPLAN_CACHE_PACKAGES = {
  github: 'kaola-workflow',
  gitlab: 'kaola-workflow-gitlab',
  gitea: 'kaola-workflow-gitea',
};

function replanResolver(surfaceType, forge) {
  const script = REPLAN_SCRIPTS[forge];
  if (surfaceType === 'command') {
    const repoCandidate = forge === 'github' ? `./scripts/${script}` : `${REPLAN_LOCAL_DIRS[forge]}/${script}`;
    const pluginRootCandidate = `\${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/${script}}`;
    const installCandidate = `$HOME/.claude/${REPLAN_CACHE_PACKAGES[forge]}/scripts/${script}`;
    return `REPLAN_SCRIPT=""\nfor _p in "${repoCandidate}" "${pluginRootCandidate}" "${installCandidate}"; do\n  [ -f "$_p" ] && { REPLAN_SCRIPT="$_p"; break; }\ndone\n[ -n "$REPLAN_SCRIPT" ] || { echo "BLOCKED: ${script} unavailable" >&2; exit 1; }`;
  }
  const localCandidate = `${REPLAN_LOCAL_DIRS[forge]}/${script}`;
  const packageName = REPLAN_CACHE_PACKAGES[forge];
  return `REPLAN_SCRIPT="${localCandidate}"\nif [ ! -f "$REPLAN_SCRIPT" ]; then\n  REPLAN_SCRIPT="$(find "$HOME/.codex/plugins/cache" -path '*/${packageName}/*/scripts/${script}' -print -quit 2>/dev/null)"\nfi\n[ -n "$REPLAN_SCRIPT" ] && [ -f "$REPLAN_SCRIPT" ] || { echo "BLOCKED: ${script} unavailable" >&2; exit 1; }`;
}

function replanControlPlane(marker, surfaceType, forge) {
  const script = REPLAN_SCRIPTS[forge];
  return `## In-progress re-plan control plane

<!-- PIN: ${marker} -->

This fence outranks every normal startup, mirror, scheduler, handoff, validation, and
finalization route. Before any such action, read the project state and transaction status. When
either reports \`replan_in_progress\`, do not mutate or replace the frozen parent
\`workflow-plan.md\`. Read-only orientation must report the exact \`replan_phase\`,
\`transaction_id\`, \`parent_plan_hash\`, \`child_plan_hash\` (or \`none\`), and
\`last_cas_result\`; never reconstruct them from memory.

The single legal mutation while the fence is active is the edition-local re-plan resume command:

\`\`\`bash
${replanResolver(surfaceType, forge)}
node "$REPLAN_SCRIPT" resume --project {project} --json
\`\`\`

The installed aggregator is \`${script}\`. Do not run mirror/open/record/close/run-chains,
ordinary adaptive handoff, claim archive, task-mirror refresh, or finalize while an intermediate
phase remains. \`decision:ask\` remains advisory and never adds a pause or gate.

If resume returns \`replan_planner_dispatch_required\`, dispatch the genuine
\`workflow-planner\` profile in its Re-plan dispatch mode with an isolated brief containing only
the repository root, project, \`transaction_id\`, \`dispatch_nonce\`, profile identity, the exact
\`.cache/replan-planner-packet.json\` path, and the packet's reason/source evidence. No role
sequence, node ids, dependencies, write sets, cardinality, shape, model, or exact DAG fragment may
be supplied by the orchestrator; an attempt earns \`planner_control_boundary_violation\`. The
planner alone writes the seeded \`workflow-plan.next.md\` and
\`.cache/replan-planner-attestation.json\`, then returns through this same resume command. Missing,
stale, replayed, or mismatched dispatch proof/attestation is
\`replan_planner_attestation_invalid\`; main must never synthesize either artifact.

An invalid unfrozen child uses the bounded unfrozen child-repair loop: re-dispatch the same planner
with the verbatim validator errors and its own child draft, then resume. The main session never
repairs the child DAG. At the retry bound, stop with the typed evidence; do not create a competing
plan, restart the claim, or route to another path. A verified legacy-v1 parent follows this same
transaction into a schema-2 child; legacy normal startup behavior otherwise stays unchanged.`;
}

function replanSlot(marker) {
  const out = { command: {}, skill: {} };
  for (const surfaceType of Object.keys(out)) {
    for (const forge of Object.keys(REPLAN_SCRIPTS)) {
      out[surfaceType][forge] = replanControlPlane(marker, surfaceType, forge);
    }
  }
  return out;
}

const SLOTS = {
  // ---- plan-run --------------------------------------------------------
  'pr-frontmatter': {
    command: "---\ndescription: Kaola-Workflow Adaptive Executor. Executes a frozen workflow-plan.md via a running-set scheduler; each frontier unit dispatched concurrently up to the fan-out cap (critical-path-first); planner-proven-disjoint (parallel_safe) write frontiers co-open in isolated legs BY DEFAULT — no operator toggles; serial is the fallback only on a named serializer (a proven exact-path overlap, the retained net not holding, or a host without worktree support) — uncertain overlap co-opens too and reconciles at the join. Resume-safe.\nargument-hint: <project name>\n---",
    skill: "---\nname: kaola-workflow-plan-run\ndescription: Use when executing a frozen adaptive workflow-plan.md — executes via a running-set scheduler; each frontier unit dispatched concurrently up to the fan-out cap (critical-path-first); planner-proven-disjoint (parallel_safe) write frontiers co-open in isolated legs BY DEFAULT — no operator toggles; serial is the fallback only on a named serializer (a proven exact-path overlap, the retained net not holding, or a host without worktree support) — uncertain overlap co-opens too and reconciles at the join. Resume-safe. Mirror of commands/kaola-workflow-plan-run.md for Codex runtime.\n---",
  },
  'pr-h1': {
    command: "# Kaola-Workflow Plan Run",
    skill: {
      github: "# Skill: kaola-workflow-plan-run",
      gitlab: "# Skill: kaola-workflow-plan-run (GitLab)",
      gitea: "# Skill: kaola-workflow-plan-run (Gitea)",
    },
  },
  'pr-intro': {
    command: "Executes a frozen `workflow-plan.md` for an adaptive project (`workflow_path: adaptive`).\nReads and updates `kaola-workflow/{project}/workflow-state.md` throughout. The plan is\nguarded by `plan_hash`; tampering is a **typed refusal**. Drive every node in the\n`## Node Ledger` to `complete` or `n/a`, honoring the computed gates, then hand off to\nFinalization. Stop and surface on any consent-halt or typed refusal.",
    skill: "Adaptive executor. Runs a frozen `workflow-plan.md` (`workflow_path: adaptive`) by\ntraversing its DAG + `## Node Ledger`. Reads and\nupdates `kaola-workflow/{project}/workflow-state.md` throughout. The plan is guarded by\n`plan_hash`; tampering is a **typed refusal**. Drive every node to `complete` or `n/a`,\nhonoring the computed gates, then route to `kaola-workflow-finalize`.",
  },
  'pr-setup-resolver': {
    command: {
      github: "ACTIVE_WORKTREE_PATH=\"$(node -e \"try{const fs=require('fs');const s=fs.readFileSync('kaola-workflow/{project}/workflow-state.md','utf8');const m=s.match(/^worktree_path:\\\\s*(.+)$/m);process.stdout.write(m?m[1].trim():'');}catch(e){}\" 2>/dev/null)\" || true\n[ -z \"$ACTIVE_WORKTREE_PATH\" ] || [ ! -d \"$ACTIVE_WORKTREE_PATH\" ] && ACTIVE_WORKTREE_PATH=\"$(pwd)\"\nkaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nKAOLA_SCRIPTS=\"$(dirname \"$(kaola_script kaola-workflow-adaptive-node.js)\")\"",
      gitlab: "ACTIVE_WORKTREE_PATH=\"$(node -e \"try{const fs=require('fs');const s=fs.readFileSync('kaola-workflow/{project}/workflow-state.md','utf8');const m=s.match(/^worktree_path:\\\\s*(.+)$/m);process.stdout.write(m?m[1].trim():'');}catch(e){}\" 2>/dev/null)\" || true\n[ -z \"$ACTIVE_WORKTREE_PATH\" ] || [ ! -d \"$ACTIVE_WORKTREE_PATH\" ] && ACTIVE_WORKTREE_PATH=\"$(pwd)\"\nkaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nKAOLA_SCRIPTS=\"$(dirname \"$(kaola_script kaola-gitlab-workflow-adaptive-node.js)\")\"",
      gitea: "ACTIVE_WORKTREE_PATH=\"$(node -e \"try{const fs=require('fs');const s=fs.readFileSync('kaola-workflow/{project}/workflow-state.md','utf8');const m=s.match(/^worktree_path:\\\\s*(.+)$/m);process.stdout.write(m?m[1].trim():'');}catch(e){}\" 2>/dev/null)\" || true\n[ -z \"$ACTIVE_WORKTREE_PATH\" ] || [ ! -d \"$ACTIVE_WORKTREE_PATH\" ] && ACTIVE_WORKTREE_PATH=\"$(pwd)\"\nkaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nKAOLA_SCRIPTS=\"$(dirname \"$(kaola_script kaola-gitea-workflow-adaptive-node.js)\")\"",
    },
    skill: {
      github: "ACTIVE_WORKTREE_PATH=\"$(node -e \"try{const fs=require('fs');const s=fs.readFileSync('kaola-workflow/' + process.env.KAOLA_PROJECT + '/workflow-state.md','utf8');const m=s.match(/^worktree_path:\\\\s*(.+)$/m);process.stdout.write(m?m[1].trim():'');}catch(e){}\" 2>/dev/null)\" || true\n[ -z \"$ACTIVE_WORKTREE_PATH\" ] && ACTIVE_WORKTREE_PATH=\"$(pwd)\"\nKAOLA_SCRIPTS=\"plugins/kaola-workflow/scripts\"\nif [ ! -f \"$KAOLA_SCRIPTS/kaola-workflow-adaptive-node.js\" ]; then\n  KAOLA_SCRIPTS=\"$(dirname \"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-adaptive-node.js' -print -quit 2>/dev/null)\")\"\nfi",
      gitlab: "ACTIVE_WORKTREE_PATH=\"$(node -e \"try{const fs=require('fs');const s=fs.readFileSync('kaola-workflow/' + process.env.KAOLA_PROJECT + '/workflow-state.md','utf8');const m=s.match(/^worktree_path:\\\\s*(.+)$/m);process.stdout.write(m?m[1].trim():'');}catch(e){}\" 2>/dev/null)\" || true\n[ -z \"$ACTIVE_WORKTREE_PATH\" ] && ACTIVE_WORKTREE_PATH=\"$(pwd)\"\nKAOLA_SCRIPTS=\"plugins/kaola-workflow-gitlab/scripts\"\nif [ ! -f \"$KAOLA_SCRIPTS/kaola-gitlab-workflow-adaptive-node.js\" ]; then\n  KAOLA_SCRIPTS=\"$(dirname \"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-adaptive-node.js' -print -quit 2>/dev/null)\")\"\nfi",
      gitea: "ACTIVE_WORKTREE_PATH=\"$(node -e \"try{const fs=require('fs');const s=fs.readFileSync('kaola-workflow/' + process.env.KAOLA_PROJECT + '/workflow-state.md','utf8');const m=s.match(/^worktree_path:\\\\s*(.+)$/m);process.stdout.write(m?m[1].trim():'');}catch(e){}\" 2>/dev/null)\" || true\n[ -z \"$ACTIVE_WORKTREE_PATH\" ] && ACTIVE_WORKTREE_PATH=\"$(pwd)\"\nKAOLA_SCRIPTS=\"plugins/kaola-workflow-gitea/scripts\"\nif [ ! -f \"$KAOLA_SCRIPTS/kaola-gitea-workflow-adaptive-node.js\" ]; then\n  KAOLA_SCRIPTS=\"$(dirname \"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-adaptive-node.js' -print -quit 2>/dev/null)\")\"\nfi",
    },
  },
  'pr-replan-control-plane': replanSlot('replan-plan-run'),
  'pr-review-validation-runner': {
    command: "Use the already-resolved `$KAOLA_SCRIPTS/kaola-workflow-validation-runner.js`; pass the exact hash-covered policy values and write one canonical JSON receipt per obligation without shell re-derivation.",
    skill: "Resolve `kaola-workflow-validation-runner.js` from the same installed runtime as the adaptive node; pass the exact hash-covered policy values and write one canonical JSON receipt per obligation without shell re-derivation.",
  },

  // ---- next (frontmatter 2-shape + H1; both forge-invariant) -----------
  "nx-frontmatter": {"command":"---\ndescription: Workflow Next. Thin router for Kaola-Workflow. Detects active work, reconstructs resume state, and routes to the correct command.\nargument-hint: (optional project name or task description)\n---","skill":"---\nname: kaola-workflow-next\ndescription: Use when resuming, routing, or starting a Kaola-Workflow for Codex project, also called kaola-workflow, from kaola-workflow state and node evidence.\n---"},
  "nx-h1": {"command":"# Workflow Next - thin router","skill":"# Kaola-Workflow Next"},
  'nx-replan-control-plane': replanSlot('replan-next'),

  // ---- init (command frontmatter carries the forge noun; skill frontmatter
  // and both H1 shapes are forge-invariant) ------------------------------
  "in-frontmatter": {"command":{"github":"---\ndescription: Initialize a project for Kaola-Workflow with CLAUDE.md guidance, roadmap tracking, docs structure, and Git/GitHub issue conventions.\nargument-hint: (optional project context)\n---","gitlab":"---\ndescription: Initialize a project for Kaola-Workflow with CLAUDE.md guidance, roadmap tracking, docs structure, and Git/GitLab issue conventions.\nargument-hint: (optional project context)\n---","gitea":"---\ndescription: Initialize a project for Kaola-Workflow with CLAUDE.md guidance, roadmap tracking, docs structure, and Git/Gitea issue conventions.\nargument-hint: (optional project context)\n---"},"skill":"---\nname: kaola-workflow-init\ndescription: Use when setting up a project for Kaola-Workflow for Codex, also called kaola-workflow or workflow-init, or refreshing its Codex-specific guidance, roadmap, and documentation scaffold.\n---"},
  "in-h1": {"command":"# Workflow Init","skill":"# Kaola-Workflow Init"},

  // ---- finalize (frontmatter 2-shape + H1; both forge-invariant) --------
  "fz-frontmatter": {"command":"---\ndescription: Kaola-Workflow Finalization. Final validation, documentation, roadmap/archive, commit, and issue update.\nargument-hint: <project name>\n---","skill":"---\nname: kaola-workflow-finalize\ndescription: Use when reviewed Kaola-Workflow for Codex work, also called kaola-workflow, needs final validation, documentation docking, issue or roadmap closure, archiving, and Git finalization.\n---"},
  "fz-h1": {"command":"# Kaola-Workflow Finalization","skill":"# Kaola-Workflow Finalize"},

  // ---- adapt (frontmatter 2-shape + H1; both forge-invariant) -----------
  "ad-frontmatter": {"command":"---\ndescription: Kaola-Workflow Adaptive Authoring. The agent freely composes a task-shaped DAG of role nodes into workflow-plan.md, then the validator proves it in-grammar and freezes it.\nargument-hint: [issue number | issue set | task description]\n---","skill":"---\nname: kaola-workflow-adapt\ndescription: Use when authoring an adaptive workflow-plan.md — freely compose a task-shaped DAG of role nodes, then the validator proves it in-grammar and freezes it. Mirror of commands/kaola-workflow-adapt.md for Codex runtime.\n---"},
  "ad-h1": {"command":"# Kaola-Workflow Adaptive Authoring (adapt)","skill":"# Skill: kaola-workflow-adapt"},
};

const SPLICES = {
  // ---- plan-run --------------------------------------------------------
  // step 2: envelope tail + finalize route (command carries the extra
  // dispatch-sub-object sentence + the slashed /kaola-workflow-finalize route).
  'pr-open-next-tail': {
    command: "evidence_file, required_tokens, dispatch:{...}}` or `{allDone:true}`; the `dispatch`\nsub-object there supersedes per-field assembly and is the only source for\n`dispatch.leg_path`. On `allDone`, run chains then route to `/kaola-workflow-finalize\n{project}`.",
    skill: "evidence_file, required_tokens, dispatch:{...}}` or `{allDone:true}`. On `allDone`, run chains\nthen route to finalize.",
  },
  // step 2: the re-run `open-next` line (command carries the crash-orphan clause).
  'pr-reopen-line': {
    command: "when no node is `in_progress` (first node, or orphan from a crash between commit and fused advance).",
    skill: "when no node is `in_progress`.",
  },
  // step 3: the lead-in to "Instruct the role to:" — 3 variants (github command
  // gets the base-role sentence at position A above; the forge commands fold it
  // here; the skill uses the Delegate/task-name phrasing).
  'pr-instruct-lead': {
    command: {
      github: "Pass `dispatch.nonce` (evidence-binding token). Instruct the role to:",
      gitlab: "Dispatch the base role profile in `dispatch.agent_type` (legacy `dispatch.role` is only\ndescriptive). Pass `dispatch.nonce` (evidence-binding token). Instruct the role to:",
      gitea: "Dispatch the base role profile in `dispatch.agent_type` (legacy `dispatch.role` is only\ndescriptive). Pass `dispatch.nonce` (evidence-binding token). Instruct the role to:",
    },
    skill: "Delegate to the base role profile matching `dispatch.agent_type`. Apply the task-name and\nreasoning-effort rule above. Pass `dispatch.nonce` (evidence-binding token). Instruct the role to:",
  },
  // step 3 bullet: command "from its work" clause.
  'pr-fill-stubs': {
    command: "- Fill in token stubs from its work; NEVER modify the `evidence-binding:` header line.",
    skill: "- Fill in token stubs; NEVER modify the `evidence-binding:` header line.",
  },
  // step 3 bullet vs continuation: test_thrash is a top-level bullet in the
  // command, a continuation line under the disjoint-frontier bullet in the skill.
  'pr-test-thrash': {
    command: "- `test_thrash` ≥ 3: escalate via `write-halt --reason test_thrash`.",
    skill: "  `test_thrash` ≥ 3: escalate via `write-halt --reason test_thrash`.",
  },
  // merge_conflict recovery: the synthesizer floor swap.
  'pr-synth-floor': {
    command: "  reasoning-floor `synthesizer` agent resolves a real conflict by intent), re-running `close-node`; on",
    skill: "  (non-lowerable floor) `synthesizer` agent resolves a real conflict by intent), re-running `close-node`; on",
  },
  // step 4: command's "it is already open" suffix.
  'pr-dispatch-next': {
    command: "On `result: ok` + `opened`: dispatch the next node (step 3) — it is already open.",
    skill: "On `result: ok` + `opened`: dispatch the next node (step 3).",
  },
  // step 5 intro: 3 variants (github command splits run-chains into a bash
  // fence; the forge command + skill inline it; command uses proceed/slashed
  // route, skill uses delegate/unslashed route).
  'pr-alldone-intro': {
    command: {
      github: "When `allDone: true`, detect the repo type and run the terminal validation appropriate to that repo:\n\n**Self-host (npm) — `package.json` declares `test:kaola-workflow:*` scripts:**\n\n```bash\nnode \"$KAOLA_SCRIPTS/kaola-workflow-run-chains.js\" --project {project}\n```",
      gitlab: "When `allDone: true`, detect the repo type and run the terminal validation appropriate to that repo, then proceed to\n`/kaola-workflow-finalize {project}`. Finalization's sink step owns its own crash-resume journals\n(`sink-receipt.json` / `sink-fallback.json`) and disposes of them itself at terminal success; if a\nstray one turns up on a later `clean and synced` check, delete it — never commit it.\n\n**Self-host (npm) — `package.json` declares `test:kaola-workflow:*` scripts:** Run `run-chains.js`\nwith `--project {project}`.",
      gitea: "When `allDone: true`, detect the repo type and run the terminal validation appropriate to that repo, then proceed to\n`/kaola-workflow-finalize {project}`. Finalization's sink step owns its own crash-resume journals\n(`sink-receipt.json` / `sink-fallback.json`) and disposes of them itself at terminal success; if a\nstray one turns up on a later `clean and synced` check, delete it — never commit it.\n\n**Self-host (npm) — `package.json` declares `test:kaola-workflow:*` scripts:** Run `run-chains.js`\nwith `--project {project}`.",
    },
    skill: "When `allDone: true`, detect the repo type and run the terminal validation appropriate to that repo, then delegate to\n`kaola-workflow-finalize {project}`. Finalization's sink step owns its own crash-resume journals\n(`sink-receipt.json` / `sink-fallback.json`) and disposes of them itself at terminal success; if a\nstray one turns up on a later `clean and synced` check, delete it — never commit it.\n\n**Self-host (npm) — `package.json` declares `test:kaola-workflow:*` scripts:** Run `run-chains.js`\nwith `--project {project}`.",
  },

  // ---- next: forge-noun / structural substitutions (3-way, per-forge).
  // Machine-derived from a 3-way LCS merge of the committed surfaces; each
  // splice replaces one skeleton line with its github/gitlab/gitea variant.
  "nx-cmd-001": {"github":"- a GitHub issue number or free-form task description for new work","gitlab":"- a GitLab issue number or free-form task description for new work","gitea":"- a Gitea issue number or free-form task description for new work"},
  "nx-cmd-002": {"github":"this capture step.","gitlab":"this capture step. Use the forge's issue tracker to file follow-ups.","gitea":"this capture step. Use the forge's issue tracker to file follow-ups."},
  "nx-cmd-003": {"github":"   kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\n   CLAIM_JS=\"$(kaola_script kaola-workflow-claim.js)\"","gitlab":"   kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\n   CLAIM_JS=\"$(kaola_script kaola-gitlab-workflow-claim.js)\"","gitea":"   kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\n   CLAIM_JS=\"$(kaola_script kaola-gitea-workflow-claim.js)\""},
  "nx-cmd-004": {"github":"3. Validate the target exists in the active consumer repository before calling startup. The validation context is the cwd's git repo (the project consuming Kaola-Workflow), not `KaolaBrother/Kaola-Workflow` unless that is the active project.\n   - Online: `gh issue view \"$KAOLA_TARGET_ISSUE\" --json number,state` against cwd's `gh` context. If the fetch fails, stop and ask — do not fall back to a different issue.","gitlab":"3. Validate the target exists before calling startup. Validate against the active consumer repository, not against the Kaola-Workflow package repository unless that is the active project.\n   - Online: `glab issue view \"$KAOLA_TARGET_ISSUE\" --output json` against the active project. If the fetch fails, stop and ask — do not fall back to a different issue.","gitea":"3. Validate the target exists before calling startup. Validate against the active consumer repository, not against the Kaola-Workflow package repository unless that is the active project.\n   - Online: `tea issues view \"$KAOLA_TARGET_ISSUE\" --output json` against the active project. If the fetch fails, stop and ask — do not fall back to a different issue."},
  "nx-cmd-005": {"github":"(all-or-nothing). There is one merge/PR sink per bundle. The finalization step","gitlab":"(all-or-nothing). There is one merge/MR sink per bundle. The finalization step","gitea":"(all-or-nothing). There is one merge/PR sink per bundle. The finalization step"},
  "nx-cmd-006": {"github":"## Startup Step 0a — PR Intent Capture","gitlab":"## Startup Step 0a — MR Intent Capture","gitea":"## Startup Step 0a — PR Intent Capture"},
  "nx-cmd-007": {"github":"Before the startup transaction, check the user's initial prompt for PR sink intent.\nIf it contains \"open a PR\", \"create a PR\", \"pull request\", \"sink=pr\", \"KAOLA_SINK=pr\",\nor \"PR sink\" (case-insensitive), export `KAOLA_SINK=pr` before the startup call.","gitlab":"Before the startup transaction, check the user's initial prompt for MR sink intent.\nIf it contains \"open an MR\", \"create an MR\", \"merge request\", \"sink=mr\", \"KAOLA_SINK=mr\",\n\"MR sink\", or the compatibility aliases \"open a PR\" / \"create a PR\" (case-insensitive),\nexport `KAOLA_SINK=mr` before the startup call.","gitea":"Before the startup transaction, check the user's initial prompt for PR sink intent.\nIf it contains \"open a PR\", \"create a PR\", \"pull request\", \"sink=pr\", \"KAOLA_SINK=pr\",\n\"PR sink\" (case-insensitive),\nexport `KAOLA_SINK=pr` before the startup call."},
  "nx-cmd-008": {"github":"   `commands/kaola-workflow-adapt.md`.","gitlab":"   `plugins/kaola-workflow-gitlab/commands/kaola-workflow-adapt.md`.","gitea":"   `plugins/kaola-workflow-gitea/commands/kaola-workflow-adapt.md`."},
  "nx-cmd-009": {"github":"   target issue and contains `kaola-workflow/{project}/workflow-plan.md`, run `watch-pr` once, then","gitlab":"   target issue and contains `kaola-workflow/{project}/workflow-plan.md`, run `watch-mr` once, then","gitea":"   target issue and contains `kaola-workflow/{project}/workflow-plan.md`, run `watch-pr` once, then"},
  "nx-cmd-010": {"github":"2. **Fresh adaptive.** Run `watch-pr` once for global PR-folder reconciliation, then route to","gitlab":"2. **Fresh adaptive.** Run `watch-mr` once for global MR-folder reconciliation, then route to","gitea":"2. **Fresh adaptive.** Run `watch-pr` once for global PR-folder reconciliation, then route to"},
  "nx-cmd-011": {"github":"   `kaola-workflow-claim.js startup --target-issue $KAOLA_TARGET_ISSUE`","gitlab":"   `kaola-gitlab-workflow-claim.js startup --target-issue $KAOLA_TARGET_ISSUE`","gitea":"   `kaola-gitea-workflow-claim.js startup --target-issue $KAOLA_TARGET_ISSUE`"},
  "nx-cmd-012": {"github":"If `kaola-workflow-claim.js` and `kaola-workflow-classifier.js` are available,","gitlab":"If `kaola-gitlab-workflow-claim.js` and `kaola-gitlab-workflow-classifier.js` are available,","gitea":"If `kaola-gitea-workflow-claim.js` and `kaola-gitea-workflow-classifier.js` are available,"},
  "nx-cmd-013": {"github":"validates the explicit target, refreshes PR-backed folders with `watch-pr`, and","gitlab":"validates the explicit target, refreshes MR-backed folders with `watch-mr`, and","gitea":"validates the explicit target, refreshes PR-backed folders with `watch-pr`, and"},
  "nx-cmd-014": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nCLAIM_JS=\"$(kaola_script kaola-workflow-claim.js)\"","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nCLAIM_JS=\"$(kaola_script kaola-gitlab-workflow-claim.js)\"","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nCLAIM_JS=\"$(kaola_script kaola-gitea-workflow-claim.js)\""},
  "nx-cmd-015": {"github":"  node \"$CLAIM_JS\" watch-pr >/dev/null 2>&1 || true","gitlab":"  node \"$CLAIM_JS\" watch-mr >/dev/null 2>&1 || true","gitea":"  node \"$CLAIM_JS\" watch-pr >/dev/null 2>&1 || true"},
  "nx-cmd-016": {"github":"On startup, also run `watch-pr` to archive PR folders for merged or closed PRs","gitlab":"On startup, also run `watch-mr` to archive MR folders for merged or closed MRs","gitea":"On startup, also run `watch-pr` to archive PR folders for merged or closed PRs"},
  "nx-cmd-017": {"github":"If a GitHub remote and authenticated `gh` are available, fetch open issues:","gitlab":"If a GitLab remote and authenticated `glab` are available, fetch open issues:","gitea":"If a Gitea remote and authenticated `tea` are available, fetch open issues:"},
  "nx-cmd-018": {"github":"gh issue list --limit 100 --json number,title,state,labels,assignees,updatedAt,url","gitlab":"glab issue list --limit 100 --json number,title,state,labels,assignees,updatedAt,url","gitea":"tea issues list --limit 100 --output json"},
  "nx-cmd-019": {"github":"Ensure `kaola-workflow/ROADMAP.md` exists. If GitHub is unavailable, continue from the local","gitlab":"Ensure `kaola-workflow/ROADMAP.md` exists. If GitLab is unavailable, continue from the local","gitea":"Ensure `kaola-workflow/ROADMAP.md` exists. If Gitea is unavailable, continue from the local"},
  "nx-cmd-020": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nROADMAP_JS=\"$(kaola_script kaola-workflow-roadmap.js)\"","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nROADMAP_JS=\"$(kaola_script kaola-gitlab-workflow-roadmap.js)\"","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nROADMAP_JS=\"$(kaola_script kaola-gitea-workflow-roadmap.js)\""},
  "nx-cmd-021": {"github":"To refresh: node .../kaola-workflow-roadmap.js generate && git add kaola-workflow/ROADMAP.md && git commit -m \"chore: refresh ROADMAP.md\"","gitlab":"To refresh: node .../kaola-gitlab-workflow-roadmap.js generate && git add kaola-workflow/ROADMAP.md && git commit -m \"chore: refresh ROADMAP.md\"","gitea":"To refresh: node .../kaola-gitea-workflow-roadmap.js generate && git add kaola-workflow/ROADMAP.md && git commit -m \"chore: refresh ROADMAP.md\""},
  "nx-cmd-022": {"github":"Commits stay phase-owned (Finalization Step 7). If `kaola-workflow-roadmap.js` is unavailable, skip validation.","gitlab":"Commits stay phase-owned (Finalization Step 7). If `kaola-gitlab-workflow-roadmap.js` is unavailable, skip validation.","gitea":"Commits stay phase-owned (Finalization Step 7). If `kaola-gitea-workflow-roadmap.js` is unavailable, skip validation."},
  "nx-cmd-023": {"github":"If multiple active folders exist from prior sessions (e.g., `issue-63` and `issue-65` in different states), they operate independently. Each folder has its own `workflow-state.md`, branch, and worktree metadata. The pre-commit hook prevents commits that stage multiple workflow project folders together.","gitlab":"If multiple active folders exist from prior sessions, they operate independently. Each folder has its own `workflow-state.md`, branch, and worktree metadata. The pre-commit hook prevents commits that stage multiple workflow project folders together.","gitea":"If multiple active folders exist from prior sessions, they operate independently. Each folder has its own `workflow-state.md`, branch, and worktree metadata. The pre-commit hook prevents commits that stage multiple workflow project folders together."},
  "nx-cmd-024": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nREPAIR_JS=\"$(kaola_script kaola-workflow-repair-state.js)\"","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nREPAIR_JS=\"$(kaola_script kaola-gitlab-workflow-repair-state.js)\"","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nREPAIR_JS=\"$(kaola_script kaola-gitea-workflow-repair-state.js)\""},
  "nx-sk-001": {"github":"this capture step.","gitlab":"this capture step. Use the forge's issue tracker to file follow-ups.","gitea":"this capture step. Use the forge's issue tracker to file follow-ups."},
  "nx-sk-002": {"github":"   claim_script=\"plugins/kaola-workflow/scripts/kaola-workflow-claim.js\"","gitlab":"   claim_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js\"","gitea":"   claim_script=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js\""},
  "nx-sk-003": {"github":"     claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-claim.js' -print -quit 2>/dev/null)\"","gitlab":"     claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-claim.js' -print -quit 2>/dev/null)\"","gitea":"     claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-claim.js' -print -quit 2>/dev/null)\""},
  "nx-sk-004": {"github":"3. Validate the target exists in the active consumer repository before calling startup. The validation context is the cwd's git repo (the project consuming Kaola-Workflow), not `KaolaBrother/Kaola-Workflow` unless that is the active project.\n   - Online: `gh issue view \"$KAOLA_TARGET_ISSUE\" --json number,state` against cwd's `gh` context. If the fetch fails, stop and ask — do not fall back to a different issue.","gitlab":"3. Validate the target exists before calling startup. Validate against the active consumer repository, not against the Kaola-Workflow package repository unless that is the active project.\n   - Online: `glab issue view \"$KAOLA_TARGET_ISSUE\" --output json` against the active project. If the fetch fails, stop and ask — do not fall back to a different issue.","gitea":"3. Validate the target exists before calling startup. Validate against the active consumer repository, not against the Kaola-Workflow package repository unless that is the active project.\n   - Online: `tea issues view \"$KAOLA_TARGET_ISSUE\" --output json` against the active project. If the fetch fails, stop and ask — do not fall back to a different issue."},
  "nx-sk-005": {"github":"(all-or-nothing). There is one merge/PR sink per bundle. The finalization step","gitlab":"(all-or-nothing). There is one merge/MR sink per bundle. The finalization step","gitea":"(all-or-nothing). There is one merge/PR sink per bundle. The finalization step"},
  "nx-sk-006": {"github":"## Startup Step 0a — PR Intent Capture","gitlab":"## Startup Step 0a — MR Intent Capture","gitea":"## Startup Step 0a — PR Intent Capture"},
  "nx-sk-007": {"github":"Before the startup transaction, check the user's initial prompt for PR sink intent.","gitlab":"Before the startup transaction, check the user's initial prompt for MR sink intent.","gitea":"Before the startup transaction, check the user's initial prompt for PR sink intent."},
  "nx-sk-008": {"github":"- \"open a PR\"\n- \"create a PR\"\n- \"pull request\"\n- \"sink=pr\"\n- \"KAOLA_SINK=pr\"\n- \"PR sink\"","gitlab":"- \"open an MR\"\n- \"create an MR\"\n- \"merge request\"\n- \"sink=mr\"\n- \"KAOLA_SINK=mr\"\n- \"MR sink\"\n- \"open a PR\" (compatibility alias)\n- \"create a PR\" (compatibility alias)","gitea":"- \"open a PR\"\n- \"create a PR\"\n- \"pull request\"\n- \"sink=pr\"\n- \"KAOLA_SINK=pr\"\n- \"PR sink\""},
  "nx-sk-009": {"github":"Then export `KAOLA_SINK=pr` before the startup call. The existing","gitlab":"The PR phrases are accepted only as compatibility aliases. Then export\n`KAOLA_SINK=mr` before the startup call. The existing","gitea":"The PR phrases are accepted only as compatibility aliases. Then export\n`KAOLA_SINK=pr` before the startup call. The existing"},
  "nx-sk-010": {"github":"   target issue and contains `kaola-workflow/{project}/workflow-plan.md`, run `watch-pr` once, then","gitlab":"   target issue and contains `kaola-workflow/{project}/workflow-plan.md`, run `watch-mr` once, then","gitea":"   target issue and contains `kaola-workflow/{project}/workflow-plan.md`, run `watch-pr` once, then"},
  "nx-sk-011": {"github":"2. **Fresh adaptive.** Run `watch-pr` once, then route to `kaola-workflow-adapt $KAOLA_TARGET_ISSUE`.\n   The adapt skill's `workflow-planner` runs `kaola-workflow-claim.js startup","gitlab":"2. **Fresh adaptive.** Run `watch-mr` once, then route to `kaola-workflow-adapt $KAOLA_TARGET_ISSUE`.\n   The adapt skill's `workflow-planner` runs `kaola-gitlab-workflow-claim.js startup","gitea":"2. **Fresh adaptive.** Run `watch-pr` once, then route to `kaola-workflow-adapt $KAOLA_TARGET_ISSUE`.\n   The adapt skill's `workflow-planner` runs `kaola-gitea-workflow-claim.js startup"},
  "nx-sk-012": {"github":"   \"Agent Issue Selection — Bundle Lane\" above for selection, and the Bundle Lane\n   section of `kaola-workflow-adapt` for the planner's claim contract.","gitlab":"   the Bundle Lane sections (above and in `kaola-workflow-adapt`) for the planner's claim contract.","gitea":"   the Bundle Lane sections (above and in `kaola-workflow-adapt`) for the planner's claim contract."},
  "nx-sk-013": {"github":"preflight_script=\"plugins/kaola-workflow/scripts/kaola-workflow-codex-preflight.js\"","gitlab":"preflight_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-workflow-codex-preflight.js\"","gitea":"preflight_script=\"plugins/kaola-workflow-gitea/scripts/kaola-workflow-codex-preflight.js\""},
  "nx-sk-014": {"github":"  preflight_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-codex-preflight.js' -print -quit 2>/dev/null)\"","gitlab":"  preflight_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-workflow-codex-preflight.js' -print -quit 2>/dev/null)\"","gitea":"  preflight_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-workflow-codex-preflight.js' -print -quit 2>/dev/null)\""},
  "nx-sk-015": {"github":"the explicit issue, refreshes PR-backed folders with `watch-pr`, and atomically","gitlab":"the explicit issue, refreshes MR-backed folders with `watch-mr`, and atomically","gitea":"the explicit issue, refreshes PR-backed folders with `watch-pr`, and atomically"},
  "nx-sk-016": {"github":"claim_script=\"plugins/kaola-workflow/scripts/kaola-workflow-claim.js\"","gitlab":"claim_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js\"","gitea":"claim_script=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js\""},
  "nx-sk-017": {"github":"  claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-claim.js' -print -quit 2>/dev/null)\"","gitlab":"  claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-claim.js' -print -quit 2>/dev/null)\"","gitea":"  claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-claim.js' -print -quit 2>/dev/null)\""},
  "nx-sk-018": {"github":"  node \"$claim_script\" watch-pr >/dev/null 2>&1 || true","gitlab":"  node \"$claim_script\" watch-mr >/dev/null 2>&1 || true","gitea":"  node \"$claim_script\" watch-pr >/dev/null 2>&1 || true"},
  "nx-sk-019": {"github":"  no plan/ledger exists yet at claim time.\nIf the startup script is unavailable, stop for repair. If startup returns `claim: \"none\"`, stop normal routing. Before\nstopping, print the refusal diagnostics:","gitlab":"  no plan/ledger exists yet at claim time. If the startup script is unavailable, stop for repair.\nIf startup returns `claim: \"none\"`, stop normal routing. Do not inspect active\nproject folders unless the user explicitly names the project to resume.","gitea":"  no plan/ledger exists yet at claim time."},
  "nx-sk-020": {"github":"If GitHub is available, refresh open issues:","gitlab":"### Git Freshness Block Recovery","gitea":"### Git Freshness Block Recovery"},
  "nx-sk-021": {"github":"gh issue list --limit 100 --json number,title,state,labels,assignees,updatedAt,url","gitlab":"git fetch --prune\ngit pull --ff-only\ngit status --short --branch","gitea":"git fetch --prune\ngit pull --ff-only\ngit status --short --branch"},
  "nx-sk-022": {"github":"repair_script=\"plugins/kaola-workflow/scripts/kaola-workflow-repair-state.js\"","gitlab":"repair_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-repair-state.js\"","gitea":"repair_script=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-repair-state.js\""},
  "nx-sk-023": {"github":"  repair_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-repair-state.js' -print -quit 2>/dev/null)\"","gitlab":"  repair_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-repair-state.js' -print -quit 2>/dev/null)\"","gitea":"  repair_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-repair-state.js' -print -quit 2>/dev/null)\""},
  "nx-sk-024": {"github":"unambiguous open GitHub issue and no active project, select it without asking","gitlab":"unambiguous open GitLab issue and no active project, select it without asking","gitea":"unambiguous open Gitea issue and no active project, select it without asking"},
  "nx-sk-025": {"github":"removes every matching `.roadmap/issue-N.md` source, regenerates `ROADMAP.md` once,\narchives one bundle folder, and then stops. To start additional work, the user must\ninvoke kaola-workflow-next again.","gitlab":"removes each `.roadmap/issue-N.md` source, regenerates `kaola-workflow/ROADMAP.md` once,\narchives one bundle folder, and then stops.","gitea":"removes each `.roadmap/issue-N.md` source, regenerates `kaola-workflow/ROADMAP.md` once,\narchives one bundle folder, and then stops."},

  // ---- init: command/skill + forge-noun substitutions (3-way, per-forge).
  // Machine-derived from a 3-way LCS merge of the committed surfaces; each
  // splice replaces one skeleton line (or one contiguous run of lines) with
  // its github/gitlab/gitea variant. `-shared-` splices sit outside any
  // REGION and render on both surface shapes.
  "in-cmd-001": {"github":"If this is not a Git repository, ask before running `git init`. If it is a Git repository without a remote, record that GitHub issue sync is pending until a GitHub remote exists.","gitlab":"If this is not a Git repository, ask before running `git init`. If it is a Git repository without a remote, record that GitLab issue sync is pending until a GitLab remote exists.","gitea":"If this is not a Git repository, ask before running `git init`. If it is a Git repository without a remote, record that Gitea issue sync is pending until a Gitea remote exists."},
  "in-cmd-002": {"github":"If `gh` is available and a GitHub repo can be inferred from `origin`, inspect open issues:","gitlab":"If `glab` is available and a GitLab repo can be inferred from `origin`, inspect open issues:","gitea":"If `tea` is available and a Gitea repo can be inferred from `origin`, inspect open issues:"},
  "in-cmd-003": {"github":"gh issue list --limit 100","gitlab":"glab issue list --limit 100","gitea":"tea issues list --limit 100"},
  "in-cmd-004": {"github":"If there is no GitHub remote, or if `gh` is unavailable or unauthenticated, skip issue fetching immediately and note that GitHub roadmap sync is pending. Do not spend time retrying GitHub calls during init.","gitlab":"If there is no GitLab remote, or if `glab` is unavailable or unauthenticated, skip issue fetching immediately and note that GitLab roadmap sync is pending. Do not spend time retrying GitLab calls during init.","gitea":"If there is no Gitea remote, or if `tea` is unavailable or unauthenticated, skip issue fetching immediately and note that Gitea roadmap sync is pending. Do not spend time retrying Gitea calls during init."},
  "in-sk-001": {"github":"   Active folder lifecycle: `kaola-workflow-claim.js` manages claim/startup (atomic folder create), status, release/discard, watch-pr, and finalize/archive. No legacy coordination layer is used.","gitlab":"   Active folder lifecycle: `kaola-gitlab-workflow-claim.js` manages claim/startup (atomic folder create), status, release/discard, watch-mr, and finalize/archive. No legacy coordination layer is used.","gitea":"   Active folder lifecycle: `kaola-gitea-workflow-claim.js` manages claim/startup (atomic folder create), status, release/discard, watch-pr, and finalize/archive. No legacy coordination layer is used."},
  "in-shared-001": {"github":"- GitHub issues are the roadmap source of truth when available; `kaola-workflow/ROADMAP.md` is the local active-work mirror.","gitlab":"- GitLab issues are the roadmap source of truth when available; `kaola-workflow/ROADMAP.md` is the local active-work mirror.","gitea":"- Gitea issues are the roadmap source of truth when available; `kaola-workflow/ROADMAP.md` is the local active-work mirror."},
  "in-sk-002": {"github":"plugin_root=\"plugins/kaola-workflow\"","gitlab":"plugin_root=\"plugins/kaola-workflow-gitlab\"","gitea":"plugin_root=\"plugins/kaola-workflow-gitea\""},
  "in-sk-003": {"github":"  script_path=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/install-codex-agent-profiles.js' -print -quit 2>/dev/null)\"","gitlab":"  script_path=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/install-codex-agent-profiles.js' -print -quit 2>/dev/null)\"","gitea":"  script_path=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/install-codex-agent-profiles.js' -print -quit 2>/dev/null)\""},
  "in-shared-002": {"github":"This file mirrors active unfinished work. GitHub issues are the source of truth when available.","gitlab":"This file mirrors active unfinished work. GitLab issues are the source of truth when available.","gitea":"This file mirrors active unfinished work. Gitea issues are the source of truth when available."},
  "in-shared-003": {"github":"| none | Initialize roadmap | open | none | Link GitHub issues or add active work |","gitlab":"| none | Initialize roadmap | open | none | Link GitLab issues or add active work |","gitea":"| none | Initialize roadmap | open | none | Link Gitea issues or add active work |"},
  "in-cmd-005": {"github":"- A separate roadmap/research session owns discovering and adding future work to GitHub issues.\n- `/workflow-next` fetches GitHub issues, mirrors active implementation work here, and advances one item per cycle.","gitlab":"- A separate roadmap/research session owns discovering and adding future work to GitLab issues.\n- `/workflow-next` fetches GitLab issues, mirrors active implementation work here, and advances one item per cycle.","gitea":"- A separate roadmap/research session owns discovering and adding future work to Gitea issues.\n- `/workflow-next` fetches Gitea issues, mirrors active implementation work here, and advances one item per cycle."},
  "in-cmd-006": {"github":"- Close linked GitHub issues only after acceptance criteria pass.","gitlab":"- Close linked GitLab issues only after acceptance criteria pass.","gitea":"- Close linked Gitea issues only after acceptance criteria pass."},
  "in-cmd-007": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nROADMAP_JS=\"$(kaola_script kaola-workflow-roadmap.js)\"","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nROADMAP_JS=\"$(kaola_script kaola-gitlab-workflow-roadmap.js)\"","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nROADMAP_JS=\"$(kaola_script kaola-gitea-workflow-roadmap.js)\""},
  "in-cmd-008": {"github":"If `kaola-workflow-roadmap.js` is unavailable (not yet installed), skip this step.","gitlab":"If `kaola-gitlab-workflow-roadmap.js` is unavailable (not yet installed), skip this step.","gitea":"If `kaola-gitea-workflow-roadmap.js` is unavailable (not yet installed), skip this step."},
  "in-cmd-009": {"github":"If a GitHub issue is known, create the active workflow folder before starting:","gitlab":"If a GitLab issue is known, create the active workflow folder before starting:","gitea":"If a Gitea issue is known, create the active workflow folder before starting:"},
  "in-cmd-010": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nCLAIM_JS=\"$(kaola_script kaola-workflow-claim.js)\"","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nCLAIM_JS=\"$(kaola_script kaola-gitlab-workflow-claim.js)\"","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nCLAIM_JS=\"$(kaola_script kaola-gitea-workflow-claim.js)\""},
  "in-cmd-011": {"github":"Replace `{project}` with the workflow project folder name (e.g., `multi-session-substrate`) and `{N}` with the GitHub issue number. If the issue number is unknown, omit `--issue`.","gitlab":"Replace `{project}` with the workflow project folder name (e.g., `multi-session-substrate`) and `{N}` with the GitLab issue number. If the issue number is unknown, omit `--issue`.","gitea":"Replace `{project}` with the workflow project folder name (e.g., `multi-session-substrate`) and `{N}` with the Gitea issue number. If the issue number is unknown, omit `--issue`."},
  "in-cmd-012": {"github":"If `kaola-workflow-claim.js` is unavailable (manual install without the script), skip this step and proceed with local workflow artifacts.","gitlab":"If `kaola-gitlab-workflow-claim.js` is unavailable (manual install without the script), skip this step and proceed with local workflow artifacts.","gitea":"If `kaola-gitea-workflow-claim.js` is unavailable (manual install without the script), skip this step and proceed with local workflow artifacts."},
  "in-cmd-013": {"github":"   - whether a GitHub remote exists","gitlab":"   - whether a GitLab remote exists","gitea":"   - whether a Gitea remote exists"},
  "in-cmd-014": {"github":"   - whether GitHub issues were available for sync","gitlab":"   - whether GitLab issues were available for sync","gitea":"   - whether Gitea issues were available for sync"},

  // ---- finalize: command/skill + forge-noun substitutions (3-way, per-forge).
  // Machine-derived the same way as the init family above.
  "fz-cmd-001": {"github":"REPLAN_SCRIPT=\"./scripts/kaola-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/kaola-workflow-replan.js}\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"$HOME/.claude/kaola-workflow/scripts/kaola-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-workflow-replan.js unavailable\" >&2; exit 1; }","gitlab":"REPLAN_SCRIPT=\"./plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/kaola-gitlab-workflow-replan.js}\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"$HOME/.claude/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitlab-workflow-replan.js unavailable\" >&2; exit 1; }","gitea":"REPLAN_SCRIPT=\"./plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/kaola-gitea-workflow-replan.js}\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"$HOME/.claude/kaola-workflow-gitea/scripts/kaola-gitea-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitea-workflow-replan.js unavailable\" >&2; exit 1; }"},
  "fz-sk-001": {"github":"REPLAN_SCRIPT=\"plugins/kaola-workflow/scripts/kaola-workflow-replan.js\"","gitlab":"REPLAN_SCRIPT=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-replan.js\"","gitea":"REPLAN_SCRIPT=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-replan.js\""},
  "fz-sk-002": {"github":"  REPLAN_SCRIPT=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-replan.js' -print -quit 2>/dev/null)\"","gitlab":"  REPLAN_SCRIPT=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-replan.js' -print -quit 2>/dev/null)\"","gitea":"  REPLAN_SCRIPT=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-replan.js' -print -quit 2>/dev/null)\""},
  "fz-sk-003": {"github":"[ -n \"$REPLAN_SCRIPT\" ] && [ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-workflow-replan.js unavailable\" >&2; exit 1; }","gitlab":"[ -n \"$REPLAN_SCRIPT\" ] && [ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitlab-workflow-replan.js unavailable\" >&2; exit 1; }","gitea":"[ -n \"$REPLAN_SCRIPT\" ] && [ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitea-workflow-replan.js unavailable\" >&2; exit 1; }"},
  "fz-cmd-002": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nVALIDATOR=\"$(kaola_script kaola-workflow-plan-validator.js)\"","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nVALIDATOR=\"$(kaola_script kaola-gitlab-workflow-plan-validator.js)\"","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nVALIDATOR=\"$(kaola_script kaola-gitea-workflow-plan-validator.js)\""},
  "fz-sk-004": {"github":"validator_script=\"plugins/kaola-workflow/scripts/kaola-workflow-plan-validator.js\"","gitlab":"validator_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-plan-validator.js\"","gitea":"validator_script=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-plan-validator.js\""},
  "fz-sk-005": {"github":"  validator_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-plan-validator.js' -print -quit 2>/dev/null)\"","gitlab":"  validator_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-plan-validator.js' -print -quit 2>/dev/null)\"","gitea":"  validator_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-plan-validator.js' -print -quit 2>/dev/null)\""},
  "fz-cmd-003": {"github":"`kaola-workflow-gap-sweep.js --check` against `.cache/run-gaps.json` + the `## Run gaps` section of","gitlab":"`kaola-gitlab-workflow-gap-sweep.js --check` against `.cache/run-gaps.json` + the `## Run gaps` section of","gitea":"`kaola-gitea-workflow-gap-sweep.js --check` against `.cache/run-gaps.json` + the `## Run gaps` section of"},
  "fz-cmd-004": {"github":"  inline; do not close a GitHub issue until acceptance passes; do not archive incomplete folders or","gitlab":"  inline; do not close a GitLab issue until acceptance passes; do not archive incomplete folders or","gitea":"  inline; do not close a Gitea issue until acceptance passes; do not archive incomplete folders or"},
  "fz-cmd-005": {"github":"  post-final commits except the sanctioned `sink-pr.js`/`sink-mr.js` metadata follow-up.","gitlab":"  post-final commits except the sanctioned `sink-mr.js`/`sink-pr.js` metadata follow-up.","gitea":"  post-final commits except the sanctioned `sink-pr.js`/`sink-mr.js` metadata follow-up."},
  "fz-cmd-006": {"github":"`noise: <justification>`; omit if empty), Closure Decision, GitHub Issue, Roadmap, Archive, a","gitlab":"`noise: <justification>`; omit if empty), Closure Decision, GitLab Issue, Roadmap, Archive, a","gitea":"`noise: <justification>`; omit if empty), Closure Decision, Gitea Issue, Roadmap, Archive, a"},
  "fz-cmd-007": {"github":"**Step 7 — GitHub Issue, Roadmap, Archive, Metadata.** If the project links an `issue_number`: close","gitlab":"**Step 7 — GitLab Issue, Roadmap, Archive, Metadata.** If the project links an `issue_number`: close","gitea":"**Step 7 — Gitea Issue, Roadmap, Archive, Metadata.** If the project links an `issue_number`: close"},
  "fz-cmd-008": {"github":"the exit-3 PR/MR auto-pivot under keep-open, and `sink-pr.js`/`sink-mr.js` refuse a project carrying","gitlab":"the exit-3 MR/PR auto-pivot under keep-open, and `sink-mr.js`/`sink-pr.js` refuse a project carrying","gitea":"the exit-3 PR/MR auto-pivot under keep-open, and `sink-pr.js`/`sink-mr.js` refuse a project carrying"},
  "fz-cmd-009": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }"},
  "fz-cmd-010": {"github":"`node \"$(kaola_script kaola-workflow-claim.js)\" resume --project {project} --json` reports","gitlab":"`node \"$(kaola_script kaola-gitlab-workflow-claim.js)\" resume --project {project} --json` reports","gitea":"`node \"$(kaola_script kaola-gitea-workflow-claim.js)\" resume --project {project} --json` reports"},
  "fz-cmd-011": {"github":"# keep-open is merge-sink-only — refuse a PR sink before dispatch.","gitlab":"# keep-open is merge-sink-only — refuse an MR sink before dispatch.","gitea":"# keep-open is merge-sink-only — refuse a PR sink before dispatch."},
  "fz-cmd-012": {"github":"  pr)\n    SINK_PR_JS=\"$(kaola_script kaola-workflow-sink-pr.js)\"\n    node \"$SINK_PR_JS\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project {project}","gitlab":"  mr|pr)\n    SINK_MR_JS=\"$(kaola_script kaola-gitlab-workflow-sink-mr.js)\"\n    node \"$SINK_MR_JS\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project {project}","gitea":"  mr|pr)\n    SINK_PR_JS=\"$(kaola_script kaola-gitea-workflow-sink-pr.js)\"\n    node \"$SINK_PR_JS\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project {project}"},
  "fz-cmd-013": {"github":"    SINK_MERGE_JS=\"$(kaola_script kaola-workflow-sink-merge.js)\"","gitlab":"    SINK_MERGE_JS=\"$(kaola_script kaola-gitlab-workflow-sink-merge.js)\"","gitea":"    SINK_MERGE_JS=\"$(kaola_script kaola-gitea-workflow-sink-merge.js)\""},
  "fz-cmd-014": {"github":"      # keep-open never auto-pivots to a PR sink (its Closes #N body would close the kept-open issue).","gitlab":"      # keep-open never auto-pivots to an MR sink (its Closes #N body would close the kept-open issue).","gitea":"      # keep-open never auto-pivots to a PR sink (its Closes #N body would close the kept-open issue)."},
  "fz-cmd-015": {"github":"        echo \"BLOCKED: sink-merge exited 3 (merge-impossible) on a keep-open run — remediate the blocker (see .cache/sink-fallback.json) and re-run sink-merge; do not pivot to a PR sink.\" >&2; exit 1","gitlab":"        echo \"BLOCKED: sink-merge exited 3 (merge-impossible) on a keep-open run — remediate the blocker (see .cache/sink-fallback.json) and re-run sink-merge; do not pivot to an MR sink.\" >&2; exit 1","gitea":"        echo \"BLOCKED: sink-merge exited 3 (merge-impossible) on a keep-open run — remediate the blocker (see .cache/sink-fallback.json) and re-run sink-merge; do not pivot to a PR sink.\" >&2; exit 1"},
  "fz-cmd-016": {"github":"      CLAIM_JS=\"$(kaola_script kaola-workflow-claim.js)\"","gitlab":"      CLAIM_JS=\"$(kaola_script kaola-gitlab-workflow-claim.js)\"","gitea":"      CLAIM_JS=\"$(kaola_script kaola-gitea-workflow-claim.js)\""},
  "fz-cmd-017": {"github":"      SINK_PR_JS=\"$(kaola_script kaola-workflow-sink-pr.js)\"\n      node \"$SINK_PR_JS\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project {project}","gitlab":"      SINK_MR_JS=\"$(kaola_script kaola-gitlab-workflow-sink-mr.js)\"\n      node \"$SINK_MR_JS\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project {project}","gitea":"      SINK_PR_JS=\"$(kaola_script kaola-gitea-workflow-sink-pr.js)\"\n      node \"$SINK_PR_JS\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project {project}"},
  "fz-cmd-018": {"github":"3 merge-impossible (receipt in `.cache/sink-fallback.json`, pivots to PR — except keep-open, a typed\nBLOCKED). `sink-pr.js`: 0 pushed + PR opened + URL recorded; 1 fatal. After `sink-pr.js` exit 0 the\nfolder stays open and is archived when `watch-pr` sees the PR MERGED/CLOSED on the next","gitlab":"3 merge-impossible (receipt in `.cache/sink-fallback.json`, pivots to MR — except keep-open, a typed\nBLOCKED). `sink-mr.js`: 0 pushed + MR opened + URL recorded; 1 fatal. After `sink-mr.js` exit 0 the\nfolder stays open and is archived when `watch-mr` sees the MR MERGED/CLOSED on the next","gitea":"3 merge-impossible (receipt in `.cache/sink-fallback.json`, pivots to PR — except keep-open, a typed\nBLOCKED). `sink-pr.js`: 0 pushed + PR opened + URL recorded; 1 fatal. After `sink-pr.js` exit 0 the\nfolder stays open and is archived when `watch-pr` sees the PR MERGED/CLOSED on the next"},
  "fz-cmd-019": {"github":"CLOSURE_AUDIT_JS=\"$(kaola_script kaola-workflow-closure-audit.js)\"","gitlab":"CLOSURE_AUDIT_JS=\"$(kaola_script kaola-gitlab-workflow-closure-audit.js)\"","gitea":"CLOSURE_AUDIT_JS=\"$(kaola_script kaola-gitea-workflow-closure-audit.js)\""},
  "fz-sk-006": {"github":"- **`gaps_unswept`** — emitted by `kaola-workflow-gap-sweep.js --check`\n  (resolved the same way as `validator_script` above) when\n  `.cache/run-gaps.json` contains a swept reason class with no matching entry\n  in the `## Run gaps` section of `finalization-summary.md`, or when that\n  section is absent while swept classes exist.","gitlab":"- **`gaps_unswept`** — emitted by `kaola-gitlab-workflow-gap-sweep.js --check`\n  (resolved the same way as `claim_script` above) when `.cache/run-gaps.json`\n  contains a swept reason class with no matching entry in the `## Run gaps`\n  section of `finalization-summary.md`, or when that section is absent while\n  swept classes exist.","gitea":"- **`gaps_unswept`** — emitted by `kaola-gitea-workflow-gap-sweep.js --check`\n  (resolved the same way as `claim_script` above) when `.cache/run-gaps.json`\n  contains a swept reason class with no matching entry in the `## Run gaps`\n  section of `finalization-summary.md`, or when that section is absent while\n  swept classes exist."},
  "fz-sk-007a": {"github":"  `deferred_red_chain`, or `manual:<slug>`), file a follow-up issue and record\n  `filed: #N` in the `## Run gaps` section. If the item is not a product\n  defect (upstream flake, tool-environment noise, or an already-filed and\n  tracked waiver), record `noise: <one-line justification>` instead.","gitlab":"  `deferred_red_chain`, or `manual:<slug>`), file a follow-up issue with the\n  forge and record `filed: #N` in the `## Run gaps` section. If the item is\n  not a product defect (upstream flake, tool-environment noise, or an\n  already-filed and tracked waiver), record `noise: <one-line justification>`\n  instead.","gitea":"  `deferred_red_chain`, or `manual:<slug>`), file a follow-up issue with the\n  forge and record `filed: #N` in the `## Run gaps` section. If the item is\n  not a product defect (upstream flake, tool-environment noise, or an\n  already-filed and tracked waiver), record `noise: <one-line justification>`\n  instead."},
  "fz-sk-009a": {"github":"1. Final validation: on self-host (npm) run the four-chain receipt gate (test suite, type check, lint, build) after all test-consumed prose/docs and code changes have landed, as the last pre-Finalization action; on a consumer (non-npm) repo run the plan's `## Meta` `validation_command` once against the final candidate state, or cite fresh prior evidence with `source: cited:<node-id>`, `validated_command`, `validated_at_head`, and `reuse_boundary`. Save output to `.cache/final-validation.md`, then bind it: record a column-0 `validated_candidate_hash:` line produced by the plan-validator's `--candidate-hash --json` as the LAST action, after every file the validation covered has landed. **State the actual validation-reuse boundary, not a false absolute:** when you cite a prior run instead of rerunning, record which node/state it covered and that later finalize-step edits (e.g. a `CHANGELOG.md`/docs touch in the finalize node) are outside it — do NOT write a terminal absolute like `No files changed after those runs` when the finalize node itself changes docs/changelog afterward. Any doubt about the boundary means run the command. (At closure, `archiveProjectDir` also mechanically neutralizes that known false-absolute phrase in the archived `.cache/final-validation.md` as a backstop.)","gitlab":"1. Final validation: on self-host (npm) run the four-chain receipt gate (test suite, type check, lint, build) after all test-consumed prose/docs and code changes have landed, as the last pre-Finalization action; on a consumer (non-npm) repo run the plan's `## Meta` `validation_command` once against the final candidate state, or cite fresh prior evidence with `source: cited:<node-id>`, `validated_command`, `validated_at_head`, and `reuse_boundary`. Save output to `.cache/final-validation.md`, then bind it: record a column-0 `validated_candidate_hash:` line produced by the plan-validator's `--candidate-hash --json` as the LAST action, after every file the validation covered has landed. Any doubt about the boundary means run the command.","gitea":"1. Final validation: on self-host (npm) run the four-chain receipt gate (test suite, type check, lint, build) after all test-consumed prose/docs and code changes have landed, as the last pre-Finalization action; on a consumer (non-npm) repo run the plan's `## Meta` `validation_command` once against the final candidate state, or cite fresh prior evidence with `source: cited:<node-id>`, `validated_command`, `validated_at_head`, and `reuse_boundary`. Save output to `.cache/final-validation.md`, then bind it: record a column-0 `validated_candidate_hash:` line produced by the plan-validator's `--candidate-hash --json` as the LAST action, after every file the validation covered has landed. Any doubt about the boundary means run the command."},
  "fz-sk-009b": {"github":"   `mv`/`git mv`. **Keep-open is merge-sink-only**: `sink-merge --keep-issue-open` comments WITHOUT\n   closing; a PR/MR sink would auto-close via its `Closes #N` body, so Step 9 refuses a non-merge\n   sink (and the exit-3 PR/MR auto-pivot) under keep-open, and `sink-pr.js`/`sink-mr.js` refuse a\n   project carrying `issue_action: comment_keep_open`.","gitlab":"   `mv`/`git mv`. **Keep-open is merge-sink-only**: `sink-merge --keep-issue-open` notes WITHOUT\n   closing; an MR/PR sink would auto-close via its `Closes #N` body, so Step 9 refuses a non-merge\n   sink (and the exit-3 MR/PR auto-pivot) under keep-open, and `sink-mr.js`/`sink-pr.js` refuse a\n   project carrying `issue_action: comment_keep_open`.","gitea":"   `mv`/`git mv`. **Keep-open is merge-sink-only**: `sink-merge --keep-issue-open` comments WITHOUT\n   closing; a PR sink would auto-close via its `Closes #N` body, so Step 9 refuses a non-merge sink\n   (and the exit-3 PR auto-pivot) under keep-open, and `sink-pr.js` refuses a project carrying\n   `issue_action: comment_keep_open`."},
  "fz-sk-009c": {"github":"     `validator_script` above) after all test-consumed prose/docs and code changes have landed, as\n     the last pre-Finalization action. The contractor only VERIFIES the resulting","gitlab":"     `claim_script` above) after all test-consumed prose/docs and code changes have landed, as the\n     last pre-Finalization action. The contractor only VERIFIES the resulting","gitea":"     `claim_script` above) after all test-consumed prose/docs and code changes have landed, as the\n     last pre-Finalization action. The contractor only VERIFIES the resulting"},
  "fz-sk-009d": {"github":"     ledger_compare_script=\"plugins/kaola-workflow/scripts/kaola-workflow-ledger-compare.js\"","gitlab":"     # The guard is forge-neutral (kaola-workflow-ledger-compare.js) but ships in this edition's tree.\n     ledger_compare_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-workflow-ledger-compare.js\"","gitea":"     # The guard is forge-neutral (kaola-workflow-ledger-compare.js) but ships in this edition's tree.\n     ledger_compare_script=\"plugins/kaola-workflow-gitea/scripts/kaola-workflow-ledger-compare.js\""},
  "fz-sk-009e": {"github":"       ledger_compare_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-ledger-compare.js' -print -quit 2>/dev/null)\"","gitlab":"       ledger_compare_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-workflow-ledger-compare.js' -print -quit 2>/dev/null)\"","gitea":"       ledger_compare_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-workflow-ledger-compare.js' -print -quit 2>/dev/null)\""},
  "fz-sk-010": {"github":"   This step runs **only when `sink: merge`**. For `sink: pr`, skip to Step 8 — the active folder must remain open so `sink-pr.js` can write `pr_url` and `watch-pr` can archive the folder when the PR merges or closes.","gitlab":"   ### Step 8b - Finalize (Archive + Status Close)","gitea":"   ### Step 8b - Finalize (Archive + Status Close)"},
  "fz-sk-011": {"github":"   Capture sink metadata from the active state before archive. Do not read\n   `kaola-workflow/${KAOLA_PROJECT}/workflow-state.md` again after this point\n   on the merge path, because `cmdFinalize` renames it into `archive/`.","gitlab":"   This step runs **only when `sink: merge`**. For `sink: mr` or `sink: pr`, skip\n   to the commit gate so `sink-mr.js` can write MR metadata into the active\n   folder and `watch-mr` can archive it when the MR merges or closes.","gitea":"   This step runs **only when `sink: merge`**. For `sink: mr` or `sink: pr`, skip\n   to the commit gate so `sink-pr.js` can write PR metadata into the active\n   folder and `watch-pr` can archive it when the PR merges or closes."},
  "fz-sk-012a": {"github":"   claim_script=\"plugins/kaola-workflow/scripts/kaola-workflow-claim.js\"","gitlab":"   claim_script=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-claim.js\"","gitea":"   claim_script=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-claim.js\""},
  "fz-sk-012b": {"github":"     claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-claim.js' -print -quit 2>/dev/null)\"","gitlab":"     claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-claim.js' -print -quit 2>/dev/null)\"","gitea":"     claim_script=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-claim.js' -print -quit 2>/dev/null)\""},
  "fz-sk-013a": {"github":"   When it runs, `cmdFinalize` atomically writes `status: closed` + `step: complete` to `workflow-state.md` and\n   renames `kaola-workflow/${KAOLA_PROJECT}/` → `kaola-workflow/archive/${KAOLA_PROJECT}/`\n   in the linked worktree. The rename is staged and committed in the commit gate below.","gitlab":"   When it runs, this atomically writes `status: closed` + `step: complete` to\n   `workflow-state.md` and renames `kaola-workflow/${KAOLA_PROJECT}/` →\n   `kaola-workflow/archive/${KAOLA_PROJECT}/` in the linked worktree. The rename\n   is staged and committed in the commit gate below.","gitea":"   When it runs, this atomically writes `status: closed` + `step: complete` to\n   `workflow-state.md` and renames `kaola-workflow/${KAOLA_PROJECT}/` →\n   `kaola-workflow/archive/${KAOLA_PROJECT}/` in the linked worktree. The rename\n   is staged and committed in the commit gate below."},
  "fz-sk-014": {"github":"   # keep-open is merge-sink-only — refuse a PR sink before dispatch.","gitlab":"   # keep-open is merge-sink-only — refuse an MR/PR sink before dispatch.","gitea":"   # keep-open is merge-sink-only — refuse a PR sink before dispatch."},
  "fz-sk-015": {"github":"     echo \"BLOCKED: issue_action: comment_keep_open is only supported on the merge sink. PR/MR sinks close via the merged PR; switch sink: merge or remove issue_action.\" >&2","gitlab":"     echo \"BLOCKED: issue_action: comment_keep_open is only supported on the merge sink. MR/PR sinks close via the merged MR; switch sink: merge or remove issue_action.\" >&2","gitea":"     echo \"BLOCKED: issue_action: comment_keep_open is only supported on the merge sink. PR sinks close via the merged PR; switch sink: merge or remove issue_action.\" >&2"},
  "fz-sk-016": {"github":"     pr)\n       node \"$scripts_dir/kaola-workflow-sink-pr.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project \"$KAOLA_PROJECT\"","gitlab":"     mr|pr)\n       node \"$scripts_dir/kaola-gitlab-workflow-sink-mr.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project \"$KAOLA_PROJECT\"","gitea":"     mr|pr)\n       node \"$scripts_dir/kaola-gitea-workflow-sink-pr.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project \"$KAOLA_PROJECT\""},
  "fz-sk-017": {"github":"       node \"$scripts_dir/kaola-workflow-sink-merge.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG $SINK_ISSUE_NUMBERS_FLAG $SINK_KEEP_OPEN_FLAG --project \"$KAOLA_PROJECT\"","gitlab":"       node \"$scripts_dir/kaola-gitlab-workflow-sink-merge.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG $SINK_ISSUE_NUMBERS_FLAG $SINK_KEEP_OPEN_FLAG --project \"$KAOLA_PROJECT\"","gitea":"       node \"$scripts_dir/kaola-gitea-workflow-sink-merge.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG $SINK_ISSUE_NUMBERS_FLAG $SINK_KEEP_OPEN_FLAG --project \"$KAOLA_PROJECT\""},
  "fz-sk-018": {"github":"         # keep-open is merge-sink-only — never auto-pivot to a PR sink (its Closes #N body\n         # would close the kept-open issue; watch-pr would delete the preserved roadmap source).","gitlab":"         # keep-open is merge-sink-only — never auto-pivot to an MR sink (its Closes #N body\n         # would close the kept-open issue; watch-mr would delete the preserved roadmap source).","gitea":"         # keep-open is merge-sink-only — never auto-pivot to a PR sink (its Closes #N body\n         # would close the kept-open issue; watch-pr would delete the preserved roadmap source)."},
  "fz-sk-019": {"github":"           echo \"BLOCKED: sink-merge exited 3 (merge-impossible) on a keep-open run. Keep-open is merge-sink-only: the PR fallback body closes the issue on merge and watch-pr would delete the preserved roadmap source. Remediate the merge blocker (see .cache/sink-fallback.json) and re-run sink-merge; do not pivot to a PR sink.\" >&2","gitlab":"           echo \"BLOCKED: sink-merge exited 3 (merge-impossible) on a keep-open run. Keep-open is merge-sink-only: the MR fallback body closes the issue on merge and watch-mr would delete the preserved roadmap source. Remediate the merge blocker (see .cache/sink-fallback.json) and re-run sink-merge; do not pivot to an MR sink.\" >&2","gitea":"           echo \"BLOCKED: sink-merge exited 3 (merge-impossible) on a keep-open run. Keep-open is merge-sink-only: the PR fallback body closes the issue on merge and watch-pr would delete the preserved roadmap source. Remediate the merge blocker (see .cache/sink-fallback.json) and re-run sink-merge; do not pivot to a PR sink.\" >&2"},
  "fz-sk-020": {"github":"         node \"$scripts_dir/kaola-workflow-claim.js\" sink-fallback \\","gitlab":"         node \"$scripts_dir/kaola-gitlab-workflow-claim.js\" sink-fallback \\","gitea":"         node \"$scripts_dir/kaola-gitea-workflow-claim.js\" sink-fallback \\"},
  "fz-sk-021": {"github":"         node \"$scripts_dir/kaola-workflow-sink-pr.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project \"$KAOLA_PROJECT\"","gitlab":"         node \"$scripts_dir/kaola-gitlab-workflow-sink-mr.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project \"$KAOLA_PROJECT\"","gitea":"         node \"$scripts_dir/kaola-gitea-workflow-sink-pr.js\" --branch \"$SINK_BRANCH\" $SINK_ISSUE_FLAG --project \"$KAOLA_PROJECT\""},
  "fz-sk-022": {"github":"   node \"$scripts_dir/kaola-workflow-sink-merge.js\" \\","gitlab":"   node \"$scripts_dir/kaola-gitlab-workflow-sink-merge.js\" \\","gitea":"   node \"$scripts_dir/kaola-gitea-workflow-sink-merge.js\" \\"},
  "fz-sk-023": {"github":"  (`gh issue close N`) or resolve the forge fault, then re-run `--sink`.","gitlab":"  (`glab issue close N`) or resolve the forge fault, then re-run `--sink`.","gitea":"  (`tea issue close N`) or resolve the forge fault, then re-run `--sink`."},
  "fz-sk-024": {"github":"stale roadmap source, or an un-archived merged-PR folder that escaped the inline catch.","gitlab":"stale roadmap source, or an un-archived merged-MR folder that escaped the inline catch.","gitea":"stale roadmap source, or an un-archived merged-PR folder that escaped the inline catch."},
  "fz-sk-025": {"github":"node \"$scripts_dir/kaola-workflow-closure-audit.js\"            # dry-run: JSON report (default)\n# node \"$scripts_dir/kaola-workflow-closure-audit.js\" --execute  # repair safe local drift","gitlab":"node \"$scripts_dir/kaola-gitlab-workflow-closure-audit.js\"            # dry-run: JSON report (default)\n# node \"$scripts_dir/kaola-gitlab-workflow-closure-audit.js\" --execute  # repair safe local drift","gitea":"node \"$scripts_dir/kaola-gitea-workflow-closure-audit.js\"            # dry-run: JSON report (default)\n# node \"$scripts_dir/kaola-gitea-workflow-closure-audit.js\" --execute  # repair safe local drift"},
  // ---- adapt: command/skill + forge-noun substitutions (3-way, per-forge).
  // Machine-derived the same way as the init and finalize families above. The
  // adapt lane needs no `-shared-` splice: every forge-varying line lands inside
  // a REGION:command or REGION:skill, and the one line whose only forge
  // difference is the adaptive-node basename is a plain skeleton literal that
  // rename-table.js renames on the way out.
  "ad-cmd-001": {"github":"plan, shaping, and the `kaola-workflow-claim.js startup` / `Write` /\n`kaola-workflow-adaptive-handoff.js` literals) lives exclusively in `agents/workflow-planner.md`; this","gitlab":"plan, shaping, and the `kaola-gitlab-workflow-claim.js startup` / `Write` /\n`kaola-gitlab-workflow-adaptive-handoff.js` literals) lives exclusively in `agents/workflow-planner.md`; this","gitea":"plan, shaping, and the `kaola-gitea-workflow-claim.js startup` / `Write` /\n`kaola-gitea-workflow-adaptive-handoff.js` literals) lives exclusively in `agents/workflow-planner.md`; this"},
  "ad-cmd-002": {"github":"REPLAN_SCRIPT=\"./scripts/kaola-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/kaola-workflow-replan.js}\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"$HOME/.claude/kaola-workflow/scripts/kaola-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-workflow-replan.js unavailable\" >&2; exit 1; }","gitlab":"REPLAN_SCRIPT=\"./plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/kaola-gitlab-workflow-replan.js}\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"$HOME/.claude/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitlab-workflow-replan.js unavailable\" >&2; exit 1; }","gitea":"REPLAN_SCRIPT=\"./plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/kaola-gitea-workflow-replan.js}\"\n[ -f \"$REPLAN_SCRIPT\" ] || REPLAN_SCRIPT=\"$HOME/.claude/kaola-workflow-gitea/scripts/kaola-gitea-workflow-replan.js\"\n[ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitea-workflow-replan.js unavailable\" >&2; exit 1; }"},
  "ad-sk-001": {"github":"REPLAN_SCRIPT=\"plugins/kaola-workflow/scripts/kaola-workflow-replan.js\"","gitlab":"REPLAN_SCRIPT=\"plugins/kaola-workflow-gitlab/scripts/kaola-gitlab-workflow-replan.js\"","gitea":"REPLAN_SCRIPT=\"plugins/kaola-workflow-gitea/scripts/kaola-gitea-workflow-replan.js\""},
  "ad-sk-002": {"github":"  REPLAN_SCRIPT=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow/*/scripts/kaola-workflow-replan.js' -print -quit 2>/dev/null)\"","gitlab":"  REPLAN_SCRIPT=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitlab/*/scripts/kaola-gitlab-workflow-replan.js' -print -quit 2>/dev/null)\"","gitea":"  REPLAN_SCRIPT=\"$(find \"$HOME/.codex/plugins/cache\" -path '*/kaola-workflow-gitea/*/scripts/kaola-gitea-workflow-replan.js' -print -quit 2>/dev/null)\""},
  "ad-sk-003": {"github":"[ -n \"$REPLAN_SCRIPT\" ] && [ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-workflow-replan.js unavailable\" >&2; exit 1; }","gitlab":"[ -n \"$REPLAN_SCRIPT\" ] && [ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitlab-workflow-replan.js unavailable\" >&2; exit 1; }","gitea":"[ -n \"$REPLAN_SCRIPT\" ] && [ -f \"$REPLAN_SCRIPT\" ] || { echo \"BLOCKED: kaola-gitea-workflow-replan.js unavailable\" >&2; exit 1; }"},
  "ad-cmd-003": {"github":"Author a `workflow-plan.md` whose `## Nodes` table passes `kaola-workflow-plan-validator.js`, freeze","gitlab":"Author a `workflow-plan.md` whose `## Nodes` table passes `kaola-gitlab-workflow-plan-validator.js`, freeze","gitea":"Author a `workflow-plan.md` whose `## Nodes` table passes `kaola-gitea-workflow-plan-validator.js`, freeze"},
  "ad-cmd-004": {"github":"(`kaola-workflow-claim.js discard --project {project}` abandons it).","gitlab":"(`kaola-gitlab-workflow-claim.js discard --project {project}` abandons it).","gitea":"(`kaola-gitea-workflow-claim.js discard --project {project}` abandons it)."},
  "ad-sk-004": {"github":"leaves a **resumable** project; `kaola-workflow-claim.js discard --project","gitlab":"leaves a **resumable** project; `kaola-gitlab-workflow-claim.js discard --project","gitea":"leaves a **resumable** project; `kaola-gitea-workflow-claim.js discard --project"},
  "ad-cmd-005": {"github":"(`node \"$(kaola_script kaola-workflow-claim.js)\" authoring-allowed`; always `authoring_allowed: true`,","gitlab":"(`node \"$(kaola_script kaola-gitlab-workflow-claim.js)\" authoring-allowed`; always `authoring_allowed: true`,","gitea":"(`node \"$(kaola_script kaola-gitea-workflow-claim.js)\" authoring-allowed`; always `authoring_allowed: true`,"},
  "ad-sk-005": {"github":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow/scripts/$_n\" \"./scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nnode \"$(kaola_script kaola-workflow-claim.js)\" authoring-allowed","gitlab":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitlab/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitlab/scripts/$_n\" \"./plugins/kaola-workflow-gitlab/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nnode \"$(kaola_script kaola-gitlab-workflow-claim.js)\" authoring-allowed","gitea":"kaola_script(){ _n=\"$1\"; _self=\"\"; [ -f \"./package.json\" ] && _self=\"$(node -e \"try{process.stdout.write(require(process.cwd()+'/package.json').name||'')}catch(e){}\" 2>/dev/null)\"; if [ \"$_self\" = \"kaola-workflow\" ]; then for _p in \"./plugins/kaola-workflow-gitea/scripts/$_n\" \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; else for _p in \"${CLAUDE_PLUGIN_ROOT:+$CLAUDE_PLUGIN_ROOT/scripts/$_n}\" \"$HOME/.claude/kaola-workflow-gitea/scripts/$_n\" \"./plugins/kaola-workflow-gitea/scripts/$_n\"; do [ -f \"$_p\" ] && { printf '%s\\n' \"$_p\"; return; }; done; fi; return 1; }\nnode \"$(kaola_script kaola-gitea-workflow-claim.js)\" authoring-allowed"},
  "ad-sk-006": {"github":"Once main is clean, **delegate to the `workflow-planner`**: it runs `kaola-workflow-claim.js startup --runtime <runtime>","gitlab":"Once main is clean, **delegate to the `workflow-planner`**: it runs `kaola-gitlab-workflow-claim.js startup --runtime <runtime>","gitea":"Once main is clean, **delegate to the `workflow-planner`**: it runs `kaola-gitea-workflow-claim.js startup --runtime <runtime>"},
  "ad-sk-007": {"github":"dispatched workflow-planner passes it; add `--sink pr` only for a requested PR sink), authors the","gitlab":"dispatched workflow-planner passes it; add `--sink mr` only for a requested MR sink), authors the","gitea":"dispatched workflow-planner passes it; add `--sink pr` only for a requested PR sink), authors the"},
  "ad-sk-008": {"github":"as a self-check (NOT `--freeze`, NOT `authoring-allowed`), then RUNS `kaola-workflow-adaptive-handoff.js --project {project} --json` (freezes, resume-checks, stages roadmap, writes Planning Evidence; does NOT open node1 or record the node1 baseline — `kaola-workflow-plan-run` owns the full node lifecycle including the first node; decision:ask is recorded metadata, not a gate), and RETURNS the handoff packet. It never JUDGES risk or asks the user (decision:ask is recorded metadata); it RUNS the handoff, which freezes mechanically, and returns the packet; it never dispatches. If the project already has a","gitlab":"as a self-check (NOT `--freeze`, NOT `authoring-allowed`), then RUNS `kaola-gitlab-workflow-adaptive-handoff.js --project {project} --json` (freezes, resume-checks, stages roadmap, writes Planning Evidence; does NOT open node1 or record the node1 baseline — `kaola-workflow-plan-run` owns the full node lifecycle including the first node; decision:ask is recorded metadata, not a gate), and RETURNS the handoff packet. It never JUDGES risk or asks the user (decision:ask is recorded metadata); it RUNS the handoff, which freezes mechanically, and returns the packet; it never dispatches. If the project already has a","gitea":"as a self-check (NOT `--freeze`, NOT `authoring-allowed`), then RUNS `kaola-gitea-workflow-adaptive-handoff.js --project {project} --json` (freezes, resume-checks, stages roadmap, writes Planning Evidence; does NOT open node1 or record the node1 baseline — `kaola-workflow-plan-run` owns the full node lifecycle including the first node; decision:ask is recorded metadata, not a gate), and RETURNS the handoff packet. It never JUDGES risk or asks the user (decision:ask is recorded metadata); it RUNS the handoff, which freezes mechanically, and returns the packet; it never dispatches. If the project already has a"},
  "ad-cmd-006": {"github":"The planner RAN `kaola-workflow-adaptive-handoff.js` and returned a checklist-backed packet (plan","gitlab":"The planner RAN `kaola-gitlab-workflow-adaptive-handoff.js` and returned a checklist-backed packet (plan","gitea":"The planner RAN `kaola-gitea-workflow-adaptive-handoff.js` and returned a checklist-backed packet (plan"},
  "ad-cmd-007": {"github":"  fresh adaptive run** (`kaola-workflow-claim.js discard --project {project}`) or **STOP + surface a","gitlab":"  fresh adaptive run** (`kaola-gitlab-workflow-claim.js discard --project {project}`) or **STOP + surface a","gitea":"  fresh adaptive run** (`kaola-gitea-workflow-claim.js discard --project {project}`) or **STOP + surface a"},
  "ad-sk-009": {"github":"**Read the handoff packet.** The planner RAN `kaola-workflow-adaptive-handoff.js` and returned a checklist-backed packet (plan already frozen, Planning Evidence written; the handoff does NOT open node1 or record the node1 baseline — `kaola-workflow-plan-run` owns the full node lifecycle including the first node). The handoff is mechanical; `decision:ask` is audit metadata only — it freezes-and-proceeds, NEVER pauses for approval.","gitlab":"**Read the handoff packet.** The planner RAN `kaola-gitlab-workflow-adaptive-handoff.js` and returned a checklist-backed packet (plan already frozen, Planning Evidence written; the handoff does NOT open node1 or record the node1 baseline — `kaola-workflow-plan-run` owns the full node lifecycle including the first node). The handoff is mechanical; `decision:ask` is audit metadata only — it freezes-and-proceeds, NEVER pauses for approval.","gitea":"**Read the handoff packet.** The planner RAN `kaola-gitea-workflow-adaptive-handoff.js` and returned a checklist-backed packet (plan already frozen, Planning Evidence written; the handoff does NOT open node1 or record the node1 baseline — `kaola-workflow-plan-run` owns the full node lifecycle including the first node). The handoff is mechanical; `decision:ask` is audit metadata only — it freezes-and-proceeds, NEVER pauses for approval."},
  "ad-sk-010": {"github":"- **`handoff_status: plan_invalid`** (validator refused; the plan never froze and `workflow-state.md` is untouched — the one write is the `.cache/acceptance-anchor.json` audit record) → bounded **repair loop**: re-dispatch the `workflow-planner` with the verbatim `errors`/`validator_verdict` so it overwrites the UNFROZEN plan with a corrected DAG and re-runs the handoff. Repair may fix `## Meta` / `## Nodes` / `## Node Briefs` / ledger scaffolding to reach in-grammar but MUST NOT alter `## Design` (the frozen decomposition intent) or `## Acceptance` (the human-values statement of what done means, which is hard-fenced) — if in-grammar is unreachable without changing either, that is not repair. Retry ~2x (counter in the orchestrator, never in the script). After repeated failure (~2x) → real decision: **discard+restart a fresh adaptive run** (`kaola-workflow-claim.js discard --project {project}` then a fresh adaptive start) / **STOP + surface a concrete blocker** with validator evidence. This fallback applies only to normal startup while the draft is unfrozen; it is forbidden under `replan_in_progress`. The only fallbacks are inside adaptive (bounded repair, in-place posture). Never silently loop.","gitlab":"- **`handoff_status: plan_invalid`** (validator refused; the plan never froze and `workflow-state.md` is untouched — the one write is the `.cache/acceptance-anchor.json` audit record) → bounded **repair loop**: re-dispatch the `workflow-planner` with the verbatim `errors`/`validator_verdict` so it overwrites the UNFROZEN plan with a corrected DAG and re-runs the handoff. Repair may fix `## Meta` / `## Nodes` / `## Node Briefs` / ledger scaffolding to reach in-grammar but MUST NOT alter `## Design` (the frozen decomposition intent) or `## Acceptance` (the human-values statement of what done means, which is hard-fenced) — if in-grammar is unreachable without changing either, that is not repair. Retry ~2x (counter in the orchestrator, never in the script). After repeated failure (~2x) → real decision: **discard+restart a fresh adaptive run** (`kaola-gitlab-workflow-claim.js discard --project {project}` then a fresh adaptive start) / **STOP + surface a concrete blocker** with validator evidence. This fallback applies only to normal startup while the draft is unfrozen; it is forbidden under `replan_in_progress`. The only fallbacks are inside adaptive (bounded repair, in-place posture). Never silently loop.","gitea":"- **`handoff_status: plan_invalid`** (validator refused; the plan never froze and `workflow-state.md` is untouched — the one write is the `.cache/acceptance-anchor.json` audit record) → bounded **repair loop**: re-dispatch the `workflow-planner` with the verbatim `errors`/`validator_verdict` so it overwrites the UNFROZEN plan with a corrected DAG and re-runs the handoff. Repair may fix `## Meta` / `## Nodes` / `## Node Briefs` / ledger scaffolding to reach in-grammar but MUST NOT alter `## Design` (the frozen decomposition intent) or `## Acceptance` (the human-values statement of what done means, which is hard-fenced) — if in-grammar is unreachable without changing either, that is not repair. Retry ~2x (counter in the orchestrator, never in the script). After repeated failure (~2x) → real decision: **discard+restart a fresh adaptive run** (`kaola-gitea-workflow-claim.js discard --project {project}` then a fresh adaptive start) / **STOP + surface a concrete blocker** with validator evidence. This fallback applies only to normal startup while the draft is unfrozen; it is forbidden under `replan_in_progress`. The only fallbacks are inside adaptive (bounded repair, in-place posture). Never silently loop."},
};

module.exports = { SLOTS, SPLICES };
